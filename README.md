# CodeRun V2 — Developer Growth Simulator

> **실행하며 배우는 개발 문제집 + 실무 판단 시뮬레이터.** SQL·Python·JS를 실제로 실행해 채점하고, 장애·설계 결정을 시뮬레이션합니다.
> 정직한 포지셔닝: *이 앱만으로 시니어가 되지는 않습니다.* 시니어는 실전(제품을 만들고·배포하고·운영하며)에서 만들어집니다. 이 앱은 그 여정의 **부교재** — 지식 공백을 메우고 실무 판단의 감을 잡는 도구로, 실전과 병행할 때 가치가 있습니다.

단일 HTML 파일(`index.html`)로 동작하는 오프라인 PWA. 서버·빌드·설치 없이 브라우저에서 바로 실행되며, 모든 상태는 `localStorage`에 저장됩니다.

```
입문  →  Junior  →  Mid  →  Senior  →  Staff  →  Principal
```

---

## 무엇이 들어있나

| 영역 | 규모 | 설명 |
|---|---|---|
| **지식 엔진** | 32트랙 · 7,409문항 | Python 705 / SQL 572 중심. choice·input·code(라이브 실행·테스트 통과) 문제 |
| **실무 디버깅 챌린지** | 124개 (검증) | 버그난 코드를 프리필 → 고쳐서 실제 테스트 통과 + 원인/해결/재발방지. **모든 문항 엔진으로 기계 검증**(정답은 통과·버그는 실패). Python 56 · SQL 48 · JavaScript 20 |
| **직접 구현 (실무 함수/쿼리)** | 54개 (검증) | 스텁에서 함수/쿼리를 구현 → 실제 실행 채점. 참조 정답을 엔진으로 돌려 전량 검증. Python 18 · SQL 16 · JavaScript 20 |
| **코드 리뷰 (결함 찾기)** | 107개 (검증) | 코드에서 실제 결함을 **모두** 골라내기(정상·오탐 distractor 섞임), 집합 일치로 채점. 백엔드·보안 14 · JS·React 14 · 파이썬 15 · SQL 16, **Java 12 · C 12 · C++ 12 · Go 12**. 실행 엔진이 없어 읽기 전용이던 4개 언어 트랙에 처음으로 비선택형 문항이 들어감 — equals/hashCode 계약·자원 누수·동시성(Java), 버퍼 경계·수명·미정의 동작(C), 소유권·반복자 무효화·rule of three(C++), goroutine 누수·에러 은닉·슬라이스 별칭(Go) |
| **분야 소개(시작 전에)** | **32트랙 전체** | 트랙을 처음 열면 먼저 보여주는 안내 — 무엇인가 / 왜 만들어졌는가 / 어디에 쓰이는가 / 대표 서비스·기업 / 장점·단점 / 비슷한 것과의 차이 / 이런 직무에서 쓴다 / 평균 난이도 / 학습 예상 시간 / **마치면 할 수 있는 것**. 언제든 트랙 상단 배너로 다시 열림 |
| **인출 모드** | 선택형 5,690문항 전체 | 보기를 먼저 감추고 **직접 떠올려 적게** 한다. 재인(알아보기)과 인출(꺼내기)은 다른 능력이고 실무에 쓰이는 건 후자. 채점은 보수적 — 정답을 적으면 91% 인정, **오답을 인정하는 경우는 0%**. 못 맞히면 보기가 열리고 평소대로 진행. 역량 근거에 "보기 없이 스스로 답한 비율"이 별도로 표시됨 |
| **난이도 5단계 태그** | 전 7,409문항 | 모든 문제에 🟢입문·🔵기초·🟡응용·🟠실무·🔴시니어 배지. 유닛 위치(재정렬된 난이도 순)와 문제 유형(직접구현·디버깅·리뷰·내부동작)으로 산출 |
| **오늘의 추천(약점 엔진)** | — | 홈 상단에서 다음 할 일을 하나로 지정 — 복습할 오답이 쌓였으면 복습 우선, 아니면 9축 중 가장 낮은 역량을 지목하고("오늘은 시스템 설계를 학습하세요") 해당 트랙·시뮬로 바로 이동 |
| **학습 코치** | — | 내 기록만으로 진단 — 최근 14일 학습량 그래프, 전체·최근 정답률, **난이도별·유형별 정답률**, 그리고 규칙 기반 코치 브리핑("난이도 시니어 구간 정답률이 43%로 가장 낮습니다", "객관식은 충분한데 직접 코딩 통과가 1개입니다"). 서버 없이 localStorage 통계만 사용 |
| **일일 · 주간 미션** | 매일 3 · 매주 4 | 오늘: 목표 문항 · 복습 비우기(없으면 레슨 2개) · 직접 코딩 1개. 이번 주: 레슨 12 · 직접구현/디버깅 5 · 시뮬/원인추적 3 · 문항 120. 주간 달성 시 XP 보상(중복 지급 없음) |
| **업적 · 활동 랭크** | 업적 34 · 랭크 7단계 | 첫 걸음부터 Principal 달성까지 34개 업적, 획득 시 토스트. 활동 랭크(브론즈→그랜드마스터)는 누적 XP 기반으로 **실력 등급(9축)과 분리** |
| **Git 시뮬레이터** | 미션 12개 | 브라우저에서 **실제로 동작하는 Git** — commit·branch·merge(3-way·충돌)·rebase(충돌·continue·abort)·reset(soft/mixed/hard)·revert·cherry-pick·stash·push/fetch/pull(non-fast-forward 거절)·tag·diff. 충돌은 진짜로 나고 직접 해결해야 통과. 미션마다 "언제 이 명령을 쓰는가"(revert vs reset, rebase 금지 조건) 설명 |
| **로그 분석** | 4유닛 · 40문항 | 실제 장애 로그에서 **근본 원인을 가리키는 줄**만 골라내기 — 결과로 따라온 증상 줄(500 폭증·502·OOM 등)이 distractor. 웹·API 장애 / 데이터베이스 / 런타임·리소스 / 배포·인프라·보안. 채점 후 각 줄에 "원인 / 증상·정상" 표시 |
| **개념(이론) 먼저** | 523유닛 · **1,401레슨 전체** | 모든 레슨에서 문제 전에 개념 프라이머(요약·핵심 설명·예시 코드·기억할 점) → "이해했어요 · 문제 풀기". 디버깅·직접구현·코드리뷰 유닛에는 방법론 프라이머(디버깅 절차, 구현 계약, 리뷰 체크리스트) |
| **기술 면접 대비** | 10유닛 · 180문항 (검증) | 백엔드 면접(DB·API·인증·성능·경험 답변법) + 설계 면접(진행법·규모추정·대표문제·트레이드오프·운영). 유닛마다 "면접관이 보는 것 / 답변 구조" 프라이머 |
| **Real Work 시뮬레이션** | 172개 | 장애·리뷰·기술선택·이해관계자·프로덕트·윤리 분기형 의사결정 (Wave 3: +47, 각 단계 best 판정을 적대적 검토로 확인) |
| **원인 추적(진단)** | 42개 | 불완전한 정보 + 단서(일부 red herring)로 근본원인 규명 (Wave 3: +16, 단서→원인 정합성 검토) |
| **프로젝트 워크스페이스** | 34개 | 8단계 라이프사이클(요구사항→설계→개발→테스트→배포→운영→장애→Postmortem), 연쇄 장애 시나리오. Staff급 4개 추가(500만 사용자 리팩토링·MSA 전환 판단·비용 40% 절감·무중단 마이그레이션) |
| **성장 로드맵(마일스톤 프로그램)** | 5단계 | 입문→Junior→Mid→Senior→Staff→Principal. 레벨은 점수가 아니라 **실제로 해낸 마일스톤**(지식·직접 코딩·판단·빌드)으로 상승 — 조건을 모두 채워야 다음 단계 잠금 해제 |
| **9축 평가 엔진** | — | coding·debugging·algorithms·database·system_design·performance·security·communication·leadership |
| **Developer Passport** | — | SVG 레이더 차트 + 역할 티어(Beginner→Principal) |

### V1과 무엇이 다른가
V1은 "코딩 문제를 맞추는" 앱이었습니다. V2는 장애 원인 추적, DevOps(SLO/MTTR/Incident Command/Runbook), Postmortem, 설계 판단을 넣어 **개발자 성장 훈련 제품**이 되었습니다.

---

## 실행

```bash
# 그냥 파일을 브라우저로 열면 됩니다. 서버 불필요.
open index.html          # macOS
xdg-open index.html      # Linux
```

의존성 0개. 네트워크 0개. 계정 0개. 전부 클라이언트에서 동작합니다.

---

## 트랙별 문항 수

**주력** Python 705 · SQL 572 · 백엔드 167 · 시스템 설계 145 · Git/DevOps 134 · 보안 110 · 알고리즘 67
**프론트엔드** JavaScript 452 (런타임·이벤트루프·V8·객체/이터레이터/정규식/DOM/Web API) · React 386 (렌더링·훅·key·Context·Suspense·라우팅·테스트·접근성·파생상태·SSR) · HTML/CSS 158
**언어** Java 478 (record·sealed·java.time·NIO·JUnit·빌드·equals 계약·enum·리플렉션·직렬화·성능) · C 423 (비트·자료구조 구현·분할컴파일·메모리 레이아웃·가변인자·이식성) · C++ 408 (STL 알고리즘·예외 안전성·string_view·현대 문법·동시성·캐스팅·초기화 순서) · Go 391 (리시버·임베딩·제네릭·동시성 패턴·HTTP·성능·모듈·타입시스템·database/sql)
**데이터/AI** AI 엔지니어링 50 (서빙·RAG·MLOps) · Pandas 45 · NumPy 45 · 통계 45 · 머신러닝 45 · ML평가 45 · 딥러닝 45
**CS** OS 31 · 네트워크 21 · DB내부 20 · 그 외
**운영** Git/DevOps 174 (로그 분석 40 포함)

> **심화 유닛(시니어/스태프 난이도)**: 12개 핵심 트랙에 각 ~22문항의 "심화 — 시니어/스태프 난이도" 유닛 추가(+255) — 시스템 설계·백엔드·알고리즘·보안·네트워크·OS·DB내부·JavaScript·React·AI·Java·DevOps. 정의 암기가 아니라 트레이드오프·내부 동작·실무 함정·엣지케이스 중심으로, 그럴듯한 오답(distractor)을 배치.

> **정답 정확성 감사(핵심 9트랙 1,901문항)**: Python·SQL·JavaScript·Java·알고리즘·C·C++·Go·React의 모든 choice/input 문항을 독립 검토 에이전트가 재검증 — 특히 코드 출력형 517문항은 **코드를 실제로 함께 보며** 재실행 검증. 결과 99.9% 무결, 실제 결함 1건(텍스트 월 정렬을 오해한 SQL `LAG` 문항)만 제거. (심화 255 + 시니어실전 72는 별도 생성 시 이미 검증)

> **시니어 실전 유닛(+72, 적대적 검증 통과)**: 프로그램의 시니어/스태프 구간을 실제로 뒷받침하도록 3개 핵심 트랙에 최심화 유닛 추가 — 시스템 설계(분산·일관성·quorum·합의·LSM·백프레셔), 백엔드(트랜잭션 격리 이상현상·MVCC·인덱스 sargability·멱등성·전달 의미론), 앱보안(XSS/CSRF/SQLi/SSRF 올바른 방어·JWT/OAuth 함정·암호 모드). 생성 후 별도 검토 에이전트가 정답을 독립 재검증 — 72문항 전부 무결(0 flagged).

> 직접 코딩(테스트 통과형): 함수를 구현하면 여러 테스트 케이스로 자동 채점(Implementation Score: 정확성·엣지·성능·품질) — 코딩 실습·알고리즘 트랙.
> 직접 SQL: `sql.js`(SQLite WASM 인라인)로 브라우저에서 실제 쿼리를 실행해 결과셋을 정답과 비교 채점 — SQL 트랙.
> 직접 Python: `Pyodide`로 브라우저에서 실제 파이썬을 실행해 함수 구현을 테스트 케이스로 채점(Implementation Score) — Python 트랙(직접 코딩 유닛). 배포(온라인)에서 동작, 오프라인은 안내 후 폴백.
> 에디터: 코드/SQL/Python 편집기는 **Monaco(VS Code 엔진)** 로 문법 강조·자동완성 제공(CDN 로드). 로컬·차단 환경에선 자동으로 일반 textarea로 폴백.
> **AI 코드 멘토(V4)**: 직접 코딩·SQL 문제를 푼 뒤 "AI 시니어 리뷰"로 정확성·엣지·성능·가독성·프로덕션 관점 피드백. **BYOK·모델 무관**(Gemini 기본, OpenAI·Claude 선택 가능) — 키는 이 브라우저에만 저장되고 서버를 거치지 않습니다. 로컬에서 `index.html`을 직접 열었을 때 동작합니다.
> **백업/복원**: 진행상황을 JSON 파일로 내보내고 다시 불러올 수 있어요(기기 이동·백업).

---

## 문서

- [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) — V2 내부 구조(데이터 모델·엔진·렌더링·상태)
- [`docs/V3_DESIGN.md`](docs/V3_DESIGN.md) — V3 설계도(DB 스키마·API·클라우드 동기화)

---

## 버전 로드맵

| 버전 | 정체성 | 상태 |
|---|---|---|
| V1 | 코딩 문제 앱 | ✅ |
| **V2.x** | **개발자 훈련 시뮬레이터** (제품급 UI·직접 코딩·SQL 실행·성장 루프) | ✅ **현재** |
| V4(부분) | AI 코드 멘토 — **BYOK 클라이언트**로 실현 (Gemini/OpenAI/Claude 직접 호출) | ✅ 앱 내 동작 |
| V3 | 개인 개발자 성장 플랫폼 (계정·클라우드 저장·성장 분석) | 📐 설계 단계 (별도 백엔드) |
| V4(서버) | 서버 프록시 AI 멘토링 (키 은닉·사용량 관리·심층 리뷰) | 🔮 백엔드 이후 |

> V4의 AI 리뷰는 **평가 기준과 문제 구조가 먼저 존재**하는 CodeRun 위에 얹혀, 오프라인 단일 파일 철학을 지키며 BYOK로 먼저 실현됐습니다. 키 은닉·사용량 관리가 필요한 서버 프록시 형태는 V3 백엔드 이후로 둡니다.
