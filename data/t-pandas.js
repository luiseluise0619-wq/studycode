__CR('t:pandas',[{"t":"Series와 DataFrame","l":[{"t":"Series 기초","q":[{"t":"choice","k":"기본 index","q":"pd.Series([10, 20, 30])을 만들면 기본 index는?","code":"import pandas as pd\ns = pd.Series([10, 20, 30])\nprint(s.index.tolist())","o":["[0, 1, 2]","[1, 2, 3]","['a', 'b', 'c']","[10, 20, 30]"],"a":0,"ex":"index를 지정하지 않으면 0부터 시작하는 정수 RangeIndex(0, 1, 2)가 자동으로 붙습니다."},{"t":"input","k":"생성 함수","q":"리스트로 1차원 Series를 만드는 pandas 함수 이름은? (pd. 뒤 부분)","a":["Series","pd.Series"],"ex":"pd.Series(data)로 라벨(index)이 붙은 1차원 배열을 만듭니다. 대문자 S에 주의하세요."},{"t":"choice","k":"라벨 접근","q":"아래 코드의 출력은?","code":"s = pd.Series([1, 2, 3], index=['a', 'b', 'c'])\nprint(s['b'])","o":["1","2","3","'b'"],"a":1,"ex":"index를 ['a','b','c']로 지정했으므로 s['b']는 라벨 'b'에 해당하는 값 2를 반환합니다."},{"t":"choice","k":"Series vs list","q":"Python list와 비교했을 때 Series의 핵심 차이는?","o":["각 값에 index(라벨)가 붙어 라벨로 접근할 수 있다","값을 절대 변경할 수 없다","숫자만 저장할 수 있다","정렬이 항상 자동으로 된다"],"a":0,"ex":"Series는 값 배열에 index가 결합된 구조라서 위치뿐 아니라 라벨로도 값을 다룰 수 있고, 연산 시 index가 정렬 기준이 됩니다."},{"t":"choice","k":"values 타입","q":"s.values가 반환하는 것은?","code":"s = pd.Series([1, 2, 3])\nprint(type(s.values).__name__)","o":["list","ndarray","Series","dict"],"a":1,"ex":"Series의 내부 값은 NumPy 배열(ndarray)로 저장되며 s.values로 그 배열을 얻을 수 있습니다."}],"th":{"sum":"**Series**는 인덱스를 가진 1차원 배열, **DataFrame**은 여러 Series를 열로 묶은 2차원 표다. 둘 다 자동으로 정수 인덱스를 부여받는다.","body":[{"h":"Series와 index","t":"`pd.Series([10,20,30])`처럼 리스트로 만들면 **RangeIndex** 0,1,2가 기본으로 붙는다. 파이썬 list와 달리 각 원소에 라벨(index)이 붙고, dtype이 하나로 통일되며, 벡터화 연산과 결측치(NaN) 처리를 지원한다. `s.values`는 내부 NumPy 배열(ndarray)을 반환한다."},{"h":"DataFrame 구조","t":"`dict`로 DataFrame을 만들면 dict의 **key가 열 이름(column)**, value 리스트가 각 열의 값이 된다. `df.shape`은 `(행 수, 열 수)` 튜플을 반환하고, `df['col']`은 해당 열을 Series로 꺼낸다."}],"code":{"c":"import pandas as pd\ndf = pd.DataFrame({'name': ['A','B'], 'age': [30, 25]})\nprint(df.shape)      # (2, 2)\nprint(df['age'].values)  # [30 25]","cap":"dict의 key가 열 이름이 되고 shape은 (행,열)"},"key":["Series는 라벨(index)을 가진 1차원, DataFrame은 2차원 표","리스트로 만들면 index는 0부터 자동 부여","dict → DataFrame에서 key는 열 이름","`.values`는 NumPy ndarray, `.shape`은 (행,열) 튜플"]}},{"t":"DataFrame 생성과 구조","q":[{"t":"choice","k":"dict로 생성","q":"dict로 DataFrame을 만들 때 dict의 key는 무엇이 되나?","code":"df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})","o":["행 index","열(column) 이름","값","데이터 타입"],"a":1,"ex":"dict의 key는 열 이름이 되고, 각 value 리스트가 그 열의 데이터가 됩니다."},{"t":"input","k":"shape 출력","q":"아래 df.shape의 출력을 튜플로 쓰면? (예: (2,2))","code":"df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})\nprint(df.shape)","a":["(3, 2)","(3,2)"],"ex":"shape는 (행 개수, 열 개수)입니다. 행 3개, 열 2개이므로 (3, 2)입니다."},{"t":"choice","k":"columns","q":"df.columns가 반환하는 값에 가장 가까운 것은?","code":"df = pd.DataFrame({'a': [1, 2], 'b': [3, 4]})\nprint(df.columns.tolist())","o":["['a', 'b']","[0, 1]","[1, 3]","['a', 'b', 'c']"],"a":0,"ex":"columns는 열 이름들의 Index 객체이며, tolist()로 보면 ['a', 'b']가 됩니다."},{"t":"choice","k":"기본 index","q":"index를 따로 지정하지 않은 DataFrame의 기본 행 index는?","o":["0부터 시작하는 RangeIndex","1부터 시작하는 정수","열 이름과 동일","무작위 정수"],"a":0,"ex":"index를 지정하지 않으면 0, 1, 2 ... 로 이어지는 RangeIndex가 행 라벨로 사용됩니다."},{"t":"input","k":"행 개수","q":"DataFrame df의 행 개수를 얻는 방법 하나를 쓰면? (함수 또는 속성 표현)","a":["len(df)","df.shape[0]"],"ex":"len(df)는 행 개수를 반환하고, df.shape[0]도 동일하게 행 개수를 줍니다."}],"th":{"sum":"**Series**는 인덱스를 가진 1차원 배열, **DataFrame**은 여러 Series를 열로 묶은 2차원 표다. 둘 다 자동으로 정수 인덱스를 부여받는다.","body":[{"h":"Series와 index","t":"`pd.Series([10,20,30])`처럼 리스트로 만들면 **RangeIndex** 0,1,2가 기본으로 붙는다. 파이썬 list와 달리 각 원소에 라벨(index)이 붙고, dtype이 하나로 통일되며, 벡터화 연산과 결측치(NaN) 처리를 지원한다. `s.values`는 내부 NumPy 배열(ndarray)을 반환한다."},{"h":"DataFrame 구조","t":"`dict`로 DataFrame을 만들면 dict의 **key가 열 이름(column)**, value 리스트가 각 열의 값이 된다. `df.shape`은 `(행 수, 열 수)` 튜플을 반환하고, `df['col']`은 해당 열을 Series로 꺼낸다."}],"code":{"c":"import pandas as pd\ndf = pd.DataFrame({'name': ['A','B'], 'age': [30, 25]})\nprint(df.shape)      # (2, 2)\nprint(df['age'].values)  # [30 25]","cap":"dict의 key가 열 이름이 되고 shape은 (행,열)"},"key":["Series는 라벨(index)을 가진 1차원, DataFrame은 2차원 표","리스트로 만들면 index는 0부터 자동 부여","dict → DataFrame에서 key는 열 이름","`.values`는 NumPy ndarray, `.shape`은 (행,열) 튜플"]}},{"t":"열 선택과 요약","q":[{"t":"choice","k":"열 선택 타입","q":"df['a']처럼 대괄호에 열 이름 하나를 넣으면 반환 타입은?","o":["Series","DataFrame","list","ndarray"],"a":0,"ex":"단일 열을 df['a']로 고르면 Series가 반환됩니다. df[['a']]처럼 리스트로 감싸면 DataFrame이 됩니다."},{"t":"input","k":"head 기본값","q":"df.head()는 기본적으로 위에서 몇 개의 행을 보여주나?","a":["5","5개"],"ex":"head()의 기본 인자는 5로, 상위 5개 행을 반환합니다. df.head(3)처럼 개수를 지정할 수도 있습니다."},{"t":"choice","k":"dtypes","q":"df.dtypes가 알려주는 정보는?","o":["각 열의 데이터 타입","각 행의 합계","결측치 개수","열의 평균값"],"a":0,"ex":"dtypes는 열마다의 자료형(int64, float64, object 등)을 보여줍니다."},{"t":"choice","k":"describe","q":"숫자형 열에 대한 df.describe()가 기본으로 포함하지 않는 통계는?","o":["최빈값(mode)","평균(mean)","표준편차(std)","사분위수(25%, 50%, 75%)"],"a":0,"ex":"describe()는 count, mean, std, min, 25%, 50%, 75%, max를 줍니다. 최빈값(mode)은 기본 출력에 없습니다."},{"t":"choice","k":"열 추가","q":"기존 두 열의 합으로 새 열 'c'를 만드는 올바른 코드는?","code":"df = pd.DataFrame({'a': [1, 2], 'b': [3, 4]})","o":["df['c'] = df['a'] + df['b']","df.c.append(df['a'])","df.add_column('c')","df['c'] == df['a'] + df['b']"],"a":0,"ex":"df['새이름'] = 식 형태로 대입하면 벡터 연산 결과가 새 열로 추가됩니다. 여기서 c는 [4, 6]이 됩니다."}],"th":{"sum":"**Series**는 인덱스를 가진 1차원 배열, **DataFrame**은 여러 Series를 열로 묶은 2차원 표다. 둘 다 자동으로 정수 인덱스를 부여받는다.","body":[{"h":"Series와 index","t":"`pd.Series([10,20,30])`처럼 리스트로 만들면 **RangeIndex** 0,1,2가 기본으로 붙는다. 파이썬 list와 달리 각 원소에 라벨(index)이 붙고, dtype이 하나로 통일되며, 벡터화 연산과 결측치(NaN) 처리를 지원한다. `s.values`는 내부 NumPy 배열(ndarray)을 반환한다."},{"h":"DataFrame 구조","t":"`dict`로 DataFrame을 만들면 dict의 **key가 열 이름(column)**, value 리스트가 각 열의 값이 된다. `df.shape`은 `(행 수, 열 수)` 튜플을 반환하고, `df['col']`은 해당 열을 Series로 꺼낸다."}],"code":{"c":"import pandas as pd\ndf = pd.DataFrame({'name': ['A','B'], 'age': [30, 25]})\nprint(df.shape)      # (2, 2)\nprint(df['age'].values)  # [30 25]","cap":"dict의 key가 열 이름이 되고 shape은 (행,열)"},"key":["Series는 라벨(index)을 가진 1차원, DataFrame은 2차원 표","리스트로 만들면 index는 0부터 자동 부여","dict → DataFrame에서 key는 열 이름","`.values`는 NumPy ndarray, `.shape`은 (행,열) 튜플"]}}]},{"t":"선택과 필터링","l":[{"t":"loc와 iloc","q":[{"t":"choice","k":"loc vs iloc","q":"loc와 iloc의 차이로 옳은 것은?","o":["loc는 라벨(이름) 기반, iloc는 정수 위치 기반","loc는 정수 위치 기반, iloc는 라벨 기반","둘 다 항상 같은 결과를 준다","loc는 열만, iloc는 행만 선택한다"],"a":0,"ex":"loc는 index/열 라벨로, iloc는 0부터 세는 정수 위치로 선택합니다."},{"t":"choice","k":"iloc 행","q":"df.iloc[0]이 선택하는 것은?","o":["첫 번째 행","'0'이라는 라벨의 열","마지막 행","index가 0인 라벨"],"a":0,"ex":"iloc[0]은 라벨과 상관없이 정수 위치 0, 즉 첫 번째 행을 Series로 반환합니다."},{"t":"choice","k":"슬라이스 차이","q":"index가 0,1,2인 df에서 df.loc[0:2]가 반환하는 행의 개수는?","code":"df = pd.DataFrame({'x': [10, 20, 30]})\nprint(len(df.loc[0:2]))","o":["3","2","1","0"],"a":0,"ex":"loc 슬라이스는 끝 라벨을 포함합니다. 0, 1, 2 세 개가 모두 포함되어 3개입니다. (iloc[0:2]는 끝 미포함으로 2개)"},{"t":"input","k":"라벨 선택","q":"라벨(index 이름)로 행/열을 고를 때 쓰는 인덱서는? (loc / iloc 중)","a":["loc",".loc"],"ex":"라벨 기반 선택은 loc입니다. 정수 위치 기반은 iloc입니다."},{"t":"choice","k":"행+열 선택","q":"df.loc[1, 'name']이 의미하는 것은?","o":["라벨 1인 행의 'name' 열 값 하나","두 번째 열의 첫 값","'name' 행의 1번 열","1과 'name' 두 열 전체"],"a":0,"ex":"loc[행라벨, 열라벨] 형태로, index 라벨 1인 행과 'name' 열이 만나는 단일 값을 반환합니다."}],"th":{"sum":"`loc`는 **라벨 기반**, `iloc`는 **정수 위치 기반** 선택이다. Boolean 인덱싱으로 조건에 맞는 행만 필터링한다.","body":[{"h":"loc vs iloc","t":"`loc`은 index 이름(라벨)과 열 이름으로 고르며 슬라이스 끝이 **포함**된다 — index 0,1,2에서 `df.loc[0:2]`는 3개 행을 반환한다. `iloc`은 정수 위치로 고르며 끝이 **제외**된다. `df.iloc[0]`은 첫 번째 행을, `df.loc[1,'name']`은 index 1인 행의 name 값을 가리킨다."},{"h":"Boolean 인덱싱과 결측","t":"`df[df['age']>30]`은 조건이 True인 행만 남긴 DataFrame을 반환한다. 조건식은 True/False Series를 만들고 이를 대괄호에 넣어 필터링한다. `value_counts()`는 값별 빈도를, `isna()`/`fillna()`는 결측치(NaN) 탐지·대체에 쓴다."}],"code":{"c":"df = pd.DataFrame({'name':['A','B','C'], 'age':[35, 28, 40]})\nprint(df[df['age'] > 30])   # A(35), C(40) 두 행\nprint(df.loc[0, 'name'])    # 'A'\nprint(df.iloc[0])           # 첫 행 전체","cap":"loc=라벨(끝 포함), iloc=위치(끝 제외), 조건은 Boolean 필터"},"key":["loc=라벨 기반·슬라이스 끝 포함, iloc=위치 기반·끝 제외","라벨로 고를 땐 loc 사용","`df[조건]`은 조건이 True인 행만 반환","value_counts는 빈도 집계, fillna는 결측 대체"]}},{"t":"Boolean 인덱싱","q":[{"t":"choice","k":"조건 필터","q":"df[df['age'] &gt; 30]이 반환하는 것은?","o":["age가 30보다 큰 행들만","age 열의 평균","age가 30인 행만","age &gt; 30 이라는 새 열"],"a":0,"ex":"df['age'] &gt; 30은 True/False Series(boolean mask)이고, 이를 df[...]에 넣으면 True인 행만 걸러집니다."},{"t":"input","k":"필터 행 수","q":"아래 필터 결과의 행 개수는?","code":"df = pd.DataFrame({'name': ['Tom', 'Amy', 'Bob'],\n                   'age': [25, 35, 45]})\nprint(len(df[df['age'] &gt; 30]))","a":["2","2개"],"ex":"age가 35(Amy), 45(Bob)인 두 행만 30 초과 조건을 만족하므로 2개입니다."},{"t":"choice","k":"여러 조건","q":"여러 조건을 AND로 결합할 때 올바른 pandas 문법은?","o":["df[(df['age'] &gt; 30) &amp; (df['city'] == 'Seoul')]","df[df['age'] &gt; 30 and df['city'] == 'Seoul']","df[df['age'] &gt; 30 &amp;&amp; df['city'] == 'Seoul']","df[df['age'] &gt; 30, df['city'] == 'Seoul']"],"a":0,"ex":"pandas에서는 and/or 대신 &amp;(and), |(or)를 쓰고, 연산자 우선순위 때문에 각 조건을 괄호로 감싸야 합니다."},{"t":"choice","k":"isin","q":"df['city']가 'Seoul' 또는 'Busan'인 행을 고르는 가장 간결한 방법은?","o":["df[df['city'].isin(['Seoul', 'Busan'])]","df[df['city'] == ['Seoul', 'Busan']]","df[df['city'].contains('Seoul', 'Busan')]","df[df['city'] in ['Seoul', 'Busan']]"],"a":0,"ex":"isin(리스트)는 값이 리스트에 포함되는지를 boolean으로 반환해 여러 값 필터에 편리합니다."},{"t":"choice","k":"연산자 의미","q":"boolean 인덱싱에서 | 연산자의 의미는?","o":["OR (또는)","AND (그리고)","NOT (부정)","XOR 전용"],"a":0,"ex":"|는 OR, &amp;는 AND, ~는 NOT입니다. 원소별(element-wise)로 동작하며 조건은 괄호로 감싸야 합니다."}],"th":{"sum":"`loc`는 **라벨 기반**, `iloc`는 **정수 위치 기반** 선택이다. Boolean 인덱싱으로 조건에 맞는 행만 필터링한다.","body":[{"h":"loc vs iloc","t":"`loc`은 index 이름(라벨)과 열 이름으로 고르며 슬라이스 끝이 **포함**된다 — index 0,1,2에서 `df.loc[0:2]`는 3개 행을 반환한다. `iloc`은 정수 위치로 고르며 끝이 **제외**된다. `df.iloc[0]`은 첫 번째 행을, `df.loc[1,'name']`은 index 1인 행의 name 값을 가리킨다."},{"h":"Boolean 인덱싱과 결측","t":"`df[df['age']>30]`은 조건이 True인 행만 남긴 DataFrame을 반환한다. 조건식은 True/False Series를 만들고 이를 대괄호에 넣어 필터링한다. `value_counts()`는 값별 빈도를, `isna()`/`fillna()`는 결측치(NaN) 탐지·대체에 쓴다."}],"code":{"c":"df = pd.DataFrame({'name':['A','B','C'], 'age':[35, 28, 40]})\nprint(df[df['age'] > 30])   # A(35), C(40) 두 행\nprint(df.loc[0, 'name'])    # 'A'\nprint(df.iloc[0])           # 첫 행 전체","cap":"loc=라벨(끝 포함), iloc=위치(끝 제외), 조건은 Boolean 필터"},"key":["loc=라벨 기반·슬라이스 끝 포함, iloc=위치 기반·끝 제외","라벨로 고를 땐 loc 사용","`df[조건]`은 조건이 True인 행만 반환","value_counts는 빈도 집계, fillna는 결측 대체"]}},{"t":"결측치와 value_counts","q":[{"t":"choice","k":"isna","q":"df['x'].isna()가 반환하는 것은?","o":["각 값이 결측치(NaN)인지 나타내는 True/False Series","결측치의 총 개수(정수)","결측치를 제거한 새 Series","결측치를 0으로 채운 Series"],"a":0,"ex":"isna()는 값마다 결측 여부를 boolean으로 알려줍니다. 개수는 df['x'].isna().sum()으로 셉니다."},{"t":"choice","k":"fillna","q":"결측치를 특정 값(예: 0)으로 채우는 메서드는?","o":["df.fillna(0)","df.dropna(0)","df.isna(0)","df.replace_na(0)"],"a":0,"ex":"fillna(값)은 NaN을 지정한 값으로 대체합니다. 평균 등으로도 채울 수 있습니다: df['x'].fillna(df['x'].mean())."},{"t":"input","k":"행 제거","q":"결측치가 있는 행을 통째로 제거하는 메서드 이름은? (df.___())","a":["dropna","dropna()"],"ex":"dropna()는 기본적으로 NaN이 하나라도 있는 행을 제거합니다. axis=1을 주면 열 단위로 제거합니다."},{"t":"input","k":"value_counts 값","q":"아래 value_counts()에서 'x'에 대응하는 개수는?","code":"s = pd.Series(['x', 'y', 'x', 'x', 'z'])\nprint(s.value_counts())","a":["3","3개"],"ex":"value_counts()는 각 고유값의 빈도를 내림차순으로 셉니다. 'x'는 3번 나오므로 3입니다."},{"t":"choice","k":"정규화","q":"value_counts(normalize=True)의 결과는?","o":["빈도 대신 비율(합이 1)을 반환","값을 알파벳순으로 정렬","결측치를 채워서 반환","누적 합계를 반환"],"a":0,"ex":"normalize=True를 주면 각 값의 상대 빈도(비율)를 반환하며, 전체 합은 1이 됩니다."}],"th":{"sum":"`loc`는 **라벨 기반**, `iloc`는 **정수 위치 기반** 선택이다. Boolean 인덱싱으로 조건에 맞는 행만 필터링한다.","body":[{"h":"loc vs iloc","t":"`loc`은 index 이름(라벨)과 열 이름으로 고르며 슬라이스 끝이 **포함**된다 — index 0,1,2에서 `df.loc[0:2]`는 3개 행을 반환한다. `iloc`은 정수 위치로 고르며 끝이 **제외**된다. `df.iloc[0]`은 첫 번째 행을, `df.loc[1,'name']`은 index 1인 행의 name 값을 가리킨다."},{"h":"Boolean 인덱싱과 결측","t":"`df[df['age']>30]`은 조건이 True인 행만 남긴 DataFrame을 반환한다. 조건식은 True/False Series를 만들고 이를 대괄호에 넣어 필터링한다. `value_counts()`는 값별 빈도를, `isna()`/`fillna()`는 결측치(NaN) 탐지·대체에 쓴다."}],"code":{"c":"df = pd.DataFrame({'name':['A','B','C'], 'age':[35, 28, 40]})\nprint(df[df['age'] > 30])   # A(35), C(40) 두 행\nprint(df.loc[0, 'name'])    # 'A'\nprint(df.iloc[0])           # 첫 행 전체","cap":"loc=라벨(끝 포함), iloc=위치(끝 제외), 조건은 Boolean 필터"},"key":["loc=라벨 기반·슬라이스 끝 포함, iloc=위치 기반·끝 제외","라벨로 고를 땐 loc 사용","`df[조건]`은 조건이 True인 행만 반환","value_counts는 빈도 집계, fillna는 결측 대체"]}}]},{"t":"그룹화와 결합","l":[{"t":"groupby 집계","q":[{"t":"choice","k":"groupby 개념","q":"df.groupby('team')['score'].mean()이 계산하는 것은?","o":["team 값별로 나눈 뒤 각 그룹의 score 평균","전체 score의 평균 하나","team 열의 평균","score를 team으로 나눈 값"],"a":0,"ex":"groupby('team')로 같은 team끼리 묶고, 각 그룹의 score 평균을 team별로 반환합니다(split-apply-combine)."},{"t":"input","k":"그룹 합계","q":"아래 결과에서 team 'A'의 합계는?","code":"df = pd.DataFrame({'team': ['A', 'B', 'A', 'B'],\n                   'score': [10, 20, 30, 40]})\nprint(df.groupby('team')['score'].sum())","a":["40","40점"],"ex":"team 'A'의 score는 10과 30이므로 합계는 40입니다. (참고로 'B'는 20+40=60)"},{"t":"choice","k":"agg","q":"한 번의 groupby로 여러 집계(합계와 평균)를 동시에 구하는 방법은?","o":["df.groupby('team')['score'].agg(['sum', 'mean'])","df.groupby('team').sum().mean()","df.groupby('team', 'sum', 'mean')","df.agg('team')['score']"],"a":0,"ex":"agg(['sum', 'mean'])처럼 함수 리스트를 주면 그룹마다 여러 집계를 한 번에 계산해 여러 열로 반환합니다."},{"t":"choice","k":"그룹 개수","q":"각 그룹의 '행 개수'를 세는 데 적합한 것은?","o":["df.groupby('team').size()","df.groupby('team').mean()","df.groupby('team').max()","df.groupby('team').sort_values()"],"a":0,"ex":"size()는 그룹별 행 수를 반환합니다. count()도 쓸 수 있으나 결측치를 제외하고 셉니다."},{"t":"choice","k":"reset_index","q":"groupby 집계 결과에서 그룹 키를 다시 일반 열로 되돌리는 메서드는?","o":["reset_index()","set_index()","drop_index()","reindex()"],"a":0,"ex":"groupby 결과는 그룹 키가 index가 됩니다. reset_index()를 호출하면 그 키가 다시 일반 열이 됩니다."}],"th":{"sum":"`groupby`는 **분할-적용-결합(split-apply-combine)**으로 그룹별 집계를 하고, `merge`/`concat`은 여러 표를 키나 축으로 결합한다.","body":[{"h":"groupby 집계","t":"`df.groupby('team')['score'].mean()`은 team별 score 평균을 계산한다. `agg(['sum','mean'])`로 여러 집계를 동시에, `size()`로 그룹별 행 개수를 센다. groupby 결과는 그룹 키가 index가 되는데, `reset_index()`로 다시 일반 열로 되돌린다."},{"h":"merge와 concat","t":"`pd.merge(a,b,on='id',how='inner')`는 양쪽에 모두 있는 id만 남긴다(교집합). `how='left'`는 왼쪽 표의 모든 행을 유지하고 오른쪽에 없으면 NaN을 채운다. `concat`은 행/열 방향으로 단순히 이어 붙이고, `pivot`은 긴 형식을 넓은 표로 재구성한다."}],"code":{"c":"df = pd.DataFrame({'team':['A','A','B'], 'score':[10, 20, 5]})\nprint(df.groupby('team')['score'].sum())   # A:30, B:5\nprint(df.groupby('team').agg(s=('score','sum'), m=('score','mean')))","cap":"groupby로 그룹별 집계, agg로 여러 집계 동시 계산"},"key":["groupby = 분할-적용-결합, `.mean()`/`.sum()`으로 집계","agg로 여러 집계 동시, size로 그룹 행 개수","merge how='inner'=교집합, 'left'=왼쪽 전체 유지","reset_index로 그룹 키를 일반 열로 복원"]}},{"t":"merge와 concat","q":[{"t":"choice","k":"inner join","q":"pd.merge(a, b, on='id', how='inner')의 결과는?","o":["두 DataFrame 모두에 존재하는 id만 남는다","a의 모든 행이 남는다","b의 모든 행이 남는다","id가 다른 행만 남는다"],"a":0,"ex":"inner join은 양쪽 키가 모두 매칭되는 행만 유지합니다. 교집합에 해당합니다."},{"t":"choice","k":"left join","q":"how='left'로 merge하면?","o":["왼쪽 DataFrame의 모든 행을 유지하고, 매칭 안 되면 NaN을 채운다","오른쪽 DataFrame의 모든 행을 유지한다","매칭되는 행만 남긴다","두 프레임의 모든 행을 합친다"],"a":0,"ex":"left join은 왼쪽 기준으로, 왼쪽 모든 행을 남기고 오른쪽에 짝이 없으면 그 열은 NaN이 됩니다."},{"t":"choice","k":"concat 방향","q":"pd.concat([df1, df2], axis=0)이 하는 일은?","o":["행 방향으로 위아래로 이어 붙인다","열 방향으로 좌우로 이어 붙인다","키 기준으로 조인한다","중복 행을 제거한다"],"a":0,"ex":"axis=0은 행 방향(세로) 결합입니다. axis=1은 열 방향(가로) 결합으로 index를 맞춰 붙입니다."},{"t":"input","k":"결합 행 수","q":"아래 concat 결과의 전체 행 개수는?","code":"df1 = pd.DataFrame({'a': [1, 2]})\ndf2 = pd.DataFrame({'a': [3, 4, 5]})\nprint(len(pd.concat([df1, df2])))","a":["5","5개"],"ex":"concat은 기본 axis=0이라 행을 이어 붙입니다. 2 + 3 = 5개 행이 됩니다."},{"t":"choice","k":"merge vs concat","q":"merge와 concat의 핵심 차이로 옳은 것은?","o":["merge는 키(공통 열) 값으로 매칭해 결합, concat은 축을 따라 단순히 이어 붙임","둘은 완전히 동일하다","concat만 키를 사용한다","merge는 항상 행을 늘리기만 한다"],"a":0,"ex":"merge는 SQL join처럼 키 매칭 기반 결합이고, concat은 지정한 축을 따라 데이터를 이어 붙이는 연결입니다."}],"th":{"sum":"`groupby`는 **분할-적용-결합(split-apply-combine)**으로 그룹별 집계를 하고, `merge`/`concat`은 여러 표를 키나 축으로 결합한다.","body":[{"h":"groupby 집계","t":"`df.groupby('team')['score'].mean()`은 team별 score 평균을 계산한다. `agg(['sum','mean'])`로 여러 집계를 동시에, `size()`로 그룹별 행 개수를 센다. groupby 결과는 그룹 키가 index가 되는데, `reset_index()`로 다시 일반 열로 되돌린다."},{"h":"merge와 concat","t":"`pd.merge(a,b,on='id',how='inner')`는 양쪽에 모두 있는 id만 남긴다(교집합). `how='left'`는 왼쪽 표의 모든 행을 유지하고 오른쪽에 없으면 NaN을 채운다. `concat`은 행/열 방향으로 단순히 이어 붙이고, `pivot`은 긴 형식을 넓은 표로 재구성한다."}],"code":{"c":"df = pd.DataFrame({'team':['A','A','B'], 'score':[10, 20, 5]})\nprint(df.groupby('team')['score'].sum())   # A:30, B:5\nprint(df.groupby('team').agg(s=('score','sum'), m=('score','mean')))","cap":"groupby로 그룹별 집계, agg로 여러 집계 동시 계산"},"key":["groupby = 분할-적용-결합, `.mean()`/`.sum()`으로 집계","agg로 여러 집계 동시, size로 그룹 행 개수","merge how='inner'=교집합, 'left'=왼쪽 전체 유지","reset_index로 그룹 키를 일반 열로 복원"]}},{"t":"apply, 정렬, pivot","q":[{"t":"input","k":"apply 출력","q":"아래 apply 결과의 첫 번째 값은?","code":"s = pd.Series([1, 2, 3])\nprint(s.apply(lambda x: x * 2).iloc[0])","a":["2","2.0"],"ex":"apply는 각 원소에 함수를 적용합니다. 첫 값 1에 x*2가 적용되어 2가 됩니다."},{"t":"choice","k":"sort_values","q":"df를 'age' 열 기준으로 정렬하는 코드는?","o":["df.sort_values('age')","df.sort_index('age')","df.order_by('age')","df.sort('age', values=True)"],"a":0,"ex":"sort_values('열이름')은 해당 열 값 기준으로 행을 정렬합니다. sort_index()는 index 기준 정렬입니다."},{"t":"choice","k":"정렬 방향","q":"df.sort_values('score')의 기본 정렬 방향은?","o":["오름차순(작은 값부터)","내림차순(큰 값부터)","무작위","정렬하지 않음"],"a":0,"ex":"기본값은 ascending=True(오름차순)입니다. 내림차순은 ascending=False를 지정합니다."},{"t":"choice","k":"pivot_table","q":"pivot_table의 역할로 가장 정확한 것은?","o":["index/columns로 지정한 기준에 따라 값을 재배치하며 집계(예: 평균)한다","결측치를 자동으로 제거한다","두 DataFrame을 키로 조인한다","열의 데이터 타입을 바꾼다"],"a":0,"ex":"pivot_table은 index와 columns 기준으로 데이터를 펼쳐 재배치하고 aggfunc(기본 평균)로 집계하는 요약표를 만듭니다."},{"t":"choice","k":"apply axis","q":"DataFrame에서 각 '행' 전체를 대상으로 함수를 적용하려면?","o":["df.apply(func, axis=1)","df.apply(func, axis=0)","df.applymap(func)","df.map(func)"],"a":0,"ex":"df.apply(func, axis=1)은 행 단위로 함수를 적용합니다. axis=0(기본)은 열 단위입니다."}],"th":{"sum":"`groupby`는 **분할-적용-결합(split-apply-combine)**으로 그룹별 집계를 하고, `merge`/`concat`은 여러 표를 키나 축으로 결합한다.","body":[{"h":"groupby 집계","t":"`df.groupby('team')['score'].mean()`은 team별 score 평균을 계산한다. `agg(['sum','mean'])`로 여러 집계를 동시에, `size()`로 그룹별 행 개수를 센다. groupby 결과는 그룹 키가 index가 되는데, `reset_index()`로 다시 일반 열로 되돌린다."},{"h":"merge와 concat","t":"`pd.merge(a,b,on='id',how='inner')`는 양쪽에 모두 있는 id만 남긴다(교집합). `how='left'`는 왼쪽 표의 모든 행을 유지하고 오른쪽에 없으면 NaN을 채운다. `concat`은 행/열 방향으로 단순히 이어 붙이고, `pivot`은 긴 형식을 넓은 표로 재구성한다."}],"code":{"c":"df = pd.DataFrame({'team':['A','A','B'], 'score':[10, 20, 5]})\nprint(df.groupby('team')['score'].sum())   # A:30, B:5\nprint(df.groupby('team').agg(s=('score','sum'), m=('score','mean')))","cap":"groupby로 그룹별 집계, agg로 여러 집계 동시 계산"},"key":["groupby = 분할-적용-결합, `.mean()`/`.sum()`으로 집계","agg로 여러 집계 동시, size로 그룹 행 개수","merge how='inner'=교집합, 'left'=왼쪽 전체 유지","reset_index로 그룹 키를 일반 열로 복원"]}}]},{"t":"결측치와 중복 데이터 처리 전략","l":[{"t":"NaN · None · pd.NA 구별하기","q":[{"t":"choice","k":"nan 비교","q":"다음 코드의 출력으로 옳은 것은?","ex":"IEEE 754 규약상 NaN은 자기 자신과도 같지 않으므로 False가 출력된다. 그래서 `df['x'] == np.nan` 같은 비교는 항상 전부 False가 되어 결측을 하나도 못 잡는다. 결측 판별은 반드시 `isna()`를 써야 한다.","code":"import numpy as np\nprint(np.nan == np.nan)","o":["True","False","None","TypeError 발생"],"a":1},{"t":"choice","k":"dtype 승격","q":"정수만 들어 있던 열에 결측이 하나 섞였을 때 기본(비-nullable) 동작으로 옳은 것은?","ex":"np.nan은 float 값이라 int64 열에 담을 수 없어 열 전체가 float64로 승격된다. 결측이 0으로 채워지거나 행이 자동 삭제되는 일은 절대 없다. 정수형을 유지하려면 nullable Int64 dtype을 써야 한다.","o":["dtype이 float64로 승격되고 결측은 NaN이 된다","dtype이 int64로 유지되고 결측은 0으로 채워진다","dtype이 object로 바뀌고 결측은 None이 된다","결측이 있는 행이 자동으로 제거된다"],"a":0},{"t":"choice","k":"pd.NA","q":"`pd.NA`에 대한 설명으로 옳은 것은?","ex":"pd.NA는 확장(nullable) dtype 체계의 결측 표식으로, Int64 열이 float로 승격되지 않고 정수형을 유지하게 해준다. np.nan과 별개의 객체이며 비교하면 True/False가 아니라 pd.NA를 돌려준다. 자동 대체 기능은 전혀 없다.","o":["object dtype에서만 쓸 수 있는 문자열 전용 결측값이다","np.nan의 별칭이라 완전히 동일하게 동작한다","Int64, boolean, string 같은 nullable dtype에서 정수형을 유지한 채 결측을 표현한다","결측을 자동으로 평균값으로 대체해 주는 헬퍼 객체다"],"a":2},{"t":"input","k":"결측 판별 메서드","q":"결측 여부를 불리언 Series로 돌려주는 pandas 메서드 이름을 쓰세요. (== 비교 대신 써야 하는 것)","ex":"`isna()`(별칭 `isnull()`)가 정답이다. NaN은 자기 자신과 같지 않으므로 `== np.nan`은 무조건 False만 나온다. 반대 판별은 `notna()`를 쓴다.","a":["isna","isna()","isnull","isnull()"]},{"t":"choice","k":"결측 개수","q":"아래 코드의 출력값은?","ex":"숫자 Series에 넣은 None은 pandas가 NaN으로 변환하므로 결측은 총 2개다. isna()가 불리언 Series를 주고 sum()이 True를 1로 세어 2가 나온다. None이라서 세지 않는다고 착각하기 쉽다.","code":"import numpy as np, pandas as pd\ns = pd.Series([1, np.nan, 3, None, 5])\nprint(s.isna().sum())","o":["0","1","2","3"],"a":2}],"th":{"sum":"pandas의 결측치는 `np.nan`, `None`, `pd.NA` 세 가지 얼굴을 가지며, 비교 연산 대신 `isna()`로 판별해야 한다. 중복 제거는 `subset`과 `keep`을 어떻게 주느냐에 따라 결과가 완전히 달라진다.","body":[{"h":"NaN, None, pd.NA는 서로 다르다","t":"`np.nan`은 실수(float) 타입의 특수값이라서 int 열에 결측이 하나라도 섞이면 열 전체가 float64로 승격된다. `None`은 파이썬 객체이며 object dtype 열에서 그대로 저장되지만, 숫자 열에 넣으면 pandas가 NaN으로 바꾼다. `pd.NA`는 nullable 확장 dtype(Int64, boolean, string)에서 쓰는 결측 표식으로, 정수형을 유지한 채 결측을 담을 수 있다."},{"h":"== 로 결측을 비교하면 안 되는 이유","t":"IEEE 754 규약에 따라 `np.nan != np.nan` 이다. 즉 결측값은 자기 자신과도 같지 않기 때문에 `df['x'] == np.nan` 은 전부 False를 돌려준다. 반드시 `df['x'].isna()` 또는 `notna()`를 써야 하며, `pd.NA`는 비교 시 True/False가 아니라 다시 `pd.NA`를 반환해 조건문에서 에러를 낸다."},{"h":"fillna, dropna, 그리고 ffill의 함정","t":"`dropna()`는 기본적으로 결측이 하나라도 있는 **행**을 지우며 `how='all'`을 주면 전부 결측인 행만 지운다. `ffill()`은 바로 위 값을 아래로 복사하는데, 정렬이 뒤죽박죽이거나 그룹이 섞여 있으면 다른 개체의 값이 넘어와 조용히 오염된다. 시계열이라면 반드시 시간순 정렬 후 `groupby(...).ffill()` 형태로 그룹 경계를 지켜야 한다."},{"h":"중복 제거와 데이터 누수","t":"`duplicated()`는 기본적으로 모든 열이 같은 행을 중복으로 보고, `subset=['id']`처럼 키를 지정하면 그 열 기준으로만 판단한다. `drop_duplicates()`의 기본값은 `keep='first'`라 첫 행을 남기고, `keep=False`는 중복된 행을 모두 버린다. 한편 결측 대체값(평균, 중앙값)을 **전체 데이터**에서 계산하면 검증셋 정보가 학습에 스며드는 데이터 누수가 생기므로 반드시 학습셋에서만 통계를 구해 검증셋에 적용해야 한다."}],"code":{"c":"import numpy as np, pandas as pd\n\ndf = pd.DataFrame({\"id\": [1, 1, 2, 3], \"v\": [10, 10, np.nan, 30]})\n\nprint(np.nan == np.nan)          # False -> == 로 비교 불가\nprint(df[\"v\"].isna().sum())      # 1  (올바른 결측 판별)\n\nprint(df.duplicated(subset=[\"id\"]).sum())        # 1\nprint(df.drop_duplicates(subset=[\"id\"]).shape)   # keep='first'\n\ns = pd.Series([1, None, 3], dtype=\"Int64\")\nprint(s.dtype, s.sum())          # Int64 유지, pd.NA 는 건너뜀","cap":"결측 판별은 isna()로, 중복은 subset/keep으로 제어한다."},"key":["np.nan은 float이고 자기 자신과도 같지 않다 — 결측 비교는 반드시 isna()","int 열에 NaN이 들어가면 float64로 승격된다 (nullable Int64는 예외)","drop_duplicates의 기본은 keep='first', subset으로 기준 열을 좁힐 수 있다","ffill은 정렬/그룹 경계를 어기면 다른 개체 값을 끌어와 데이터를 오염시킨다","결측 대체 통계는 학습셋에서만 계산해야 데이터 누수를 막는다"]}},{"t":"fillna · dropna · ffill의 함정","q":[{"t":"choice","k":"dropna 기본","q":"`df.dropna()`를 인자 없이 호출했을 때의 기본 동작은?","ex":"기본값은 `axis=0, how='any'`라서 결측이 하나만 있어도 그 행 전체가 사라진다. 전부 결측인 행만 지우려면 `how='all'`을 명시해야 한다. 열을 지우려면 `axis=1`이 필요하다.","o":["모든 값이 결측인 행만 제거한다","결측이 하나라도 있는 행을 제거한다","결측이 하나라도 있는 열을 제거한다","결측을 0으로 채우고 행은 유지한다"],"a":1},{"t":"choice","k":"ffill 결과","q":"아래 코드에서 출력되는 리스트는?","ex":"ffill은 앞의 값을 뒤로 전파하므로 2.0이 두 칸 복사되지만, 맨 앞은 참조할 이전 값이 없어 NaN으로 남는다. 첫 결측까지 채워질 거라 착각하는 실수가 잦다. 앞부분을 채우려면 `bfill()`을 함께 쓴다.","code":"import numpy as np, pandas as pd\ns = pd.Series([np.nan, 2.0, np.nan, np.nan, 5.0])\nprint(s.ffill().tolist())","o":["[2.0, 2.0, 2.0, 2.0, 5.0]","[0.0, 2.0, 2.0, 2.0, 5.0]","[nan, 2.0, 2.0, 2.0, 5.0]","[nan, 2.0, 5.0, 5.0, 5.0]"],"a":2},{"t":"choice","k":"ffill 오염","q":"여러 사용자의 로그가 한 DataFrame에 섞여 있을 때 그냥 `df['v'].ffill()`을 부르면 생기는 문제는?","ex":"ffill은 그룹 경계를 전혀 인식하지 않고 바로 위 행의 값을 그대로 가져온다. 따라서 정렬 후 `df.groupby('user')['v'].ffill()`처럼 그룹 안에서만 채워야 한다. 에러가 나지 않아 발견이 늦는 것이 특히 위험하다.","o":["결측이 전혀 채워지지 않아 원본과 동일해진다","숫자 열이 문자열 dtype으로 바뀐다","정렬 순서가 무작위로 섞여 인덱스가 깨진다","앞 행이 다른 사용자면 남의 값이 넘어와 데이터가 조용히 오염된다"],"a":3},{"t":"input","k":"그룹별 채우기","q":"사용자별로만 앞 값을 전파하려 합니다. 빈칸에 들어갈 메서드 이름은?\n`df.groupby('user')['v'].____()`","ex":"`ffill`(옛 이름 `pad`)을 groupby 뒤에 붙이면 각 그룹 내부에서만 앞 값이 전파된다. 그룹 없이 호출하면 이전 사용자 값이 다음 사용자에게 넘어간다. 최신 pandas에서는 `fillna(method=...)` 대신 `ffill()` 사용이 권장된다.","a":["ffill","ffill()","pad","fillna(method='ffill')"]},{"t":"choice","k":"누수 방지","q":"결측을 평균으로 대체할 때 데이터 누수를 피하는 올바른 절차는?","ex":"대체 통계는 반드시 학습셋에서만 계산하고, 그 값을 검증셋에 그대로 적용해야 한다. 전체 평균을 쓰면 검증셋 정보가 학습 과정에 스며들어 성능이 실제보다 좋게 나온다. 이런 누수는 실서비스 배포 후 성능 급락으로 드러난다.","o":["전체 데이터의 평균을 구해 학습셋과 검증셋에 동일하게 적용한다","학습셋에서만 평균을 계산해 학습셋과 검증셋 모두에 적용한다","검증셋 평균으로 학습셋을 채워 분포를 맞춘다","행마다 그 행의 다른 열 평균으로 채우면 누수가 사라진다"],"a":1}],"th":{"sum":"pandas의 결측치는 `np.nan`, `None`, `pd.NA` 세 가지 얼굴을 가지며, 비교 연산 대신 `isna()`로 판별해야 한다. 중복 제거는 `subset`과 `keep`을 어떻게 주느냐에 따라 결과가 완전히 달라진다.","body":[{"h":"NaN, None, pd.NA는 서로 다르다","t":"`np.nan`은 실수(float) 타입의 특수값이라서 int 열에 결측이 하나라도 섞이면 열 전체가 float64로 승격된다. `None`은 파이썬 객체이며 object dtype 열에서 그대로 저장되지만, 숫자 열에 넣으면 pandas가 NaN으로 바꾼다. `pd.NA`는 nullable 확장 dtype(Int64, boolean, string)에서 쓰는 결측 표식으로, 정수형을 유지한 채 결측을 담을 수 있다."},{"h":"== 로 결측을 비교하면 안 되는 이유","t":"IEEE 754 규약에 따라 `np.nan != np.nan` 이다. 즉 결측값은 자기 자신과도 같지 않기 때문에 `df['x'] == np.nan` 은 전부 False를 돌려준다. 반드시 `df['x'].isna()` 또는 `notna()`를 써야 하며, `pd.NA`는 비교 시 True/False가 아니라 다시 `pd.NA`를 반환해 조건문에서 에러를 낸다."},{"h":"fillna, dropna, 그리고 ffill의 함정","t":"`dropna()`는 기본적으로 결측이 하나라도 있는 **행**을 지우며 `how='all'`을 주면 전부 결측인 행만 지운다. `ffill()`은 바로 위 값을 아래로 복사하는데, 정렬이 뒤죽박죽이거나 그룹이 섞여 있으면 다른 개체의 값이 넘어와 조용히 오염된다. 시계열이라면 반드시 시간순 정렬 후 `groupby(...).ffill()` 형태로 그룹 경계를 지켜야 한다."},{"h":"중복 제거와 데이터 누수","t":"`duplicated()`는 기본적으로 모든 열이 같은 행을 중복으로 보고, `subset=['id']`처럼 키를 지정하면 그 열 기준으로만 판단한다. `drop_duplicates()`의 기본값은 `keep='first'`라 첫 행을 남기고, `keep=False`는 중복된 행을 모두 버린다. 한편 결측 대체값(평균, 중앙값)을 **전체 데이터**에서 계산하면 검증셋 정보가 학습에 스며드는 데이터 누수가 생기므로 반드시 학습셋에서만 통계를 구해 검증셋에 적용해야 한다."}],"code":{"c":"import numpy as np, pandas as pd\n\ndf = pd.DataFrame({\"id\": [1, 1, 2, 3], \"v\": [10, 10, np.nan, 30]})\n\nprint(np.nan == np.nan)          # False -> == 로 비교 불가\nprint(df[\"v\"].isna().sum())      # 1  (올바른 결측 판별)\n\nprint(df.duplicated(subset=[\"id\"]).sum())        # 1\nprint(df.drop_duplicates(subset=[\"id\"]).shape)   # keep='first'\n\ns = pd.Series([1, None, 3], dtype=\"Int64\")\nprint(s.dtype, s.sum())          # Int64 유지, pd.NA 는 건너뜀","cap":"결측 판별은 isna()로, 중복은 subset/keep으로 제어한다."},"key":["np.nan은 float이고 자기 자신과도 같지 않다 — 결측 비교는 반드시 isna()","int 열에 NaN이 들어가면 float64로 승격된다 (nullable Int64는 예외)","drop_duplicates의 기본은 keep='first', subset으로 기준 열을 좁힐 수 있다","ffill은 정렬/그룹 경계를 어기면 다른 개체 값을 끌어와 데이터를 오염시킨다","결측 대체 통계는 학습셋에서만 계산해야 데이터 누수를 막는다"]}},{"t":"duplicated와 drop_duplicates 제어","q":[{"t":"choice","k":"keep 기본값","q":"`drop_duplicates()`의 `keep` 파라미터 기본값은?","ex":"기본값은 `keep='first'`로 중복 그룹 중 처음 등장한 행을 남긴다. `'last'`는 마지막 행을, `keep=False`는 중복된 행을 모두 제거한다. `'any'`라는 값은 존재하지 않는다.","o":["'last'","False","'first'","'any'"],"a":2},{"t":"choice","k":"subset 효과","q":"아래 코드의 출력 행 수는?","ex":"subset=['id']는 v 값이 달라도 id만 같으면 중복으로 보므로 id 1과 2에서 각각 첫 행만 남아 2행이 된다. subset을 빼면 (1,10),(1,99),(2,20) 세 행이 남는다. 어떤 열을 키로 볼지 명시하는 것이 핵심이다.","code":"import pandas as pd\ndf = pd.DataFrame({\"id\": [1, 1, 2, 2], \"v\": [10, 99, 20, 20]})\nprint(len(df.drop_duplicates(subset=[\"id\"])))","o":["4","3","2","1"],"a":2},{"t":"choice","k":"keep=False","q":"`df.drop_duplicates(subset=['id'], keep=False)`의 의미로 옳은 것은?","ex":"keep=False는 중복 그룹 전체를 통째로 삭제하므로 유일하게 한 번만 나타난 id의 행만 남는다. '하나는 남겠지'라고 오해하기 쉬운 옵션이다. 중복 자체를 조사할 때 유용하다.","o":["id가 한 번만 등장한 행만 남기고 중복된 id는 전부 버린다","중복된 id 중 마지막 행만 남긴다","중복 여부와 무관하게 모든 행을 그대로 유지한다","id 열 자체를 DataFrame에서 제거한다"],"a":0},{"t":"input","k":"중복 판별 메서드","q":"중복 여부를 불리언 Series로 반환하는 DataFrame 메서드 이름을 쓰세요.","ex":"`duplicated()`가 정답이며 중복이면 True를 돌려준다. `drop_duplicates()`는 실제로 행을 지우는 메서드라 역할이 다르다. 중복을 지우기 전에 먼저 `duplicated().sum()`으로 규모를 확인하는 습관이 좋다.","a":["duplicated","duplicated()","df.duplicated","DUPLICATED"]},{"t":"choice","k":"duplicated 개수","q":"아래 코드의 출력값은?","ex":"subset 없이 호출하면 모든 열이 같아야 중복이므로 (1,5)가 두 번 나온 것 중 두 번째 행 하나만 True가 되어 1이 나온다. (1,6)은 v가 달라 중복이 아니다. 첫 등장 행은 중복으로 세지 않는다는 점을 놓치기 쉽다.","code":"import pandas as pd\ndf = pd.DataFrame({\"id\": [1, 1, 1, 2], \"v\": [5, 5, 6, 7]})\nprint(df.duplicated().sum())","o":["3","2","0","1"],"a":3},{"t":"choice","k":"결측 대체 순서","q":"전처리 파이프라인에서 결측 대체와 학습/검증 분할의 올바른 순서는?","ex":"먼저 분할한 뒤 학습셋에서만 통계를 구해 검증셋에 적용해야 누수가 없다. 전체에서 미리 계산하면 검증셋 정보가 새어 들어가고, 검증셋에서 따로 계산하면 실제 추론 시점에는 불가능한 정보를 쓰는 셈이 된다.","o":["대체값 계산 후 분할 — 어차피 같은 분포라 상관없다","분할 후 학습셋 통계로만 대체값을 계산해 양쪽에 적용한다","분할 후 각 셋에서 따로 평균을 구해 각자 채운다","대체는 모델 학습이 끝난 뒤에 수행한다"],"a":1}],"th":{"sum":"pandas의 결측치는 `np.nan`, `None`, `pd.NA` 세 가지 얼굴을 가지며, 비교 연산 대신 `isna()`로 판별해야 한다. 중복 제거는 `subset`과 `keep`을 어떻게 주느냐에 따라 결과가 완전히 달라진다.","body":[{"h":"NaN, None, pd.NA는 서로 다르다","t":"`np.nan`은 실수(float) 타입의 특수값이라서 int 열에 결측이 하나라도 섞이면 열 전체가 float64로 승격된다. `None`은 파이썬 객체이며 object dtype 열에서 그대로 저장되지만, 숫자 열에 넣으면 pandas가 NaN으로 바꾼다. `pd.NA`는 nullable 확장 dtype(Int64, boolean, string)에서 쓰는 결측 표식으로, 정수형을 유지한 채 결측을 담을 수 있다."},{"h":"== 로 결측을 비교하면 안 되는 이유","t":"IEEE 754 규약에 따라 `np.nan != np.nan` 이다. 즉 결측값은 자기 자신과도 같지 않기 때문에 `df['x'] == np.nan` 은 전부 False를 돌려준다. 반드시 `df['x'].isna()` 또는 `notna()`를 써야 하며, `pd.NA`는 비교 시 True/False가 아니라 다시 `pd.NA`를 반환해 조건문에서 에러를 낸다."},{"h":"fillna, dropna, 그리고 ffill의 함정","t":"`dropna()`는 기본적으로 결측이 하나라도 있는 **행**을 지우며 `how='all'`을 주면 전부 결측인 행만 지운다. `ffill()`은 바로 위 값을 아래로 복사하는데, 정렬이 뒤죽박죽이거나 그룹이 섞여 있으면 다른 개체의 값이 넘어와 조용히 오염된다. 시계열이라면 반드시 시간순 정렬 후 `groupby(...).ffill()` 형태로 그룹 경계를 지켜야 한다."},{"h":"중복 제거와 데이터 누수","t":"`duplicated()`는 기본적으로 모든 열이 같은 행을 중복으로 보고, `subset=['id']`처럼 키를 지정하면 그 열 기준으로만 판단한다. `drop_duplicates()`의 기본값은 `keep='first'`라 첫 행을 남기고, `keep=False`는 중복된 행을 모두 버린다. 한편 결측 대체값(평균, 중앙값)을 **전체 데이터**에서 계산하면 검증셋 정보가 학습에 스며드는 데이터 누수가 생기므로 반드시 학습셋에서만 통계를 구해 검증셋에 적용해야 한다."}],"code":{"c":"import numpy as np, pandas as pd\n\ndf = pd.DataFrame({\"id\": [1, 1, 2, 3], \"v\": [10, 10, np.nan, 30]})\n\nprint(np.nan == np.nan)          # False -> == 로 비교 불가\nprint(df[\"v\"].isna().sum())      # 1  (올바른 결측 판별)\n\nprint(df.duplicated(subset=[\"id\"]).sum())        # 1\nprint(df.drop_duplicates(subset=[\"id\"]).shape)   # keep='first'\n\ns = pd.Series([1, None, 3], dtype=\"Int64\")\nprint(s.dtype, s.sum())          # Int64 유지, pd.NA 는 건너뜀","cap":"결측 판별은 isna()로, 중복은 subset/keep으로 제어한다."},"key":["np.nan은 float이고 자기 자신과도 같지 않다 — 결측 비교는 반드시 isna()","int 열에 NaN이 들어가면 float64로 승격된다 (nullable Int64는 예외)","drop_duplicates의 기본은 keep='first', subset으로 기준 열을 좁힐 수 있다","ffill은 정렬/그룹 경계를 어기면 다른 개체 값을 끌어와 데이터를 오염시킨다","결측 대체 통계는 학습셋에서만 계산해야 데이터 누수를 막는다"]}}]},{"t":"merge/join과 키 중복에 의한 행 폭증 (중급)","l":[{"t":"how와 기본 동작 이해","q":[{"t":"choice","k":"기본 how","q":"`pd.merge(a, b, on='k')`처럼 how를 생략했을 때 적용되는 기본값은?","ex":"pandas merge의 기본 how는 'inner'라서 양쪽에 모두 존재하는 키만 남는다. DataFrame.join의 기본이 'left'인 것과 헷갈리기 쉽다. 매칭되지 않은 행을 보존하려면 how를 명시해야 한다.","o":["'outer'","'left'","'cross'","'inner'"],"a":3},{"t":"choice","k":"행 수 오개념","q":"merge 결과의 행 수에 대한 설명으로 옳은 것은?","ex":"행 수를 결정하는 것은 how가 아니라 키의 유일성이다. 오른쪽에 같은 키가 여러 개면 왼쪽 한 행이 여러 행으로 복제되므로 inner든 left든 행이 늘어난다. '조인은 행을 줄인다'는 직관이 대표적인 오개념이다.","o":["merge는 항상 왼쪽 DataFrame의 행 수보다 줄거나 같다","how='inner'면 절대 행이 늘어나지 않는다","키가 중복이면 어떤 how를 쓰든 행이 늘어날 수 있다","how='left'는 행 수를 왼쪽 원본과 항상 동일하게 유지한다"],"a":2},{"t":"choice","k":"left 조인 결과","q":"아래 코드의 출력값은?","ex":"왼쪽의 k=1은 오른쪽에 짝이 없어 w가 NaN이 되므로 결측은 1개다. left 조인은 왼쪽 키를 모두 보존하고 없는 값은 NaN으로 채운다. 여기서는 오른쪽 키가 유일해 행 수도 3으로 유지된다.","code":"import pandas as pd\na = pd.DataFrame({\"k\": [1, 2, 3], \"v\": [\"x\", \"y\", \"z\"]})\nb = pd.DataFrame({\"k\": [2, 3], \"w\": [20, 30]})\nprint(a.merge(b, on=\"k\", how=\"left\")[\"w\"].isna().sum())","o":["1","0","2","3"],"a":0},{"t":"input","k":"합집합 조인","q":"양쪽 DataFrame의 키를 모두 보존하는 how 값을 쓰세요.","ex":"`how='outer'`는 양쪽 키의 합집합을 남기고 짝이 없는 자리는 NaN으로 채운다. 기본값 inner는 교집합만 남기므로 매칭 실패 행이 조용히 사라진다. 데이터 손실을 점검할 때 outer + indicator 조합이 유용하다.","a":["outer","'outer'","how='outer'","full outer"]},{"t":"choice","k":"join 기본값","q":"`DataFrame.join`의 기본 동작으로 옳은 것은?","ex":"join은 기본적으로 인덱스를 키로 쓰고 how는 'left'다. merge와 기본값이 달라 결과 행 수가 달라지는 혼동이 자주 생긴다. 같은 동작을 merge로 쓰려면 `left_index=True, right_index=True, how='left'`를 준다.","o":["열 이름 기준으로 합치며 기본 how는 'inner'다","인덱스 기준으로 합치며 기본 how는 'left'다","인덱스 기준으로 합치며 기본 how는 'outer'다","행 방향으로 단순히 이어 붙인다 (concat과 동일)"],"a":1}],"th":{"sum":"`merge`의 결과 행 수는 키의 유일성에 달려 있어, 양쪽 키가 중복이면 각 키 그룹의 곱만큼 행이 폭증한다. `validate=`와 `indicator=`로 이런 사고를 사전에 잡아낼 수 있다.","body":[{"h":"how가 결정하는 것은 '어느 쪽 키를 남길지'","t":"pandas `merge`의 기본값은 `how='inner'`로 양쪽에 모두 있는 키만 남긴다. `left`는 왼쪽 키를 전부 보존하고 오른쪽에 없으면 NaN을 채우며, `outer`는 양쪽 합집합을 남긴다. 주의할 점은 `how`가 행 수의 **상한**을 정하지 않는다는 것이다 — 어떤 how를 써도 키가 중복이면 행은 늘어날 수 있다."},{"h":"many-to-many는 카티션 곱","t":"왼쪽에 키 A가 2행, 오른쪽에 키 A가 3행이면 결과에는 A가 **2 × 3 = 6행** 생긴다. 즉 결과 행 수는 각 키별 곱의 합이며, 조인 한 번으로 수백만 행이 튀어나오기도 한다. '조인하면 행이 줄어든다'는 직관은 키가 유일할 때만 성립하므로, 조인 전에 `df['key'].duplicated().sum()`으로 유일성을 확인하는 습관이 필요하다."},{"h":"validate와 indicator로 방어하기","t":"`validate='one_to_one'`, `'one_to_many'`, `'many_to_one'`을 지정하면 가정이 깨질 때 조용히 행이 늘어나는 대신 `MergeError`가 즉시 발생한다. `indicator=True`를 주면 `_merge` 열이 추가되어 각 행이 `left_only`, `right_only`, `both` 중 무엇인지 알려주므로 매칭 실패 규모를 바로 진단할 수 있다. 이 두 옵션은 비용이 거의 없으니 습관적으로 켜 두는 편이 좋다."},{"h":"suffixes와 index 기반 join","t":"양쪽에 같은 이름의 비-키 열이 있으면 기본 `suffixes=('_x', '_y')`가 붙는데, 의미를 알기 어려우니 `suffixes=('_left', '_right')`처럼 명시하는 편이 낫다. `DataFrame.join`은 기본이 인덱스 기준이고 기본 how가 `'left'`라 `merge`와 다르며, `merge(left_index=True, right_index=True)`로 동일한 동작을 만들 수 있다. 인덱스 조인은 미리 `set_index`를 해 둔 경우 빠르고 간결하다."}],"code":{"c":"import pandas as pd\n\nleft  = pd.DataFrame({\"k\": [\"A\", \"A\"],      \"v\": [1, 2]})\nright = pd.DataFrame({\"k\": [\"A\", \"A\", \"A\"], \"w\": [10, 20, 30]})\n\nout = left.merge(right, on=\"k\")      # how='inner' 가 기본\nprint(len(out))                       # 2 * 3 = 6  <- 행 폭증!\n\nchk = left.merge(right, on=\"k\", how=\"left\", indicator=True)\nprint(chk[\"_merge\"].value_counts())\n\n# 가정이 깨지면 즉시 MergeError\n# left.merge(right, on=\"k\", validate=\"one_to_one\")","cap":"키가 양쪽 모두 중복이면 결과는 키별 곱(카티션)만큼 늘어난다."},"key":["merge의 기본 how는 'inner' 이며, 조인은 행을 줄이기도 늘리기도 한다","양쪽 키가 중복이면 결과 행 = 키별 (왼쪽 개수 × 오른쪽 개수)의 합","validate='one_to_many' 등을 쓰면 가정 위반 시 MergeError로 즉시 발견된다","indicator=True는 _merge 열로 left_only/right_only/both 매칭 상태를 보여준다","join은 기본이 인덱스 기준이고 기본 how='left' 라 merge와 다르다"]}},{"t":"키 중복과 카티션 곱","q":[{"t":"choice","k":"행 폭증 계산","q":"왼쪽에 키 'A'가 3행, 오른쪽에 키 'A'가 4행 있고 다른 키는 없을 때, `on='k'`로 inner merge한 결과의 행 수는?","ex":"many-to-many 조인은 키 그룹별 카티션 곱이므로 3 × 4 = 12행이 된다. 더하기(7)로 착각하는 실수가 매우 흔하다. 대용량에서는 이 곱셈 때문에 메모리가 순식간에 터진다.","o":["7","4","12","3"],"a":2},{"t":"choice","k":"코드 결과","q":"아래 코드의 출력값은?","ex":"키 A는 2 × 2 = 4행, 키 B는 1 × 1 = 1행이라 합계 5행이다. 원본과 같은 3행이 나올 거라는 기대가 대표적인 함정이다. 키별 곱의 합이라는 규칙을 기억하자.","code":"import pandas as pd\nl = pd.DataFrame({\"k\": [\"A\", \"A\", \"B\"], \"v\": [1, 2, 3]})\nr = pd.DataFrame({\"k\": [\"A\", \"A\", \"B\"], \"w\": [9, 8, 7]})\nprint(len(l.merge(r, on=\"k\")))","o":["3","5","6","9"],"a":1},{"t":"choice","k":"validate","q":"`validate='one_to_one'`을 지정했는데 왼쪽 키에 중복이 있으면 어떻게 되는가?","ex":"validate는 가정이 깨지면 MergeError를 던져 즉시 문제를 알려 준다. 자동으로 중복을 정리해 주지 않으므로 중복 원인을 직접 확인해야 한다. 조용한 행 폭증을 막는 가장 값싼 방어 수단이다.","o":["중복 행이 자동으로 제거된 뒤 조인된다","경고만 출력하고 카티션 곱 결과를 반환한다","중복 키에 해당하는 행이 조용히 제외된다","MergeError 예외가 발생하며 조인이 중단된다"],"a":3},{"t":"input","k":"유일성 사전 점검","q":"조인 전에 왼쪽 df의 키 열 'k'가 유일한지 확인하려 합니다. 빈칸 메서드는?\n`df['k'].____().sum() == 0`","ex":"`duplicated()`의 합이 0이면 키가 유일하다는 뜻이다. `df['k'].is_unique`로도 같은 판단을 할 수 있다. 조인 전 이 한 줄이 나중의 행 폭증 디버깅 몇 시간을 아껴 준다.","a":["duplicated","duplicated()","is_unique","df['k'].duplicated"]},{"t":"choice","k":"many_to_one","q":"`validate='many_to_one'`이 보장하는 조건은?","ex":"many_to_one은 '왼쪽 여러 행 : 오른쪽 한 행' 관계를 뜻하므로 오른쪽 키의 유일성만 검사한다. 이 경우 결과 행 수는 왼쪽과 같아 안전하다. 방향을 반대로 외우는 실수가 잦다.","o":["오른쪽 키가 유일해야 하고 왼쪽은 중복이어도 된다","왼쪽 키가 유일해야 하고 오른쪽은 중복이어도 된다","양쪽 키가 모두 유일해야 한다","양쪽 키가 모두 중복이어야 한다"],"a":0}],"th":{"sum":"`merge`의 결과 행 수는 키의 유일성에 달려 있어, 양쪽 키가 중복이면 각 키 그룹의 곱만큼 행이 폭증한다. `validate=`와 `indicator=`로 이런 사고를 사전에 잡아낼 수 있다.","body":[{"h":"how가 결정하는 것은 '어느 쪽 키를 남길지'","t":"pandas `merge`의 기본값은 `how='inner'`로 양쪽에 모두 있는 키만 남긴다. `left`는 왼쪽 키를 전부 보존하고 오른쪽에 없으면 NaN을 채우며, `outer`는 양쪽 합집합을 남긴다. 주의할 점은 `how`가 행 수의 **상한**을 정하지 않는다는 것이다 — 어떤 how를 써도 키가 중복이면 행은 늘어날 수 있다."},{"h":"many-to-many는 카티션 곱","t":"왼쪽에 키 A가 2행, 오른쪽에 키 A가 3행이면 결과에는 A가 **2 × 3 = 6행** 생긴다. 즉 결과 행 수는 각 키별 곱의 합이며, 조인 한 번으로 수백만 행이 튀어나오기도 한다. '조인하면 행이 줄어든다'는 직관은 키가 유일할 때만 성립하므로, 조인 전에 `df['key'].duplicated().sum()`으로 유일성을 확인하는 습관이 필요하다."},{"h":"validate와 indicator로 방어하기","t":"`validate='one_to_one'`, `'one_to_many'`, `'many_to_one'`을 지정하면 가정이 깨질 때 조용히 행이 늘어나는 대신 `MergeError`가 즉시 발생한다. `indicator=True`를 주면 `_merge` 열이 추가되어 각 행이 `left_only`, `right_only`, `both` 중 무엇인지 알려주므로 매칭 실패 규모를 바로 진단할 수 있다. 이 두 옵션은 비용이 거의 없으니 습관적으로 켜 두는 편이 좋다."},{"h":"suffixes와 index 기반 join","t":"양쪽에 같은 이름의 비-키 열이 있으면 기본 `suffixes=('_x', '_y')`가 붙는데, 의미를 알기 어려우니 `suffixes=('_left', '_right')`처럼 명시하는 편이 낫다. `DataFrame.join`은 기본이 인덱스 기준이고 기본 how가 `'left'`라 `merge`와 다르며, `merge(left_index=True, right_index=True)`로 동일한 동작을 만들 수 있다. 인덱스 조인은 미리 `set_index`를 해 둔 경우 빠르고 간결하다."}],"code":{"c":"import pandas as pd\n\nleft  = pd.DataFrame({\"k\": [\"A\", \"A\"],      \"v\": [1, 2]})\nright = pd.DataFrame({\"k\": [\"A\", \"A\", \"A\"], \"w\": [10, 20, 30]})\n\nout = left.merge(right, on=\"k\")      # how='inner' 가 기본\nprint(len(out))                       # 2 * 3 = 6  <- 행 폭증!\n\nchk = left.merge(right, on=\"k\", how=\"left\", indicator=True)\nprint(chk[\"_merge\"].value_counts())\n\n# 가정이 깨지면 즉시 MergeError\n# left.merge(right, on=\"k\", validate=\"one_to_one\")","cap":"키가 양쪽 모두 중복이면 결과는 키별 곱(카티션)만큼 늘어난다."},"key":["merge의 기본 how는 'inner' 이며, 조인은 행을 줄이기도 늘리기도 한다","양쪽 키가 중복이면 결과 행 = 키별 (왼쪽 개수 × 오른쪽 개수)의 합","validate='one_to_many' 등을 쓰면 가정 위반 시 MergeError로 즉시 발견된다","indicator=True는 _merge 열로 left_only/right_only/both 매칭 상태를 보여준다","join은 기본이 인덱스 기준이고 기본 how='left' 라 merge와 다르다"]}},{"t":"indicator · suffixes · 인덱스 조인","q":[{"t":"choice","k":"indicator 열","q":"`merge(..., indicator=True)`가 추가하는 열의 이름과 값으로 옳은 것은?","ex":"indicator=True는 `_merge`라는 category 열을 붙이고 각 행의 출처를 left_only, right_only, both로 표시한다. `value_counts()`로 매칭 실패 규모를 즉시 파악할 수 있다. 열 이름은 문자열을 넘겨 바꿀 수도 있다.","o":["'_flag' 열에 True/False 두 가지 값","'_join' 열에 inner/outer 두 가지 값","'_merge' 열에 left_only/right_only/both 세 가지 값","'_source' 열에 원본 DataFrame 변수명"],"a":2},{"t":"choice","k":"suffixes 기본","q":"양쪽에 같은 이름의 비-키 열 'v'가 있을 때 기본 merge 결과의 열 이름은?","ex":"기본 `suffixes=('_x', '_y')`가 적용되어 v_x, v_y가 된다. 의미를 알기 어려우니 `suffixes=('_order', '_user')`처럼 직접 지정하는 것이 좋다. 충돌 열이 여러 개면 혼란이 커진다.","o":["v_left, v_right","v_x, v_y","v_1, v_2","v, v.1"],"a":1},{"t":"input","k":"인덱스 조인 옵션","q":"merge에서 왼쪽의 인덱스를 조인 키로 쓰려면 지정해야 하는 파라미터 이름을 쓰세요.","ex":"`left_index=True`(오른쪽은 `right_index=True`)를 주면 열 대신 인덱스를 키로 사용한다. 둘 다 켜면 `join`과 사실상 같은 동작이 된다. `on=`과 인덱스 옵션을 동시에 잘못 조합하면 에러가 난다.","a":["left_index","left_index=True","left_index = True"]},{"t":"choice","k":"매칭 실패 진단","q":"아래 코드에서 `chk['_merge'].value_counts()`의 left_only 개수는?","ex":"왼쪽 키 1과 2는 오른쪽에 없으므로 left_only가 2개, 3과 4는 both가 된다. how='left'라서 오른쪽에만 있는 5는 결과에 나타나지 않아 right_only는 0이다. 양쪽 미매칭을 모두 보려면 how='outer'가 필요하다.","code":"import pandas as pd\na = pd.DataFrame({\"k\": [1, 2, 3, 4]})\nb = pd.DataFrame({\"k\": [3, 4, 5]})\nchk = a.merge(b, on=\"k\", how=\"left\", indicator=True)","o":["0","1","3","2"],"a":3},{"t":"choice","k":"안전한 조인 습관","q":"대용량 데이터에서 조인 사고를 예방하는 습관으로 가장 적절한 것은?","ex":"사전에 키 유일성을 점검하고 validate로 가정을 강제하며 indicator로 매칭 상태를 확인하는 것이 정석이다. 조인 후 drop_duplicates는 정상 데이터까지 지울 수 있고, outer는 오히려 행을 더 늘린다. inplace=True가 메모리를 아낀다는 것도 흔한 오해다.","o":["조인 후 무조건 drop_duplicates()를 호출해 정리한다","조인 전 키 유일성을 확인하고 validate와 indicator를 함께 쓴다","항상 how='outer'로 조인해 데이터 손실을 원천 차단한다","inplace=True를 붙여 메모리 사용을 줄인다"],"a":1}],"th":{"sum":"`merge`의 결과 행 수는 키의 유일성에 달려 있어, 양쪽 키가 중복이면 각 키 그룹의 곱만큼 행이 폭증한다. `validate=`와 `indicator=`로 이런 사고를 사전에 잡아낼 수 있다.","body":[{"h":"how가 결정하는 것은 '어느 쪽 키를 남길지'","t":"pandas `merge`의 기본값은 `how='inner'`로 양쪽에 모두 있는 키만 남긴다. `left`는 왼쪽 키를 전부 보존하고 오른쪽에 없으면 NaN을 채우며, `outer`는 양쪽 합집합을 남긴다. 주의할 점은 `how`가 행 수의 **상한**을 정하지 않는다는 것이다 — 어떤 how를 써도 키가 중복이면 행은 늘어날 수 있다."},{"h":"many-to-many는 카티션 곱","t":"왼쪽에 키 A가 2행, 오른쪽에 키 A가 3행이면 결과에는 A가 **2 × 3 = 6행** 생긴다. 즉 결과 행 수는 각 키별 곱의 합이며, 조인 한 번으로 수백만 행이 튀어나오기도 한다. '조인하면 행이 줄어든다'는 직관은 키가 유일할 때만 성립하므로, 조인 전에 `df['key'].duplicated().sum()`으로 유일성을 확인하는 습관이 필요하다."},{"h":"validate와 indicator로 방어하기","t":"`validate='one_to_one'`, `'one_to_many'`, `'many_to_one'`을 지정하면 가정이 깨질 때 조용히 행이 늘어나는 대신 `MergeError`가 즉시 발생한다. `indicator=True`를 주면 `_merge` 열이 추가되어 각 행이 `left_only`, `right_only`, `both` 중 무엇인지 알려주므로 매칭 실패 규모를 바로 진단할 수 있다. 이 두 옵션은 비용이 거의 없으니 습관적으로 켜 두는 편이 좋다."},{"h":"suffixes와 index 기반 join","t":"양쪽에 같은 이름의 비-키 열이 있으면 기본 `suffixes=('_x', '_y')`가 붙는데, 의미를 알기 어려우니 `suffixes=('_left', '_right')`처럼 명시하는 편이 낫다. `DataFrame.join`은 기본이 인덱스 기준이고 기본 how가 `'left'`라 `merge`와 다르며, `merge(left_index=True, right_index=True)`로 동일한 동작을 만들 수 있다. 인덱스 조인은 미리 `set_index`를 해 둔 경우 빠르고 간결하다."}],"code":{"c":"import pandas as pd\n\nleft  = pd.DataFrame({\"k\": [\"A\", \"A\"],      \"v\": [1, 2]})\nright = pd.DataFrame({\"k\": [\"A\", \"A\", \"A\"], \"w\": [10, 20, 30]})\n\nout = left.merge(right, on=\"k\")      # how='inner' 가 기본\nprint(len(out))                       # 2 * 3 = 6  <- 행 폭증!\n\nchk = left.merge(right, on=\"k\", how=\"left\", indicator=True)\nprint(chk[\"_merge\"].value_counts())\n\n# 가정이 깨지면 즉시 MergeError\n# left.merge(right, on=\"k\", validate=\"one_to_one\")","cap":"키가 양쪽 모두 중복이면 결과는 키별 곱(카티션)만큼 늘어난다."},"key":["merge의 기본 how는 'inner' 이며, 조인은 행을 줄이기도 늘리기도 한다","양쪽 키가 중복이면 결과 행 = 키별 (왼쪽 개수 × 오른쪽 개수)의 합","validate='one_to_many' 등을 쓰면 가정 위반 시 MergeError로 즉시 발견된다","indicator=True는 _merge 열로 left_only/right_only/both 매칭 상태를 보여준다","join은 기본이 인덱스 기준이고 기본 how='left' 라 merge와 다르다"]}}]},{"t":"groupby: agg · transform · apply · filter 완전 구분 (중급)","l":[{"t":"agg와 transform의 shape 차이","q":[{"t":"choice","k":"transform 길이","q":"원본 행이 100개, 그룹이 5개일 때 `df.groupby('g')['v'].transform('mean')`의 길이는?","ex":"transform은 원본과 동일한 길이·인덱스를 유지하므로 100이 된다. '그룹 수만큼 반환한다'는 것이 대표적인 오개념이며 그건 agg의 동작이다. 그래서 transform 결과는 바로 새 열로 대입할 수 있다.","o":["5","100","20","1"],"a":1},{"t":"choice","k":"agg 길이","q":"같은 상황에서 `df.groupby('g')['v'].agg('mean')`의 길이는?","ex":"agg는 각 그룹을 하나의 값으로 축약하므로 그룹 수인 5가 된다. 결과의 인덱스는 그룹 키가 되며 원본 인덱스는 사라진다. 원본에 붙이려면 merge하거나 transform을 쓰는 편이 낫다.","o":["100","1","5","95"],"a":2},{"t":"choice","k":"코드 출력","q":"아래 코드의 출력값은?","ex":"a 그룹 평균 2.0이 두 행에, b 그룹 평균 10.0이 한 행에 브로드캐스트되어 원본 3행이 유지된다. [2.0, 10.0]은 agg의 결과이고, 전체 평균이 채워지는 것도 아니다. 그룹별 값이 행마다 되돌아온다는 점이 핵심이다.","code":"import pandas as pd\ndf = pd.DataFrame({\"g\": [\"a\", \"a\", \"b\"], \"v\": [1, 3, 10]})\nprint(df.groupby(\"g\")[\"v\"].transform(\"mean\").tolist())","o":["[2.0, 10.0]","[2.0, 2.0, 10.0]","[1.0, 3.0, 10.0]","[4.666, 4.666, 4.666]"],"a":1},{"t":"input","k":"편차 계산","q":"각 행의 값에서 자기 그룹 평균을 뺀 편차 열을 만들려 합니다. 빈칸 메서드는?\n`df['v'] - df.groupby('g')['v'].____('mean')`","ex":"`transform`은 원본 인덱스에 맞춰 그룹 평균을 되돌려 주므로 뺄셈이 행 단위로 바로 정렬된다. agg를 쓰면 길이가 달라 인덱스가 맞지 않고 NaN이 대량 발생한다. 그룹 통계를 원본에 붙일 때의 표준 관용구다.","a":["transform","transform()","TRANSFORM",".transform"]},{"t":"choice","k":"as_index","q":"`df.groupby('g', as_index=False).agg({'v':'sum'})`의 결과 특징은?","ex":"as_index=False는 그룹 키를 인덱스로 올리지 않고 열로 유지해 이후 merge나 파일 저장이 편해진다. 기본값 True에서는 키가 인덱스가 되어 `reset_index()`를 따로 불러야 한다. 행 수는 여전히 그룹 수만큼으로 축약된다.","o":["그룹 키 g가 인덱스가 아닌 일반 열로 남는다","그룹 키 g가 인덱스가 되고 열에서는 사라진다","원본과 같은 행 수를 유지한다","정렬이 해제되어 그룹 순서가 무작위가 된다"],"a":0}],"th":{"sum":"`agg`는 그룹당 1행으로 축약하고, `transform`은 원본과 동일한 길이·인덱스를 유지하며, `filter`는 그룹 단위로 행 뭉치를 통째로 선택한다. 이 셋의 반환 shape 차이를 아는 것이 groupby 실력의 핵심이다.","body":[{"h":"agg는 축약, transform은 shape 보존","t":"`df.groupby('g')['v'].agg('mean')`은 그룹 개수만큼의 행을 가진 Series를 돌려준다. 반면 `df.groupby('g')['v'].transform('mean')`은 **원본과 같은 길이와 인덱스**를 유지한 채 각 행에 자기 그룹의 평균을 브로드캐스트해 준다. 그래서 그룹 평균을 새 열로 붙이거나 편차를 계산할 때는 merge 없이 transform 한 줄이면 충분하다."},{"h":"filter는 그룹 단위 선택","t":"`groupby('g').filter(lambda s: len(s) >= 3)`은 조건을 만족하는 그룹에 속한 원본 행 전체를 남기고 나머지 그룹은 통째로 버린다. 반환은 값이 아니라 **원본의 부분집합 DataFrame**이며, 인덱스도 원본 것을 그대로 유지한다. 행 단위 조건 필터링(불리언 인덱싱)과 그룹 단위 필터링을 혼동하지 말아야 한다."},{"h":"apply는 유연하지만 느리고 예측이 어렵다","t":"`groupby.apply`는 각 그룹을 통째로 파이썬 함수에 넘기므로 무엇이든 할 수 있지만, 반환 형태(스칼라/Series/DataFrame)에 따라 결과 shape이 달라져 예측이 어렵다. 또 그룹 수만큼 파이썬 함수 호출이 일어나 `agg`/`transform`의 최적화된 내부 경로보다 느리다. **먼저 agg나 transform으로 표현할 수 있는지 고민하고, 정말 안 될 때만 apply를 쓰자.**"},{"h":"as_index, 다중 집계, dropna","t":"`as_index=False`를 주면 그룹 키가 인덱스가 아니라 일반 열로 남아 이후 merge나 저장이 편해진다. 다중 집계는 `agg(mean_v=('v','mean'), max_v=('v','max'))` 형태의 named aggregation이 열 이름까지 깔끔하게 정해 준다. 그리고 groupby의 `dropna` 기본값은 True라 **키가 결측인 행은 집계에서 아예 빠지므로**, 결측 그룹도 보고 싶다면 `dropna=False`를 명시해야 한다."}],"code":{"c":"import pandas as pd\n\ndf = pd.DataFrame({\"g\": [\"a\", \"a\", \"b\", \"b\", \"b\"], \"v\": [1, 3, 10, 20, 30]})\n\nprint(len(df.groupby(\"g\")[\"v\"].agg(\"mean\")))        # 2  (그룹당 1행)\nprint(len(df.groupby(\"g\")[\"v\"].transform(\"mean\")))  # 5  (원본 shape 유지)\n\ndf[\"dev\"] = df[\"v\"] - df.groupby(\"g\")[\"v\"].transform(\"mean\")\n\nbig = df.groupby(\"g\").filter(lambda s: len(s) >= 3)  # b 그룹 3행만 남음\n\nout = df.groupby(\"g\", as_index=False).agg(\n    mean_v=(\"v\", \"mean\"), max_v=(\"v\", \"max\")\n)","cap":"agg는 그룹 수만큼, transform은 원본 행 수만큼 결과를 돌려준다."},"key":["transform은 원본과 같은 길이·인덱스를 반환한다 (그룹 수가 아니다)","agg는 그룹당 1행으로 축약한다","filter는 조건을 만족한 그룹의 원본 행 전체를 남긴다","apply는 그룹 단위 파이썬 호출이라 agg/transform보다 일반적으로 느리다","groupby의 dropna 기본값은 True — 키가 결측인 그룹은 제외된다"]}},{"t":"filter와 apply, 그리고 다중 집계","q":[{"t":"choice","k":"filter 반환","q":"`df.groupby('g').filter(lambda s: len(s) &gt;= 3)`이 반환하는 것은?","ex":"filter는 그룹을 통째로 남기거나 버리며 원본 행과 인덱스를 그대로 유지한 부분집합을 돌려준다. 축약(agg)이나 불리언 마스크와는 역할이 다르다. 예를 들어 '표본이 3개 이상인 그룹만 분석'할 때 쓴다.","o":["조건을 만족한 그룹의 개수를 담은 정수","그룹별 True/False를 담은 불리언 Series","그룹당 1행으로 축약된 요약 DataFrame","조건을 만족한 그룹에 속한 원본 행 전체로 이루어진 DataFrame"],"a":3},{"t":"choice","k":"filter 결과 행 수","q":"아래 코드의 출력값은?","ex":"b 그룹만 3행 이상이라 b의 원본 3행이 남는다. 그룹 개수(1)가 아니라 행 개수가 반환된다는 점이 포인트다. a 그룹은 통째로 제거된다.","code":"import pandas as pd\ndf = pd.DataFrame({\"g\": [\"a\", \"a\", \"b\", \"b\", \"b\"], \"v\": [1, 2, 3, 4, 5]})\nprint(len(df.groupby(\"g\").filter(lambda s: len(s) &gt;= 3)))","o":["3","1","2","5"],"a":0},{"t":"choice","k":"apply 성능","q":"`groupby.apply`에 대한 설명으로 옳은 것은?","ex":"apply는 각 그룹을 파이썬 함수에 넘기므로 최적화된 내부 경로를 타는 agg/transform보다 느린 것이 일반적이다. 반환 형태에 따라 결과 shape도 달라져 예측이 어렵다. 표현 가능한 경우 agg/transform을 우선하자.","o":["내부적으로 C로 최적화되어 agg보다 항상 빠르다","그룹마다 파이썬 함수를 호출하므로 agg/transform보다 일반적으로 느리다","반환 shape이 항상 그룹 수와 같아 예측하기 쉽다","transform과 완전히 동일한 결과를 더 짧게 쓰는 문법이다"],"a":1},{"t":"input","k":"그룹 단위 선택","q":"조건을 만족하는 그룹의 원본 행 전체를 남기는 groupby 메서드 이름을 쓰세요.","ex":"`filter`가 그룹 단위 선택을 담당한다. 행 단위 조건은 일반 불리언 인덱싱으로 처리하며 groupby가 필요 없다. 반환값은 원본의 부분집합이라 인덱스가 보존된다.","a":["filter","filter()","groupby.filter","FILTER"]},{"t":"choice","k":"named aggregation","q":"`df.groupby('g').agg(avg=('v','mean'), cnt=('v','size'))`의 결과 열 이름은?","ex":"named aggregation 문법에서는 키워드 이름이 곧 결과 열 이름이 되어 avg, cnt가 된다. 리스트로 여러 집계를 주면 MultiIndex 열이 생겨 다루기 번거롭다. 열 이름을 직접 정할 수 있어 후속 처리가 깔끔하다.","o":["v_mean, v_size","mean, size","avg, cnt","0, 1"],"a":2},{"t":"choice","k":"여러 집계","q":"한 열에 대해 합계와 최댓값을 동시에 구하는 올바른 방법은?","ex":"agg에 리스트를 넘기면 여러 집계를 한 번에 계산해 열로 펼쳐 준다. transform은 축약이 아니라 shape 보존용이라 이런 다중 집계에 맞지 않는다. 열 이름을 지정하고 싶으면 named aggregation을 쓴다.","o":["agg를 두 번 연달아 체이닝한다","transform(['sum','max'])를 사용한다","apply로만 가능하며 agg로는 불가능하다","agg(['sum','max'])처럼 집계 함수 리스트를 넘긴다"],"a":3}],"th":{"sum":"`agg`는 그룹당 1행으로 축약하고, `transform`은 원본과 동일한 길이·인덱스를 유지하며, `filter`는 그룹 단위로 행 뭉치를 통째로 선택한다. 이 셋의 반환 shape 차이를 아는 것이 groupby 실력의 핵심이다.","body":[{"h":"agg는 축약, transform은 shape 보존","t":"`df.groupby('g')['v'].agg('mean')`은 그룹 개수만큼의 행을 가진 Series를 돌려준다. 반면 `df.groupby('g')['v'].transform('mean')`은 **원본과 같은 길이와 인덱스**를 유지한 채 각 행에 자기 그룹의 평균을 브로드캐스트해 준다. 그래서 그룹 평균을 새 열로 붙이거나 편차를 계산할 때는 merge 없이 transform 한 줄이면 충분하다."},{"h":"filter는 그룹 단위 선택","t":"`groupby('g').filter(lambda s: len(s) >= 3)`은 조건을 만족하는 그룹에 속한 원본 행 전체를 남기고 나머지 그룹은 통째로 버린다. 반환은 값이 아니라 **원본의 부분집합 DataFrame**이며, 인덱스도 원본 것을 그대로 유지한다. 행 단위 조건 필터링(불리언 인덱싱)과 그룹 단위 필터링을 혼동하지 말아야 한다."},{"h":"apply는 유연하지만 느리고 예측이 어렵다","t":"`groupby.apply`는 각 그룹을 통째로 파이썬 함수에 넘기므로 무엇이든 할 수 있지만, 반환 형태(스칼라/Series/DataFrame)에 따라 결과 shape이 달라져 예측이 어렵다. 또 그룹 수만큼 파이썬 함수 호출이 일어나 `agg`/`transform`의 최적화된 내부 경로보다 느리다. **먼저 agg나 transform으로 표현할 수 있는지 고민하고, 정말 안 될 때만 apply를 쓰자.**"},{"h":"as_index, 다중 집계, dropna","t":"`as_index=False`를 주면 그룹 키가 인덱스가 아니라 일반 열로 남아 이후 merge나 저장이 편해진다. 다중 집계는 `agg(mean_v=('v','mean'), max_v=('v','max'))` 형태의 named aggregation이 열 이름까지 깔끔하게 정해 준다. 그리고 groupby의 `dropna` 기본값은 True라 **키가 결측인 행은 집계에서 아예 빠지므로**, 결측 그룹도 보고 싶다면 `dropna=False`를 명시해야 한다."}],"code":{"c":"import pandas as pd\n\ndf = pd.DataFrame({\"g\": [\"a\", \"a\", \"b\", \"b\", \"b\"], \"v\": [1, 3, 10, 20, 30]})\n\nprint(len(df.groupby(\"g\")[\"v\"].agg(\"mean\")))        # 2  (그룹당 1행)\nprint(len(df.groupby(\"g\")[\"v\"].transform(\"mean\")))  # 5  (원본 shape 유지)\n\ndf[\"dev\"] = df[\"v\"] - df.groupby(\"g\")[\"v\"].transform(\"mean\")\n\nbig = df.groupby(\"g\").filter(lambda s: len(s) >= 3)  # b 그룹 3행만 남음\n\nout = df.groupby(\"g\", as_index=False).agg(\n    mean_v=(\"v\", \"mean\"), max_v=(\"v\", \"max\")\n)","cap":"agg는 그룹 수만큼, transform은 원본 행 수만큼 결과를 돌려준다."},"key":["transform은 원본과 같은 길이·인덱스를 반환한다 (그룹 수가 아니다)","agg는 그룹당 1행으로 축약한다","filter는 조건을 만족한 그룹의 원본 행 전체를 남긴다","apply는 그룹 단위 파이썬 호출이라 agg/transform보다 일반적으로 느리다","groupby의 dropna 기본값은 True — 키가 결측인 그룹은 제외된다"]}},{"t":"결측 그룹과 실전 패턴","q":[{"t":"choice","k":"dropna 기본","q":"groupby의 `dropna` 파라미터 기본값과 그 효과는?","ex":"기본값 True에서는 그룹 키가 NaN인 행이 결과에서 통째로 빠져 합계가 조용히 줄어든다. 결측 그룹도 보고 싶다면 `dropna=False`를 명시해야 한다. 집계 총합이 원본과 안 맞을 때 가장 먼저 의심할 지점이다.","o":["True — 그룹 키가 결측인 행은 집계에서 제외된다","False — 결측 키도 하나의 그룹으로 집계된다","True — 값이 결측인 행만 제외하고 키 결측은 유지된다","None — 결측이 있으면 에러를 발생시킨다"],"a":0},{"t":"choice","k":"결측 키 집계","q":"아래 코드의 출력값은?","ex":"dropna 기본값이 True라 NaN 키 행이 빠지고 a, b 두 그룹만 남아 길이는 2다. `dropna=False`를 주면 NaN 그룹이 추가되어 3이 된다. 총합이 6이 아니라 4가 되는 점도 함께 주의하자.","code":"import numpy as np, pandas as pd\ndf = pd.DataFrame({\"g\": [\"a\", np.nan, \"b\"], \"v\": [1, 2, 3]})\nprint(len(df.groupby(\"g\")[\"v\"].sum()))","o":["3","1","2","0"],"a":2},{"t":"input","k":"결측 그룹 포함","q":"그룹 키가 NaN인 행도 하나의 그룹으로 집계하려면 groupby에 어떤 인자를 주어야 하는지 쓰세요.","ex":"`groupby('g', dropna=False)`를 쓰면 NaN도 별도의 그룹으로 나타난다. 기본값 True는 결측 키 행을 조용히 버리므로 집계 총합이 원본과 달라진다. 데이터 품질 점검 시 특히 유용한 옵션이다.","a":["dropna=False","dropna = False","dropna=false","dropna"]},{"t":"choice","k":"그룹 비율 계산","q":"각 행의 값이 자기 그룹 합계에서 차지하는 비율 열을 만들 때 가장 적절한 코드는?","ex":"transform이 원본 인덱스에 맞춘 그룹 합계를 돌려주므로 행 단위 나눗셈이 그대로 성립한다. agg는 길이가 그룹 수로 줄어 인덱스가 어긋나고 NaN이 대량 생긴다. filter에 문자열 집계 이름을 넘기는 문법은 존재하지 않는다.","o":["df['v'] / df.groupby('g')['v'].agg('sum')","df['v'] / df.groupby('g')['v'].filter('sum')","df.groupby('g')['v'].apply(sum) / df['v']","df['v'] / df.groupby('g')['v'].transform('sum')"],"a":3},{"t":"choice","k":"메서드 선택","q":"'그룹 평균을 새 열로 추가'와 '그룹별 요약 표 생성'에 각각 알맞은 메서드 조합은?","ex":"원본 shape을 유지해야 하는 새 열 추가에는 transform, 그룹당 1행 요약표에는 agg가 맞다. 순서를 뒤집으면 길이 불일치로 NaN이 쏟아진다. 반환 shape 기준으로 메서드를 고르는 습관이 중요하다.","o":["transform / agg","agg / transform","filter / apply","apply / filter"],"a":0}],"th":{"sum":"`agg`는 그룹당 1행으로 축약하고, `transform`은 원본과 동일한 길이·인덱스를 유지하며, `filter`는 그룹 단위로 행 뭉치를 통째로 선택한다. 이 셋의 반환 shape 차이를 아는 것이 groupby 실력의 핵심이다.","body":[{"h":"agg는 축약, transform은 shape 보존","t":"`df.groupby('g')['v'].agg('mean')`은 그룹 개수만큼의 행을 가진 Series를 돌려준다. 반면 `df.groupby('g')['v'].transform('mean')`은 **원본과 같은 길이와 인덱스**를 유지한 채 각 행에 자기 그룹의 평균을 브로드캐스트해 준다. 그래서 그룹 평균을 새 열로 붙이거나 편차를 계산할 때는 merge 없이 transform 한 줄이면 충분하다."},{"h":"filter는 그룹 단위 선택","t":"`groupby('g').filter(lambda s: len(s) >= 3)`은 조건을 만족하는 그룹에 속한 원본 행 전체를 남기고 나머지 그룹은 통째로 버린다. 반환은 값이 아니라 **원본의 부분집합 DataFrame**이며, 인덱스도 원본 것을 그대로 유지한다. 행 단위 조건 필터링(불리언 인덱싱)과 그룹 단위 필터링을 혼동하지 말아야 한다."},{"h":"apply는 유연하지만 느리고 예측이 어렵다","t":"`groupby.apply`는 각 그룹을 통째로 파이썬 함수에 넘기므로 무엇이든 할 수 있지만, 반환 형태(스칼라/Series/DataFrame)에 따라 결과 shape이 달라져 예측이 어렵다. 또 그룹 수만큼 파이썬 함수 호출이 일어나 `agg`/`transform`의 최적화된 내부 경로보다 느리다. **먼저 agg나 transform으로 표현할 수 있는지 고민하고, 정말 안 될 때만 apply를 쓰자.**"},{"h":"as_index, 다중 집계, dropna","t":"`as_index=False`를 주면 그룹 키가 인덱스가 아니라 일반 열로 남아 이후 merge나 저장이 편해진다. 다중 집계는 `agg(mean_v=('v','mean'), max_v=('v','max'))` 형태의 named aggregation이 열 이름까지 깔끔하게 정해 준다. 그리고 groupby의 `dropna` 기본값은 True라 **키가 결측인 행은 집계에서 아예 빠지므로**, 결측 그룹도 보고 싶다면 `dropna=False`를 명시해야 한다."}],"code":{"c":"import pandas as pd\n\ndf = pd.DataFrame({\"g\": [\"a\", \"a\", \"b\", \"b\", \"b\"], \"v\": [1, 3, 10, 20, 30]})\n\nprint(len(df.groupby(\"g\")[\"v\"].agg(\"mean\")))        # 2  (그룹당 1행)\nprint(len(df.groupby(\"g\")[\"v\"].transform(\"mean\")))  # 5  (원본 shape 유지)\n\ndf[\"dev\"] = df[\"v\"] - df.groupby(\"g\")[\"v\"].transform(\"mean\")\n\nbig = df.groupby(\"g\").filter(lambda s: len(s) >= 3)  # b 그룹 3행만 남음\n\nout = df.groupby(\"g\", as_index=False).agg(\n    mean_v=(\"v\", \"mean\"), max_v=(\"v\", \"max\")\n)","cap":"agg는 그룹 수만큼, transform은 원본 행 수만큼 결과를 돌려준다."},"key":["transform은 원본과 같은 길이·인덱스를 반환한다 (그룹 수가 아니다)","agg는 그룹당 1행으로 축약한다","filter는 조건을 만족한 그룹의 원본 행 전체를 남긴다","apply는 그룹 단위 파이썬 호출이라 agg/transform보다 일반적으로 느리다","groupby의 dropna 기본값은 True — 키가 결측인 그룹은 제외된다"]}}]},{"t":"시계열 리샘플링·롤링과 성능·메모리 심화","l":[{"t":"resample과 DatetimeIndex","q":[{"t":"choice","k":"resample 전제","q":"`df.resample('D').mean()`을 쓰기 위한 전제 조건으로 옳은 것은?","ex":"resample은 시간 축 기준 재구간화라 DatetimeIndex(또는 PeriodIndex)가 필요하고, 날짜가 열에 있으면 `on='ts'`로 알려 줘야 한다. 문자열 날짜는 먼저 `pd.to_datetime`으로 변환해야 한다. 중간에 빠진 날짜가 있어도 리샘플 자체는 동작하며 빈 구간은 NaN이 된다.","o":["인덱스가 정수 RangeIndex여야 한다","인덱스가 DatetimeIndex이거나 on=으로 날짜 열을 지정해야 한다","날짜 열이 문자열 dtype이어야 자동 인식된다","데이터가 결측 없이 매일 채워져 있어야 한다"],"a":1},{"t":"choice","k":"리샘플 결과 길이","q":"아래 코드의 출력값은?","ex":"6일치를 2일 단위로 묶으면 3개의 버킷이 되어 길이는 3이다. resample은 다운샘플링 시 행을 줄이고 업샘플링 시 늘린다. 원본 길이가 유지된다고 착각하기 쉽다.","code":"import pandas as pd\nidx = pd.date_range(\"2024-01-01\", periods=6, freq=\"D\")\ns = pd.Series(range(6), index=idx)\nprint(len(s.resample(\"2D\").mean()))","o":["6","2","12","3"],"a":3},{"t":"input","k":"날짜 변환 함수","q":"문자열 날짜 열을 datetime 타입으로 바꾸는 pandas 함수 이름을 쓰세요.","ex":"`pd.to_datetime`으로 변환한 뒤 `set_index`를 하면 resample을 쓸 수 있다. object dtype 문자열 상태로는 시간 연산이 전혀 되지 않는다. 형식이 일정하면 `format=`을 주는 편이 훨씬 빠르다.","a":["pd.to_datetime","to_datetime","pandas.to_datetime","to_datetime()"]},{"t":"choice","k":"업샘플링","q":"시간당 데이터를 `resample('D')`로 묶는 작업에 대한 설명으로 옳은 것은?","ex":"시간 단위를 일 단위로 넓히는 것은 다운샘플링이라 mean, sum 같은 집계가 필요하다. 반대로 일 단위를 시간 단위로 쪼개면 업샘플링이라 ffill이나 interpolate로 빈 값을 채운다. 인덱스는 여전히 DatetimeIndex로 유지된다.","o":["다운샘플링이며 집계 함수로 값을 요약해야 한다","업샘플링이며 결측을 채우는 방법을 지정해야 한다","행 수가 원본과 항상 동일하게 유지된다","인덱스가 정수로 초기화된다"],"a":0},{"t":"choice","k":"on= 사용","q":"날짜가 인덱스가 아니라 'ts' 열에 들어 있을 때 일 단위 평균을 구하는 올바른 코드는?","ex":"`on='ts'`가 열을 시간 축으로 지정하는 정식 인자다. key, axis, by는 resample의 인자가 아니라 다른 API의 것이다. `set_index('ts')` 후 resample하는 방법도 동일한 결과를 준다.","o":["df.resample('D', key='ts').mean()","df.resample('D', axis='ts').mean()","df.resample('D', by='ts').mean()","df.resample('D', on='ts').mean()"],"a":3}],"th":{"sum":"`resample`과 `rolling`은 DatetimeIndex와 윈도우 규칙을 정확히 이해해야 미래 정보 누수 없이 쓸 수 있고, 성능은 벡터화와 dtype 선택에서 갈린다.","body":[{"h":"resample에는 DatetimeIndex가 필요하다","t":"`df.resample('D').mean()`은 인덱스가 DatetimeIndex(또는 PeriodIndex)여야 동작하며, 아니면 `TypeError`가 난다. 날짜가 일반 열에 있다면 `pd.to_datetime` 후 `set_index`를 하거나 `resample('D', on='ts')`처럼 `on=`으로 열을 지정하면 된다. 리샘플링은 시간 축을 규칙적인 간격으로 다시 자르는 작업이라, 데이터가 없는 구간에는 NaN 버킷이 생길 수 있다."},{"h":"rolling의 앞부분 NaN과 min_periods","t":"`s.rolling(3).mean()`은 `min_periods` 기본값이 window와 같아서 **앞의 2개가 NaN**이 된다. 값을 일찍부터 얻고 싶으면 `rolling(3, min_periods=1)`을 주면 되지만, 이 경우 초반 값은 1~2개 샘플만으로 계산된 불안정한 평균이라는 점을 알고 써야 한다. 롤링은 기본적으로 현재 행을 포함한 과거 방향 윈도우다."},{"h":"shift로 미래 정보 누수 막기","t":"예측 모델의 피처로 롤링 통계를 쓸 때 현재 행의 타깃이 윈도우에 포함되면 미래 정보가 새어 들어간다. `s.rolling(3).mean().shift(1)`처럼 한 칸 밀어 **현재 시점 이전 정보만** 담기게 해야 한다. 이 누수는 검증 성능만 비정상적으로 높게 만들고 실서비스에서 무너지므로, 시계열 검증은 반드시 시간 순서를 지켜 분할해야 한다."},{"h":"성능: 벡터화, dtype, 순회 방식","t":"`df.apply(func, axis=1)`은 행마다 파이썬 함수를 호출하는 루프에 가까워 `df['a'] + df['b']` 같은 벡터화 연산보다 일반적으로 훨씬 느리다. 순회가 꼭 필요하면 `iterrows`(행마다 Series 객체 생성)보다 `itertuples`가 빠르지만, 그보다 먼저 벡터화를 고민하는 것이 순서다. 메모리 측면에서는 카디널리티가 낮은 object dtype 문자열 열을 `category`로 바꾸면 사용량이 크게 줄고, 정수 열은 `int64` 대신 `int32`/`int16`으로 다운캐스트할 수 있다. 참고로 `inplace=True`는 대부분 내부 복사를 피하지 못하므로 '항상 메모리를 아낀다'는 말은 사실이 아니다."}],"code":{"c":"import numpy as np, pandas as pd\n\nidx = pd.date_range(\"2024-01-01\", periods=6, freq=\"D\")\ns = pd.Series([1, 2, 3, 4, 5, 6], index=idx)\n\nprint(s.rolling(3).mean().isna().sum())              # 2  (앞 2개 NaN)\nprint(s.rolling(3, min_periods=1).mean().isna().sum())  # 0\n\nfeat = s.rolling(3).mean().shift(1)   # 미래 정보 누수 방지\nprint(s.resample(\"2D\").mean().shape)  # DatetimeIndex 필요\n\ndf = pd.DataFrame({\"city\": [\"서울\", \"부산\"] * 5000})\nprint(df[\"city\"].memory_usage(deep=True) >\n      df[\"city\"].astype(\"category\").memory_usage(deep=True))  # True","cap":"rolling 앞부분 NaN, shift로 누수 차단, category로 메모리 절감."},"key":["resample은 DatetimeIndex가 필요하며, 열에 있으면 on=으로 지정한다","rolling(3).mean()은 min_periods 기본이 window라 앞 2개가 NaN이다","롤링 피처는 shift(1)로 밀어야 현재 시점 미래 정보 누수를 막는다","apply(axis=1)는 파이썬 루프에 가까워 벡터화 연산보다 일반적으로 느리다","카디널리티가 낮은 문자열 열을 category로 바꾸면 메모리가 크게 준다","iterrows보다 itertuples가 빠르지만, 벡터화가 최우선이다"]}},{"t":"rolling 윈도우와 누수 방지","q":[{"t":"choice","k":"앞부분 NaN","q":"길이 10인 Series에 `rolling(3).mean()`을 적용하면 NaN은 몇 개인가?","ex":"min_periods 기본값이 window와 같아 관측이 3개 모이기 전인 앞 2개가 NaN이 된다. window 개수만큼 NaN이 생긴다고 착각하기 쉽다. 일반적으로 NaN 개수는 window - 1이다.","o":["2","3","0","9"],"a":0},{"t":"choice","k":"min_periods","q":"아래 코드의 출력값은?","ex":"min_periods=1이면 관측이 하나만 있어도 계산하므로 NaN이 전혀 없다. 다만 첫 두 값은 1~2개 샘플로만 계산된 불안정한 평균이라는 점을 인지해야 한다. 기본값이었다면 NaN이 2개였을 것이다.","code":"import pandas as pd\ns = pd.Series([1, 2, 3, 4, 5])\nprint(s.rolling(3, min_periods=1).mean().isna().sum())","o":["2","1","0","3"],"a":2},{"t":"input","k":"누수 차단 메서드","q":"롤링 평균 피처에서 현재 시점 정보가 섞이지 않게 한 칸 뒤로 미는 메서드 이름을 쓰세요.","ex":"`rolling(3).mean().shift(1)`처럼 shift로 밀면 윈도우에 현재 행의 값이 포함되지 않아 미래 정보 누수가 차단된다. 누수가 있으면 검증 점수만 비정상적으로 좋아지고 실서비스에서 무너진다. 시계열 분할도 시간 순서를 지켜야 한다.","a":["shift","shift(1)","shift()",".shift(1)"]},{"t":"choice","k":"누수 개념","q":"시계열 예측에서 '미래 정보 누수'에 해당하는 것은?","ex":"예측 시점에 알 수 없어야 할 미래 값이 피처에 들어가는 것이 누수다. 앞부분 NaN이나 dtype 승격은 누수가 아니라 별개의 이슈다. center=True 롤링이나 shift 누락이 전형적인 원인이다.","o":["학습 데이터에 결측이 많아 dropna로 행을 지운 경우","예측 시점 이후의 값이 포함된 롤링 통계를 피처로 쓴 경우","롤링 윈도우 앞부분에 NaN이 생기는 경우","int 열이 NaN 때문에 float64로 승격된 경우"],"a":1},{"t":"choice","k":"롤링 방향","q":"`s.rolling(5).mean()`의 기본 윈도우 방향으로 옳은 것은?","ex":"기본은 현재 행을 마지막으로 하는 과거 방향 윈도우다. 중앙 정렬은 `center=True`를 명시해야 하며, 이 옵션은 미래 값을 끌어와 예측 피처로는 위험하다. 방향을 정확히 아는 것이 누수 방지의 출발점이다.","o":["현재 행을 기준으로 앞뒤 대칭인 중앙 정렬 윈도우","현재 행 이후의 미래 5개를 보는 윈도우","현재 행을 포함한 과거 방향 5개 윈도우","전체 구간을 5등분한 고정 구간"],"a":2}],"th":{"sum":"`resample`과 `rolling`은 DatetimeIndex와 윈도우 규칙을 정확히 이해해야 미래 정보 누수 없이 쓸 수 있고, 성능은 벡터화와 dtype 선택에서 갈린다.","body":[{"h":"resample에는 DatetimeIndex가 필요하다","t":"`df.resample('D').mean()`은 인덱스가 DatetimeIndex(또는 PeriodIndex)여야 동작하며, 아니면 `TypeError`가 난다. 날짜가 일반 열에 있다면 `pd.to_datetime` 후 `set_index`를 하거나 `resample('D', on='ts')`처럼 `on=`으로 열을 지정하면 된다. 리샘플링은 시간 축을 규칙적인 간격으로 다시 자르는 작업이라, 데이터가 없는 구간에는 NaN 버킷이 생길 수 있다."},{"h":"rolling의 앞부분 NaN과 min_periods","t":"`s.rolling(3).mean()`은 `min_periods` 기본값이 window와 같아서 **앞의 2개가 NaN**이 된다. 값을 일찍부터 얻고 싶으면 `rolling(3, min_periods=1)`을 주면 되지만, 이 경우 초반 값은 1~2개 샘플만으로 계산된 불안정한 평균이라는 점을 알고 써야 한다. 롤링은 기본적으로 현재 행을 포함한 과거 방향 윈도우다."},{"h":"shift로 미래 정보 누수 막기","t":"예측 모델의 피처로 롤링 통계를 쓸 때 현재 행의 타깃이 윈도우에 포함되면 미래 정보가 새어 들어간다. `s.rolling(3).mean().shift(1)`처럼 한 칸 밀어 **현재 시점 이전 정보만** 담기게 해야 한다. 이 누수는 검증 성능만 비정상적으로 높게 만들고 실서비스에서 무너지므로, 시계열 검증은 반드시 시간 순서를 지켜 분할해야 한다."},{"h":"성능: 벡터화, dtype, 순회 방식","t":"`df.apply(func, axis=1)`은 행마다 파이썬 함수를 호출하는 루프에 가까워 `df['a'] + df['b']` 같은 벡터화 연산보다 일반적으로 훨씬 느리다. 순회가 꼭 필요하면 `iterrows`(행마다 Series 객체 생성)보다 `itertuples`가 빠르지만, 그보다 먼저 벡터화를 고민하는 것이 순서다. 메모리 측면에서는 카디널리티가 낮은 object dtype 문자열 열을 `category`로 바꾸면 사용량이 크게 줄고, 정수 열은 `int64` 대신 `int32`/`int16`으로 다운캐스트할 수 있다. 참고로 `inplace=True`는 대부분 내부 복사를 피하지 못하므로 '항상 메모리를 아낀다'는 말은 사실이 아니다."}],"code":{"c":"import numpy as np, pandas as pd\n\nidx = pd.date_range(\"2024-01-01\", periods=6, freq=\"D\")\ns = pd.Series([1, 2, 3, 4, 5, 6], index=idx)\n\nprint(s.rolling(3).mean().isna().sum())              # 2  (앞 2개 NaN)\nprint(s.rolling(3, min_periods=1).mean().isna().sum())  # 0\n\nfeat = s.rolling(3).mean().shift(1)   # 미래 정보 누수 방지\nprint(s.resample(\"2D\").mean().shape)  # DatetimeIndex 필요\n\ndf = pd.DataFrame({\"city\": [\"서울\", \"부산\"] * 5000})\nprint(df[\"city\"].memory_usage(deep=True) >\n      df[\"city\"].astype(\"category\").memory_usage(deep=True))  # True","cap":"rolling 앞부분 NaN, shift로 누수 차단, category로 메모리 절감."},"key":["resample은 DatetimeIndex가 필요하며, 열에 있으면 on=으로 지정한다","rolling(3).mean()은 min_periods 기본이 window라 앞 2개가 NaN이다","롤링 피처는 shift(1)로 밀어야 현재 시점 미래 정보 누수를 막는다","apply(axis=1)는 파이썬 루프에 가까워 벡터화 연산보다 일반적으로 느리다","카디널리티가 낮은 문자열 열을 category로 바꾸면 메모리가 크게 준다","iterrows보다 itertuples가 빠르지만, 벡터화가 최우선이다"]}},{"t":"벡터화·dtype·메모리 최적화","q":[{"t":"choice","k":"성능 비교","q":"동일한 덧셈 연산을 수행할 때 일반적으로 가장 빠른 방식은?","ex":"벡터화 연산은 C 레벨에서 배열 전체를 한 번에 처리해 가장 빠르다. apply(axis=1)와 iterrows는 행마다 파이썬 객체를 만들어 특히 느리며, itertuples는 그중 상대적으로 빠를 뿐이다. 'apply가 벡터화보다 빠르다'는 흔한 오해다.","o":["df['a'] + df['b'] (벡터화)","df.apply(lambda r: r['a'] + r['b'], axis=1)","for _, r in df.iterrows() 루프","for r in df.itertuples() 루프"],"a":0},{"t":"choice","k":"순회 방식","q":"굳이 행 순회가 필요할 때 `iterrows`보다 `itertuples`가 빠른 이유는?","ex":"iterrows는 행마다 Series를 생성해 오버헤드가 크고 dtype도 섞이면 object로 승격된다. itertuples는 가벼운 namedtuple을 돌려줘 훨씬 빠르다. 다만 둘 다 벡터화보다는 느리므로 먼저 벡터화를 고민해야 한다.","o":["여러 CPU 코어로 자동 병렬 처리하기 때문","행마다 Series 객체를 만들지 않고 가벼운 namedtuple을 넘기기 때문","내부적으로 데이터를 미리 정렬해 캐시 적중률을 높이기 때문","결측이 있는 행을 자동으로 건너뛰기 때문"],"a":1},{"t":"choice","k":"category 절감","q":"100만 행짜리 'city' 열에 서로 다른 값이 20개뿐일 때 메모리를 크게 줄이는 방법은?","ex":"카디널리티가 낮은 문자열 열은 category로 바꾸면 값 사전 + 정수 코드로 저장되어 메모리가 크게 준다. object dtype은 각 문자열마다 파이썬 객체 포인터를 유지해 훨씬 무겁다. inplace=True는 대부분 내부 복사를 피하지 못해 메모리 절감 수단이 아니다.","o":["float64로 변환한다","inplace=True 옵션을 붙여 연산한다","astype('category')로 변환한다","object dtype 그대로 두는 것이 가장 작다"],"a":2},{"t":"input","k":"dtype 변환","q":"문자열 열을 category dtype으로 바꾸는 메서드 호출을 쓰세요. (예: df['c'].____)","ex":"`df['c'] = df['c'].astype('category')`가 표준 방법이다. 고유값이 적을수록 절감 효과가 크고, 반대로 거의 모든 값이 고유하면 오히려 커질 수 있다. 변환 후에는 문자열 메서드 사용 시 `.cat` 접근자나 재변환이 필요할 수 있다.","a":["astype('category')","astype(\"category\")","astype(category)","astype"]},{"t":"choice","k":"메모리 점검","q":"문자열 열의 실제 메모리 사용량을 정확히 보려면 어떻게 해야 하는가?","ex":"object dtype은 포인터만 배열에 담고 실제 문자열은 밖에 있어 deep=True를 줘야 실제 사용량이 집계된다. 기본 출력은 포인터 크기만 세어 과소평가된다. 최적화 전후 비교에 반드시 필요한 옵션이다.","o":["df.shape로 행·열 개수만 확인하면 충분하다","df.info(memory_usage='deep') 또는 memory_usage(deep=True)를 쓴다","len(df)에 8을 곱해 계산한다","df.describe()의 count 값을 확인한다"],"a":1},{"t":"choice","k":"inplace 오해","q":"`inplace=True`에 대한 설명으로 옳은 것은?","ex":"inplace=True는 내부적으로 새 객체를 만든 뒤 참조를 바꾸는 경우가 많아 메모리 이득이 없는 경우가 흔하다. 게다가 None을 반환해 메서드 체이닝이 끊기고 가독성도 떨어진다. 실제 절감은 dtype 최적화나 열 선별 로드에서 온다.","o":["항상 복사를 피해 메모리를 절약해 준다","연산 속도를 항상 두 배 이상 빠르게 만든다","대부분 내부 복사가 일어나 메모리 절감 효과가 보장되지 않는다","체이닝이 가능해져 코드 가독성이 높아진다"],"a":2}],"th":{"sum":"`resample`과 `rolling`은 DatetimeIndex와 윈도우 규칙을 정확히 이해해야 미래 정보 누수 없이 쓸 수 있고, 성능은 벡터화와 dtype 선택에서 갈린다.","body":[{"h":"resample에는 DatetimeIndex가 필요하다","t":"`df.resample('D').mean()`은 인덱스가 DatetimeIndex(또는 PeriodIndex)여야 동작하며, 아니면 `TypeError`가 난다. 날짜가 일반 열에 있다면 `pd.to_datetime` 후 `set_index`를 하거나 `resample('D', on='ts')`처럼 `on=`으로 열을 지정하면 된다. 리샘플링은 시간 축을 규칙적인 간격으로 다시 자르는 작업이라, 데이터가 없는 구간에는 NaN 버킷이 생길 수 있다."},{"h":"rolling의 앞부분 NaN과 min_periods","t":"`s.rolling(3).mean()`은 `min_periods` 기본값이 window와 같아서 **앞의 2개가 NaN**이 된다. 값을 일찍부터 얻고 싶으면 `rolling(3, min_periods=1)`을 주면 되지만, 이 경우 초반 값은 1~2개 샘플만으로 계산된 불안정한 평균이라는 점을 알고 써야 한다. 롤링은 기본적으로 현재 행을 포함한 과거 방향 윈도우다."},{"h":"shift로 미래 정보 누수 막기","t":"예측 모델의 피처로 롤링 통계를 쓸 때 현재 행의 타깃이 윈도우에 포함되면 미래 정보가 새어 들어간다. `s.rolling(3).mean().shift(1)`처럼 한 칸 밀어 **현재 시점 이전 정보만** 담기게 해야 한다. 이 누수는 검증 성능만 비정상적으로 높게 만들고 실서비스에서 무너지므로, 시계열 검증은 반드시 시간 순서를 지켜 분할해야 한다."},{"h":"성능: 벡터화, dtype, 순회 방식","t":"`df.apply(func, axis=1)`은 행마다 파이썬 함수를 호출하는 루프에 가까워 `df['a'] + df['b']` 같은 벡터화 연산보다 일반적으로 훨씬 느리다. 순회가 꼭 필요하면 `iterrows`(행마다 Series 객체 생성)보다 `itertuples`가 빠르지만, 그보다 먼저 벡터화를 고민하는 것이 순서다. 메모리 측면에서는 카디널리티가 낮은 object dtype 문자열 열을 `category`로 바꾸면 사용량이 크게 줄고, 정수 열은 `int64` 대신 `int32`/`int16`으로 다운캐스트할 수 있다. 참고로 `inplace=True`는 대부분 내부 복사를 피하지 못하므로 '항상 메모리를 아낀다'는 말은 사실이 아니다."}],"code":{"c":"import numpy as np, pandas as pd\n\nidx = pd.date_range(\"2024-01-01\", periods=6, freq=\"D\")\ns = pd.Series([1, 2, 3, 4, 5, 6], index=idx)\n\nprint(s.rolling(3).mean().isna().sum())              # 2  (앞 2개 NaN)\nprint(s.rolling(3, min_periods=1).mean().isna().sum())  # 0\n\nfeat = s.rolling(3).mean().shift(1)   # 미래 정보 누수 방지\nprint(s.resample(\"2D\").mean().shape)  # DatetimeIndex 필요\n\ndf = pd.DataFrame({\"city\": [\"서울\", \"부산\"] * 5000})\nprint(df[\"city\"].memory_usage(deep=True) >\n      df[\"city\"].astype(\"category\").memory_usage(deep=True))  # True","cap":"rolling 앞부분 NaN, shift로 누수 차단, category로 메모리 절감."},"key":["resample은 DatetimeIndex가 필요하며, 열에 있으면 on=으로 지정한다","rolling(3).mean()은 min_periods 기본이 window라 앞 2개가 NaN이다","롤링 피처는 shift(1)로 밀어야 현재 시점 미래 정보 누수를 막는다","apply(axis=1)는 파이썬 루프에 가까워 벡터화 연산보다 일반적으로 느리다","카디널리티가 낮은 문자열 열을 category로 바꾸면 메모리가 크게 준다","iterrows보다 itertuples가 빠르지만, 벡터화가 최우선이다"]}}]}]);
