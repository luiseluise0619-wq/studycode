module.exports = {
  track: "mobile", guide: "", xp: 80,
  unit: "실행형 실전 — 시뮬레이터에서는 안 보이는 버그 고치기",
  source: "./dbg_mobile.cjs",
  lessons: [
    { t: "화면과 목록이 어긋나는 곳", n: 7,
      th: { sum: "목록 화면의 버그는 **자리 번호를 믿었을 때**와 **도착 순서를 믿었을 때** 난다 — 둘 다 언제든 바뀌는 값이다.",
        body: [
          { h: "자리 번호는 상태가 아니다", t: "새 글이 오고, 정렬이 바뀌고, 필터가 걸리면 <b>자리 번호는 전부 밀린다</b>. 선택·확장·스크롤 위치는 모두 <code>id</code> 로 붙들어야 하고, 리스트 키에도 자리 번호를 쓰면 안 된다. 오프셋 페이지네이션은 겹침을 피할 수 없으니 <b>id 로 합쳐 넣고</b>, 가능하면 커서 방식으로 바꾼다. 되돌리기는 '반대 동작' 이 아니라 '<b>이전 상태로 복원</b>' 이므로 지울 때 <b>항목과 자리</b>를 함께 기억한다." },
          { h: "도착 순서도 상태가 아니다", t: "네트워크는 순서를 보장하지 않는다 — 먼저 보낸 요청의 응답이 나중에 오면 <b>옛 결과가 최신 화면을 덮는다</b>. 순번으로 낡은 응답을 버리거나, 새 요청 때 이전 것을 취소하거나, 디바운스로 요청 자체를 줄인다. 가상 목록의 범위는 <b>절대 좌표</b>에서 <code>ceil</code> 로 끝을 잡고 0 과 total 로 자른다. 표시 규칙도 마찬가지다 — 금액의 소수 자릿수는 통화가 정하고, 날짜는 <b>오프셋을 더한 뒤</b> 읽는다." }],
          code: { c: "resp.seq > state.seq ? { … } : state          // 낡은 응답은 버린다\nMath.ceil((scrollTop + viewport) / rowH) + 1   // 걸친 행까지 + 여유\nnew Date(utcMs + offsetMin * 60000)            // 옮기고 UTC 로 읽는다", cap: "번호와 순서는 언제든 바뀐다" },
        key: ["선택 상태는 id 로 붙든다", "겹치는 페이지는 합쳐 넣는다", "낡은 응답을 순번으로 버린다", "자릿수는 통화가, 날짜는 오프셋이 정한다"] } },
    { t: "앱이 죽고 살아나는 사이", n: 6,
      th: { sum: "모바일 앱은 **언제든 죽는다**. 살아 있는 동안 잘 도는 코드와, 죽었다 살아나도 맞는 코드는 다르다.",
        body: [
          { h: "보장되는 콜백에서만 저장한다", t: "종료 콜백은 <b>호출이 보장되지 않는다</b> — 메모리가 부족하면 시스템이 아무 알림 없이 회수한다. 지켜야 할 상태는 <b>화면에서 물러나는 순간</b>에, <b>빠르고 원자적으로</b> 저장한다. 그리고 <code>JSON.stringify</code> 는 표현할 수 없는 값을 <b>조용히 버린다</b> — 객체의 <code>undefined</code> 필드는 사라지고 <code>Set</code>·<code>Map</code> 은 <code>{}</code> 가 된다. 저장용 자료형을 따로 두고 <b>왕복 테스트</b>로 못 박는다." },
          { h: "바깥에서 오는 값은 허용 목록으로", t: "딥링크는 다른 앱·웹·문자 어디서든 오므로 <b>어떤 문자열이 와도 죽지 않아야</b> 한다 — <code>?</code> 유무, 값 안의 <code>=</code>, 퍼센트 인코딩 셋을 모두 다룬다. 권한 상태 같은 열거값은 '<b>허가된 것만 빼고 전부</b>' 가 아니라 <b>허용 목록</b>으로 판정해야 새 상태가 늘어도 안전한 쪽으로 떨어진다. 버전처럼 <b>숫자가 섞인 문자열</b>은 문자열 비교로 견주면 <code>1.9</code> 가 <code>1.10</code> 보다 커진다." }],
        code: { c: "if (e === 'pause') saved = draft;                 // 보장되는 시점\nout[decodeURIComponent(k)] = decodeURIComponent(v);\nstate === 'unasked' || state === 'denied'        // 허용 목록", cap: "죽었다 살아나도 맞아야 한다" },
        key: ["보장되는 콜백 목록을 적어 둔다", "저장은 왕복 테스트로 확인한다", "딥링크는 어떤 입력에도 안 죽는다", "버전은 숫자로 견준다"] } },
  ],
};
