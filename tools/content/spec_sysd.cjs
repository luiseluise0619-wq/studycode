module.exports={
  track:"sysd", guide:"🏛️", xp:80,
  unit:"실행형 실전 — 설계 정책을 직접 계산한다",
  source:"./exec_sysd.cjs",
  lessons:[
    { t:"트래픽을 받아 내는 정책", n:3,
      th:{ sum:"앞단에서 부하를 다스리는 세 가지 — **얼마나 받을지, 몰릴 때 어떻게 합칠지, 무엇을 버릴지**.",
        body:[
          {h:"창을 어떻게 자르는가", t:"고정 창 레이트 리밋은 카운터 하나로 끝나 싸지만 **경계에서 두 배까지 통과**시킨다 — 분당 100회 제한에서 59초에 100회, 60초에 100회를 보내면 1초 안에 200회가 지나간다. 슬라이딩 로그는 정확하지만 타임스탬프를 전부 들고 있어야 한다. 고를 때 묻는 것은 '정확한가' 가 아니라 '**무엇을 보호하는가**' 다."},
          {h:"몰릴 때와 넘칠 때", t:"인기 키가 만료되는 순간 그 키를 기다리던 요청 전부가 캐시 미스를 겪고 하류로 내려간다(스탬피드). 해법의 핵심은 '있음/없음' 이 아니라 '**진행 중**' 이라는 세 번째 상태를 두는 것이다. 그리고 캐시가 넘칠 때 무엇을 버릴지는 LRU(최근성)와 LFU(인기도)가 다른 답을 내며, 자주 쓰는 키를 버리는 쪽이 어느 것인지는 워크로드가 정한다."}],
        code:{ c:"if key in inflight and t < inflight[key]:\n    continue      # 진행 중인 적재에 합류한다", cap:"두 상태로만 보면 전부 새로 적재한다" },
        key:["고정 창은 경계에서 두 배를 통과시킨다","스탬피드는 '진행 중' 상태로 막는다","LRU 는 최근성, LFU 는 인기도","동점 규칙이 없으면 동작이 재현되지 않는다"] } },
    { t:"중복과 실패를 다루는 규약", n:3,
      th:{ sum:"분산 환경의 기본 전제는 '**메시지는 두 번 올 수 있고, 중간에 실패할 수 있다**' 이다.",
        body:[
          {h:"두 번째 요청에 무엇을 돌려주는가", t:"멱등 키의 계약은 '두 번째가 아무 일도 하지 않는다' 가 아니라 '**첫 번째와 똑같은 응답을 돌려준다**' 이다. 응답이 다르면 클라이언트는 실패로 판단해 또 시도한다. TTL 은 저장소가 무한히 자라지 않게 하지만, **재시도 정책보다 짧으면** 중복 실행이 그대로 통과한다 — 두 값은 함께 정해야 한다."},
          {h:"되돌리는 순서와 기억하는 범위", t:"사가는 실패 지점까지 완료된 단계를 **역순으로** 보상한다 — 진행할 때 쌓은 순서를 정확히 거꾸로 벗겨야 각 시점이 일관되기 때문이다. 그리고 at-least-once 소비자의 중복 제거 창이 재전달 간격보다 좁으면 중복이 통과하는데, 이 사고는 **트래픽이 늘어야** 드러난다."}],
        code:{ c:"return steps[:fail_at][::-1]   # 완료된 것들을 역순으로", cap:"실패한 단계 자신은 보상 대상이 아니다" },
        key:["멱등은 '같은 응답' 이 계약이다","TTL 은 재시도 정책보다 길게","보상은 역순으로","중복 제거 창은 개수보다 시간 기준으로"] } },
    { t:"규모를 숫자로 다룬다", n:3,
      th:{ sum:"설계 결정의 근거는 취향이 아니라 **자릿수**다 — 계산이 어디서 구조가 바뀌는지 알려 준다.",
        body:[
          {h:"어느 쪽이 더 싼가", t:"팬아웃은 push(쓸 때 팔로워 수만큼 복사)와 pull(읽을 때 팔로잉 수만큼 수집) 중 어느 쪽이 싼지가 **팔로워 수에 따라 뒤집힌다** — 그래서 실제 서비스는 둘을 섞는다. 용량 산정에서 가장 자주 빠지는 것은 **피크 배수**인데, 평균 QPS 로 맞추면 저녁 시간대에 정확히 절반이 실패한다."},
          {h:"믿을 수 있는 것과 없는 것", t:"블룸 필터는 '없다' 는 확실하고 '있다' 는 확률이다 — 이 비대칭 덕분에 '없으면 비싼 조회를 건너뛰는' 용도에 맞는다. 넣을 때와 볼 때 해시 개수가 다르면 **위음성이 생겨** 필터 자체가 쓸모없어진다. 복제 지연 환경에서 자기가 쓴 것을 읽게 하려면 그 사용자의 읽기만 잠시 주 노드로 보내야 하고, 전부 보내면 복제본을 둔 의미가 없다."}],
        code:{ c:"peak_qps = dau * actions / 86400 * peak_mult\n# 평균으로 맞추면 피크에 절반이 실패한다", cap:"저장량에는 복제·인덱스·백업이 빠져 있다" },
        key:["팬아웃 비용은 팔로워 수에 따라 뒤집힌다","피크 배수를 빼면 3배를 적게 준비한다","블룸 필터에 위음성이 생기면 끝이다","read-your-writes 는 그 사용자만 좁혀서"] } },
    { t:"샤딩과 핫키", n:3,
      th:{ sum:"고르게 나누는 것만으로는 부족하다 — **나눈 뒤에 늘릴 수 있는가**, 그리고 **한 키에 몰리면 어떻게 하는가**.",
        body:[
          {h:"늘릴 때의 비용", t:"나머지 샤딩은 샤드를 하나만 늘려도 **키의 대부분이 이사한다**(4→5 에서 80%). 그래서 '트래픽이 늘면 샤드를 추가하면 되지' 라는 계획이 실제로는 며칠짜리 프로젝트가 된다. 일관된 해싱은 평균 `1/n` 만 움직이고, 논리 샤드를 미리 많이 만들어 두는 방식은 재계산 자체를 없앤다."},
          {h:"몰리는 키", t:"샤딩이 아무리 고르게 돼도 인기 게시물의 카운터처럼 **키 하나에 트래픽이 몰리면** 그 샤드만 죽는다. 쪼개서 흩고 읽을 때 합치면 쓰기 부하가 `1/shards` 로 줄지만 읽기가 그만큼 늘어난다 — 그래서 **문턱을 두고 소수의 핫키에만** 적용한다. 어려운 부분은 쪼개는 코드가 아니라 **핫키를 언제 발견하는가** 다."}],
        code:{ c:"sum(1 for k in keys if k % before != k % after)\n# 4 → 5 에서 100개 중 80개가 이사한다", cap:"'하나만 늘리는 것' 이 싸지 않다" },
        key:["나머지 샤딩은 거의 전부 이동한다","일관된 해싱은 1/n 만 이동","핫키는 문턱을 넘는 것만 쪼갠다","쪼개면 읽기 비용이 늘어난다"] } },
  ]
};
