/* Cloud 실행형 — 클라우드가 속으로 하는 계산을 직접 만들어 본다.
   CIDR 과 라우팅, 보안 그룹과 정책 평가, 비용 계산, 오토스케일과 헬스 체크,
   가용성 수치와 복구 목표, 인프라 코드의 계획 비교.
   콘솔에서 클릭하며 외우던 것을 규칙으로 바꾸는 것이 목적이다. */
module.exports = [

/* ───────────── 네트워크 계산 ───────────── */
{ track: "cloud", k: "이 주소가 이 대역에 드는가 · cidrContains", cat: "internals",
  q: "<code>\"10.0.0.0/16\"</code> 같은 CIDR 과 IPv4 주소를 받아 그 주소가 대역 안에 드는지 판정해 주세요. 접두 길이만큼의 상위 비트가 같으면 안에 드는 것입니다.",
  src: "function cidrContains(cidr, ip) {\n  // TODO: 주소를 32비트 정수로 바꾸고 마스크를 씌워 보세요\n  return false;\n}",
  sol: "function cidrContains(cidr, ip) {\n  const toNum = (a) => a.split(\".\").reduce((n, x) => n * 256 + Number(x), 0);\n  const bits = Number(cidr.split(\"/\")[1]);\n  const mask = bits === 0 ? 0 : (-1 << (32 - bits)) >>> 0;\n  return ((toNum(cidr.split(\"/\")[0]) & mask) >>> 0)\n      === ((toNum(ip) & mask) >>> 0);\n}",
  tests: [['cidrContains("10.0.0.0/16","10.0.5.7")', 'true'],
    ['cidrContains("10.0.0.0/16","10.1.0.1")', 'false'],
    ['cidrContains("192.168.1.0/24","192.168.1.255")', 'true'],
    ['cidrContains("0.0.0.0/0","8.8.8.8")', 'true']],
  edge: [['cidrContains("10.0.0.1/32","10.0.0.1")', 'true'],
    ['cidrContains("10.0.0.1/32","10.0.0.2")', 'false']],
  ex: "보안 그룹 규칙, 라우팅 테이블, 방화벽이 전부 이 계산을 합니다. <b>접두 길이만큼의 상위 비트가 같은가</b> 하나뿐입니다.\n<code>/0</code> 을 따로 다루는 줄이 중요합니다. <code>-1 &lt;&lt; 32</code> 는 자바스크립트에서 <b>0이 아니라 -1</b> 이 됩니다 — 시프트 연산이 32로 나눈 나머지를 쓰기 때문입니다. 그래서 <code>0.0.0.0/0</code>(전체 허용)이 아무것도 허용하지 않는 것으로 뒤집히는데, 보안 규칙을 다루는 코드에서 이런 실수는 그대로 사고가 됩니다.\n<code>&gt;&gt;&gt; 0</code> 이 붙은 이유도 같습니다. 비트 연산의 결과는 <b>부호 있는</b> 32비트라 최상위 비트가 켜지면 음수가 되고, 비교가 어긋납니다. 부호 없는 값으로 되돌려야 합니다.\n<code>/32</code> 는 주소 하나만 가리킵니다. 관리 포트를 열 때 특정 서버 하나만 허용하는 규칙이 이 모양입니다." },

{ track: "cloud", k: "서브넷에 몇 대나 들어가나 · subnetHosts", cat: "impl",
  q: "접두 길이를 받아 <b>실제로 쓸 수 있는 주소 개수</b>를 돌려주세요. 클라우드는 서브넷마다 <b>5개</b>를 예약합니다. 남는 것이 없으면 0 입니다.",
  src: "function subnetHosts(prefix) {\n  // TODO: 전체 개수에서 예약분을 빼세요\n  return Math.pow(2, 32 - prefix);\n}",
  sol: "function subnetHosts(prefix) {\n  const total = Math.pow(2, 32 - prefix);\n  return total <= 5 ? 0 : total - 5;\n}",
  tests: [['subnetHosts(24)', '251'],
    ['subnetHosts(28)', '11'],
    ['subnetHosts(16)', '65531'],
    ['subnetHosts(30)', '0']],
  edge: [['subnetHosts(29)', '3'], ['subnetHosts(32)', '0']],
  ex: "'<code>/28</code> 이면 16개니까 넉넉하겠지' 하고 잡았다가 <b>실제로는 11대</b>밖에 못 들어가는 일이 흔합니다. 네트워크 주소·브로드캐스트·게이트웨이·DNS·예비까지 다섯이 먼저 빠집니다.\n오토스케일이 늘어나려는데 <b>주소가 모자라 실패</b>하는 사고가 여기서 납니다. 게다가 서브넷은 나중에 넓히기 어려워서, 처음에 넉넉히 잡는 것이 거의 언제나 옳습니다.\n<code>/29</code> 는 겨우 3대입니다. 로드 밸런서처럼 여러 주소를 요구하는 서비스는 최소 접두 길이를 따로 정해 두는 경우가 많습니다.\n사설 대역은 넉넉하니 아낄 이유가 없습니다. <b>아껴서 얻는 것은 없고 모자라서 잃는 것은 큽니다.</b>" },

{ track: "cloud", k: "가장 구체적인 경로가 이긴다 · longestPrefix", cat: "internals",
  q: "<code>[[CIDR, 대상], …]</code> 형태의 라우팅 표와 목적지 주소를 받아, <b>접두 길이가 가장 긴</b> 일치 항목의 대상을 돌려주세요. 맞는 것이 없으면 <code>null</code> 입니다.",
  src: "function longestPrefix(routes, ip) {\n  // TODO: 일치하는 것 중 접두 길이가 가장 긴 것을 고르세요\n  return null;\n}",
  sol: "function longestPrefix(routes, ip) {\n  const toNum = (a) => a.split(\".\").reduce((n, x) => n * 256 + Number(x), 0);\n  const dest = toNum(ip);\n  let best = null;\n  let bits = -1;\n  routes.forEach((r) => {\n    const n = Number(r[0].split(\"/\")[1]);\n    const mask = n === 0 ? 0 : (-1 << (32 - n)) >>> 0;\n    if (n > bits && ((toNum(r[0].split(\"/\")[0]) & mask) >>> 0) === ((dest & mask) >>> 0)) {\n      bits = n;\n      best = r[1];\n    }\n  });\n  return best;\n}",
  tests: [['longestPrefix([["0.0.0.0/0","igw"],["10.0.0.0/16","local"]],"10.0.1.5")', '"local"'],
    ['longestPrefix([["0.0.0.0/0","igw"],["10.0.0.0/16","local"]],"8.8.8.8")', '"igw"'],
    ['longestPrefix([["10.0.0.0/16","local"],["10.0.1.0/24","nat"]],"10.0.1.9")', '"nat"'],
    ['longestPrefix([["10.0.0.0/16","local"]],"192.168.0.1")', 'null']],
  edge: [['longestPrefix([],"1.1.1.1")', 'null'],
    ['longestPrefix([["0.0.0.0/0","igw"]],"10.0.0.1")', '"igw"']],
  ex: "라우팅 테이블이 목적지를 고르는 규칙이 <b>가장 긴 접두 일치</b> 하나입니다. 여러 경로가 맞아도 더 구체적인 쪽이 이깁니다.\n이 규칙 덕분에 '기본은 인터넷 게이트웨이로, 다만 VPC 안쪽은 내부로' 같은 구성이 두 줄로 표현됩니다. <code>0.0.0.0/0</code> 은 접두 길이가 0이라 <b>언제나 마지막 후보</b>가 됩니다.\n세 번째 테스트가 실무에서 자주 쓰는 모양입니다. 서브넷 하나만 NAT 로 보내고 나머지는 그대로 두는 식으로 <b>더 구체적인 규칙을 덧씌우는</b> 방식입니다.\n'왜 이 트래픽이 저기로 가지' 를 조사할 때는 언제나 <b>가장 구체적인 규칙부터</b> 찾아야 합니다. 넓은 규칙만 보고 판단하면 답이 나오지 않습니다." },

{ track: "cloud", k: "보안 그룹이 허용하는가 · sgAllows", cat: "security",
  q: "<code>[[포트, 출발지], …]</code> 규칙 목록과 요청의 포트·출발지를 받아 허용되는지 판정해 주세요. 출발지는 문자열이 같으면 일치하고, <code>\"0.0.0.0/0\"</code> 규칙은 어떤 출발지든 허용합니다.",
  src: "function sgAllows(rules, port, source) {\n  // TODO: 포트가 같고 출발지가 맞는 규칙이 하나라도 있으면 허용입니다\n  return true;\n}",
  sol: "function sgAllows(rules, port, source) {\n  return rules.some((r) =>\n    r[0] === port && (r[1] === source || r[1] === \"0.0.0.0/0\"));\n}",
  tests: [['sgAllows([[443,"0.0.0.0/0"]],443,"sg-web")', 'true'],
    ['sgAllows([[3306,"sg-web"]],3306,"sg-web")', 'true'],
    ['sgAllows([[3306,"sg-web"]],3306,"sg-batch")', 'false'],
    ['sgAllows([[3306,"sg-web"]],5432,"sg-web")', 'false']],
  edge: [['sgAllows([],443,"sg-web")', 'false'],
    ['sgAllows([[22,"10.0.0.0/8"]],22,"10.0.0.0/8")', 'true']],
  ex: "보안 그룹에는 <b>거부 규칙이 없습니다.</b> 허용 목록에 없으면 막히는 것이 전부라, 판정 코드가 <code>some</code> 한 줄로 끝납니다.\n이 단순함이 안전한 이유는 <b>실수의 방향</b>에 있습니다. 규칙을 빠뜨리면 열리는 것이 아니라 닫힙니다. 거부 목록 방식이었다면 빠뜨린 것이 그대로 뚫립니다.\n출발지를 IP 대신 <b>다른 보안 그룹</b>으로 지정할 수 있다는 점이 클라우드의 요령입니다. 두 번째 테스트가 그 모양인데, 이렇게 두면 오토스케일로 인스턴스가 늘어나거나 IP 가 바뀌어도 규칙을 고칠 일이 없습니다.\n첫 번째 테스트의 <code>0.0.0.0/0</code> 은 HTTPS 라면 정상이지만 <b>관리 포트에 붙으면 사고</b>입니다. 공개된 SSH 포트는 몇 분 안에 자동 스캔에 걸립니다." },

{ track: "cloud", k: "두 대역이 겹치는가 · cidrOverlap", cat: "debug",
  q: "CIDR 두 개를 받아 겹치는 부분이 있는지 판정해 주세요. <b>더 짧은 접두 길이</b>를 기준으로 상위 비트가 같으면 겹칩니다.",
  src: "function cidrOverlap(a, b) {\n  // TODO: 둘 중 더 넓은 쪽(짧은 접두)의 마스크로 비교하세요\n  return a === b;\n}",
  sol: "function cidrOverlap(a, b) {\n  const toNum = (x) => x.split(\".\").reduce((n, s) => n * 256 + Number(s), 0);\n  const cut = (c) => [toNum(c.split(\"/\")[0]), Number(c.split(\"/\")[1])];\n  const ca = cut(a);\n  const cb = cut(b);\n  const bits = Math.min(ca[1], cb[1]);\n  const mask = bits === 0 ? 0 : (-1 << (32 - bits)) >>> 0;\n  return ((ca[0] & mask) >>> 0) === ((cb[0] & mask) >>> 0);\n}",
  tests: [['cidrOverlap("10.0.0.0/16","10.0.1.0/24")', 'true'],
    ['cidrOverlap("10.0.0.0/16","10.1.0.0/16")', 'false'],
    ['cidrOverlap("192.168.0.0/16","10.0.0.0/8")', 'false'],
    ['cidrOverlap("0.0.0.0/0","10.0.0.0/8")', 'true']],
  edge: [['cidrOverlap("10.0.0.0/8","10.0.0.0/8")', 'true'],
    ['cidrOverlap("172.16.0.0/12","172.32.0.0/12")', 'false']],
  ex: "VPC 를 만들기 전에 반드시 돌려 봐야 하는 계산입니다. <b>주소 범위가 겹치면 두 네트워크를 연결할 수 없습니다</b> — 라우팅이 어느 쪽으로 보낼지 정할 수 없기 때문입니다.\n이 실수는 나중에 고치기가 거의 불가능합니다. VPC 의 대역은 바꿀 수 없고, 옮기려면 그 안의 모든 것을 새로 만들어야 합니다. 그래서 조직 차원에서 <b>대역 대장을 만들어 관리하는</b> 곳이 많습니다.\n<b>넓은 쪽 기준으로 비교하는 것</b>이 핵심입니다. <code>/16</code> 안에 <code>/24</code> 가 통째로 들어 있으면 겹치는 것이므로, 짧은 접두로 잘라 비교해야 합니다.\n사무실 네트워크, 인수한 회사의 VPC, 파트너와의 전용선까지 언젠가 연결될 수 있습니다. <b>지금 연결할 계획이 없어도</b> 겹치지 않게 잡아 두는 편이 낫습니다." },

/* ───────────── 권한과 정책 ───────────── */
{ track: "cloud", k: "정책을 평가한다 · policyEval", cat: "security",
  q: "<code>[[효과, 액션], …]</code> 형태의 정책과 요청 액션을 받아 결과를 돌려주세요. 명시적 <code>\"Deny\"</code> 가 있으면 <code>\"Deny\"</code>, 아니면 <code>\"Allow\"</code> 가 있을 때 <code>\"Allow\"</code>, 둘 다 없으면 <code>\"ImplicitDeny\"</code> 입니다. 액션 <code>\"*\"</code> 는 모두에 맞습니다.",
  src: "function policyEval(statements, action) {\n  // TODO: 거부를 먼저 보세요\n  return \"Allow\";\n}",
  sol: "function policyEval(statements, action) {\n  const hit = (a) => a === action || a === \"*\";\n  if (statements.some((st) => st[0] === \"Deny\" && hit(st[1]))) return \"Deny\";\n  if (statements.some((st) => st[0] === \"Allow\" && hit(st[1]))) return \"Allow\";\n  return \"ImplicitDeny\";\n}",
  tests: [['policyEval([["Allow","s3:GetObject"]],"s3:GetObject")', '"Allow"'],
    ['policyEval([["Allow","*"],["Deny","s3:DeleteObject"]],"s3:DeleteObject")', '"Deny"'],
    ['policyEval([],"s3:GetObject")', '"ImplicitDeny"'],
    ['policyEval([["Allow","s3:GetObject"]],"s3:PutObject")', '"ImplicitDeny"']],
  edge: [['policyEval([["Deny","*"],["Allow","*"]],"anything")', '"Deny"'],
    ['policyEval([["Allow","*"]],"x")', '"Allow"']],
  ex: "IAM 의 평가 규칙이 이 여섯 줄에 전부 들어 있습니다. <b>기본은 거부이고, 명시적 허용이 있어야 통과하며, 명시적 거부는 어떤 허용보다 우선합니다.</b>\n순서나 구체성이 결과에 영향을 주지 않는다는 점이 중요합니다. 방화벽 규칙처럼 '먼저 쓴 것이 이긴다' 가 아니라 <b>거부가 있으면 그냥 거부</b>입니다. 그래서 결과를 예측하기 쉽습니다.\n이 성질 덕분에 조직 차원의 금지 규칙을 거부로 걸어 두면 하위 계정에서 아무리 넓게 허용해도 뚫리지 않습니다. 두 번째 테스트가 그 모양입니다 — <b>넓게 열어 두고 위험한 것만 못 박는</b> 실무의 흔한 구성입니다.\n네 번째 테스트의 <code>\"ImplicitDeny\"</code> 를 <code>\"Deny\"</code> 와 구분한 이유는 조사할 때 다르기 때문입니다. 명시적 거부는 '누군가 일부러 막았다', 암묵적 거부는 '아무도 허용하지 않았다' 입니다." },

{ track: "cloud", k: "쓰이지 않은 권한 · unusedPermissions", cat: "review",
  q: "부여된 권한 목록과 실제로 쓰인 권한 목록을 받아, <b>부여되었지만 한 번도 쓰이지 않은 것</b>을 사전순으로 돌려주세요.",
  src: "function unusedPermissions(granted, used) {\n  // TODO: 쓰인 목록에 없는 것만 남기세요\n  return granted;\n}",
  sol: "function unusedPermissions(granted, used) {\n  return granted.filter((g) => used.indexOf(g) < 0).sort();\n}",
  tests: [['unusedPermissions(["s3:Get","s3:Put","ec2:Run"],["s3:Get"])', '["ec2:Run","s3:Put"]'],
    ['unusedPermissions(["a"],["a"])', '[]'],
    ['unusedPermissions([],["a"])', '[]'],
    ['unusedPermissions(["b","a"],[])', '["a","b"]']],
  edge: [['unusedPermissions(["s3:*"],["s3:Get"])', '["s3:*"]'],
    ['unusedPermissions(["A"],["a"])', '["A"]']],
  ex: "권한은 <b>늘어나기만 하고 줄어들지 않는</b> 성질이 있습니다. 급할 때 넓게 준 것을 좁힐 계기가 생기지 않기 때문입니다. 그래서 정기적으로 걷어 내는 절차가 필요하고, 대부분의 클라우드가 마지막 사용 시각을 알려 줍니다.\n첫 엣지 케이스가 이 단순한 비교의 한계를 보여 줍니다. <code>s3:*</code> 는 <code>s3:Get</code> 을 <b>포함하는데도</b> 문자열이 달라 '안 쓰임' 으로 나옵니다. 실제 도구는 와일드카드를 펼쳐 비교해야 하고, 그 펼치는 일이 생각보다 까다롭습니다.\n두 번째 엣지 케이스는 <b>대소문자를 구분한다</b>는 사실을 못 박습니다. 액션 이름의 대소문자가 다르면 다른 권한으로 취급됩니다.\n실무의 요령은 '한 번에 완벽히 좁히기' 가 아니라 <b>넓은 것부터 하나씩</b> 입니다. <code>*</code> 권한을 가진 역할부터 찾아 실제 사용 기록으로 대체합니다." },

{ track: "cloud", k: "빠진 태그를 찾는다 · missingTags", cat: "impl",
  q: "자원에 붙은 태그와 필수 태그 키 목록을 받아, <b>붙어 있지 않은 키</b>를 사전순으로 돌려주세요. 값이 빈 문자열이어도 키가 있으면 있는 것으로 봅니다.",
  src: "function missingTags(tags, required) {\n  // TODO: 값이 아니라 키의 존재를 보세요\n  return required;\n}",
  sol: "function missingTags(tags, required) {\n  return required\n    .filter((k) => !Object.prototype.hasOwnProperty.call(tags, k))\n    .sort();\n}",
  tests: [['missingTags({team:"pay"},["team","env"])', '["env"]'],
    ['missingTags({team:"pay",env:"prod"},["team","env"])', '[]'],
    ['missingTags({},["team"])', '["team"]'],
    ['missingTags({env:""},["env"])', '[]']],
  edge: [['missingTags({team:"a"},[])', '[]'],
    ['missingTags({},["b","a"])', '["a","b"]']],
  ex: "태그가 없으면 <b>비용을 나눠 볼 수 없습니다.</b> '클라우드 비용이 늘었다' 는 아무도 움직이게 하지 못하지만 '우리 팀 개발 환경이 두 배가 되었다' 는 움직이게 합니다.\n네 번째 테스트가 미묘한 지점입니다. <code>{env:\"\"}</code> 는 <b>키는 있고 값이 빈</b> 상태입니다. <code>tags[k]</code> 로 검사했다면 빈 문자열이 거짓 같은 값이라 '없음' 으로 판정했을 텐데, 태그 규칙에서는 대개 키의 존재만 봅니다. 값까지 검사할 것인지는 정하고 문서에 적어 두어야 합니다.\n<code>hasOwnProperty</code> 를 <code>Object.prototype</code> 에서 꺼내 부르는 이유는 태그 이름이 <code>toString</code> 같은 것일 수 있기 때문입니다. 사용자가 정하는 문자열을 키로 쓸 때는 늘 이 위험이 있습니다.\n실무에서는 이 검사를 <b>만들기 전에</b> 겁니다. 태그 없는 자원을 아예 만들 수 없게 정책으로 막는 편이, 나중에 수천 개를 찾아 붙이는 것보다 훨씬 쉽습니다." },

{ track: "cloud", k: "키를 로그에 남기기 전에 · maskSecret", cat: "security",
  q: "문자열과 남길 뒷자리 수를 받아 앞부분을 <code>*</code> 로 가려 주세요. 남길 자리가 문자열보다 길거나 같으면 그대로 돌려줍니다.",
  src: "function maskSecret(s, keep) {\n  // TODO: 남길 자리 수가 0 이하이거나 전체 길이 이상인 경우를 먼저 처리하세요\n  return s;\n}",
  sol: "function maskSecret(s, keep) {\n  const text = String(s);\n  if (keep <= 0) return \"*\".repeat(text.length);\n  if (keep >= text.length) return text;\n  return \"*\".repeat(text.length - keep) + text.slice(-keep);\n}",
  tests: [['maskSecret("AKIA1234567890",4)', '"**********7890"'],
    ['maskSecret("abc",0)', '"***"'],
    ['maskSecret("ab",5)', '"ab"'],
    ['maskSecret("",4)', '""']],
  edge: [['maskSecret("secret",6)', '"secret"'], ['maskSecret("secret",1)', '"*****t"']],
  ex: "로그와 오류 보고에 키가 통째로 찍히는 사고가 아주 흔합니다. 설정을 덤프하는 코드 한 줄, 예외를 그대로 출력하는 처리 하나면 충분합니다.\n뒷자리를 몇 개 남기는 것은 <b>어느 키인지 알아보기 위해서</b>입니다. 전부 가리면 '이 키가 그 키인가' 를 확인할 수 없어 조사할 때 곤란합니다. 다만 남기는 자리가 많아지면 그 자체가 단서가 되므로 넷 안팎으로 둡니다.\n세 번째 테스트가 함정입니다. <code>keep</code> 이 문자열보다 길면 <b>아무것도 가려지지 않습니다.</b> 짧은 값을 마스킹한다고 안심하면 안 되는 이유입니다 — 짧은 비밀은 애초에 마스킹으로 지킬 수 없습니다.\n가장 중요한 것은 마스킹이 <b>사후 대책</b>이라는 점입니다. 유출된 키를 발견하면 지우는 것이 아니라 <b>폐기하고 새로 발급하는 것</b>이 먼저입니다." },

{ track: "cloud", k: "이 역할을 넘겨받을 수 있는가 · canAssume", cat: "security",
  q: "신뢰 목록과 요청 주체를 받아 역할을 넘겨받을 수 있는지 판정해 주세요. <code>\"*\"</code> 는 모두 허용이고, 끝이 <code>*</code> 인 항목은 <b>앞부분이 같으면</b> 허용합니다.",
  src: "function canAssume(trust, principal) {\n  // TODO: 와일드카드가 붙은 항목은 접두사로 비교하세요\n  return false;\n}",
  sol: "function canAssume(trust, principal) {\n  return trust.some((t) => {\n    if (t === \"*\") return true;\n    if (t.slice(-1) === \"*\") return principal.indexOf(t.slice(0, -1)) === 0;\n    return t === principal;\n  });\n}",
  tests: [['canAssume(["arn:acct:111:role/app"],"arn:acct:111:role/app")', 'true'],
    ['canAssume(["arn:acct:111:*"],"arn:acct:111:role/app")', 'true'],
    ['canAssume(["arn:acct:111:*"],"arn:acct:222:role/app")', 'false'],
    ['canAssume(["*"],"anything")', 'true']],
  edge: [['canAssume([],"x")', 'false'], ['canAssume(["*"],"")', 'true']],
  ex: "역할을 넘겨받는 구조 덕분에 <b>계정마다 권한을 복제하지 않아도</b> 됩니다. 개발 계정의 사용자가 운영 계정의 읽기 전용 역할을 잠시 빌려 쓰는 식입니다.\n신뢰 정책은 <b>'누가 이 역할을 쓸 수 있는가'</b> 를 정하고, 권한 정책은 <b>'그 역할이 무엇을 할 수 있는가'</b> 를 정합니다. 두 개가 모두 허용해야 통과하는 이중 잠금입니다.\n마지막 엣지 케이스가 위험을 보여 줍니다. <code>\"*\"</code> 를 신뢰 목록에 넣으면 <b>다른 계정 누구든</b> 그 역할을 넘겨받을 수 있습니다. 실제로 이 설정 실수로 외부에서 자원을 읽어 간 사고가 여러 번 있었습니다.\n접두사 방식도 조심해야 합니다. <code>arn:acct:11*</code> 처럼 자르면 계정 <code>111</code> 뿐 아니라 <code>112</code>·<code>119</code> 까지 들어옵니다 — 와일드카드를 쓸 때는 경계가 어디까지인지 확인해야 합니다." },

/* ───────────── 비용 ───────────── */
{ track: "cloud", k: "몇 시간이나 켜져 있었나 · instanceHours", cat: "impl",
  q: "<code>[[시각, \"start\"|\"stop\"], …]</code> 기록과 관측 종료 시각을 받아 <b>켜져 있던 총 시간</b>을 구해 주세요. 마지막에 꺼지지 않았다면 종료 시각까지 켜져 있던 것으로 봅니다.",
  src: "function instanceHours(events, endHour) {\n  // TODO: 켜진 시점을 들고 있다가 꺼질 때 더하세요\n  return 0;\n}",
  sol: "function instanceHours(events, endHour) {\n  let total = 0;\n  let from = -1;\n  events.forEach((e) => {\n    if (e[1] === \"start\" && from < 0) from = e[0];\n    else if (e[1] === \"stop\" && from >= 0) {\n      total += e[0] - from;\n      from = -1;\n    }\n  });\n  return from >= 0 ? total + (endHour - from) : total;\n}",
  tests: [['instanceHours([[0,"start"],[8,"stop"]],24)', '8'],
    ['instanceHours([[0,"start"]],24)', '24'],
    ['instanceHours([[0,"start"],[4,"stop"],[10,"start"],[12,"stop"]],24)', '6'],
    ['instanceHours([],24)', '0']],
  edge: [['instanceHours([[5,"stop"]],24)', '0'],
    ['instanceHours([[0,"start"],[0,"stop"]],24)', '0']],
  ex: "클라우드 요금은 대부분 <b>켜져 있던 시간</b>으로 매겨지므로, 이 계산이 곧 청구서의 뼈대입니다.\n두 번째 테스트가 실무에서 가장 비싼 경우입니다. <b>끄지 않은 것</b>이 계속 요금을 만듭니다. 실험용으로 켜 두고 잊은 인스턴스가 한 달을 돌면 그것만으로 눈에 띄는 금액이 됩니다.\n개발·테스트 환경을 밤과 주말에 끄면 일주일 168시간 중 <b>50시간 남짓</b>만 켜집니다. 3분의 2가 사라지는 셈이라, 어떤 최적화보다 효과가 큽니다.\n첫 엣지 케이스처럼 <b>켜진 적 없는데 꺼짐 기록만 있는</b> 경우를 걸러 내는 줄이 필요합니다. 실제 이벤트 로그는 누락과 중복이 흔해서, 기록을 그대로 믿고 빼면 음수가 나옵니다." },

{ track: "cloud", k: "어느 요금제가 가장 싼가 · cheapestPlan", cat: "impl",
  q: "한 달 사용 시간과 온디맨드 시간당 단가, 약정의 월 정액, 스팟 시간당 단가를 받아 가장 싼 것의 이름(<code>\"온디맨드\"</code>·<code>\"약정\"</code>·<code>\"스팟\"</code>)을 돌려주세요. 값이 같으면 <b>먼저 나온 쪽</b>을 고릅니다.",
  src: "function cheapestPlan(hours, onDemand, reserved, spot) {\n  // TODO: 세 값을 계산해 견주세요\n  return \"온디맨드\";\n}",
  sol: "function cheapestPlan(hours, onDemand, reserved, spot) {\n  const costs = [\n    [\"온디맨드\", hours * onDemand],\n    [\"약정\", reserved],\n    [\"스팟\", hours * spot]\n  ];\n  costs.sort((a, b) => a[1] - b[1]);\n  return costs[0][0];\n}",
  tests: [['cheapestPlan(730, 0.1, 50, 0.03)', '"스팟"'],
    ['cheapestPlan(730, 0.1, 20, 0.09)', '"약정"'],
    ['cheapestPlan(10, 0.1, 50, 0.5)', '"온디맨드"'],
    ['cheapestPlan(100, 0.1, 50, 0.03)', '"스팟"']],
  edge: [['cheapestPlan(0, 1, 50, 1)', '"온디맨드"'],
    ['cheapestPlan(730, 0.1, 73, 0.2)', '"온디맨드"']],
  ex: "같은 기계인데 값이 세 가지인 이유는 <b>파는 것이 성능이 아니라 약속</b>이기 때문입니다. 오래 쓰겠다고 약속하면 깎아 주고, 회수해도 좋다고 하면 더 깎아 줍니다.\n세 번째 테스트가 중요한 경우입니다. <b>조금만 쓸 거라면 약정이 가장 비쌉니다</b> — 안 쓰는 시간에도 정액을 내기 때문입니다. 그래서 규모를 예측할 수 없는 초기에 크게 약정하면 안 쓰는 것에 오래 묶입니다.\n스팟이 늘 이기는 것처럼 보이지만 계산에 들어 있지 않은 비용이 있습니다. <b>회수될 때 잃는 작업</b>과 그것을 다루는 코드입니다. 사용자 요청을 직접 받는 자리에는 쓰지 않고, 중단되어도 되는 일에 씁니다.\n실무의 답은 대개 섞는 것입니다 — <b>바닥 부하는 약정, 오르내리는 부분은 온디맨드, 중단 가능한 것은 스팟</b>." },

{ track: "cloud", k: "나이에 따라 등급을 옮긴다 · tierFor", cat: "impl",
  q: "객체가 만들어진 지 며칠 되었는지를 받아 알맞은 보관 등급을 돌려주세요. 30일 미만 <code>\"표준\"</code>, 90일 미만 <code>\"저빈도\"</code>, 365일 미만 <code>\"아카이브\"</code>, 그 이상은 <code>\"삭제\"</code> 입니다.",
  src: "function tierFor(days) {\n  // TODO: 경계를 어느 쪽에 넣을지 정하세요\n  return \"표준\";\n}",
  sol: "function tierFor(days) {\n  if (days < 30) return \"표준\";\n  if (days < 90) return \"저빈도\";\n  if (days < 365) return \"아카이브\";\n  return \"삭제\";\n}",
  tests: [['tierFor(0)', '"표준"'],
    ['tierFor(30)', '"저빈도"'],
    ['tierFor(90)', '"아카이브"'],
    ['tierFor(365)', '"삭제"']],
  edge: [['tierFor(29)', '"표준"'], ['tierFor(364)', '"아카이브"']],
  ex: "수명 주기 규칙이 하는 계산이 이것입니다. 사람이 손대지 않아도 오래된 객체가 값싼 등급으로 내려가고 결국 지워집니다.\n테스트가 전부 <b>경계값</b>인 것이 의도입니다. 30일에 저빈도로 갈 것인지 31일에 갈 것인지는 부등호 한 글자 차이인데, 그 차이가 청구서에 나타납니다. 그래서 규칙을 정할 때 <b>경계를 어느 쪽에 넣을지 명시</b>해야 합니다.\n중요한 함정은 <b>값싼 등급에 꺼내는 값이 붙는다</b>는 것입니다. 90일 지난 로그를 아카이브로 옮겼는데 매주 조회한다면, 옮겨서 아낀 것보다 꺼내느라 내는 것이 큽니다. 옮기기 전에 <b>접근 빈도를 먼저 재야</b> 합니다.\n마지막 <code>\"삭제\"</code> 는 특히 조심스럽게 다뤄야 합니다. 지운 뒤에 필요해지는 일이 실제로 있으므로, 규제나 감사 요건이 있는 데이터는 별도 규칙으로 뺍니다." },

{ track: "cloud", k: "나가는 데이터 요금 · egressCost", cat: "impl",
  q: "이번 달 나간 데이터(GB), 무료 제공량(GB), GB 당 단가를 받아 요금을 구해 주세요. 무료 제공량을 넘은 만큼만 내고, <b>소수 둘째 자리까지</b> 반올림합니다.",
  src: "function egressCost(gb, freeGb, rate) {\n  // TODO: 무료 제공량을 넘은 부분이 음수가 되지 않게 하세요\n  return gb * rate;\n}",
  sol: "function egressCost(gb, freeGb, rate) {\n  const billable = Math.max(gb - freeGb, 0);\n  return Math.round(billable * rate * 100) / 100;\n}",
  tests: [['egressCost(100, 100, 0.09)', '0'],
    ['egressCost(200, 100, 0.09)', '9'],
    ['egressCost(150, 100, 0.09)', '4.5'],
    ['egressCost(50, 100, 0.09)', '0']],
  edge: [['egressCost(0, 0, 0.09)', '0'], ['egressCost(1000, 100, 0.09)', '81']],
  ex: "클라우드 요금에서 <b>가장 자주 놀라는 항목</b>이 이것입니다. 들어오는 데이터는 대개 무료인데 나가는 데이터에는 붙는 비대칭이 있습니다.\n<code>Math.max(…, 0)</code> 이 없으면 무료 제공량 안에서 <b>음수 요금</b>이 나옵니다. 실제 청구 로직에서 이런 부호 실수는 조용히 다른 항목과 상쇄되어 한참 뒤에 발견됩니다.\n이미지나 동영상을 그대로 서비스하면 전송비가 저장비를 훌쩍 넘습니다. 그래서 <b>앞에 CDN 을 두어</b> 원본에서 나가는 양을 줄이는 것이 가장 큰 절감이 되는 경우가 많습니다.\nAZ 사이와 리전 사이의 이동에도 따로 붙는다는 점을 함께 기억해야 합니다. '같은 리전 안이니 괜찮겠지' 로 AZ 를 넘나들게 설계하면 조용히 쌓입니다." },

{ track: "cloud", k: "놀고 있는 자원을 고른다 · idleResources", cat: "review",
  q: "<code>[[이름, 사용률], …]</code> 목록과 기준값을 받아 <b>기준 미만</b>인 자원의 이름을 사전순으로 돌려주세요.",
  src: "function idleResources(usage, threshold) {\n  // TODO: 기준 미만만 남기고 이름만 뽑으세요\n  return [];\n}",
  sol: "function idleResources(usage, threshold) {\n  return usage\n    .filter((u) => u[1] < threshold)\n    .map((u) => u[0])\n    .sort();\n}",
  tests: [['idleResources([["web",60],["batch",3]],10)', '["batch"]'],
    ['idleResources([["a",5],["b",2]],10)', '["a","b"]'],
    ['idleResources([["a",50]],10)', '[]'],
    ['idleResources([],10)', '[]']],
  edge: [['idleResources([["a",10]],10)', '[]'],
    ['idleResources([["z",1],["a",1]],5)', '["a","z"]']],
  ex: "비용을 줄이러 들어가면 절반 이상이 <b>쓰지도 않는데 켜져 있는 것</b>에서 나옵니다. 이 목록을 만드는 일이 절감의 첫 단계입니다.\n다만 <b>사용률이 낮다고 지워도 되는 것은 아닙니다.</b> 재해 복구용 대기 서버는 평소에 놀고 있는 것이 정상이고, 배치 전용 서버는 하루 한 번만 바쁩니다. 그래서 이 목록은 '지울 것' 이 아니라 <b>'물어볼 것'</b> 입니다.\n첫 엣지 케이스가 경계를 못 박습니다. 기준값과 <b>같은 것</b>은 포함되지 않습니다 — <code>&lt;</code> 와 <code>&lt;=</code> 중 무엇을 쓸지는 정하고 적어 두어야 나중에 다투지 않습니다.\n측정 기간도 중요합니다. 하루만 재면 그날 마침 한가했던 서버가 걸리고, 한 달을 재면 월말에만 바쁜 정산 서버를 걸러 낼 수 있습니다." },

/* ───────────── 규모와 가용성 ───────────── */
{ track: "cloud", k: "몇 대로 늘릴 것인가 · desiredCount", cat: "impl",
  q: "현재 대수·현재 지표값·목표 지표값과 최소·최대 대수를 받아 목표 대수를 구해 주세요. <b>올림</b>한 뒤 최소·최대 범위로 맞춥니다. 목표값이 0이면 현재 대수를 그대로 둡니다.",
  src: "function desiredCount(current, metric, target, min, max) {\n  // TODO: 0으로 나누기를 먼저 막고, 올림한 뒤 범위로 맞추세요\n  return current;\n}",
  sol: "function desiredCount(current, metric, target, min, max) {\n  if (!target) return current;\n  const want = Math.ceil(current * metric / target);\n  return Math.min(Math.max(want, min), max);\n}",
  tests: [['desiredCount(4, 80, 40, 1, 10)', '8'],
    ['desiredCount(4, 20, 40, 1, 10)', '2'],
    ['desiredCount(4, 200, 40, 1, 10)', '10'],
    ['desiredCount(4, 0, 40, 2, 10)', '2']],
  edge: [['desiredCount(4, 40, 0, 1, 10)', '4'],
    ['desiredCount(1, 45, 40, 1, 10)', '2']],
  ex: "목표 추적 방식 오토스케일이 하는 계산입니다. <b>지금 지표가 목표의 두 배면 대수도 두 배로</b> 라는 비례식 하나입니다.\n세 번째 테스트가 <b>최대 대수를 두는 이유</b>를 보여 줍니다. 지표가 튀면 계산상 스무 대가 나오는데, 상한이 없으면 잘못된 지표 하나로 요금이 폭발합니다. 상한은 성능 제한이 아니라 <b>사고 방지 장치</b>입니다.\n<b>올림</b>인 것도 의도적입니다. 계산이 4.2대라면 다섯 대여야 합니다 — 내림하면 언제나 조금씩 모자랍니다.\n이 계산만으로는 부족한 것이 <b>시간</b>입니다. 새 인스턴스가 요청을 받기까지 몇 분이 걸리므로, 부하가 그보다 빨리 몰리면 계산이 맞아도 늦습니다. 예상되는 시각에는 일정 기반으로 미리 늘려 둡니다." },

{ track: "cloud", k: "헬스 체크 상태를 판정한다 · healthState", cat: "debug",
  q: "성공 여부 배열과 <b>비정상으로 바꿀 연속 실패 수</b>·<b>정상으로 되돌릴 연속 성공 수</b>를 받아 마지막 상태(<code>\"정상\"</code> 또는 <code>\"비정상\"</code>)를 돌려주세요. 시작은 정상입니다.",
  src: "function healthState(results, unhealthyN, healthyN) {\n  // TODO: 현재 상태와 반대되는 결과가 연속으로 몇 번 왔는지 세세요\n  return \"정상\";\n}",
  sol: "function healthState(results, unhealthyN, healthyN) {\n  let state = \"정상\";\n  let streak = 0;\n  results.forEach((ok) => {\n    if (ok === (state === \"정상\")) {\n      streak = 0;\n      return;\n    }\n    streak++;\n    if (state === \"정상\" && streak >= unhealthyN) {\n      state = \"비정상\";\n      streak = 0;\n    } else if (state === \"비정상\" && streak >= healthyN) {\n      state = \"정상\";\n      streak = 0;\n    }\n  });\n  return state;\n}",
  tests: [['healthState([true,true,true],2,2)', '"정상"'],
    ['healthState([false,false],2,2)', '"비정상"'],
    ['healthState([false,true,false],2,2)', '"정상"'],
    ['healthState([false,false,true,true],2,2)', '"정상"']],
  edge: [['healthState([],2,2)', '"정상"'],
    ['healthState([false,false,false],2,3)', '"비정상"']],
  ex: "로드 밸런서가 서버를 목록에서 빼고 넣는 판단이 이 상태 기계입니다. <b>한 번 실패했다고 바로 빼지 않는 것</b>이 요점입니다.\n세 번째 테스트가 그 이유를 보여 줍니다. 실패·성공·실패로 흔들리는 동안에는 <b>연속이 끊기므로 상태가 바뀌지 않습니다.</b> 이 완충이 없으면 순간적인 지연 하나로 서버가 들락날락하고, 그때마다 연결이 끊깁니다.\n연속 횟수를 크게 잡으면 안정적이지만 <b>진짜 죽은 서버를 늦게 뺍니다.</b> 확인 주기가 5초이고 세 번 연속이면 죽은 서버로 15초 동안 요청이 갑니다. 주기와 횟수는 함께 정해야 합니다.\n들어갈 때와 나올 때의 기준을 다르게 두는 것도 흔합니다 — <b>빼는 것은 빠르게, 되돌리는 것은 신중하게</b>. 아직 회복되지 않은 서버를 성급히 넣으면 다시 실패가 시작됩니다." },

{ track: "cloud", k: "AZ 에 고르게 나눈다 · spreadPlan", cat: "impl",
  q: "총 인스턴스 수와 AZ 개수를 받아 <b>AZ 별 배치 수</b>를 배열로 돌려주세요. 나머지는 앞쪽 AZ 부터 하나씩 더 줍니다.",
  src: "function spreadPlan(total, azs) {\n  // TODO: 몫을 고르게 주고 나머지를 앞에서부터 나눠 주세요\n  return [];\n}",
  sol: "function spreadPlan(total, azs) {\n  const out = [];\n  for (let i = 0; i < azs; i++) {\n    out.push(Math.floor(total / azs) + (i < total % azs ? 1 : 0));\n  }\n  return out;\n}",
  tests: [['spreadPlan(6,3)', '[2,2,2]'],
    ['spreadPlan(7,3)', '[3,2,2]'],
    ['spreadPlan(2,3)', '[1,1,0]'],
    ['spreadPlan(0,3)', '[0,0,0]']],
  edge: [['spreadPlan(5,1)', '[5]'], ['spreadPlan(3,4)', '[1,1,1,0]']],
  ex: "여러 AZ 에 나눠 두는 것이 <b>가장 값싼 고가용성</b>입니다. 한 건물이 통째로 죽어도 서비스가 삽니다.\n세 번째 테스트가 중요한 경우를 보여 줍니다. 두 대를 세 AZ 에 나누면 <b>한 곳은 비어 있습니다.</b> 그래도 두 AZ 에는 들어가 있으므로 한 AZ 가 죽어도 하나는 남습니다 — 최소 두 대를 두는 이유가 이것입니다.\n반대로 <b>한 대만 두면</b> AZ 를 아무리 나눠도 의미가 없습니다. 그 한 대가 있는 AZ 가 죽으면 서비스가 멈춥니다.\n실무에서는 용량 계산도 함께 합니다. AZ 하나가 죽어도 견디려면 <b>남은 AZ 들이 전체 부하를 감당</b>할 수 있어야 하므로, 세 AZ 라면 각각이 50%를 감당할 여유를 두는 식으로 잡습니다." },

{ track: "cloud", k: "재시도 간격을 만든다 · retryPlan", cat: "impl",
  q: "첫 간격·상한·재시도 횟수를 받아 간격 목록을 돌려주세요. 간격은 <b>두 배씩</b> 늘리되 상한을 넘지 않습니다.",
  src: "function retryPlan(base, cap, times) {\n  // TODO: 두 배씩 늘리되 상한으로 잘라 담으세요\n  return [];\n}",
  sol: "function retryPlan(base, cap, times) {\n  const out = [];\n  let cur = base;\n  for (let i = 0; i < times; i++) {\n    out.push(Math.min(cur, cap));\n    cur = cur * 2;\n  }\n  return out;\n}",
  tests: [['retryPlan(1,30,5)', '[1,2,4,8,16]'],
    ['retryPlan(1,8,5)', '[1,2,4,8,8]'],
    ['retryPlan(2,100,3)', '[2,4,8]'],
    ['retryPlan(1,30,0)', '[]']],
  edge: [['retryPlan(5,5,3)', '[5,5,5]'], ['retryPlan(1,1,2)', '[1,1]']],
  ex: "실패하면 다시 시도하는 것은 옳지만, <b>같은 간격으로 계속 두드리면</b> 회복하려는 서버를 다시 넘어뜨립니다. 간격을 두 배씩 늘려 압력을 줄입니다.\n상한이 필요한 이유는 단순합니다. 두 배씩 늘리다 보면 열 번째에는 <b>몇 분씩 기다리게</b> 되어 사용자가 이미 떠난 뒤입니다. 30초 안팎에서 자르는 것이 보통입니다.\n이 함수에 <b>빠진 것이 하나</b> 있습니다 — <b>지터</b>입니다. 간격이 1·2·4로 모두에게 똑같으면 실패한 클라이언트 수천 개가 여전히 같은 순간에 몰려옵니다. 각 간격에 무작위를 섞어야 시점이 흩어집니다.\n그리고 재시도를 붙이기 전에 반드시 확인할 것이 있습니다. <b>그 요청이 여러 번 실행되어도 안전한가.</b> 결제 요청에 무심코 재시도를 걸면 그것이 곧 중복 결제입니다." },

{ track: "cloud", k: "어디로 넘길 것인가 · failoverPick", cat: "debug",
  q: "<code>[[이름, 정상 여부, 우선순위], …]</code> 를 받아 <b>정상인 것 중 우선순위 숫자가 가장 작은</b> 노드의 이름을 돌려주세요. 없으면 <code>null</code> 입니다. 우선순위가 같으면 <b>먼저 나온 쪽</b>입니다.",
  src: "function failoverPick(nodes) {\n  // TODO: 정상인 것만 남기고 우선순위로 정렬하세요\n  return null;\n}",
  sol: "function failoverPick(nodes) {\n  const alive = nodes.filter((n) => n[1]).sort((a, b) => a[2] - b[2]);\n  return alive.length ? alive[0][0] : null;\n}",
  tests: [['failoverPick([["a",false,1],["b",true,2]])', '"b"'],
    ['failoverPick([["a",true,3],["b",true,1]])', '"b"'],
    ['failoverPick([["a",false,1]])', 'null'],
    ['failoverPick([])', 'null']],
  edge: [['failoverPick([["a",true,1],["b",true,1]])', '"a"'],
    ['failoverPick([["z",true,5]])', '"z"']],
  ex: "장애 전환이 고르는 방식입니다. <b>살아 있는 것 중에서 가장 앞선 것</b> 하나를 뽑습니다.\n<code>null</code> 을 돌려주는 경우를 분명히 하는 것이 중요합니다. <b>넘길 곳이 없다</b>는 사실 자체가 경보를 울려야 할 상태인데, 이것을 빈 문자열이나 첫 노드로 얼버무리면 죽은 노드로 트래픽이 갑니다.\n엣지 케이스의 <b>동점 규칙</b>도 정해 두어야 합니다. 규칙이 없으면 실행할 때마다 다른 노드가 뽑혀, 장애 조사에서 '왜 그때는 저쪽으로 갔지' 를 설명할 수 없게 됩니다. 자바스크립트의 <code>sort</code> 는 안정 정렬이라 원래 순서가 유지됩니다.\n실제 전환에서는 이 선택보다 <b>그다음이 더 어렵습니다</b> — 옛 주 노드가 되살아나 자기도 주라고 주장하는 상황(스플릿 브레인)을 막는 장치가 필요합니다." },

/* ───────────── 운영과 복구 ───────────── */
{ track: "cloud", k: "99.9% 는 몇 분인가 · downtimeMinutes", cat: "impl",
  q: "가용성 목표(%)를 받아 <b>30일 기준으로 허용되는 중단 시간(분)</b>을 구해 주세요. 소수 첫째 자리까지 반올림합니다.",
  src: "function downtimeMinutes(pct) {\n  // TODO: 30일을 분으로 바꾸고 허용 비율을 곱하세요\n  return 0;\n}",
  sol: "function downtimeMinutes(pct) {\n  const total = 30 * 24 * 60;\n  return Math.round(total * (100 - pct) / 100 * 10) / 10;\n}",
  tests: [['downtimeMinutes(99.9)', '43.2'],
    ['downtimeMinutes(99)', '432'],
    ['downtimeMinutes(100)', '0'],
    ['downtimeMinutes(99.99)', '4.3']],
  edge: [['downtimeMinutes(99.999)', '0.4'], ['downtimeMinutes(95)', '2160']],
  ex: "'가용성 99.9%' 가 어느 정도인지 숫자로 보면 감이 달라집니다. <b>한 달에 43분</b>입니다 — 배포 한 번 잘못하면 그것으로 끝나는 양입니다.\n9 하나가 늘 때마다 허용 시간이 <b>10분의 1</b>이 됩니다. 99.99%는 4.3분이고, 99.999%는 26초입니다. 26초 안에 사람이 알아채고 대응하는 것은 불가능하므로, 그 수준은 <b>자동 전환 없이는 달성할 수 없습니다.</b>\n그래서 목표를 정하는 일이 곧 <b>비용을 정하는 일</b>입니다. 9 하나를 더 붙이려면 이중화·자동 전환·무중단 배포가 전부 필요해집니다. 모든 서비스에 최고 수준을 적용하면 감당할 수 없습니다.\n계획된 점검을 포함할지도 정해야 합니다. 대부분의 약속은 <b>예고된 점검을 제외</b>하는데, 사용자에게는 예고 여부와 무관하게 안 되는 것은 안 되는 것입니다." },

{ track: "cloud", k: "오류 예산이 얼마나 남았나 · errorBudgetLeft", cat: "impl",
  q: "목표 성공률(%)·전체 요청 수·실패 수를 받아 <b>남은 오류 예산의 비율(%)</b>을 소수 첫째 자리까지 돌려주세요. 다 썼으면 0, 요청이 없으면 100 입니다.",
  src: "function errorBudgetLeft(target, total, failed) {\n  // TODO: 허용되는 실패 수를 먼저 구하고 얼마나 썼는지 보세요\n  return 100;\n}",
  sol: "function errorBudgetLeft(target, total, failed) {\n  if (!total) return 100;\n  const budget = total * (100 - target) / 100;\n  if (budget <= 0) return failed ? 0 : 100;\n  return Math.max(Math.round((1 - failed / budget) * 1000) / 10, 0);\n}",
  tests: [['errorBudgetLeft(99.9, 100000, 50)', '50'],
    ['errorBudgetLeft(99, 1000, 5)', '50'],
    ['errorBudgetLeft(99.9, 0, 0)', '100'],
    ['errorBudgetLeft(99.9, 100000, 100)', '0']],
  edge: [['errorBudgetLeft(100, 1000, 0)', '100'],
    ['errorBudgetLeft(100, 1000, 1)', '0']],
  ex: "오류 예산은 <b>'안정성과 속도 중 무엇이 먼저인가' 를 매번 논쟁하지 않게 해 주는 장치</b>입니다. 예산이 남으면 새 기능을 빠르게 내고, 다 쓰면 안정화에 집중합니다.\n숫자로 두는 것이 핵심입니다. '요즘 장애가 좀 많은 것 같다' 는 사람마다 다르게 느끼지만 '이번 달 예산의 80%를 썼다' 는 같은 판단을 만듭니다.\n마지막 엣지 케이스가 <b>목표를 100%로 잡으면 안 되는 이유</b>를 보여 줍니다. 예산이 0이라 실패 하나에 바로 소진되고, 그러면 아무것도 배포할 수 없게 됩니다. <b>100% 를 목표로 하는 것은 목표가 없는 것과 같습니다.</b>\n예산을 다 썼을 때 무엇을 할지도 미리 정해 두어야 합니다. 정하지 않으면 숫자만 보고 아무 일도 일어나지 않습니다." },

{ track: "cloud", k: "복구 목표를 지켰나 · rpoOk", cat: "debug",
  q: "백업 시각 목록(분)·장애 발생 시각·허용 손실 시간(분)을 받아 RPO 를 지켰는지 판정해 주세요. <b>장애 시각 이전의 가장 최근 백업</b>과의 차이가 허용치 이내여야 합니다. 이전 백업이 없으면 실패입니다.",
  src: "function rpoOk(backups, failAt, rpoMin) {\n  // TODO: 장애 시각 이후의 백업은 쓸 수 없습니다\n  return true;\n}",
  sol: "function rpoOk(backups, failAt, rpoMin) {\n  const before = backups.filter((b) => b <= failAt);\n  if (!before.length) return false;\n  return failAt - Math.max.apply(null, before) <= rpoMin;\n}",
  tests: [['rpoOk([0,60,120],130,15)', 'true'],
    ['rpoOk([0,60,120],150,15)', 'false'],
    ['rpoOk([],100,15)', 'false'],
    ['rpoOk([0],0,15)', 'true']],
  edge: [['rpoOk([100],90,15)', 'false'], ['rpoOk([0,15,30],45,15)', 'true']],
  ex: "RPO 는 <b>'몇 분치 데이터까지 잃어도 되는가'</b> 입니다. 이 함수는 그 약속을 실제 백업 기록으로 검산합니다.\n두 번째 테스트가 흔한 실패입니다. 백업 주기가 60분인데 RPO 가 15분이면 <b>구조적으로 지킬 수 없습니다.</b> 주기를 줄이거나 지속 복제로 바꾸어야 합니다 — 숫자가 방식을 정한다는 말이 이 뜻입니다.\n첫 엣지 케이스가 미묘합니다. <b>장애 이후에 만들어진 백업은 쓸 수 없습니다.</b> 이미 망가진 데이터를 담고 있기 때문입니다. 필터가 없으면 '가장 최근 백업' 을 잘못 골라 통과했다고 오판합니다.\n그리고 이 검산이 통과해도 끝이 아닙니다. <b>복원해 보지 않은 백업은 백업이 아닙니다</b> — 실제로 돌려 보면 빠진 테이블이나 없는 권한이 대개 하나는 나옵니다." },

{ track: "cloud", k: "무엇이 바뀌는지 미리 본다 · planDiff", cat: "review",
  q: "현재 상태와 원하는 상태를 <code>{이름: 설정}</code> 형태로 받아 변경 목록을 만들어 주세요. 새로 생기면 <code>\"+이름\"</code>, 설정이 달라지면 <code>\"~이름\"</code>, 없어지면 <code>\"-이름\"</code> 이고 사전순으로 정렬합니다.",
  src: "function planDiff(current, desired) {\n  // TODO: 원하는 쪽을 먼저 훑고, 그다음 사라진 것을 찾으세요\n  return [];\n}",
  sol: "function planDiff(current, desired) {\n  const out = [];\n  Object.keys(desired).forEach((k) => {\n    if (!(k in current)) out.push(\"+\" + k);\n    else if (current[k] !== desired[k]) out.push(\"~\" + k);\n  });\n  Object.keys(current).forEach((k) => {\n    if (!(k in desired)) out.push(\"-\" + k);\n  });\n  return out.sort();\n}",
  tests: [['planDiff({a:1},{a:1,b:2})', '["+b"]'],
    ['planDiff({a:1},{a:2})', '["~a"]'],
    ['planDiff({a:1,b:2},{a:1})', '["-b"]'],
    ['planDiff({},{})', '[]']],
  edge: [['planDiff({a:1},{b:1})', '["+b","-a"]'],
    ['planDiff({a:1,b:1},{b:2,c:3})', '["+c","-a","~b"]']],
  ex: "인프라 코드 도구의 '계획' 단계가 하는 일입니다. <b>적용하기 전에 무엇이 바뀌는지 보여 주는 것</b>이 실수로 운영 자원을 지우는 사고를 막는 가장 큰 장치입니다.\n세 기호 중 <b><code>-</code> 가 나오면 멈춰야 합니다.</b> 삭제는 되돌릴 수 없고, 데이터베이스가 목록에 있다면 그 안의 데이터까지 함께 사라집니다.\n<code>~</code> 도 안심할 수 없습니다. 어떤 속성은 바꿀 수 없어서 도구가 <b>지우고 다시 만드는 방식</b>으로 처리하는데, 계획에는 그것이 재생성으로 표시됩니다. 무엇이 재생성 대상인지 반드시 읽어야 합니다.\n첫 엣지 케이스가 이름을 바꾸는 경우입니다. 도구에게는 <b>'하나가 생기고 하나가 사라지는 일'</b> 로 보일 뿐이라, 그 사이에 데이터를 옮겨 주지 않습니다. 이름 변경이 위험한 이유입니다." },
];
