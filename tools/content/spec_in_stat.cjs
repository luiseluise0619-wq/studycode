module.exports = {
  track: "stat", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 요약값과 검정 용어",
  source: "./in_stat.cjs",
  lessons: [
    { t: "자료를 한 숫자로 줄일 때", n: 8,
      th: { sum: "요약값은 **무엇을 버리는가**로 고른다. 평균은 극단값을 다 담고, 중앙값은 흘려보내며, 표준편차는 흩어짐만 남긴다.",
        body: [
          { h: "세 요약값이 다르면", t: "평균은 모든 값을 쓰는 대신 <b>극단값 하나에 크게 흔들리고</b>, 중앙값은 절반을 가르는 값이라 흔들리지 않으며, 최빈값은 범주형에 유일하게 쓸 수 있다. 셋이 서로 다르면 분포가 <b>치우쳐 있다</b>는 신호다. 응답 시간처럼 꼬리가 긴 분포에서는 평균이 실제 경험을 대표하지 못하므로 p50·p95 를 함께 본다." },
          { h: "흩어짐과 정밀도", t: "표준편차는 편차 제곱의 평균에 제곱근을 씌운 값이고, 표본에서 모집단을 추정할 때는 <b><code>n-1</code></b> 로 나눈다 — 넘파이 기본은 <code>n</code>, 판다스 기본은 <code>n-1</code> 이라 같은 데이터에서 값이 다르다. 표준오차는 <code>σ/√n</code> 이라 표본을 <b>네 배</b> 늘려야 절반이 되고, 정밀도를 열 배 높이려면 백 배가 필요하다 — 그래서 표본을 늘리기보다 <b>분산을 줄이는</b> 편이 값쌀 때가 많다." }],
        code: { c: "표본 표준편차: √( Σ(x-x̄)² / (n-1) )\nSE = σ / √n        // 네 배 늘려야 절반\n95% CI = x̄ ± 1.96 × SE", cap: "무엇을 버리는지 알고 고른다" },
        key: ["평균은 극단값에 흔들린다", "표본은 n-1 로 나눈다", "표준오차는 √n 에 반비례", "95%에는 1.96"] } },
    { t: "검정이 실제로 말해 주는 것", n: 7,
      th: { sum: "p 값은 '**귀무가설이 참일 때** 이 정도 결과가 나올 확률' 이지 '귀무가설이 참일 확률' 이 아니다. 이 한 줄을 놓치면 나머지가 전부 어긋난다.",
        body: [
          { h: "두 오류와 검정력", t: "<b>1 종 오류</b>는 없는 효과를 있다고 하는 것이고 유의수준 α 가 그 허용치, <b>2 종 오류</b>는 있는 효과를 놓치는 것이고 그 확률이 β 다. 검정력 <code>1-β</code> 는 관례적으로 <b>0.8</b> 을 목표로 하고, 효과 크기·표본 크기·유의수준 중 셋을 정하면 나머지가 계산된다 — 실험 전에 이 계산을 안 하면 효과가 있어도 못 잡는 실험을 몇 주씩 돌린다. 두 오류는 맞바꿈이라 <b>둘 다 줄이려면 표본을 늘려야</b> 한다." },
          { h: "여러 번 보면 생기는 일", t: "20 개 지표를 0.05 로 검정하면 아무 효과가 없어도 하나쯤 유의하게 나오므로 <b>본페로니</b> 같은 보정을 한다. 실험 중간에 자꾸 들여다보다 유의해지는 순간 멈추는 <b>피킹</b> 도 같은 문제다 — 표본 크기를 미리 정하거나 순차 검정을 설계에 넣는다. 상관계수 0 은 '<b>직선 관계가 없다</b>' 는 뜻일 뿐이라 반드시 산점도를 함께 본다(앤스컴의 사중주)." }],
        code: { c: "α/검정수            // 본페로니\n표본 크기를 먼저 정하고 그때까지 보지 않는다\nE[X] = np, Var[X] = np(1-p)", cap: "미리 정해 두는 것이 통계의 절반" },
        key: ["p 값은 가설이 참일 확률이 아니다", "검정력 0.8 이 관례", "여러 번 보면 보정한다", "상관 0 은 직선 관계가 없다는 뜻"] } },
  ],
};
