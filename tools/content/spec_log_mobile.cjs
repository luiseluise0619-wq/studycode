module.exports = {
  track: "mobile", guide: "", xp: 70,
  unit: "로그 분석 — 기기가 남긴 것",
  source: "./log_ops2.cjs", slice: [24, 32],
  lessons: [
    { t: "멈춤과 죽음", n: 4,
      th: { sum: "모바일 사고 보고를 읽을 때는 **누가 죽였는가**를 먼저 본다. 앱이 스스로 죽는 경우보다 시스템이 죽이는 경우가 많다.",
        body: [
          { h: "막힌 스레드와 큰 이미지", t: "응답 없음은 화면을 그리는 스레드가 막혔다는 뜻이고, 시스템은 그것을 몇 초만 참는다. 검사 도구가 그 자리를 먼저 짚어 주므로 **예외가 난 곳보다 위반이 기록된 곳**을 본다. 메모리 부족도 원인이 정해져 있다 — 사진 한 장을 원본 크기로 펼치면 수십 메가바이트라, 목록을 내리며 몇십 장을 만들면 앱에 허용된 몫을 넘는다. 화면에 필요한 크기로 **줄여 읽으면** 사용량이 수십 분의 일이 된다." },
          { h: "판과 빌드가 갈라놓는 것", t: "'일부 기기에서만' 의 정체는 대개 **운영체제 판**이다. 새 판이 요구 사항을 더하면 옛 판에서는 지나가던 호출이 예외가 되므로, 사고 보고를 판 별로 나눠 보면 경계가 또렷하다. '배포판에서만' 은 **배포판에서만 하는 일** 때문이다 — 이름 줄이기가 클래스 이름을 바꾸면 이름을 보고 값을 맞추는 코드가 무너진다. 짧아진 이름이 예외 메시지에 남는 것이 단서이고, 처방은 규칙을 걸어 두고 배포판으로도 시험을 돌리는 것이다." }],
        code: { c: "StrictMode 위반 줄을 먼저 읽는다\nBitmapFactory.Options().apply { inSampleSize = 4 }\n# 사고 보고를 OS 판 별로 나눠 본다", cap: "누가 죽였는지 먼저 본다" },
        key: ["응답 없음은 주 스레드가 막힌 것", "이미지는 줄여 읽는다", "일부 기기 = 대개 OS 판", "배포판 사고는 이름 줄이기"] } },
    { t: "사라지는 것과 새는 것", n: 4,
      th: { sum: "화면은 언제든 사라지고 앱은 언제든 정리된다. 그것을 **정상으로 놓고** 짜지 않으면 상태를 잃거나 메모리를 붙든다.",
        body: [
          { h: "정리되는 것은 정상이다", t: "앞에 있는 앱에 메모리가 필요하면 시스템은 뒤에 있는 앱을 정리한다. 돌아왔을 때 화면은 처음부터 다시 만들어지고, 저장해 둔 상태가 없으면 입력하던 내용이 사라진다. 개발자 설정으로 **언제든 재현할 수 있다**는 점이 이것이 사고가 아니라 설계임을 말해 준다. 잃으면 안 되는 것은 메모리가 아니라 저장소에 임시로 남긴다. 반대로 회수되지 <b>않는</b> 문제도 있다 — 오래 사는 단일 객체의 목록에 화면이 하나씩 쌓이면 회전할 때마다 화면 전체가 붙들린다." },
          { h: "등록에는 해제가 따라온다", t: "누수와 배터리 소모는 뿌리가 같다. 화면이 뒤로 갔는데 위치 갱신이 살아 있으면 밤새 수신기와 잠금이 배터리를 먹고, 리스너가 남아 있으면 죽은 화면이 회수되지 않는다. **등록한 자리마다 짝이 되는 해제**를 두는 규칙 하나로 둘 다 사라진다. 네트워크도 같은 결이다 — 연결이 없다는 것을 시스템이 알려 주는데도 1초마다 무한히 다시 시도하면, 아무것도 못 보내면서 무선 장치만 계속 깨운다." }],
        code: { c: "onSaveInstanceState / 임시 저장은 저장소에\nonDestroy { manager.removeListener(l) }\n# 연결이 없으면 시도하지 않고 복구 신호를 기다린다", cap: "등록했으면 해제한다" },
        key: ["뒤에 있는 앱은 정리된다", "잃으면 안 될 것은 저장소로", "단일 객체가 화면을 붙든다", "끊긴 채 두드리면 배터리만 먹는다"] } },
  ],
};
