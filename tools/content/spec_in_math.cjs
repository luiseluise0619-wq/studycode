module.exports = {
  track: "math", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 세고, 접고, 재는 셈",
  source: "./in_math.cjs",
  lessons: [
    { t: "세는 셈과 접는 셈", n: 8,
      th: { sum: "경우의 수는 **자리마다 남은 개수를 곱하는 일**이고, 나머지 연산은 **큰 수를 만들기 전에 접는 일**이다.",
        body: [
          { h: "순서를 따지는가 아닌가", t: "다섯 개를 줄 세우면 <code>5! = 120</code>, 순서를 따져 둘을 뽑으면 <code>P(5,2) = 20</code>, 순서를 무시하면 <code>C(6,2) = 15</code> 다 — 조합은 '<b>순서를 무시한 순열</b>' 이라 뽑는 순서의 가짓수로 나눈다. 팩토리얼은 아주 빨리 커져서 <code>13!</code> 이면 이미 60 억이다. 모든 순서를 다 해 보는 알고리즘은 n 이 10 만 넘어도 못 쓴다는 감각이 여기서 나온다." },
          { h: "나머지와 벡터", t: "나머지 연산은 곱셈·덧셈과 <b>순서를 바꿔도 되므로</b> 매 단계 접을 수 있다 — 큰 수를 만든 뒤 줄이면 그 사이에 넘친다. 나머지 세계에는 나눗셈이 없어 <b>역원</b>을 곱하는데, 서로소일 때만 존재한다. 벡터의 내적은 자리끼리 곱해 더한 값이고 '<b>얼마나 같은 방향인가</b>' 에 두 길이를 곱한 것이라, 직각이면 0 이고 반대면 음수다 — 길이만 견줄 때는 제곱근을 생략해도 순서가 같다." }],
        code: { c: "(7 * 8) % 5 === (2 * 3) % 5     // 매 단계 접어도 같다\nC(n,2) ≈ n²/2                   // 모든 쌍을 견주는 비용\na·b > 0 같은 쪽 · = 0 직각 · < 0 반대", cap: "곱하기 전에 접고, 견주기 전에 줄인다" },
        key: ["순서를 따지면 순열, 아니면 조합", "나머지는 매 단계 접는다", "역원은 서로소일 때만 있다", "내적의 부호가 방향 관계다"] } },
    { t: "확률이 직관을 배신하는 곳", n: 7,
      th: { sum: "확률에서 틀리는 자리는 정해져 있다 — **드문 일**, **짝의 수**, **비율의 평균**, 그리고 **기댓값만 보는 것**.",
        body: [
          { h: "드문 일과 짝의 수", t: "정확도가 99% 여도 <b>병이 드물면</b> 양성의 절반이 오탐이다 — 기저율을 무시하는 것이 대표적인 직관 오류이고, 이상 탐지·스팸 분류가 정확도 대신 <b>정밀도·재현율</b>을 보는 이유다. 생일 문제가 23 명에서 반반이 되는 것도 사람 수가 아니라 <b>짝의 수</b>가 늘기 때문이다 — 해시 충돌도 값의 가짓수가 N 이면 <code>√N</code> 개만 넣어도 반반이 된다." },
          { h: "평균의 종류와 로그의 크기", t: "'단위 시간당' 같은 <b>비율</b>을 평균 낼 때는 산술평균이 아니라 <b>조화평균</b>이다 — 갈 때 60, 올 때 30 이면 45 가 아니라 40 이고, 느린 구간에 더 오래 머물기 때문이다. 기댓값은 '여러 번 하면 평균적으로' 이지 한 번에 나올 값이 아니라, <b>분산</b>을 함께 봐야 한다. 밑 2 로그는 '몇 번 반으로 접으면 1 이 되는가' 라 아주 천천히 큰다 — 백만이면 20, 10 억이어도 30 이다." }],
        code: { c: "P(병|양성) = 진양성 / (진양성 + 위양성)\n조화평균 = 2ab / (a + b)        // 비율의 평균\nMath.ceil(Math.log2(n))         // 반으로 접는 횟수", cap: "평균 하나로는 알 수 없는 것들" },
        key: ["드문 일에는 기저율을 먼저 본다", "짝의 수는 제곱으로 는다", "비율은 조화평균으로", "기댓값과 함께 분산을 본다"] } },
  ],
};
