const UNIT = "코드 리뷰 — 무엇을 결함이라 부를 것인가";
module.exports = {
  source: "./rev_ops2.cjs", xp: 70,
  entries: [

    { track: "dbt", guide: "", unit: UNIT, lesson: "모델과 설정을 읽는다", slice: [0, 4],
      th: { sum: "데이터 파이프라인의 리뷰는 **틀린 값**보다 **조용히 어긋나는 값**을 찾는 일이다. 실패한 파이프라인은 눈에 띄지만, 성공한 채 틀린 파이프라인은 몇 달을 간다.",
        body: [
          { h: "의존을 손으로 적지 마라", t: "dbt 의 값어치 절반은 **의존 그래프**에서 나온다. `ref()` 로 적으면 실행 순서·환경 분리·계보 문서가 공짜로 따라오지만, 스키마를 손으로 적는 순간 셋이 함께 사라진다. `select *` 도 같은 종류의 새는 구멍이다 — 상류의 변화가 하류로 그대로 흘러가고, 이름이 겹치는 열은 어느 쪽인지 알 수 없다. 날짜나 임계를 문자열로 박아 두는 것도 그렇다: **오늘은 맞지만 내년에는 조용히 틀린** 표가 된다." },
          { h: "막지 않는 검사는 없는 검사다", t: "`not_null` 하나로는 '값이 있다' 까지만 지킨다. 데이터에서 가장 흔하고 가장 늦게 발견되는 사고가 **조인으로 인한 행 증식**이고, 그것을 잡는 것은 유일성 검사다. 그리고 심각도를 경고로 낮추면 깨져도 배포가 지나가므로 결국 아무도 고치지 않는다. 같은 이야기가 **신선도**에도 있다 — 파이프라인이 성공했다는 말과 데이터가 최신이라는 말은 다르다. 상류가 멈추면 어제 것으로 오늘의 보고서가 나간다." }],
        code: { c: "from {{ ref('stg_orders') }}     -- 손으로 적지 않는다\ntests: [unique, not_null]        -- 심각도를 낮추지 않는다\nfreshness: { warn_after: {count: 6, period: hour} }", cap: "성공했다는 말과 맞다는 말은 다르다" },
        key: ["ref() 가 그래프를 만든다", "select * 는 변화를 흘려보낸다", "행 증식은 유일성 검사로 잡는다", "신선도는 성공과 별개다"] } },

    { track: "dl", guide: "", unit: UNIT, lesson: "학습 코드를 읽는다", slice: [4, 8],
      th: { sum: "딥러닝 코드의 결함은 대개 **에러를 내지 않는다** — 학습이 되는 것처럼 보이면서 조용히 망가지거나, 점수를 실제보다 좋게 만든다.",
        body: [
          { h: "조용히 망가지는 자리", t: "기울기를 비우지 않으면 배치마다 더해져 엉뚱한 방향으로 걸음을 내딛지만 **손실은 그래도 내려간다**. 손실 텐서를 그대로 누적하면 계산 그래프가 통째로 살아남아 며칠 뒤 메모리가 터진다. 스케줄러를 밟는 주기를 에폭이 아니라 배치로 잡으면 첫 에폭 안에서 주기가 다 돌아 나머지가 헛돈다. 셋 다 <b>예외가 나지 않는다</b>는 것이 공통점이라, 읽어서 잡는 수밖에 없다." },
          { h: "점수를 부풀리는 자리", t: "정규화 통계를 나누기 전 전체에서 계산하면 검증의 정보가 학습으로 새어 든다. 증강을 평가에까지 걸면 재는 자가 흔들린다. 그리고 가장 흔한 것 — **시험 집합으로 가장 좋은 에폭을 고르고 그 집합으로 점수를 내는 것**은 50번 중 가장 운 좋은 값을 뽑아 오는 일이다. 고르기는 검증 집합으로 하고 시험 집합은 마지막에 한 번만 본다. 씨앗을 고정하지 않으면 두 실험을 견줄 수조차 없다." }],
        code: { c: "optimizer.zero_grad()      # 빠뜨려도 에러가 안 난다\ntotal += loss.item()       # 텐서를 그대로 더하지 않는다\n# 고르기는 val, 최종 점수는 test 한 번", cap: "에러가 없다고 맞는 것은 아니다" },
        key: ["기울기는 우리가 비운다", "텐서 누적은 그래프를 붙든다", "증강은 평가에 걸지 않는다", "고른 데이터로 점수를 내지 않는다"] } },

    { track: "mobile", guide: "", unit: UNIT, lesson: "화면 코드를 읽는다", slice: [8, 12],
      th: { sum: "모바일 리뷰의 두 축은 **주 스레드**와 **생명주기**다. 서버와 달리 화면을 그리는 스레드가 하나뿐이고, 화면은 언제든 사라진다.",
        body: [
          { h: "주 스레드를 막지 마라", t: "화면을 그리는 스레드가 막히면 스크롤도 버튼도 죽고, 오래 막히면 시스템이 앱을 강제로 끝낸다. 네트워크·파일·복호화처럼 **기다리는 일**은 예외 없이 다른 곳으로 보낸다. 메모리도 같은 자리에서 터진다 — 목록은 보이는 것만 만드는 구조인데 이미지를 미리 다 올려 버리면 그 이점을 스스로 버리는 것이다. 그리고 모바일에서 **연결이 끊기는 것은 예외가 아니라 평상시**라, 실패 경로가 없는 코드는 반드시 죽는다." },
          { h: "등록했으면 해제하라", t: "화면은 회전 한 번에도 다시 만들어진다. 그래서 화면을 전역 객체가 붙들거나, 리스너를 떼지 않거나, 예약한 일을 취소하지 않으면 **죽은 화면이 계속 살아 있게** 된다. 셋은 서로 다른 증상으로 나타나지만 뿌리가 같다 — 화면보다 오래 사는 것에 화면을 묶어 두었다. 등록한 자리마다 짝이 되는 해제를 두는 규칙 하나로 함께 사라진다. 권한도 생명주기의 일부다: 필요한 때에 이유와 함께 묻고, **거절도 답으로 다룬다**." }],
        code: { c: "// 기다리는 일은 다른 곳에서\nlifecycleScope.launch { val items = api.fetchFeed() }\n// 등록한 자리마다 짝이 되는 해제\noverride fun onDestroy() { LocationManager.removeListener(l) }", cap: "막지 말고, 등록했으면 해제한다" },
        key: ["주 스레드에서 기다리지 않는다", "보이는 것만 만든다", "연결 끊김은 평상시다", "등록에는 해제가 따라온다"] } },

    { track: "arduino", guide: "", unit: UNIT, lesson: "스케치와 배선을 읽는다", slice: [12, 16],
      th: { sum: "임베디드 리뷰에는 다른 곳에 없는 축이 둘 있다 — **하드웨어가 실제로 죽는다**는 것, 그리고 **보호 장치가 없다**는 것.",
        body: [
          { h: "코드가 부품을 망가뜨린다", t: "핀은 신호를 내보내는 자리이지 힘을 내는 자리가 아니다. 모터를 직접 물리면 당장은 도는 것처럼 보여도 핀이 서서히 죽고, 코일을 끄는 순간의 역전압은 다이오드가 없으면 마이크로컨트롤러까지 넘어뜨린다. 저장 장치도 닳는다 — EEPROM 의 쓰기 수명은 십만 번 남짓이라 루프마다 쓰면 몇 분 만에 그 자리가 죽는다. 입력 핀을 그냥 두면 **떠 있는 상태**가 되어 주변 잡음만으로 눌린 것처럼 읽히므로, 풀업을 켜고 판정을 뒤집는다." },
          { h: "막는 코드와 인터럽트", t: "`delay()` 는 그 시간 동안 보드를 통째로 붙잡는다. 그 사이에 들어온 명령은 버퍼가 차면 버려지고, 임계를 넘겨도 반응하지 못한다. 시간을 재서 지나갔는지 견주는 방식으로 바꾸면 그동안 다른 일을 할 수 있다. 인터럽트 안에서는 **짧고, 기다리지 않고, 막지 않아야** 하므로 직렬 출력 같은 것을 부르면 안 되고, 인터럽트가 건드리는 변수에는 `volatile` 을 붙여야 컴파일러가 읽기를 지우지 않는다. 램이 2KB 인 세계에서는 `String` 이어 붙이기도 시한폭탄이다." }],
        code: { c: "pinMode(BTN, INPUT_PULLUP);          // 떠 있지 않게\nvolatile unsigned long count = 0;    // 인터럽트가 바꾸는 값\nif (millis() - last >= 1000) { ... } // delay 대신", cap: "부품이 죽는 것이 언제나 먼저다" },
        key: ["핀은 힘을 내는 자리가 아니다", "코일에는 다이오드", "EEPROM 은 닳는다", "인터럽트는 짧게, volatile 로"] } },
  ],
};
