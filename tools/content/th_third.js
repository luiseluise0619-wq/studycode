/* 3차 이론 — 초보자가 세 번째로 여는 유닛들.

   앞서 첫 유닛 23개, 두 번째 유닛 13개를 다시 썼다. 여기는 그 다음 줄이다.
   web 폼·시맨틱, react 기초, c 배열과 문자열, cpp 참조와 포인터.

   규칙은 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기
   문단 순서: 이게 뭔지 → 어떻게 쓰는지 → 어디서 틀리는지 */
module.exports = {

web: [
{
  unit: "폼과 입력", lesson: "폼 기초",
  th: {
    sum: "폼은 사용자가 값을 적어 **보내는** 묶음이다. 입력칸과 보내기 버튼이 한 세트다.",
    body: [
      { h: "무엇이 한 묶음인가", t: "`<form>` 안에 있는 입력칸들이 함께 보내진다. 밖에 있는 칸은 아무리 옆에 있어도 같이 가지 않는다. 값이 서버에 안 도착하면 그 칸이 `<form>` 안에 있는지부터 본다." },
      { h: "이름이 있어야 값이 간다", t: "각 입력칸에는 `name` 을 적는다. 서버는 이 이름으로 값을 찾는다. `name` 이 없으면 그 칸의 값은 **아예 전송되지 않는다** — 화면에는 멀쩡히 보이는데 서버에는 안 온다." },
      { h: "보내는 버튼", t: "`<button type=\"submit\">` 이어야 폼이 제출된다. `type=\"button\"` 은 아무것도 보내지 않는다. `<button>` 안에 `type` 을 안 적으면 폼 안에서는 자동으로 submit 이 되므로, 보내면 안 되는 버튼에는 반드시 `type` 을 적어 준다." },
    ],
    code: { c: "<form action=\"/join\" method=\"post\">\n  <input name=\"email\" type=\"email\">\n  <button type=\"submit\">보내기</button>\n</form>", cap: "form 안에 있고 name 이 있어야 전송된다" },
    key: ["`<form>` 안의 칸만 함께 간다", "`name` 이 없으면 전송되지 않는다", "보내려면 `type=\"submit\"`"],
  },
},
{
  unit: "폼과 입력", lesson: "input 종류",
  th: {
    sum: "`type` 을 바꾸면 입력칸의 **모양과 검사**가 함께 바뀐다. 모바일 자판도 달라진다.",
    body: [
      { h: "자주 쓰는 종류", t: "`text` 는 아무 글자, `email` 은 이메일 모양, `number` 는 숫자, `password` 는 가려지는 글자, `date` 는 날짜 고르기다. `email` 이나 `number` 를 쓰면 휴대폰에서 알맞은 자판이 뜬다." },
      { h: "브라우저 검사는 덤이다", t: "`type=\"email\"` 은 형식이 틀리면 제출을 막아 준다. 편하지만 **서버 검증을 대신하지 못한다.** 개발자 도구로 얼마든지 우회할 수 있어서, 서버에서 다시 확인하지 않으면 이상한 값이 그대로 들어온다." },
      { h: "필수와 범위", t: "`required` 는 비어 있으면 제출을 막고, `min`·`max` 는 숫자 범위를 정한다. `maxlength` 는 글자 수를 제한한다. 이것들도 사용자 편의를 위한 것이지 보안 장치가 아니다." },
    ],
    code: { c: "<input name=\"age\" type=\"number\" min=\"0\" max=\"120\" required>\n<input name=\"pw\" type=\"password\" minlength=\"8\">", cap: "브라우저 검사는 편의일 뿐 서버 검증이 아니다" },
    key: ["`type` 이 자판과 검사를 바꾼다", "브라우저 검사는 서버 검증이 아니다", "`required`·`min`·`max` 로 조건을 준다"],
  },
},
{
  unit: "폼과 입력", lesson: "입력 도우미",
  th: {
    sum: "이름표(`label`)는 꾸미기가 아니라 **입력칸과 이어 주는 장치**다. 이어져야 눌러서 커서가 간다.",
    body: [
      { h: "for 와 id 를 맞춘다", t: "`<label for=\"email\">` 의 `for` 와 `<input id=\"email\">` 의 `id` 가 같아야 한 쌍이 된다. 이어지면 이름표를 눌러도 커서가 입력칸으로 간다 — 손가락으로 쓰는 화면에서 특히 편해진다." },
      { h: "안 이어지면 생기는 일", t: "글자만 위에 적어 두면 화면 낭독기는 그 둘이 한 쌍인 줄 모른다. '편집란' 이라고만 읽어서 무엇을 넣는 칸인지 알 수 없다. 눌러도 커서가 가지 않아 누르는 영역도 좁아진다." },
      { h: "placeholder 는 이름표가 아니다", t: "`placeholder` 는 값을 적기 시작하면 사라진다. 그래서 그것만 두면 나중에 '이 칸이 뭐였지' 를 알 수 없다. 이름표는 따로 두고, `placeholder` 는 예시를 보여 주는 데만 쓴다." },
    ],
    code: { c: "<label for=\"email\">이메일</label>\n<input id=\"email\" name=\"email\" type=\"email\"\n       placeholder=\"you@example.com\">", cap: "for 와 id 가 같아야 이어진다" },
    key: ["`label[for]` 와 `input[id]` 를 맞춘다", "안 이어지면 무슨 칸인지 못 읽는다", "`placeholder` 는 이름표를 대신 못 한다"],
  },
},
{
  unit: "시맨틱 & 구조", lesson: "시맨틱 태그",
  th: {
    sum: "시맨틱 태그는 '이 구역이 무엇인지' 를 이름으로 밝히는 태그다. `div` 는 아무 뜻이 없다.",
    body: [
      { h: "구역에 이름을 붙인다", t: "`<header>` 머리글, `<nav>` 길잡이, `<main>` 본문, `<article>` 완결된 글, `<footer>` 바닥글이다. 화면에 보이는 모양은 `div` 와 거의 같지만, 기계가 읽는 뜻이 다르다." },
      { h: "왜 뜻이 필요한가", t: "화면 낭독기 사용자는 이 표지를 따라 '본문으로 건너뛰기' 를 한다. `div` 만 있으면 건너뛸 표지가 없어 메뉴부터 끝까지 들어야 한다. 검색 엔진도 어디가 본문인지 이 태그로 판단한다." },
      { h: "class 이름으로는 안 된다", t: "`<div class=\"header\">` 라고 이름을 지어도 기계에게는 그냥 `div` 다. class 는 사람과 CSS 를 위한 이름일 뿐이다. 뜻을 밝히려면 태그 자체를 바꿔야 한다." },
    ],
    code: { c: "<header>가게 이름</header>\n<main>\n  <article>글 내용</article>\n</main>\n<footer>© 2026</footer>", cap: "class 이름이 아니라 태그가 뜻을 정한다" },
    key: ["구역은 뜻이 있는 태그로", "`<main>` 은 페이지에 하나", "class 이름은 뜻을 만들지 못한다"],
  },
},
{
  unit: "시맨틱 & 구조", lesson: "구획 나누기",
  th: {
    sum: "문서를 나눌 때는 '이 덩어리가 혼자 떼어 놔도 말이 되는가' 를 기준으로 고른다.",
    body: [
      { h: "article 과 section", t: "`<article>` 은 떼어 내도 말이 되는 덩어리다 — 블로그 글 하나, 상품 카드 하나. `<section>` 은 한 문서 안의 주제 구분이다 — '소개', '가격' 같은 묶음. 헷갈리면 '이것만 퍼가도 되나' 를 물어본다." },
      { h: "제목과 짝지어 쓴다", t: "`<section>` 에는 보통 제목(`<h2>` 등)이 함께 온다. 제목 없는 `section` 은 그냥 `div` 를 쓰는 편이 낫다. 뜻 없이 쓰면 낭독기가 '구역' 이라고만 읽어 오히려 방해가 된다." },
      { h: "무조건 시맨틱이 낫지는 않다", t: "단지 여백을 주려고 감싸는 상자는 `<div>` 가 맞다. 뜻이 없는데 시맨틱 태그를 쓰면 기계가 잘못된 구조를 읽는다. **뜻이 있을 때만** 뜻 있는 태그를 쓴다." },
    ],
    code: { c: "<article>\n  <h2>가을 신메뉴</h2>\n  <p>…</p>\n</article>\n\n<div class=\"spacer\"></div>   <!-- 뜻이 없으면 div -->", cap: "떼어 놔도 말이 되면 article" },
    key: ["떼어 놔도 말이 되면 `<article>`", "`<section>` 에는 제목을 함께", "뜻이 없으면 `<div>` 가 맞다"],
  },
},
{
  unit: "시맨틱 & 구조", lesson: "div와 span",
  th: {
    sum: "`div` 와 `span` 은 뜻이 없는 상자다. 차이는 **줄을 차지하느냐**뿐이다.",
    body: [
      { h: "줄을 차지하는가", t: "`div` 는 한 줄을 통째로 차지한다(블록). `span` 은 글자 사이에 끼어든다(인라인). 문단 가운데 한 단어만 색을 바꾸려면 `span`, 영역을 묶으려면 `div` 다." },
      { h: "언제 쓰나", t: "꾸미거나 자바스크립트로 다루려고 감쌀 때 쓴다. 뜻이 있는 덩어리라면 `article`·`nav` 같은 태그가 먼저다. 뜻이 없을 때 남는 선택지가 `div` 와 `span` 이다." },
      { h: "span 안에 div 를 넣지 않는다", t: "인라인 안에 블록을 넣으면 브라우저가 구조를 제멋대로 고친다. 화면이 이상하게 어긋나는데 원인이 잘 안 보인다. 감쌀 때는 바깥이 블록인지 인라인인지 먼저 확인한다." },
    ],
    code: { c: "<div class=\"card\">\n  <p>가격 <span class=\"won\">3000원</span></p>\n</div>", cap: "div 는 한 줄, span 은 글자 사이" },
    key: ["`div` 는 블록, `span` 은 인라인", "뜻이 있으면 시맨틱 태그가 먼저", "`span` 안에 `div` 를 넣지 않는다"],
  },
},
],

react: [
{
  unit: "React 기초", lesson: "컴포넌트와 JSX",
  th: {
    sum: "컴포넌트는 **화면 조각을 만들어 돌려주는 함수**다. 그 조각을 적는 문법이 JSX 다.",
    body: [
      { h: "함수가 화면을 돌려준다", t: "`function App() { return <div>안녕</div>; }` 처럼 쓴다. 이름은 **대문자로 시작**해야 한다. 소문자로 시작하면 React 가 HTML 태그로 알아듣고 그런 태그가 없다며 조용히 아무것도 안 그린다." },
      { h: "JSX 는 HTML 이 아니다", t: "생김새는 비슷하지만 자바스크립트다. 그래서 `class` 대신 `className`, `for` 대신 `htmlFor` 를 쓴다. 이 둘은 자바스크립트의 예약어라 그대로 쓸 수 없기 때문이다." },
      { h: "돌려주는 것은 하나여야 한다", t: "나란히 두 덩어리를 돌려주면 오류가 난다. 하나로 감싸야 하는데, 굳이 상자를 만들기 싫으면 빈 태그 `<> </>` 로 감싼다. 화면에는 아무것도 안 나오면서 묶어만 준다." },
    ],
    code: { c: "function App() {\n  return (\n    <>\n      <h1 className=\"t\">안녕</h1>\n      <p>반가워요</p>\n    </>\n  );\n}", cap: "대문자로 시작하고, 하나로 감싼다" },
    key: ["컴포넌트 이름은 대문자로", "`class` 가 아니라 `className`", "돌려주는 덩어리는 하나"],
  },
},
{
  unit: "React 기초", lesson: "props 와 state",
  th: {
    sum: "`props` 는 **밖에서 받은 값**, `state` 는 **자기가 들고 있는 값**이다. 바꿀 수 있는 것은 state 뿐이다.",
    body: [
      { h: "받은 값과 가진 값", t: "부모가 넘겨 준 것이 props 다. 자식은 읽기만 한다. 반면 state 는 그 컴포넌트가 직접 들고 바꾸는 값이다 — 눌린 횟수, 입력한 글자 같은 것들이다." },
      { h: "바꾸려면 setter 를 부른다", t: "`const [n, setN] = useState(0)` 에서 `n` 을 직접 고쳐도 화면은 안 바뀐다. React 는 `setN` 이 불린 것을 보고 다시 그리기 때문이다. 값만 바꾸면 데이터와 화면이 어긋난 채로 남는다." },
      { h: "무엇을 state 로 둘까", t: "다른 값에서 계산할 수 있으면 state 로 두지 않는다. 예를 들어 목록과 '목록 개수' 를 둘 다 state 로 두면 둘이 어긋나기 시작한다. 개수는 그때그때 세면 된다." },
    ],
    code: { c: "function Counter({ start }) {   // props\n  const [n, setN] = React.useState(start);  // state\n  return <button onClick={() => setN(n + 1)}>{n}</button>;\n}", cap: "props 는 읽기, state 는 setter 로만" },
    key: ["`props` 는 받은 값, 못 고친다", "`state` 는 setter 로만 바꾼다", "계산할 수 있으면 state 로 두지 않는다"],
  },
},
{
  unit: "React 더 배우기", lesson: "이벤트와 폼",
  th: {
    sum: "React 에서 이벤트는 **함수를 넘겨** 처리한다. 부르지 말고 넘겨야 한다.",
    body: [
      { h: "넘기기와 부르기", t: "`onClick={handle}` 은 함수를 넘긴다. `onClick={handle()}` 은 그리는 순간 **바로 실행**하고 그 결과를 넘긴다. 버튼을 누르지도 않았는데 실행되는 사고가 여기서 난다. 인자가 필요하면 `onClick={() => handle(1)}` 처럼 감싼다." },
      { h: "입력칸은 상태와 이어 둔다", t: "`value={text}` 만 주고 `onChange` 를 빼면 타이핑이 안 먹힌다. React 가 매번 상태의 값으로 되돌려 놓기 때문이다. 입력칸이 얼어붙은 것처럼 보이면 이걸 의심한다." },
      { h: "제출은 기본 동작을 막는다", t: "폼을 제출하면 브라우저가 페이지를 새로 불러온다. React 앱에서는 대개 원하지 않으므로 `e.preventDefault()` 로 막는다. 안 막으면 화면이 깜빡이며 상태가 전부 초기화된다." },
    ],
    code: { c: "<input value={text}\n       onChange={e => setText(e.target.value)} />\n<form onSubmit={e => { e.preventDefault(); 보내기(); }}>", cap: "함수를 넘기고, 기본 동작은 막는다" },
    key: ["`onClick={handle}` — 괄호를 붙이지 않는다", "`value` 에는 `onChange` 가 짝", "제출은 `preventDefault()`"],
  },
},
{
  unit: "React 더 배우기", lesson: "Hooks 맛보기",
  th: {
    sum: "훅은 `use` 로 시작하는 함수다. 컴포넌트에 **기억과 부수 작업**을 붙여 준다.",
    body: [
      { h: "가장 많이 쓰는 둘", t: "`useState` 는 값을 기억한다. `useEffect` 는 화면을 그린 **뒤에** 할 일을 적는 자리다 — 데이터 불러오기, 타이머 걸기 같은 것들이다." },
      { h: "부르는 자리가 정해져 있다", t: "훅은 컴포넌트 맨 위에서, **항상 같은 순서로** 불러야 한다. `if` 안이나 반복문 안에서 부르면 순서가 달라져 React 가 값을 뒤섞는다. 조건이 필요하면 훅 안쪽에서 가른다." },
      { h: "useEffect 의 두 번째 인자", t: "`useEffect(fn, [])` 의 대괄호는 '이 값들이 바뀌면 다시 해라' 는 목록이다. 빈 배열이면 처음 한 번만 돈다. 아예 빼면 그릴 때마다 매번 돌아서, 그 안에서 상태를 바꾸면 무한히 반복된다." },
    ],
    code: { c: "React.useEffect(() => {\n  document.title = `${n}회`;\n}, [n]);   // n 이 바뀔 때만", cap: "대괄호가 '언제 다시 할지' 를 정한다" },
    key: ["훅은 맨 위에서 같은 순서로", "`useEffect` 는 그린 뒤에 돈다", "두 번째 인자를 빼면 매번 돈다"],
  },
},
],

c: [
{
  unit: "배열과 문자열", lesson: "char 배열과 문자열",
  th: {
    sum: "C 에는 문자열 타입이 없다. 글자를 담은 **배열**이 문자열 역할을 한다.",
    body: [
      { h: "끝을 알리는 글자", t: "문자열의 끝에는 `'\\0'` 이라는 보이지 않는 글자가 붙는다. 함수들은 이것을 만날 때까지가 문자열이라고 판단한다. `\"abc\"` 는 실제로 네 칸을 쓴다 — a, b, c 그리고 `\\0`." },
      { h: "자리를 하나 더 잡는다", t: "글자 5개를 담으려면 배열은 **6칸**이어야 한다. 마지막 한 칸이 `\\0` 자리다. 이 +1 을 빼먹으면 끝 표시를 쓸 자리가 없어 남의 자리를 건드린다." },
      { h: "끝 표시가 없으면", t: "`\\0` 이 없는 배열을 문자열처럼 출력하면 만날 때까지 계속 읽는다. 엉뚱한 글자가 줄줄 나오거나 프로그램이 죽는다. 잘림보다 **끝 표시가 없는 것**이 더 위험하다." },
    ],
    code: { c: "char s[6] = \"hello\";   // 5글자 + '\\0'\nprintf(\"%s\\n\", s);\n\nchar t[5] = {'h','i'};  // 나머지는 0 으로 찬다", cap: "글자 수 + 1 칸이 필요하다" },
    key: ["문자열의 끝은 `'\\0'`", "글자 수보다 한 칸 더 잡는다", "끝 표시가 없으면 계속 읽는다"],
  },
},
{
  unit: "배열과 문자열", lesson: "문자열 끝과 strlen",
  th: {
    sum: "`strlen` 은 `\\0` 을 만날 때까지 센다. 그래서 **끝 표시를 세지 않는다.**",
    body: [
      { h: "길이와 자리 수는 다르다", t: "`char s[10] = \"abc\";` 에서 `strlen(s)` 는 3이고 `sizeof(s)` 는 10이다. 앞엣것은 글자 수, 뒤엣것은 잡아 둔 자리다. 담을 자리를 계산할 때는 `strlen + 1` 이 필요하다." },
      { h: "매번 세는 비용", t: "`strlen` 은 부를 때마다 처음부터 다시 센다. 반복문 조건에 넣으면 매 바퀴 전체를 훑어 전체가 O(n²)이 된다. 길이가 안 변하면 **반복 전에 한 번 재서** 변수에 담아 둔다." },
      { h: "배열이 넘어가면 못 잰다", t: "함수에 배열을 넘기면 크기 정보가 사라지고 주소만 간다. 그래서 함수 안에서 `sizeof` 로 길이를 재면 포인터 크기가 나온다. 길이는 따로 넘기거나 `strlen` 으로 세야 한다." },
    ],
    code: { c: "char s[10] = \"abc\";\nstrlen(s);   // 3\nsizeof(s);   // 10\n\nsize_t n = strlen(s);        // 한 번만 잰다\nfor (size_t i = 0; i < n; i++) { … }", cap: "strlen 은 매번 처음부터 센다" },
    key: ["`strlen` 은 `\\0` 을 세지 않는다", "반복 조건에 넣으면 O(n²)", "함수 안에서는 `sizeof` 로 길이를 못 잰다"],
  },
},
{
  unit: "배열과 문자열", lesson: "2차원 배열과 반복",
  th: {
    sum: "2차원 배열은 '줄과 칸' 으로 된 표다. `a[줄][칸]` 으로 꺼낸다.",
    body: [
      { h: "메모리에는 한 줄로 놓인다", t: "`int a[2][3]` 은 6칸이 **줄 순서대로** 이어져 놓인다. 첫 줄 3칸 다음에 둘째 줄 3칸이 온다. 그래서 안쪽 반복을 칸으로 돌리는 편이 메모리를 순서대로 읽어 빠르다." },
      { h: "반복 순서가 성능을 가른다", t: "바깥을 줄, 안쪽을 칸으로 도는 것이 자연스럽다. 반대로 돌리면 메모리를 건너뛰며 읽어 느려진다. 표가 클수록 차이가 커진다 — 같은 답인데 시간이 몇 배 난다." },
      { h: "함수에 넘길 때", t: "2차원 배열을 함수로 넘길 때는 **둘째 크기를 반드시 적어야** 한다. `void f(int a[][3])` 처럼. 컴파일러가 한 줄이 몇 칸인지 알아야 자리를 계산할 수 있기 때문이다." },
    ],
    code: { c: "int a[2][3] = {{1,2,3},{4,5,6}};\n\nfor (int r = 0; r < 2; r++)\n    for (int c = 0; c < 3; c++)\n        printf(\"%d \", a[r][c]);", cap: "바깥이 줄, 안쪽이 칸" },
    key: ["`a[줄][칸]` 으로 꺼낸다", "안쪽을 칸으로 돌리는 편이 빠르다", "함수에 넘길 때 둘째 크기를 적는다"],
  },
},
],

cpp: [
{
  unit: "참조와 포인터", lesson: "참조(&) 기초",
  th: {
    sum: "참조는 **이미 있는 값에 붙이는 다른 이름**이다. 새 자리를 만들지 않는다.",
    body: [
      { h: "이름을 하나 더 붙인다", t: "`int& r = n;` 이라고 하면 `r` 과 `n` 은 같은 값이다. `r` 을 고치면 `n` 도 바뀐다 — 같은 것이기 때문이다. 포인터와 달리 별표를 붙일 필요가 없어 읽기 쉽다." },
      { h: "만들 때 반드시 묶는다", t: "참조는 선언과 동시에 무엇을 가리킬지 정해야 한다. `int& r;` 만 쓰면 컴파일 오류다. 그리고 한 번 묶이면 **다른 것을 가리키게 바꿀 수 없다** — 이 점이 포인터와 가장 다르다." },
      { h: "함수 인자에서 진가가 난다", t: "`void f(const std::string& s)` 로 받으면 복사가 없어 빠르고, `const` 라 실수로 고칠 수도 없다. 값으로 받으면 큰 문자열이 통째로 복사되어 느려진다." },
    ],
    code: { c: "int n = 1;\nint& r = n;\nr = 9;          // n 도 9 가 된다\n\nvoid f(const std::string& s);   // 복사 없음", cap: "참조는 같은 값의 다른 이름" },
    key: ["참조는 새 자리를 만들지 않는다", "만들 때 반드시 묶는다", "읽기만 하면 `const&` 로 받는다"],
  },
},
{
  unit: "참조와 포인터", lesson: "포인터(*) 기초",
  th: {
    sum: "포인터는 **주소를 담는 값**이다. 참조와 달리 비어 있을 수도, 가리키는 곳을 바꿀 수도 있다.",
    body: [
      { h: "주소를 담고 꺼낸다", t: "`int* p = &n;` 에서 `&n` 은 n 의 주소다. `*p` 는 그 주소에 있는 값이다. 별표가 선언에 붙으면 '주소를 담는다', 쓸 때 붙으면 '가리키는 곳' 이라는 뜻이라 헷갈리기 쉽다." },
      { h: "비어 있을 수 있다", t: "포인터는 `nullptr` 일 수 있다. 그 상태로 `*p` 를 하면 프로그램이 죽는다. 그래서 쓰기 전에 `if (p)` 로 확인한다. 참조는 늘 무언가를 가리키므로 이 확인이 필요 없다." },
      { h: "언제 어느 쪽인가", t: "'없을 수도 있다' 를 표현해야 하면 포인터, 항상 있으면 참조를 쓴다. 요즘 C++ 에서는 직접 `new`·`delete` 하는 대신 `unique_ptr` 같은 것에 맡겨 자동으로 정리되게 한다." },
    ],
    code: { c: "int n = 1;\nint* p = &n;\nif (p) *p = 9;      // 확인하고 쓴다\n\nint* q = nullptr;   // 비어 있음", cap: "쓰기 전에 비었는지 확인한다" },
    key: ["`&n` 은 주소, `*p` 는 그 값", "포인터는 비어 있을 수 있다", "없을 수도 있으면 포인터"],
  },
},
{
  unit: "참조와 포인터", lesson: "값 전달 vs 참조 전달",
  th: {
    sum: "값으로 넘기면 **복사본**이 간다. 참조로 넘기면 원본이 간다. 이 차이가 성능과 결과를 모두 바꾼다.",
    body: [
      { h: "복사는 공짜가 아니다", t: "`void f(std::vector<int> v)` 는 벡터를 통째로 복사한다. 원소가 많으면 그만큼 시간과 메모리를 쓴다. 읽기만 할 거면 `const std::vector<int>& v` 로 받아 복사를 없앤다." },
      { h: "고치려면 참조로", t: "값으로 받은 매개변수를 고쳐도 부른 쪽은 그대로다. 사본을 고치고 버리기 때문이다. 컴파일 오류가 나지 않아 '분명 고쳤는데 안 바뀐다' 로 한참 헤맨다." },
      { h: "기준으로 삼을 규칙", t: "읽기만 하면 `const&`, 고치면 `&`, 아주 작은 값(`int` 같은)은 그냥 값으로 받는다. 작은 값은 복사가 주소를 넘기는 것보다 오히려 싸다." },
    ],
    code: { c: "void 읽기(const std::vector<int>& v);  // 복사 없음\nvoid 고치기(std::vector<int>& v);      // 원본이 바뀐다\nvoid 작은값(int n);                    // 그냥 값으로", cap: "읽기는 const&, 고치기는 &" },
    key: ["값 전달은 복사본이 간다", "고치려면 참조로 받는다", "작은 값은 그냥 값으로"],
  },
},
],

};
