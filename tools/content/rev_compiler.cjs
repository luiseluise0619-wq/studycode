/* compiler 트랙 코드 리뷰 — 작은 컴파일러·인터프리터 코드에 한 줄 지적을 단다. */
module.exports = [
{
  t: "review", k: "숫자를 한 글자만 읽는 렉서", cat: "review", d: 2, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function lex(src) {\n  const out = [];\n  for (let i = 0; i < src.length; i++) {\n    const c = src[i];\n    if (c === \" \") continue;\n    if (c >= \"0\" && c <= \"9\") out.push({ t: \"num\", v: Number(c) });\n    else out.push({ t: \"op\", v: c });\n  }\n  return out;\n}",
  items: [
    { txt: "숫자를 <b>한 글자씩</b> 토큰으로 만든다. <code>12</code> 가 <code>1</code> 과 <code>2</code> 두 토큰이 되어 파서는 문법 오류로 보거나 엉뚱한 식을 만들므로, 이어지는 자릿수를 모두 삼키는 반복이 필요하다.", bad: true },
    { txt: "식별자와 키워드를 전혀 다루지 않아 <code>a</code> 나 <code>if</code> 같은 입력이 모두 연산자 토큰이 된다. <b>글자로 시작하면 이름을 끝까지 읽고</b>, 예약어 목록과 대조해 키워드인지 판정하는 것이 렉서의 몫이다.", bad: true },
    { txt: "공백만 건너뛰고 줄바꿈·탭은 연산자로 만든다 — 빈칸 종류를 한데 묶어 처리해야 하며, 그러지 않으면 줄을 나눈 입력이 통째로 깨진다.", bad: true },
    { txt: "토큰에 원본 위치가 없으면 나중에 오류 메시지가 어디를 가리켜야 할지 알 수 없다 — 이 렉서가 약속한 범위에는 들어 있지 않지만, 줄과 열을 토큰마다 함께 담아야 한다.", bad: false },
    { txt: "문자열을 코드 유닛 단위로 훑으므로 UTF-16 서러게이트 쌍이 한가운데서 쪼개져 깨진 글자가 토큰에 실릴 수 있다 — 이모지를 식별자로 허용하는 언어가 아니라면 드문 경우이지만, 코드 포인트 단위로 순회하도록 바꿔야 한다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>여러 자리 숫자</b>를 못 읽는다 — 렉서의 기본 임무가 '이어진 글자를 하나의 토큰으로 묶는 것' 이다. (2) <b>이름과 키워드</b>를 다루지 않는다. (3) <b>줄바꿈·탭</b>이 연산자가 된다. 셋 다 '어떤 글자 무리를 어떤 토큰으로 묶을지' 를 정하지 않은 데서 온다.\n디스트랙터: 토큰 위치는 오류 메시지를 위해 필요하지만 이 함수가 약속한 범위 밖이고, 서러게이트 쌍은 이런 언어에서 드문 경우다.",
},
{
  t: "review", k: "좌재귀로 쓴 문법", cat: "review", d: 3, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "// expr := expr '+' term | term\nfunction parseExpr() {\n  const left = parseExpr();\n  if (peek() === \"+\") { next(); return { op: \"+\", left, right: parseTerm() }; }\n  return left;\n}",
  items: [
    { txt: "뺄셈을 다루지 않아 같은 우선순위의 연산자를 놓친다 — 문법 주석에 적힌 범위가 덧셈뿐이므로 이 코드가 약속을 어긴 것은 아니지만, 실전에서는 함께 처리하는 편이 자연스럽다.", bad: false },
    { txt: "<b>좌재귀</b>라 <code>parseExpr</code> 이 토큰을 하나도 소비하지 않고 자기 자신을 부른다. 입력과 무관하게 곧바로 스택이 넘치므로, 재귀 하강에서는 좌재귀를 <b>반복문</b>으로 펴서 써야 한다.", bad: true },
    { txt: "<code>peek</code> 과 <code>next</code> 가 전역 상태를 쓰므로 파서를 두 개 동시에 굴릴 수 없다 — 학습용 파서에서 흔히 두는 단순화이기는 하지만, 읽기 위치를 객체에 담아 넘겨야 한다.", bad: false },
    { txt: "덧셈이 여러 번 나오는 식을 하나만 처리하고 끝낸다 — <code>1 + 2 + 3</code> 을 왼쪽부터 접으려면 <code>while (peek() === \"+\")</code> 로 <b>계속 이어 붙이는</b> 형태여야 한다.", bad: true },
    { txt: "오른쪽 피연산자를 <code>parseTerm</code> 으로 읽어 우선순위 계층을 지킨 것은 올바르다 — 곱셈처럼 더 강하게 묶여야 하는 연산을 아래 단계에 맡기고, 이 단계는 자기 우선순위의 연산자만 다루는 재귀 하강의 정석 구조다.", bad: false },
  ],
  ex: "결함 2가지. (1) <b>좌재귀</b>는 재귀 하강에서 그대로 무한 재귀가 된다 — 진행을 만드는 토큰 소비가 재귀보다 먼저 와야 한다. (2) 그래서 <b>반복으로 펴야</b> 여러 번의 덧셈을 왼쪽 결합으로 접을 수 있다.\n디스트랙터: 뺄셈은 주석에 적힌 문법의 범위 밖이고, 전역 상태는 학습용 단순화다. 오른쪽을 <code>parseTerm</code> 으로 읽은 것은 우선순위를 지키는 올바른 구조다.",
},
{
  t: "review", k: "결합 방향이 뒤집힌 파서", cat: "review", d: 3, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "// term := factor ('-' term)?\nfunction parseTerm() {\n  const left = parseFactor();\n  if (peek() === \"-\") { next(); return { op: \"-\", left, right: parseTerm() }; }\n  return left;\n}",
  items: [
    { txt: "<code>peek</code> 가 입력 끝에서 무엇을 돌려주는지 코드만으로는 알 수 없어 <code>undefined</code> 비교가 될 수 있다 — 토큰 스트림 계약에 달린 문제이며 이 조각의 초점은 아니다.", bad: false },
    { txt: "노드에 위치 정보가 없어 나중에 오류를 가리키기 어렵다 — 파싱 결과의 정확성과는 별개의 이야기이지만, 각 노드에 시작·끝 위치를 실어야 한다.", bad: false },
    { txt: "오른쪽에서 <code>parseTerm</code> 을 다시 부르므로 <b>우결합</b>으로 묶인다. <code>10 - 3 - 2</code> 가 <code>10 - (3 - 2)</code> 가 되어 9 를 내놓지만 뺄셈은 왼쪽 결합이므로 5 가 맞고, 문법이 아니라 <b>의미가 틀린다</b>.", bad: true },
    { txt: "왼쪽 결합으로 만들려면 <code>while</code> 로 돌면서 <b>지금까지 만든 노드를 왼쪽에 두고</b> 새 항을 오른쪽에 붙여야 한다 — 재귀로 쓰면 자연스럽게 우결합이 되므로, 결합 방향은 반복이냐 재귀냐로 갈린다.", bad: true },
    { txt: "문법 주석과 구현이 일치하므로 코드 자체는 주석을 지킨다 — 그러나 <b>주석에 적힌 문법이 이미 우결합</b>이므로, 고쳐야 할 것은 코드가 아니라 문법과 코드 둘 다이다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>우결합으로 묶인다</b> — 문법은 통과하는데 계산 결과가 틀리는, 가장 찾기 어려운 종류의 버그다. (2) 왼쪽 결합은 <b>반복으로 접어야</b> 나온다. (3) 주석의 문법 자체가 우결합이므로 <b>문법과 코드를 함께</b> 고쳐야 한다.\n디스트랙터: <code>peek</code> 가 입력 끝에서 무엇을 돌려주는지는 토큰 스트림의 계약에 달린 문제라 이 조각만으로는 판단할 수 없고, 노드에 위치를 담는 것은 진단 품질을 위한 개선 항목이지 파싱 결과의 정확성과는 무관하다.",
},
{
  t: "review", k: "스코프가 하나뿐인 심볼 테이블", cat: "review", d: 3, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "const symbols = new Map();\n\nfunction declare(name, type) {\n  symbols.set(name, type);\n}\nfunction lookup(name) {\n  return symbols.get(name);\n}",
  items: [
    { txt: "스코프가 하나뿐이라 <b>안쪽 블록의 선언이 바깥 것을 덮어쓴다</b>. 블록을 벗어나도 되돌아오지 않으므로 함수 지역 변수가 전역처럼 살아남고, 서로 다른 함수의 같은 이름이 충돌한다.", bad: true },
    { txt: "<code>Map</code> 대신 평범한 객체를 쓰면 <code>__proto__</code> 같은 이름이 프로토타입을 건드릴 수 있다 — <code>Map</code> 을 고른 이 코드는 오히려 그 함정을 피한 셈이다.", bad: false },
    { txt: "타입만 저장하고 선언 위치를 담지 않아 '여기서 처음 선언됨' 같은 메시지를 만들 수 없다 — 지금 구조의 결함이라기보다 확장에 가깝지만, 선언 위치를 함께 저장해야 한다.", bad: false },
    { txt: "같은 이름을 <b>다시 선언해도 조용히 덮어쓴다</b>. 대부분의 언어에서 같은 스코프의 중복 선언은 오류이므로, 이미 있으면 그 자리에서 진단을 내야 사용자가 실수를 안다.", bad: true },
    { txt: "찾지 못했을 때 <code>undefined</code> 를 돌려주므로 호출자가 '선언되지 않음' 과 '타입이 undefined' 를 구별할 수 없다 — 없는 이름은 별도의 결과로 알려 주도록 바꿔야 하며, 그러지 않으면 진단이 엉뚱한 곳에서 난다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>스코프가 없다</b> — 들어가고 나올 수 있는 층이 있어야 지역 변수와 섀도잉이 성립한다. (2) <b>중복 선언을 잡지 않는다</b> — 조용히 덮어쓰면 사용자는 자기 실수를 모른다. (3) <b>없음과 값을 구별할 수 없다</b>. 층을 쌓는 구조(부모를 가리키는 스코프)로 바꾸면 셋이 함께 풀린다.\n디스트랙터: <code>Map</code> 선택은 오히려 잘한 부분이고, 선언 위치 저장은 다음 단계의 확장이다.",
},
{
  t: "review", k: "단축 평가를 하지 않는 평가기", cat: "review", d: 2, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function evalNode(n, env) {\n  if (n.op === \"&&\") {\n    const l = evalNode(n.left, env);\n    const r = evalNode(n.right, env);\n    return l && r;\n  }\n  ...\n}",
  items: [
    { txt: "오른쪽을 <b>먼저 평가하고 나서</b> 논리곱을 계산한다. 왼쪽이 거짓이면 오른쪽은 실행되지 않아야 하는데, <code>p != null &amp;&amp; p.name</code> 같은 흔한 관용구가 그대로 터진다.", bad: true },
    { txt: "부수효과가 있는 오른쪽 식이 <b>언제나 실행된다</b>. 함수 호출이나 대입이 들어 있으면 프로그램의 의미가 달라지므로, 단축 평가는 최적화가 아니라 <b>언어 규칙</b>으로 지켜야 한다.", bad: true },
    { txt: "재귀로 트리를 훑으므로 깊은 식에서 스택이 넘칠 수 있다 — 사람이 쓰는 식의 깊이에서는 거의 문제가 되지 않지만, 명시적 스택으로 바꿔야 한다.", bad: false },
    { txt: "노드 종류를 <code>op</code> 문자열로 분기하므로 오타를 컴파일 시점에 잡을 수 없다 — 열거형이나 태그 유니온으로 바꾸면 좋겠지만, 동작의 정확성과는 다른 층위다.", bad: false },
    { txt: "자바스크립트의 <code>&amp;&amp;</code> 로 결과를 만들면 대상 언어의 진리값 규칙이 아니라 호스트 언어의 규칙이 적용된다 — 두 언어의 규칙이 다르면 결과가 갈리므로 명시적으로 변환해야 한다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>단축 평가가 없다</b> — 왼쪽이 거짓이면 오른쪽은 아예 실행되지 않아야 한다. (2) 그래서 <b>부수효과의 실행 여부</b>가 달라져 프로그램의 의미가 바뀐다. (3) <b>호스트 언어의 진리값 규칙</b>이 새어 든다 — 대상 언어의 규칙으로 명시해 변환해야 한다.\n디스트랙터: 재귀로 트리를 훑는 깊이는 사람이 쓰는 식의 규모에서는 문제가 되지 않고, 노드 종류를 문자열로 분기하는 것은 타입 안전성의 아쉬움일 뿐 지금 코드가 잘못된 값을 내놓는 이유가 아니다.",
},
{
  t: "review", k: "어디가 틀렸는지 말하지 않는 진단", cat: "review", d: 2, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function expect(kind) {\n  if (peek().t !== kind) {\n    throw new Error(\"syntax error\");\n  }\n  return next();\n}",
  items: [
    { txt: "메시지에 <b>위치도 기대한 것도 실제로 본 것도</b> 없다. 사용자는 파일 어디를 고쳐야 할지 알 수 없으므로, 최소한 줄·열과 '무엇을 기대했는데 무엇이 왔다' 를 담아야 한다.", bad: true },
    { txt: "첫 오류에서 <b>예외로 즉시 중단</b>하므로 한 번에 하나씩만 고칠 수 있다. 실용적인 컴파일러는 그 자리에서 회복해 계속 훑으면서 <b>여러 진단을 모아</b> 한꺼번에 보여 준다.", bad: true },
    { txt: "입력 끝에서 <code>peek()</code> 가 무엇을 돌려주는지에 따라 <code>.t</code> 읽기가 터질 수 있다 — 끝을 나타내는 토큰을 두어 <b>흐름 안에서</b> 다루는 편이 안전하다.", bad: true },
    { txt: "일반 <code>Error</code> 는 잡는 쪽에서 다른 오류와 구분되지 않는다 — 메시지에 정보가 없다는 문제와 견주면 부차적이지만, 전용 오류 타입을 만들어야 한다.", bad: false },
    { txt: "성공했을 때 소비한 토큰을 그대로 돌려주므로 호출자가 <code>const id = expect(&quot;ident&quot;)</code> 처럼 값을 바로 받아 쓸 수 있다 — 검사와 소비를 한 함수에 묶은 이 형태는 재귀 하강 파서에서 널리 쓰이는 관용구다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>메시지에 정보가 없다</b> — 위치와 기대·실제가 없으면 진단이 아니라 통보다. (2) <b>회복이 없다</b> — 첫 오류에서 멈추면 사용자는 고치고 다시 돌리기를 반복해야 한다. (3) <b>입력 끝</b>을 흐름 안에서 다루지 않아 파서가 터질 수 있다.\n디스트랙터: 전용 오류 타입을 만들면 잡는 쪽에서 구분하기 좋지만 메시지에 정보가 없다는 문제에 견주면 부차적이고, 검사와 소비를 한 함수에 묶은 <code>expect</code> 형태는 재귀 하강 파서의 널리 쓰이는 관용구다.",
},
{
  t: "review", k: "너무 일찍 접는 상수 폴딩", cat: "review", d: 4, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function fold(n) {\n  if (n.left.k === \"num\" && n.right.k === \"num\") {\n    const a = n.left.v, b = n.right.v;\n    if (n.op === \"+\") return { k: \"num\", v: a + b };\n    if (n.op === \"/\") return { k: \"num\", v: a / b };\n  }\n  return n;\n}",
  items: [
    { txt: "덧셈과 나눗셈만 접으므로 곱셈이나 뺄셈은 그대로 남는다 — 최적화를 덜 하는 것 자체는 정확성 문제가 아니지만, 접을 수 있는 연산자를 모두 다뤄야 한다.", bad: false },
    { txt: "<code>b</code> 가 0 인 나눗셈을 <b>컴파일 시점에 접는다</b>. 대상 언어가 실행 시점 오류로 정한 동작이라면 그 오류가 사라지고 엉뚱한 값이 박히므로, 접을 수 없는 경우는 그대로 두어야 한다.", bad: true },
    { txt: "호스트 언어의 산술로 계산하므로 <b>대상 언어의 수 체계와 어긋날 수 있다</b>. 정수 나눗셈·넘침·부동소수 반올림 규칙이 다르면 컴파일한 결과와 직접 실행한 결과가 갈린다.", bad: true },
    { txt: "<code>n.left</code> 나 <code>n.right</code> 가 없는 단항 노드에서 그대로 터진다 — 노드 종류를 먼저 확인해야 하며, 트리를 훑는 함수는 <b>모든 모양</b>을 다룰 수 있어야 한다.", bad: true },
    { txt: "접은 결과를 새 노드로 만들어 돌려주므로 원본 트리를 바꾸지 않는다 — 여러 번 훑는 최적화 단계에서 안전한 방식이며 결함이 아니다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>0 으로 나누기를 접는다</b> — 실행 시점 오류가 되어야 할 것이 컴파일 결과에 값으로 박힌다. (2) <b>호스트 산술</b>로 계산해 대상 언어의 규칙과 갈릴 수 있다. (3) <b>단항 노드</b>에서 터진다. 최적화는 '의미를 바꾸지 않는 변환' 이어야 하고, 확신이 없으면 접지 않는 편이 언제나 옳다.\n디스트랙터: 접는 연산자가 적은 것은 최적화의 범위 문제이고, 새 노드를 만드는 방식은 오히려 안전하다.",
},
{
  t: "review", k: "헤더에 정의를 담았다", cat: "review", d: 3, track: "compiler",
  q: "이 코드의 결함을 모두 고르세요",
  code: "// util.h\nint counter = 0;\n\nint bump(void) {\n    return ++counter;\n}",
  items: [
    { txt: "전역 카운터를 여러 스레드가 동시에 증가시키면 값이 어긋난다 — 이 코드가 단일 스레드용이라면 결함이라 하기 어렵지만, 원자적 연산으로 바꿔야 한다.", bad: false },
    { txt: "<code>bump</code> 가 인자를 받지 않음을 <code>void</code> 로 명시한 것은 C 에서 의미가 있다 — 비워 두면 '인자 정보 없음' 이 되어 검사를 받지 못하므로, 이 부분은 잘 쓴 편이다.", bad: false },
    { txt: "헤더에 <b>정의</b>를 두면 그것을 포함한 모든 번역 단위가 같은 심볼을 만든다. 링크 시점에 중복 정의로 실패하고, 컴파일러 확장으로 통과하더라도 <b>번역 단위마다 다른 변수</b>가 생겨 값이 갈린다.", bad: true },
    { txt: "함수 본문도 헤더에 있으므로 같은 이름의 함수가 여러 번 정의된다 — 헤더에는 선언만 두고 정의는 하나의 소스 파일에 두거나, 인라인으로 만들 것이라면 그 뜻을 명시해야 한다.", bad: true },
    { txt: "포함 가드나 <code>#pragma once</code> 가 없어 같은 헤더를 두 번 포함하면 한 번역 단위 안에서도 중복 정의가 난다 — 헤더에는 언제나 가드를 두어야 한다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>헤더의 변수 정의</b>는 포함한 곳마다 심볼을 만든다 — 선언(<code>extern</code>)과 정의를 나눠야 한다. (2) <b>함수 정의</b>도 마찬가지다. (3) <b>포함 가드가 없다</b>. 헤더는 '무엇이 있다고 약속하는 곳' 이고 정의는 한 곳에만 있어야 한다는 규칙이 이 셋을 함께 설명한다.\n디스트랙터: 스레드 안전성은 사용 맥락에 달렸고, <code>void</code> 매개변수 목록은 C 에서 오히려 올바른 표기다.",
},
];
