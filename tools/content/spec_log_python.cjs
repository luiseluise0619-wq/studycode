module.exports = {
  track: "python", guide: "📜", xp: 70,
  unit: "로그 분석 II — 조용히 틀리는 자리",
  source: "./log_python.cjs",
  lessons: [
    { t: "값이 조용히 틀릴 때", n: 5,
      th: { sum: "파이썬의 무서운 사고는 예외가 아니라 **예외 없이 틀린 값**이다.",
        body: [
          { h: "시간과 돈", t: "시간대 없는 <code>datetime</code> 에 <code>timedelta</code> 를 더하면 벽시계로만 계산되어 서머타임에 중복·누락이 생기고, 돈을 <code>float</code> 로 누적하면 순서에 따라 몇 원씩 달라집니다. 둘 다 <b>오류가 나지 않아</b> 발견이 늦습니다. 시간은 UTC 로, 돈은 <code>Decimal</code> 이나 정수로 다룹니다." },
          { h: "한 번만 흐르는 것들", t: "제너레이터는 두 번 순회하면 비어 있고, 클래스 본문에 만든 리스트는 모든 인스턴스가 공유합니다. 예외 대신 <b>0 이나 뒤섞인 값</b>이 나오므로 로그에 아무 흔적이 없습니다. 집계 결과가 0 이거나 건수가 갑자기 줄면 <b>계산 자체가 일어났는지</b>부터 확인합니다." }],
        code: { c: "self.items = []          # __init__ 안에서\nrows = list(read_csv())  # 두 번 볼 데이터\nDecimal('0.1') + Decimal('0.2')", cap: "예외가 없다고 맞은 것은 아니다" },
        key: ["시간은 UTC 로 계산", "돈은 Decimal·정수로", "제너레이터는 한 번만", "클래스 속성은 공유된다"] } },
    { t: "환경이 다르면 다르게 돈다", n: 5,
      th: { sum: "'내 장비에서는 되는데' 의 정체는 대개 **경로·인코딩·인터프리터**다.",
        body: [
          { h: "어디를 먼저 보는가", t: "파이썬은 스크립트가 있는 디렉터리를 먼저 뒤지므로 <code>queue.py</code> 같은 파일 하나가 표준 모듈을 가립니다. <code>pip</code> 와 실행 인터프리터가 다르면 설치는 성공했는데 모듈이 없습니다. 둘 다 <b>어느 것을 쓰고 있는지</b>를 로그에 찍으면 즉시 드러납니다." },
          { h: "기본값에 기대지 않기", t: "<code>open()</code> 은 인코딩을 안 주면 로케일을 따르고, 포크는 스레드를 복사하지 않아 잠금만 남긴 자식을 만듭니다. 워커 타임아웃은 유휴가 아니라 <b>요청 처리 시간</b>에 걸립니다. 전부 기본값이 환경마다 다르거나 직관과 다른 경우입니다." }],
        code: { c: "python -m pip install ...      # 실행할 인터프리터로\nopen(p, encoding=\"utf-8\")\nmultiprocessing.set_start_method(\"spawn\")", cap: "기본값을 명시로 바꾼다" },
        key: ["같은 이름 파일이 모듈을 가린다", "pip 와 실행 인터프리터를 맞춘다", "인코딩은 항상 명시", "fork 는 스레드를 안 옮긴다"] } },
  ],
};
