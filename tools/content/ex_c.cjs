/* c 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "c", fixes: [
  { k: "개념", ex0: "main() 함수에서 실행이 시작", ex: "C 프로그램은 운영체제가 main() 을 호출하면서 시작한다. 이름이 정해져 있어서 run(), start(), begin() 이라는 함수를 만들어도 시작점이 되지 않는다. main 이 없으면 링크 단계에서 시작점을 찾지 못해 오류가 난다." },
  { k: "빈칸", ex0: "stdio.h (표준 입출력) 가 필요", ex: "printf 와 scanf 는 표준 입출력 헤더 stdio.h 에 선언되어 있다. #include &lt;stdio.h&gt; 를 빠뜨리면 컴파일러가 printf 의 원형을 몰라 경고나 오류를 낸다. stdlib.h 는 malloc 과 exit, string.h 는 strlen 같은 문자열 함수의 헤더라 용도가 다르다." },
  { k: "출력", ex0: "C 는 printf() 를 쓴다", ex: "C 의 표준 출력 함수는 printf() 다. cout 은 C++ 의 스트림 객체이고, println() 은 Java 와 Kotlin, echo() 는 PHP 나 셸의 것이라 C 컴파일러는 알지 못한다. printf 는 형식 문자열에 %d 같은 지정자를 넣어 값을 끼워 찍는다." },
  { k: "줄바꿈", ex0: "\\n 은 줄바꿈(newline)", ex: "\\n 은 줄바꿈(newline) 이스케이프 문자다. \\r 은 커서를 줄 맨 앞으로 되돌리는 캐리지 리턴이고, \\t 는 탭이다. /n 은 슬래시 방향이 반대라 이스케이프가 아니어서 글자 그대로 /n 이 찍힌다. printf(\"hi\\n\") 처럼 문자열 안에 넣는다." },
  { k: "문장 끝", ex0: "세미콜론(;) 으로 문장을 끝낸다", ex: "C 는 문장 끝에 세미콜론(;)을 반드시 붙인다. 빠뜨리면 다음 줄까지 한 문장으로 읽혀 엉뚱한 곳에서 컴파일 오류가 난다. 콜론은 case 와 레이블 뒤에, 마침표는 구조체 멤버 접근에 쓰이고, 닫는 중괄호는 블록의 끝이지 문장의 끝이 아니다." },
  { k: "빈칸", ex0: "int x = 10; 형태로 선언", ex: "정수 변수는 int x = 10; 처럼 타입, 이름, 초기값 순서로 선언한다. C 는 타입을 반드시 적어야 하므로 x = 10; 만 쓰면 선언되지 않은 변수라는 오류가 난다. 실수라면 double, 문자 하나라면 char 를 쓴다." },
  { k: "형식 지정자", ex0: "%d 는 정수, %f 는 실수, %s 는 문자열", ex: "printf 에서 int 는 %d 로 찍는다. %f 는 double 같은 실수, %c 는 문자 하나, %s 는 문자열용이다. 타입과 지정자가 어긋나면 컴파일은 되어도 쓰레기 값이 찍히므로(예: int 를 %f 로), 값의 타입에 맞는 지정자를 골라야 한다." },
  { k: "개념", ex0: "int 는 정수형", ex: "정수는 int 에 담는다. float 은 소수점이 있는 실수형, char 는 문자 하나(실제로는 1 바이트 정수)를 담는 타입이다. str 은 C 에 없는 이름이며 문자열은 char 배열로 다룬다. 더 큰 정수는 long 이나 long long 을 쓴다." },
  { k: "실수", ex0: "double(또는 float)은 실수형", ex: "소수를 담는 대표 자료형은 double 이다. float 도 실수형이지만 정밀도가 낮아 기본 실수 리터럴(3.14)은 double 로 취급된다. int 와 long 은 정수만 담아 소수점이 잘리고, boolean 은 C 키워드가 아니다(C99 부터 _Bool 과 stdbool.h 의 bool 이 있다)." },
  { k: "문자", ex0: "char 는 문자 1개를 저장", ex: "문자 하나는 char 에 담고 'A' 처럼 작은따옴표로 쓴다. string 과 text 는 C 에 없는 타입이며 문자열은 char 배열로 만든다. int 에 'A' 를 넣으면 컴파일은 되지만 문자 코드 65 를 저장하는 것이라 의도와 다르다." },
  { k: "출력 결과", ex0: "7을 2로 나눈 나머지 1", ex: "% 는 나머지 연산자라 7 % 2 는 7 을 2 로 나눈 나머지 1 이다. 몫 3 은 7 / 2 의 결과이고, 3.5 는 실수 나눗셈의 값이다. 0 은 짝수를 2 로 나눴을 때 나오는 나머지다. 홀짝 판정(x % 2) 에 자주 쓴다." },
  { k: "증가", ex0: "++ 는 1 증가, -- 는 1 감소", ex: "x++ 는 x 를 1 늘린다. x-- 는 반대로 1 줄이고, x// 와 x** 는 C 연산자가 아니다(// 는 주석 시작, ** 는 포인터의 포인터 선언에서나 보인다). ++x 는 먼저 증가시킨 값을, x++ 는 증가 전 값을 식의 값으로 쓴다는 차이가 있다." },
  { k: "나눗셈", ex0: "정수끼리 나누면 소수점은 버려져 3", ex: "int 끼리 나누면 정수 나눗셈이라 소수점 아래를 버려 7 / 2 는 3 이다. 3.5 를 얻으려면 7.0 / 2 처럼 한쪽을 실수로 만들어야 한다. 반올림해서 4 가 되지 않고, 2 는 몫이 아니라 나누는 수다." },
  { k: "비교", ex0: "== 는 비교, = 는 대입", ex: "'같다' 는 == 로 비교한다. = 는 대입이라 if (x = 5) 라고 쓰면 비교가 아니라 5 를 넣고 항상 참이 되는 유명한 실수다. != 는 '다르다' 이고, => 는 C 에 없는 기호다(다른 언어의 화살표 함수나 큰따옴표 비교로 착각하기 쉽다)." },
  { k: "여러 갈래", ex0: "switch-case 로 여러 경우를 처리", ex: "값에 따라 여러 갈래로 나누는 문장은 switch 다. switch (n) { case 1: ... break; } 처럼 case 로 각 값을 받는다. fork 는 프로세스를 복제하는 시스템 호출, branch 와 split 은 C 키워드가 아니다. case 끝에 break 를 빠뜨리면 다음 case 로 흘러내린다." },
  { k: "빈칸", ex0: "else 는 그 외의 경우", ex: "if 의 조건이 거짓일 때 실행할 코드는 else 뒤에 둔다. 또 다른 조건을 붙이려면 else if 를 쓴다. otherwise 나 elif 는 C 에 없다(elif 는 Python 의 것이다). else 는 바로 앞의 if 와 짝을 이루므로 중첩 if 에서는 중괄호로 범위를 분명히 해야 한다." },
  { k: "개념", ex0: "if (조건) { ... } 형태", ex: "조건 분기는 if (조건) { ... } 이다. case 는 switch 안에서만 쓰는 가지 표시이고, when 과 check 는 C 키워드가 아니다(when 은 Kotlin 의 것이다). 조건은 0 이면 거짓, 0 이 아니면 참으로 판정하므로 if (x) 처럼 정수를 바로 넣어도 된다." },
  { k: "블록", ex0: "중괄호 { } 로 블록을 감싸다", ex: "C 의 코드 블록은 중괄호 { } 로 감싼다. 함수 본문, if 와 for 의 몸통이 모두 이것이다. ( ) 는 함수 호출과 조건식, [ ] 는 배열 인덱스, < > 는 비교 연산자나 #include &lt;stdio.h&gt; 의 헤더 표시에 쓰여 블록과 무관하다." },
  { k: "배열", ex0: "배열 int a[5]; 는 정수 5칸", ex: "같은 자료형을 여러 개 이어 담는 것은 배열이다. int a[5]; 는 정수 5 칸을 연속으로 잡는다. 구조체는 서로 다른 타입을 한 묶음으로 만드는 것이고, 포인터는 주소 하나를 담으며, 함수는 코드를 묶는 단위라 데이터 저장소가 아니다." },
  { k: "포인터", ex0: "& 는 주소, * 는 그 주소의 값", ex: "변수의 주소는 & 연산자로 얻는다. &x 는 x 가 놓인 메모리 주소다. * 는 반대로 주소에 가서 값을 읽는 역참조이고, # 은 전처리기 지시문 표시, @ 는 C 에서 쓰이지 않는 기호다. scanf(\"%d\", &x) 에 & 가 붙는 이유가 이것이다." },
  { k: "개념", ex0: "for(초기;조건;증감) 형태로 반복", ex: "정해진 횟수만큼 도는 반복문은 for 다. for (int i = 0; i < n; i++) 처럼 시작, 조건, 증감을 한 줄에 둔다. if 는 분기, switch 는 값에 따른 갈래 나누기이고, goto 는 무조건 점프라 반복에 쓸 수는 있어도 구조가 엉켜 권장하지 않는다." },
  { k: "인덱스", ex0: "배열 인덱스는 0부터 시작", ex: "C 배열은 인덱스가 0 부터 시작해 첫 원소는 a[0] 이다. a[1] 은 두 번째이고, a(0) 은 함수 호출 문법이라 배열에 쓸 수 없으며, a.first 같은 멤버는 배열에 없다. 마지막 원소는 a[크기 - 1] 이고 그 너머를 읽으면 정의되지 않은 동작이다." },
  { k: "빈칸", ex0: "return 문은 값을 돌려주고 함수를 즉시", ex: "함수가 값을 돌려줄 때는 return 값; 을 쓴다. return 을 만나면 그 자리에서 함수가 끝나므로 뒤의 문장은 실행되지 않는다. 반환형이 void 가 아닌데 return 없이 끝나면 쓰레기 값이 돌아갈 수 있어 경고가 난다." },
  { k: "void", ex0: "void 반환형은 함수가 어떤 값도 돌려주지 않음", ex: "반환형 void 는 돌려줄 값이 없다는 뜻이다. 화면 출력만 하는 함수처럼 결과를 넘길 필요가 없을 때 쓴다. 매개변수는 자유롭게 가질 수 있고 이름도 당연히 필요하다. 항상 0 을 돌려주는 것은 int 반환형 함수의 return 0; 이지 void 가 아니다." },
  { k: "빈칸", ex0: "값을 돌려주지 않는 함수는 반환형을 void", ex: "돌려주는 값이 없는 함수는 반환형을 void 로 선언한다. void print(int x) { ... } 처럼 쓰고 본문에서 return; 을 값 없이 쓰거나 생략한다. int 로 두면 return 이 없을 때 경고가 나고, none 이나 null 은 C 의 반환형 키워드가 아니다." },
  { k: "매개변수", ex0: "함수가 받는 입력값을 담는 변수를 매개변수", ex: "int add(int a, int b) 에서 괄호 안의 a 와 b 는 함수가 입력을 받는 변수인 매개변수(파라미터)다. 반환값은 return 으로 돌려주는 결과이고, 전역 상수는 함수 밖에서 const 로 정한 값이다. 포인터는 주소를 담는 변수라 여기서는 타입이 int 이므로 해당하지 않는다." },
  { k: "선언", ex0: "char s[5]; 는 char 값 5개를 담는 배열", ex: "문자 5 개를 담는 배열은 char s[5]; 다. 타입, 이름, 대괄호 안 크기 순서다. char 5[s]; 는 이름과 크기가 뒤바뀌었고, char s; 는 문자 하나만 담는 변수이며, int s(5); 는 함수 선언처럼 읽히는 데다 타입도 정수라 문자 배열이 아니다." },
  { k: "빈칸", ex0: "char 는 하나의 문자를 저장하는 자료형", ex: "문자 하나를 담는 C 타입은 char 다. 'A' 처럼 작은따옴표 리터럴을 넣고, 내부적으로는 1 바이트 정수라 'A' + 1 이 'B' 가 된다. 문자열은 char 배열이며, string 이라는 타입은 C 에 없다(C++ 의 std::string 과 다르다)." },
  { k: "접근", ex0: "s[0] 은 첫 글자 H", ex: "\"Hi\" 를 담은 char s[3] 은 s[0] 이 'H', s[1] 이 'i', s[2] 가 문자열 끝을 뜻하는 널 문자 '\\0' 이다. 인덱스는 0 부터 시작하므로 s[0] 은 첫 글자 H 다. 3 은 배열 크기일 뿐 값이 아니고, 공백은 문자열 안에 없다." },
  { k: "함수", ex0: "strlen 은 널 문자 앞까지의 글자 수", ex: "문자열 길이는 string.h 의 strlen 으로 구한다. 널 문자 '\\0' 직전까지의 글자 수를 돌려주므로 \"Hi\" 는 2 다. sizeof 는 배열 전체의 바이트 크기(널 포함)를 주기 때문에 다르고, length 나 len 이라는 함수는 C 표준에 없다." },
  { k: "접근", ex0: "첫 행 두 번째 열은 m[0][1]", ex: "2 차원 배열은 m[행][열] 로 접근하고 두 인덱스 모두 0 부터 시작한다. 첫 번째 행 두 번째 열은 m[0][1] 이다. m[1][2] 는 두 번째 행 세 번째 열이고, m(0,1) 은 함수 호출 문법이며, m.0.1 은 C 에 없는 표기다." },
  { k: "선언", ex0: "int *p; 는 int 값을 가리키는 포인터", ex: "정수를 가리키는 포인터는 int *p; 로 선언한다. * 가 이름 앞에 붙어 '가리키는 것' 임을 나타낸다. int p*; 는 * 위치가 틀려 문법 오류이고, int p(); 는 함수 선언이며, pointer 라는 키워드는 C 에 없다." },
  { k: "개념", ex0: "포인터는 값 자체가 아니라 다른 변수가 저장된 메모리 주소", ex: "포인터는 다른 변수가 놓인 메모리 주소를 담는 변수다. 값 자체를 복사하지 않기 때문에 그 주소로 가서 원본을 읽거나 바꿀 수 있다. 정수만 담거나 문자열 전체를 담는 것이 아니고, 함수 이름을 담는 것은 함수 포인터라는 별도 개념이다." },
  { k: "역참조", ex0: "p 는 a 를 가리키므로 *p 는 a 의 값인 10", ex: "p 에 &a 를 넣었으니 p 는 a 의 주소를 담고, *p 는 그 주소에 가서 값을 읽으므로 a 의 값 10 이다. a 의 주소가 나오는 것은 * 없이 p 를 찍었을 때다. 0 이나 '알 수 없음' 은 초기화하지 않은 포인터를 역참조할 때의 이야기다." },
  { k: "주소", ex0: "연산자는 변수 앞에 붙여 그 변수의 메모리 주소", ex: "변수 앞에 & 를 붙이면 그 변수의 메모리 주소를 얻는다. * 는 반대 방향으로, 주소에서 값을 꺼내는 역참조 연산자다. = 는 대입, + 는 덧셈이라 주소와 관계없다. scanf 에 &x 를 넘기는 것도 값을 써 넣을 주소를 알려 주기 위해서다." },
  { k: "형", ex0: "가리키는 대상의 자료형에 맞춰 double *p;", ex: "포인터는 가리키는 대상의 타입에 맞춰 선언하므로 double 을 가리키려면 double *p; 다. int *p; 로 double 을 가리키면 타입이 어긋나 경고가 나고 값도 잘못 읽힌다. *double p; 는 * 위치가 틀렸고, double p&; 는 문법에 없는 형태다." },
  { k: "빈칸", ex0: "NULL 은 포인터가 아무 대상도 가리키지 않음", ex: "아무것도 가리키지 않는 포인터에는 NULL 을 넣는다. 보통 ((void*)0) 로 정의된 매크로이며 stdio.h 나 stddef.h 에 있다. NULL 포인터를 역참조하면 프로그램이 죽으므로 if (p != NULL) 로 먼저 확인한다. nil 은 Go 나 Lua, None 은 Python 의 이름이다." }
] };
