/* algo 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "algo", fixes: [
  { k: "개념", ex0: "큐는 줄서기처럼 먼저 온 것이 먼저 처리", ex: "먼저 넣은 것이 먼저 나오는(FIFO) 구조는 큐다. 줄서기처럼 앞에서 빼고 뒤에 넣는다. 스택은 반대로 마지막에 넣은 것이 먼저 나오고(LIFO), 힙은 가장 크거나 작은 값이 먼저 나오는 우선순위 구조이며, 해시는 키로 값을 찾는 구조라 넣은 순서와 관계없다." },
  { k: "개념", ex0: "배열은 인덱스로 O(1) 접근이 가능", ex: "값을 순서대로 이어 담고 인덱스로 바로 접근하는 구조는 배열이다. 메모리가 연속이라 a[i] 의 위치를 곧바로 계산해 O(1) 에 읽는다. 링크드리스트는 노드를 따라가야 하므로 i 번째 접근이 O(n) 이고, 스택과 큐는 한쪽 끝에서만 넣고 빼도록 제한한 구조다." },
  { k: "개념", ex0: "스택은 쌓기(접시)처럼 마지막 것이 먼저", ex: "먼저 넣은 것이 나중에 나오는(LIFO) 구조는 스택이다. 접시를 쌓듯 위에 올리고 위에서 꺼낸다. 큐는 먼저 넣은 것이 먼저 나오고, 배열은 어느 위치든 인덱스로 접근하며, 트리는 계층 구조라 넣고 빼는 순서를 정하는 구조가 아니다. 함수 호출 기록과 되돌리기 기능이 스택으로 구현된다." },
  { k: "개념", ex0: "해시맵은 키로 값을 매우 빠르게 찾는다", ex: "키로 값을 찾는 구조는 해시(맵, 딕셔너리)다. 키를 해시 함수로 바꿔 저장 위치를 바로 계산하므로 평균 O(1) 에 찾는다. 배열은 정수 인덱스로만 접근하고 문자열 키를 쓰려면 처음부터 훑어야 하며, 스택과 큐는 끝에서만 넣고 빼는 구조라 임의의 키로 찾을 수 없다." },
  { k: "개념", ex0: "정렬(sort)은 값을 순서대로 배열", ex: "값들을 크기 순서로 다시 늘어놓는 것이 정렬이다. 탐색은 원하는 값을 찾는 것이고, 해싱은 키를 저장 위치로 바꾸는 것이며, 재귀는 함수가 자신을 부르는 기법이라 정렬을 구현하는 데 쓰일 수는 있어도 정렬 그 자체는 아니다. 정렬해 두면 이진 탐색이 가능해진다." },
  { k: "개념", ex0: "이진 탐색은 O(log n) 으로 매우 빠르다", ex: "정렬된 배열에서 가운데와 비교해 절반을 버리며 찾는 것이 이진 탐색이다. 매번 후보가 반으로 줄어 O(log n) 이다. 순차 탐색은 처음부터 하나씩 보는 O(n), 완전 탐색은 모든 경우를 다 시도하는 것이고, DFS 는 그래프를 깊이 우선으로 도는 방법이라 정렬된 배열 탐색과 다르다." },
  { k: "개념", ex0: "O(n), O(1) 처럼 빅오 표기를 쓴다", ex: "알고리즘이 입력 크기에 따라 얼마나 오래 걸리는지를 O(n), O(log n) 처럼 적는 것이 빅오 표기법이다. 정규 표현식은 문자열 패턴 표기, 아스키 코드는 문자 인코딩, API 는 프로그램끼리의 접점이라 효율과 무관하다. 빅오는 상수를 무시하고 가장 빠르게 커지는 항만 남긴다." },
  { k: "복잡도", ex0: "전체를 한 번 보면 O(n)", ex: "n 개를 처음부터 끝까지 한 번씩 보면 작업량이 n 에 비례하므로 O(n) 이다. O(1) 은 크기와 무관하게 한 번에 끝나는 것(배열 인덱스 접근), O(log n) 은 절반씩 줄이는 것(이진 탐색), O(n²) 은 각 원소마다 전체를 다시 훑는 것(이중 반복문)이다." },
  { k: "주의", ex0: "호출 스택이 쌓여 넘치면 stack overflow", ex: "함수를 부를 때마다 호출 정보가 스택에 쌓이는데 재귀가 너무 깊으면 이 공간이 넘쳐 스택 오버플로가 난다. 메모리 누수는 할당한 메모리를 해제하지 않아 새는 것, 교착 상태는 여러 스레드가 서로를 기다리는 것이라 다르다. 기저 조건을 빠뜨린 재귀가 흔한 원인이다." },
  { k: "개념", ex0: "재귀는 문제를 같은 형태의 더 작은 문제로 나눈다", ex: "함수가 자기 자신을 다시 부르는 기법이 재귀다. 문제를 같은 모양의 더 작은 문제로 줄여 가다가 기저 조건에서 멈춘다. 반복문은 같은 일을 되풀이하지만 자신을 부르지 않고, 분기는 조건에 따라 갈라지는 것이며, 정렬은 재귀로 구현할 수도 있는 하나의 문제일 뿐이다." },
  { k: "핵심", ex0: "기저 조건에서 재귀가 멈춘다", ex: "재귀에는 더 이상 자신을 부르지 않고 답을 바로 돌려주는 기저 조건(base case)이 있어야 멈춘다. 없으면 무한히 호출되어 스택 오버플로가 난다. 반복문은 재귀와 대체 관계이고, 전역 변수와 누적 배열은 값을 모으는 수단일 뿐 종료를 보장하지 않는다." },
  { k: "증명", ex0: "비교 정렬의 하한은 O(n log n)", ex: "원소끼리 비교만으로 정렬하려면 n! 가지 순서를 구별해야 하고 비교 한 번에 경우가 절반으로 줄어드니 최소 log(n!) ≈ n log n 번 비교가 필요하다. 그래서 하한이 O(n log n) 이고 병합 정렬과 힙 정렬이 이를 달성한다. O(n) 과 O(1) 은 비교 정렬로는 불가능하고, O(n²) 은 버블 정렬처럼 느린 쪽이다. 계수 정렬은 비교를 안 하므로 예외다." },
  { k: "이유", ex0: "n → n/2 → n/4 … 절반씩 줄어 log n 번", ex: "이진 탐색은 가운데와 비교할 때마다 후보가 n, n/2, n/4 로 절반씩 줄어 log₂n 번이면 하나만 남는다. 그래서 O(log n) 이다. 한 번에 전부 훑으면 O(n) 이고, 순서가 무작위이면 절반을 버릴 수 없으며, 정렬되어 있어야만 '왼쪽은 더 작다' 는 판단이 가능하다." },
  { k: "기법", ex0: "DP 는 memoization 으로 같은 계산을 반복하지 않는다", ex: "작은 문제의 답을 표나 캐시에 저장해 두고 다시 쓰는 것이 동적 계획법(DP)이다. 피보나치처럼 같은 부분 문제가 반복될 때 지수 시간을 다항 시간으로 줄인다. 완전 탐색은 모든 경우를 다 해 보고, 백트래킹은 안 되는 가지를 되돌아가며 쳐내며, 분할 정복은 나눈 문제가 서로 겹치지 않을 때 쓴다." },
  { k: "복잡도", ex0: "뒤 원소를 모두 밀어야 해서 O(n)", ex: "배열 중간에 끼워 넣으려면 그 뒤의 원소를 모두 한 칸씩 밀어야 하므로 평균 n/2 번 이동, 즉 O(n) 이다. O(1) 은 맨 끝에 붙일 때나 링크드리스트에서 위치를 이미 알 때의 비용이고, O(log n) 은 절반씩 줄이는 탐색, O(n²) 은 이중 반복이라 해당하지 않는다." },
  { k: "탐색", ex0: "BFS 는 큐를 사용해 가까운 노드부터 방문", ex: "시작점에서 가까운 노드부터 층층이 넓게 퍼져 가는 탐색이 BFS(너비 우선)다. 큐에 이웃을 넣고 순서대로 꺼내므로 가중치 없는 그래프에서 최단 경로를 준다. DFS 는 한 방향으로 끝까지 파고들고, 이진 탐색은 정렬된 배열용이며, 정렬은 탐색이 아니다." },
  { k: "탐색", ex0: "DFS 는 스택(또는 재귀)으로 깊이 먼저 탐색", ex: "한 방향으로 끝까지 파고든 뒤 막히면 되돌아와 다른 길을 가는 탐색이 DFS(깊이 우선)다. 스택이나 재귀로 구현한다. BFS 는 가까운 곳부터 넓게 퍼지고, 순차 탐색은 배열을 앞에서부터 보는 것이며, 해싱은 키로 위치를 계산하는 것이라 그래프 탐색과 다르다." },
  { k: "기법", ex0: "그리디는 항상 최적은 아니지만 특정 문제에서", ex: "매 순간 가장 좋아 보이는 선택을 하고 되돌리지 않는 방법이 그리디(탐욕법)다. 거스름돈이나 활동 선택처럼 지역 최선이 전체 최선이 되는 문제에서만 최적을 보장한다. 동적 계획법은 부분 문제의 답을 저장해 전체를 맞추고, 분할 정복은 문제를 쪼개 합치며, 이분 탐색은 정렬된 범위를 반씩 줄이는 탐색이다." }
] };
