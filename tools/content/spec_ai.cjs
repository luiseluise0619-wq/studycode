module.exports={
  track:"ai", guide:"🤖", xp:80,
  unit:"실행형 실전 — 추론 파이프라인을 직접 만든다",
  source:"./exec_aidl.cjs", slice:[0,6],
  lessons:[
    { t:"확률로 바꾸고 손실을 재는 곳", n:3,
      th:{ sum:"로짓을 확률로 바꾸고 손실을 재는 두 단계는 **수식은 짧고 수치 함정은 많다**.",
        body:[
          {h:"넘치는 쪽과 사라지는 쪽", t:"softmax 는 입력 전체에 같은 수를 더하거나 빼도 결과가 같으므로(shift invariance), **최댓값을 빼면** 지수의 인자가 0 이하가 되어 `exp` 가 넘칠 수 없다. 이 처리가 없으면 작은 예제에서는 완벽히 돌고 실제 로짓(수백~수천)에서 `OverflowError` 로 죽는다. 반대로 값이 아주 작으면 `exp` 가 전부 0 이 되어 0으로 나누기가 되는데, 오버플로는 예외로 드러나지만 **언더플로는 조용히 nan 을 만든다**."},
          {h:"자르고 나면 다시 정규화한다", t:"교차 엔트로피는 정답 확률이 0 이면 발산하므로 클리핑이 필요하다 — float16 언더플로·반올림·마스킹 실수로 실제로 일어난다. 그리고 top-k 로 꼬리를 자른 뒤 **재정규화를 빼먹으면 확률이 아닌 것**을 샘플링하게 된다. 두 경우 모두 결과가 '정상적인 리스트' 라 형태만 봐서는 통과한다."}],
        code:{ c:"m = max(xs)\nexps = [math.exp(x - m) for x in xs]   # 넘치지 않는다", cap:"[1,2,3] 으로 테스트하면 버그가 숨는다" },
        key:["softmax 는 최댓값을 빼고 계산","log(0) 은 클리핑으로 막는다","자른 뒤에는 반드시 재정규화","실전 크기의 값을 테스트에 넣는다"] } },
    { t:"샘플링·토크나이저·청킹", n:3,
      th:{ sum:"LLM 을 실제로 쓸 때 만지는 셋 — **무엇을 뽑고, 어떻게 쪼개고, 어디서 겹치게 할 것인가**.",
        body:[
          {h:"경계에서 한 개가 달라진다", t:"top-p 는 top-k 와 달리 **분포의 모양에 적응**한다 — 확신하는 지점에서는 후보가 1개, 애매한 지점에서는 수십 개가 된다. 그런데 `>=` 와 `>` 한 글자 차이로 후보 개수가 달라지고, 결과가 여전히 정상적인 확률 분포라 **눈으로는 알 수 없다**. 문턱을 하나씩 넘겨 보는 테스트만이 이 버그를 잡는다."},
          {h:"겹치지 않게, 그리고 겹치게", t:"BPE 병합은 소비한 만큼 전진해야 한다 — 한 칸만 움직이면 방금 합친 토큰의 뒷조각을 다시 병합해 **원본보다 긴 결과**가 나온다. 반대로 검색용 청킹에서는 의미가 경계에서 잘리지 않게 **일부러 겹치게** 만들며, 전진 폭이 `size − overlap` 이다. `overlap >= size` 는 전진하지 못해 무한 루프가 되므로 가드가 필요하다."}],
        code:{ c:"i += size - overlap   # 겹치는 만큼 덜 전진한다\n# overlap >= size 면 전진이 0 — 메모리를 다 쓴다", cap:"설정 실수가 무한 루프가 되는 지점" },
        key:["top-p 는 '이상' 에서 멈춘다","BPE 는 소비한 만큼 전진","청킹 전진 폭은 size − overlap","무한 루프가 될 설정에는 가드"] } },
  ]
};
