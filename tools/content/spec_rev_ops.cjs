const UNIT = "코드 리뷰 — 무엇을 결함이라 부를 것인가";
module.exports = {
  source: "./rev_ops.cjs", xp: 70,
  entries: [

    { track: "git", guide: "", unit: UNIT, lesson: "설정과 절차를 읽는다", slice: [0, 4],
      th: { sum: "Git 에서 리뷰할 것은 **코드만이 아니다** — 워크플로 파일, 되돌리는 절차, PR 을 묶은 방식까지가 리뷰 대상이고 사고는 대개 거기서 난다.",
        body: [
          { h: "CI 설정은 가장 권한이 센 코드다", t: "워크플로 파일은 **시크릿을 쥐고 도는 코드**인데, 애플리케이션 코드만큼 읽히지 않는다. 두 가지만 봐도 절반은 걸러진다. 첫째, **누구의 코드가 어떤 권한으로 도는가** — 포크의 코드를 기본 브랜치의 권한으로 실행하면 외부인이 시크릿을 읽는다. 둘째, **무엇에 의존하는가** — 남의 액션을 움직이는 브랜치로 참조하면 그 저장소가 바뀌는 순간 우리 CI 에서 남의 새 코드가 돈다. 의존은 언제나 고정하고, 고정할 수 없으면 권한을 줄인다." },
          { h: "되돌리는 방법이 사람 수를 탄다", t: "혼자 쓰는 브랜치에서는 이력을 다시 써도 아무 일이 없지만, **남이 딛고 선 이력**을 다시 쓰면 그 사람들의 작업이 갈라진다. 그래서 공유 브랜치의 기본은 `revert` 다 — 이력을 그대로 두고 되돌리는 커밋을 얹는다. 새어 나간 비밀도 같은 모양이다: 이력에서 지우는 것은 **복제된 사본까지 지우지 못하므로**, 첫 조치는 언제나 폐기와 재발급이고 지우는 것은 그 다음이다." }],
        code: { c: "git revert abc123          # 공유 브랜치의 기본\ngit push --force origin main   # 남이 딛고 선 바닥을 바꾼다", cap: "혼자의 이력인지 남들의 이력인지가 갈림길" },
        key: ["워크플로는 시크릿을 쥔 코드다", "의존은 SHA 로 고정한다", "공유 브랜치에서는 revert", "새어 나간 비밀은 먼저 폐기한다"] } },

    { track: "linux", guide: "", unit: UNIT, lesson: "스크립트와 설정을 읽는다", slice: [4, 8],
      th: { sum: "셸과 시스템 설정의 리뷰는 **'지금 틀린 것' 과 '더 좋아질 것' 을 나누는 일**이다. 둘을 섞어 적으면 정작 위험한 것이 묻힌다.",
        body: [
          { h: "빈 변수와 넓은 권한", t: "셸 사고의 전형은 두 가지 모양이다. 하나는 **비어 있는 변수** — `rm -rf $BASE/tmp` 에서 `$BASE` 가 비면 `/tmp` 를 지운다. 따옴표로 감싸고 `\"${1:?경로를 주세요}\"` 처럼 비면 멈추게 하면 둘 다 막힌다. 다른 하나는 **넓게 여는 것** — `chmod -R 777` 은 진단이 아니라 포기다. 무엇이 왜 막혔는지 알아내지 않고 전부 연 것이라 원인은 남고 공격면만 넓어진다. 그리고 `set -euo pipefail` 이 없으면 중간이 깨져도 끝까지 달려 성공을 기록한다." },
          { h: "환경이 다르면 결과가 다르다", t: "손으로는 되는데 크론이나 서비스에서만 안 되는 일의 정체는 대부분 **환경 차이**다. 크론의 `PATH` 는 로그인 셸보다 훨씬 짧고 `HOME`·로케일도 다르며, systemd 도 로그인 셸의 환경을 물려받지 않는다. 그래서 절대 경로를 쓰거나 환경을 명시한다. 같은 자리에서 볼 것이 둘 더 있다: 겹쳐 도는 것을 막았는가(`flock`), 그리고 **실패가 보이는가** — 출력을 `/dev/null` 로 버리면 크론이 알려 주던 실패까지 함께 사라진다." }],
          code: { c: "set -euo pipefail\n: \"${1:?경로를 주세요}\"\nflock -n /tmp/etl.lock ./run.sh   # 겹치면 그냥 넘어간다", cap: "세 줄이 셸 사고의 대부분을 막는다" },
        key: ["빈 변수가 엉뚱한 곳을 지운다", "777 은 진단이 아니라 포기", "크론과 systemd 의 환경은 좁다", "버린 출력은 사라진 실패다"] } },

    { track: "test", guide: "", unit: UNIT, lesson: "테스트 코드를 읽는다", slice: [8, 12],
      th: { sum: "테스트도 코드이고, **잘못된 테스트는 없는 것보다 나쁘다** — 초록불로 안심시키면서 아무것도 지키지 않기 때문이다.",
        body: [
          { h: "무엇을 지키는지 물어라", t: "테스트를 읽을 때 던질 질문은 하나다: **이 코드가 틀리면 이 테스트가 빨개지는가.** `toBeTruthy()` 는 빈 객체도 `\"error\"` 문자열도 통과시키므로 답이 '아니오' 다. 호출 여부만 보는 단언(`toHaveBeenCalled`)도 마찬가지라, 계산이 틀려도 부르기만 했으면 통과한다. 더 나쁜 것은 **시험 대상 자체를 가짜로 만든 것**이다 — 협력자를 가짜로 두는 것이 모킹이지 대상을 바꾸면 실제 코드가 한 줄도 돌지 않는다." },
          { h: "흔들림을 덮지 마라", t: "가끔 실패하는 테스트에 재시도를 붙이면 초록불은 돌아오지만 **원인을 조사할 계기까지 사라진다.** 그 불안정은 사라진 것이 아니라 운영에서 드러난다. 흔들림의 흔한 뿌리는 두 가지다 — **고정 시간 대기**(`sleep`)와 **테스트 사이의 의존**(앞 테스트가 만든 데이터와 연결에 기대는 것). 둘 다 고칠 수 있는 것이라 덮을 이유가 없다. 커버리지 숫자도 같은 함정이다: 줄 100% 는 모든 줄이 한 번 실행됐다는 뜻일 뿐, 어려운 파일을 제외 목록에 넣어 만든 100% 는 거짓말이 된다." }],
        code: { c: "expect(r).toBeTruthy();          // 거의 모든 값이 통과한다\nexpect(r.total).toBe(13500);     // 무엇을 기대하는지 적는다", cap: "코드가 틀리면 빨개지는가" },
        key: ["틀리면 빨개지는지 물어라", "대상을 가짜로 만들면 남는 게 없다", "재시도는 흔들림을 덮는다", "줄 커버리지는 안전이 아니다"] } },

    { track: "cloud", guide: "", unit: UNIT, lesson: "인프라 정의를 읽는다", slice: [12, 16],
      th: { sum: "클라우드 설정 리뷰는 **기본값이 무엇이고 우리가 무엇을 뒤집었는가**를 읽는 일이다. 대개 사고는 '열어 둔 것' 과 '남긴 기록' 에서 난다.",
        body: [
          { h: "누구에게 열려 있는가", t: "네트워크와 저장소 설정은 질문 하나로 압축된다: **이것은 누구에게 열려 있는가.** 관리 포트를 전 세계에 여는 것과 웹 포트를 여는 것은 완전히 다른 일이고, 데이터베이스가 인터넷에 직접 붙어 있는 것은 그 자체로 사고다. 보안 그룹은 대역이 아니라 **다른 보안 그룹**을 출발지로 지정할 수 있어 '앱 서버에서만' 을 정확히 표현한다. 저장소도 같다 — 사용자 업로드를 공개 읽기로 두는 대신 **서명된 URL** 로 시간을 정해 빌려주고, 클라이언트에 액세스 키를 심는 대신 서버가 업로드용 서명을 발급한다." },
          { h: "기록과 되돌릴 길", t: "인프라 코드에서 **상태는 '무엇이 실제로 있는가' 의 기록**이라, 사람마다 다르면 도구가 엉뚱한 판단을 한다. 원격 백엔드와 잠금을 두고, 적용은 각자 노트북이 아니라 CI 에서 계획을 리뷰한 뒤에 한다. 비밀을 코드에 적으면 **상태 파일에도 평문으로 남는다**는 점을 함께 봐야 하고, 최종 스냅샷을 생략하는 설정처럼 **되돌릴 길을 없애는 값**은 운영 자원에서 특히 눈에 띄어야 한다. 오토스케일에서는 반대로 시차를 본다 — 쿨다운 0 은 늘린 것이 지표에 반영되기 전에 또 판단한다." }],
        code: { c: "# 대역이 아니라 보안 그룹을 출발지로\ningress: { port: 5432, source_sg: app-sg }\nskip_final_snapshot = true   # 되돌릴 길을 없앤다", cap: "누구에게 열려 있고, 무엇을 되돌릴 수 있는가" },
        key: ["관리 포트와 DB 는 좁힌다", "업로드는 서명된 URL 로", "상태는 원격에 두고 잠근다", "쿨다운 0 은 시차를 무시한다"] } },
  ],
};
