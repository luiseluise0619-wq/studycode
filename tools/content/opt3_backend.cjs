/* backend — 정답률 15% 아래 끝맺음의 오답 19개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "backend", fixes: [
  { k: "be2-rest-idempotency-key", from: "규약을 지키면 중복은 생기지 않는다", to: "클라이언트가 재시도하지 않도록 API 문서에만 분명히 명시해 둔다. 규약을 지키는 한 중복은 막힌다" },
  { k: "be2-oauth-redirect-uri", from: "서버끼리 주고받으므로 문제가 없다", to: "리다이렉트 URI 를 검증하는 일은 보안과 무관하다. 어차피 토큰은 서버끼리 주고받으므로 URI 는 안내용일 뿐이다" },
  { k: "half-open", from: "빨리 알 수 있기 때문이다", to: "모든 요청을 한꺼번에 다시 통과시켜 본다. 회복 여부를 가장 빨리 알 수 있어서다" },
  { k: "PUT vs POST", from: "PUT은 구식이라 신규 API에서는 쓰지 않는다", to: "PUT은 구식이라 신규 API에서는 PATCH 로 대체한다" },
  { k: "be2-mq-at-least-once", from: "코드 수정이 필요 없다", to: "큐를 exactly-once로 바꾸면 소비자 코드는 그대로 두면 된다" },
  { k: "JWT 서명 의미", from: "내용을 확인할 수 있기 때문이다", to: "서명이 되어 있어 내용이 암호화되므로 민감 정보를 담아도 된다. 키가 없으면 페이로드를 열어 볼 수 없다. 서명 키를 가진 쪽만 내용을 확인할 수 있는 구조다" },
  { k: "토큰 저장 위치", from: "XSS는 어차피 막을 수 없기 때문이다", to: "localStorage에 저장한다. XSS는 어차피 막지 못하니 편의를 택한다" },
  { k: "계정 연결", from: "별도 연결 절차도 필요 없다", to: "문제 없다. 이메일은 전 세계적으로 유일하므로 안전한 식별자다. 같은 주소를 두 사람이 쓸 수 없기 때문이다 제공자가 발급한 계정은 이미 검증을 거친 것이므로 따로 확인할 필요도 없다. 그래서 별도 연결 절차는 군더더기다" },
  { k: "XSS 방어", from: "출력 맥락은 신경 쓰지 않아도 된다", to: "입력 시점에 모든 태그를 제거하면 완전히 해결된다. 저장된 값이 깨끗하면 어디에 출력해도 안전하다 저장 단계에서 한 번만 거르면 이후 출력 맥락은 어디든 같다" },
  { k: "캐시 스탬피드", from: "캐시 설정은 손대지 않아도 된다", to: "커넥션 누수이며, 커넥션 풀을 무제한으로 늘리면 해결된다. 대기가 사라지면 몰린 요청도 처리된다 대기 큐가 사라지면 DB 도 정상 속도로 돌아오므로 캐시 설정은 그대로 둔다" },
  { k: "Lost update", from: "따로 잠글 필요가 없다", to: "격리 수준과 무관하게 DB 가 자동으로 방지해 준다. 잠금은 DB 가 알아서 건다" },
  { k: "이중 검사 + 버전", from: "검사는 두 번 하되 락은 필요 없다", to: "검사를 두 번 하면 락 없이도 중복 실행이 막힌다" },
  { k: "JWT vs 세션", from: "위조가 불가능해 서명 검증이 필요 없다", to: "JWT는 위조가 불가능해 서명 검증을 생략해도 된다" },
  { k: "호환 변경 판단", from: "문서화도 필요 없다", to: "필드 추가는 어차피 아무도 안 보므로 문서화는 생략해도 된다" },
  { k: "커서 제약", from: "다음 항목을 볼 수 있기 때문이다", to: "전체 목록을 한 번에 조회할 수 없게 된다. 커서를 따라가야만 다음 항목이 보이는 구조다" },
  { k: "키 충돌 처리", from: "응답을 새로 만들 필요가 없다", to: "저장된 첫 번째 응답을 조용히 재생해 준다 — 어차피 같은 키로 온 요청이기 때문이다 응답은 첫 번째 것으로 굳어진다" },
  { k: "페이로드 설계", from: "어떤 크기든 문제가 없다", to: "메시지를 압축하면 어떤 크기든 큐가 받아 준다" },
  { k: "중복 스케줄 실행", from: "겹치지 않으면 중복도 생기지 않는다", to: "cron 시각을 서버마다 1분씩 다르게 설정해 둔다. 겹치지 않으면 중복도 사라진다" },
  { k: "무효화 시점", from: "성공하는 상태가 생기지 않는다", to: "캐시 삭제는 트랜잭션 안에서 해야 원자성이 보장된다 — 커밋과 삭제가 한 단위로 묶여야 둘 중 하나만 성공하는 상태가 막힌다" }
] };
