const UNIT="실행형 실전 — 인프라 알고리즘을 직접 만든다";
module.exports={
  track:"devops", guide:"🚀", xp:80,
  unit:UNIT,
  source:"./exec_devops.cjs",
  lessons:[
    { t:"분산 시스템의 방어 패턴", n:4,
      th:{ sum:"장애가 전체로 번지는 것을 막고 **시스템을 보호하는** 알고리즘들이다.",
        body:[
          {h:"백오프와 토큰 버킷", t:"API 호출 실패 시 단순히 기다리면 천둥 떼 현상이 발생하므로, 지연 시간에 지터(Jitter)를 더해 부하를 분산해야 한다. 토큰 버킷은 Leaky Bucket 과 달리 일시적인 폭주(Burst)를 허용하며, 백그라운드 스레드 없이 시간 차이만으로 계산할 수 있어 매우 가볍다."},
          {h:"서킷 브레이커와 라운드 로빈", t:"서킷 브레이커는 연속 실패 시 회로를 열어 즉시 실패하게 만들어 하위 서비스의 자원 고갈을 막고, 반-열림 상태로 회복을 감지한다. 가중치 라운드 로빈은 서버 스펙에 따라 트래픽을 고르게 분배하지만, 가중치가 모두 0이 될 때 무한 루프에 빠지는 것을 반드시 방어해야 한다."}],
        code:{ c:"jitter = random.uniform(0, delay)\ncurrent_delay = delay + jitter", cap:"지터(Jitter)는 천둥 떼 현상을 막는다" },
        key:["지터로 재시도 부하를 분산한다","토큰 버킷은 시간 차이로 계산한다","서킷 브레이커는 연쇄 장애를 막는다","가중치가 0일 때의 무한 루프를 막아라"] } },
    { t:"라우팅과 배포 설계", n:4,
      th:{ sum:"트래픽을 나누고 **무중단 배포를 안전하게** 수행하는 로직이다.",
        body:[
          {h:"일관된 해싱과 SemVer", t:"일관된 해싱(Consistent Hashing)은 노드가 추가/삭제될 때 전체 키가 아닌 인접한 키만 재배치되도록 해 대규모 캐시 미스를 막는다. 유의적 버전(SemVer)에서 하위 버전 초기화 규칙을 어기면 의존성 관리 도구가 빌드를 망가뜨린다."},
          {h:"비밀값 마스킹과 롤링 업데이트", t:"로그 마스킹 시 짧은 비밀을 먼저 치환하면 긴 비밀의 일부가 남게 되므로, 반드시 긴 문자열부터 치환해야 한다. 롤링 업데이트 시 maxSurge 와 maxUnavailable 의 조합이 전체 가용성을 결정하며, 이 설정이 틀리면 배포 도중 502 에러가 폭증한다."}],
        code:{ c:"take_down = min(rem, max_unav)\ncurrent = total - take_down + max_surge", cap:"롤링 업데이트 중의 가용 노드 계산" },
        key:["일관된 해싱은 캐시 미스를 줄인다","상위 버전업 시 하위 버전을 0으로","마스킹은 긴 문자열부터 치환한다","롤링 업데이트는 가용성과 자원의 교환이다"] } },
    { t:"인프라 운영 핵심", n:4,
      th:{ sum:"네트워크 대역, 로그 관리, 빌드 캐시, **정족수 합의**의 원리다.",
        body:[
          {h:"CIDR 과 로그 로테이션", t:"IP 대역(CIDR) 매칭은 마스크 비트 수만큼 오른쪽으로 시프트(>>)하여 상위 비트만 깔끔하게 비교해야 한다. 로그 로테이션 시 파일 번호가 클수록 오래된 파일이며, 문자열이 아닌 숫자로 파싱해 정렬해야 올바른 파일을 지울 수 있다."},
          {h:"도커 캐시와 정족수 락", t:"Dockerfile 은 한 레이어의 캐시가 깨지면 이후의 모든 명령어가 종속적으로 다시 실행되므로, 자주 바뀌는 소스코드는 맨 아래에 두어야 한다. 분산 환경의 정족수(Quorum)는 스플릿 브레인을 막기 위해 반드시 N/2 '초과' 여야 하며, 이 때문에 클러스터는 항상 홀수 대를 쓴다."}],
        code:{ c:"shift = 32 - bits\nreturn (ip_i >> shift) == (net_i >> shift)", cap:"CIDR 비교는 비트 시프트로 상위 비트만 본다" },
        key:["CIDR 은 상위 비트를 비교한다","로그는 확장자 번호를 숫자로 정렬한다","도커 캐시는 깨진 시점 이후 전부 재실행","정족수는 무조건 과반수 초과여야 한다"] } }
  ]
};
