module.exports = {
  track: "algo", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 복잡도 표기와 자료구조의 성질",
  source: "./in_algo.cjs",
  lessons: [
    { t: "자료구조가 정하는 비용", n: 8,
      th: { sum: "복잡도는 코드가 아니라 **자료구조와 나누는 방식**이 정한다. 같은 문제도 무엇에 담느냐로 비용이 갈린다.",
        body: [
          { h: "반으로 나누면 로그, 한 칸씩이면 선형", t: "8 → 4 → 2 → 1 로 세 번 나누면 끝이라 병합 정렬의 재귀 깊이는 <code>log₂n</code> 이고, 각 깊이에서 전체를 한 번 훑어 <code>O(n log n)</code> 이다. 반대로 <b>한 칸씩만 줄어드는</b> 재귀는 깊이가 n 이라 스택이 넘친다 — 정렬된 입력에 첫 원소를 기준 삼은 퀵 정렬이 그렇다. 포화 이진 트리는 높이 h 에 <code>2^(h+1) - 1</code> 개이고, <b>마지막 층 하나가 나머지 전부보다 많다</b>." },
          { h: "무엇에 담느냐가 복잡도다", t: "인접 <b>리스트</b>로 두면 너비 우선 탐색이 <code>O(V+E)</code> 이지만 인접 <b>행렬</b>이면 <code>O(V²)</code> 다. 힙은 전체를 정렬해 두지 않고 <b>루트만</b> 최솟값이라, 상위 k 개만 필요하면 크기 k 짜리 힙으로 <code>O(n log k)</code> 에 끝난다. 배열로 만든 힙에서 0 기반 부모는 <code>(i-1)/2</code> 의 내림 — 1 기반과 섞으면 반드시 한 칸이 어긋난다." }],
        code: { c: "O(V+E)  인접 리스트  ·  O(V²)  인접 행렬\n부모 = (i - 1) >> 1        // 0 기반\n크기 k 힙으로 상위 k개  →  O(n log k)", cap: "담는 그릇이 비용을 정한다" },
        key: ["반으로 나누면 로그 깊이", "마지막 층이 나머지보다 많다", "리스트냐 행렬이냐로 복잡도가 갈린다", "0 기반과 1 기반을 섞지 않는다"] } },
    { t: "이름이 곧 보장인 성질들", n: 7,
      th: { sum: "'안정', 'DAG', '음수 간선 불가', '거의 상수' — 알고리즘의 이름과 조건은 **무엇을 보장하고 무엇을 보장하지 않는지**를 적어 둔 것이다.",
        body: [
          { h: "보장되는 것", t: "<b>안정 정렬</b>은 값이 같은 원소의 원래 순서를 지켜서 '부서로 정렬한 뒤 이름으로 정렬' 이 뜻대로 되게 한다 — 병합 정렬에는 있고 힙 정렬에는 없다. 이진 탐색 트리를 <b>중위 순회</b>하면 정의상 오름차순이라, '정렬된 목록이 필요한가' 가 트리와 해시를 고르는 갈림길이다(DB 인덱스가 B-tree 인 이유). 유니온-파인드는 <b>경로 압축과 랭크를 함께</b> 써야 거의 상수가 된다." },
          { h: "보장되지 않는 것", t: "위상 정렬은 <b>DAG</b> 에서만 되고, 그래서 그 알고리즘은 곧 <b>사이클 탐지기</b>다 — 결과 개수가 정점 수보다 적으면 순환 의존성이다. 다익스트라는 '한 번 확정한 최단거리는 더 줄지 않는다' 는 전제 위에 서 있어 <b>음수 간선</b>을 못 다룬다(그때는 벨만-포드). 계수 정렬이 <code>O(n+k)</code> 인 것은 <b>비교를 하지 않기</b> 때문이고, <code>k</code> 가 크면 메모리가 폭발한다." }],
        code: { c: "out.length === nodes.length ? out : []   // 개수가 곧 사이클 검사\nlower/upper bound 의 간격 = 같은 값의 개수\n계수 정렬 O(n + k) — 값 범위가 좁을 때만", cap: "조건을 못 지키면 보장도 없다" },
        key: ["안정 정렬은 여러 단계 정렬의 전제", "위상 정렬은 사이클 탐지기다", "다익스트라는 음수 간선 불가", "비교를 안 하면 n log n 하한을 피한다"] } },
  ],
};
