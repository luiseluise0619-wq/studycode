/* 9차 이론 — java 불변 객체, cpp 메모리 모델, c 정수 승격과 UB.

   셋 다 세 레슨이 같은 글을 나눠 쓰고 있었다. java '불변 객체' 는 '왜' 도 없었다.
   cpp '메모리 모델' 은 '메모리·스택·힙' 을 풀지 않고 썼는데, 그 셋이 바로 그
   레슨의 주제라 풀지 않으면 아무것도 전달되지 않는다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

java: [
{
  unit: "불변 객체와 방어적 복사 (중급)", lesson: "불변 객체 만들기",
  th: {
    sum: "만든 뒤에 **값이 바뀌지 않는 객체**를 불변 객체라 한다. 바뀌지 않으면 걱정할 일이 줄어든다.",
    body: [
      { h: "왜 바뀌지 않는 편이 나은가", t: "값이 바뀔 수 있으면, 내가 넘긴 객체를 남이 고쳐 놓았는지 늘 의심해야 한다. 바뀌지 않는다고 보장되면 어디에 넘기든 마음이 편하다. 여러 곳에서 동시에 써도 안전한 것도 같은 이유다." },
      { h: "만드는 법은 셋", t: "필드를 `final` 로 두고, 값을 바꾸는 메서드를 만들지 않고, 클래스를 상속하지 못하게 막는다. 셋 중 하나만 빠져도 어딘가에서 값이 바뀐다 — 상속을 열어 두면 자식이 규칙을 깨뜨릴 수 있다." },
      { h: "final 은 안쪽까지 지키지 않는다", t: "`final List<String> items` 는 **다른 목록으로 갈아 끼우지 못한다**는 뜻일 뿐이다. 그 목록에 값을 더하는 것은 그대로 된다. 안쪽까지 지키려면 복사해서 담거나 못 바꾸는 목록으로 감싸야 한다." },
      { h: "바꾸는 대신 새로 만든다", t: "값을 고친 것이 필요하면 자기를 고치지 말고 새 객체를 만들어 돌려준다. 원래 객체를 들고 있던 다른 코드는 영향을 받지 않는다. `String` 이 이 방식으로 만들어져 있다." },
    ],
    code: { c: "public final class User {\n    private final String name;\n    public User withName(String n) { return new User(n); }\n}", cap: "고치지 말고 새로 만든다" },
    key: ["바뀌지 않으면 의심할 일이 없다", "`final` 은 안쪽을 못 지킨다", "고칠 때는 새 객체로"],
  },
},
{
  unit: "불변 객체와 방어적 복사 (중급)", lesson: "방어적 복사",
  th: {
    sum: "받은 것과 돌려주는 것을 **복사해서** 주고받는 것이 방어적 복사다. 손잡이를 나눠 주지 않는다.",
    body: [
      { h: "받은 그대로 담으면 손잡이를 나눈 것", t: "생성자가 받은 목록을 그대로 필드에 두면, 넘겨준 쪽이 나중에 그 목록을 고칠 때 내 객체도 같이 바뀐다. 내가 모르는 사이에 내용이 달라지므로, 어디서 바뀌었는지 찾기도 어렵다." },
      { h: "돌려줄 때도 막는다", t: "게터가 내부 목록을 그대로 내보내면, 받은 쪽이 거기에 값을 더하는 순간 내 상태가 바뀐다. 들어올 때만 막고 나갈 때 안 막으면 절반만 지킨 셈이다." },
      { h: "감싸는 것만으로는 부족할 때가 있다", t: "`Collections.unmodifiableList` 는 그 껍데기를 통해 고치는 것을 막아 준다. 하지만 원본 목록을 쥔 쪽이 원본을 고치면 껍데기 너머로 그대로 비친다. 확실하게 하려면 복사한 뒤에 감싼다." },
      { h: "복사가 아까울 때", t: "목록이 아주 크고 자주 주고받는다면 복사 비용이 부담일 수 있다. 그럴 때는 아예 처음부터 못 바꾸는 자료구조를 쓰는 편이 낫다 — 복사도 방어도 필요 없어진다." },
    ],
    code: { c: "Team(List<String> ms) { this.ms = new ArrayList<>(ms); }   // 들어올 때\nList<String> get() { return new ArrayList<>(ms); }         // 나갈 때", cap: "양쪽 다 막아야 한다" },
    key: ["받은 것을 그대로 담지 않는다", "돌려줄 때도 복사한다", "감싸기 전에 복사한다"],
  },
},
{
  unit: "불변 객체와 방어적 복사 (중급)", lesson: "빌더 패턴과 생성 API",
  th: {
    sum: "인자가 많아지면 생성자만으로는 읽기 어렵다. **이름을 붙여 하나씩 채우는 방식**이 빌더다.",
    body: [
      { h: "인자가 많으면 순서를 틀린다", t: "`new User(\"루이\", 20, true, false)` 를 보고 마지막 두 개가 무슨 뜻인지 알 수 없다. 같은 타입이 이어지면 순서를 바꿔 넣어도 컴파일러가 잡아 주지 못한다. 그래서 실행해 보고 나서야 이상한 값이 드러난다." },
      { h: "이름을 붙여 채운다", t: "`builder().name(\"루이\").age(20).build()` 처럼 쓰면 무엇을 넣는지 글자로 남는다. 필요 없는 값은 아예 안 적으면 되어, 기본값이 다른 생성자를 여러 벌 만들 필요도 없다." },
      { h: "검사는 build 에서 한 번에", t: "빌더에 값을 채우는 동안에는 아직 불완전한 상태다. 마지막 `build()` 에서 필수 값이 다 찼는지 확인하고, 거기서만 객체를 만든다. 그러면 만들어진 객체는 언제나 올바른 상태다." },
      { h: "언제나 쓸 필요는 없다", t: "인자가 두세 개면 생성자가 더 짧고 읽기 쉽다. 빌더는 코드가 늘어나므로, 인자가 많거나 선택 항목이 여럿일 때만 꺼낸다. 도구를 습관처럼 쓰면 단순한 코드가 복잡해진다." },
    ],
    code: { c: "User u = User.builder()\n    .name(\"루이\")\n    .age(20)\n    .build();     // 여기서 검사하고 만든다", cap: "이름을 붙여 채우고 마지막에 검사" },
    key: ["인자가 많으면 순서를 틀린다", "이름을 붙이면 글자로 남는다", "검사는 `build` 에서"],
  },
},
],

cpp: [
{
  unit: "메모리 모델과 정렬 (심화)", lesson: "스택과 힙",
  th: {
    sum: "값이 놓이는 자리는 크게 둘이다. **함수와 함께 사라지는 자리**와 **직접 반납해야 하는 자리**다.",
    body: [
      { h: "함수와 함께 사라지는 자리", t: "함수 안에서 `int x;` 라고 쓰면 그 값은 함수가 끝날 때 자동으로 사라진다. 이 자리를 스택이라 부른다. 빠르고 정리도 자동이지만, 크기가 미리 정해져 있고 함수를 벗어나면 없어진다." },
      { h: "직접 반납해야 하는 자리", t: "`new` 로 만든 값은 함수가 끝나도 남는다. 이 자리를 힙이라 부른다. 크기를 실행 중에 정할 수 있고 오래 들고 있을 수 있지만, 다 쓰고 반납하지 않으면 계속 쌓인다." },
      { h: "사라진 자리를 가리키면 안 된다", t: "함수 안의 값을 가리키는 주소를 밖으로 돌려주면, 함수가 끝나는 순간 그 자리는 남의 것이 된다. 잠깐은 멀쩡히 동작하다가 언젠가 이상한 값이 나온다 — 가장 찾기 어려운 부류의 사고다." },
      { h: "직접 관리하지 않는 편이 낫다", t: "`new` 와 `delete` 를 손으로 맞추면 언젠가 하나를 빠뜨린다. `std::vector` 나 `std::unique_ptr` 를 쓰면 범위를 벗어날 때 알아서 반납한다. 요즘 C++ 에서는 이쪽이 기본이다." },
    ],
    code: { c: "int* bad() { int x = 1; return &x; }   // 사라진 자리를 가리킨다\n\nauto ok = std::make_unique<int>(1);    // 범위를 벗어나면 반납된다", cap: "자동으로 사라지느냐, 직접 반납하느냐" },
    key: ["스택은 함수와 함께 사라진다", "힙은 직접 반납한다", "사라진 자리를 가리키지 않는다"],
  },
},
{
  unit: "메모리 모델과 정렬 (심화)", lesson: "정렬과 구조체 크기",
  th: {
    sum: "값들은 아무 자리에나 놓이지 않는다. **자기 크기의 배수 자리**에 놓이길 원한다 — 이것이 정렬이다.",
    body: [
      { h: "왜 자리를 맞추나", t: "CPU 는 정해진 크기 단위로 메모리를 읽는다. 값이 그 경계에 걸쳐 있으면 두 번 읽어 붙여야 해서 느려진다. 그래서 컴파일러가 알아서 자리를 맞춰 준다." },
      { h: "그래서 빈칸이 생긴다", t: "`char` 다음에 `int` 가 오면, `int` 를 4의 배수 자리에 놓기 위해 사이에 빈칸 3개가 들어간다. 구조체 크기를 손으로 더한 값과 `sizeof` 가 다른 이유가 이것이다." },
      { h: "순서만 바꿔도 작아진다", t: "큰 것부터 작은 것 순으로 늘어놓으면 빈칸이 줄어든다. 같은 필드인데 순서만 바꿔 크기가 절반이 되기도 한다. 구조체를 수백만 개 담는다면 이 차이가 그대로 메모리 사용량이 된다." },
      { h: "파일이나 통신에 그대로 쓰지 않는다", t: "빈칸의 크기와 위치는 컴파일러와 기계에 따라 다르다. 구조체를 통째로 파일에 쓰거나 네트워크로 보내면, 다른 환경에서 읽을 때 어긋난다. 필드를 하나씩 정해진 형식으로 적어야 한다." },
    ],
    code: { c: "struct A { char c; int n; char d; };   // 12바이트 — 빈칸이 있다\nstruct B { int n; char c; char d; };   // 8바이트 — 순서만 바꿨다", cap: "큰 것부터 늘어놓는다" },
    key: ["값은 자기 크기의 배수 자리에", "그래서 빈칸이 생긴다", "구조체를 그대로 주고받지 않는다"],
  },
},
{
  unit: "메모리 모델과 정렬 (심화)", lesson: "placement new와 수동 소멸",
  th: {
    sum: "**이미 잡아 둔 자리에** 객체를 만드는 방법이 placement new 다. 자리 잡기와 만들기를 나눈다.",
    body: [
      { h: "보통은 둘이 한 번에 일어난다", t: "`new T()` 는 자리를 잡고 그 자리에 객체를 만든다. 두 가지 일이 한 번에 일어나는 셈이다. 자리 잡기가 비싸거나 자리를 미리 정해 두고 싶을 때, 이 둘을 나누고 싶어진다." },
      { h: "미리 잡아 둔 자리에 만든다", t: "큰 덩어리를 한 번에 잡아 두고 그 안에 객체를 하나씩 만들면, 자리 잡는 일이 한 번으로 줄어든다. `std::vector` 가 안에서 이렇게 동작한다 — 미리 잡아 둔 여유 칸에 값을 만들어 넣는다." },
      { h: "직접 만들었으면 직접 없앤다", t: "이렇게 만든 객체는 `delete` 하지 않는다. 자리는 내가 따로 잡은 것이기 때문이다. 대신 소멸자를 직접 부른다. 잊으면 정리 코드가 돌지 않아 파일이나 연결이 열린 채 남는다." },
      { h: "웬만하면 쓰지 않는다", t: "순서를 하나만 틀려도 찾기 어려운 사고가 난다. 표준 컨테이너가 이미 이 일을 안전하게 해 주므로, 직접 쓸 일은 자기만의 자료구조를 만들 때 정도다." },
    ],
    code: { c: "void* raw = ::operator new(sizeof(T));\nT* p = new (raw) T();   // 잡아 둔 자리에 만든다\np->~T();                 // 직접 없앤다\n::operator delete(raw);", cap: "자리 잡기와 만들기를 나눈다" },
    key: ["자리 잡기와 만들기를 나눈다", "`delete` 대신 소멸자를 직접", "웬만하면 컨테이너에 맡긴다"],
  },
},
],

c: [
{
  unit: "정수 승격·정의되지 않은 동작·함수 포인터 심화", lesson: "정수 승격과 부호 비교",
  th: {
    sum: "작은 정수는 계산 전에 **자동으로 int 로 바뀐다.** 부호가 있는 것과 없는 것을 견주면 여기서 사고가 난다.",
    body: [
      { h: "작은 타입은 먼저 커진다", t: "`char` 나 `short` 는 계산에 들어가기 전에 `int` 로 바뀐다. 그래서 `char` 두 개를 더해도 결과는 `int` 다. 이걸 모르면 '왜 넘치지 않았지' 또는 '왜 타입이 달라졌지' 로 헷갈린다." },
      { h: "부호 없는 쪽으로 끌려간다", t: "`int` 와 `unsigned int` 를 견주면, 부호 있는 쪽이 부호 없는 쪽으로 바뀐다. 그래서 `-1 < 1u` 가 **거짓**이 된다. -1이 아주 큰 값으로 읽히기 때문이다." },
      { h: "가장 흔한 사고는 sizeof 비교", t: "`sizeof` 와 `strlen` 은 부호 없는 값을 준다. `for (int i = 0; i < strlen(s) - 1; i++)` 에서 문자열이 비면 `0 - 1` 이 아주 큰 값이 되어 반복문이 끝없이 돈다." },
      { h: "타입을 맞춰 두고 비교한다", t: "비교하는 두 값의 타입을 같게 맞추거나, 길이를 먼저 부호 있는 변수에 담아 둔다. 컴파일러 경고를 켜 두면 이런 자리를 미리 알려 준다 — 경고를 끄고 지내면 실행 중에야 만난다." },
    ],
    code: { c: "if (-1 < 1u) { }          // 거짓이다\n\nint n = (int)strlen(s);\nfor (int i = 0; i < n - 1; i++) { }   // 이제 안전하다", cap: "부호 없는 쪽으로 끌려간다" },
    key: ["작은 타입은 `int` 로 커진다", "부호 없는 쪽으로 맞춰진다", "길이는 부호 있는 변수에 담는다"],
  },
},
{
  unit: "정수 승격·정의되지 않은 동작·함수 포인터 심화", lesson: "오버플로와 UB 대표 사례",
  th: {
    sum: "C 에는 '이렇게 하면 무슨 일이 일어날지 정하지 않는다' 는 자리가 있다. 이것이 **정의되지 않은 동작(UB)** 이다.",
    body: [
      { h: "정하지 않았다는 것의 뜻", t: "오류가 난다는 뜻이 아니라, 무엇이든 일어날 수 있다는 뜻이다. 어떤 기계에서는 잘 돌고 어떤 기계에서는 이상한 값이 나온다. 그래서 '내 컴퓨터에서는 되는데' 가 가장 위험한 말이 된다." },
      { h: "부호 있는 정수의 넘침", t: "`int` 의 최댓값을 넘기는 계산은 UB 다. 컴파일러는 '넘치는 일은 없다' 고 가정하고 코드를 줄이기 때문에, `if (a + 1 < a)` 같은 검사가 통째로 사라지기도 한다. 넘침을 막으려면 계산하기 전에 확인해야 한다." },
      { h: "범위 밖 접근과 해제 뒤 사용", t: "배열 밖을 읽고 쓰거나 이미 반납한 메모리를 다시 쓰는 것도 UB 다. 대개는 바로 안 죽고 한참 뒤에 엉뚱한 자리에서 이상한 값으로 드러난다. 원인과 증상이 멀리 떨어져 있어 찾기 어렵다." },
      { h: "도구에 맡긴다", t: "새니타이저를 켜고 돌리면 이런 자리를 그 자리에서 잡아 준다. 눈으로 찾으려 하지 말고, 개발 중에는 켜 두는 것이 훨씬 싸다." },
    ],
    code: { c: "// 넘칠 수 있는지 먼저 확인한다\nif (b > 0 && a > INT_MAX - b) { /* 못 더한다 */ }\nelse sum = a + b;", cap: "정하지 않았다 = 무엇이든 일어난다" },
    key: ["UB 는 오류가 아니라 '무엇이든'", "넘침 검사는 계산 전에", "새니타이저를 켜 둔다"],
  },
},
{
  unit: "정수 승격·정의되지 않은 동작·함수 포인터 심화", lesson: "함수 포인터",
  th: {
    sum: "함수도 주소가 있다. 그 주소를 담아 두면 **어떤 함수를 부를지 실행 중에 정할 수 있다.**",
    body: [
      { h: "무엇에 쓰나", t: "정렬 기준을 바꿔 주거나, 사건이 생겼을 때 부를 함수를 미리 등록해 둘 때 쓴다. `qsort` 가 비교 함수를 받는 것이 대표적이다. 이게 없으면 기준마다 정렬 함수를 따로 만들어야 한다." },
      { h: "모양을 정확히 맞춘다", t: "받는 인자와 돌려주는 타입이 정확히 같아야 한다. 다른 모양의 함수를 억지로 넘기면, 컴파일은 지나가도 부르는 순간 무슨 일이 일어날지 알 수 없다. 경고를 무시하지 않는 것이 첫 번째 방어다." },
      { h: "typedef 로 읽을 수 있게", t: "`int (*cmp)(const void*, const void*)` 같은 선언은 읽기 어렵다. `typedef` 로 이름을 붙여 두면 함수 서명이 한눈에 들어온다. 인자로 주고받을 때 특히 차이가 크다." },
      { h: "표로 만들면 분기가 사라진다", t: "명령 이름과 함수를 짝지은 표를 만들어 두면, 긴 `if` 사슬 대신 표에서 찾아 부르면 된다. 명령을 하나 늘릴 때 표에 한 줄만 더하면 되어 빠뜨릴 일이 줄어든다." },
    ],
    code: { c: "typedef int (*Cmp)(const void*, const void*);\n\nvoid sort_with(int *xs, int n, Cmp cmp) {\n    qsort(xs, n, sizeof(int), cmp);\n}", cap: "부를 함수를 실행 중에 정한다" },
    key: ["함수도 주소가 있다", "모양이 정확히 맞아야 한다", "표로 만들면 분기가 사라진다"],
  },
},
],

};
