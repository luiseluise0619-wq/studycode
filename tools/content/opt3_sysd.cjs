/* sysd — 정답률 15% 아래 끝맺음의 오답 10개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "sysd", fixes: [
  { k: "sd2-jwt-vs-server", from: "발급 서버에만 있기 때문이다", to: "JWT 는 여러 서버에서 검증할 수 없어 스티키 세션이 필수다. 서명 키가 발급 서버에만 있는 탓이다" },
  { k: "sd2-mq-backpressure", from: "큐 용량이 무한하니 신경 쓰지 않아도 된다", to: "큐 용량이 무한하니 적체는 그대로 두어도 된다" },
  { k: "presigned-url", from: "누구나 올릴 수 있으면 인증이 필요 없다", to: "스토리지 버킷을 공개(public)로 열어 둔다. 누구나 올릴 수 있으면 인증 단계가 사라진다" },
  { k: "정확히한번", from: "따로 중복을 걸러 낼 필요가 없다", to: "2단계 커밋을 쓰면 전달 자체를 정확히 한 번으로 만들 수 있다. 커밋이 하나로 묶이므로 중복도 유실도 생기지 않는다. 그래서 애플리케이션 쪽 중복 제거는 군더더기다" },
  { k: "CRDT", from: "CRDT는 네트워크가 필요 없다", to: "CRDT는 네트워크 없이도 동기화된다" },
  { k: "재시도·백오프", from: "사람이 정하게 하면 폭풍은 생기지 않는다", to: "재시도를 완전히 비활성화한다 — 실패는 사용자에게 곧바로 알리고 다시 시도할지는 사람이 정하게 하면 폭풍은 사라진다" },
  { k: "sysd-idem-two-generals", from: "TCP가 응답을 재전송하므로 문제가 없다", to: "TCP가 응답을 재전송하므로 클라이언트는 결국 성공을 받는다" },
  { k: "sysd-ms-orch-vs-chor", from: "오케스트레이션은 보상 트랜잭션이 필요 없다", to: "오케스트레이션은 보상 트랜잭션 없이 되돌린다" },
  { k: "다중 AZ/리전", from: "고려할 필요가 없다", to: "다중 AZ 구성만으로 리전 전체 장애를 견딜 수 있다 — AZ 는 서로 다른 데이터센터이므로 리전에 문제가 생겨도 AZ 중 하나는 남아 있고, 그래서 다중 리전은 규제 요구가 있는 금융권 같은 특수한 경우에만 남겨 둔다" },
  { k: "관측성 3요소", from: "지표와 추적은 필요 없다", to: "로그만 충분히 남기면 지표와 추적은 로그에서 뽑아낸다" }
] };
