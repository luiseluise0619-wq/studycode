module.exports = {
  track: "cs", guide: "", xp: 80,
  unit: "실행형 실전 — 교과서 코드를 반쯤 옮긴 자리 고치기",
  source: "./dbg_cs.cjs",
  lessons: [
    { t: "전제와 기저 조건이 빠진 곳", n: 6,
      th: { sum: "알고리즘 버그는 대개 **깨끗한 예제에서 전부 통과한다** — 중복이 없고, 정렬돼 있고, 한쪽이 비어 있지 않은 입력.",
        body: [
          { h: "전제를 코드가 지키는가", t: "구간 병합은 **정렬이 전제**이고, 이진 탐색은 '어느 자리를 줄 것인가' 를 정해야 한다 — 중복이 있으면 '찾으면 반환' 은 아무 자리나 준다. 회문 검사는 '무엇을 같다고 볼 것인가'(대소문자·문장부호)를 먼저 정해야 한다. 전제는 **함수가 보장하거나 계약이 요구하거나** 둘 중 하나여야 한다." },
          { h: "기저 조건이 정답의 절반", t: "DP 버그의 대부분은 점화식이 아니라 **첫 행과 첫 열**에 있다. 편집 거리의 `d[i][0]` 은 '빈 문자열로 만드는 비용' 이라 `i` 다. BFS 에서 시작 노드의 거리는 **0** 이고, 최대공약수는 음수가 될 수 없다. 셋 다 '가장 기본적인 입력' 하나로 잡힌다." }],
        code: { c: "for (let i = 0; i <= n; i += 1) d[i][0] = i;\nfor (let j = 0; j <= m; j += 1) d[0][j] = j;", cap: "표의 첫 줄이 뜻하는 바를 적어 본다" },
        key: ["중복 있는 입력을 테스트한다", "정렬 전제를 명시한다", "자기 자신까지 거리는 0", "빈 입력을 첫 테스트로"] } },
    { t: "표현 범위와 순서가 흔들리는 곳", n: 6,
      th: { sum: "값이 조용히 어긋나는 세 가지 — **진법을 안 적었다**, **큰 수를 먼저 만들었다**, **동점을 순서로 풀었다**.",
        body: [
          { h: "작게 유지하며 계산한다", t: "`parseInt` 는 진법을 생략하면 10진수로 읽고, `2 ** 62` 는 정수 정확도를 넘겨 **그럴듯한 틀린 값**이 된다. 나머지 연산은 곱할 때마다 접고, 확률의 곱은 로그로 다룬다 — '큰 수를 만든 뒤 줄이기' 대신 '작게 유지하며 계산하기' 가 원칙이다." },
          { h: "순서에 기대지 않는다", t: "동점을 '먼저 만난 쪽' 으로 풀면 답이 입력 순서에 딸려 가 재현이 안 된다. `sort` 는 원본을 바꾸고 비교 함수를 안 주면 문자열로 견준다. 순환 인덱스는 **쓰기 전에 정규화**해야 `k` 가 길이를 넘거나 음수여도 무너지지 않는다." }],
        code: { c: "if (e % 2 === 1) r = (r * b) % mod;   // 매 단계 접는다\nconst s = ((k % n) + n) % n;          // 먼저 정규화", cap: "범위를 넘기기 전에 줄인다" },
        key: ["parseInt 에 진법을 적는다", "나머지는 매 단계 접는다", "동점에는 값 기준을 더한다", "회전 값은 미리 접는다"] } },
  ],
};
