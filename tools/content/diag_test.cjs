/* Testing 진단 유닛 — 테스트 자체가 고장 났을 때.
   확인해 주는 줄 알았던 것이 아무것도 확인하지 않고 있거나,
   같은 코드에서 결과가 달라질 때 무엇을 의심할 것인가.

   보기 규칙: 네 보기의 길이를 흩는다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const D=(k,q,o,a,ex)=>({t:"choice",cat:"debug",k,q,o,a,ex});
const I=(k,q,a,ex)=>({t:"input",cat:"debug",k,q,a,ex});

module.exports=shuffleUnits([

{ t:"진단 — 테스트가 고장 났을 때", l:[

  { t:"통과하는데 확인하지 않는다", xp:40,
    th:{ sum:"가장 위험한 테스트는 실패하는 테스트가 아니라 **아무것도 확인하지 않으면서 통과하는 테스트**다. 초록불이 켜져 있으니 아무도 들여다보지 않는다.",
      body:[
        {h:"어서션이 실행되지 않는다", t:"비동기 함수를 기다리지 않고 끝내면 그 안의 어서션은 **테스트가 끝난 뒤에** 돌거나 아예 돌지 않는다. `await` 를 빠뜨리거나 `expect` 를 콜백 안에만 두는 경우가 대표적이다. 이런 테스트는 코드를 통째로 지워도 통과하므로, 의심스러우면 **일부러 깨뜨려 보는 것**이 가장 빠른 확인이다 — 깨지지 않으면 그 테스트는 아무 일도 하지 않고 있다."},
        {h:"커버리지는 실행만 센다", t:"90%가 나와도 어서션이 없으면 확인된 것은 없다. 커버리지는 **그 줄을 지나갔는가**만 세기 때문이다. 그래서 숫자를 게이트로 걸면 사람들이 '실행만 하는 테스트' 로 숫자를 채우게 된다. 진짜로 알고 싶은 것 — **테스트가 결함을 잡아내는가** — 는 코드를 일부러 바꿔 보고 테스트가 실패하는지 재는 방식으로만 잴 수 있다."}],
      code:{ c:"test(\"저장된다\", () => {\n  save(item).then((r) => {\n    expect(r.ok).toBe(true);   // 여기까지 오지 않는다\n  });\n});                          // 기다리지 않고 끝난다\n\n// 코드를 지워도 통과한다", cap:"깨뜨려 보면 바로 드러난다" },
      key:["기다리지 않으면 어서션이 안 돈다","의심되면 일부러 깨뜨려 본다","커버리지는 실행만 센다","결함을 잡는지는 따로 재야 한다"] },
    q:[
      D("코드를 지워도 통과","확인 대상 코드를 통째로 지워도 테스트가 통과합니다. 원인으로 알맞은 것은?",
        ["어서션이 실행되지 않는 자리에 있다",
         "테스트 러너가 그 파일을 통째로 건너뛰고 있다",
         "기대값이 실제값과 우연히 같아졌다",
         "테스트가 캐시된 결과를 쓰고 있다"],0,
        "**어서션이 돌지 않는 것**이 가장 흔합니다. 비동기를 기다리지 않았거나 콜백 안에만 있어 실행 전에 테스트가 끝난 경우입니다.\n러너가 파일을 건너뛰었다면 그 테스트는 애초에 목록에 나오지 않으므로 구분됩니다.\n의심스러울 때는 **기대값을 일부러 틀리게 바꿔 보는 것**이 가장 빠른 확인입니다."),
      D("혼자는 통과, 전체는 실패","단독 실행은 되는데 전체 스위트에서는 실패합니다. 원인으로 알맞은 것은?",
        ["앞 테스트가 남긴 상태에 영향을 받는다",
         "전체 실행 시 시간 제한이 짧아진다",
         "테스트 러너가 그 테스트를 무작위로 건너뛴다",
         "전체를 함께 돌리면 어서션이 느슨하게 동작한다"],0,
        "**상태 공유**입니다. 전역 변수, 데이터베이스 행, 모듈 수준 캐시, 되돌리지 않은 목이 남아 다음 테스트에 영향을 줍니다.\n각 테스트가 자기 데이터를 만들고 끝나면 되돌리도록 하면 사라집니다.\n실행 순서를 일부러 섞어 보면 이런 의존이 빨리 드러납니다."),
      D("CI 에서만 실패","로컬은 통과하는데 CI 에서만 실패합니다. 흔한 원인으로 알맞은 것은?",
        ["환경 차이와 로컬에 남아 있던 상태",
         "CI 서버의 저장 공간이 부족하기 때문",
         "CI 에서는 어서션이 더 엄격하게 동작하기 때문",
         "CI 는 테스트를 항상 무작위 순서로 돌리기 때문"],0,
        "**깨끗한 환경에서 처음부터 돌린다**는 점이 CI 의 값어치이자 차이의 원인입니다. 로컬에 남아 있던 캐시·파일·환경 변수가 없습니다.\n시간대, 언어 설정, 파일 이름 대소문자 구분도 흔한 차이입니다.\n로컬에서 깨끗한 상태로 돌려 보면 대부분 재현됩니다."),
      D("커버리지는 높은데","커버리지 90%인데 운영에서 버그가 계속 납니다. 무엇을 의심해야 하나요?",
        ["실행만 하고 결과를 확인하지 않는 테스트가 많다",
         "커버리지 도구가 숫자를 잘못 계산하고 있다",
         "테스트 개수가 아직 부족하다",
         "커버리지 목표를 더 높여야 한다"],0,
        "커버리지는 **그 줄을 지나갔는가**만 셉니다. 어서션이 없어도 숫자는 올라갑니다.\n숫자를 게이트로 걸면 사람들이 실행만 하는 테스트로 채우게 되어, 목표를 올릴수록 오히려 나빠지기도 합니다.\n테스트가 결함을 잡아내는지는 코드를 일부러 바꿔 보고 실패하는지 재는 방식으로만 확인됩니다."),
      D("리팩터링마다 깨진다","동작을 바꾸지 않았는데 테스트가 우수수 깨집니다. 원인으로 알맞은 것은?",
        ["구현 방식을 못 박는 테스트가 많다",
         "리팩터링 도구가 테스트 파일까지 바꾼다",
         "테스트 이름이 구현 이름과 같기 때문",
         "커버리지가 재계산되기 때문"],0,
        "**목이 많거나 내부 상태를 확인하는 테스트**가 원인입니다. '어떻게 하는가' 를 못 박아 두면 방식만 바꿔도 깨집니다.\n결과로 확인할 수 있는 것은 결과로 확인하고, 결과에 드러나지 않는 부수 효과만 목으로 봅니다.\n이런 테스트는 안전망이 아니라 족쇄가 되어, 결국 리팩터링을 포기하게 만듭니다."),
      D("스냅숏을 늘 갱신","스냅숏 테스트가 실패할 때마다 갱신하고 넘어갑니다. 문제로 알맞은 것은?",
        ["회귀가 그대로 승인되어 통과한다",
         "스냅숏 파일의 용량이 계속 늘어난다",
         "갱신할 때마다 실행 시간이 길어진다",
         "스냅숏은 한 번 만들면 갱신할 수 없다"],0,
        "**차이를 보지 않고 승인하는 것**이 스냅숏의 가장 큰 위험입니다. 진짜 회귀가 그대로 통과합니다.\n스냅숏이 클수록 심해지므로 작게 유지하고 핵심만 담는 것이 좋습니다.\n의도가 분명한 어서션 몇 개가 큰 스냅숏 하나보다 대개 낫습니다."),
      D("경계에서만 틀린다","테스트는 다 통과하는데 특정 값에서만 결과가 틀립니다. 빠진 것으로 알맞은 것은?",
        ["경계값 확인",
         "성능 측정",
         "통합 테스트",
         "코드 리뷰"],0,
        "**경계값**이 빠진 것입니다. 범위 한가운데 값만 넣으면 부등호를 잘못 쓴 실수가 드러나지 않습니다.\n경계마다 바로 아래·경계·바로 위 세 값을 넣으면 이 종류가 통째로 걸립니다.\n빈 값·널·아주 큰 수 같은 타입 경계도 함께 봐야 합니다."),
      I("빈 테스트","어서션이 없거나 실행되지 않아 무엇도 확인하지 못하면서 통과하는 테스트를 뭐라고 부르나? (한국어)",
        ["빈 테스트","가짜 통과","무의미한 테스트","거짓 음성"],
        "정답은 **빈 테스트**(또는 거짓 음성) 입니다. 문제가 있는데 통과하는 상태입니다.\n거짓 양성(문제가 없는데 실패)은 눈에 띄어 곧 고쳐지지만, 이쪽은 초록불이라 아무도 들여다보지 않습니다.\n확인하는 방법은 하나뿐입니다 — **일부러 깨뜨려 보고 정말 실패하는지 봅니다.**")
    ] },

  { t:"느리고 흔들린다", xp:40,
    th:{ sum:"스위트가 느려지거나 결과가 흔들리기 시작하면 **사람들이 기다리지 않고 빨간불을 무시한다.** 그 순간부터 테스트는 값어치를 잃는다.",
      body:[
        {h:"느려지는 원인은 대개 준비 단계", t:"쿼리나 프레임워크보다 **테스트마다 만드는 데이터의 양**이 범인인 경우가 훨씬 많다. 확인하려는 것에 필요한 최소한만 만들면 대부분 빨라진다. 그다음이 **가장 오래 걸리는 몇 개** 다 — 전체를 고르게 줄이려 하지 말고 상위 몇 개만 손봐도 체감이 크게 달라진다. 마지막으로 나눠 돌릴 때는 **긴 것부터 한가한 샤드에** 넣어야 균형이 맞는다."},
        {h:"흔들림은 재시도로 덮이지 않는다", t:"플레이키의 원인은 대개 넷 중 하나다 — **고정 시간 대기**, **테스트 간 상태 공유**, **시간·난수 의존**, **실행 순서 의존**. 재시도를 걸면 빨간불은 사라지지만 원인은 남고, 그 불안정성이 운영에서는 열 번에 한 번 실패하는 기능일 수 있다. 당장 못 고치면 격리하되 **담당자와 기한**을 함께 정한다."}],
      code:{ c:"// 흔들리는 코드\nawait sleep(3000);\nexpect(screen.getByText(\"저장됨\")).toBeVisible();\n\n// 조건을 기다린다\nawait screen.findByText(\"저장됨\");", cap:"시간이 아니라 조건을 기다린다" },
      key:["느림의 범인은 대개 데이터 양","가장 오래 걸리는 몇 개부터","플레이키 원인은 대개 넷 중 하나","재시도는 원인을 숨긴다"] },
    q:[
      D("점점 느려진다","테스트 스위트가 몇 달 사이 두 배로 느려졌습니다. 먼저 볼 것으로 알맞은 것은?",
        ["테스트마다 만드는 데이터의 양",
         "테스트 프레임워크의 버전",
         "저장소에 들어 있는 테스트 파일의 총 개수",
         "어서션의 개수"],0,
        "**준비하는 데이터가 필요 이상으로 많은 것**이 가장 흔한 원인입니다. 테스트 하나에 사용자 100명이 필요한 경우는 드뭅니다.\n확인하려는 것에 필요한 최소한만 만들면 대부분 빨라집니다.\n프레임워크나 어서션 개수는 실질적인 영향이 거의 없습니다."),
      D("E2E 가 가끔 실패","E2E 가 열 번에 한 번쯤 실패합니다. 가장 흔한 원인으로 알맞은 것은?",
        ["고정 시간 대기",
         "브라우저 버전 차이",
         "테스트 코드의 문법 오류",
         "화면 해상도 설정"],0,
        "**고정 대기**가 압도적으로 흔합니다. 느린 환경에서는 부족하고 빠른 환경에서는 낭비입니다.\n'이 텍스트가 나타날 때까지' 처럼 **확인 조건 자체를 대기 조건**으로 바꾸면 두 문제가 함께 사라집니다.\n대기 시간을 늘리는 대응은 스위트만 느려지게 하고 언젠가 다시 실패합니다."),
      D("병렬로 켰더니 실패","병렬 실행을 켜자 실패가 시작됩니다. 원인으로 알맞은 것은?",
        ["여러 테스트가 같은 자원을 함께 쓰고 있다",
         "병렬 실행은 어서션 순서를 바꾼다",
         "병렬로 돌리면 시간 제한이 짧아진다",
         "병렬 실행은 커버리지 계산 방식과 충돌한다"],0,
        "**공유 자원**입니다. 같은 데이터베이스 행, 같은 파일, 같은 포트, 같은 공용 계정을 두 테스트가 동시에 건드리는 것입니다.\n테스트마다 고유한 데이터를 만들면 대부분 해결됩니다.\n병렬 실행은 숨어 있던 독립성 문제를 드러내는 것이지 새로 만드는 것이 아닙니다."),
      D("원인을 못 찾는다","CI 에서만 나는 E2E 실패의 원인을 찾지 못하고 있습니다. 가장 도움이 되는 것은?",
        ["실패 시점의 스크린숏과 실행 기록",
         "테스트 코드의 변경 이력",
         "CI 서버의 사양과 거기 설치된 도구 정보",
         "실패 횟수의 통계"],0,
        "**그때 화면이 어떤 상태였는지**를 보는 것이 가장 빠릅니다. 대부분의 도구가 실패 시 스크린숏·영상·네트워크 기록을 남길 수 있습니다.\nCI 설정에서 **실패했을 때만** 이 결과물을 올리도록 해 두면 비용도 크지 않습니다.\n통계는 '흔들린다' 는 사실만 알려 줄 뿐 원인을 말해 주지 않습니다."),
      D("재시도로 덮었다","실패할 때마다 자동 재시도하도록 설정했습니다. 남는 위험으로 알맞은 것은?",
        ["같은 불안정성이 운영에서 실패로 나타난다",
         "재시도 때문에 커버리지가 낮아진다",
         "재시도한 테스트는 결과가 어디에도 기록되지 않는다",
         "재시도는 어서션 일부를 건너뛰게 만든다"],0,
        "**원인이 사라지지 않고 숨습니다.** 테스트에서 열 번에 한 번 실패하는 것은 운영에서도 열 번에 한 번 실패할 수 있습니다.\n재시도는 급할 때의 임시 방편이지 해결이 아닙니다. 재시도한 사실을 기록해 두고 정기적으로 들여다봐야 합니다.\n커버리지나 어서션과는 관계가 없습니다."),
      D("DB 테스트가 오염된다","데이터베이스를 쓰는 테스트들이 서로 영향을 줍니다. 가장 깔끔한 해법은?",
        ["각 테스트를 트랜잭션으로 감싸고 끝나면 롤백한다",
         "테스트를 하나씩 순서대로만 실행한다",
         "테스트마다 데이터베이스 서버를 새로 띄웠다 내린다",
         "테스트가 끝날 때마다 모든 테이블의 데이터를 비운다"],0,
        "**롤백**은 정리 코드가 필요 없고 테스트가 실패해도 자동으로 되돌아갑니다.\n테이블을 비우는 방식은 정리 코드를 유지해야 하고 외래 키 순서 문제가 생기기도 합니다.\n서버를 매번 띄우는 것은 확실하지만 너무 느려 아무도 돌리지 않게 됩니다."),
      D("격리 목록만 는다","플레이키를 격리해 두었는데 목록이 계속 길어집니다. 빠진 것으로 알맞은 것은?",
        ["담당자와 기한",
         "격리 자동화 도구",
         "더 자세한 실패 로그",
         "재시도 횟수 설정"],0,
        "**기한이 붙지 않은 격리는 잊어버리기와 같습니다.** 담당자와 날짜가 없으면 목록은 1년 뒤에도 그대로 남습니다.\n격리는 스위트 전체의 신뢰를 지키기 위한 **유예**이지 처분이 아닙니다.\n목록이 일정 길이를 넘으면 새 격리를 막는 규칙을 두는 팀도 있습니다."),
      I("실패 증거","E2E 도구가 실패 조사를 위해 남기는, 각 단계의 화면과 네트워크를 담은 기록은? (영어)",
        ["trace","트레이스"],
        "정답은 **트레이스(trace)** 입니다. 단계별 화면·DOM·네트워크 요청을 함께 담아 되짚어 볼 수 있게 해 줍니다.\nCI 에서만 나는 실패를 조사할 때 결정적입니다 — 로컬에서 재현되지 않아도 그때 상태가 그대로 보입니다.\n실패했을 때만 남기도록 설정하면 저장 비용도 크지 않습니다.")
    ] }
]}

]);
