/* security 트랙 코드 리뷰 — 방어하는 쪽에서 코드를 읽고 위험한 줄을 짚는다. */
module.exports = [
{
  t: "review", k: "비밀번호를 저장하는 방식", cat: "review", d: 2, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "async function signup(email, password) {\n  const hash = sha1(password);\n  await db.users.insert({ email, hash });\n}\n\nasync function login(email, password) {\n  const u = await db.users.find({ email });\n  return u && u.hash === sha1(password);\n}",
  items: [
    { txt: "<code>sha1</code> 은 초당 수십억 번 계산할 수 있게 설계된 <b>빠른</b> 해시라 비밀번호에 맞지 않는다. 유출되면 흔한 비밀번호는 사실상 즉시 뚫리므로, 일부러 느린 <code>bcrypt</code>·<code>argon2</code> 계열을 써야 한다.", bad: true },
    { txt: "사용자마다 다른 <b>솔트가 없다</b>. 같은 비밀번호를 쓴 계정의 해시가 똑같아 한 번의 계산으로 여러 계정이 함께 뚫리고, 미리 만들어 둔 표를 그대로 대조할 수도 있게 된다.", bad: true },
    { txt: "존재하지 않는 이메일이면 해시 계산을 건너뛰고 곧바로 거짓을 돌려주므로, 응답 시간만 재어도 <b>가입 여부</b>를 알아낼 수 있다 — 없는 계정에도 더미 검증을 수행해 시간을 맞추는 것이 정석이다.", bad: true },
    { txt: "관리되는 런타임에서는 문자열을 지워도 사본이 남을 수 있어 이 코드만의 결함으로 보기 어렵지만, 비밀번호는 즉시 덮어쓸 수 있는 버퍼로 받아야 한다.", bad: false },
    { txt: "회원 가입에서 이메일 형식과 중복을 검사하지 않아 같은 주소로 여러 계정이 생길 수 있다 — 필요한 검증이지만 이 리뷰의 초점인 자격 증명 저장과는 다른 층위의 이야기다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>빠른 해시</b>는 비밀번호 저장에 쓰면 안 된다 — 공격자의 계산 속도가 그대로 무기가 된다. (2) <b>솔트가 없어</b> 같은 비밀번호가 같은 해시가 되고, 미리 계산된 표에 그대로 걸린다. (3) <b>없는 계정에서 빨리 끝나는</b> 코드는 가입 여부를 흘린다. 세 가지 모두 표준 비밀번호 해시 라이브러리를 쓰면 함께 해결된다.\n디스트랙터: 메모리에 잠시 남는 문제는 이 코드만의 결함이 아니고, 이메일 검증은 다른 층위의 요구사항이다.",
},
{
  t: "review", k: "예측 가능한 토큰", cat: "review", d: 2, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function makeResetToken(userId) {\n  const t = Math.random().toString(36).slice(2);\n  tokens.set(t, userId);\n  return t;\n}",
  items: [
    { txt: "메모리에 담으면 재시작할 때 사라지고 인스턴스끼리 공유되지 않는다 — 보안보다 배포 구조의 제약에 가깝지만 공유 저장소로 옮겨야 한다.", bad: false },
    { txt: "<code>Math.random</code> 은 <b>암호학적으로 안전하지 않다</b>. 몇 개의 출력만 관찰하면 내부 상태를 복원해 다음 값을 예측할 수 있으므로, 재설정 토큰에는 <code>crypto.randomUUID</code> 나 <code>randomBytes</code> 를 써야 한다.", bad: true },
    { txt: "토큰에 <b>만료가 없다</b>. 한 번 발급되면 영원히 유효하므로 유출된 메일 하나가 몇 달 뒤에도 계정을 열어 주고, 쓰고 난 뒤 폐기하는 처리도 없어 재사용까지 가능하다.", bad: true },
    { txt: "<code>toString(36).slice(2)</code> 는 길이가 들쭉날쭉해 아주 짧은 토큰이 나올 수 있다 — 무작위원이 안전하더라도 <b>엔트로피가 모자라면</b> 대입으로 뚫리므로 길이를 고정해야 한다.", bad: true },
    { txt: "함수가 사용자 식별자를 받아 그대로 저장하므로 존재하지 않는 사용자에게도 토큰이 만들어진다 — 호출하는 쪽에서 검증하기로 했다면 계약대로이며 그 자체로 위험하지는 않다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>예측 가능한 난수</b>는 토큰의 근거를 무너뜨린다. (2) <b>만료와 일회성이 없어</b> 유출된 토큰이 영구히 살아 있다. (3) <b>길이가 보장되지 않아</b> 엔트로피가 모자란 토큰이 섞인다. 재설정 토큰은 '충분히 길고, 예측 불가능하고, 짧게 살고, 한 번만 쓰이는' 네 가지를 모두 만족해야 한다.\n디스트랙터: 메모리 저장은 배포 구조의 문제이고, 사용자 검증 위치는 계약에서 정할 일이다.",
},
{
  t: "review", k: "출처를 그대로 되비추는 CORS", cat: "review", d: 3, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "app.use((req, res, next) => {\n  res.setHeader(\"Access-Control-Allow-Origin\", req.headers.origin || \"*\");\n  res.setHeader(\"Access-Control-Allow-Credentials\", \"true\");\n  next();\n});",
  items: [
    { txt: "모든 응답에 헤더가 따라붙어 크기가 조금 커진다 — 실질적인 영향은 미미하지만 필요한 응답에만 붙이도록 좁혀야 한다.", bad: false },
    { txt: "미들웨어에서 <code>next()</code> 를 부르므로 이후 처리로 정상적으로 넘어간다 — 헤더만 설정하고 흐름을 막지 않는 구조 자체는 올바르다.", bad: false },
    { txt: "요청이 보낸 출처를 <b>그대로 되비추면</b> 사실상 모든 사이트를 허용하는 것과 같다. 자격 증명까지 함께 허용했으므로, 로그인한 사용자가 공격자의 페이지를 열기만 해도 그 페이지가 우리 API 를 <b>사용자 권한으로</b> 부를 수 있다.", bad: true },
    { txt: "허용 목록을 두고 <b>목록에 있을 때만</b> 그 출처를 돌려주는 것이 올바른 형태다. 함께 <code>Vary: Origin</code> 을 붙이지 않으면 캐시가 한 출처의 응답을 다른 출처에 돌려줄 수 있어 허용 범위가 의도치 않게 넓어진다.", bad: true },
    { txt: "<code>*</code> 와 자격 증명 허용은 <b>함께 쓸 수 없어</b> 브라우저가 거부한다 — 출처가 없는 요청에서 이 조합이 나오므로, 그런 요청에는 애초에 CORS 헤더를 붙이지 않는 편이 맞다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>출처 되비추기 + 자격 증명 허용</b>은 모든 사이트에 로그인 세션을 내주는 것과 같다. (2) 허용 목록으로 좁히고 <b><code>Vary: Origin</code></b> 을 붙여 캐시가 섞이지 않게 해야 한다. (3) <code>*</code> 와 자격 증명은 함께 쓸 수 없으므로 <b>출처 없는 요청</b>에는 헤더를 붙이지 않는다.\n디스트랙터: 헤더가 조금 커지는 것은 실질적인 문제가 아니고, <code>next()</code> 호출은 올바른 구조다.",
},
{
  t: "review", k: "화면에 문자열을 심는다", cat: "review", d: 2, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function renderComment(c) {\n  const el = document.createElement(\"div\");\n  el.innerHTML = \"<b>\" + c.author + \"</b>: \" + c.body;\n  el.setAttribute(\"onclick\", \"openProfile('\" + c.author + \"')\");\n  list.appendChild(el);\n}",
  items: [
    { txt: "<code>innerHTML</code> 에 사용자 문자열을 이어 붙이면 <b>그것이 마크업으로 해석된다</b>. 댓글에 태그를 넣는 것만으로 다른 사람의 브라우저에서 우리 도메인 권한으로 코드가 돌므로, <code>textContent</code> 로 넣거나 정해진 규칙으로 정제해야 한다.", bad: true },
    { txt: "정제 규칙을 직접 만들기보다 검증된 정제 라이브러리를 쓰고 콘텐츠 보안 정책(CSP)으로 인라인 실행을 막아 방어를 겹쳐야 한다 — 다만 그것은 뚫렸을 때를 대비한 층이고, 지금 코드에서 먼저 고칠 것은 문자열을 마크업으로 만드는 조립 자체다.", bad: false },
    { txt: "요소를 하나씩 만들어 붙이므로 댓글이 많으면 화면 갱신이 여러 번 일어난다 — 조각 문서에 모아 한 번에 붙이면 빨라지지만, 보안과는 무관한 성능 이야기다.", bad: false },
    { txt: "<code>onclick</code> 속성에 문자열을 조립하면 <b>또 하나의 실행 지점</b>이 생긴다. 작은따옴표 하나만 넣어도 코드가 이어지므로, 속성 대신 <code>addEventListener</code> 로 붙이고 값은 데이터로 전달해야 한다.", bad: true },
    { txt: "<code>document.createElement</code> 로 요소를 만든 것은 문자열로 통째로 조립하는 것보다 낫다 — 구조를 코드로 표현하면 어디에 값이 들어가는지 눈에 보이기 때문이다.", bad: false },
  ],
  ex: "결함 2가지. (1) <b><code>innerHTML</code> 조립</b>은 사용자 입력을 마크업으로 만든다 — 값으로 넣을 자리는 <code>textContent</code> 다. (2) <b>인라인 이벤트 속성</b>은 문자열이 코드가 되는 또 하나의 통로다 — 리스너로 붙이고 값은 데이터 속성으로 넘긴다.\n디스트랙터: 정제 라이브러리와 CSP 는 겹쳐 두면 좋은 방어이지 이 코드에서 먼저 고칠 지점은 아니다. 렌더링 성능은 별개의 주제이고, <code>createElement</code> 를 쓴 것은 오히려 나은 부분이다.",
},
{
  t: "review", k: "권한을 화면에서만 가린다", cat: "review", d: 2, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "// 프런트엔드\nif (user.role === \"admin\") showDeleteButton();\n\n// 서버\napp.post(\"/api/posts/:id/delete\", async (req, res) => {\n  await db.posts.delete(req.params.id);\n  res.json({ ok: true });\n});",
  items: [
    { txt: "삭제를 <code>POST</code> 로 받은 것은 링크나 이미지 태그만으로는 호출되지 않게 하는 최소한의 방어가 되지만, 의미를 분명히 하려면 <code>DELETE</code> 메서드와 자원 경로로 바꿔야 한다 — 실제 보안 차이는 크지 않으므로 결함이라기보다 표현의 문제다.", bad: false },
    { txt: "서버가 <b>누구인지도 무엇을 할 자격이 있는지도 확인하지 않는다</b>. 버튼을 감추는 것은 화면의 편의일 뿐이고, 주소를 아는 사람은 누구나 직접 호출할 수 있으므로 삭제는 서버에서 막아야 한다.", bad: true },
    { txt: "요청 위조를 막는 토큰이 없으므로 쿠키 인증을 쓴다면 다른 사이트가 사용자의 브라우저로 이 요청을 보낼 수 있다 — 인증 방식에 따라 필요 여부가 갈리는 별개의 방어다.", bad: false },
    { txt: "삭제 결과를 <code>{ ok: true }</code> 로만 돌려주므로 무엇이 지워졌는지 클라이언트가 알 수 없다 — 응답 설계의 문제이지 권한과는 무관하다.", bad: false },
    { txt: "삭제 대상이 요청자의 것인지 확인하지 않으므로, 관리자 검사를 추가하더라도 <b>일반 사용자가 남의 글을 지우는</b> 경로는 그대로 남는다 — 자원마다 소유자 또는 권한을 함께 따져야 한다.", bad: true },
  ],
  ex: "결함 2가지. (1) <b>서버에 인가가 없다</b> — 화면에서 감춘 기능은 감춰졌을 뿐 막힌 것이 아니다. (2) <b>자원의 주인을 따지지 않는다</b> — 역할 검사만으로는 '관리자가 아닌 사용자가 남의 글을 지우는' 경로가 남는다.\n디스트랙터: <code>POST</code> 대 <code>DELETE</code> 는 의미의 문제이고, CSRF 토큰은 인증 방식에 따라 달라지는 별개의 방어다. 응답 형식은 권한과 무관하다.",
},
{
  t: "review", k: "코드에 박아 둔 열쇠", cat: "review", d: 2, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "const API_KEY = \"sk_live_9f21c7a44b\";\n\nasync function charge(order) {\n  console.log(\"결제 요청\", { order, key: API_KEY });\n  return psp.charge(order, API_KEY);\n}",
  items: [
    { txt: "비밀을 소스에 적으면 <b>저장소 이력에 영원히 남는다</b>. 나중에 지워도 옛 커밋에 그대로 있고, 저장소를 볼 수 있는 모든 사람과 포크·미러가 함께 갖게 되므로 환경변수나 비밀 관리 도구로 옮겨야 한다.", bad: true },
    { txt: "키를 로그에 그대로 찍는다. 로그 저장소는 대개 애플리케이션보다 접근 권한이 넓고 보관 기간도 길어서, 한 번 남으면 회수할 수 없고 <b>수집기를 거쳐 더 넓은 곳</b>으로 퍼진다.", bad: true },
    { txt: "주문 객체를 통째로 로그에 남기므로 카드 뒷자리나 주소 같은 개인정보가 함께 저장될 수 있다 — 무엇을 남길지 <b>고르는 것</b>이 기본이고, 통째로 찍는 습관이 사고를 만든다.", bad: true },
    { txt: "호출자가 다루기로 계약했다면 예외를 올려보내는 편이 낫지만, 재시도와 사용자 안내를 위해 이 자리에서 잡아 결과 객체로 바꿔야 한다.", bad: false },
    { txt: "키를 상수로 한 번만 선언해 여러 곳에서 재사용하므로 값이 갈릴 일이 없다 — 관리 지점을 하나로 모은 것 자체는 나쁘지 않은 구조다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>소스에 박힌 비밀</b>은 이력에 남아 회수할 수 없다. (2) <b>로그에 찍힌 키</b>는 훨씬 넓은 곳으로 퍼진다. (3) <b>객체를 통째로</b> 남기면 개인정보까지 함께 저장된다. 로깅 계층에서 비밀과 개인정보를 가리고, 키는 실행 환경에서 주입해야 한다.\n디스트랙터: 예외를 위로 올리는 것은 계약에 따라 옳을 수 있고, 상수로 모은 구조 자체는 문제가 아니다.",
},
{
  t: "review", k: "업로드한 파일을 그대로 받는다", cat: "review", d: 3, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "app.post(\"/upload\", async (req, res) => {\n  const f = req.files.avatar;\n  if (!f.name.endsWith(\".jpg\")) return res.status(400).end();\n  await fs.writeFile(\"./public/uploads/\" + f.name, f.data);\n  res.json({ url: \"/uploads/\" + f.name });\n});",
  items: [
    { txt: "크기 제한을 두지 않아 아주 큰 파일이 디스크를 채울 수 있으므로 요청 본문 상한과 파일 개수 제한을 함께 걸어야 한다 — 자원 고갈은 실제 위험이지만 보통 업로드 미들웨어 설정에서 다루므로, 이 조각만 보고 결함으로 단정하기는 어렵다.", bad: false },
    { txt: "이름을 그대로 경로에 이어 붙이므로 <code>../</code> 가 들어가면 <b>지정한 폴더 밖</b>에 쓸 수 있다. 파일 이름은 사용자 입력이므로 서버가 새 이름을 만들어 쓰고, 원래 이름은 표시용으로만 따로 보관해야 한다.", bad: true },
    { txt: "확장자만 보고 종류를 판단한다. <code>.jpg</code> 로 끝나는 이름은 누구나 지을 수 있으므로 <b>내용을 검사</b>해야 하고, 실행될 수 있는 위치에 저장하면 확장자 검사를 우회하는 순간 서버에서 코드가 돌 수도 있다.", bad: true },
    { txt: "정적으로 서빙되는 폴더에 그대로 저장하므로 올린 파일이 <b>우리 도메인에서 바로 열린다</b>. 스크립트가 담긴 파일이라면 같은 출처에서 실행되는 셈이라, 업로드물은 별도 도메인이나 저장소에 두는 편이 안전하다.", bad: true },
    { txt: "실패했을 때 본문 없이 400 만 돌려주므로 클라이언트가 이유를 알 수 없다 — 사용자 경험의 문제이지 보안 결함은 아니며, 오히려 자세한 이유를 숨기는 편이 나을 때도 있다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>사용자 이름을 경로에</b> 그대로 쓴다 — 서버가 이름을 정해야 한다. (2) <b>확장자만 믿는다</b> — 내용을 확인해야 한다. (3) <b>실행 가능한 위치</b>에 저장한다 — 업로드물은 같은 출처에서 열리지 않게 분리한다. 셋을 함께 고쳐야 실제로 막힌다.\n디스트랙터: 크기 제한은 보통 미들웨어에서 다루고, 오류 메시지의 상세함은 보안 관점에서 오히려 줄이는 편이 나을 때가 있다.",
},
{
  t: "review", k: "토큰을 받아들이는 방식", cat: "review", d: 4, track: "security",
  q: "이 코드의 결함을 모두 고르세요",
  code: "function auth(req) {\n  const raw = req.headers.authorization.split(\" \")[1];\n  const payload = jwt.verify(raw, PUBLIC_KEY);\n  return { id: payload.sub, role: payload.role };\n}",
  items: [
    { txt: "<code>jwt.verify</code> 는 만료 시각을 기본적으로 검사하지만 그 사실이 코드에 드러나지 않으므로, 남는 유효 시간을 직접 계산해 임계값 아래면 거절하도록 명시해야 한다 — 실제로는 라이브러리의 기본 동작에 맡겨도 되는 자리다.", bad: false },
    { txt: "반환 객체에 필요한 필드만 담아 넘기므로 토큰의 다른 클레임이 애플리케이션 곳곳으로 퍼지지 않는다 — 경계에서 모양을 좁히는 좋은 습관이다.", bad: false },
    { txt: "허용 알고리즘을 지정하지 않았다. 토큰 헤더가 알고리즘을 정하게 두면 <b>대칭 알고리즘으로 바꿔치기</b> 해 공개 키를 비밀 키처럼 쓰는 공격이 성립하므로, <code>algorithms: [\"RS256\"]</code> 처럼 못박아야 한다.", bad: true },
    { txt: "<code>authorization</code> 헤더가 없으면 <code>split</code> 에서 예외가 난다. 인증 실패를 401 로 다루지 못하고 500 으로 떨어지므로, 헤더의 존재와 <code>Bearer</code> 접두사를 <b>먼저 확인</b>해야 한다.", bad: true },
    { txt: "<code>role</code> 을 토큰에서 그대로 읽어 쓴다. 서명이 유효하더라도 그것은 <b>발급 시점의 주장</b>일 뿐이라, 권한이 회수된 뒤에도 만료 전까지 통하므로 중요한 작업에서는 서버가 현재 상태를 확인해야 한다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>알고리즘을 못박지 않았다</b> — 토큰이 검증 방식을 고르게 두면 안 된다. (2) <b>헤더가 없을 때</b>를 다루지 않아 인증 실패가 서버 오류가 된다. (3) <b>역할을 토큰의 주장 그대로</b> 믿는다 — 권한 변경이 만료 전까지 반영되지 않는다.\n디스트랙터: 만료 시각 검사는 <code>jwt.verify</code> 의 기본 동작이라 따로 코드를 쓰지 않아도 만료된 토큰은 거절되므로 결함이 아니다. 반환 객체에 필요한 필드만 담아 넘긴 것도 경계에서 모양을 좁히는 좋은 습관이며, 토큰의 다른 클레임이 애플리케이션 곳곳으로 번지는 것을 막아 준다.",
},
];
