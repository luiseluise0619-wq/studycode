module.exports = {
  track: "mobile", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 생명주기·성능·배포",
  source: "./in_mobile.cjs",
  lessons: [
    { t: "기기가 정하는 제약", n: 8,
      th: { sum: "모바일에서는 **시간·배터리·손가락**이 설계를 정한다. 그 숫자를 알면 무엇을 미루고 무엇을 키울지 저절로 정해진다.",
        body: [
          { h: "시간과 크기의 숫자", t: "60fps 를 지키려면 한 프레임에 <b>약 16ms</b> 뿐이고 120Hz 기기에서는 8ms 다 — 이미지 디코딩이나 날짜 포맷을 그리는 도중에 하면 금방 넘어간다. 터치 대상은 <b>48dp</b> 이상이어야 하고, 보이는 크기와 눌리는 크기는 달라도 된다. 목록은 <b>가상화</b>로 화면에 보이는 것만 만들되, 재사용되는 칸에 이전 내용이 남을 수 있으니 모든 필드를 매번 새로 채우고 <b>안정적인 id</b> 를 준다." },
          { h: "언제 죽고 언제 다시 나는가", t: "화면 회전·다크 모드 전환·언어 변경은 액티비티를 <b>재생성</b>하므로 화면에 들고 있던 값이 사라진다. 앱은 <b>백그라운드로 물러나는 순간</b>이 마지막 안전한 저장 지점이고, 종료 콜백은 호출이 보장되지 않는다 — 저장은 빠르고 원자적이어야 한다. 프로세스가 없는 상태에서 시작하는 <b>콜드 스타트</b>가 가장 느리고, 주범은 대개 시작 시점에 몰아 넣은 초기화다." }],
        code: { c: "1000 / 60 ≈ 16ms · 터치 대상 48dp\nonPause/background → 저장\n칸마다 안정적인 id, 자리 번호 금지", cap: "숫자를 알면 무엇을 미룰지 정해진다" },
        key: ["프레임 예산은 16ms", "터치 대상은 48dp 이상", "설정 변경은 화면을 재생성한다", "저장은 백그라운드 진입에서"] } },
    { t: "밖과 주고받는 길", n: 7,
      th: { sum: "권한·푸시·링크·배포는 전부 **시스템이 중간에 끼는** 길이다. 그래서 '내가 부르면 반드시 된다' 는 전제가 통하지 않는다.",
        body: [
          { h: "시스템이 거절할 수 있다", t: "'다시 묻지 않음' 을 고른 뒤에는 권한 창이 <b>아예 뜨지 않고</b> 즉시 거부된다 — 사용자에게는 아무 일도 안 일어나는 버튼이 되므로 설정 화면으로 보내는 안내를 띄운다. 푸시(<b>FCM</b>)는 운영체제가 연결 하나를 관리해 배터리를 아끼는 대신 <b>전달이 보장되지 않는다</b> — 알림에는 '새 것이 있다' 만 싣고 데이터는 앱이 다시 받아 온다. 미뤄도 되는 일은 <b>WorkManager</b> 에 맡겨 조건이 맞을 때 모아 실행한다." },
          { h: "끊겨도, 아껴도 돌아가게", t: "오프라인 우선은 변경 요청을 <b>큐</b>에 담고 화면은 먼저 바꾸는데, 재시도가 있으니 요청이 <b>멱등</b>해야 하고 거절됐을 때의 처리도 정해 둬야 한다. 이미지 캐시는 <b>메모리</b>와 디스크 두 단계로 두되 둘 다 상한과 버리는 규칙이 있어야 한다. 안 바뀐 자원에는 <b>304</b> 로 답해 데이터와 배터리를 아낀다. 링크는 도메인 소유를 증명하는 <b>앱 링크</b>로 두고, 배포는 <b>AAB</b>·<b>TestFlight</b> 를 쓴다." }],
        code: { c: "If-None-Match: <etag>   →  304 Not Modified\n오프라인 큐 + 멱등 키\n권한: 허용 목록으로 판정, blocked 면 설정으로", cap: "시스템이 끼면 보장이 사라진다" },
        key: ["권한은 창이 안 뜰 수 있다", "푸시 전달은 보장되지 않는다", "오프라인 큐에는 멱등이 필요하다", "안 바뀌었으면 304"] } },
  ],
};
