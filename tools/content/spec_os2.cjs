module.exports={
  track:"os", guide:"🧩", xp:80,
  unit:"실행형 실전 — 커널 알고리즘을 직접 돌린다",
  source:"./exec_os2.cjs",
  lessons:[
    { t:"페이지 교체 세 가지", n:3,
      th:{ sum:"FIFO·LRU·OPT 는 코드가 거의 같고 **누구를 내보내는지만** 다르다. 그 한 줄이 부재율을 가른다.",
        body:[
          {h:"한 줄이 알고리즘을 바꾼다", t:"부재 처리 코드는 셋이 동일하고, 갈리는 곳은 두 군데다 — 적중했을 때 순서를 갱신하는가(LRU), 그리고 어느 끝에서 내보내는가(`pop(0)` vs `pop()`). 그래서 이 부류의 버그는 코드 리뷰로 잡히지 않고 **부재율 숫자만 봐서도** 알아채기 어렵다. `1,2,1,2,1,2,3` 처럼 차이가 극대화되는 참조열을 테스트로 두는 것이 유일한 방어다."},
          {h:"하한선으로 쓰는 알고리즘", t:"OPT 는 미래를 알아야 해서 구현할 수 없지만, **어떤 알고리즘도 그보다 잘할 수 없다**는 하한선을 준다 — 실제 트레이스로 OPT 를 오프라인 계산하면 '개선 여지가 얼마인지' 를 알 수 있다. 구현의 핵심은 '다시 쓰이지 않는 페이지' 에 가장 먼 거리를 주는 것이고, 부호를 뒤집으면 **곧 쓸 페이지를 골라 버리는 최악의 알고리즘**이 된다."}],
        code:{ c:"if p in used:\n    used.remove(p); used.append(p)   # 이 두 줄이 LRU 를 만든다\n    continue", cap:"빼먹으면 이름만 LRU 인 FIFO" },
        key:["적중 시 순서 갱신이 LRU 의 전부","pop(0) 과 pop() 은 다른 알고리즘","OPT 는 개선 여지를 재는 하한선","부호 실수는 그럴듯한 숫자를 낸다"] } },
    { t:"이상 현상과 CPU 스케줄링", n:3,
      th:{ sum:"자원을 늘렸는데 나빠지는 경우가 있고, CPU 를 나누는 두 방식은 **평균과 최악** 사이에서 갈린다.",
        body:[
          {h:"늘렸는데 나빠진다", t:"FIFO 에는 **프레임을 늘렸는데 부재가 늘어나는** Belady 이상 현상이 있다(3프레임 9번 → 4프레임 10번). LRU·OPT 같은 스택 알고리즘에는 없다. 시스템에서 같은 모양이 반복된다 — 스레드를 늘렸는데 처리량이 떨어지고(락 경쟁), 캐시를 키웠는데 느려지고, 배치를 늘렸는데 지연이 커진다. 교훈은 하나다: **늘린 뒤에 반드시 다시 측정한다.**"},
          {h:"짧은 것 먼저 vs 돌아가며", t:"SJF 는 평균 대기시간을 최소화하는 것이 증명되어 있지만 실행시간을 미리 알 수 없고 **기아**를 만든다 — 실제 스케줄러는 과거 패턴으로 추정하고 aging 을 더한다. 라운드 로빈은 응답성을 위해 처리량을 내준다. 두 시뮬레이션 모두 **시계를 정확히 움직이는 것**이 어렵다: 유휴 구간에서 시간을 점프시키지 않거나 남은 시간보다 많이 쓰면 뒤의 모든 시각이 조용히 어긋난다."}],
        code:{ c:"run = min(q, b)   # 남은 만큼만 돈다\n# q 를 그대로 쓰면 시계가 실제보다 빨라진다", cap:"완료 시각까지 테스트로 못 박는다" },
        key:["FIFO 는 프레임을 늘려도 나빠질 수 있다","늘린 뒤에 다시 측정한다","SJF 는 최적이지만 기아를 만든다","유휴 구간에서 시계를 점프시킨다"] } },
    { t:"데드락·배치·주소 변환", n:3,
      th:{ sum:"나눌 수 없는 자원이 만드는 교착, 공간을 고르는 전략, 그리고 주소를 옮기는 계산.",
        body:[
          {h:"영원히 멈춘 스레드", t:"데드락 탐지는 대기 그래프의 순환 찾기지만, 실무의 질문은 '**어떤 스레드가 영원히 멈춰 있는가**' 이고 여기에는 순환에 이르는 스레드도 포함된다. '기다릴 대상이 남지 않은 스레드는 진행할 수 있다' 를 반복해 벗겨 내면 남는 것이 정확히 답이다. 2-순환만 찾는 도구는 정작 리뷰를 통과해 운영에서 터지는 **3개 이상의 얽힘**을 놓친다."},
          {h:"어디에 넣고 어떻게 옮기는가", t:"배치 전략(최초·최적·최악 적합)은 각각 다른 것을 아끼며 어느 것도 항상 이기지 않는다 — 결과가 '유효한 인덱스' 라서 **테스트 없이는 어느 전략인지 알 수 없다**. 주소 변환은 '상위 비트는 페이지 번호, 하위 비트는 오프셋' 이 전부이고 **오프셋은 변환되지 않고 그대로 통과**한다. 몫과 나머지를 바꿔 쓰면 항상 페이지 폴트가 나는데 결과가 여전히 숫자라 원인을 찾기 어렵다."}],
        code:{ c:"# 락을 언제나 같은 전역 순서로 잡으면 순환이 생길 수 없다\nwith locks[min(a, b)], locks[max(a, b)]:", cap:"탐지는 마지막 수단 — 이미 멈춘 뒤에 알려 준다" },
        key:["멈춘 스레드에는 순환에 이르는 것도 포함","데드락은 순환 대기를 깨는 것이 가장 쉽다","배치 전략은 결과만 봐서 구별할 수 없다","오프셋은 변환 없이 통과한다"] } },
    { t:"디스크·파일시스템·버퍼", n:3,
      th:{ sum:"저장 계층의 셋 — **헤드를 얼마나 움직이고, 블록을 얼마나 쓰고, 큐가 언제 가득 찬 것인가**.",
        body:[
          {h:"움직인 거리와 차지한 블록", t:"SSTF 는 고전적 요청열에서 FCFS 의 2.7배 빠르지만 멀리 있는 요청을 기아 상태로 만든다 — 그래서 실무는 최대 대기에 상한이 있는 SCAN 계열을 쓰고, SSD 에서는 물리 헤드가 없어 이 논의 자체가 무의미해진다(**알고리즘의 가치는 하드웨어 가정이 유효할 때까지**). 아이노드는 직접·1단·2단 간접으로 '작은 파일은 싸게, 큰 파일도 가능하게' 만들지만 문턱에서 **비용이 계단처럼 뛰고** 접근 지연이 구간마다 달라진다."},
          {h:"빈 것과 가득 찬 것", t:"원형 버퍼에서 `head == tail` 은 '비었다' 와 '가득 찼다' 를 동시에 뜻한다. 해법은 셋뿐이다 — 개수를 따로 세기, 한 칸을 비워 두기, 순번을 감싸지 않고 계속 늘리기. 구별하지 못하면 **가득 찬 상태에서 조용히 덮어써** 데이터가 사라지는데 아무 오류도 나지 않는다. 그리고 감싸기(wraparound) 경로에서만 나타나는 버그는 짧은 테스트로 잡히지 않는다."}],
        code:{ c:"if count == cap:\n    return False      # 가득 찼다 — 덮어쓰지 않는다\nif count == 0:\n    return None       # 비었다", cap:"head == tail 만으로는 구별할 수 없다" },
        key:["SSTF 는 빠르지만 기아를 만든다","간접 블록도 디스크를 차지한다","원형 버퍼는 개수를 따로 센다","감싸기 경로를 테스트에 넣는다"] } },
  ]
};
