module.exports = {
  track: "ai", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 파이프라인의 용어와 숫자",
  source: "./in_ai.cjs",
  lessons: [
    { t: "찾아서 붙이기까지", n: 8,
      th: { sum: "RAG 는 **세는 단위**, **견주는 척도**, **자르는 방법**, **다시 줄 세우기** 네 걸음이다. 답이 나쁘면 생성이 아니라 이쪽부터 본다.",
        body: [
          { h: "세고 견주기", t: "컨텍스트는 글자가 아니라 <b>토큰</b>으로 세고 영어는 대략 4 글자에 하나지만, 한국어와 전문 용어는 잘게 쪼개져 더 많이 든다 — 길이 제한은 반드시 토크나이저로 확인한다. 임베딩은 <b>코사인</b>(각도)으로 견주므로 저장할 때 길이를 1 로 맞춰 두면 내적이 곧 유사도가 된다. 수백만 개를 다 견줄 수는 없어 <b>ANN</b> 으로 후보를 좁히고, 그 손잡이가 재현율과 속도를 맞바꾼다." },
          { h: "자르고 다시 줄 세우기", t: "청킹의 <b>오버랩</b>은 경계에서 뜻이 반으로 나뉘는 것을 막지만 크게 잡으면 결과가 중복된다 — 대개 조각 크기의 10~20% 이고, 문장 경계를 존중하면 더 작아도 된다. 1 차 검색 뒤 <b>리랭커</b>로 다시 줄 세우면 값싸게 품질이 오른다(상위 50 → 5). 근거 없는 내용을 지어내는 <b>환각</b>은 근거를 붙이고 '없으면 모른다고 답하라' 를 못 박으면 크게 줄지만 0 은 아니다." }],
        code: { c: "저장 시 정규화 → 내적 = 코사인\nrerank(top50) → top5\n인용한 문장이 근거에 실제로 있는지 자동 검사", cap: "답이 나쁘면 찾아온 것부터 본다" },
        key: ["길이는 토크나이저로 센다", "코사인은 각도만 본다", "오버랩은 조각의 10~20%", "리랭커가 값싼 한 수"] } },
    { t: "생성을 조절하고 평가하기", n: 7,
      th: { sum: "생성은 **손잡이 몇 개**로 조절하고, 평가는 **사람이 매긴 표본과 얼마나 맞는지**로 검증한다.",
        body: [
          { h: "손잡이와 프롬프트", t: "<b>temperature</b> 는 확률 분포를 날카롭게/평평하게 만들고, 0 이면 <b>결정적</b>이라 평가와 회귀 테스트에 알맞다(대신 나쁜 답도 언제나 똑같이 나쁘다). <b>few-shot</b> 예시는 형식을 잡는 데 특히 효과적이지만 라벨이 쏠린 예시는 편향을 심는다. <b>사고 사슬</b>은 여러 단계 추론에서 정확도를 올리는 대신 비용과 지연을 늘린다 — 쉬운 작업에서는 이득이 없다. <b>시스템 프롬프트</b>는 규칙을 두기 좋은 자리지만 절대적 방어벽은 아니다." },
          { h: "비용과 채점", t: "<b>KV 캐시</b> 덕분에 토큰을 하나 더 만드는 비용이 제곱이 아니라 선형이지만, 그 메모리가 동시 사용자 수의 상한을 정한다 — 시스템 프롬프트가 길면 앞부분을 미리 계산해 공유할 수 있다. 정답이 하나가 아닌 생성물은 <b>모델이 채점</b>하게 하면 매번 돌릴 수 있어 값어치가 크지만, 긴 답·자기 답·먼저 나온 답을 선호하는 편향이 있다 — 사람 표본과의 <b>일치율</b>을 먼저 확인하고 기준을 항목으로 쪼갠다." }],
        code: { c: "temperature=0        # 평가·회귀 테스트\nprefix caching       # 긴 시스템 프롬프트 공유\n채점 기준을 항목으로 쪼개고 일치율을 잰다", cap: "조절은 손잡이로, 검증은 표본으로" },
        key: ["평가는 temperature 0 으로", "예시는 형식을 잡는다", "KV 캐시가 동시성 상한을 정한다", "채점기는 사람 표본으로 검증한다"] } },
  ],
};
