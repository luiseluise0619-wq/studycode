module.exports = {
  track: "stat", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 숫자는 나오는데 뜻이 틀린 코드 고치기",
  source: "./dbg_stat.cjs",
  lessons: [
    { t: "분모와 경계를 잘못 잡은 곳", n: 6,
      th: { sum: "통계 코드의 버그는 **오류를 내지 않는다** — 그럴듯한 숫자가 나와서 대시보드와 판단까지 함께 틀린다.",
        body: [
          { h: "무엇으로 나누는가", t: "평균의 흩어짐은 표준편차가 아니라 **표준오차**(`sd/√n`) 다 — 이 나눗셈이 빠지면 표본을 4배로 늘려도 신뢰구간이 안 좁아진다. 가중평균의 분모는 **가중치의 합**이지 개수가 아니고, 표준편차가 0 인 상수 열은 나누기 전에 걸러야 한다. 셋 다 균등하거나 정상적인 입력에서는 우연히 맞는다." },
          { h: "가장자리를 어떻게 하는가", t: "백분위수의 순위는 1 부터, 배열 자리는 0 부터 센다. 이동평균은 창이 **채워지기 전** 값을 내면 안 되고, 다중 비교 보정은 순위별 문턱을 써야 진짜 효과를 놓치지 않는다. '가장자리 규칙' 은 취향이 아니라 계약이라 문서와 테스트에 적어야 한다." }],
        code: { c: "return z * sd / Math.sqrt(n);      // 평균의 흩어짐\nconst rank = Math.max(1, Math.ceil(p / 100 * n));\nreturn s[rank - 1];", cap: "분모와 첫 자리를 먼저 확인한다" },
        key: ["표본 4배 → 구간 절반인지 검산", "가중평균 분모는 Σw", "순위는 1부터, 자리는 0부터", "창이 찬 뒤부터 값을 낸다"] } },
    { t: "극단값과 합계가 흔들리는 곳", n: 6,
      th: { sum: "'평균으로 극단값을 찾기' 와 '각자 반올림한 뒤 합치기' 는 **구조적으로** 틀린다.",
        body: [
          { h: "찾으려는 것에 끌려간다", t: "평균과 표준편차는 이상치 자신에게 끌려가므로 '평균 ± 3σ' 는 큰 이상치를 품어 버린다. 순위 기반(중앙값·사분위수)이 답이고, MAD 는 **기준도 흩어짐도 모두** 중앙값이어야 강건하다. 지연 시간처럼 꼬리가 긴 자료에 평균을 쓰면 거의 언제나 잘못된 결론이 나온다." },
          { h: "합계는 저절로 보존되지 않는다", t: "각자 반올림한 백분율은 99% 나 101% 가 된다 — 내림한 뒤 **소수부가 큰 순서로** 남은 몫을 나눠야 100 이 보장된다. 비율도 마찬가지라, 집단별 비율의 평균이 아니라 **분자끼리·분모끼리 더해** 한 번만 나눠야 표본 1 개짜리 집단이 결과를 흔들지 않는다." }],
        code: { c: "const out = raw.map(v => Math.floor(v));\n// 남은 몫을 소수부 큰 순서로 1씩\nreturn okSum / nSum;   // 비율은 풀링", cap: "합계 보존을 성질 테스트로" },
        key: ["이상치는 순위 기반으로 찾는다", "MAD 는 두 번 다 중앙값", "백분율 합은 100 으로 맞춘다", "비율은 풀링해서 낸다"] } },
  ],
};
