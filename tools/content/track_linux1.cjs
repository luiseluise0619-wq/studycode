/* Linux 트랙 전반부 — 유닛 1~6.
   셸·파일시스템·권한·프로세스·텍스트 도구·파이프.
   _p 가 붙은 문항은 임시 폴더에서 실제 셸을 돌려 마지막 줄을 대조한다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex,p)=>{ const o={t:"input",k,q,a,ex}; if(p) o._p=p; return o; };
const P=(sh,want)=>({sh,want});

const UNITS=[

/* ═══════════════ 유닛 1 ═══════════════ */
{ t:"셸 — 명령을 주고받는 창구", l:[

  { t:"셸이 하는 일과 하지 않는 일", xp:15,
    th:{ sum:"셸은 **명령을 해석해 프로그램을 대신 실행해 주는 프로그램**이다. `ls` 는 셸의 기능이 아니라 셸이 찾아서 실행해 주는 별도의 프로그램이다.",
      body:[
        {h:"셸이 먼저 손대고 넘긴다", t:"`ls *.txt` 를 치면 `ls` 는 `*.txt` 를 본 적이 없다. **셸이 먼저 `*` 를 파일 이름 목록으로 펼쳐** `ls a.txt b.txt` 로 바꿔 실행한다. 따옴표·변수·`~`·`$(…)` 도 전부 셸이 처리한 뒤 넘긴다. 이 순서를 모르면 '왜 따옴표를 씌우면 동작이 달라지지' 를 영영 이해할 수 없다."},
        {h:"어디서 찾아 실행하는가", t:"명령 이름만 치면 셸은 **`PATH` 환경 변수에 적힌 폴더들을 앞에서부터** 뒤져 같은 이름의 실행 파일을 찾는다. 그래서 같은 이름이 여러 곳에 있으면 앞선 폴더가 이긴다. `which`·`type` 으로 무엇이 실제로 실행되는지 확인할 수 있고, 현재 폴더(`.`)가 `PATH` 에 없는 것은 **보안상 의도된 것**이다."}],
      code:{ c:"echo $PATH            # 어디를 뒤지는지\ntype -a python        # 무엇이 실행되는지 (여러 개면 전부)\nls *.txt              # * 를 펼치는 것은 ls 가 아니라 셸이다", cap:"셸이 먼저 손대고 프로그램에 넘긴다" },
      key:["셸은 해석하고 프로그램은 따로 있다","* · 변수 · ~ 는 셸이 먼저 펼친다","PATH 를 앞에서부터 뒤진다","현재 폴더가 PATH 에 없는 건 의도"] },
    q:[
      I("셸 이름","지금 쓰고 있는 셸의 경로가 담긴 환경 변수는? (달러 기호 포함)",
        ["$SHELL","SHELL"],
        "정답은 `$SHELL` 입니다. `echo $SHELL` 로 확인합니다.\n다만 이것은 **로그인 셸**이라 지금 실행 중인 셸과 다를 수 있습니다. 실제로 도는 셸은 `ps -p $$` 로 확인하는 편이 정확합니다."),
      C("역할","<code>ls</code> 명령의 정체로 옳은 것은?",
        ["셸 안에 내장되어 있는 기능이라서 따로 실행 파일이 존재하지 않는다","/usr/bin 등에 있는 별도의 실행 파일이다",
         "커널이 직접 처리해 주는 시스템 호출의 한 종류다","셸이 화면에 글자를 그리는 함수다"],1,
        "`ls` 는 **독립된 프로그램**입니다. `type -a ls` 로 위치를 확인할 수 있습니다.\n반대로 `cd` 는 정말 셸 내장입니다 — 별도 프로그램이 폴더를 옮기면 그 프로그램만 옮겨지고 셸은 제자리이기 때문입니다.\n이 구분을 알면 '왜 어떤 명령은 `man` 이 있고 어떤 건 `help` 로 봐야 하는지' 도 함께 풀립니다."),
      C("와일드카드","<code>ls *.txt</code> 에서 <code>*</code> 를 실제로 펼치는 것은?",
        ["ls 프로그램이 인자를 받은 다음 스스로 파일 목록으로 펼친다","셸이 먼저 펼쳐서 파일 이름들을 ls 에 넘긴다",
         "커널이 파일 시스템에서 펼쳐 준다","아무도 펼치지 않아 별표가 문자 그대로 전달된다"],1,
        "**셸이 먼저** 펼칩니다(glob). `ls` 는 이미 펼쳐진 이름 목록만 받습니다.\n그래서 맞는 파일이 하나도 없으면 bash 는 `*.txt` 를 **글자 그대로** 넘겨 버려 이상한 오류가 납니다.\n펼치지 않고 그대로 넘기려면 `ls '*.txt'` 처럼 따옴표로 감쌉니다 — 따옴표는 '셸아, 손대지 마라' 는 뜻입니다."),
      I("실행 파일 위치","명령이 실제로 어느 파일에서 실행되는지 보는 명령은? (명령어만, 대상은 제외)",
        ["which","type","command -v"],
        "정답은 `which`(또는 `type`, `command -v`)입니다.\n`type -a` 가 가장 정확합니다 — 별칭·함수·내장 명령까지 구분해 보여 주고, 같은 이름이 여러 곳에 있으면 **전부** 나열해 어느 것이 이기는지 알려 줍니다.",
        P("cd /tmp && which ls","/usr/bin/ls")),
      C("PATH","<code>PATH</code> 에 같은 이름의 프로그램이 두 폴더에 있으면?",
        ["오류가 나서 실행되지 않는다","PATH 에 먼저 적힌 폴더의 것이 실행된다",
         "PATH 목록에서 더 나중에 적혀 있는 폴더의 것이 실행된다","수정 시각이 더 새로운 쪽의 파일이 실행된다"],1,
        "**앞에서부터** 찾다가 처음 만난 것을 실행합니다. 그래서 `PATH` 앞쪽에 폴더를 추가하면 시스템 명령을 덮어쓸 수 있습니다.\n이 성질이 개발 도구 버전 관리(`pyenv`·`nvm`)의 원리이자, 동시에 **공격 경로**이기도 합니다 — 쓰기 가능한 폴더가 `PATH` 앞에 있으면 남이 `ls` 를 심을 수 있습니다.\n그래서 현재 폴더 `.` 를 `PATH` 에 넣지 않는 것이 관례입니다."),
      C("따옴표","<code>echo \"$HOME\"</code> 와 <code>echo '$HOME'</code> 의 차이는?",
        ["따옴표의 종류와 상관없이 홈 경로가 그대로 출력된다","큰따옴표는 변수를 펼치고 작은따옴표는 글자 그대로 둔다",
         "작은따옴표만 변수를 펼쳐 주기 때문에 홈 경로는 그쪽에서만 나온다","작은따옴표로 감싸면 문법 오류가 난다"],1,
        "**큰따옴표는 변수와 `$(…)` 를 펼치고, 작은따옴표는 아무것도 펼치지 않습니다.**\n경로에 공백이 들어갈 수 있는 변수는 항상 `\"$변수\"` 로 감싸야 합니다 — 감싸지 않으면 공백에서 잘려 인자가 여러 개가 됩니다.\n정규식이나 `$` 가 든 문자열을 그대로 넘길 때는 작은따옴표가 안전합니다."),
      C("cd 가 내장인 이유","<code>cd</code> 가 별도 프로그램이 아니라 셸 내장인 이유는?",
        ["너무 자주 쓰이는 명령이라서 프로세스를 띄우는 시간을 아끼기 위해서","별도 프로그램이 폴더를 옮기면 그 프로그램만 옮겨지고 셸은 그대로여서",
         "커널 권한이 필요해서","아주 오래전부터 그렇게 해 온 역사적인 관례일 뿐 기술적인 이유는 따로 없다"],1,
        "현재 작업 디렉터리는 **프로세스마다** 갖는 상태입니다. 자식 프로세스가 자기 폴더를 옮기고 끝나면 부모인 셸에는 아무 영향이 없습니다.\n같은 이유로 `export`·`source`·`alias` 도 내장이어야 합니다 — 셸 자신의 상태를 바꿔야 하기 때문입니다.\n'스크립트를 실행했는데 환경 변수가 안 바뀐다' 는 질문의 답도 여기 있습니다. `./script.sh` 대신 `source script.sh` 를 써야 합니다."),
      C("탭 완성","셸에서 탭 키를 누르는 습관이 중요한 실무적 이유는?",
        ["타자가 빨라져서","경로 오타로 엉뚱한 대상에 명령이 실행되는 사고를 줄여서",
         "입력한 명령이 기록으로 남아서 나중에 다시 불러 쓸 수 있기 때문에","입력한 대상에 접근할 권한이 있는지를 미리 확인해 주기 때문에"],1,
        "속도보다 **정확성**이 요점입니다. 탭이 완성되지 않으면 그 경로는 존재하지 않는다는 뜻이라, 치기 전에 알 수 있습니다.\n`rm -rf` 같은 명령에서 경로 오타 한 글자의 대가는 큽니다.\n같은 맥락에서 위험한 명령은 먼저 `ls` 로 대상을 확인하고, 그 줄을 화살표 위로 불러와 `ls` 만 `rm` 으로 바꾸는 습관이 널리 쓰입니다.")
    ] },

  { t:"파일 시스템 — 전부가 하나의 나무다", xp:15,
    th:{ sum:"리눅스에는 드라이브 문자가 없다. **하나의 뿌리 `/` 아래** 모든 것이 매달리고, 디스크·USB·네트워크 저장소도 그 나무의 한 가지로 '붙는다(mount)'.",
      body:[
        {h:"경로 두 가지", t:"`/` 로 시작하면 **절대 경로**, 아니면 **현재 폴더 기준 상대 경로**다. `.` 는 현재, `..` 는 부모, `~` 는 내 홈이다. 스크립트에서는 상대 경로가 위험하다 — 어디서 실행되느냐에 따라 대상이 달라지기 때문이다. 그래서 스크립트는 대개 맨 앞에서 자기 위치를 구해 절대 경로로 바꾼다."},
        {h:"어디에 무엇이 있나", t:"`/etc` 는 설정, `/var` 는 변하는 것(로그·큐), `/tmp` 는 재부팅하면 사라지는 임시, `/home` 은 사용자, `/usr/bin` 은 프로그램, `/proc` 은 **파일처럼 보이는 커널 정보**다. 이 배치는 관례가 아니라 표준(FHS)이라, 배포판이 달라도 대체로 같은 자리에 있다."}],
      code:{ c:"pwd                 # 지금 어디\ncd ~/projects       # 홈 아래로\nls -la /etc | head  # 설정 파일들\ncat /proc/loadavg   # 파일처럼 보이지만 커널이 만들어 주는 값", cap:"모든 것이 하나의 나무에 매달린다" },
      key:["뿌리는 하나, 드라이브 문자가 없다","/etc 설정 · /var 변하는 것 · /tmp 임시","/proc 은 커널이 만드는 가짜 파일","스크립트에서 상대 경로는 위험하다"] },
    q:[
      I("홈으로","아무 인자 없이 홈 디렉터리로 이동하는 명령은?",
        ["cd","cd ~","cd $HOME"],
        "정답은 `cd` 입니다. 인자를 주지 않으면 홈으로 갑니다.\n`cd -` 는 직전 폴더로 돌아가는데, 두 곳을 오갈 때 아주 편합니다. 두 명령 모두 손에 익혀 두면 경로를 다시 치는 일이 크게 줄어듭니다."),
      C("뿌리","리눅스 파일 시스템의 최상위 경로는?",
        ["/root","/","/home (사용자 홈들이 모이는 곳)","C:\\"],1,
        "최상위는 `/` 하나뿐입니다. `/root` 는 root 사용자의 **홈 디렉터리**로 완전히 다른 것입니다.\n디스크를 추가해도 새 드라이브 문자가 생기지 않고, `/mnt/data` 같은 **폴더에 붙습니다(mount)**.\n덕분에 프로그램은 저장 장치가 무엇인지 신경 쓰지 않고 경로만 다루면 됩니다."),
      C("설정 위치","시스템과 서비스의 설정 파일이 모이는 표준 위치는?",
        ["/var","/etc","/opt","/proc (커널이 만드는 가상 파일)"],1,
        "`/etc` 입니다. nginx·ssh·cron 설정이 전부 여기 아래에 있습니다.\n서버를 옮길 때 **무엇을 백업해야 하는가**의 답이 대체로 `/etc` 와 `/var/lib` 입니다.\n`/var` 는 로그·큐·DB 데이터처럼 **계속 변하는 것**이 들어갑니다."),
      C("proc","<code>/proc</code> 아래 파일들의 특징은?",
        ["부팅하는 시점에 한 번 만들어져서 디스크에 그대로 저장된다","디스크에 없고 커널이 읽는 순간 만들어 준다",
         "루트 사용자만 읽을 수 있게 보호된 백업 파일이다","프로세스가 남긴 로그 파일이다"],1,
        "`/proc` 은 **가상 파일 시스템**입니다. 디스크를 차지하지 않고, 읽는 순간 커널이 값을 만들어 줍니다.\n`/proc/cpuinfo`·`/proc/meminfo`·`/proc/<PID>/status` 처럼 시스템 상태를 **파일 읽기로** 얻을 수 있어, 특별한 API 없이 `cat` 만으로 관찰이 됩니다.\n'모든 것은 파일' 이라는 유닉스 철학이 가장 잘 드러나는 자리입니다."),
      I("현재 위치","지금 있는 폴더의 절대 경로를 출력하는 명령은?",
        ["pwd"],
        "정답은 `pwd`(print working directory)입니다.\n스크립트 안에서는 `pwd` 가 **실행한 사람이 어디 있었는지**에 따라 달라지므로, 스크립트 자신의 위치가 필요하면 `dirname \"$0\"` 이나 `${BASH_SOURCE[0]}` 를 씁니다.",
        P("mkdir -p /tmp/crx && cd /tmp/crx && pwd","/tmp/crx")),
      C("숨김 파일","리눅스에서 숨김 파일로 취급되는 것은?",
        ["파일 속성에 hidden 이 설정되어 있는 파일","이름이 점(.)으로 시작하는 파일","시스템이 정한 /hidden 폴더 안에 들어 있는 파일","실행 권한이 없는 파일"],1,
        "**이름이 점으로 시작하면** 숨김입니다. 별도 속성이 아니라 이름 규칙일 뿐이라, `ls -a` 로 전부 볼 수 있습니다.\n그래서 `.git`·`.env`·`.bashrc` 가 목록에 보이지 않습니다 — 없는 것이 아니라 안 보여 주는 것입니다.\n`ls *` 에도 걸리지 않으므로, 복사·삭제 스크립트가 숨김 파일을 조용히 빠뜨리는 사고가 흔합니다."),
      C("상대 경로","스크립트에서 상대 경로가 위험한 이유는?",
        ["느려서","실행한 위치에 따라 대상이 달라지기 때문","실행하는 계정에 따라 권한 문제가 생겨서","경로 문자열이 시스템의 길이 제한에 걸릴 수 있기 때문에"],1,
        "상대 경로는 **현재 작업 디렉터리 기준**입니다. cron 이나 systemd 가 실행하면 그 위치가 예상과 다릅니다.\n'내 터미널에서는 되는데 cron 에서는 안 된다' 의 가장 흔한 원인이 이것과 `PATH` 차이입니다.\n스크립트 맨 앞에서 `cd \"$(dirname \"$0\")\"` 로 기준을 못 박거나 절대 경로를 쓰는 것이 정석입니다."),
      C("확장자","리눅스에서 파일 확장자의 의미는?",
        ["커널이 파일 확장자를 보고 어떤 형식의 파일인지를 스스로 판단하기 때문에","사람과 응용 프로그램의 관례일 뿐 커널은 신경 쓰지 않는다",
         "확장자가 제대로 붙어 있지 않으면 아예 실행할 수가 없다","확장자가 권한을 결정한다"],1,
        "커널은 확장자를 보지 않습니다. **실행 권한이 있고 형식이 맞으면** 이름이 무엇이든 실행됩니다.\n파일 종류를 알고 싶으면 `file 파일명` 으로 **내용을 보고** 판단하게 합니다.\n이 성질 때문에 `.jpg` 로 위장한 실행 파일이 가능해, 업로드 처리에서 확장자만 믿으면 안 됩니다.")
    ] }
]},

/* ═══════════════ 유닛 2 ═══════════════ */
{ t:"파일 다루기 — 만들고, 찾고, 옮긴다", l:[

  { t:"보고 만들고 옮기기", xp:20,
    th:{ sum:"파일 명령은 몇 개 안 되지만, **되돌릴 수 없는 것**이 섞여 있다. 어떤 명령이 조용히 덮어쓰는지를 아는 것이 실력이다.",
      body:[
        {h:"덮어쓰기는 묻지 않는다", t:"`cp a b` 와 `mv a b` 는 `b` 가 이미 있어도 **아무 말 없이 덮어쓴다**. `-i` 를 붙이면 물어보고, `-n` 을 붙이면 건너뛴다. `>` 리다이렉션도 마찬가지라 `cmd > 있는파일` 은 즉시 비운다. 위험한 자리에서는 `>>`(덧붙이기)나 `set -o noclobber` 를 쓴다."},
        {h:"보는 명령도 골라 써야 한다", t:"`cat` 은 전체를 쏟아 내므로 큰 파일에 쓰면 터미널이 마비된다. `less` 는 필요한 만큼만 읽어 **수 GB 로그도 즉시 열린다**. `head`·`tail` 은 앞뒤만 보고, `tail -f` 는 새 줄이 붙는 대로 따라간다 — 서버에서 가장 많이 쓰는 조합이다."}],
      code:{ c:"cp -i a.txt b.txt      # 덮어쓸 때 물어본다\nmv old new             # 이름 바꾸기도 mv\nless big.log           # 큰 파일은 less\ntail -f /var/log/app.log   # 새로 붙는 줄을 따라간다", cap:"큰 파일에 cat 을 쓰지 않는다" },
      key:["cp·mv 는 묻지 않고 덮어쓴다","> 는 즉시 비우고 >> 는 덧붙인다","큰 파일은 less, 실시간은 tail -f","이름 바꾸기도 mv 다"] },
    q:[
      I("파일 종류","확장자가 아니라 내용을 보고 파일 종류를 알려 주는 명령은?",
        ["file"],
        "정답은 `file` 입니다. 파일 앞부분의 시그니처를 읽어 판단합니다.\n리눅스에서 확장자는 관례일 뿐이라, 업로드된 파일의 종류를 확장자로만 믿으면 위험합니다. `.jpg` 로 위장한 실행 파일이 실제로 쓰이는 공격 수법입니다."),
      C("덮어쓰기","<code>cp a.txt b.txt</code> 에서 <code>b.txt</code> 가 이미 있으면?",
        ["덮어쓸 수 없다는 오류가 나고 중단된다","확인을 묻는다","아무 말 없이 덮어쓴다","덮어쓰기 전에 자동으로 백업을 만든다"],2,
        "**조용히 덮어씁니다.** 유닉스 도구의 기본 태도는 '시킨 대로 한다' 입니다.\n`-i` 를 붙이면 묻고, `-n` 이면 건너뛰고, `--backup` 이면 사본을 남깁니다.\n대화형에서는 `alias cp='cp -i'` 를 걸어 두는 사람이 많지만, **스크립트에서는 별칭이 적용되지 않는다**는 점을 함께 알아야 합니다."),
      C("리다이렉션","<code>echo hi &gt; log.txt</code> 를 이미 내용이 있는 파일에 실행하면?",
        ["기존에 있던 내용은 그대로 두고 맨 끝에 덧붙여 기록된다","기존 내용이 지워지고 hi 만 남는다","오류가 난다","기존에 있던 내용의 맨 앞줄에 삽입된다"],1,
        "`>` 는 **파일을 비우고** 씁니다. 덧붙이려면 `>>` 입니다.\n로그 파일에 실수로 `>` 를 쓰면 그 순간 역사가 사라집니다. `set -o noclobber` 를 켜면 기존 파일에 `>` 를 쓸 때 막아 줍니다.\n비우는 것이 목적이라면 `: > file` 이나 `truncate -s 0 file` 이 의도가 분명합니다."),
      I("이름 바꾸기","파일 이름을 바꾸는 명령은? (명령어만)",
        ["mv"],
        "정답은 `mv` 입니다. 리눅스에는 rename 이 기본 명령으로 없고, **옮기기와 이름 바꾸기가 같은 일**입니다.\n파일 이름은 디렉터리 안의 항목일 뿐이라, 같은 디렉터리 안에서 항목 이름만 바꾸면 '이름 바꾸기' 가 됩니다.",
        P("cd /tmp && rm -rf crx2 && mkdir crx2 && cd crx2 && touch a && mv a b && ls","b")),
      C("큰 파일","10GB 로그 파일의 마지막 부분만 보려면?",
        ["cat big.log","tail -n 100 big.log","less big.log 로 열어 끝까지 스크롤한다","grep . big.log 로 내용을 훑는다"],1,
        "`tail -n 100` 은 **끝부분만** 읽습니다. `cat` 은 전체를 출력해 터미널과 시간을 낭비합니다.\n실시간으로 따라가려면 `tail -f`, 파일이 회전(rotate)되어도 따라가려면 `tail -F` 입니다.\n앞부분은 `head`, 자유롭게 훑으려면 `less` 입니다 — `less` 도 필요한 만큼만 읽습니다."),
      C("폴더 통째로","폴더를 하위 내용까지 복사하려면?",
        ["cp dir1 dir2","cp -r dir1 dir2","mv -r dir1 dir2 로 옮긴다","cat dir1 > dir2"],1,
        "`-r`(재귀)이 필요합니다. 없이 폴더를 복사하려 하면 '디렉터리는 생략한다' 는 안내와 함께 아무 일도 하지 않습니다.\n권한·타임스탬프까지 그대로 보존하려면 `cp -a` 를 씁니다 — 백업 용도에서는 이쪽이 정석입니다.\n원격 동기화에는 `rsync -a` 가 훨씬 낫습니다 — 바뀐 부분만 보내고 중단돼도 이어서 할 수 있습니다."),
      C("빈 파일","존재하지 않는 파일을 빈 상태로 만드는 가장 간단한 명령은?",
        ["mkdir a.txt 로 만든다","touch a.txt","echo a.txt 로 이름만 출력한다","cat a.txt"],1,
        "`touch` 는 파일이 없으면 만들고, 있으면 **수정 시각만 갱신**합니다.\n두 번째 용도가 실무에서 더 자주 쓰입니다 — 빌드 도구에게 '이 파일이 바뀌었다' 고 알려 재빌드를 유도할 때 씁니다.\n`.gitkeep` 같은 빈 표시 파일을 만들 때도 이 명령입니다."),
      C("링크","<code>ln -s</code> 로 만든 심볼릭 링크의 특징은?",
        ["원본 파일을 지우는 순간 링크도 함께 같이 사라진다","원본을 지우면 링크는 남지만 깨진 링크가 된다",
         "원본과 내용을 각각 따로 보관한다","서로 다른 파일 시스템 사이를 넘어서는 만들 수 없다"],1,
        "심볼릭 링크는 **경로를 적어 둔 작은 파일**입니다. 원본이 사라지면 가리킬 곳이 없어 깨진 링크가 됩니다.\n하드 링크는 반대로 같은 실체를 가리켜 원본을 지워도 남지만, 파일 시스템을 넘을 수 없습니다.\n배포에서 `current -> releases/2024-01-05` 처럼 링크만 바꿔 **즉시 전환·즉시 롤백**하는 방식이 널리 쓰입니다.")
    ] },

  { t:"찾기 — find 와 grep", xp:20,
    th:{ sum:"**이름으로 파일을 찾으면 `find`, 내용으로 찾으면 `grep`.** 이 한 줄만 기억하면 나머지는 옵션이다.",
      body:[
        {h:"find 는 조건을 이어 붙인다", t:"`find 시작경로 조건…` 형태다. `-name`(이름) `-type f`(파일만) `-mtime -7`(7일 안에 수정) `-size +100M`(100MB 초과)을 조합한다. 찾은 것에 명령을 돌리려면 `-exec … {} +` 나 `-print0 | xargs -0` 을 쓰는데, **파일 이름에 공백이 있어도 안전**하기 때문이다."},
        {h:"grep 은 줄 단위로 본다", t:"`-r` 재귀, `-n` 줄 번호, `-i` 대소문자 무시, `-v` 반대로, `-c` 개수, `-A/-B/-C` 앞뒤 줄까지. 로그에서 원인을 찾을 때 `grep -n -C 3 ERROR app.log` 처럼 **앞뒤 맥락을 함께** 보는 것이 요령이다. 코드 검색에는 `ripgrep(rg)` 이 훨씬 빠르지만 옵션 체계는 거의 같다."}],
      code:{ c:"find . -name '*.log' -mtime +30 -delete\nfind . -type f -size +100M -exec ls -lh {} +\ngrep -rn --include='*.py' 'TODO' .\ngrep -n -C 3 ERROR app.log", cap:"이름은 find, 내용은 grep" },
      key:["이름은 find · 내용은 grep","find 는 조건을 이어 붙인다","-print0 | xargs -0 로 공백 안전","-C 로 앞뒤 맥락을 함께 본다"] },
    q:[
      I("이름으로 찾기","현재 폴더 아래에서 이름이 config 인 파일을 찾는 명령은? (find 부터 전부)",
        ["find . -name config","find . -name 'config'"],
        "정답은 `find . -name config` 입니다.\n패턴에 `*` 를 쓸 때는 `find . -name '*.log'` 처럼 **따옴표로 감싸야** 합니다. 감싸지 않으면 셸이 먼저 펼쳐 버려 엉뚱한 것을 찾게 됩니다."),
      C("도구 선택","파일 <b>내용</b>에서 특정 문자열이 든 파일을 찾으려면?",
        ["find 로 이름을 훑는다","grep -r","ls -R 로 하위까지 나열","du"],1,
        "내용은 `grep -r` 입니다. `find` 는 **이름·크기·시각** 같은 파일의 속성으로 찾습니다.\n둘을 함께 쓰기도 합니다 — `find . -name '*.py' -exec grep -l TODO {} +` 처럼 대상을 좁힌 뒤 내용을 봅니다.\n`grep --include='*.py' -r` 로도 같은 일을 더 짧게 할 수 있습니다."),
      I("재귀 검색","현재 폴더 아래 전부에서 문자열을 찾는 grep 옵션은? (옵션 한 글자, 대시 포함)",
        ["-r","-R"],
        "정답은 `-r` 입니다. `-R` 은 심볼릭 링크까지 따라 들어간다는 점만 다릅니다.\n실무에서는 `-rn`(줄 번호 포함)을 붙여 쓰고, 결과를 편집기로 바로 열 수 있게 `파일:줄번호` 형식으로 뽑습니다.",
        P("cd /tmp && rm -rf crx3 && mkdir -p crx3/sub && cd crx3 && echo hello > a.txt && echo hello > sub/b.txt && grep -rl hello . | wc -l","2")),
      C("공백 안전","파일 이름에 공백이 있어도 안전하게 처리하려면?",
        ["find … | xargs 명령","find … -print0 | xargs -0 명령","find 결과를 while read 로 한 줄씩 받아 처리한다","find 명령의 결과를 for 문에 그대로 넣어 돌린다"],1,
        "`-print0` 은 이름을 **널 문자로 구분**하고 `xargs -0` 이 그렇게 읽습니다. 공백·줄바꿈이 든 이름도 안전합니다.\n`$(find …)` 를 for 문에 쓰면 셸이 공백에서 잘라 버려 `My Document.txt` 가 두 개로 갈립니다.\n`find … -exec 명령 {} +` 도 같은 문제를 피하면서 프로세스를 적게 띄웁니다."),
      C("오래된 파일","30일보다 오래 전에 수정된 로그를 찾는 조건은?",
        ["-mtime 30","-mtime +30","-mtime -30 (30일 이내)","-ctime 30 으로 지정"],1,
        "`+30` 이 '30일보다 **더 오래된**' 입니다. `-30` 은 '30일 **이내**', 부호 없는 `30` 은 '정확히 30일째' 라는 뜻입니다.\n이 부호 규칙은 `-size` 에도 똑같이 적용됩니다.\n지우기 전에는 반드시 `-delete` 없이 먼저 돌려 **목록을 눈으로 확인**해야 합니다."),
      C("맥락","로그에서 오류 줄과 그 앞뒤를 함께 보려면?",
        ["grep ERROR app.log","grep -C 3 ERROR app.log","grep -v ERROR app.log 로 반대만 보기","grep -c ERROR app.log 로 개수 세기"],1,
        "`-C 3` 은 앞뒤 3줄씩 함께 보여 줍니다(`-A` 뒤, `-B` 앞).\n오류 줄만 보면 원인을 알 수 없는 경우가 대부분입니다 — 직전 요청이나 직후 스택 트레이스에 단서가 있습니다.\n`-c` 는 개수만 세고, `-v` 는 매칭되지 <b>않는</b> 줄을 보여 줍니다."),
      C("제외","검색에서 특정 폴더를 빼려면 가장 간단한 방법은?",
        ["나온 결과를 눈으로 훑어 직접 거른다","grep --exclude-dir=node_modules 를 쓴다","find 명령을 서로 다른 조건으로 두 번 돌린 뒤 겹치는 결과를 빼내어 정리한다","불가능하다"],1,
        "`--exclude-dir` 로 통째로 뺍니다. `node_modules`·`.git`·`dist` 를 빼면 검색이 몇 배 빨라지고 결과도 읽을 만해집니다.\n`ripgrep(rg)` 은 `.gitignore` 를 자동으로 존중해 이 설정이 대부분 필요 없습니다.\n검색 결과가 너무 많아 쓸모없어지는 것도 '찾지 못하는 것' 만큼 흔한 실패입니다."),
      C("실행 파일 찾기","시스템 전체에서 이름이 <code>nginx</code> 인 파일을 찾을 때 <code>find / -name nginx</code> 의 문제는?",
        ["문법이 틀렸다","권한 없는 폴더에서 오류가 쏟아지고 매우 느리다","탐색 범위가 너무 넓어서 nginx 를 오히려 찾지 못한다","시스템 전체를 훑으려면 루트 권한이 반드시 필요하다"],1,
        "`/proc`·`/sys` 까지 훑고, 읽을 수 없는 폴더마다 오류를 뱉습니다.\n오류를 숨기려면 `2>/dev/null` 을 붙이고, 범위를 `/usr /etc` 처럼 좁히는 것이 실용적입니다.\n미리 만들어 둔 색인을 쓰는 `locate` 는 훨씬 빠르지만 **색인이 갱신된 시점 기준**이라는 한계가 있습니다.")
    ] }
]},

/* ═══════════════ 유닛 3 ═══════════════ */
{ t:"권한 — 누가 무엇을 할 수 있나", l:[

  { t:"읽기 · 쓰기 · 실행, 그리고 세 부류", xp:25,
    th:{ sum:"리눅스 권한은 **세 부류(소유자·그룹·나머지) × 세 동작(읽기·쓰기·실행)** 아홉 칸이다. `ls -l` 의 앞 열 글자가 그 표다.",
      body:[
        {h:"rwx 를 숫자로 읽는다", t:"읽기 4, 쓰기 2, 실행 1을 더한다. `rwx`=7, `rw-`=6, `r-x`=5, `r--`=4. 그래서 `644` 는 '소유자는 읽고 쓰기, 나머지는 읽기만', `755` 는 '소유자는 전부, 나머지는 읽기와 실행' 이다. 일반 파일은 644, 실행 파일과 폴더는 755 가 기본이다."},
        {h:"폴더의 x 는 '들어가기' 다", t:"폴더에서 `r` 은 **목록 보기**, `w` 는 **항목 추가·삭제**, `x` 는 **통과해 들어가기**다. 그래서 `x` 없이 `r` 만 있으면 이름은 보이는데 열 수 없고, `x` 만 있으면 목록은 못 보지만 **정확한 이름을 알면** 접근할 수 있다. 파일을 지울 수 있는지는 파일 권한이 아니라 **그 파일이 든 폴더의 `w`** 가 정한다 — 처음 보면 놀라는 규칙이다."}],
      code:{ c:"ls -l\n# -rw-r--r-- 1 me me  120 Jan 5 app.conf   → 644\n# drwxr-xr-x 2 me me 4096 Jan 5 bin/       → 755\nchmod 600 ~/.ssh/id_ed25519   # 나만 읽고 쓴다", cap:"맨 앞 글자는 종류(- 파일 · d 폴더 · l 링크)" },
      key:["r=4 w=2 x=1 을 더한다","폴더의 x 는 들어가기","삭제 권한은 폴더의 w 가 정한다","개인키는 600 이 아니면 ssh 가 거부한다"] },
    q:[
      I("소유자 변경","파일의 소유자와 그룹을 바꾸는 명령은? (명령어만)",
        ["chown"],
        "정답은 `chown` 입니다. `chown 사용자:그룹 파일` 형태로 씁니다.\n권한 비트를 바꾸는 `chmod` 와 헷갈리기 쉽습니다 — `chown` 은 **누구의 것인가**, `chmod` 는 **무엇을 할 수 있는가** 입니다."),
      C("숫자","<code>chmod 644 file</code> 의 뜻은?",
        ["소유자와 그룹과 나머지가 모두 읽고 쓸 수 있다","소유자는 읽기·쓰기, 그룹과 나머지는 읽기만","소유자만 읽을 수 있고 나머지 사용자는 아무것도 하지 못한다","실행 권한을 준다"],1,
        "6 = 4(읽기)+2(쓰기), 4 = 읽기입니다. 일반 파일의 표준값입니다.\n실행 파일이나 폴더에는 `755` 를 씁니다 — 폴더는 `x` 가 없으면 들어갈 수 없기 때문입니다.\n`777` 은 '누구나 무엇이든' 이라 거의 언제나 잘못된 선택입니다. 문제가 풀리는 것처럼 보여도 원인을 덮을 뿐입니다."),
      C("폴더의 x","폴더에 <code>r</code> 은 있고 <code>x</code> 가 없으면?",
        ["그 폴더에 대해 아무것도 할 수 없게 된다","이름 목록은 볼 수 있지만 안으로 들어가거나 파일을 열 수 없다",
         "안으로 들어갈 수는 있지만 그 안에 무엇이 있는지 목록은 전혀 볼 수가 없다","파일을 지울 수 있다"],1,
        "폴더에서 `x` 는 **통과 권한**입니다. 없으면 그 안의 어떤 경로에도 접근할 수 없습니다.\n반대로 `x` 만 있고 `r` 이 없으면 목록은 못 보지만 **정확한 이름을 알면** 열 수 있습니다 — 이것을 이용해 홈 디렉터리를 `711` 로 두는 서버 설정이 흔합니다.\n웹 서버가 '파일은 있는데 403' 을 뱉는 원인의 상당수가 상위 폴더의 `x` 누락입니다."),
      C("삭제","파일을 지울 수 있는지는 무엇이 정하는가?",
        ["그 파일 자체에 붙어 있는 쓰기(w) 권한","그 파일이 들어 있는 폴더의 w 권한","그 파일에 실행 권한(x)이 붙어 있는지 아닌지 여부","소유자 여부만"],1,
        "삭제는 **폴더 안의 항목을 지우는 일**이라 폴더의 `w` 가 필요합니다. 파일 자체가 읽기 전용이어도 지워집니다.\n그래서 `/tmp` 처럼 모두가 쓸 수 있는 폴더에는 **스티키 비트(`1777`)** 를 걸어 '자기 것만 지울 수 있게' 제한합니다.\n이 규칙을 모르면 '읽기 전용으로 만들었는데 지워졌다' 는 사고를 이해할 수 없습니다."),
      I("실행 권한 주기","파일에 실행 권한을 더하는 가장 짧은 표기는? (chmod 뒤 인자만)",
        ["+x","a+x","u+x"],
        "정답은 `+x` 입니다. `chmod +x script.sh` 로 씁니다.\n숫자 표기는 **전체를 덮어쓰지만** 기호 표기(`+x`·`-w`·`g+r`)는 **더하고 뺍니다**. 기존 권한을 지키고 싶을 때는 기호 표기가 안전합니다.",
        P("cd /tmp && rm -f crxs.sh && touch crxs.sh && chmod +x crxs.sh && stat -c %a crxs.sh","755")),
      C("SSH 키","<code>~/.ssh/id_ed25519</code> 의 권한이 <code>644</code> 이면?",
        ["정상이다","ssh 가 너무 열려 있다며 키 사용을 거부한다","ssh 가 접속하는 시점에 알아서 600 으로 바꿔 준다","경고 문구만 뜨고 접속 자체는 그대로 이루어진다"],1,
        "개인키가 **다른 사용자에게 읽힐 수 있으면** ssh 가 아예 거부합니다. `600` 이어야 합니다.\n불친절해 보이지만, 개인키 유출은 되돌릴 수 없으므로 도구가 강하게 막는 것이 옳습니다.\n`.ssh` 폴더 자체도 `700` 이어야 하고, 파일을 옮기거나 압축을 풀었을 때 권한이 바뀌어 이 문제가 자주 생깁니다."),
      C("sudo","<code>sudo</code> 를 쓸 때의 원칙으로 가장 적절한 것은?",
        ["편하니 항상 붙인다","필요한 명령에만 붙이고, 무엇을 하는지 아는 명령에만 쓴다",
         "아예 root 계정으로 로그인해 두고 쓰는 편이 훨씬 낫다","파일 권한을 chmod 777 로 활짝 열어 sudo 를 대신하게 한다"],1,
        "`sudo` 는 **안전장치를 잠시 끄는 일**입니다. 습관적으로 붙이면 오타 하나의 결과가 시스템 전체로 번집니다.\n인터넷에서 복사한 `sudo` 명령을 이해하지 않고 붙여 넣는 것이 가장 흔한 사고 경로입니다.\n필요한 권한만 주려면 `/etc/sudoers` 에서 특정 명령만 허용할 수 있습니다 — 서비스 계정에는 이쪽이 정석입니다."),
      C("umask","새로 만든 파일이 <code>644</code> 로 생기는 이유는?",
        ["커널이 아예 바꿀 수 없도록 미리 고정해 둔 값이기 때문에","umask 가 기본 권한에서 일부를 깎기 때문","지금 쓰고 있는 파일 시스템의 종류 때문에","소유자 설정 때문"],1,
        "기본값 `666` 에서 `umask 022` 가 그룹·기타의 쓰기 권한을 깎아 `644` 가 됩니다. 폴더는 `777` 에서 깎여 `755` 가 됩니다.\n서비스 계정에서 `umask 077` 을 쓰면 새로 만드는 파일이 **자기만 읽을 수 있게** 되어 안전합니다.\n'파일이 왜 이 권한으로 생기지' 의 답은 대부분 여기 있습니다.")
    ] },

  { t:"사용자 · 그룹 · 서비스 계정", xp:25,
    th:{ sum:"서버에서 권한 사고를 막는 가장 좋은 방법은 **각 서비스를 자기 계정으로 돌리는 것**이다. 무엇이 잘못돼도 그 계정이 할 수 있는 만큼만 잘못된다.",
      body:[
        {h:"그룹으로 묶는다", t:"사용자마다 권한을 따로 주면 관리가 무너진다. **그룹**을 만들어 권한을 주고 사람을 그룹에 넣는다. `docker` 그룹이 대표적인데, 이 그룹에 넣는 것은 사실상 **root 를 주는 것**과 같다는 점을 알아야 한다 — 컨테이너로 호스트 파일 시스템을 마운트할 수 있기 때문이다."},
        {h:"서비스는 로그인할 수 없어야 한다", t:"nginx·postgres 같은 서비스 계정은 **셸을 `/usr/sbin/nologin`** 으로 두어 로그인 자체를 막는다. 공격자가 그 계정을 잡아도 셸을 얻지 못한다. 홈 디렉터리도 필요 없고 비밀번호도 없다 — **줄 수 있는 것을 최대한 안 주는 것**이 원칙이다."}],
      code:{ c:"id                       # 나는 누구이고 어떤 그룹인가\nsudo useradd -r -s /usr/sbin/nologin appsvc\nsudo chown -R appsvc:appsvc /srv/app\nsudo -u appsvc ./run.sh   # 그 계정으로 실행", cap:"서비스마다 자기 계정" },
      key:["권한은 그룹에 주고 사람을 넣는다","docker 그룹은 사실상 root","서비스 계정은 nologin","최소 권한이 원칙"] },
    q:[
      I("다른 계정으로 실행","특정 계정의 권한으로 명령을 실행하는 sudo 옵션은? (대시 포함)",
        ["-u","--user"],
        "정답은 `-u` 입니다. `sudo -u appsvc ./run.sh` 처럼 씁니다.\n서비스 계정에 로그인 셸을 주지 않아도 이 방법으로 그 계정의 권한으로 실행할 수 있어, 불필요한 로그인 권한을 주지 않아도 됩니다."),
      I("내 정보","현재 사용자와 소속 그룹을 한 줄로 보여 주는 명령은?",
        ["id"],
        "정답은 `id` 입니다. UID·GID·소속 그룹을 전부 보여 줍니다.\n`whoami` 는 이름만 알려 줍니다. 권한 문제를 볼 때는 **그룹까지** 봐야 하므로 `id` 가 낫습니다 — 대개 원인이 그룹에 있습니다."),
      C("docker 그룹","사용자를 <code>docker</code> 그룹에 넣을 때 알아야 할 점은?",
        ["읽기만 가능해지는 것이므로 특별한 위험이 전혀 없다","사실상 root 권한을 주는 것과 같다","읽기 권한만 준다","주어진 권한이 컨테이너 안에서만 유효하다"],1,
        "호스트 파일 시스템을 마운트한 컨테이너를 띄울 수 있으므로 **root 와 다름없습니다**.\n'편의를 위해 개발자를 전부 docker 그룹에' 는 흔하지만, 그 서버가 무엇을 담고 있는지 알고 결정해야 합니다.\n대안으로 rootless 도커나 podman 이 있습니다."),
      C("서비스 계정","서비스 전용 계정의 셸을 <code>nologin</code> 으로 두는 이유는?",
        ["홈 디렉터리가 차지하게 될 디스크 공간을 아끼려고","계정이 탈취돼도 대화형 셸을 얻지 못하게 하려고",
         "로그인 과정을 통째로 건너뛰도록 만들어 시작 속도를 높이려고","로그를 줄이려고"],1,
        "**공격 표면을 줄이는** 조치입니다. 서비스는 로그인할 필요가 없으니 그 능력을 주지 않습니다.\n같은 맥락에서 홈 디렉터리도 두지 않고, 비밀번호도 설정하지 않습니다.\n관리자가 그 계정으로 무언가 실행해야 하면 `sudo -u appsvc 명령` 으로 충분합니다."),
      C("소유자 바꾸기","<code>/srv/app</code> 이하 전체의 소유자를 바꾸려면?",
        ["chmod -R appsvc /srv/app 로 바꾼다","chown -R appsvc:appsvc /srv/app","chgrp /srv/app","usermod 명령을 사용해서 /srv/app 의 소유자를 변경"],1,
        "`chown` 이 소유자와 그룹을 바꾸고 `-R` 이 하위까지 적용합니다. `chmod` 는 권한 비트를 바꾸는 다른 명령입니다.\n배포 후 '권한 오류' 의 상당수가 파일을 root 로 풀어 놓고 서비스 계정으로 실행하는 데서 옵니다.\n심볼릭 링크를 따라갈지(`-h`·`-L`)는 옵션으로 정해야 하므로, 링크가 섞인 트리에서는 주의합니다."),
      C("최소 권한","'최소 권한 원칙' 을 가장 잘 지킨 설정은?",
        ["모든 개발자에게 sudo 권한을 주고 알아서 조심하도록 신뢰한다","서비스 계정에 필요한 디렉터리만 쓰기 권한을 준다",
         "chmod 777 로 열어 권한 문제 자체를 없앤다","root 로 모든 서비스를 실행한다"],1,
        "**할 수 있는 일의 범위를 미리 좁혀 두는 것**이 최소 권한입니다. 사고가 나도 그 범위 안에서 끝납니다.\n`777` 은 문제를 없애는 것이 아니라 원인을 덮는 것이고, 대개 다음 사고의 씨앗이 됩니다.\n권한이 부족해 보일 때는 넓히기 전에 **정확히 무엇이 막혔는지** 로그로 확인하는 순서가 먼저입니다."),
      C("비밀번호 파일","<code>/etc/shadow</code> 가 <code>/etc/passwd</code> 와 분리된 이유는?",
        ["등록된 항목이 계속 늘어나면서 파일이 너무 커지기 때문에","비밀번호 해시를 일반 사용자가 읽지 못하게 하려고",
         "속도 때문에","사용자 정보를 백업하기 쉽게 만들기 위해서"],1,
        "`/etc/passwd` 는 사용자 정보 조회에 쓰여 **누구나 읽을 수 있어야** 합니다. 여기에 해시가 있으면 오프라인 대입 공격의 표적이 됩니다.\n그래서 해시만 root 만 읽을 수 있는 `/etc/shadow` 로 분리했습니다.\n'읽기 권한이 필요한 정보' 와 '비밀' 을 같은 파일에 두지 않는다는 원칙은 애플리케이션 설계에도 그대로 적용됩니다."),
      C("SUID","실행 파일에 SUID 비트가 걸려 있으면?",
        ["설정된 권한과 무관하게 누구나 읽을 수 있게 된다","실행한 사람이 아니라 파일 소유자의 권한으로 실행된다",
         "실행이 금지된다","실행할 때마다 명령 앞에 자동으로 sudo 가 붙게 된다"],1,
        "`passwd` 처럼 일반 사용자가 root 권한이 필요한 일을 해야 할 때 씁니다 — 실행하는 동안만 소유자 권한을 빌립니다.\n강력한 만큼 **권한 상승의 단골 통로**이기도 해서, 서버 점검에서 `find / -perm -4000` 으로 목록을 확인합니다.\n직접 만든 프로그램에 SUID 를 거는 것은 거의 언제나 잘못된 선택입니다.")
    ] }
]},

/* ═══════════════ 유닛 4 ═══════════════ */
{ t:"프로세스 — 지금 무엇이 돌고 있나", l:[

  { t:"보고, 멈추고, 되살린다", xp:25,
    th:{ sum:"서버가 이상할 때 첫 질문은 언제나 같다. **무엇이 CPU/메모리를 쓰고 있는가, 그리고 누가 그것을 띄웠는가.**",
      body:[
        {h:"프로세스에는 부모가 있다", t:"모든 프로세스는 다른 프로세스가 낳는다(PID 1 제외). 그래서 `ps -ef --forest` 로 **누가 무엇을 띄웠는지** 나무 모양으로 볼 수 있다. 부모가 먼저 죽으면 자식은 PID 1 에게 입양되는데, 이 때문에 컨테이너에서 PID 1 이 신호를 제대로 처리하지 않으면 좀비 프로세스가 쌓인다."},
        {h:"신호는 부탁이지 명령이 아니다", t:"`kill` 은 죽이는 것이 아니라 **신호를 보내는 것**이다. 기본값 `TERM`(15)은 '정리하고 끝내라' 는 부탁이라 프로그램이 무시할 수 있다. `KILL`(9)은 커널이 강제로 끊어 **무시할 수 없지만**, 프로그램이 뒷정리를 못 해 임시 파일과 잠금이 남는다. 그래서 순서는 언제나 **TERM 먼저, 안 되면 KILL** 이다."}],
      code:{ c:"ps -ef --forest | head        # 누가 누구를 낳았나\ntop            # 실시간 (htop 이 더 읽기 좋다)\nkill 1234      # TERM — 정리하고 끝내라\nkill -9 1234   # KILL — 강제 (마지막 수단)", cap:"TERM 먼저, 안 되면 KILL" },
      key:["프로세스에는 부모가 있다","kill 은 신호를 보내는 것","TERM 은 무시될 수 있고 KILL 은 못 한다","KILL 은 뒷정리를 못 하게 만든다"] },
    q:[
      I("이름으로 종료","프로세스 이름으로 신호를 보내는 명령은? (명령어만)",
        ["pkill","killall"],
        "정답은 `pkill`(또는 `killall`)입니다. PID 를 몰라도 이름으로 보낼 수 있습니다.\n편한 만큼 위험합니다 — 이름이 부분 일치해 의도하지 않은 프로세스까지 잡을 수 있으므로, 먼저 `pgrep -a 이름` 으로 **무엇이 걸리는지 확인**하는 것이 안전합니다."),
      C("kill 의 의미","<code>kill</code> 명령이 실제로 하는 일은?",
        ["프로세스를 즉시 메모리에서 제거한다","프로세스에 신호를 보낸다","그 프로세스의 CPU 사용을 멈춘다","파일을 닫는다"],1,
        "이름과 달리 **신호를 보내는** 명령입니다. 기본 신호가 `TERM` 이라 대개 종료로 이어질 뿐입니다.\n`kill -l` 로 보낼 수 있는 신호 목록을 볼 수 있고, `HUP` 은 설정 다시 읽기, `USR1` 은 프로그램이 정한 동작에 쓰입니다.\nnginx 에 `HUP` 을 보내 무중단으로 설정을 반영하는 것이 대표적인 예입니다."),
      C("9 번 신호","<code>kill -9</code> 를 마지막 수단으로 두는 이유는?",
        ["느려서","프로그램이 뒷정리를 못 해 임시 파일·잠금·미저장 데이터가 남기 때문",
         "보낼 때 루트 권한이 반드시 필요해서","같은 프로세스 그룹에 속해 있던 다른 프로세스들까지 한꺼번에 함께 죽어 버려서"],1,
        "`KILL` 은 프로그램에 전달되지 않고 **커널이 바로 끊습니다**. 저장 중이던 데이터, 열린 트랜잭션, 잠금 파일이 그대로 남습니다.\nDB 를 `-9` 로 끄면 다음 기동에서 복구 과정을 거치고, 최악에는 손상됩니다.\n먼저 `TERM` 을 보내고 몇 초 기다린 뒤에도 남아 있을 때만 `-9` 를 씁니다."),
      I("프로세스 목록","현재 시스템의 모든 프로세스를 보는 고전적인 옵션 조합은? (ps 뒤 옵션만)",
        ["-ef","aux","-aux"],
        "정답은 `-ef`(또는 BSD 스타일 `aux`)입니다. 둘 다 널리 쓰이고 출력 형식만 조금 다릅니다.\n`ps -ef --forest` 로 부모-자식 관계를 나무로 보면 '이 프로세스를 누가 띄웠나' 가 바로 보여, 원인 추적이 훨씬 빨라집니다."),
      C("포트 점유","8080 포트를 이미 쓰고 있는 프로세스를 찾으려면?",
        ["ps aux 의 결과를 훑어 8080 이라는 값을 찾는다","ss -ltnp | grep 8080","top","df -h 명령으로 디스크에 남은 공간을 본다"],1,
        "`ss -ltnp`(예전에는 `netstat -ltnp`)가 **어떤 프로세스가 어떤 포트를 듣고 있는지** 보여 줍니다.\n`ps | grep 8080` 은 명령줄에 8080 이 적혀 있을 때만 우연히 걸립니다 — 신뢰할 수 없습니다.\n`lsof -i :8080` 도 같은 일을 하고, 포트 충돌은 배포 실패의 단골 원인입니다."),
      C("좀비","좀비(defunct) 프로세스가 생기는 이유는?",
        ["메모리가 부족해서","자식이 끝났는데 부모가 종료 상태를 거둬 가지 않아서",
         "CPU 가 도저히 감당하지 못할 만큼 심한 과부하 상태에 빠져서","디스크에 남은 공간이 꽉 차 버렸기 때문에"],1,
        "자식이 끝나면 커널은 **종료 상태를 남겨 두고** 부모가 `wait` 로 거둬 가기를 기다립니다. 부모가 거두지 않으면 그 항목이 좀비로 남습니다.\n좀비 자체는 메모리를 거의 안 쓰지만, 쌓이면 PID 를 소진합니다.\n컨테이너에서 흔한데, PID 1 이 된 앱이 자식을 거두지 않기 때문입니다 — `--init` 이나 `tini` 로 해결합니다."),
      C("백그라운드","실행 중인 명령을 백그라운드로 보내는 순서는?",
        ["Ctrl+C 후 bg","Ctrl+Z 로 멈춘 뒤 bg","이미 실행되고 있는 명령 뒤에 & 를 붙인다","실행한 뒤에 nohup 을 붙인다"],1,
        "`Ctrl+Z` 가 **일시 정지**(STOP 신호), `bg` 가 백그라운드에서 재개입니다. `fg` 로 다시 앞으로 가져옵니다.\n`Ctrl+C` 는 `INT` 신호로 아예 종료시키므로 되살릴 수 없습니다.\n터미널을 닫아도 살아 있게 하려면 `nohup`·`disown` 이나 `tmux`·`screen` 을 씁니다."),
      C("메모리 부족","OOM Killer 가 프로세스를 죽였는지 확인하려면?",
        ["top 을 열어서 지금 돌고 있는 프로세스 목록을 처음부터 살펴본다","dmesg 나 journalctl 에서 oom 기록을 찾는다","ps 로 프로세스 목록을 확인한다","df 를 본다"],1,
        "커널이 메모리가 부족해 프로세스를 죽이면 **커널 로그에 기록**됩니다. `dmesg -T | grep -i oom` 또는 `journalctl -k` 로 찾습니다.\n'아무 이유 없이 앱이 죽었다' 의 흔한 정답이고, 애플리케이션 로그에는 아무것도 남지 않아 더 헷갈립니다.\n죽일 대상은 점수(`oom_score`)로 정해지므로, 메모리를 많이 쓰는 큰 프로세스가 먼저 희생됩니다.")
    ] },

  { t:"자원 보기 — CPU · 메모리 · 디스크", xp:25,
    th:{ sum:"'서버가 느리다' 는 신고를 받으면 **네 가지**를 순서대로 본다. CPU, 메모리, 디스크 공간, 디스크 입출력. 원인은 대개 이 넷 중 하나다.",
      body:[
        {h:"load average 는 CPU 사용률이 아니다", t:"`uptime` 이 보여 주는 세 숫자는 **실행 대기 중인 작업의 평균 개수**다(1·5·15분). 코어가 4개인데 load 가 4면 대략 포화, 8이면 대기가 쌓이는 중이다. 리눅스에서는 **디스크를 기다리는 프로세스도 포함**되므로, CPU 는 한가한데 load 가 높으면 I/O 병목을 의심한다."},
        {h:"쓸 수 있는 메모리는 free 가 아니다", t:"리눅스는 남는 메모리를 **파일 캐시로 다 쓴다**. 그래서 `free -h` 의 `free` 는 늘 작게 보이고, 실제로 중요한 것은 **`available`** 이다. 이 차이를 모르면 '메모리가 없다' 고 잘못 진단한다. 디스크 공간이 꽉 찼는지는 `df -h`, 무엇이 차지하는지는 `du -sh *` 로 좁혀 간다."}],
      code:{ c:"uptime               # load average (코어 수와 비교)\nfree -h              # available 을 본다\ndf -h                # 파티션별 남은 공간\ndu -sh * | sort -h   # 이 폴더에서 뭐가 큰가", cap:"free 가 아니라 available" },
      key:["load 는 대기 중인 작업 수","리눅스 load 는 I/O 대기도 포함","메모리는 available 을 본다","df 로 좁히고 du 로 찾는다"] },
    q:[
      I("가동 시간","서버가 켜진 뒤 얼마나 지났는지와 load average 를 한 줄로 보는 명령은?",
        ["uptime"],
        "정답은 `uptime` 입니다. 가동 시간, 접속자 수, load average 세 숫자를 함께 보여 줍니다.\n가동 시간이 예상보다 짧으면 **누군가 재부팅했거나 서버가 죽었다 살아난 것**이라, 그 자체가 장애 조사의 단서가 됩니다."),
      C("load average","코어 4개 서버에서 load average 가 8.0 이라면?",
        ["CPU 사용률이 800% 에 이를 만큼 몹시 바쁜 상태라는 뜻","처리 능력의 두 배쯤 되는 작업이 대기 중이라는 뜻","메모리가 부족해져서 스왑이 계속 일어나고 있다","정상 범위다"],1,
        "load 는 **실행 대기 중인 작업의 평균 개수**입니다. 코어 수와 비교해야 의미가 생깁니다 — 코어 4에 load 4면 대략 포화입니다.\n리눅스에서는 디스크를 기다리는 프로세스도 세므로, CPU 사용률이 낮은데 load 가 높으면 **I/O 병목**입니다.\n그 경우 `iostat`·`iotop` 으로 디스크 쪽을 봐야 합니다."),
      C("메모리","<code>free -h</code> 에서 실제로 봐야 할 열은?",
        ["free (남은 것으로 표시된 양)","available","used","shared (공유 메모리)"],1,
        "`available` 이 **새 프로그램이 실제로 쓸 수 있는 양**입니다. 캐시로 쓰인 메모리는 필요하면 즉시 회수되기 때문입니다.\n`free` 만 보면 늘 부족해 보여 잘못된 결론에 이릅니다 — 리눅스는 남는 메모리를 놀리지 않고 파일 캐시로 씁니다.\n캐시가 많다는 것은 오히려 **잘 쓰고 있다**는 신호입니다."),
      I("디스크 공간","파티션별 남은 공간을 사람이 읽기 좋게 보는 명령은? (명령과 옵션)",
        ["df -h 명령으로 디스크에 남은 공간을 본다","df -h "],
        "정답은 `df -h` 입니다. `-h` 가 GB·MB 로 바꿔 줍니다.\n`Use%` 가 100% 인 파티션이 있으면 거기서 모든 것이 멈춥니다. 로그가 쌓이는 `/var` 가 가장 흔한 범인이라, 로그 회전 설정이 곧 예방책입니다."),
      C("큰 폴더 찾기","디스크가 꽉 찼을 때 무엇이 차지하는지 좁혀 가는 명령은?",
        ["df -h 명령으로 디스크에 남은 공간을 본다","du -sh * | sort -h","ls -l","free -h 로 메모리 상태를 확인한다"],1,
        "`du -sh *` 로 현재 폴더의 항목별 크기를 재고, 큰 곳으로 들어가며 좁혀 갑니다. `sort -h` 가 사람이 읽는 단위로 정렬해 줍니다.\n`df` 는 **파티션 단위**라 어느 폴더가 범인인지는 알려 주지 않습니다.\n주의할 점 하나 — 지워진 파일을 프로세스가 열고 있으면 `du` 에는 안 잡히는데 공간은 안 돌아옵니다. 그때는 `lsof +L1` 로 찾습니다."),
      C("실시간 관찰","프로세스별 CPU·메모리를 실시간으로 보려면?",
        ["ps 로 프로세스 확인하기","top 또는 htop","df","dmesg 명령으로 커널 메시지 확인하기"],1,
        "`top` 이 표준이고, `htop` 은 색과 조작이 편해 널리 쓰입니다.\n`ps` 는 **그 순간의 스냅숏**이라 변화를 볼 수 없습니다. 대신 스크립트로 다루기에는 `ps` 가 낫습니다.\n`top` 에서 `P` 는 CPU 순, `M` 은 메모리 순 정렬입니다."),
      C("디스크 I/O","CPU 는 한가한데 응답이 느리다. 다음으로 볼 것은?",
        ["서버에 꽂힌 메모리 총량","디스크 I/O 대기 (iostat 의 %iowait)","지금 이 순간 시스템에 동시에 떠 있는 전체 프로세스의 개수","커널 버전"],1,
        "`%iowait` 가 높으면 **디스크를 기다리느라** 아무 일도 못 하고 있다는 뜻입니다.\n느린 디스크, 스왑 발생, 로그 과다 기록이 흔한 원인입니다. `iotop` 으로 어느 프로세스가 읽고 쓰는지 좁힐 수 있습니다.\n메모리가 부족해 스왑이 일어나면 메모리 문제가 I/O 문제로 나타나므로, 둘을 함께 봐야 합니다."),
      C("서비스 로그","systemd 로 관리되는 서비스의 로그를 보려면?",
        ["cat /var/log/syslog","journalctl -u 서비스명","dmesg 명령으로 커널 메시지 확인하기","tail /etc/service.log 로 확인"],1,
        "`journalctl -u nginx -f` 처럼 서비스 단위로 보고 따라갈 수 있습니다. `--since '10 min ago'` 로 시간도 좁힙니다.\n`dmesg` 는 커널 메시지 전용이라 애플리케이션 로그는 없습니다.\n로그 위치를 외우지 않아도 되는 것이 journald 의 큰 장점이지만, 저장 용량 설정을 안 하면 디스크를 채웁니다.")
    ] }
]},

/* ═══════════════ 유닛 5 ═══════════════ */
{ t:"파이프와 텍스트 도구 — 작은 도구를 잇는다", l:[

  { t:"파이프 — 출력이 다음의 입력이 된다", xp:30,
    th:{ sum:"유닉스의 핵심 발상은 '**하나만 잘하는 작은 도구를 이어 붙인다**' 이다. 그 이음매가 파이프 `|` 다.",
      body:[
        {h:"세 개의 기본 통로", t:"모든 프로그램에는 표준 입력(0)·표준 출력(1)·표준 오류(2)가 있다. `|` 는 **표준 출력만** 다음 명령의 표준 입력으로 잇는다. 그래서 오류 메시지는 파이프를 타지 않고 화면에 그대로 남는다. 오류까지 넘기려면 `2>&1` 로 합치고, 버리려면 `2>/dev/null` 로 보낸다."},
        {h:"조합이 곧 표현력이다", t:"`정렬 → 중복 제거 → 개수 세기 → 다시 정렬` 은 로그 분석의 기본 관용구다. `sort | uniq -c | sort -rn | head` 한 줄이면 '가장 많이 나온 것 10개' 가 나온다. **`uniq` 는 인접한 중복만 지우므로 반드시 먼저 `sort`** 해야 하는데, 이것을 모르면 결과가 조용히 틀린다."}],
      code:{ c:"cut -d' ' -f1 access.log | sort | uniq -c | sort -rn | head\n#  IP 만 뽑아 → 정렬 → 세고 → 많은 순 → 상위 10\n\ncmd 2>/dev/null    # 오류는 버린다\ncmd 2>&1 | less    # 오류까지 함께 본다", cap:"uniq 앞에는 반드시 sort" },
      key:["| 는 표준 출력만 잇는다","오류는 2> 로 따로 다룬다","uniq 는 인접한 중복만 지운다","sort | uniq -c | sort -rn 은 관용구"] },
    q:[
      I("입력을 인자로","표준 입력으로 들어온 목록을 명령의 인자로 바꿔 주는 명령은?",
        ["xargs"],
        "정답은 `xargs` 입니다. `ls | rm` 이 동작하지 않는 이유를 메워 줍니다.\n대부분의 명령은 표준 입력이 아니라 **인자**를 받기 때문입니다. 파일 이름에 공백이 있을 수 있으므로 `find … -print0 | xargs -0` 형태가 안전한 기본형입니다."),
      C("파이프","<code>a | b</code> 에서 파이프를 타고 넘어가는 것은?",
        ["a 의 표준 출력과 표준 오류 모두","a 의 표준 출력만","a 의 종료 코드","a 명령이 새로 만든 파일"],1,
        "**표준 출력만** 넘어갑니다. 표준 오류는 파이프를 타지 않고 화면에 남습니다.\n그래서 `make 2>&1 | tee build.log` 처럼 오류까지 기록하려면 명시적으로 합쳐야 합니다.\n`2>&1` 을 파이프 <b>뒤</b>에 쓰면 이미 늦으므로 순서가 중요합니다."),
      C("uniq","<code>uniq</code> 앞에 <code>sort</code> 를 두는 이유는?",
        ["로그인 과정을 통째로 건너뛰도록 만들어 시작 속도를 높이려고","uniq 는 인접한 중복만 지우기 때문","결과가 알파벳 순으로 나와야 해서","한 번에 올리는 분량을 줄여 메모리를 아끼려고"],1,
        "`uniq` 는 **바로 앞 줄과만** 비교합니다. 떨어져 있는 같은 값은 그대로 남습니다.\n정렬 없이 쓰면 결과가 조용히 틀리는데, 오류도 나지 않아 발견하기 어렵습니다.\n`sort -u` 로 한 번에 할 수도 있지만, 개수를 세려면 `uniq -c` 가 필요하므로 두 단계로 씁니다."),
      I("상위 집계","가장 많이 나온 값을 세는 관용구의 순서는? (sort 로 시작해 네 명령을 파이프로)",
        ["sort | uniq -c | sort -rn | head","sort|uniq -c|sort -rn|head"],
        "정답은 `sort | uniq -c | sort -rn | head` 입니다.\n정렬 → 세기 → 많은 순 → 상위. 로그 분석에서 손이 저절로 치게 되는 조합이고, 접속 IP·에러 코드·느린 URL 을 찾을 때 그대로 씁니다.",
        P("printf 'b\\na\\nb\\nc\\nb\\n' | sort | uniq -c | sort -rn | head -1 | awk '{print $1$2}'","3b")),
      C("오류 버리기","오류 메시지만 화면에서 치우려면?",
        ["cmd > /dev/null","cmd 2>/dev/null","cmd | /dev/null 로 파이프에 흘려보낸다","cmd -q"],1,
        "`2>` 가 표준 오류의 방향을 바꿉니다. `>` 만 쓰면 정상 출력이 사라지고 오류만 남습니다.\n`find / -name x 2>/dev/null` 처럼 권한 오류가 쏟아질 때 유용합니다.\n다만 진짜 문제를 숨길 수 있으므로, 디버깅 중에는 버리지 않는 편이 낫습니다."),
      C("중간 저장","파이프 중간 결과를 파일로도 남기면서 계속 넘기려면?",
        ["cmd > file | next 로 저장하며 넘긴다","cmd | tee file | next","cmd >> file | next 로 덧붙이며 넘긴다","cmd | cat file"],1,
        "`tee` 는 입력을 **파일과 다음 명령 양쪽에** 보냅니다. 이름 그대로 T 자 분기입니다.\n`| tee -a` 는 덧붙이기이고, `| sudo tee /etc/…` 는 리다이렉션에 sudo 가 안 먹는 문제를 푸는 관용구입니다.\n긴 파이프라인을 디버깅할 때 중간에 끼워 넣으면 어디서 어긋났는지 바로 보입니다."),
      C("열 뽑기","공백으로 나뉜 로그에서 첫 번째 열만 뽑으려면?",
        ["cut -d' ' -f1","cut -c1 으로 첫 글자만","head -1","sort -k1 으로 첫 번째 열을 기준으로 정렬"],0,
        "`-d` 가 구분자, `-f` 가 몇 번째 필드인지입니다. `-c` 는 **글자 위치**라 전혀 다른 결과가 나옵니다.\n공백이 여러 개 연속이면 `cut` 은 빈 필드를 만들어 어긋나므로, 그때는 `awk '{print $1}'` 가 낫습니다 — awk 는 연속 공백을 하나로 봅니다.\n로그 형식이 고정 폭이면 `cut -c` 가 더 정확할 때도 있습니다."),
      C("xargs","<code>xargs</code> 가 필요한 이유는?",
        ["출력 결과를 사람이 읽기 좋은 순서로 다시 정렬해 주기 위해서","표준 입력으로 들어온 목록을 명령의 인자로 바꿔 주려고",
         "넘겨받은 파일을 압축하려고","권한을 바꾸려고"],1,
        "대부분의 명령은 **표준 입력이 아니라 인자**를 받습니다. `ls | rm` 이 안 되는 이유입니다.\n`xargs` 가 그 사이를 이어 줍니다. 인자 개수 제한도 알아서 나눠 여러 번 실행합니다.\n공백이 든 이름 때문에 `-print0 | xargs -0` 조합을 쓰는 것이 안전한 기본형입니다.")
    ] },

  { t:"sed 와 awk — 한 줄로 끝내기", xp:30,
    th:{ sum:"`sed` 는 **줄을 고치는 도구**, `awk` 는 **열로 나눠 계산하는 도구**다. 둘 다 파일 전체를 메모리에 올리지 않아 거대한 로그에도 쓸 수 있다.",
      body:[
        {h:"sed 는 치환이 9할", t:"실무에서 쓰는 sed 는 대부분 `s/찾을것/바꿀것/g` 하나다. `-i` 를 붙이면 파일을 직접 고치는데, **되돌릴 수 없으므로** 먼저 `-i` 없이 돌려 결과를 확인하는 것이 정석이다. `-i.bak` 을 쓰면 원본을 `.bak` 으로 남겨 준다."},
        {h:"awk 는 작은 프로그래밍 언어", t:"`awk '{print $1, $9}'` 처럼 열을 뽑고, `awk '$9 >= 500'` 처럼 조건으로 거르고, `awk '{s+=$1} END {print s}'` 처럼 **합계·평균**을 낸다. `$0` 은 줄 전체, `$1`·`$2` 는 필드, `NF` 는 필드 수, `NR` 은 줄 번호다. 이 다섯 개만 알아도 로그 분석의 대부분이 된다."}],
      code:{ c:"sed 's/old/new/g' f.txt          # 화면으로 먼저 확인\nsed -i.bak 's/old/new/g' f.txt   # 확인 후 파일 수정 (백업 남김)\n\nawk '$9 >= 500 {print $7}' access.log | sort | uniq -c\nawk '{sum += $10} END {print sum/NR}' access.log", cap:"sed -i 는 확인한 뒤에" },
      key:["sed 는 치환, awk 는 열 계산","-i 전에 반드시 화면으로 확인","$0 줄 전체 · NF 필드 수 · NR 줄 번호","둘 다 스트리밍이라 큰 파일에 강하다"] },
    q:[
      I("구분자 지정","awk 에서 쉼표를 필드 구분자로 쓰는 옵션은? (옵션과 값 붙여서)",
        ["-F,","-F ,","-F','"],
        "정답은 `-F,` 입니다. `awk -F, '{print $2}' data.csv` 처럼 씁니다.\n다만 값 안에 쉼표가 들어간 CSV 는 이 방법으로 깨집니다. 따옴표 규칙까지 지키려면 전용 파서를 써야 하고, 그 지점이 awk 를 놓아야 할 때입니다."),
      C("두 도구의 역할","<code>sed</code> 와 <code>awk</code> 의 전형적인 역할 구분은?",
        ["sed 는 줄을 정렬해 주고 awk 는 내용을 검색해 준다","sed 는 줄 단위 치환, awk 는 열로 나눠 계산",
         "sed 는 파일을 압축해 주고 awk 는 그것을 다시 해제해 준다","둘은 완전히 같다"],1,
        "`sed`(stream editor)는 **줄을 고치고**, `awk` 는 **필드로 나눠 계산**합니다.\n둘 다 한 줄씩 흘려보내며 처리하므로 수십 GB 파일에도 메모리 걱정 없이 쓸 수 있습니다.\n복잡해지면 파이썬으로 넘어가는 것이 낫지만, 한 줄로 끝나는 일에 스크립트를 만드는 것도 낭비입니다."),
      C("-i 옵션","<code>sed -i</code> 를 쓸 때 지켜야 할 습관은?",
        ["권한 문제를 피하기 위해서 언제나 sudo 와 함께 붙여 쓴다","-i 없이 먼저 돌려 결과를 확인한 뒤 적용한다",
         "반드시 -r 과 함께 쓴다","여러 파일에 한꺼번에 적용해서 작업 시간을 아낀다"],1,
        "`-i` 는 **파일을 직접 덮어씁니다.** 정규식이 조금만 어긋나도 파일이 망가지고 되돌릴 수 없습니다.\n먼저 화면으로 결과를 확인하고, 적용할 때도 `-i.bak` 으로 원본을 남기는 것이 안전합니다.\n버전 관리 아래에 있는 파일이라면 `git diff` 로 확인할 수 있으니 그것도 좋은 안전장치입니다."),
      I("치환","파일에서 old 를 new 로 모두 바꾸는 sed 표현은? (따옴표 안의 식만)",
        ["s/old/new/g","s/old/new/g "],
        "정답은 `s/old/new/g` 입니다. 끝의 `g` 가 없으면 **각 줄의 첫 번째 하나만** 바뀝니다.\n구분자는 `/` 가 아니어도 됩니다 — 경로를 치환할 때는 `s|/old/path|/new/path|g` 처럼 `|` 를 쓰면 이스케이프가 필요 없어 읽기 좋습니다.",
        P("printf 'a old b old\\n' | sed 's/old/new/g'","a new b new")),
      C("필드","<code>awk '{print $1}'</code> 이 출력하는 것은?",
        ["입력으로 들어온 맨 첫 번째 줄 전체","각 줄의 첫 번째 필드","인자로 준 첫 번째 파일","첫 글자"],1,
        "`$1` 은 **각 줄의 첫 번째 필드**입니다. `$0` 은 줄 전체입니다.\n기본 구분자는 공백이고 **연속된 공백을 하나로** 취급하므로, 정렬이 어긋난 로그에도 `cut` 보다 강합니다.\n다른 구분자는 `-F,` 처럼 지정합니다 — CSV 를 다룰 때 자주 씁니다."),
      C("조건","<code>awk '$9 &gt;= 500'</code> 이 하는 일은?",
        ["9번째 줄을 출력한다","9번째 필드가 500 이상인 줄을 출력한다","맨 처음부터 500번째 줄까지 차례로 출력한다","9번째 필드의 값과 500 을 더해서 출력한다"],1,
        "패턴만 쓰고 동작을 생략하면 **조건에 맞는 줄 전체를 출력**합니다.\n웹 로그에서 9번째 필드가 상태 코드인 형식이 많아, 5xx 오류만 골라내는 관용구로 쓰입니다.\n`awk '$9 >= 500 {print $7}'` 처럼 동작을 붙이면 그 요청 경로만 뽑을 수 있습니다."),
      C("합계","<code>awk '{s+=$1} END {print s}'</code> 의 결과는?",
        ["맨 첫 줄에 들어 있는 첫 번째 필드의 값","모든 줄의 첫 번째 필드를 더한 값","줄 수","모든 줄의 첫 번째 필드 중 가장 큰 값"],1,
        "`END` 블록은 **모든 줄을 처리한 뒤 한 번** 실행됩니다. 그래서 합계·평균·최댓값을 낼 수 있습니다.\n변수는 선언 없이 쓸 수 있고 초기값은 0이라 `s+=$1` 이 바로 동작합니다.\n평균은 `print s/NR` 인데, `NR` 이 마지막 줄 번호이자 전체 줄 수이기 때문입니다."),
      C("JSON 로그","JSON 로그를 다룰 때 sed·awk 보다 나은 도구는?",
        ["grep 으로 문자열을 찾는다","jq","cut","tr"],1,
        "`jq` 는 **JSON 구조를 이해**합니다. 중첩과 이스케이프가 있는 데이터를 정규식으로 자르는 것은 언젠가 반드시 깨집니다.\n`jq '.level, .msg'` 처럼 필드를 뽑고, `select` 로 거르고, 다시 텍스트로 내보낼 수 있습니다.\n'구조가 있는 데이터에는 구조를 아는 도구를 쓴다' 는 원칙은 HTML·XML·YAML 에도 똑같이 적용됩니다.")
    ] }
]},

/* ═══════════════ 유닛 6 ═══════════════ */
{ t:"셸 스크립트 — 반복을 자동으로", l:[

  { t:"안전한 스크립트의 첫 세 줄", xp:30,
    th:{ sum:"셸 스크립트의 기본값은 **위험하다**. 명령이 실패해도 다음 줄로 넘어가고, 없는 변수는 빈 문자열이 된다. 그래서 첫 줄에 안전장치를 건다.",
      body:[
        {h:"set -euo pipefail", t:"`-e` 는 명령이 실패하면 **즉시 중단**, `-u` 는 **없는 변수를 쓰면 오류**, `-o pipefail` 은 **파이프 중간이 실패해도 실패로** 친다. 이 셋이 없으면 `rm -rf \"$DIR/\"` 에서 `DIR` 이 비었을 때 `rm -rf /` 가 되는 종류의 사고가 난다. 실제로 이 사고로 스팀 클라이언트가 사용자 홈을 지운 일이 있었다."},
        {h:"변수는 항상 따옴표로", t:"`\"$file\"` 처럼 감싸지 않으면 공백이 든 이름에서 인자가 갈라진다. `$@` 도 `\"$@\"` 로 써야 각 인자가 그대로 전달된다. `${VAR:-기본값}` 으로 기본값을, `${VAR:?메시지}` 로 '없으면 오류' 를 표현할 수 있다."}],
      code:{ c:"#!/usr/bin/env bash\nset -euo pipefail\nIFS=$'\\n\\t'\n\nDIR=\"${1:?사용법: $0 <디렉터리>}\"\nfor f in \"$DIR\"/*.log; do\n  [ -e \"$f\" ] || continue    # 하나도 없을 때\n  echo \"처리: $f\"\ndone", cap:"첫 두 줄이 사고의 절반을 막는다" },
      key:["set -euo pipefail 을 먼저 쓴다","변수는 언제나 \"$var\"","${VAR:?메시지} 로 필수 인자 강제","glob 이 안 맞을 때를 처리한다"] },
    q:[
      I("실행 과정 보기","스크립트가 실행하는 명령을 하나씩 출력하게 하는 옵션은? (set 뒤 옵션만)",
        ["-x","-o xtrace"],
        "정답은 `set -x` 입니다. 실행되는 명령이 변수까지 펼쳐진 상태로 출력됩니다.\n'어디까지 갔고 변수에 무엇이 들어 있었는지' 가 한눈에 보여, 셸 스크립트 디버깅에서 가장 먼저 켜는 옵션입니다. 끝나면 `set +x` 로 끕니다."),
      C("set -e","<code>set -e</code> 가 하는 일은?",
        ["오류가 났을 때 메시지를 훨씬 더 자세히 보여 준다","명령이 실패하면 스크립트를 즉시 중단한다",
         "실행 과정을 출력한다","쓰지 않은 변수를 알아서 자동으로 선언해 준다"],1,
        "기본 셸은 명령이 실패해도 **아무 일 없다는 듯 다음 줄로** 갑니다. 백업이 실패했는데 그 다음 줄에서 원본을 지우는 사고가 여기서 납니다.\n`set -e` 는 그 순간 멈춥니다. 실행 과정을 보여 주는 것은 `set -x` 로 다른 옵션입니다.\n다만 `if` 조건이나 `||` 뒤의 실패는 중단하지 않으므로, 의도적으로 실패를 허용할 자리는 그렇게 씁니다."),
      C("set -u","<code>set -u</code> 를 켜면 무엇이 달라지나?",
        ["크기가 0인 빈 파일을 알아서 건너뛴다","선언되지 않은 변수를 쓰면 오류로 멈춘다","출력이 사라진다","실행하기 전에 대상이 되는 파일의 권한을 미리 검사한다"],1,
        "오타 난 변수 이름이 **조용히 빈 문자열**이 되는 것을 막습니다. `rm -rf \"$DIRR/\"` 같은 사고의 직접적 예방책입니다.\n기본값이 필요한 자리는 `${VAR:-기본값}` 으로 명시하면 `-u` 와 함께 쓸 수 있습니다.\n`$@` 를 쓸 때 인자가 하나도 없으면 오류가 나므로, 오래된 bash 에서는 `${@:-}` 로 감싸기도 합니다."),
      I("안전장치","셸 스크립트 맨 앞에 관례적으로 넣는 안전 옵션 한 줄은? (set 부터)",
        ["set -euo pipefail","set -eu -o pipefail"],
        "정답은 `set -euo pipefail` 입니다.\n`-e` 실패 시 중단, `-u` 없는 변수 오류, `pipefail` 파이프 중간 실패도 실패로. 이 한 줄이 셸 스크립트 사고의 상당 부분을 막아 줍니다."),
      C("인자 분리","<code>rm $file</code> 에서 <code>file</code> 이 <code>My Doc.txt</code> 이면?",
        ["정상 삭제된다","My 와 Doc.txt 두 개를 지우려 해서 실패하거나 엉뚱한 것을 지운다",
         "셸이 알아서 따옴표를 붙여 주기 때문에 공백이 있어도 하나의 이름으로 다뤄진다","해당 인자가 오류 없이 조용히 무시된다"],1,
        "셸이 공백에서 **인자를 나눕니다.** 그래서 `rm My Doc.txt` 가 되어 두 파일을 지우려 합니다.\n최악은 우연히 `My` 라는 파일이 존재하는 경우입니다 — 조용히 엉뚱한 것이 사라집니다.\n예외 없이 `\"$file\"` 로 감싸는 것이 유일한 방어책입니다."),
      C("종료 코드","이전 명령의 성공 여부를 담고 있는 변수는?",
        ["$0","$?","$! (마지막 백그라운드 PID)","$#"],1,
        "`$?` 가 **직전 명령의 종료 코드**입니다. 0이 성공, 나머지가 실패입니다.\n`$0` 은 스크립트 이름, `$#` 은 인자 개수, `$!` 는 마지막 백그라운드 작업의 PID 입니다.\n종료 코드를 확인해 분기하는 것이 스크립트의 기본 제어 흐름이고, `if 명령; then` 이 그 축약형입니다."),
      C("shebang","<code>#!/usr/bin/env bash</code> 를 <code>#!/bin/bash</code> 보다 선호하는 이유는?",
        ["더 빨라서","PATH 에서 bash 를 찾으므로 설치 위치가 달라도 동작해서",
         "실행할 때 별도 권한이 필요 없어서","어차피 주석으로 처리되는 줄이라서 셸이 그냥 무시하고 넘어가기 때문에"],1,
        "macOS 나 BSD 계열에서 bash 가 `/bin` 이 아닌 곳에 있을 수 있습니다. `env` 가 `PATH` 에서 찾아 주므로 이식성이 좋습니다.\n반대로 `PATH` 를 신뢰할 수 없는 환경에서는 절대 경로가 더 안전하다는 반론도 있습니다.\n어느 쪽이든 shebang 이 없으면 실행하는 셸에 따라 동작이 달라지므로, 반드시 적어야 합니다."),
      C("검사 도구","셸 스크립트의 흔한 실수를 자동으로 잡아 주는 도구는?",
        ["shellcheck","bashfmt 라는 정렬 도구","shlint","checkbash 라는 검사 도구"],0,
        "`shellcheck` 는 따옴표 누락, 쓰이지 않는 변수, 이식성 문제를 지적해 줍니다.\n에디터 플러그인과 CI 양쪽에서 쓰이며, 지적마다 위키 설명 링크가 붙어 배우기에도 좋습니다.\n셸 스크립트를 쓴다면 가장 먼저 붙여야 할 도구입니다 — 사람이 놓치는 것을 정확히 잡습니다.")
    ] },

  { t:"반복과 판단 — 스크립트가 프로그램이 되는 곳", xp:30,
    th:{ sum:"반복문과 조건문이 붙으면 스크립트는 프로그램이 된다. 다만 셸은 **모든 것을 문자열로** 다루므로 비교 문법이 독특하다.",
      body:[
        {h:"[ 는 명령이다", t:"`if [ \"$a\" = \"$b\" ]` 에서 `[` 는 문법이 아니라 **`test` 라는 프로그램**이다. 그래서 앞뒤 공백이 반드시 필요하고, 인자가 비면 문법 오류가 난다 — 따옴표가 필요한 또 하나의 이유다. bash 에서는 `[[ … ]]` 가 더 안전하고 `&&`·정규식도 쓸 수 있다. 숫자 비교는 `-eq`·`-lt`, 문자열은 `=`·`!=` 로 갈린다."},
        {h:"파일을 순회할 때의 함정", t:"`for f in $(ls)` 는 공백에서 잘려 깨진다. `for f in *.log` 가 올바른데, **맞는 파일이 없으면 패턴 자체가 그대로** 들어오므로 `[ -e \"$f\" ] || continue` 로 걸러야 한다. 파일이 아주 많거나 깊이 있는 경우에는 `find … -print0 | while IFS= read -r -d '' f` 형태가 안전하다."}],
      code:{ c:"for f in *.log; do\n  [ -e \"$f\" ] || continue\n  if [ \"$(wc -l < \"$f\")\" -gt 1000 ]; then\n    echo \"$f: 큼\"\n  fi\ndone", cap:"숫자 비교는 -gt, 문자열은 =" },
      key:["[ 는 프로그램이라 공백이 필요","bash 라면 [[ … ]] 가 안전","숫자는 -eq/-lt, 문자열은 =/!=","glob 이 안 맞을 때를 처리한다"] },
    q:[
      I("인자 개수","셸 스크립트에서 전달된 인자의 개수가 담긴 변수는? (달러 포함)",
        ["$#"],
        "정답은 `$#` 입니다. 인자 검증의 첫 줄에 자주 쓰입니다.\n`if [ $# -lt 1 ]; then echo \"사용법: $0 <경로>\"; exit 1; fi` 형태가 관용구입니다. `$0` 은 스크립트 이름이고, `$@` 는 인자 전체입니다."),
      C("대괄호","<code>if [ \"$a\" = \"$b\" ]</code> 에서 <code>[</code> 의 정체는?",
        ["셸이 알아서 해석하는 문법 기호","test 라는 이름의 명령","그 줄을 통째로 주석으로 만드는 기호","변수 선언"],1,
        "`[` 는 **`test` 와 같은 명령**입니다. `/usr/bin/[` 파일이 실제로 존재합니다.\n명령이므로 앞뒤에 공백이 필요하고, `[\"$a\"` 처럼 붙여 쓰면 그런 명령이 없다는 오류가 납니다.\nbash 전용이라면 `[[ … ]]` 가 낫습니다 — 따옴표를 빠뜨려도 덜 위험하고 `&&`·`=~` 를 쓸 수 있습니다."),
      C("숫자 비교","두 수의 크기를 비교하는 올바른 표기는?",
        ["[ \"$n\" > 10 ] 으로 비교","[ \"$n\" -gt 10 ]","[ \"$n\" >> 10 ] 과 같이 써서 크기를 비교","[ $n gt 10 ]"],1,
        "`-gt`(greater than)입니다. `>` 는 `[ ]` 안에서 **리다이렉션으로 해석**되어 `10` 이라는 파일을 만들어 버립니다.\n문자열 비교는 반대로 `=`·`!=` 를 씁니다 — 이 둘을 헷갈리면 `\"10\" < \"9\"` 같은 결과가 나옵니다.\n`(( n > 10 ))` 로 산술 문맥을 쓰면 익숙한 기호를 그대로 쓸 수 있습니다."),
      C("파일 순회","파일 목록을 순회하는 올바른 방법은?",
        ["for f in $(ls)","for f in *.log","for f in `ls -1`","while ls | read f 로 한 줄씩"],1,
        "`for f in *.log` 는 **셸이 직접 펼치므로** 공백이 든 이름도 안전합니다.\n`$(ls)` 는 출력을 문자열로 받아 공백에서 잘립니다 — 널리 퍼진 잘못된 관용구입니다.\n맞는 파일이 없으면 `*.log` 가 글자 그대로 들어오므로 `[ -e \"$f\" ] || continue` 를 함께 씁니다."),
      I("존재 확인","파일이 존재하는지 검사하는 test 연산자는? (대시 포함 두 글자)",
        ["-e","-f"],
        "정답은 `-e`(존재)입니다. `-f`(일반 파일)·`-d`(디렉터리)·`-r`(읽기 가능)·`-s`(비어 있지 않음)도 함께 자주 씁니다.\n'디렉터리도 통과시켜도 되는가' 를 생각해 `-e` 와 `-f` 중에 고르는 것이 정확합니다."),
      C("함수","셸 함수에서 값을 돌려주는 관례적인 방법은?",
        ["return 문에 문자열을 실어서 호출한 쪽으로 돌려준다","echo 로 출력하고 호출자가 $(…) 로 받는다",
         "전역 변수를 통해서만 값을 주고받을 수 있다","불가능하다"],1,
        "`return` 은 **0~255 종료 코드**만 돌려줍니다. 문자열은 `echo` 로 출력하고 `result=$(myfunc)` 로 받습니다.\n그래서 함수 안에서 디버그 메시지를 `echo` 로 찍으면 **반환값에 섞입니다** — 그럴 때는 `>&2` 로 표준 오류에 보내야 합니다.\n이 규칙을 모르면 '함수가 이상한 값을 돌려준다' 는 버그를 오래 헤매게 됩니다."),
      C("반복 대안","파일 수만 줄이고 같은 명령을 여러 번 실행할 때 for 문보다 나은 것은?",
        ["while true 로 무한히 반복","find … -exec 명령 {} +","반복문을 쓰지 않고 명령을 하나씩 수동으로 실행","alias"],1,
        "`{} +` 는 **가능한 만큼 인자를 모아** 명령을 적게 실행합니다. 파일이 수만 개일 때 속도 차이가 큽니다.\n`{} \\;` 는 파일마다 프로세스를 하나씩 띄워 훨씬 느립니다.\n`xargs` 도 같은 최적화를 하며, 병렬 실행(`-P`)까지 지원합니다."),
      C("cron 함정","cron 에서만 스크립트가 실패하는 가장 흔한 원인은?",
        ["cron 데몬의 처리가 느려서","PATH 와 환경 변수가 로그인 셸과 달라서","권한이 없어서","cron 이 bash 실행 파일의 위치를 찾지 못해서"],1,
        "cron 은 **최소한의 환경**으로 실행합니다. `.bashrc` 를 읽지 않아 `PATH` 가 짧고, 언어 설정·가상환경도 없습니다.\n그래서 '터미널에서는 되는데 cron 에서는 안 된다' 가 생깁니다.\n해결은 스크립트 안에서 **절대 경로를 쓰고 필요한 환경 변수를 직접 설정**하는 것입니다. 출력을 로그로 남겨 두면 원인 파악이 훨씬 빨라집니다.")
    ] }
]}

];

module.exports=shuffleUnits(UNITS);
