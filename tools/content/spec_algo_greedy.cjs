module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 그리디와 정렬 기준 설계",
  source: "./algo_greedy.cjs",
  lessons: [
    { t: "구간 문제 — 무엇으로 정렬하느냐가 답을 정한다", n: 6,
      th: { sum: "구간 문제의 그리디는 코드가 아니라 **정렬 기준**에서 갈린다. 기준이 틀리면 코드가 아무리 깔끔해도 답이 틀린다.",
        body: [
          { h: "겹치지 않게 최대로 고를 때는 '끝나는 순'", t: "시작이 이른 순으로 고르면 `[1,10]` 같은 긴 구간 하나가 뒤를 통째로 막는다. 짧은 순으로 고르면 가운데 낀 짧은 구간이 양옆 두 개를 동시에 막는다. **끝나는 시각이 이른 것부터** 고르면 뒤에 남는 시간이 가장 길어지고, 이것이 최적임은 교환 논증으로 증명된다 — 최적해의 첫 구간을 가장 빨리 끝나는 것으로 바꿔도 개수는 줄지 않는다." },
          { h: "겹친 개수를 세는 문제로 바꾼다", t: "회의실 개수는 '어느 회의를 어느 방에' 가 아니라 **같은 순간에 겹친 최대 개수**다. 시작과 끝을 따로 정렬해 훑으면 짝을 맞추지 않고도 답이 나온다. 문제를 **세는 문제**로 다시 쓰는 순간 구현이 절반으로 줄어드는 일이 자주 있다 — 답에 필요 없는 정보를 먼저 버려라." }],
        code: { c: "const sorted = jobs.slice().sort((a, b) => a[1] - b[1]);\nif (s >= free) { taken++; free = e; }", cap: "끝나는 순 — 뒤에 남는 시간이 가장 길다" },
        key: ["최대 개수는 끝나는 순", "최소 화살·최소 제거도 같은 골격", "겹침 세기는 시작·끝 따로 정렬", "등호를 붙일지가 규칙을 정한다"] } },
    { t: "그리디가 옳은지 따져 보기", n: 6,
      th: { sum: "그리디는 **짧아서 먼저 손이 가고, 반례를 안 찾으면 대부분 맞고 특정 입력에서만 조용히 틀린다.** 쓰기 전에 왜 옳은지 한 번은 세워 봐야 한다.",
        body: [
          { h: "교환 논증 — 최적해를 내 선택으로 바꿔 본다", t: "'최적해가 있다고 하자. 그 첫 선택을 내 그리디 선택으로 **바꿔도 손해가 아니다**' 를 보이면 그리디가 옳다. 쿠키는 덜 까다로운 아이에게 작은 것을 주어도 손해가 아니고, 분수 배낭은 단위 가치가 높은 것을 먼저 담아도 손해가 아니다. 이 문장을 못 만들면 그리디를 쓰면 안 된다." },
          { h: "조건 한 줄이 바뀌면 무너진다", t: "분수 배낭은 그리디가 최적이지만 **쪼갤 수 없으면**(0/1 배낭) 무너져 DP 로 가야 한다. 주식은 횟수 제한이 없으면 오르는 구간을 다 먹으면 되지만 **두 번까지**로 제한되면 DP 다. 동전은 원화·달러 같은 정준 동전계에서만 그리디가 최적이고 `[1,3,4]` 에서는 틀린다. **알고리즘의 성질이 아니라 입력에 달린 성질**이라는 점이 함정이다." }],
          code: { c: "greedyCoins([1,3,4], 6)  // 4+1+1 → 3개\n// 최적은 3+3 → 2개", cap: "그리디가 조용히 틀리는 자리" },
        key: ["교환 논증을 세워 보고 쓴다", "쪼갤 수 있는가 / 횟수 제한이 있는가", "동전계가 정준인지 확인", "반례를 못 찾으면 완전탐색과 대조한다"] } },
  ],
};
