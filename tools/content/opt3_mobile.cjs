/* mobile — 정답률 15% 아래 끝맺음의 오답 19개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "mobile", fixes: [
  { k: "mob-idempotent", from: "중복이 생기지 않는다", to: "비동기성(요청을 보내고 기다리지 않음) 요청을 보내고 응답을 기다리지 않으면 중복은 서버가 걸러 낸다" },
  { k: "mob-etag", from: "별도 Authorization 헤더가 필요 없다", to: "조건부 요청이 붙으면 인증 절차까지 자동으로 처리된다 — ETag 가 세션 토큰 역할을 하므로 Authorization 헤더는 생략된다" },
  { k: "mob-runtime-perm", from: "거부 상태를 처리할 필요가 없다", to: "앱이 코드로 권한을 강제 승인할 수 있다 — 사용자가 거부해도 시스템 API 로 다시 켤 수 있으므로 거부 상태는 코드로 덮어쓰면 된다" },
  { k: "mob-perm-timing", from: "흐름이 끊기지 않기 때문이다", to: "권한을 받지 않고 몰래 카메라를 사용 필요할 때 바로 쓸 수 있어 흐름이 끊기지 않아서다" },
  { k: "화면이 넓어지면", from: "따로 만드는 것 말고는 방법이 없다", to: "태블릿용 화면을 처음부터 따로 만들어야 한다" },
  { k: "누르는 도중에 마음이 바뀌면", from: "제공하는 것 말고는 방법이 없다", to: "실행 후 되돌리기 버튼을 두어 사후에 취소하게 한다" },
  { k: "충분히 진한가", from: "색 대비는 따지지 않아도 된다", to: "글자 크기가 충분히 크면 색 대비는 기준에서 빠진다" },
  { k: "03/04 는 언제인가", from: "앱이 관여할 수 없기 때문이다", to: "날짜 형식은 사용자가 설정에서 고르는 것이므로 앱이 관여할 여지가 없어서다" },
  { k: "정렬 순서", from: "서버에서 하면 이 문제가 생기지 않는다", to: "정렬은 서버에서 하면 언어 차이가 사라진다" },
  { k: "버전 코드와 버전 이름", from: "헷갈리는 일은 생기지 않는다", to: "버전 이름만 2.2.9 로 되돌리면 되고 버전 코드는 아무 값이나 상관없다 스토어는 버전 이름만 보고 순서를 판단하므로 코드 값은 자유롭게 정해도 된다 코드 값이 작아져도 스토어가 순서를 헷갈리는 일은 막혀 있다" },
  { k: "강제 업데이트 게이트", from: "앱은 아무것도 하지 않아도 된다", to: "스토어의 '강제 업데이트' 설정을 켜면 OS 가 알아서 막아 주므로 앱 쪽 코드는 비워 두면 된다" },
  { k: "링크가 앱으로 열리려면", from: "설정을 건드리지 않아도 된다", to: "패턴 등록만으로 충분하며, 열리지 않는다면 앱을 다시 설치하면 해결된다 재설치하면 OS 가 패턴을 다시 읽어 등록하므로 그때부터 앱이 열린다 검증 파일이나 지문을 따로 준비할 필요는 없고, 설정도 그대로 두면 된다" },
  { k: "링크로 들어온 사용자의 뒤로 가기", from: "스택은 건드릴 필요가 없다", to: "뒤로 가기 버튼을 가로채 홈으로 보내면 해결된다 — 스택은 그대로 두면 된다" },
  { k: "실험실 수치와 실제 수치", from: "상위 백분위는 신경 쓰지 않아도 된다", to: "평균값이 기준이므로 평균이 목표 안에 들면 상위 백분위는 참고 자료일 뿐이다" },
  { k: "직접 보낼 수 없다", from: "알림 내용을 암호화해 주기 때문", to: "플랫폼 푸시 서비스가 알림 내용을 암호화해 주어서" },
  { k: "신호와 데이터", from: "데이터를 조회하지 않아도 된다", to: "서버 부하가 줄어든다 — 알림을 만들 때 데이터 조회 단계가 빠진다" },
  { k: "두 번 도착하면", from: "서버는 손대지 않아도 된다", to: "알림 도착 시각을 비교해 짧은 간격의 중복을 걸러 낸다. 도착 간격이 짧으면 같은 알림으로 보고 뒤에 온 것을 버린다. 서버는 그대로 둔다" },
  { k: "많이 보낼수록", from: "응답이 느려질 수 있기 때문이다", to: "서버 부하다 — 발송이 몰리면 응답이 느려질 수 있어서다" },
  { k: "테스트하기", from: "드물게 일어나므로 검사할 필요가 없다", to: "충돌은 드물게 일어나므로 검사 대상에서 뺀다" }
] };
