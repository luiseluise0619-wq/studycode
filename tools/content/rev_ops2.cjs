/* 코드 리뷰 16문항 — review 가 0이던 여덟 트랙 중 넷(dbt·dl·mobile·arduino)에 4개씩.
   보기 순서는 미리 배분해 흩어 두었다 — 결함이 늘 같은 자리면 읽지 않아도 골라진다. */
module.exports = [

/* ══════════════ dbt ══════════════ */
{
  t: "review", k: "마트 모델 SQL", cat: "review", d: 3, track: "dbt",
  q: "이 dbt 모델의 결함을 모두 고르세요",
  code: "-- models/marts/fct_orders.sql\n{{ config(materialized='table') }}\n\nselect *\nfrom analytics.stg_orders o\nleft join analytics.stg_customers c on o.customer_id = c.id\nwhere o.created_at >= '2026-01-01'",
  items: [
    { txt: "상류 모델을 스키마째 직접 적어 참조했다. <code>ref()</code> 를 쓰지 않으면 dbt 가 모델 사이의 의존을 모르므로 실행 순서를 잡지 못하고, 개발 환경으로 바꿔 돌릴 수도 없다.", bad: true },
    { txt: "<code>left join</code> 이라 짝이 없는 고객의 주문도 남는다 — 사실 테이블에서는 대개 그편을 원하므로 이 선택 자체는 자연스럽다.", bad: false },
    { txt: "<code>select *</code> 라 두 테이블의 열이 통째로 딸려 오고, <code>id</code> 나 <code>created_at</code> 처럼 이름이 겹치는 열이 어느 쪽인지 알 수 없다. 상류에 열이 하나 늘면 하류가 조용히 바뀐다.", bad: true },
    { txt: "<code>materialized='table'</code> 은 돌 때마다 표를 통째로 다시 만든다 — 행이 수억 개로 늘면 시간과 비용이 함께 커지므로, 이 표를 증분으로 바꿀지는 조회 빈도와 비용을 함께 보고 정한다.", bad: false },
    { txt: "별칭을 <code>o</code>, <code>c</code> 처럼 한 글자로 붙였다 — 조인이 늘어나면 읽기 어려워지므로 뜻이 있는 이름을 쓰는 편이 낫다.", bad: false },
    { txt: "시작 날짜를 문자열로 박아 두어 이 모델은 그 날짜 이후만 담는다. 기준이 필요하면 변수로 빼야 하고, 그러지 않으면 아무도 모르는 사이에 잘린 표가 된다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b><code>ref()</code> 없는 직접 참조</b> — dbt 의 값어치 절반이 의존 그래프에서 나오는데, 스키마를 손으로 적으면 그 그래프가 끊긴다. 실행 순서도, 환경 분리도, 계보 문서도 함께 사라진다. (2) <b><code>select *</code></b> — 상류의 변화가 하류로 그대로 새어 나가고, 이름이 겹치는 열은 어느 쪽인지 알 수 없다. (3) <b>박아 둔 날짜</b> — 오늘은 맞지만 내년에는 조용히 틀린 표가 된다.\n디스트랙터: <code>left join</code> 은 사실 테이블에서 흔하고 옳은 선택이다. 재료화 방식과 별칭 길이는 손볼 값어치가 있지만 <b>지금 결과를 틀리게 만드는 것</b>은 아니다 — 리뷰에서는 이 둘을 나눠 적어야 상대가 무엇부터 고칠지 안다.",
},
{
  t: "review", k: "스키마 테스트 정의", cat: "review", d: 3, track: "dbt",
  q: "이 <code>schema.yml</code> 의 결함을 모두 고르세요",
  code: "models:\n  - name: fct_orders\n    columns:\n      - name: order_id\n        tests:\n          - not_null:\n              severity: warn\n      - name: amount\n        tests:\n          - dbt_utils.accepted_range:\n              min_value: 0",
  items: [
    { txt: "금액의 범위 검사에 최댓값이 없어 비정상적으로 큰 값은 걸리지 않는다 — 상한을 정하기 어려운 값이라면 아래쪽만 막아 두는 것도 하나의 선택이다.", bad: false },
    { txt: "기본키에 <code>unique</code> 검사가 없다. 상류의 조인이 한 줄을 여러 줄로 늘려도 아무 신호가 없고, 그 결과 모든 합계가 조용히 부풀어 오른다.", bad: true },
    { txt: "테스트가 마트 모델에만 붙어 있다 — 원천 가까이에서 먼저 걸러 주면 원인을 좁히기 쉬워지지만, 마트에 붙인 것 자체가 잘못은 아니다.", bad: false },
    { txt: "<code>severity: warn</code> 이라 검사가 깨져도 파이프라인이 초록불로 지나간다. 경고는 결국 아무도 보지 않으므로, 키에 대한 검사라면 실패로 두어야 뜻이 있다.", bad: true },
    { txt: "열에 설명이 없어 생성되는 문서가 비어 있다 — 카탈로그의 값어치는 떨어지지만 데이터가 틀리지는 않는다.", bad: false },
  ],
  ex: "결함 2가지. (1) <b>키에 <code>unique</code> 가 없다</b> — <code>not_null</code> 하나로는 '값이 있다' 까지만 지킨다. 데이터 파이프라인에서 가장 흔하고 가장 늦게 발견되는 사고가 조인으로 인한 행 증식이고, 그것을 잡는 검사가 바로 유일성이다. (2) <b>경고로 낮춘 심각도</b> — 막지 않는 검사는 없는 검사와 같다. 깨져도 배포가 지나가면 아무도 고치지 않는다.\n디스트랙터: 범위 상한, 테스트를 붙인 층, 문서용 설명은 모두 더 좋아질 여지이지 지금 데이터를 틀리게 만드는 것은 아니다. 다만 원천 쪽 검사는 원인을 좁히는 데 도움이 되므로 다음 단계로 적어 둘 값어치가 있다.",
},
{
  t: "review", k: "증분 모델 설정", cat: "review", d: 4, track: "dbt",
  q: "이 증분 모델의 결함을 모두 고르세요",
  code: "{{ config(materialized='incremental') }}\n\nselect event_id, user_id, event_at, payload\nfrom {{ ref('stg_events') }}\n{% if is_incremental() %}\nwhere event_at > (select max(event_at) from {{ this }})\n{% endif %}",
  items: [
    { txt: "<code>payload</code> 를 통째로 담아 표가 빠르게 커진다 — 필요한 필드만 뽑아 두면 저장과 조회가 모두 가벼워지지만, 지금 값이 틀리는 이유는 아니다.", bad: false },
    { txt: "<code>unique_key</code> 를 정하지 않아 같은 이벤트가 다시 들어오면 중복으로 쌓인다. 키가 없으면 dbt 는 갱신할 자리를 찾지 못하고 덧붙이기만 한다.", bad: true },
    { txt: "<code>is_incremental()</code> 로 감싼 것은 옳다 — 처음 만들 때는 전체를 읽고 그 뒤로는 새 것만 읽으므로, 같은 파일 하나로 두 상황을 다 다룬다.", bad: false },
    { txt: "경계를 <code>&gt;</code> 로만 잘라 늦게 도착한 이벤트를 영영 놓친다. 며칠쯤 겹쳐 다시 훑도록 <code>max(event_at) - interval '3 day'</code> 처럼 여유를 두어야 한다.", bad: true },
    { txt: "<code>on_schema_change</code> 를 정하지 않았다. 기본 동작이 무시라, 상류에 열이 늘어도 증분 표에는 들어오지 않고 그 사실이 아무 데도 남지 않는다.", bad: true },
    { txt: "전체를 다시 만들고 싶으면 <code>--full-refresh</code> 가 있다 — 증분이어도 되돌릴 길이 있다는 뜻이므로, 증분 설정 자체를 위험하다고 볼 자리는 아니다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b><code>unique_key</code> 없음</b> — 상류가 같은 이벤트를 다시 보내는 일은 흔한데, 키가 없으면 갱신이 아니라 덧붙이기가 되어 중복이 쌓인다. (2) <b>겹침 없는 경계</b> — 이벤트는 만들어진 시각과 도착한 시각이 다르다. 도착이 늦은 것을 잡으려면 며칠 겹쳐 다시 읽고, 키로 갱신해 중복을 지운다. (3) <b>스키마 변경 처리 미지정</b> — 상류가 열을 늘려도 조용히 빠진 채로 몇 달이 지난다.\n디스트랙터: <code>payload</code> 를 통째로 담는 것과 <code>--full-refresh</code> 는 성능과 운영의 이야기다. <code>is_incremental()</code> 로 감싼 것은 <b>옳게 쓴 부분</b>이라 지적하면 안 된다 — 잘된 것을 결함으로 적으면 나머지 지적까지 힘을 잃는다.",
},
{
  t: "review", k: "프로젝트 기본 설정", cat: "review", d: 3, track: "dbt",
  q: "이 프로젝트 설정과 원천 정의의 결함을 모두 고르세요",
  code: "# dbt_project.yml\nmodels:\n  shop:\n    +materialized: view\n    +schema: analytics      # 개발·운영 공통\n\n# sources.yml\nsources:\n  - name: raw\n    tables:\n      - name: orders\n      - name: customers",
  items: [
    { txt: "모든 모델을 뷰로 두면 마트를 한 번 조회할 때마다 그 아래 사슬이 통째로 다시 계산된다. 자주 읽는 마트는 표로 굳혀 두어야 한다.", bad: true },
    { txt: "스테이징을 뷰로 두는 것은 흔한 선택이다 — 얇은 이름 정리와 형 변환뿐이라 굳혀 둘 값어치가 적다.", bad: false },
    { txt: "원천에 신선도 기준이 없어 상류 적재가 멈춰도 파이프라인은 옛 데이터로 초록불을 낸다. 늦은 데이터는 틀린 데이터보다 알아채기 어렵다.", bad: true },
    { txt: "원천 테이블에 설명이 없다 — 문서를 채우면 카탈로그가 쓸모 있어지지만 지금 파이프라인이 잘못 도는 이유는 아니다.", bad: false },
    { txt: "개발과 운영이 같은 스키마를 쓴다. 누군가 자기 브랜치에서 돌린 실행이 운영 표를 그대로 덮으므로, 대상 스키마를 환경마다 갈라야 한다.", bad: true },
    { txt: "모델 폴더가 평평해서 어느 것이 스테이징이고 어느 것이 마트인지 이름으로만 짐작한다 — 층을 나눠 두면 읽기 좋지만 동작에는 영향이 없다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>전부 뷰</b> — 뷰는 저장하지 않는 대신 볼 때마다 계산한다. 층이 다섯 겹이면 대시보드 한 번에 다섯 겹이 다시 돈다. (2) <b>신선도 기준 없음</b> — 파이프라인이 성공했다는 말과 데이터가 최신이라는 말은 다르다. 상류가 멈추면 어제 것으로 오늘의 보고서가 나간다. (3) <b>환경이 갈리지 않았다</b> — 개발자의 실행이 운영을 덮는 구조는 사고가 나기를 기다리는 설정이다.\n디스트랙터: 스테이징을 뷰로 두는 것은 오히려 정석이고, 원천 설명과 폴더 구조는 읽기의 문제다. 앞의 셋은 <b>지금 새고 있는 구멍</b>이고 이 셋은 정리하며 고칠 것이라, 리뷰에서는 순서를 함께 말해야 한다.",
},

/* ══════════════ dl ══════════════ */
{
  t: "review", k: "학습 루프", cat: "review", d: 3, track: "dl",
  q: "이 학습 루프의 결함을 모두 고르세요",
  code: "for epoch in range(30):\n    total = 0\n    for x, y in train_loader:\n        out = model(x)\n        loss = criterion(out, y)\n        loss.backward()\n        optimizer.step()\n        total += loss\n    print(epoch, total / len(train_loader))\n    torch.save(model.state_dict(), \"ckpt.pt\")",
  items: [
    { txt: "30에폭을 고정으로 돌린다 — 검증 점수가 더 좋아지지 않을 때 멈추는 장치를 두면 시간도 전기도 아낄 수 있지만, 이 코드가 잘못 배우는 이유는 아니고 적절한 에폭 수는 곡선을 보고 정한다.", bad: false },
    { txt: "<code>optimizer.zero_grad()</code> 가 없어 기울기가 배치마다 계속 더해진다. 앞선 배치의 기울기가 섞인 방향으로 걸음을 내딛으므로 손실이 널뛰고 결과도 재현되지 않는다.", bad: true },
    { txt: "<code>total += loss</code> 는 텐서를 그대로 더해 계산 그래프를 붙들고 있다. 에폭이 갈수록 메모리가 쌓여 결국 터지므로 <code>loss.item()</code> 을 더해야 한다.", bad: true },
    { txt: "체크포인트를 같은 이름으로 매번 덮어쓴다 — 가장 좋은 것만 남기려면 조건이 필요하지만, 마지막 상태를 남기는 것도 하나의 방침이다.", bad: false },
    { txt: "손실을 배치 개수로 나눠 평균을 낸다 — 마지막 배치가 작으면 조금 어긋나지만 경향을 읽는 데는 지장이 없다.", bad: false },
    { txt: "검증이 전혀 없다. 학습 손실만 보고 30에폭을 돌리면 어디서부터 과적합이 시작됐는지 알 길이 없고, 저장한 체크포인트가 좋은 것인지도 모른다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>기울기를 비우지 않았다</b> — 프레임워크는 기울기를 더해 쌓도록 만들어져 있어(그것이 필요한 상황이 있다) 비우는 것은 우리 몫이다. 빠뜨리면 학습이 되는 것처럼 보이면서 조용히 망가진다. (2) <b>텐서를 그대로 누적</b> — 손실 텐서에는 그래프가 달려 있어 더하는 순간 그 그래프가 통째로 살아남는다. (3) <b>검증 없음</b> — 학습 손실은 언제나 내려가므로, 그것만 보는 것은 아무것도 보지 않는 것과 같다.\n디스트랙터: 고정 에폭 수, 체크포인트 덮어쓰기, 평균 내는 방식은 모두 손볼 여지이지 지금 결과를 망가뜨리는 것은 아니다.",
},
{
  t: "review", k: "데이터 준비 과정", cat: "review", d: 4, track: "dl",
  q: "이 데이터 준비 코드의 결함을 모두 고르세요",
  code: "full = CIFAR10(root)\nmean = full.data.mean(axis=(0,1,2)) / 255\nstd  = full.data.std(axis=(0,1,2)) / 255\n\ntf = Compose([RandomCrop(32, 4), RandomHorizontalFlip(), ToTensor(),\n              Normalize(mean, std)])\nfull.transform = tf\n\ntrain, val = random_split(full, [45000, 5000])\ntrain_loader = DataLoader(train, batch_size=128, shuffle=True)\nval_loader   = DataLoader(val,   batch_size=128, shuffle=True)",
  items: [
    { txt: "같은 변환을 검증에도 그대로 쓴다. 무작위 자르기와 뒤집기가 평가에까지 걸리므로 같은 모델의 점수가 돌릴 때마다 달라지고, 실제보다 낮게 나온다.", bad: true },
    { txt: "검증 로더에 섞기를 켠 것은 필요 없는 설정이지만 점수를 바꾸지는 않는다 — 전부 한 번씩 지나가므로 순서는 결과에 영향을 주지 않는다.", bad: false },
    { txt: "배치 크기 128은 이 크기의 데이터에서 흔한 값이다 — 메모리에 맞춰 조정할 일이지 그 자체가 결함은 아니다.", bad: false },
    { txt: "정규화 통계를 나누기 전 전체 데이터에서 계산했다. 검증 데이터의 분포가 학습 쪽으로 새어 들어와 점수가 실제보다 좋아 보인다.", bad: true },
    { txt: "나누기에 씨앗을 주지 않아 돌릴 때마다 다른 5,000장이 검증으로 간다. 어제의 점수와 오늘의 점수를 견줄 수 없으므로 실험 자체가 성립하지 않는다.", bad: true },
    { txt: "데이터를 통째로 메모리에 올린다 — 이 크기라면 문제가 없고, 더 큰 데이터셋이라면 그때 스트리밍으로 바꿀 일이다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>평가에까지 걸린 증강</b> — 증강은 학습을 어렵게 만들어 일반화를 돕는 장치라, 평가에 쓰면 재는 자를 흔드는 것이 된다. (2) <b>전체 데이터로 계산한 정규화 통계</b> — 아주 조용한 누설이다. 평균과 표준편차는 <b>학습 부분에서만</b> 구하고 그 값을 검증에도 적용한다. (3) <b>고정되지 않은 분할</b> — 나눔이 매번 달라지면 점수 차이가 모델 때문인지 운 때문인지 구별할 수 없다.\n디스트랙터: 검증 로더의 섞기는 쓸모없을 뿐 해롭지 않고, 배치 크기와 메모리 적재는 이 규모에서 타당한 선택이다 — 무해한 것과 조용히 해로운 것을 가려내는 것이 이 리뷰의 요점이다.",
},
{
  t: "review", k: "학습률과 스케줄러", cat: "review", d: 4, track: "dl",
  q: "배치 크기를 여덟 배로 올린 이 설정의 결함을 모두 고르세요",
  code: "# 배치 크기를 128 -> 1024 로 올렸다. 한 에폭은 44배치.\nopt   = SGD(model.parameters(), lr=0.05, momentum=0.9)\nsched = CosineAnnealingLR(opt, T_max=30)\n\nfor epoch in range(30):\n    for x, y in loader:\n        loss = step(x, y)\n        opt.step()\n        sched.step()\n    print(epoch, loss.item())",
  items: [
    { txt: "<code>T_max=30</code> 은 에폭 수를 뜻하는데 스케줄러를 배치마다 밟는다. 44배치면 첫 에폭 안에서 주기가 다 돌아, 남은 29에폭은 거의 0에 가까운 학습률로 헛돈다.", bad: true },
    { txt: "배치를 여덟 배로 키우면서 학습률은 그대로 두었다. 한 걸음이 훑는 데이터가 여덟 배인데 보폭이 같으므로 같은 에폭 수로는 훨씬 덜 배운다 — 함께 올리고 예열을 붙이는 것이 정석이다.", bad: true },
    { txt: "에폭마다 찍는 값이 그 에폭 마지막 배치 하나의 손실이라 크게 흔들린다. 전체 평균을 내야 곡선이 읽히고 언제 멈출지 판단할 수 있다.", bad: true },
    { txt: "SGD 에 모멘텀 0.9를 준 것은 흔하고 무난한 조합이다 — 다른 최적화기가 초기에 더 빠를 수는 있지만 결함이라 부를 자리는 아니다.", bad: false },
    { txt: "30에폭이 충분한지는 데이터와 모델에 달렸다 — 곡선을 보고 정할 일이라 이 코드만 보고는 길다 짧다를 말할 수 없다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>스케줄러를 밟는 주기가 어긋났다</b> — 코사인 스케줄은 <code>T_max</code> 번 밟는 동안 학습률을 한 주기 돌린다. 에폭 단위로 잡아 두고 배치마다 밟으면 44배 빨리 소진된다. (2) <b>배치만 키우고 학습률은 그대로</b> — 큰 배치는 기울기가 덜 흔들리는 대신 걸음 수가 줄어드니, 보폭을 함께 늘리고 초반에는 예열로 천천히 올린다. (3) <b>마지막 배치의 손실을 기록</b> — 배치 하나의 값은 잡음이 커서 학습 곡선으로 쓸 수 없다.\n디스트랙터: 최적화기 선택과 총 에폭 수는 실험으로 정할 일이지 이 코드만 보고 결함이라 부를 수 없다 — 리뷰에서 취향을 결함으로 적으면 진짜 결함이 묻힌다.",
},
{
  t: "review", k: "모델 고르기와 평가", cat: "review", d: 3, track: "dl",
  q: "가장 좋은 모델을 고르는 이 절차의 결함을 모두 고르세요",
  code: "best = 0\nfor epoch in range(50):\n    train_one_epoch(model, train_loader)\n    acc = evaluate(model, test_loader)\n    if acc > best:\n        best = acc\n        torch.save(model.state_dict(), \"best.pt\")\n\nmodel.load_state_dict(torch.load(\"best.pt\"))\nprint(\"최종 정확도\", evaluate(model, test_loader))",
  items: [
    { txt: "가중치만 저장해 최적화기 상태가 남지 않는다 — 이어서 학습하려면 아쉽지만, 저장한 것을 추론에만 쓸 것이라면 이대로도 충분하다.", bad: false },
    { txt: "50에폭 내내 평가를 돌려 시간이 든다 — 몇 에폭에 한 번으로 줄이면 학습이 빨라지지만 결과가 달라지지는 않는다.", bad: false },
    { txt: "시험 집합으로 가장 좋은 에폭을 골랐다. 고르는 데 쓴 순간 그것은 더 이상 시험 집합이 아니라 검증 집합이 된다.", bad: true },
    { txt: "그렇게 고른 뒤 같은 집합으로 최종 점수를 냈다. 50번 중 가장 운이 좋았던 값을 뽑아 온 것이라, 이 숫자는 언제나 실제 성능보다 높다.", bad: true },
    { txt: "저장 파일 이름이 고정이라 여러 실험이 서로를 덮는다 — 실험마다 폴더를 나누면 좋지만 지금 점수가 부풀려지는 이유는 아니다.", bad: false },
    { txt: "무작위 씨앗을 고정하지 않아 같은 코드가 매번 다른 결과를 낸다. 초기화와 섞기와 증강을 모두 묶어야 두 실험을 견줄 수 있다.", bad: true },
  ],
  ex: "결함 3가지. (1)(2) <b>고르는 데 쓴 데이터로 점수를 냈다</b> — 이 둘은 한 뿌리의 두 얼굴이다. 50에폭 중 시험 점수가 가장 높은 지점을 뽑는 것은 시험 집합에 맞춰 조정한 것이고, 그 뒤에 같은 집합으로 잰 숫자는 고르기의 운까지 포함한다. 검증 집합으로 고르고 시험 집합은 <b>마지막에 한 번만</b> 본다. (3) <b>씨앗 미고정</b> — 견줄 수 없는 실험은 실험이 아니다.\n디스트랙터: 최적화기 상태 저장, 평가 주기, 파일 이름은 모두 편의와 속도의 이야기다. 점수를 부풀리지는 않으므로, 급한 것과 나눠서 적어야 한다.",
},

/* ══════════════ mobile ══════════════ */
{
  t: "review", k: "목록 화면", cat: "review", d: 3, track: "mobile",
  q: "이 목록 화면 코드의 결함을 모두 고르세요",
  code: "class FeedActivity : Activity() {\n  override fun onCreate(s: Bundle?) {\n    super.onCreate(s)\n    val items   = api.fetchFeed()                       // 동기 호출\n    val bitmaps = items.map { BitmapFactory.decodeFile(it.path) }\n    list.adapter = FeedAdapter(items, bitmaps)\n  }\n}",
  items: [
    { txt: "네트워크 호출을 화면이 만들어지는 자리에서 곧바로 한다. 주 스레드가 응답을 기다리는 동안 화면이 얼어붙고, 오래 걸리면 시스템이 응답 없음으로 판단해 앱을 끝낸다.", bad: true },
    { txt: "목록의 모든 이미지를 미리 원본 크기로 메모리에 올린다. 실제로 보이는 것은 몇 장뿐인데 전부 올리므로, 목록이 길어지면 곧바로 메모리가 바닥난다.", bad: true },
    { txt: "화면 단위로 <code>Activity</code> 를 직접 쓴다 — 요즘은 더 가벼운 단위로 나누는 편이지만 이 코드가 멈추는 이유는 아니다.", bad: false },
    { txt: "어댑터를 새로 만들어 통째로 갈아 끼운다 — 갱신이 잦다면 바뀐 항목만 다시 그리는 편이 낫지만, 여기서는 처음 한 번이라 차이가 없다.", bad: false },
    { txt: "실패했을 때가 없다. 연결이 끊겼거나 파일이 사라졌으면 그대로 예외가 나 앱이 죽는다 — 모바일에서 연결이 끊기는 것은 예외 상황이 아니라 평상시다.", bad: true },
    { txt: "항목 목록과 비트맵 목록을 따로 넘겨 길이가 어긋날 여지가 있다 — 하나의 자료형으로 묶어 두면 나중에 걸러 내는 기능을 붙일 때도 안전하지만, 지금은 같은 목록에서 한 번에 만들어 어긋날 수 없다.", bad: false },
  ],
  ex: "결함 3가지. (1) <b>주 스레드에서 네트워크</b> — 모바일에서 가장 먼저 배우는 금기다. 화면을 그리는 스레드가 막히면 스크롤도 버튼도 죽고, 시스템이 앱을 강제 종료한다. (2) <b>이미지를 통째로 미리 올림</b> — 목록은 보이는 것만 만드는 구조인데 그 이점을 스스로 버렸다. 화면 크기에 맞춰 줄여 읽고, 필요한 때 읽는다. (3) <b>실패 경로 없음</b> — 연결이 끊기는 것을 기본값으로 놓고 짜야 한다.\n디스트랙터: 화면 단위의 선택, 어댑터를 갈아 끼우는 방식, 두 목록을 따로 넘긴 것은 모두 더 좋아질 여지이지 지금 앱을 멈추게 하는 것은 아니다.",
},
{
  t: "review", k: "권한 요청", cat: "review", d: 2, track: "mobile",
  q: "이 권한 요청 코드의 결함을 모두 고르세요",
  code: "override fun onCreate(s: Bundle?) {\n  super.onCreate(s)\n  requestPermissions(\n    arrayOf(CAMERA, ACCESS_FINE_LOCATION, READ_CONTACTS, RECORD_AUDIO), 100)\n}\n\noverride fun onRequestPermissionsResult(\n    code: Int, perms: Array<String>, results: IntArray) {\n  // 비어 있음\n}",
  items: [
    { txt: "요청 코드로 뜻이 없는 숫자를 그대로 적었다 — 한 화면이 여러 종류의 권한을 따로 묻기 시작하면 어느 응답인지 헷갈리므로, 이름 있는 상수로 빼 두기를 권한다.", bad: false },
    { txt: "앱을 켜자마자 네 가지를 한꺼번에 묻는다. 왜 필요한지 보여 주기 전에 물으면 사람들은 대개 거절하고, 한 번 거절당한 권한은 다시 묻기가 훨씬 어려워진다.", bad: true },
    { txt: "카메라와 마이크를 함께 요청한다 — 영상 통화가 있는 앱이라면 자연스러운 짝이므로 묶은 것 자체가 문제는 아니다.", bad: false },
    { txt: "결과를 받는 자리가 비어 있다. 거절당했는데도 앱은 권한이 있는 것처럼 계속 진행하고, 실제로 쓰려는 순간에야 예외가 난다.", bad: true },
    { txt: "연락처가 이 화면에 왜 필요한지 코드 어디에도 나타나지 않는다. 쓰지 않는 권한은 심사에서 막히고, 받아 둔 것 자체가 유출의 대상이 된다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>한꺼번에, 맥락 없이</b> — 권한은 그 기능을 쓰려는 순간에, 왜 필요한지 보여 준 뒤에 묻는 것이 원칙이다. 켜자마자 넷을 묻는 화면은 거절률이 훨씬 높고, 한 번 거절되면 설정 화면까지 안내해야 하는 처지가 된다. (2) <b>결과를 안 본다</b> — 거절도 답이다. 답을 무시하면 앱은 없는 권한을 있다고 믿고 굴러가다 나중에 죽는다. (3) <b>쓰지 않는 권한</b> — 최소 권한은 보안 원칙이자 심사 기준이다.\n디스트랙터: 요청 코드를 상수로 빼는 것은 읽기의 문제이고, 카메라와 마이크를 함께 묻는 것은 기능이 그렇다면 타당하다 — 권한 리뷰의 초점은 개수가 아니라 <b>필요한 때에 필요한 것만</b>인지다.",
},
{
  t: "review", k: "생명주기와 참조", cat: "review", d: 4, track: "mobile",
  q: "이 화면 코드의 결함을 모두 고르세요",
  code: "object Session { var activity: Activity? = null }\n\nclass MainActivity : Activity() {\n  override fun onCreate(s: Bundle?) {\n    super.onCreate(s)\n    Session.activity = this\n    LocationManager.addListener(locationListener)\n    Handler().postDelayed({ showTip() }, 60_000)\n  }\n}",
  items: [
    { txt: "화면 자체를 전역 객체가 붙들고 있다. 화면이 닫혀도 참조가 남아 회수되지 않으므로, 회전을 몇 번만 해도 죽은 화면이 여러 개 쌓인다.", bad: true },
    { txt: "단일 객체를 하나 두는 형태 자체는 흔하다 — 담아 두는 것이 화면이 아니라 설정 값이나 저장소라면 문제가 되지 않는다.", bad: false },
    { txt: "위치 리스너를 붙이기만 하고 떼지 않는다. 화면이 사라진 뒤에도 갱신이 계속 와서 배터리를 먹고, 없어진 화면을 건드리다 예외가 난다.", bad: true },
    { txt: "1분 뒤에 실행할 일을 예약해 두고 취소하지 않는다. 그 사이에 화면이 닫히면 사라진 화면에 안내를 띄우려다 그대로 죽는다.", bad: true },
    { txt: "예약에 <code>Handler</code> 를 쓴다 — 취소를 붙이기 쉬운 요즘 도구로 바꾸면 낫지만, 도구를 바꾼다고 취소를 잊는 습관이 저절로 고쳐지지는 않는다.", bad: false },
    { txt: "안내를 1분 뒤에 띄우는 것이 좋은 경험인지는 제품이 정할 문제다 — 코드의 결함으로 볼 자리는 아니다.", bad: false },
  ],
  ex: "결함 3가지. 셋 다 <b>같은 뿌리</b>다 — 화면보다 오래 사는 것에 화면을 묶어 두었다. (1) 전역 객체가 화면을 붙들면 그 화면과 거기 매달린 모든 것이 회수되지 않는다. (2) 리스너를 떼지 않으면 시스템 서비스가 죽은 화면을 계속 부른다. (3) 예약한 일을 취소하지 않으면 타이머가 죽은 화면을 부른다. 화면에서 무언가를 등록했다면 <b>짝이 되는 자리에서 반드시 해제</b>한다는 규칙 하나로 셋이 함께 사라진다.\n디스트랙터: 단일 객체를 쓰는 것 자체, 예약에 쓴 도구, 1분이라는 시각은 결함이 아니다. 특히 도구를 바꾸자는 제안은 진짜 문제(해제를 안 한다)를 가릴 수 있어 조심해야 한다.",
},
{
  t: "review", k: "동기화와 재시도", cat: "review", d: 3, track: "mobile",
  q: "이 동기화 코드의 결함을 모두 고르세요",
  code: "// 앱이 켜져 있는 동안 15초마다 호출한다\nsuspend fun sync() {\n  while (true) {\n    try {\n      api.push(pending)\n      return\n    } catch (e: Exception) {\n      delay(1000)\n    }\n  }\n}",
  items: [
    { txt: "예외를 종류를 가리지 않고 넓게 잡는다 — 나눠서 다루면 더 나아지지만, 다시 해 볼 만한 실패가 대부분이라 이 자체가 사고를 만들지는 않는다.", bad: false },
    { txt: "실패해도 1초 간격으로 영원히 다시 시도한다. 서버가 아파서 실패하는 중이라면 모든 기기가 함께 두드려 회복을 막고, 그동안 배터리와 데이터 요금이 녹는다.", bad: true },
    { txt: "15초라는 주기 자체는 제품이 정할 값이다 — 실시간에 가깝게 맞춰야 하는 앱이라면 타당한 선택이다.", bad: false },
    { txt: "<code>suspend</code> 함수로 만들어 부르는 쪽이 취소할 수 있게 했다 — 오히려 잘된 부분이라 지적할 자리가 아니다.", bad: false },
    { txt: "연결 상태를 전혀 보지 않는다. 비행기 모드에서도 같은 속도로 두드리므로, 연결이 돌아왔을 때 깨우는 방식으로 바꿔야 한다.", bad: true },
    { txt: "보낼 목록을 성공한 뒤에 비우는지 이 코드에서는 보이지 않는다 — 확인할 값어치는 있지만 보이는 범위 밖의 이야기다.", bad: false },
  ],
  ex: "결함 2가지. (1) <b>끝이 없는 고정 간격 재시도</b> — 재시도에는 두 가지가 반드시 붙어야 한다. 간격을 점점 늘리는 것(백오프)과, 기기마다 조금씩 흩뜨리는 것(지터)이다. 둘이 없으면 장애가 난 서버에 모든 기기가 박자를 맞춰 몰려들어 회복을 막는다. 횟수 상한도 함께 둔다. (2) <b>연결 상태를 보지 않음</b> — 끊긴 것을 아는데도 두드리는 것은 순수한 낭비다. 시스템이 주는 연결 복구 신호에 맞춰 깨우면 배터리도 요금도 아낀다.\n디스트랙터: 넓게 잡은 예외와 15초 주기는 상황에 따라 타당하고, <code>suspend</code> 로 취소 가능하게 만든 것은 <b>잘한 부분</b>이다. 보이지 않는 부분은 질문으로 남기되 결함으로 적지 않는다.",
},

/* ══════════════ arduino ══════════════ */
{
  t: "review", k: "센서 루프", cat: "review", d: 3, track: "arduino",
  q: "이 스케치의 결함을 모두 고르세요",
  code: "int count = 0;\n\nvoid tick() { count++; Serial.println(\"tick\"); }\n\nvoid setup() {\n  Serial.begin(9600);\n  attachInterrupt(0, tick, RISING);\n}\n\nvoid loop() {\n  int v = analogRead(A0);\n  Serial.println(v);\n  delay(1000);\n  if (v > 700) digitalWrite(RELAY, HIGH);\n}",
  items: [
    { txt: "<code>analogRead</code> 값을 한 번만 읽어 그대로 쓴다 — 여러 번 읽어 평균을 내거나 이전 값과 섞어 완만하게 만들면 흔들림이 줄어들지만, 몇 번을 읽을지는 센서의 잡음을 보고 정한다.", bad: false },
    { txt: "직렬 속도를 낮게 잡았다 — 더 올릴 수 있지만 초당 한 줄을 내보내는 데는 넉넉하다.", bad: false },
    { txt: "<code>count</code> 에 <code>volatile</code> 이 없다. 인터럽트가 바꾸는 변수인데 컴파일러가 그 사실을 모르므로, 값을 레지스터에 붙들어 두면 본체 코드가 갱신을 영영 못 본다.", bad: true },
    { txt: "인터럽트 번호를 숫자로 적었다 — 핀 번호로 적는 함수를 쓰면 보드가 달라져도 맞지만, 이 보드에서는 같은 곳을 가리킨다.", bad: false },
    { txt: "인터럽트 안에서 직렬 출력을 부른다. 직렬 전송은 인터럽트가 살아 있어야 끝나는 일인데 그 안에서는 인터럽트가 막혀 있어, 버퍼가 차는 순간 그대로 멈춘다.", bad: true },
    { txt: "<code>delay(1000)</code> 이 1초 내내 아무것도 못 하게 붙잡는다. 그동안은 임계를 넘겨도 반응하지 못하므로, 시간을 재서 지나갔는지 확인하는 형태로 바꿔야 한다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b><code>volatile</code> 누락</b> — 컴파일러는 본체 코드만 보고 '이 변수는 아무도 안 바꾼다' 고 판단해 읽기를 지워 버린다. 인터럽트가 건드리는 변수에는 반드시 붙인다. (2) <b>인터럽트 안의 직렬 출력</b> — 인터럽트 처리는 <b>짧고, 기다리지 않고, 막지 않아야</b> 한다. 안에서는 깃발만 세우고 실제 출력은 본체에서 한다. (3) <b><code>delay</code> 로 만든 주기</b> — 그 1초 동안 보드는 아무 일도 못 한다. 경과 시간을 견주는 방식으로 바꾸면 그 사이에 다른 일을 할 수 있다.\n디스트랙터: 아날로그 값을 한 번만 읽는 것, 직렬 속도, 인터럽트 번호 표기는 모두 다듬을 여지이지 지금 스케치를 멈추게 하는 것은 아니다.",
},
{
  t: "review", k: "문자열과 버퍼", cat: "review", d: 3, track: "arduino",
  q: "이 로그 출력 코드의 결함을 모두 고르세요",
  code: "void loop() {\n  String msg = \"sensor=\";\n  msg += analogRead(A0);\n  msg += \",t=\";\n  msg += millis();\n\n  char buf[64];\n  sprintf(buf, \"%s\", msg.c_str());\n  Serial.println(buf);\n  delay(100);\n}",
  items: [
    { txt: "루프를 돌 때마다 <code>String</code> 을 새로 만들고 이어 붙인다. 조각이 남아 힙이 잘게 부서지고, 며칠 뒤 메모리가 남았는데도 할당이 실패하는 모양으로 멈춘다.", bad: true },
    { txt: "한 줄씩 내보내는 형식이라 받는 쪽에서 나누기 쉽다 — 구분자를 정해 두면 파싱이 더 편해지지만 지금 멈추는 이유는 아니다.", bad: false },
    { txt: "<code>millis()</code> 는 약 50일에 한 번 되돌아간다 — 시간 간격을 뺄셈으로 재면 그때도 맞고, 여기서는 값을 찍기만 하므로 문제가 되지 않는다.", bad: false },
    { txt: "버퍼는 64바이트인데 담을 길이를 확인하지 않고 복사한다. 문자열이 길어지면 옆에 있는 값을 덮어써서, 전혀 상관없어 보이는 자리에서 이상하게 동작한다.", bad: true },
    { txt: "문자열 리터럴을 그대로 두면 RAM 을 먹는다 — 플래시에 남기는 매크로로 감싸면 아낄 수 있지만 여기서는 몇 바이트의 이야기다.", bad: false },
    { txt: "<code>delay(100)</code> 으로 주기를 만든다 — 이 스케치에는 그동안 할 다른 일이 없으므로 이대로도 무방하다.", bad: false },
  ],
  ex: "결함 2가지. (1) <b>루프 안의 <code>String</code> 이어 붙이기</b> — 이어 붙일 때마다 새 버퍼를 잡고 옛 것을 버리므로, 램이 2KB 남짓인 보드에서는 힙이 금세 조각난다. 무서운 점은 <b>바로 안 죽는다</b>는 것이다: 며칠 잘 돌다가 어느 밤에 멈춘다. 고정 길이 버퍼에 <code>snprintf</code> 로 찍는 편이 안전하다. (2) <b>길이 확인 없는 복사</b> — 임베디드에는 보호 장치가 없어 넘친 바이트가 그대로 옆 변수를 덮는다. 증상이 원인과 멀리 떨어져 나타나 추적이 가장 어려운 종류의 고장이다.\n디스트랙터: 출력 형식, 되돌아가는 시각 값, 리터럴이 먹는 램, 주기를 만드는 방식은 모두 다듬을 여지이지 이 스케치가 며칠 뒤 멈추는 이유는 아니다.",
},
{
  t: "review", k: "버튼 입력 처리", cat: "review", d: 3, track: "arduino",
  q: "버튼을 세는 이 코드의 결함을 모두 고르세요",
  code: "int counter = 0;\n\nvoid setup() { pinMode(BTN, INPUT); }\n\nvoid loop() {\n  if (digitalRead(BTN) == HIGH) {\n    counter++;\n    EEPROM.write(0, counter);\n  }\n}",
  items: [
    { txt: "<code>INPUT</code> 으로만 두면 아무것도 연결되지 않은 동안 핀이 떠 있어, 주변의 전기 잡음만으로도 눌린 것처럼 읽힌다. 내부 풀업을 켜고 판정을 뒤집는 것이 정석이다.", bad: true },
    { txt: "핀 번호를 이름 있는 상수로 뺀 것은 잘한 부분이다 — 배선이 바뀌어도 한 곳만 고치면 된다.", bad: false },
    { txt: "눌림을 상태로 보고 있어 한 번 누르는 동안 루프가 도는 횟수만큼 세어진다. 눌린 순간만 세도록 이전 값과 견주고, 접점이 떨리는 구간도 걸러야 한다.", bad: true },
    { txt: "<code>counter</code> 의 폭을 생각하면 언젠가 넘친다 — 오래 도는 장치라면 정해 두어야 하지만, 지금 세는 값이 틀리는 이유는 그것이 아니다.", bad: false },
    { txt: "셀 때마다 EEPROM 에 쓴다. 쓰기 수명이 십만 번 남짓이라 몇 분이면 그 자리가 죽으므로, 값이 바뀔 때만 그것도 드물게 써야 한다.", bad: true },
  ],
  ex: "결함 3가지. (1) <b>떠 있는 입력 핀</b> — 연결되지 않은 핀은 0도 1도 아니라 주변 전기장을 따라 흔들린다. 내부 풀업을 켜 두고 버튼이 눌리면 접지로 떨어지게 만들면, 부품 하나 없이 안정된 판정이 된다. (2) <b>상태와 변화를 구별하지 않음</b> — 사람이 버튼을 누르는 시간은 수십 밀리초이고 그 사이에 루프는 수천 번 돈다. 세야 하는 것은 '눌려 있다' 가 아니라 '방금 눌렸다' 이다. (3) <b>EEPROM 을 매번 쓴다</b> — 이 저장 장치는 쓰기 횟수가 정해져 있어, 루프마다 쓰면 하드웨어가 실제로 닳아 없어진다.\n디스트랙터: 핀 번호를 상수로 뺀 것은 <b>잘한 부분</b>이고, 세는 값의 폭은 훨씬 나중의 이야기다.",
},
{
  t: "review", k: "모터 구동과 응답성", cat: "review", d: 4, track: "arduino",
  q: "모터를 돌리는 이 스케치의 결함을 모두 고르세요",
  code: "void setup() {\n  pinMode(MOTOR, OUTPUT);       // 모터를 이 핀에 직접 연결했다\n  Serial.begin(9600);\n}\n\nvoid loop() {\n  digitalWrite(MOTOR, HIGH);\n  delay(2000);\n  digitalWrite(MOTOR, LOW);\n  delay(2000);\n  if (Serial.available()) handle(Serial.read());\n}",
  items: [
    { txt: "2초씩 켜고 끄는 주기는 만들려는 장치가 정할 값이다 — 펌프라면 짧아야 하고 환풍기라면 길어도 되므로, 무엇을 돌리는 장치인지 모르는 채로 코드만 보고 길다 짧다를 말할 수 없다.", bad: false },
    { txt: "모터를 핀에 직접 물렸다. 핀 하나가 낼 수 있는 전류는 수십 밀리암페어인데 작은 모터도 그 몇 배를 먹으므로, 트랜지스터나 전용 드라이버를 사이에 두어야 한다.", bad: true },
    { txt: "코일이 꺼질 때 생기는 역기전력을 흘려보낼 다이오드가 없다. 끄는 순간의 역전압이 핀과 마이크로컨트롤러를 조용히 망가뜨린다.", bad: true },
    { txt: "직렬 입력을 4초에 한 번만 들여다본다. 지연으로 묶여 있는 동안 들어온 명령은 수신 버퍼가 차는 순간 그대로 버려진다.", bad: true },
    { txt: "멈췄을 때 스스로 되살아나게 하는 감시 타이머를 켜지 않았다 — 있으면 든든하지만, 먼저 멈추지 않게 만드는 것이 순서다.", bad: false },
    { txt: "<code>handle</code> 이 무엇을 하는지 이 코드에서는 보이지 않는다 — 확인할 값어치는 있지만 지금 보이는 범위의 결함은 아니다.", bad: false },
  ],
  ex: "결함 3가지. 앞의 둘은 <b>전기</b>의 문제이고 나머지 하나는 <b>구조</b>의 문제다. (1) <b>모터를 핀에 직접</b> — 핀은 신호를 내보내는 자리이지 힘을 내는 자리가 아니다. 당장은 도는 것처럼 보여도 핀이 서서히 죽는다. (2) <b>환류 다이오드 없음</b> — 코일에 흐르던 전류는 갑자기 끊기지 않고 역방향 전압을 만든다. 그 순간의 전압이 부품을 넘어뜨리므로 다이오드로 길을 열어 준다. (3) <b>지연에 갇힌 응답</b> — 4초 중 대부분을 아무것도 못 듣고 보내므로 명령이 사라진다.\n디스트랙터: 주기의 길이, 감시 타이머, 보이지 않는 함수는 지금 지적할 자리가 아니다 — 하드웨어 리뷰에서는 <b>부품이 죽는 것</b>이 언제나 먼저다.",
},

];
