module.exports = {
  track: "ai", guide: "📜", xp: 70,
  unit: "로그 분석 — 추론 서버가 남긴 기록",
  source: "./log_ai.cjs",
  lessons: [
    { t: "프롬프트와 컨텍스트가 어긋날 때", n: 6,
      th: { sum: "LLM 앱의 사고는 대개 **모델이 받은 것**이 우리가 보낸 줄 알았던 것과 다르기 때문에 일어난다.",
        body: [
          { h: "잘리고 비고 어긋난다", t: "한도를 넘겨 앞에서부터 자르면 가장 먼저 사라지는 것이 system 프롬프트다. 검색 필터가 모든 문서를 걸러도 파이프라인은 <b>근거 0건</b>으로 그냥 물어보고, 모델은 아는 대로 지어낸다. 색인과 질의를 다른 임베딩 모델로 만들면 차원이 같아 오류 없이 <b>엉뚱한 문서</b>가 돌아온다." },
          { h: "형식은 부탁이 아니라 강제", t: "'JSON 으로만 답하세요' 는 95% 만 지켜지고, 나머지 5% 는 매일 수백 건이다. 응답이 비었는데 200 인 경우도 있다 — <code>finish_reason</code> 을 보지 않으면 필터에 걸린 것을 정상으로 저장한다." }],
        code: { c: "잘라내기: system 은 제외, 오래된 대화부터\n근거 0건 → 생성 중단, '찾지 못함' 으로 응답\nfinish_reason: stop / length / content_filter 를 분기", cap: "모델이 실제로 받은 것을 로그로 확인" },
        key: ["앞에서 자르면 지시가 사라진다", "근거 0건에 게이트를 둔다", "색인·질의 임베딩은 같아야", "200 이 답변 있음은 아니다"] } },
    { t: "비용과 지연이 새는 자리", n: 6,
      th: { sum: "추론 비용과 지연은 대개 **같은 것을 여러 번 보내거나, 줄을 세우거나, 다 같이 다시 던져서** 생긴다.",
        body: [
          { h: "같은 것을 다시 보낸다", t: "첨부 문서를 턴마다 다시 실으면 9턴 대화에서 아홉 번 지불한다. 비용은 <b>입력·출력을 나눠</b> 봐야 어디가 샜는지 보인다. 캐시는 키를 잘못 만들면(현재 시각 포함) 저장만 되고 적중은 0 이라, <b>적중률 0%</b> 는 캐시가 없는 것보다 나쁘다." },
          { h: "줄을 세우거나 몰려간다", t: "GPU 가 포화인데 동시 요청을 여덟 배로 올리면 처리량은 그대로이고 대기만 늘어난다. 429 에 즉시 재시도하면 부하가 배로 뛰어 제한 창이 끝없이 갱신된다. 스트리밍은 첫 토큰까지 시간이 걸리므로 <b>버퍼링을 켠 프록시</b>가 죽은 연결로 오해해 끊는다." }],
        code: { c: "입력 4.1억 → 24.6억, 출력 그대로 → 문서 재전송\n캐시 키 = 모델+system+질문+온도 (시각 제외)\n429 → 지수 백오프 + 지터 + Retry-After\n스트리밍: 버퍼링 off, 하트비트", cap: "새는 곳은 늘 반복과 대기" },
        key: ["입력·출력을 나눠 본다", "캐시 키에 변하는 값 금지", "포화에서 동시성은 대기만 늘린다", "재시도에는 백오프와 지터"] } },
  ],
};
