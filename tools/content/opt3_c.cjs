/* c — 정답률 15% 아래 끝맺음의 오답 32개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "c", fixes: [
  { k: "void", from: "이름이 필요 없다", to: "이름 없이 정의할 수 있다" },
  { k: "개념", from: "반복문이 필요 없다", to: "포인터 하나로 끝까지 훑으면 되어 반복문 없이도 방문된다" },
  { k: "다중 평가", from: "컴파일 오류가 난다", to: "i++ 를 매크로 인자로 넘기는 것은 전처리기가 거부한다" },
  { k: "매크로 vs 함수", from: "잘못된 타입을 넣으면 오류가 난다", to: "매크로는 컴파일 단계에서 인자 타입을 검사하므로 잘못된 타입을 넣으면 컴파일러가 잡아낸다" },
  { k: "상수 정의", from: "헤더에 넣을 수 없게 된다", to: "헤더에 넣으면 중복 정의가 되어 링크가 깨진다" },
  { k: "c2-static-zero", from: "컴파일 오류가 난다", to: "초기화 없는 정적 배열 선언은 컴파일러가 거부한다" },
  { k: "c2-pragma-once", from: "중복 정의 오류가 난다", to: "인클루드 가드와 함께 쓰면 헤더가 두 번 처리된 것으로 간주되어 중복 정의로 컴파일이 실패한다" },
  { k: "c2-header-selfcontained", from: "static 은 쓰지 않는다", to: "모든 내부 헬퍼 함수도 헤더에 선언해 두어 다른 파일에서 재사용할 수 있게 하고, static 은 붙이지 않는다" },
  { k: "c3-loop-2", from: "깊이를 걱정할 필요가 없다", to: "최적화를 끈 -O0 에서도 꼬리 재귀는 루프로 바뀌므로 깊이는 얼마든지 깊어도 된다" },
  { k: "c3-loop-2", from: "어떤 컴파일러든 스택을 쓰지 않는다", to: "C 표준은 꼬리 재귀를 루프로 바꾸도록 요구하므로 어떤 컴파일러든 스택을 늘리지 않고 돈다" },
  { k: "c3-loop-4", from: "스택이 필요 없다", to: "goto 로 함수 시작 지점으로 되돌아가면 호출 없이 같은 코드를 다시 돌 수 있어 스택 없이 끝난다" },
  { k: "c3-td-6", from: "함수 안에서 선언하면 컴파일 오류가 난다", to: "typedef 는 파일 범위에서만 허용되므로 함수 안에서 선언하면 컴파일러가 거부한다" },
  { k: "c3-nm-2", from: "확장으로 허용해 주기 때문", to: "실제로는 충돌이라 오류가 나야 하지만 대부분의 컴파일러가 확장으로 눈감아 주어서" },
  { k: "c3-nm-6", from: "실행 중에 검사되어 오류가 난다", to: "LogLevel 변수에 목록 밖의 정수를 넣으면 실행 중에 검사되어 프로그램이 중단된다" },
  { k: "c3-bss-2", from: "컴파일 단계에서 오류가 난다", to: "정적 배열은 상수식으로만 초기화할 수 있는데 {1} 은 부분 초기화라 컴파일 단계에서 거부된다" },
  { k: "큰 지역 배열", from: "실행할 때는 아무 문제가 없다", to: "배열 크기만큼 코드가 커져 컴파일 시간이 길어질 뿐, 실행할 때는 일반 배열과 똑같이 돈다" },
  { k: "소유권", from: "검사할 필요가 없다", to: "`malloc` 은 실패해도 빈 블록을 돌려주므로 반환값이 `NULL` 인지 검사하는 코드는 군더더기다" },
  { k: "수정", from: "오류가 난다", to: "역참조에 값을 대입할 수 없어 실행이 멈춘다" },
  { k: "다른 멤버 읽기", from: "컴파일 오류가 난다", to: "마지막에 쓴 멤버가 아닌 멤버를 읽는 것은 컴파일러가 막는다" },
  { k: "pack 위험", from: "배열을 만들 수 없게 된다", to: "원소 사이의 정렬이 깨져 그 구조체로는 배열을 만들지 못하게 된다" },
  { k: "pack 위험", from: "선언 순서를 믿을 수 없게 된다", to: "컴파일러가 패딩 대신 멤버 순서를 바꿔 채우므로 선언 순서와 메모리 순서가 어긋난다" },
  { k: "size_t 함정", from: "컴파일 오류가 난다", to: "size_t 에서 1 을 빼는 식은 부호 경고가 오류로 승격되어 컴파일이 멈춘다" },
  { k: "c2-2d-init", from: "중첩 없이 나열하면 컴파일 오류가 난다", to: "안쪽 배열마다 중괄호가 있어야 하므로 중첩 없이 나열하면 컴파일러가 거부한다" },
  { k: "c2-vla", from: "이식성을 걱정할 필요가 없다", to: "C89 부터 표준에 있던 문법이라 모든 컴파일러가 필수로 지원하며, 이식성은 보장된다" },
  { k: "c2-fopen-fail", from: "검사할 필요가 없다", to: "프로그램이 자동으로 종료되므로 검사 코드는 실행될 기회조차 없다" },
  { k: "c2-assert-ndebug", from: "컴파일 오류가 난다", to: "NDEBUG 가 정의된 상태의 assert 는 컴파일러가 거부한다" },
  { k: "c2-signal-vs-sigaction", from: "정리 코드를 돌릴 기회를 주기 때문", to: "sigaction 은 SIGKILL 과 SIGSTOP 까지 잡을 수 있어 강제 종료 앞에서도 정리 코드를 돌릴 기회를 주어서" },
  { k: "c2-localtime-static", from: "따로 검사하지 않아도 된다", to: "시간 값은 변환할 수 있으므로 반환값의 NULL 검사는 생략해도 된다" },
  { k: "c3-const-2", from: "다른 스레드가 바꿀 수 없게 된다", to: "인자로 넘어온 문자열이 읽기 전용 영역으로 복사되어 함수가 도는 동안 다른 스레드가 바꾸지 못하게 된다" },
  { k: "c3-size-6", from: "sizeof는 malloc과 함께 쓰지 않는다", to: "sizeof 를 malloc 의 인자로 쓰면 크기가 두 번 곱해진다" },
  { k: "c3-align-1", from: "정해져 있기 때문이다", to: "구조체는 어떤 경우에도 16바이트 단위로 크기가 맞춰지도록 정해져 있어서다" }
] };
