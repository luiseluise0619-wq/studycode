module.exports = {
  track: "linux", guide: "", xp: 80,
  unit: "직접 구현 — 셸과 커널이 하는 계산",
  source: "./linux_exec.cjs",
  lessons: [
    { t: "권한과 경로 — 비트와 스택", n: 5,
      th: { sum: "`ls -l` 의 아홉 글자와 경로 해석은 **비트 연산과 스택 연산**이다. 만들어 보면 규칙이 외울 것 없이 남는다.",
        body: [
          { h: "8진수를 쓰는 이유", t: "읽기 4 · 쓰기 2 · 실행 1 은 **겹치지 않는 비트**라 더한 값에서 각각을 다시 뽑아낼 수 있고, 세 비트가 8진수 한 자리에 정확히 들어맞는다. umask 는 그 비트를 **깎기만** 한다 — 더할 수는 없어서 새 파일이 처음부터 실행 권한을 갖는 일이 없다. 폴더의 기본이 `777` 인 것은 `x`(들어가기)가 없으면 폴더가 쓸모없어지기 때문이다." },
          { h: "`..` 는 pop 이다", t: "경로 정규화는 `/` 로 쪼개 스택에 쌓다가 `..` 에서 빼는 것이 전부다. 빈 스택에서 `pop()` 이 아무 일도 하지 않으므로 **뿌리 위로 올라가지 않는다**는 규칙이 저절로 지켜진다. 이 정규화 없이 문자열만 검사하면 `../../../etc/passwd` 로 의도한 폴더 밖을 읽는 **경로 순회 공격**이 통한다. 심볼릭 링크까지 고려한 진짜 확인은 `realpath` 가 한다." }],
        code: { c: "return (n & 4 ? \"r\" : \"-\")\n     + (n & 2 ? \"w\" : \"-\")\n     + (n & 1 ? \"x\" : \"-\");", cap: "4·2·1 은 겹치지 않는 비트" },
        key: ["r=4 w=2 x=1, 8진수 한 자리", "umask 는 깎기만 한다", "`..` 는 스택 pop", "정규화 없는 경로 검사는 우회된다"] } },
    { t: "셸이 먼저 하는 일 — glob · PATH · 인자", n: 5,
      th: { sum: "`ls *.txt` 에서 `ls` 는 별표를 본 적이 없다. **셸이 먼저 펼쳐** 이름 목록으로 바꿔 넘긴다. 이 순서를 알면 따옴표와 PATH 의 동작이 전부 설명된다.",
        body: [
          { h: "맞는 것이 없으면 패턴이 그대로 간다", t: "bash 의 기본 동작이라 `rm *.bak` 이 '`*.bak` 이라는 파일이 없다' 는 이상한 오류를 낸다(`shopt -s nullglob` 으로 바꿀 수 있다). 패턴을 정규식으로 옮길 때는 **특수 문자를 먼저 이스케이프**해야 한다 — `.` 를 그대로 두면 아무 문자나 맞는다. `*` 가 슬래시를 넘지 않는 규칙은 `.gitignore` 를 비롯한 여러 도구가 그대로 물려받았다." },
          { h: "순서가 곧 우선순위, 따옴표가 곧 경계", t: "`PATH` 는 앞에서부터 찾다 **처음 만난 것**을 실행한다. 그래서 앞에 폴더를 넣으면 시스템 명령을 덮어쓸 수 있고(`pyenv`·`nvm`), 같은 이유로 쓰기 가능한 폴더가 앞에 있으면 공격 경로가 된다. 인자 쪼개기에서는 따옴표가 경계다 — `rm $file` 에서 이름에 공백이 있으면 **인자가 갈라져** 엉뚱한 것을 지운다. 예외 없이 `\"$var\"` 로 감싼다." }],
        code: { c: "for (const dir of PATH.split(\":\"))\n  if (has(dir, name)) return dir + \"/\" + name;\n// 앞에서 처음 만난 것이 이긴다", cap: "순서가 곧 우선순위" },
        key: ["셸이 먼저 펼쳐 넘긴다", "안 맞으면 패턴이 그대로 간다", "PATH 는 앞이 이긴다", "변수는 언제나 \"$var\""] } },
    { t: "프로세스와 자원 — 숫자를 해석한다", n: 5,
      th: { sum: "`load 8` 도 `free 50MB` 도 그 자체로는 아무 뜻이 없다. **무엇과 비교하느냐**가 정해져야 판단이 된다.",
        body: [
          { h: "load 는 코어 수로 나눈다", t: "코어 32개의 load 8은 한가하고 코어 1개의 load 4는 심각하다. 리눅스의 load 는 CPU 뿐 아니라 **디스크를 기다리는 프로세스도 포함**하므로, CPU 는 한가한데 load 가 높으면 I/O 병목이다. 1·5·15분 세 숫자를 함께 보는 이유는 **추세** 때문이다 — 1분이 15분보다 크면 지금 나빠지는 중이다." },
          { h: "free 가 아니라 available", t: "리눅스는 남는 메모리를 **파일 캐시로 전부 쓴다**. 그 캐시는 필요하면 즉시 회수되므로 사실상 여유 공간이고, `free` 열만 보면 늘 부족해 보인다. 프로세스는 부모를 알 뿐 자식을 모르므로 후손 목록은 전체를 훑어 만들어야 하는데, 부모만 죽이면 자식이 살아남아 포트를 잡고 있는다 — systemd 가 cgroup 으로 묶어 관리하는 이유다." }],
        code: { c: "const per = load / cores;   // 이 값으로 판단한다\nconst avail = free + buffCache;  // 캐시는 회수된다", cap: "무엇과 비교하는지가 먼저" },
        key: ["load 는 코어 수와 비교", "리눅스 load 는 I/O 대기 포함", "free 말고 available", "부모만 죽이면 자식이 남는다"] } },
    { t: "로그와 집계 — 셸 관용구를 코드로", n: 5,
      th: { sum: "`sort | uniq -c | sort -rn | head` 는 로그 분석의 기본기다. 코드로 옮겨 보면 **왜 앞에 sort 가 필요한지**가 분명해진다.",
        body: [
          { h: "uniq 는 인접한 것만 본다", t: "정렬하지 않으면 떨어져 있는 같은 값이 따로 세어지는데, **오류도 나지 않아** 조용히 틀린다. 집계에서는 **동점 규칙**을 반드시 정해야 한다 — 없으면 실행할 때마다 순서가 흔들려 결과를 비교할 수 없다. 로그 파싱은 '앞의 정해진 개수만 나누고 나머지는 통째로' 가 기본형이다. 메시지에 공백이 몇 개든 마지막 조각이 흡수한다." },
          { h: "비율에는 0으로 나누기가 숨어 있다", t: "장애 판단은 오류의 개수가 아니라 **비율**이다. 요청이 열 배 늘면 오류도 열 배 늘어 보이지만 서비스는 멀쩡할 수 있다. 다만 줄이 하나도 없을 때 `0/0` 이 `NaN` 이 되고, 그 `NaN` 이 경보 조건 비교에서 **조용히 거짓**이 되어 새벽에 아무도 모르게 서비스가 죽는다. 실무 경보는 구간·임계값에 **최소 표본 수**까지 함께 정한다." }],
        code: { c: "if (!levels.length) return 0;   // 이 줄이 없으면 NaN\nreturn Math.round(bad / levels.length * 100);", cap: "NaN 은 비교에서 조용히 거짓이 된다" },
        key: ["uniq 앞에는 반드시 sort", "동점 규칙을 정한다", "앞만 나누고 나머지는 붙인다", "0으로 나누기를 먼저 막는다"] } },
    { t: "운영의 계산 — 회전 · 재시도 · 접근 제어", n: 4,
      th: { sum: "서버 운영의 자동화는 대개 **작은 판정 함수**다. 무엇을 회전시킬지, 얼마나 기다렸다 재시도할지, 누구를 들여보낼지.",
        body: [
          { h: "지우는 명령에는 방어를 두 겹", t: "`rm -rf \"$DIR/\"` 에서 `DIR` 이 비면 `rm -rf /` 가 된다 — 스팀 클라이언트가 사용자 홈을 지운 사고가 이 형태였다. `set -u` 로 없는 변수를 오류로 만드는 것이 첫 겹, **대상 경로를 실행 전에 검사**하는 것이 두 번째 겹이다. 로그도 마찬가지로 `rm` 이 아니라 **옮긴 뒤 신호를 보내** 회전시킨다 — 열린 파일을 지우면 공간이 반환되지 않는다." },
          { h: "재시도와 접근 제어의 기본값", t: "같은 간격 재시도는 회복하려는 서버를 계속 밀어붙인다. **두 배씩 늘리되 상한**을 두고, 거기에 **지터**를 더해야 수천 클라이언트가 같은 순간 몰려가는 것을 막는다. 붙이기 전에 그 요청이 **여러 번 실행돼도 안전한가**를 먼저 확인한다. 접근 제어 함수는 잘못된 입력에 참을 돌려주면 그것이 곧 보안 구멍이므로, **애매하면 거부**한다." }],
        code: { c: "out.push(Math.min(cur, cap));\ncur = cur * 2;      // 두 배씩, 상한까지", cap: "여기에 지터를 더해야 완성된다" },
        key: ["지우는 명령은 두 겹으로 막는다", "로그는 옮기고 신호를 보낸다", "백오프에는 상한과 지터", "접근 제어는 애매하면 거부"] } },
  ],
};
