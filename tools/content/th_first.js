/* 초보자가 트랙을 열면 처음 만나는 레슨들의 이론 — 레슨마다 따로.

   왜 다시 쓰는가
     1. 621개 유닛 중 574개가 레슨마다 이론이 똑같다. '연산자' 레슨에서 '출력과 변수'
        이론이 나온다. 지금 배우는 것과 다른 글을 읽는 셈이다.
     2. 1,933개 레슨 중 1,216개(63%)에 '왜' 가 없다. '이렇게 한다' 만 있고
        '안 그러면 어떻게 되는가' 가 없으면 남는 건 외울 것뿐이다.

   쓰는 규칙 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기 — 넘으면 읽다가 앞을 잊는다
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지' 를 적기
     · 용어를 쓰면 그 자리에서 풀기 */

/* 트랙별로 나눠 담는다. 각 항목은 {unit, lesson, th}. */
module.exports = {

python: [
{
  unit: "파이썬 첫걸음", lesson: "화면에 출력하기",
  th: {
    sum: "컴퓨터는 시키지 않으면 아무것도 보여 주지 않는다. `print()` 는 '이 값을 화면에 보여 줘' 라는 뜻이다.",
    body: [
      { h: "괄호 안에 보여줄 것을 넣는다", t: "`print(\"안녕\")` 이라고 쓰면 화면에 `안녕` 이 나온다. 괄호를 빠뜨리고 `print \"안녕\"` 이라고 쓰면 오류가 난다. 파이썬은 괄호를 보고 '무언가를 시키는 중' 이라고 판단하기 때문이다." },
      { h: "따옴표는 글자를 감싸는 표시다", t: "글자는 따옴표로 감싼다. 숫자는 감싸지 않는다. `print(\"7\")` 과 `print(7)` 은 화면에는 똑같이 `7` 로 보이지만, 앞엣것은 글자이고 뒤엣것은 숫자다. 글자끼리는 더할 수 없어서 나중에 계산할 때 갈린다." },
      { h: "따옴표는 화면에 안 나온다", t: "따옴표는 '여기부터 여기까지가 글자' 라고 알려 주는 표시일 뿐이다. 그래서 화면에는 나오지 않는다. 따옴표를 한쪽만 쓰면 파이썬은 글자가 어디서 끝나는지 몰라 오류를 낸다." },
    ],
    code: { c: "print(\"안녕\")     # 안녕\nprint(7)          # 7\nprint(\"7\")        # 7 — 보이는 건 같지만 글자다", cap: "괄호 안에 보여줄 것을 넣는다" },
    key: ["보여주려면 `print(...)`", "글자는 따옴표로 감싼다", "따옴표는 화면에 나오지 않는다"],
  },
},
{
  unit: "파이썬 첫걸음", lesson: "변수와 자료형",
  th: {
    sum: "변수는 값에 이름을 붙여 두는 것이다. 이름을 붙여 두면 나중에 그 이름으로 다시 꺼내 쓸 수 있다.",
    body: [
      { h: "이름 = 값", t: "`나이 = 20` 이라고 쓰면 20이라는 값에 '나이' 라는 이름이 붙는다. 여기서 `=` 는 '같다' 가 아니라 '넣는다' 는 뜻이다. 수학의 등호와 다르다 — 이걸 헷갈리면 나중에 비교할 때 막힌다." },
      { h: "글자와 숫자는 다르게 다뤄진다", t: "`20` 은 숫자이고 `\"20\"` 은 글자다. 숫자끼리 더하면 `40` 이지만 글자끼리 더하면 `\"2020\"` 이 된다. 사용자가 입력한 값은 항상 글자로 들어오므로, 계산하려면 `int()` 로 숫자로 바꿔야 한다." },
      { h: "이름은 다시 붙일 수 있다", t: "`나이 = 20` 뒤에 `나이 = 21` 이라고 쓰면 이름이 새 값을 가리킨다. 예전 값은 사라진다. 그래서 값을 바꾸기 전에 필요하면 다른 이름에 먼저 담아 둬야 한다." },
    ],
    code: { c: "나이 = 20\n이름 = \"가영\"\nprint(나이 + 1)      # 21 — 숫자 계산\nprint(\"20\" + \"1\")   # 201 — 글자 이어 붙이기", cap: "= 는 '넣는다' 는 뜻이다" },
    key: ["`이름 = 값` 으로 이름을 붙인다", "`=` 는 넣기, 비교가 아니다", "글자 `+` 는 이어 붙이기"],
  },
},
{
  unit: "파이썬 첫걸음", lesson: "조건문 if",
  th: {
    sum: "`if` 는 '이 조건이 맞을 때만 이걸 해' 라는 뜻이다. 맞지 않으면 그 부분을 통째로 건너뛴다.",
    body: [
      { h: "콜론과 들여쓰기가 범위를 정한다", t: "`if 나이 >= 18:` 처럼 끝에 콜론을 찍고, 다음 줄부터 **들여쓰기**를 한다. 들여쓴 줄들이 '조건이 맞을 때 할 일' 이다. 들여쓰기를 빠뜨리면 조건과 상관없이 항상 실행되어 버린다 — 파이썬에서 들여쓰기는 꾸미기가 아니라 문법이다." },
      { h: "비교는 등호 두 개", t: "값이 같은지 물을 때는 `==` 를 쓴다. `=` 하나는 '넣는다' 는 뜻이라 뜻이 완전히 다르다. 그 밖에 `!=`(다르다), `>=`(크거나 같다), `<=`(작거나 같다) 를 쓴다." },
      { h: "아니면 else, 또 다른 조건이면 elif", t: "`else:` 는 조건이 맞지 않았을 때 할 일이다. 조건이 여럿이면 `elif` 로 잇는다. **위에서부터 처음 맞는 것 하나만** 실행되므로, 좁은 조건을 위에 둬야 한다." },
    ],
    code: { c: "나이 = 20\nif 나이 >= 18:\n    print(\"성인\")\nelse:\n    print(\"미성년\")", cap: "콜론 뒤 들여쓴 줄이 '할 일' 이다" },
    key: ["`if 조건:` 뒤 들여쓴 줄만 조건부", "비교는 `==`, 넣기는 `=`", "`elif` 는 처음 맞는 것 하나만"],
  },
},
],

javascript: [
{
  unit: "자바스크립트 첫걸음", lesson: "출력 console.log",
  th: {
    sum: "`console.log()` 는 값을 개발자 도구 콘솔에 찍어 보는 도구다. 코드가 어떻게 도는지 확인하는 가장 기본적인 방법이다.",
    body: [
      { h: "화면이 아니라 콘솔에 나온다", t: "`console.log(\"안녕\")` 은 웹 페이지가 아니라 브라우저의 개발자 도구 콘솔에 찍힌다. 사용자에게 보여주는 것이 아니라 **만드는 사람이 확인하는 용도**다. 이걸 모르면 '왜 화면에 안 나오지' 하고 헤맨다." },
      { h: "여러 개를 한 번에", t: "`console.log(\"나이\", 20)` 처럼 쉼표로 여러 값을 넘기면 나란히 찍힌다. 변수 이름과 값을 함께 찍어 두면 나중에 무엇을 본 것인지 알아보기 쉽다." },
      { h: "글자는 따옴표로", t: "글자는 `\"안녕\"` 처럼 따옴표로 감싼다. 감싸지 않으면 자바스크립트는 그것을 변수 이름으로 알아듣고, 그런 변수가 없다며 오류를 낸다." },
    ],
    code: { c: "console.log(\"안녕\");        // 안녕\nconsole.log(\"나이\", 20);    // 나이 20\nconsole.log(1 + 2);         // 3", cap: "콘솔은 만드는 사람이 보는 곳이다" },
    key: ["`console.log` 는 콘솔에 찍는다", "쉼표로 여러 값을 함께", "글자는 따옴표로 감싼다"],
  },
},
{
  unit: "자바스크립트 첫걸음", lesson: "변수 let 과 const",
  th: {
    sum: "변수는 값에 이름을 붙여 두는 것이다. 자바스크립트는 '나중에 바꿀 것인가' 에 따라 이름 붙이는 방법이 둘로 나뉜다.",
    body: [
      { h: "바꿀 거면 let, 안 바꿀 거면 const", t: "`let` 으로 만든 이름은 나중에 다른 값을 넣을 수 있다. `const` 로 만든 이름은 다시 넣으려 하면 오류가 난다. 실수로 값을 덮어쓰는 사고를 컴파일 단계에서 막아 주므로, **먼저 `const` 로 쓰고 바꿔야 할 때만 `let`** 으로 바꾸는 것이 요즘 방식이다." },
      { h: "var 는 쓰지 않는다", t: "예전에 쓰던 `var` 는 선언한 블록 밖에서도 보이고, 같은 이름을 두 번 선언해도 오류가 안 난다. 그래서 어디서 값이 바뀌었는지 추적하기 어렵다. 새로 쓰는 코드에서는 `let` 과 `const` 만 쓴다." },
      { h: "const 는 '내용물' 까지 고정하지는 않는다", t: "`const` 는 '이 이름에 다른 것을 다시 넣지 못한다' 는 뜻이다. 배열이나 객체를 담았다면 그 **안쪽 내용은 바꿀 수 있다.** `const xs = [1]` 뒤에 `xs.push(2)` 는 된다." },
    ],
    code: { c: "const 이름 = \"가영\";\nlet 나이 = 20;\n나이 = 21;              // 된다\n// 이름 = \"나연\";       // 오류 — const 는 다시 못 넣는다", cap: "먼저 const, 바꿔야 할 때만 let" },
    key: ["기본은 `const`, 바꿀 때만 `let`", "`var` 는 쓰지 않는다", "`const` 여도 배열 안쪽은 바뀐다"],
  },
},
{
  unit: "자바스크립트 첫걸음", lesson: "비교와 조건문",
  th: {
    sum: "조건문은 '이 조건이 맞을 때만 이걸 해' 라는 뜻이다. 자바스크립트에서 비교는 반드시 등호 **세 개**로 한다.",
    body: [
      { h: "=== 를 쓰는 이유", t: "`==` 는 타입이 다르면 몰래 맞춰 보고 비교한다. 그래서 `1 == \"1\"` 이 참이 된다. 편해 보이지만 `0 == \"\"` 도 참이라 예상 못 한 곳에서 조용히 틀린다. `===` 는 타입까지 같아야 참이므로 실무에서는 이쪽만 쓴다." },
      { h: "중괄호로 범위를 정한다", t: "`if (조건) { ... }` 의 중괄호 안이 '조건이 맞을 때 할 일' 이다. 중괄호를 생략하면 **바로 다음 한 줄만** 조건에 걸린다. 나중에 줄을 하나 더 넣었다가 조건 밖에 놓이는 사고가 자주 난다 — 항상 중괄호를 쓰는 편이 안전하다." },
      { h: "거짓처럼 쓰이는 값들", t: "`0`, `\"\"`(빈 글자), `null`, `undefined` 는 조건에서 거짓으로 취급된다. 그래서 `if (수량)` 은 수량이 0일 때도 건너뛴다. '값이 없다' 와 '값이 0이다' 를 구별해야 하면 조건을 정확히 적어야 한다." },
    ],
    code: { c: "if (나이 >= 18) {\n  console.log(\"성인\");\n} else {\n  console.log(\"미성년\");\n}\n\n1 === \"1\"   // false — 이게 안전하다", cap: "비교는 === , 범위는 중괄호" },
    key: ["비교는 항상 `===`", "중괄호를 생략하지 않는다", "`0` 과 빈 글자는 거짓으로 쓰인다"],
  },
},
],

sql: [
{
  unit: "데이터 조회 기초", lesson: "SELECT 로 가져오기",
  th: {
    sum: "SQL 은 '무엇을 원하는지' 를 적는 말이다. 어떻게 찾을지는 데이터베이스가 알아서 정한다.",
    body: [
      { h: "무엇을 · 어디서", t: "`SELECT 이름 FROM 회원` 은 '회원 표에서 이름 칸을 가져와' 라는 뜻이다. `SELECT` 뒤에는 원하는 칸 이름을, `FROM` 뒤에는 표 이름을 적는다. 이 두 조각이 모든 조회의 뼈대다." },
      { h: "별표는 편하지만 위험하다", t: "`SELECT *` 는 모든 칸을 가져온다. 급할 때 편하지만, 나중에 표에 칸이 추가되면 필요 없는 데이터까지 딸려 온다. 실무 코드에서는 **필요한 칸만 이름으로 적는다.**" },
      { h: "표는 그대로 남는다", t: "`SELECT` 는 표를 읽기만 한다. 결과가 화면에 보여도 원래 표는 하나도 바뀌지 않는다. 그래서 조회는 마음 놓고 여러 번 해 봐도 된다 — 고치는 명령과 다른 점이다." },
    ],
    code: { c: "SELECT 이름, 도시\nFROM 회원;\n\n-- 모든 칸 (편하지만 실무에선 피한다)\nSELECT * FROM 회원;", cap: "무엇을(SELECT) 어디서(FROM)" },
    key: ["`SELECT 칸 FROM 표` 가 뼈대", "`*` 대신 필요한 칸만 적는다", "조회는 표를 바꾸지 않는다"],
  },
},
{
  unit: "데이터 조회 기초", lesson: "WHERE 로 걸러내기",
  th: {
    sum: "`WHERE` 는 '이 조건에 맞는 행만' 이라는 뜻이다. 조건에 맞지 않는 행은 결과에서 빠진다.",
    body: [
      { h: "조건을 적는 자리", t: "`SELECT 이름 FROM 회원 WHERE 도시 = '서울'` 은 도시가 서울인 사람만 가져온다. 글자를 비교할 때는 작은따옴표로 감싼다. 숫자는 감싸지 않는다 — 감싸면 글자로 취급되어 크기 비교가 이상해진다." },
      { h: "여러 조건 잇기", t: "`AND` 는 둘 다 맞아야 하고, `OR` 는 하나만 맞아도 된다. 섞어 쓸 때는 괄호로 묶어 뜻을 분명히 한다. 괄호가 없으면 `AND` 가 먼저 묶여서, 생각한 것과 다르게 걸러진다." },
      { h: "빈 칸은 = 로 못 찾는다", t: "값이 비어 있는 칸(NULL)은 '모른다' 는 뜻이라 어떤 값과 비교해도 맞지 않는다. `= NULL` 로는 절대 찾아지지 않는다. 빈 칸을 찾으려면 `IS NULL` 이라고 물어야 한다." },
    ],
    code: { c: "SELECT 이름 FROM 회원\nWHERE 도시 = '서울' AND 나이 >= 20;\n\nSELECT 이름 FROM 회원\nWHERE 나이 IS NULL;   -- = NULL 은 안 된다", cap: "조건에 맞는 행만 남긴다" },
    key: ["글자는 작은따옴표로 감싼다", "`AND` 는 둘 다, `OR` 는 하나만", "빈 칸은 `IS NULL` 로 찾는다"],
  },
},
{
  unit: "데이터 조회 기초", lesson: "정렬과 개수 제한",
  th: {
    sum: "`ORDER BY` 는 줄 세우기, `LIMIT` 은 앞에서 몇 개만 가져오기다. 순서를 정하지 않으면 순서는 보장되지 않는다.",
    body: [
      { h: "줄 세우기", t: "`ORDER BY 나이` 는 나이가 작은 것부터 세운다. 큰 것부터 원하면 뒤에 `DESC` 를 붙인다. 기준을 여럿 적으면 앞엣것이 같을 때 뒤엣것으로 가른다 — `ORDER BY 도시, 나이` 처럼." },
      { h: "안 적으면 순서가 없다", t: "`ORDER BY` 를 쓰지 않으면 데이터베이스가 편한 순서로 준다. 지금 우연히 원하는 대로 나와도, 데이터가 늘거나 저장 방식이 바뀌면 달라진다. **순서가 중요하면 반드시 적는다.**" },
      { h: "앞에서 몇 개만", t: "`LIMIT 5` 는 앞 다섯 줄만 준다. 어떤 다섯 줄인지는 `ORDER BY` 가 정한다. 둘을 함께 쓰지 않으면 '아무 다섯 줄' 이 되어 '상위 5명' 이 되지 못한다." },
    ],
    code: { c: "SELECT 이름, 나이 FROM 회원\nORDER BY 나이 DESC\nLIMIT 5;   -- 나이 많은 순 다섯 명", cap: "정렬이 있어야 '상위 몇 개' 가 된다" },
    key: ["`ORDER BY` 없이는 순서가 없다", "큰 것부터는 `DESC`", "`LIMIT` 은 `ORDER BY` 와 짝"],
  },
},
],

web: [
{
  unit: "HTML 기초", lesson: "태그와 문서 구조",
  th: {
    sum: "HTML 은 꾸미는 말이 아니라 **'이것이 무엇인지' 를 알려 주는 말**이다. 제목인지 문단인지 목록인지를 태그로 적는다.",
    body: [
      { h: "태그는 여닫는 짝이다", t: "`<p>글</p>` 처럼 여는 태그와 닫는 태그가 짝을 이룬다. 닫는 태그에는 빗금이 붙는다. 닫는 것을 빠뜨리면 브라우저가 알아서 추측하는데, 그 추측이 원하는 모양과 다를 때가 많다." },
      { h: "왜 의미가 중요한가", t: "글씨를 크게 하려고 아무 태그에나 큰 글씨를 주면 화면은 비슷해진다. 하지만 검색 엔진과 화면 낭독기는 **태그를 보고** 판단한다. 제목을 제목 태그로 적지 않으면, 눈으로 보는 사람 말고는 그것이 제목인 줄 모른다." },
      { h: "제목은 단계가 있다", t: "`<h1>` 이 가장 큰 제목이고 `<h6>` 까지 있다. 한 페이지에 `<h1>` 은 하나만 두고 그 아래로 `<h2>`, `<h3>` 순으로 내려간다. 단계를 건너뛰면 문서의 뼈대가 어그러진다." },
    ],
    code: { c: "<h1>가게 이름</h1>\n<p>오늘의 메뉴입니다.</p>\n<h2>커피</h2>\n<p>아메리카노 3000원</p>", cap: "무엇인지를 태그로 말한다" },
    key: ["태그는 여닫는 짝", "화면이 아니라 의미를 적는다", "`<h1>` 은 하나, 아래로 단계"],
  },
},
{
  unit: "HTML 기초", lesson: "링크·이미지·텍스트",
  th: {
    sum: "링크와 이미지는 태그만으로는 부족하다. **어디로 갈지, 무엇이 보이는지**를 함께 적어야 한다.",
    body: [
      { h: "링크에는 주소가 필요하다", t: "`<a href=\"/about\">소개</a>` 에서 `href` 가 갈 곳이다. 파란 글씨로 보이게 꾸며도 `href` 가 없으면 링크가 아니다. 눌러도 아무 일이 없고, 화면 낭독기도 링크로 읽지 않는다." },
      { h: "이미지에는 대체 글이 필요하다", t: "`<img src=\"cat.png\" alt=\"창가의 고양이\">` 에서 `alt` 는 그림을 볼 수 없을 때 대신 읽히는 글이다. 그림이 안 뜰 때도, 화면 낭독기를 쓸 때도 이 글이 쓰인다. 빠뜨리면 파일 이름을 그대로 읽어 버린다." },
      { h: "꾸미기용 그림은 비워 둔다", t: "선이나 장식처럼 뜻이 없는 그림은 `alt=\"\"` 로 **비워 둔다.** 비워 두면 낭독기가 조용히 건너뛴다. 아예 빼는 것과 비워 두는 것은 다르다 — 빼면 읽고, 비우면 건너뛴다." },
    ],
    code: { c: "<a href=\"/about\">소개</a>\n<img src=\"cat.png\" alt=\"창가의 고양이\">\n<img src=\"line.png\" alt=\"\">   <!-- 장식 -->", cap: "href 와 alt 를 빠뜨리지 않는다" },
    key: ["링크에는 `href` 가 있어야 한다", "이미지에는 `alt` 를 적는다", "장식 그림은 `alt=\"\"`"],
  },
},
{
  unit: "HTML 기초", lesson: "목록과 표",
  th: {
    sum: "나열되는 것은 목록으로, 칸이 나뉜 자료는 표로 적는다. 줄바꿈으로 흉내 내면 기계는 알아보지 못한다.",
    body: [
      { h: "순서가 있나 없나", t: "차례가 중요하면 `<ol>`(번호 목록), 아니면 `<ul>`(점 목록)을 쓴다. 항목은 둘 다 `<li>` 다. `<li>` 는 반드시 `<ul>` 이나 `<ol>` 안에 있어야 하고, 밖에 두면 브라우저가 제멋대로 고쳐 놓는다." },
      { h: "표는 자료를 담는 것이지 배치가 아니다", t: "`<table>` 은 행과 열로 된 **자료**를 담는 태그다. 예전에는 화면 배치에 쓰기도 했지만, 지금은 배치에 쓰면 안 된다. 낭독기가 '3행 4열 표' 라고 읽어 버려 듣는 사람이 혼란스러워진다." },
      { h: "표에는 머리 행을 준다", t: "`<th>` 는 칸의 이름을 적는 자리다. `<td>` 는 값이다. 머리 행이 있으면 낭독기가 '가격: 3000원' 처럼 이름과 값을 함께 읽어 준다. `<td>` 만 쓰면 숫자만 줄줄 읽힌다." },
    ],
    code: { c: "<ul>\n  <li>커피</li>\n  <li>차</li>\n</ul>\n\n<table>\n  <tr><th>메뉴</th><th>가격</th></tr>\n  <tr><td>커피</td><td>3000</td></tr>\n</table>", cap: "나열은 목록, 칸 자료는 표" },
    key: ["차례가 있으면 `<ol>`, 없으면 `<ul>`", "`<li>` 는 목록 안에만", "표의 이름 칸은 `<th>`"],
  },
},
],

c: [
{
  unit: "C 언어 첫걸음", lesson: "기본 구조와 출력",
  th: {
    sum: "C 프로그램은 반드시 `main` 이라는 함수에서 시작한다. 컴퓨터가 어디부터 실행할지 알아야 하기 때문이다.",
    body: [
      { h: "main 이 출발점이다", t: "`int main(void) { ... }` 안의 코드가 위에서부터 실행된다. `main` 이 없으면 컴파일은 되어도 실행 파일이 만들어지지 않는다. 마지막의 `return 0;` 은 '문제없이 끝났다' 를 운영체제에 알리는 신호다." },
      { h: "쓰기 전에 알려 줘야 한다", t: "`printf` 를 쓰려면 맨 위에 `#include <stdio.h>` 를 적는다. C 는 미리 알려 주지 않은 함수를 모른다. 이걸 빠뜨리면 '선언되지 않은 함수' 라는 경고나 오류가 난다." },
      { h: "형식 문자를 맞춰야 한다", t: "`printf(\"%d\\n\", 7)` 의 `%d` 는 '여기에 정수를 넣어라' 는 뜻이다. 정수 자리에 실수를 넣거나 반대로 하면 엉뚱한 값이 찍힌다. 오류가 아니라 **이상한 값이 조용히 나오므로** 더 위험하다." },
    ],
    code: { c: "#include <stdio.h>\n\nint main(void) {\n    printf(\"안녕\\n\");\n    printf(\"%d\\n\", 7);\n    return 0;\n}", cap: "main 에서 시작하고 0 으로 끝낸다" },
    key: ["`main` 이 출발점", "쓰기 전에 `#include`", "`%d` 는 정수 자리"],
  },
},
{
  unit: "C 언어 첫걸음", lesson: "변수와 자료형",
  th: {
    sum: "C 는 변수를 만들 때 **무엇을 담을지 미리 밝혀야** 한다. 담을 것의 크기를 알아야 자리를 잡을 수 있기 때문이다.",
    body: [
      { h: "타입을 먼저 적는다", t: "`int 나이 = 20;` 처럼 타입을 앞에 쓴다. `int` 는 정수, `double` 은 소수, `char` 는 글자 하나다. 타입을 정하고 나면 다른 종류를 담을 수 없다 — 파이썬이나 자바스크립트와 크게 다른 점이다." },
      { h: "값을 안 넣으면 쓰레기가 들어 있다", t: "`int n;` 이라고만 쓰면 그 자리에 예전에 쓰던 값이 그대로 남아 있다. 0이 아니다. 그 상태로 읽으면 실행할 때마다 다른 값이 나온다. **만들 때 바로 값을 넣는 습관**이 이 사고를 막는다." },
      { h: "정수끼리 나누면 정수", t: "`7 / 2` 는 `3` 이다. 소수점이 잘린다. 소수가 필요하면 한쪽을 실수로 만든다 — `7 / 2.0` 또는 `(double)7 / 2`. 평균을 구할 때 이걸 놓쳐서 값이 뭉툭해지는 일이 흔하다." },
    ],
    code: { c: "int 나이 = 20;      // 값을 바로 넣는다\ndouble 키 = 175.5;\nchar 등급 = 'A';\n\nprintf(\"%d\\n\", 7 / 2);      // 3\nprintf(\"%.1f\\n\", 7 / 2.0);  // 3.5", cap: "타입을 먼저, 값은 바로" },
    key: ["타입을 먼저 적는다", "값을 안 넣으면 쓰레기가 남아 있다", "정수끼리 나누면 소수점이 잘린다"],
  },
},
{
  unit: "C 언어 첫걸음", lesson: "연산자",
  th: {
    sum: "연산자는 값을 가지고 무언가를 하는 기호다. 순서와 뜻을 헷갈리면 오류 없이 답만 틀린다.",
    body: [
      { h: "= 와 == 는 완전히 다르다", t: "`=` 는 '넣는다', `==` 는 '같은가' 다. `if (n = 0)` 이라고 쓰면 n 에 0을 넣고 그 값이 거짓이라 블록을 건너뛴다. 컴파일 오류가 아니라서 찾기 어렵다 — C 에서 가장 오래된 함정이다." },
      { h: "나머지 연산자", t: "`%` 는 나눈 나머지다. `10 % 3` 은 `1` 이다. 짝수인지 볼 때 `n % 2 == 0` 처럼 쓴다. 음수에서는 결과 부호가 언어마다 달라 주의가 필요하다." },
      { h: "++ 는 자리에 따라 다르다", t: "`n++` 는 '지금 값을 쓰고 나서 1 늘린다', `++n` 은 '1 늘리고 나서 그 값을 쓴다' 다. 혼자 한 줄에 쓸 때는 같지만, 다른 식 안에 섞으면 결과가 달라진다. 헷갈리면 줄을 나누는 편이 낫다." },
    ],
    code: { c: "int n = 5;\nprintf(\"%d\\n\", n % 2);   // 1 — 홀수\n\nint a = n++;   // a 는 5, n 은 6\nint b = ++n;   // n 이 7 이 되고 b 도 7", cap: "= 는 넣기, == 는 비교" },
    key: ["`=` 는 넣기, `==` 는 비교", "`%` 는 나머지", "`n++` 와 `++n` 은 시점이 다르다"],
  },
},
],

cpp: [
{
  unit: "C++ 첫걸음", lesson: "입출력 cout / cin",
  th: {
    sum: "C++ 는 `cout` 으로 내보내고 `cin` 으로 받는다. 화살표 모양이 **값이 흘러가는 방향**을 나타낸다.",
    body: [
      { h: "화살표의 방향", t: "`std::cout << 값` 은 값이 화면 쪽으로 흘러간다. `std::cin >> 변수` 는 입력이 변수 쪽으로 흘러온다. 방향을 거꾸로 쓰면 컴파일 오류가 나므로, 모양을 보고 기억하면 헷갈리지 않는다." },
      { h: "줄바꿈 넣기", t: "`std::endl` 이나 `\"\\n\"` 을 함께 흘려보내면 줄이 바뀐다. `endl` 은 줄바꿈에 더해 화면에 즉시 내보내기까지 하므로 조금 느리다. 많이 찍는 반복문에서는 `\"\\n\"` 쪽이 낫다." },
      { h: "std:: 를 붙이는 이유", t: "`std` 는 표준 라이브러리의 이름표다. `using namespace std;` 로 생략할 수도 있지만, 내가 만든 이름과 부딪힐 수 있어 큰 코드에서는 권하지 않는다. 처음에는 붙여 쓰는 습관을 들이는 편이 안전하다." },
    ],
    code: { c: "#include <iostream>\n\nint main() {\n    int n;\n    std::cin >> n;\n    std::cout << \"입력값: \" << n << \"\\n\";\n    return 0;\n}", cap: "<< 는 내보내기, >> 는 받기" },
    key: ["`cout <<` 내보내기, `cin >>` 받기", "줄바꿈은 `\"\\n\"` 이 가볍다", "`std::` 는 표준 라이브러리 이름표"],
  },
},
{
  unit: "C++ 첫걸음", lesson: "변수와 자료형",
  th: {
    sum: "C++ 도 타입을 먼저 적는다. 다만 컴파일러가 알아낼 수 있으면 `auto` 로 맡길 수 있다.",
    body: [
      { h: "만들 때 값을 넣는다", t: "`int n;` 만 쓰면 그 자리에 예전 값이 남아 있어 읽을 때마다 다른 값이 나온다. `int n = 0;` 또는 `int n{0};` 처럼 만들 때 바로 넣는다. 중괄호 방식은 값이 잘려 들어가는 경우를 컴파일 단계에서 막아 준다." },
      { h: "문자열은 std::string", t: "C 의 문자 배열 대신 `std::string` 을 쓴다. 길이를 알아서 관리해 주고 `+` 로 이어 붙일 수 있다. `#include <string>` 을 잊으면 못 쓴다." },
      { h: "auto 는 편하지만 참조를 떨어뜨린다", t: "`auto x = v[0];` 은 값을 **복사**한다. 원소를 고치려면 `auto& x` 라고 써야 한다. 이 앰퍼샌드 하나를 빠뜨려 '고쳤는데 안 바뀐다' 로 헤매는 일이 아주 흔하다." },
    ],
    code: { c: "#include <string>\n\nint n{0};\ndouble ratio = 1.5;\nstd::string name = \"가영\";\n\nauto a = v[0];    // 복사\nauto& b = v[0];   // 원소 자체", cap: "만들 때 값을 넣고, auto 에는 & 를 살핀다" },
    key: ["만들 때 값을 넣는다", "문자열은 `std::string`", "`auto` 는 복사, `auto&` 는 원본"],
  },
},
{
  unit: "C++ 첫걸음", lesson: "조건과 반복",
  th: {
    sum: "조건은 `if`, 반복은 `for` 와 `while` 이다. 컨테이너를 돌 때는 범위 기반 `for` 가 가장 안전하다.",
    body: [
      { h: "범위 기반 for", t: "`for (int x : v)` 라고 쓰면 인덱스를 다룰 일이 없어 경계를 넘는 실수가 사라진다. 다만 `x` 는 **복사본**이라 값을 고쳐도 컨테이너는 그대로다. 고치려면 `for (int& x : v)`, 읽기만 하면 `for (const auto& x : v)` 다." },
      { h: "중괄호를 생략하지 않는다", t: "`if (조건) 한줄;` 처럼 중괄호를 빼면 바로 다음 한 줄만 조건에 걸린다. 나중에 줄을 하나 더 넣었다가 조건 밖에 놓이는 사고가 잦다. 항상 중괄호를 쓰는 편이 안전하다." },
      { h: "무한 반복을 막는 것", t: "`while` 은 조건이 거짓이 되어야 끝난다. 반복 안에서 조건에 쓰이는 값이 **반드시 변해야** 한다. 변하지 않으면 프로그램이 멈추지 않는다 — 조건에 쓰는 값이 어디서 바뀌는지 먼저 확인한다." },
    ],
    code: { c: "for (const auto& x : v) {\n    std::cout << x << \"\\n\";\n}\n\nint i = 0;\nwhile (i < 3) {\n    i++;          // 이게 없으면 안 끝난다\n}", cap: "읽기는 const auto&, 고치기는 auto&" },
    key: ["범위 기반 `for` 가 안전하다", "중괄호를 생략하지 않는다", "`while` 조건 값이 변해야 끝난다"],
  },
},
],

java: [
{
  unit: "Java 첫걸음", lesson: "출력과 기본 구조",
  th: {
    sum: "자바는 모든 코드가 **클래스 안**에 들어간다. 그리고 프로그램은 `main` 메서드에서 시작한다.",
    body: [
      { h: "파일 이름과 클래스 이름을 맞춘다", t: "`public class Hello` 는 반드시 `Hello.java` 파일에 있어야 한다. 이름이 다르면 컴파일러가 거부한다. 처음 자바를 쓸 때 가장 자주 만나는 오류이고, 규칙을 알면 바로 풀린다." },
      { h: "main 의 모양은 정해져 있다", t: "`public static void main(String[] args)` 는 통째로 외우는 편이 빠르다. 하나라도 다르면 자바가 시작점으로 알아보지 못하고 '메인 메서드를 찾을 수 없다' 며 멈춘다." },
      { h: "출력 두 가지", t: "`System.out.println` 은 찍고 줄을 바꾸고, `System.out.print` 는 줄을 바꾸지 않는다. 줄바꿈 여부만 다르다 — 여러 값을 한 줄에 이어 찍을 때 뒤엣것을 쓴다." },
    ],
    code: { c: "public class Hello {\n    public static void main(String[] args) {\n        System.out.println(\"안녕\");\n    }\n}", cap: "클래스 이름과 파일 이름을 맞춘다" },
    key: ["코드는 클래스 안에", "`main` 의 모양은 고정", "`println` 은 줄바꿈, `print` 는 안 함"],
  },
},
{
  unit: "Java 첫걸음", lesson: "변수와 흐름 제어",
  th: {
    sum: "자바도 타입을 먼저 적는다. 그리고 글자 비교는 `==` 가 아니라 `.equals()` 로 한다.",
    body: [
      { h: "기본형과 참조형", t: "`int`, `double`, `boolean` 은 값을 그대로 담는다(기본형). `String` 이나 배열은 값이 있는 곳을 가리킨다(참조형). 이 차이가 비교할 때 드러난다." },
      { h: "글자 비교는 .equals()", t: "`==` 는 '같은 물건인가' 를 묻는다. 글자가 같아도 다른 물건이면 거짓이다. 값이 같은지 물으려면 `a.equals(b)` 를 쓴다. 짧은 글자에서는 `==` 가 우연히 맞기도 해서, **테스트에서는 맞고 실제로는 틀리는** 모양이 된다." },
      { h: "조건과 반복", t: "`if`·`for`·`while` 은 C 계열과 모양이 같다. 배열이나 목록을 돌 때는 `for (int x : xs)` 형태가 짧고 경계 실수가 없다. 중괄호는 생략하지 않는 편이 안전하다." },
    ],
    code: { c: "String a = \"kim\";\nString b = new String(\"kim\");\na == b          // false — 다른 물건\na.equals(b)     // true  — 값은 같다\n\nfor (int x : xs) { … }", cap: "글자 비교는 .equals()" },
    key: ["타입을 먼저 적는다", "글자 비교는 `.equals()`", "목록은 `for (T x : xs)` 로"],
  },
},
],

go: [
{
  unit: "Go 첫걸음", lesson: "출력과 기본 구조",
  th: {
    sum: "Go 프로그램은 `package main` 으로 시작하고 `func main()` 에서 실행된다. 규칙이 적고 엄격하다.",
    body: [
      { h: "안 쓰는 것은 오류다", t: "가져오기(import)해 놓고 안 쓰거나, 변수를 만들어 놓고 안 쓰면 **컴파일 오류**다. 경고가 아니라 오류라서 반드시 지워야 한다. 처음에는 답답하지만, 죽은 코드가 쌓이지 않는다." },
      { h: "출력 세 가지", t: "`fmt.Println` 은 값을 찍고 줄을 바꾼다. `fmt.Printf` 는 형식을 정해 찍는다. `fmt.Sprintf` 는 화면에 찍지 않고 **문자열로 돌려준다** — 로그 메시지를 만들 때 쓴다." },
      { h: "세미콜론을 안 쓴다", t: "줄 끝에 세미콜론을 붙이지 않는다. 대신 여는 중괄호를 **같은 줄에** 둬야 한다. 다음 줄로 내리면 Go 가 자동으로 문장을 끊어 버려 오류가 난다." },
    ],
    code: { c: "package main\n\nimport \"fmt\"\n\nfunc main() {\n    fmt.Println(\"안녕\")\n    fmt.Printf(\"%d개\\n\", 3)\n}", cap: "안 쓰는 import 와 변수는 오류다" },
    key: ["`package main` + `func main()`", "안 쓰는 import·변수는 오류", "여는 중괄호는 같은 줄에"],
  },
},
{
  unit: "Go 첫걸음", lesson: "변수와 반복",
  th: {
    sum: "Go 는 `:=` 로 변수를 짧게 만든다. 그리고 반복문은 `for` 하나뿐이다.",
    body: [
      { h: "짧은 선언", t: "`n := 10` 은 타입을 알아서 정하며 변수를 만든다. 함수 **안에서만** 쓸 수 있다. 함수 밖에서는 `var n = 10` 이라고 적는다. 이미 있는 변수에 `:=` 를 다시 쓰면 오류가 난다." },
      { h: "제로값", t: "`var n int` 는 자동으로 `0` 이 된다. 문자열은 `\"\"`, 불리언은 `false`, 포인터·맵·슬라이스는 `nil` 이다. 쓰레기 값이 남는 C 와 달리 항상 정해진 값이라 안전하다 — 다만 '값이 없음' 과 '0' 을 구별하려면 따로 표시해야 한다." },
      { h: "for 하나로 다 한다", t: "Go 에는 `while` 이 없다. `for 조건 { }` 이 while 이고, `for { }` 는 무한 반복이다. 목록을 돌 때는 `for i, v := range xs` 인데, **첫 번째가 인덱스이고 두 번째가 값**이다. 값만 필요하면 `for _, v := range xs` 로 인덱스를 버린다." },
    ],
    code: { c: "n := 10\nvar s string      // \"\" 로 시작한다\n\nfor _, v := range xs {\n    fmt.Println(v)\n}", cap: "range 는 인덱스가 먼저 나온다" },
    key: ["함수 안에서는 `:=`", "제로값이 자동으로 채워진다", "`range` 는 인덱스가 먼저"],
  },
},
],

arduino: [
{
  unit: "아두이노 첫걸음", lesson: "아두이노란?",
  th: {
    sum: "아두이노는 작은 컴퓨터가 달린 보드다. 전기 신호를 읽고 내보내서 **바깥 세상과 이어지는** 것이 목적이다.",
    body: [
      { h: "컴퓨터와 다른 점", t: "화면도 키보드도 없다. 대신 핀이라는 금속 다리가 있어서 전선을 꽂는다. 프로그램은 컴퓨터에서 작성해 USB 로 보드에 옮겨 넣는다. 옮기고 나면 컴퓨터 없이 전원만 있으면 혼자 돈다." },
      { h: "setup 과 loop", t: "`setup()` 은 전원이 켜질 때 **한 번만** 돈다. 준비 작업을 여기에 적는다. `loop()` 는 그 뒤로 **끝없이 반복**된다. 준비 작업을 실수로 loop 에 적으면 매번 다시 준비하느라 이상하게 동작한다." },
      { h: "코드가 맞아도 안 될 수 있다", t: "아두이노는 배선이 함께 맞아야 동작한다. 코드가 완벽해도 전선 하나가 빠져 있으면 아무 일도 일어나지 않는다. '안 되면 코드보다 배선을 먼저 본다' 가 기본이다." },
    ],
    code: { c: "void setup() {\n    pinMode(13, OUTPUT);   // 한 번만\n}\n\nvoid loop() {\n    digitalWrite(13, HIGH);  // 끝없이 반복\n}", cap: "setup 은 한 번, loop 는 계속" },
    key: ["핀으로 바깥 세상과 잇는다", "`setup` 은 한 번, `loop` 는 반복", "안 되면 배선을 먼저 본다"],
  },
},
{
  unit: "아두이노 첫걸음", lesson: "핀과 제어",
  th: {
    sum: "핀은 전기가 드나드는 문이다. 각 핀을 '내보내는 문' 으로 쓸지 '받아들이는 문' 으로 쓸지 먼저 정해 준다.",
    body: [
      { h: "먼저 방향을 정한다", t: "`pinMode(13, OUTPUT)` 은 13번 핀을 내보내기용으로 쓰겠다는 선언이다. 읽으려면 `INPUT` 이다. 이걸 빠뜨리면 값이 제대로 나가지도 들어오지도 않는다 — `setup()` 에서 한 번 정해 둔다." },
      { h: "디지털은 둘, 아날로그는 여럿", t: "`digitalWrite` 는 켬(HIGH)과 끔(LOW) 둘 중 하나만 보낸다. `analogRead` 는 0~5V 사이의 값을 0~1023 숫자로 읽는다. 밝기나 온도처럼 정도가 있는 값은 아날로그 쪽이다." },
      { h: "delay 는 모든 것을 멈춘다", t: "`delay(1000)` 은 1초 동안 **아무것도 못 하게** 만든다. 그동안 버튼을 눌러도 반응하지 않는다. 여러 일을 함께 하려면 `millis()` 로 시각을 재서 '지날 때가 됐는지' 확인하는 방식으로 바꾼다." },
    ],
    code: { c: "void setup() {\n    pinMode(13, OUTPUT);\n    pinMode(2, INPUT);\n}\n\nvoid loop() {\n    if (digitalRead(2) == HIGH)\n        digitalWrite(13, HIGH);\n}", cap: "방향을 먼저 정하고 쓴다" },
    key: ["`pinMode` 로 방향을 먼저 정한다", "디지털은 켬·끔, 아날로그는 0~1023", "`delay` 는 그동안 아무것도 못 한다"],
  },
},
],

};
