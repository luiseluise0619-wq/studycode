module.exports = {
  track: "dl", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 층의 성질과 크기 계산",
  source: "./in_dl.cjs",
  lessons: [
    { t: "층마다 다른 성질", n: 8,
      th: { sum: "활성화·정규화·드롭아웃은 **학습과 추론에서 다르게 돌거나**, **기울기를 다르게 흘린다**. 그 차이가 곧 버그의 자리다.",
        body: [
          { h: "무엇을 통과시키는가", t: "ReLU 는 음수를 0 으로 만들고 양수 쪽 기울기가 1 이라 깊게 쌓아도 기울기가 흐르지만, 죽은 뉴런은 안 살아난다. 시그모이드는 0~1 을 내어 확률로 쓰기 좋은 대신 양 끝 기울기가 0 이라 은닉층에는 안 쓴다 — 마지막 층에 두었다면 손실은 <b>로짓을 직접 받는</b> 것을 써야 수치가 안정적이다. 소프트맥스는 합이 1 이라 <b>서로 경쟁</b>하므로 멀티라벨에는 못 쓴다." },
          { h: "학습 때와 추론 때가 다르다", t: "드롭아웃은 추론에서 <b>꺼야</b> 하고, 배치 정규화는 학습 중 모아 둔 <b>이동 평균</b>으로 바뀐다 — '학습은 잘되는데 추론만 이상하다' 의 단골 원인이라 두 가지를 함께 확인한다. 배치가 작으면 통계가 흔들리므로 층 정규화가 안정적이다(트랜스포머가 그쪽을 택한 이유). <b>기울기 소실</b>은 1 보다 작은 값이 층마다 곱해져 생기고, 대책은 ReLU 계열·정규화 층·<b>지름길 연결</b> 셋이다." }],
        code: { c: "model.eval()          # 드롭아웃·BN 전환\nBCEWithLogitsLoss()   # 로짓을 직접 받는다\nout = layer(x) + x    # 지름길 연결", cap: "학습과 추론이 같은 층인지 묻는다" },
        key: ["소프트맥스는 합이 1 이라 경쟁한다", "추론에서 드롭아웃을 끈다", "BN 은 배치, LN 은 특성 단위", "지름길이 기울기를 흘려 준다"] } },
    { t: "크기와 학습을 계산으로 잡기", n: 7,
      th: { sum: "층을 쌓기 전에 **크기와 파라미터 수를 손으로 적어 내려가면** 대부분의 형상 오류와 메모리 사고가 미리 잡힌다.",
        body: [
          { h: "크기와 개수", t: "합성곱 출력은 <code>⌊(n + 2p - k)/s⌋ + 1</code> 로 <b>커널이 놓일 시작 자리의 개수</b>다 — 크기를 유지하려면 스트라이드 1 에 패딩 <code>(k-1)/2</code> 다. 완전 연결층의 파라미터는 <code>입력 × 출력 + 출력</code> 이라 <b>입력 크기에 비례해 폭발</b>하고, 그래서 이미지에는 합성곱을 쓴다. 어휘가 크면 <b>임베딩 표</b>가 파라미터의 절반을 넘기도 해, 입·출력에서 같은 표를 나눠 쓰면 크게 줄어든다." },
          { h: "학습을 안정시키는 손잡이", t: "아담은 <b>모멘텀</b>(방향의 관성)과 파라미터별 보폭을 합친 것이고, 초기 편향 보정 항이 함께 들어 있다 — 가중치 감쇠를 제대로 걸려면 <code>AdamW</code> 를 쓴다. 초반에는 학습률을 서서히 올리는 <b>워밍업</b>이 필요하고, 손실이 <code>NaN</code> 이 되면 이 구간을 늘리는 것이 첫 조치다. 검증 손실이 오르기 시작하면 <b>조기 종료</b> 하되, 멈춘 시점이 아니라 <b>가장 좋았던 시점</b>을 저장해야 한다. 어텐션은 시퀀스 길이의 <b>제곱</b>에 비례해 늘어난다." }],
        code: { c: "out = ⌊(n + 2p - k) / s⌋ + 1\nparams = in × out + out\nscore = QKᵀ / √d          # 차원이 커지면 나눈다", cap: "설계는 계산으로 확인한다" },
        key: ["출력 크기는 시작 자리의 개수", "FC 파라미터는 입력에 비례", "워밍업 → 코사인 감쇠", "가장 좋았던 체크포인트를 저장한다"] } },
  ],
};
