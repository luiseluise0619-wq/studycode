module.exports = {
  track: "ml", guide: "🏗️", xp: 80,
  unit: "설계 · 직접 그리는 학습 파이프라인",
  source: "./arch_ml.cjs",
  lessons: [
    { t: "재현하고, 어긋나지 않게", n: 5,
      th: { sum: "학습 파이프라인 설계의 두 축은 **다시 만들 수 있는가**와 **학습과 서빙이 같은 것을 보는가**다.",
        body: [
          { h: "입력을 전부 값으로", t: "코드만 고정해서는 재현되지 않는다. 데이터는 <code>latest</code> 가 아니라 <b>버전</b>으로 가리키고, 하이퍼파라미터와 seed 를 파일로 남기고, 산출된 모델에 그 셋을 태그로 붙인다. 실험 추적기는 사치가 아니라 <b>비교의 전제</b>다 — 무엇을 바꿔 얼마나 좋아졌는지 말할 수 없으면 결론도 없다." },
          { h: "정의를 하나만 둔다", t: "같은 특성을 학습용 SQL 과 서빙용 코드가 각각 계산하면 언젠가 갈라진다(학습·서빙 왜곡). 정의를 한 곳에 두고 두 경로가 그것을 읽게 하며, 같은 입력에 두 값이 같은지 <b>대조 검사</b>를 주기적으로 돌린다. 오프라인 조회는 반드시 <b>시점 기준</b>이어야 미래가 새어 들지 않는다." }],
        code: { c: "dataset: s3://ds/orders/v7   (latest 금지)\nseed: 42 · commit: 9f1c2a\nparity: 같은 입력 → 오프라인·온라인 특성값 비교", cap: "재현성은 입력을 값으로 만드는 일" },
        key: ["데이터·설정·seed 를 고정한다", "모델에 입력 버전을 태그로", "특성 정의는 한 곳에", "오프라인 조회는 시점 기준"] } },
  ],
};
