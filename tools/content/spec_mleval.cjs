module.exports={
  track:"mleval", guide:"📐", xp:80,
  unit:"실행형 실전 — 지표를 직접 만든다",
  source:"./exec_mlmet.cjs", slice:[6,12],
  lessons:[
    { t:"분류 지표의 경계", n:3,
      th:{ sum:"지표는 공식이 아니라 **경계 처리**에서 갈린다 — 분모가 0인 경우, 점수가 겹치는 경우, 순서가 다른 경우.",
        body:[
          {h:"분모가 0인 경우가 정상이다", t:"아무것도 양성으로 예측하지 않으면 정밀도의 분모가 0, 실제 양성이 없으면 재현율의 분모가 0이다 — 학습 초기와 극단적 불균형에서 반드시 겪는다. 지표 계산이 예외로 죽으면 **몇 시간 돌린 학습이 검증 단계에서 날아간다**. 0으로 두는 것이 관례지만, '0 = 다 틀렸다' 와 '0 = 예측한 게 없다' 는 다르므로 분모를 함께 로그에 남긴다."},
          {h:"동점과 순서", t:"AUC 는 '양성 하나가 음성 하나보다 높은 점수를 받을 확률' 이라 **동점을 0.5로** 세야 한다 — 트리·앙상블은 같은 잎의 샘플에 똑같은 점수를 주므로 동점이 대량으로 발생하고, 무시하면 값이 체계적으로 낮게 나온다. AP 는 반대로 **순서에 민감**해서, 같은 개수를 맞혀도 앞에서 맞히면 높고 뒤에서 맞히면 낮다."}],
        code:{ c:"prec = tp / (tp + fp) if tp + fp else 0.0\n# 0 과 '정의되지 않음' 은 다르다 — 분모도 로그에 남긴다", cap:"학습 루프를 죽이지 않는 것이 먼저다" },
        key:["분모 0 은 예외가 아니라 정상 상황","AUC 의 동점은 0.5점","AP 는 상위 결과의 순서를 본다","불균형에서는 AUC 보다 AP"] } },
    { t:"확률·임계값·평균 방식", n:3,
      th:{ sum:"모델을 배포할 때 실제로 정하는 것은 가중치가 아니라 **확률을 믿을지, 어디서 자를지, 무엇을 평균할지**다.",
        body:[
          {h:"확률과 임계값", t:"정확도와 **보정**은 다른 문제다 — '90% 확신' 이라고 말한 예측 100건 중 90건이 맞아야 그 확률로 기대값을 계산할 수 있다. ECE 는 그 어긋남을 재는데, 칸마다 든 개수가 다르므로 **가중 평균**이어야 한다. 임계값은 통계가 아니라 제품이 정한다: '정밀도 95% 는 지켜야 한다' 는 제약 아래 **가장 낮은 임계값**을 고르면 그것이 재현율 최대 지점이며, 평가셋이 아니라 별도 검증셋에서 정해야 한다."},
          {h:"무엇을 평균하는가", t:"단일 라벨 다중 클래스에서 **micro F1 = 정확도**다. 그래서 90%가 한 클래스인 데이터에서 '전부 그 클래스' 라고 답하는 모델의 micro 는 0.9 로 훌륭해 보이고, macro 는 0.47 로 실패를 드러낸다. 반대로 macro 는 샘플 3개짜리 클래스를 10만 개짜리와 동등하게 취급하므로, 어느 쪽이 옳은지는 **지표가 아니라 목적**이 정한다."}],
        code:{ c:"# 제약 안에서 재현율을 최대로\nfor t in sorted(set(scores)):\n    if precision_at(t) >= target:\n        return t   # 가장 낮은 임계값", cap:"가장 정밀한 임계값을 고르면 쓸모가 없다" },
        key:["보정은 정확도와 별개 문제","ECE 는 칸 크기로 가중 평균","임계값은 검증셋에서, 제약 아래 최저값으로","micro F1 = 정확도, macro 는 소수 클래스를 드러낸다"] } },
  ]
};
