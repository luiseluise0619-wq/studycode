/* Git 진단 유닛 — 증상 하나를 주고 원인을 좁히게 한다.
   설명을 읽어 아는 것과 화면에 뜬 메시지를 보고 원인을 대는 것은 다른 능력이다.

   보기 규칙: 네 보기의 길이를 흩는다. 정답이 가장 긴 문항·중간인 문항·
   가장 짧은 문항이 고르게 섞여야 길이가 단서가 되지 않는다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const D=(k,q,o,a,ex)=>({t:"choice",cat:"debug",k,q,o,a,ex});
const I=(k,q,a,ex)=>({t:"input",cat:"debug",k,q,a,ex});

module.exports=shuffleUnits([

{ t:"진단 — 증상에서 원인으로", l:[

  { t:"밀고 당길 때 막히는 것들", xp:40,
    th:{ sum:"Git 의 오류 메시지는 불친절해 보이지만 **거의 언제나 정확하다.** 문제는 메시지가 가리키는 곳과 사람이 보는 곳이 다르다는 것이다.",
      body:[
        {h:"거부는 대개 '내가 모르는 커밋이 저기 있다'", t:"`rejected (non-fast-forward)` 는 **원격에 내가 안 가진 커밋이 있다**는 뜻 하나다. 남이 밀었거나, 내가 rebase 로 이력을 갈아 끼웠거나 둘 중 하나다. 답은 `--force` 가 아니라 **먼저 가져와 얹는 것**이고, 정말 밀어야 한다면 `--force-with-lease` 로 '내가 본 그 상태일 때만' 조건을 건다. 그냥 `--force` 는 그 사이 남이 올린 것을 말없이 지운다."},
        {h:"추적과 무시는 순서가 있다", t:"`.gitignore` 는 **아직 추적되지 않은 파일**에만 듣는다. 이미 커밋된 파일은 무시 목록에 넣어도 계속 따라오므로 `git rm --cached` 로 색인에서 먼저 빼야 한다. 줄바꿈 때문에 파일 전체가 바뀐 것으로 보이는 것도 같은 종류다 — 내용이 아니라 **저장 방식**이 달라진 것이라 `core.autocrlf` 와 `.gitattributes` 로 규칙을 못 박는다."}],
      code:{ c:"! [rejected] main -> main (non-fast-forward)\n\n# 원격에 내가 모르는 커밋이 있다\n#   → git fetch && git rebase origin/main\n#   정말 밀어야 하면 --force-with-lease", cap:"force 가 아니라 먼저 가져온다" },
      key:["거부는 '모르는 커밋이 있다'","force 대신 force-with-lease","gitignore 는 추적 전 파일에만","줄바꿈 차이는 attributes 로"] },
    q:[
      D("푸시 거부","<code>push</code> 가 <code>non-fast-forward</code> 로 거부됩니다. 원인으로 알맞은 것은?",
        ["원격에 내가 가지지 않은 커밋이 있다",
         "커밋 메시지가 규칙에 맞지 않게 작성되었다",
         "브랜치 이름이 원격과 다르게 지어져 있다",
         "로컬에 아직 커밋하지 않은 변경이 남아 있다"],0,
        "**원격이 앞서 있다**는 뜻입니다. 남이 밀었거나 내가 이력을 갈아 끼웠거나 둘 중 하나입니다.\n먼저 `fetch` 해서 얹은 뒤 다시 밀면 됩니다. 커밋하지 않은 변경이 있으면 그것은 다른 메시지로 막힙니다.\n메시지나 브랜치 이름은 이 거부와 관계가 없습니다."),
      D("강제로 밀 때","이력을 정리한 뒤 원격에 밀어야 할 때 안전한 방법으로 알맞은 것은?",
        ["--force-with-lease 로 내가 본 상태일 때만 민다",
         "--force 로 밀고 문제가 생기면 그때 되살린다",
         "원격 브랜치를 지운 뒤 같은 이름으로 새로 만들어 올린다",
         "머지 커밋을 하나 만들어 두 이력을 그대로 합친다"],0,
        "**`--force-with-lease`** 는 원격이 내가 마지막으로 본 그 상태일 때만 밀어 줍니다. 그 사이 남이 올렸다면 거부됩니다.\n그냥 `--force` 는 남의 커밋을 말없이 지웁니다 — 되살릴 수는 있지만 그 사실을 아무도 모르는 것이 문제입니다.\n지웠다 다시 만드는 것은 force 와 같은 위험에 더해 보호 규칙과 PR 까지 끊어 버립니다."),
      D("무시가 안 듣는다",".gitignore 에 넣었는데 그 파일이 계속 변경 목록에 뜹니다. 원인으로 알맞은 것은?",
        ["이미 추적 중인 파일이기 때문",
         "무시 규칙의 경로 표기가 절대 경로여야 하기 때문",
         "무시 파일이 저장소 뿌리에 있어야만 동작하기 때문",
         "무시 규칙은 커밋해야만 적용되기 때문"],0,
        "`.gitignore` 는 **아직 추적되지 않은 파일**에만 듣습니다. 한 번 커밋된 파일은 계속 따라옵니다.\n`git rm --cached <파일>` 로 색인에서 빼고 커밋하면 그때부터 무시됩니다 — 작업 디렉터리의 파일은 그대로 남습니다.\n무시 파일은 하위 폴더에도 둘 수 있고, 커밋하지 않아도 로컬에서는 적용됩니다."),
      D("파일 전체가 변경으로","한 줄만 고쳤는데 파일 전체가 바뀐 것으로 보입니다. 가장 그럴듯한 원인은?",
        ["줄바꿈 방식이 달라졌다",
         "파일 권한 비트가 함께 바뀌었다",
         "편집기가 파일을 다시 만들어 저장했다",
         "diff 를 계산하는 알고리즘이 바뀌었다"],0,
        "**줄바꿈(CRLF/LF) 차이**가 가장 흔한 원인입니다. 내용은 같은데 모든 줄이 바뀐 것으로 보입니다.\n`.gitattributes` 에 `* text=auto` 처럼 규칙을 못 박아 두면 팀 전체가 같은 방식으로 저장합니다.\n권한 비트 변경은 별도 줄로 표시되고, 인코딩(UTF-8 ↔ EUC-KR)이 바뀐 경우도 같은 증상을 냅니다."),
      D("큰 파일 거부","용량이 큰 파일 때문에 push 가 거부됩니다. 올바른 대응으로 알맞은 것은?",
        ["이력에서 그 파일을 걷어 내고 대용량 저장소를 쓴다",
         "그 파일을 지우는 커밋을 하나 더 얹어서 다시 민다",
         "커밋을 여러 개로 쪼개 조금씩 나눠서 민다",
         "압축한 뒤 같은 자리에 다시 커밋한다"],0,
        "**이력에 한 번 들어간 파일은 지우는 커밋을 얹어도 이력에 남습니다.** 저장소 크기도 그대로입니다.\n이력에서 걷어 내려면 이력을 다시 쓰는 도구가 필요하고, 그 뒤에는 모두가 새로 클론해야 합니다.\n애초에 대용량 파일은 Git LFS 나 객체 스토리지에 두고 저장소에는 참조만 두는 것이 답입니다."),
      D("HEAD 가 떨어졌다","<code>detached HEAD</code> 상태에서 커밋한 뒤 다른 브랜치로 옮겼습니다. 그 커밋을 지키는 방법으로 알맞은 것은?",
        ["그 커밋 해시에 브랜치를 붙인다",
         "다시 detached HEAD 로 돌아가 커밋을 반복한다",
         "작업 디렉터리를 복사해 두었다가 붙여 넣는다",
         "원격에 밀어 두면 자동으로 브랜치가 생긴다"],0,
        "**커밋은 남아 있고 가리키는 이름만 없는 상태**입니다. `git branch <이름> <해시>` 로 이름을 붙이면 됩니다.\n해시를 잊었다면 `git reflog` 에 남아 있습니다 — HEAD 가 지나온 모든 자리가 기록됩니다.\n이름 없는 커밋은 정리 작업 때 사라질 수 있으므로 미루지 않는 편이 좋습니다."),
      D("인증 실패","어제까지 되던 push 가 갑자기 인증에 실패합니다. 먼저 확인할 것으로 알맞은 것은?",
        ["토큰이나 키가 만료되었는지",
         "저장소의 브랜치 보호 규칙이 바뀌었는지",
         "커밋 작성자 이메일이 계정과 같은지",
         "네트워크 속도가 충분한지"],0,
        "**자격 증명의 만료**가 가장 흔합니다. 개인 액세스 토큰은 기한이 있고, SSH 키도 회수될 수 있습니다.\n원격 주소가 HTTPS 인지 SSH 인지에 따라 쓰는 자격 증명이 다르므로 `git remote -v` 로 함께 확인합니다.\n보호 규칙 위반이나 이메일 불일치는 인증이 아니라 **거부** 메시지로 나타나 구분됩니다."),
      I("덮어쓰기 조건","원격이 내가 마지막으로 본 상태일 때만 강제로 밀어 주는 옵션은? (영어)",
        ["--force-with-lease","force-with-lease"],
        "정답은 **--force-with-lease** 입니다.\n내가 마지막으로 가져온 원격 상태와 지금이 같을 때만 밀고, 그 사이 남이 올렸다면 거부합니다.\n`--force` 는 그 확인 없이 덮어써서 남의 커밋을 말없이 지웁니다. 습관을 이쪽으로 바꿔 두는 것만으로 사고 하나가 사라집니다.")
    ] },

  { t:"사라진 것 같은 것들", xp:40,
    th:{ sum:"Git 에서 **정말 사라지는 것은 드물다.** 커밋된 것은 대개 남아 있고, 가리키는 이름이 없어졌을 뿐이다.",
      body:[
        {h:"reflog 가 지나온 길을 적어 둔다", t:"`reset --hard` 로 날린 커밋도, rebase 전의 이력도 **reflog 에 남는다.** HEAD 와 각 브랜치가 어디를 가리켰는지가 시간순으로 적혀 있어, 해시를 찾아 브랜치를 붙이면 되살아난다. 기본 보관 기간은 넉넉하지만 무한하지 않으니 **알아채면 바로** 확인하는 것이 좋다. 반대로 **커밋하지 않은 변경**은 어디에도 남지 않는다 — 그것이 커밋을 자주 하는 실질적인 이유다."},
        {h:"머지했는데 내 변경이 없다", t:"충돌을 풀 때 실수로 **한쪽을 통째로 고른** 경우가 대부분이다. 병합 커밋은 정상으로 보이지만 내용이 빠져 있다. `git log -p` 나 `git diff <머지전>..<머지후>` 로 확인하고, 되돌리기보다 **빠진 변경만 다시 얹는** 편이 안전하다. 같은 충돌을 반복해 풀고 있다면 `rerere` 를 켜 두면 앞서 푼 해법을 기억해 자동으로 적용해 준다."}],
      code:{ c:"git reflog\n  a1b2c3 HEAD@{2}: commit: 결제 검증 추가\n  d4e5f6 HEAD@{1}: reset: moving to HEAD~3   ← 여기서 날렸다\n\ngit branch rescue a1b2c3", cap:"지나온 자리는 다 적혀 있다" },
      key:["커밋된 것은 reflog 에 남는다","커밋하지 않은 것은 남지 않는다","머지 실수는 한쪽을 통째로 고른 것","rerere 가 반복 충돌을 기억한다"] },
    q:[
      D("리셋으로 날렸다","<code>reset --hard</code> 로 커밋 세 개를 날렸습니다. 되살리는 방법으로 알맞은 것은?",
        ["reflog 에서 해시를 찾아 브랜치를 붙인다",
         "원격 저장소에서 다시 클론해 로컬을 통째로 덮어쓴다",
         "운영체제 휴지통에서 파일을 복구해 온다",
         "같은 내용을 다시 작성한다"],0,
        "**reflog** 에 HEAD 가 지나온 자리가 전부 적혀 있습니다. 날리기 직전의 해시를 찾아 브랜치를 붙이면 그대로 돌아옵니다.\n원격에 밀지 않았어도 로컬에 남아 있으므로 다시 클론할 필요가 없습니다.\n다만 **커밋하지 않았던 변경**은 어디에도 남지 않습니다 — 이것이 커밋을 자주 하는 실질적인 이유입니다."),
      D("머지 후 변경이 없다","머지가 끝났는데 내가 고친 내용이 보이지 않습니다. 가장 그럴듯한 원인은?",
        ["충돌을 풀면서 한쪽을 통째로 골랐다",
         "머지 커밋에는 원래 변경 내용이 담기지 않는다",
         "머지 후에는 이전 커밋이 자동으로 정리된다",
         "브랜치가 이미 다른 곳에 머지되어 있었다"],0,
        "**충돌 해결에서 한쪽을 통째로 고른 것**이 압도적으로 흔합니다. 병합 커밋 자체는 정상으로 보여 눈치채기 어렵습니다.\n되돌리기보다 **빠진 변경만 다시 얹는** 편이 안전합니다 — 되돌리면 그 뒤의 작업까지 얽힙니다.\n머지 후에도 원래 커밋은 그대로 남아 있으므로 거기서 내용을 가져올 수 있습니다."),
      D("스태시가 안 보인다","<code>stash</code> 해 둔 변경을 찾을 수 없습니다. 확인할 것으로 알맞은 것은?",
        ["다른 브랜치에서 이미 적용했는지",
         "스태시가 재부팅 뒤에도 남아 있는지",
         "스태시가 원격 저장소에도 함께 올라갔는지",
         "스태시 개수에 제한이 있는지"],0,
        "**스태시는 브랜치와 무관하게 쌓입니다.** 다른 브랜치에서 `pop` 했다면 목록에서 사라진 것입니다.\n`pop` 은 적용하고 지우는 것이라, 적용 후 문제가 생겨도 목록에는 없습니다. `apply` 를 쓰면 남습니다.\n스태시도 커밋으로 저장되므로 지워졌다면 reflog 로 찾을 수 있고, 재부팅과는 관계가 없습니다."),
      D("브랜치를 지웠다","머지하지 않은 브랜치를 실수로 지웠습니다. 대응으로 알맞은 것은?",
        ["reflog 로 마지막 커밋을 찾아 같은 이름으로 다시 만든다",
         "지운 브랜치는 되살릴 수 없으므로 처음부터 다시 작업한다",
         "원격에서 같은 이름의 브랜치를 내려받는다",
         "머지하지 않은 브랜치는 애초에 지워지지 않는다"],0,
        "브랜치는 **커밋을 가리키는 이름표**일 뿐이라 지워도 커밋은 남습니다. reflog 에서 해시를 찾아 이름을 다시 붙이면 됩니다.\n지울 때 Git 이 알려 준 해시를 그대로 쓸 수도 있습니다 — 그래서 그 메시지를 지우지 말고 읽어야 합니다.\n`-d` 는 머지 안 된 브랜치를 막지만 `-D` 는 막지 않습니다."),
      D("커밋했는데 파일이 비었다","커밋은 만들어졌는데 파일 내용이 들어가지 않았습니다. 원인으로 알맞은 것은?",
        ["변경을 색인에 올리지 않았다",
         "커밋 메시지가 비어 있었다",
         "파일이 무시 목록에 들어 있었다",
         "작업 디렉터리가 깨끗한 상태였다"],0,
        "Git 은 **색인에 올린 것만** 커밋합니다. `add` 하지 않으면 작업 디렉터리의 변경은 담기지 않습니다.\n`git commit -a` 는 이미 추적 중인 파일만 자동으로 담고, 새 파일은 여전히 `add` 가 필요합니다.\n무시 목록에 있다면 `add` 자체가 조용히 무시되므로, 그 경우도 함께 확인할 값어치가 있습니다."),
      D("같은 충돌이 반복된다","긴 rebase 중 같은 충돌을 여러 번 풀고 있습니다. 도움이 되는 것으로 알맞은 것은?",
        ["rerere 를 켜 앞서 푼 해법을 기억하게 한다",
         "충돌이 난 파일을 미리 백업해 두었다가 덮어쓴다",
         "rebase 대신 머지로 바꿔 충돌을 한 번에 푼다",
         "충돌이 나는 파일을 무시 목록에 넣는다"],0,
        "**rerere(reuse recorded resolution)** 는 앞서 푼 해법을 기억했다가 같은 충돌이 나오면 자동으로 적용합니다.\n긴 rebase 나 되풀이되는 병합에서 특히 값어치가 큽니다.\n머지로 바꾸면 충돌은 한 번에 풀리지만 이력의 모양이 달라지므로, 그것은 별개의 판단입니다."),
      D("저장소가 느리다","클론과 조회가 눈에 띄게 느려졌습니다. 먼저 볼 것으로 알맞은 것은?",
        ["이력에 큰 파일이 들어가 있는지",
         "만들어 둔 브랜치의 개수가 지나치게 많은지",
         "커밋 메시지가 너무 긴지",
         "태그가 너무 많은지"],0,
        "**이력에 들어간 대용량 파일**이 가장 흔한 원인입니다. 지금은 없어도 이력에 있으면 클론할 때 전부 받습니다.\n`git count-objects -vH` 로 크기를 보고, 큰 객체를 찾아 이력에서 걷어 냅니다.\n필요한 만큼만 받는 얕은 클론이나 부분 클론으로 임시로 완화할 수도 있습니다."),
      I("지나온 자리","HEAD 와 브랜치가 지나온 자리를 시간순으로 적어 두는 기록을 뭐라고 부르나? (영어)",
        ["reflog","git reflog"],
        "정답은 **reflog** 입니다. 로컬에만 있고 원격에는 올라가지 않습니다.\n`reset --hard`·rebase·브랜치 삭제로 잃은 것처럼 보이는 커밋을 되살리는 거의 모든 경우가 여기서 해결됩니다.\n보관 기간이 무한하지 않으니 **알아챘을 때 바로** 확인하는 것이 좋습니다.")
    ] }
]}

]);
