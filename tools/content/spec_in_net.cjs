module.exports = {
  track: "net", guide: "", xp: 60,
  unit: "단답으로 확인하기 — 규격에 적힌 숫자와 이름",
  source: "./in_net.cjs",
  lessons: [
    { t: "주소와 크기의 숫자", n: 8,
      th: { sum: "네트워크의 성능 문제는 대개 **몇 왕복인가**와 **한 번에 얼마나 실리는가**로 설명된다. 그 숫자를 외워 두면 원인이 바로 짚인다.",
        body: [
          { h: "여는 비용과 닫는 비용", t: "TCP 연결은 <b>3 개</b>의 패킷으로 열리므로 최소 <b>왕복 한 번</b>이 들고, TLS 1.3 이 한 번 더 들어 첫 요청까지 두 왕복이다 — 지연 200ms 회선이면 그것만으로 0.4 초다. 그래서 연결 재사용과 커넥션 풀이 큰 파일보다 <b>작은 요청</b>에서 더 큰 차이를 낸다. 닫는 것은 <b>4 단계</b>이고, 먼저 닫은 쪽에 <code>TIME_WAIT</code> 이 남아 포트가 잠깐 묶인다." },
          { h: "주소와 실리는 양", t: "IPv4 는 <b>32 비트</b>라 43 억 개뿐이고, <code>/24</code> 는 네트워크·브로드캐스트를 빼 <b>254 개</b>가 쓰인다. 이더넷의 기본 MTU 는 <b>1500 바이트</b>이고, 터널을 지나면 줄어 '큰 요청만 멈추는' 증상을 만든다. TCP 와 IPv4 헤더가 각각 <b>20 바이트</b>라 1 바이트를 보내도 41 바이트가 나간다 — 작은 패킷을 자주 보내면 회선의 절반이 헤더다." }],
        code: { c: "hosts = 2^(32-prefix) - 2     // /24 → 254\nMTU 1500 · TCP 20 + IPv4 20 = 40 바이트 오버헤드\nTCP 1왕복 + TLS 1왕복 = 첫 요청까지 2왕복", cap: "몇 왕복인지, 얼마나 실리는지" },
        key: ["연결 열기 3, 닫기 4", "/24 는 254 개", "MTU 1500 · 헤더 40", "포트 80 · 443 · 53"] } },
    { t: "이름이 붙은 규칙들", n: 7,
      th: { sum: "'슬로 스타트', 'NAT', '멀티플렉싱', 'TTL' — 이름마다 **무엇을 포기하고 무엇을 얻었는지**가 적혀 있다.",
        body: [
          { h: "보장과 그 대가", t: "TCP 는 순번과 재전송으로 <b>순서와 도착</b>을 보장하지만, 잃어버린 조각을 기다리느라 뒤가 멈춘다 — 실시간 음성·영상은 그 보장을 포기하는 편이 낫다. <b>슬로 스타트</b>는 '느리게' 가 아니라 '<b>작게 시작</b>' 이라 왕복마다 배로 커지며, 짧은 연결은 이 단계를 벗어나기도 전에 끝난다. HTTP/2 의 <b>멀티플렉싱</b>은 한 연결에 요청을 겹쳐 보내지만, TCP 위라 패킷 하나가 유실되면 전부 멈춘다." },
          { h: "숨기고 세는 장치", t: "<b>NAT</b> 는 사설 주소 여럿을 공인 주소 하나 뒤에 두어 자연스러운 방화벽이 되는 대신, 바깥에서 안으로 먼저 연결할 수 없게 만든다 — P2P 가 어려운 이유이고 STUN·TURN 이 필요한 이유다. <b>TTL</b> 은 이름과 달리 <b>거쳐 갈 라우터 수</b>이고, 이 값을 1 부터 올려 가며 보내는 것이 <code>traceroute</code> 다. 리다이렉트는 <b>301 이 영구</b>라 캐시되니 확신이 없으면 302 를 쓴다." }],
        code: { c: "TCP_NODELAY        // 모아 보내기를 끈다\ntraceroute = TTL 1, 2, 3 … 로 보내기\n307/308           // 메서드를 지키는 리다이렉트", cap: "이름에 절충이 적혀 있다" },
        key: ["TCP 는 보장, UDP 는 지연", "슬로 스타트는 작게 시작", "NAT 뒤로는 먼저 못 들어온다", "301 은 캐시되어 되돌리기 어렵다"] } },
  ],
};
