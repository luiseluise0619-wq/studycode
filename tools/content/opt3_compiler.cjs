/* compiler — 정답률 15% 아래 끝맺음의 오답 19개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "compiler", fixes: [
  { k: "구분", from: "둘 다 아니며 어느 쪽도 쓰지 않는다", to: "둘 다 아니며 가상 머신에서 바이트코드로 돈다" },
  { k: "스택이 싼 이유", from: "빠른 메모리 장치에 놓여 있기 때문이다", to: "스택이 힙보다 빠른 메모리 장치에 놓여 있어서다" },
  { k: "힙으로 올라가는 값", from: "힙에만 둘 수 있기 때문이다", to: "클로저 안에서 쓰이는 변수는 문법적으로 힙에만 둘 수 있어서다" },
  { k: "힙으로 올라가는 값", from: "스택에서는 지원되지 않기 때문이다", to: "증가시키는 연산이 스택에서는 지원되지 않아서다" },
  { k: "지워 버리기", from: "실행 중 오류가 난다", to: "타입이 다른 목록끼리 대입되어 실행 중에 뒤섞인다" },
  { k: "헤더 의존성", from: "피할 방법이 없다", to: "헤더는 원래 매번 전체 재빌드를 유발하므로 감수해야 한다" },
  { k: "디버그 심볼", from: "애초에 만들 필요가 없다", to: "릴리스 빌드에서는 최적화 때문에 심볼이 의미가 없으므로 애초에 만들지 않는다" },
  { k: "왜 계속 읽나", from: "원인을 알 수 있기 때문이다", to: "뒤쪽 코드를 읽어야 앞쪽 오류의 원인이 드러나서다" },
  { k: "위치를 들고 다니기", from: "자리를 되찾을 수 있기 때문이다", to: "오류가 났을 때 소스를 처음부터 다시 읽어 같은 내용을 찾으면 된다. 토큰이 위치를 들고 다니지 않아도 문자열만 맞춰 보면 자리를 되찾을 수 있어서다" },
  { k: "위치를 들고 다니기", from: "뒤 단계에서는 필요 없다", to: "줄 번호는 어휘 분석기가 세고 뒤 단계에서는 버린다" },
  { k: "펼친 뒤에 난 오류", from: "위치 정보가 아예 없기 때문이다", to: "매크로는 어휘 분석 전에 처리되어 위치 정보가 아예 없어서다" },
  { k: "타이핑하는 동안 보여 주려면", from: "파서는 쓰지 않는다", to: "편집기는 정규식으로 검사하고 파서는 거치지 않는다" },
  { k: "고친 곳만 다시 하기", from: "아무 준비도 필요 없다", to: "컴파일러가 자동으로 판단하므로 준비할 것은 없다" },
  { k: "무시당하는 경고의 운명", from: "상태를 논할 필요가 없다", to: "경고는 빌드 결과에 영향을 주지 않으므로 상태와는 무관하다" },
  { k: "줄 번호가 하나뿐인 이유", from: "설정되어 있기 때문이다", to: "오류 보고 도구가 첫 줄만 기록하도록 설정되어 있어서다" },
  { k: "이름이 짧아진다", from: "아무 영향을 주지 않기 때문이다", to: "이름은 주석과 같아 실행에 아무 영향을 주지 않아서다" },
  { k: "몇 개로 줄었나", from: "명령을 줄이지 않기 때문이다", to: "7개 — 상수 전파는 명령을 줄이지 않아서다" },
  { k: "한 바퀴로는 부족하다", from: "되살려야 하는 경우가 있기 때문이다", to: "지운 명령을 되살려야 하는 경우가 있어서다" },
  { k: "공통 부분식", from: "두 번 나올 수 없기 때문이다", to: "SSA 에서는 같은 연산이 두 번 나올 수 없어서다" }
] };
