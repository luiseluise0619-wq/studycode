/* 나머지 24트랙 — 정답이 5자 이상 단독 최장인 문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = [
{ track: "algo", fixes: [
  { k: "algo2-sort-quick-worst", o: [
    "어떤 입력을 넣더라도 O(n²) 로 동작하기 때문이며, 이것을 미리 피해 갈 방법은 없으므로 병합 정렬로 바꿔야 한다",
    "메모리가 부족해 재귀 호출 스택이 밀릴 때만 나타나는 현상이므로, 스택 크기를 늘리거나 반복문으로 바꾸면 사라진다",
    "재귀를 쓰지 않고 반복문으로 구현했을 때만 발생하므로, 교과서대로 재귀 형태를 유지하면 최악의 경우를 걱정할 필요가 없다",
    "피벗 선택이 나쁘면(이미 정렬된 입력+끝 피벗) 발생하며, 랜덤/중앙값 피벗이나 introsort로 회피" ] },
  { k: "algo-g-dag-shortest", o: [
    "DAG 는 사이클이 없으므로 구조상 트리 모양이 되어 각 정점까지의 경로가 하나뿐이라 비교할 것이 없기 때문",
    "DAG 에는 애초에 음수 간선이 존재할 수 없도록 정의되어 있어 다익스트라를 그대로 써도 되기 때문",
    "위상 순서대로 정점을 처리하면 각 정점의 거리가 확정된 뒤에만 그 정점의 나가는 간선을 완화하기 때문",
    "다익스트라가 DAG 에서는 음수 간선을 허용하도록 동작이 바뀌어 우선순위 큐 순서가 그대로 맞기 때문" ] },
  { k: "algo-gr-comparator", o: [
    "비교 함수가 어떤 두 원소에 대해서도 참을 반환하도록 만들어져 있어야만 정렬이 멈추지 않는다",
    "비교 대상 원소가 모두 서로 다른 값이어야 하며 같은 값이 섞이면 순서가 정의되지 않는다",
    "비교 함수가 추이성을 만족하는 전순서(strict weak ordering)를 이루어야 한다",
    "정렬할 원소의 개수가 짝수여야 하며 홀수면 마지막 원소를 맞바꿀 짝이 없어 결과가 틀린다" ] },
  { k: "algo-g-dijkstra-why", o: [
    "음수 간선이 하나라도 섞여 들어가게 되면 그래프 전체가 연결된 상태를 유지하지 못하게 되어 버리기 때문에 도달할 수 없는 정점이 생기기 때문",
    "우선순위 큐가 음수 값을 키로 저장할 수 없어 음수 간선을 거친 거리가 큐에서 빠지고, 그 정점은 영영 꺼내지지 않기 때문",
    "거리 배열을 무한대 값으로 초기화할 수 없어 음수 간선을 거치면 무한대와 음수의 덧셈이 정의되지 않은 값이 되기 때문",
    "한 번 최단 거리로 확정한 정점은 다시 갱신하지 않는다는 탐욕적 가정이, 나중에 나타난 음수 간선 때문에 깨지기 때문" ] },
  { k: "algo-bs-value-space", o: [
    "맞다. 값 공간에서 답을 찾아야 할 때는 정렬된 배열이 없으므로 선형 탐색으로 훑어 나가야만 한다",
    "틀리다. 이진 탐색은 정렬 여부와 완전히 무관하게 쓸 수 있으며 정렬은 속도를 위한 선택일 뿐이다",
    "틀리다. 판정 함수가 단조라면 배열이 아닌 정답의 값 공간에서도 이분 탐색을 할 수 있다",
    "맞다. 배열이 정렬되어 있다는 것이 유일한 전제이며, 그 밖의 상황에서는 다른 탐색을 써야 한다" ] },
  { k: "캐시레이아웃", o: [
    "필요한 필드가 메모리에 연속 배치되어 캐시 라인 활용률이 높고 미사용 필드로 인한 미스가 줄므로",
    "SoA 로 바꾸면 알고리즘의 복잡도가 O(log n) 수준으로 낮아져 순회 횟수 자체가 줄어들기 때문에",
    "SoA 가 자동으로 SIMD 벡터화를 강제해 주므로 컴파일러 설정과 무관하게 한 번에 여러 원소가 처리되기 때문",
    "SoA 는 분기 예측 실패를 없애 주므로 조건문이 섞인 순회에서도 파이프라인이 멈추지 않기 때문" ] },
  { k: "algo2-bigo-small-n", o: [
    "O(n log n) 을 보장하는 자료구조로 갈아 끼워 상한을 낮추고 이론적 안전판을 둔다",
    "루프를 재귀로 바꿔 컴파일러가 꼬리 호출 최적화를 적용하기 좋게 만든다",
    "단순한 O(n²) 선형 스캔이 상수가 작아 오히려 빠를 수 있으니 그대로 둔다",
    "조회가 잦으니 해시맵으로 바꿔 두어 어떤 크기에서도 O(1) 을 보장한다" ] },
  { k: "algo2-hash-o1-condition", o: [
    "좋은 해시 함수로 충돌이 적고 부하율(load factor)이 낮게 유지되어야 한다",
    "저장하는 키가 정수 타입이어야 하고 그중에 음수가 섞이지 않아야 한다는 점",
    "삽입한 순서대로 항목이 정렬되어 있어야 이진 탐색으로 버킷을 찾을 수 있다는 점",
    "값으로 저장하는 것이 문자열이어야 해시 계산이 한 번에 끝난다는 점" ] },
  { k: "algo-dp-tab-order", o: [
    "메모리 접근이 연속되도록 인덱스 순서만 맞춰 주면 충분하고 나머지 의존 관계는 캐시가 알아서 처리하므로 상관하지 않아도 된다",
    "인덱스가 커지는 오름차순이기만 하면 올바르게 계산되며, 내림차순이 필요한 경우는 정의상 존재하지 않는다",
    "어떤 상태를 계산할 때 그것이 의존하는 상태들이 이미 계산되어 있도록, 의존 관계의 위상 순서를 따라야 한다",
    "순회 순서는 최종 결과에 아무 영향을 주지 않으며, 표의 모든 칸이 결국 채워지기만 하면 값은 같아진다" ] },
  { k: "algo-gr-rooms", o: [
    "서로 겹치지 않게 골라낸 최대 회의 개수",
    "어느 한 시점에서의 최대 동시 겹침 회의 수",
    "가장 오래 진행되는 회의 하나의 길이",
    "하루에 잡혀 있는 전체 회의의 총 개수" ] },
  { k: "algo-gr-always-optimal", o: [
    "틀리다. 탐욕은 어떤 문제에서도 최적해를 주지 못하며 근사해에만 그치고 마는 방법일 뿐이다",
    "맞다. 매 단계에서 최선을 고르므로 전체 결과도 최선이 되며, 그것이 탐욕이라는 이름의 뜻이다",
    "맞다. 정렬 기준만 올바르게 잡으면 언제나 최적이며, 틀린다면 기준을 잘못 잡은 것이다",
    "틀리다. 탐욕 선택 속성과 최적 부분 구조가 성립할 때만 최적이며 그렇지 않으면 반례가 존재한다" ] }
] },
{ track: "security", fixes: [
  { k: "sec2-logout", o: [
    "로그아웃을 누르면 비밀번호를 재설정하게 만들어 이전 자격증명과 세션을 한꺼번에 무효로 돌린다 — 그래야 탈취된 식별자도 함께 죽는다",
    "클라이언트에서 쿠키를 지우면 브라우저가 서버에 종료 신호를 보내 서버 세션도 함께 무효화되므로 별도 처리가 필요 없다",
    "서버 측 세션 저장소에서 해당 세션을 삭제·무효화해야 하며, 쿠키 삭제만으로는 탈취된 식별자가 계속 유효하다",
    "세션에 만료 시간이 설정돼 있으므로 별도의 로그아웃 처리는 하지 않아도 되고, 만료 전까지의 위험은 감수할 만한 수준이다" ] },
  { k: "sec5-obscurity", o: [
    "알고리즘과 내부 구조를 숨기는 것이 가장 강력한 보안 통제이며, 설계가 공개되는 순간 나머지 통제가 모두 무력해지므로 공개는 피해야 한다",
    "오픈소스를 쓰면 내부 구조가 그대로 드러나므로 어떤 시스템이든 결국 취약해질 수밖에 없고, 그래서 보안이 중요한 곳은 비공개 소프트웨어만 써야 한다",
    "외부에 노출되지 않는 내부 시스템이라면 구조를 숨기는 것만으로 충분한 보안이 되므로, 내부망 서비스에는 인증이나 암호화를 따로 두지 않아도 된다",
    "숨기는 것 자체는 통제가 아니며, 시스템은 설계가 공개되어도 키와 인증만으로 안전해야 한다. 다만 불필요한 정보 노출을 줄이는 것은 유용한 보조 조치다" ] },
  { k: "sec3-bypass-headers", o: [
    "경로 대소문자, 인코딩된 문자, 뒤 슬래시, X-Original-URL 같은 헤더 처리 차이로 규칙이 우회될 수 있으므로 애플리케이션 내부에서도 인가를 검증해야 한다",
    "프록시는 애플리케이션과 완전히 똑같은 방식으로 경로를 해석해 주므로 두 곳의 규칙이 어긋날 일이 없고, 애플리케이션 안에서 인가를 한 번 더 검증할 필요도 없다",
    "게이트웨이에 정의한 규칙이 애플리케이션 설정으로 자동 동기화되므로 두 곳을 따로 볼 필요가 없고, 인가 규칙은 게이트웨이 한 곳에서만 관리하면 그것으로 충분하다",
    "경로 매칭이 요청마다 한 번씩 더 일어나 지연이 늘어나는 성능 저하만 발생할 뿐이며, 보안상으로는 애플리케이션 안에서 검사하는 것과 완전히 똑같은 효과를 낸다" ] },
  { k: "sec3-token-role-staleness", o: [
    "클레임은 클라이언트가 마음대로 조작할 수 있는 값이므로 서버는 사용하지 말아야 하고, 권한은 서버 쪽 세션 저장소에서만 읽어 판단하는 것이 옳다",
    "클레임은 발급 시점의 스냅샷이므로, 권한이 회수돼도 토큰 만료 전까지는 옛 권한이 유지될 수 있어 민감 작업은 서버에서 현재 권한을 재확인하는 편이 안전하다",
    "역할 정보는 클라이언트가 계산해 요청 헤더에 담아 보내야 서버 부하가 줄어들고, 서버는 그 헤더를 믿고 처리하면 되므로 토큰 안의 클레임은 참고용으로만 둔다",
    "서명이 유효하다면 그 안의 클레임 값은 만료 시점과 무관하게 영구히 신뢰할 수 있으므로, 서버는 서명 검증 뒤 role 값을 그대로 써서 관리자 여부를 판단하면 된다" ] },
  { k: "sec5-upload", o: [
    "업로드 시점에 파일 확장자 허용목록만 검사하면 실행 가능한 파일이 들어올 수 없으므로, 저장 위치를 웹 루트 바깥으로 옮기거나 다운로드 응답 헤더를 서버가 지정하는 일은 따로 신경 쓰지 않아도 된다",
    "파일 크기 상한만 넉넉히 정해 두면 대량 업로드로 인한 문제도 없고 실행 위험도 함께 사라지므로, 유형 검사나 저장 위치 분리 같은 추가 조치는 사용자 경험만 해칠 뿐 보안에는 아무 도움이 되지 않는다",
    "허용 유형을 제한하되, 저장 위치를 웹 루트 밖으로 두어 실행 가능성을 없애고, 서버가 정한 파일명으로 저장하며, 다운로드 시 Content-Type과 Content-Disposition을 서버가 지정한다",
    "업로드된 파일이 보내온 Content-Type 헤더를 그대로 믿고 그 값에 맞춰 저장하면 브라우저가 올바른 형식으로 해석하므로, 서버가 굳이 파일 내용을 따로 검사하거나 이름을 바꿔 저장할 필요가 없다" ] },
  { k: "sec2-mfa-phishing", o: [
    "휴대폰 번호로 전달되는 SMS 일회용 코드를 입력하게 해 기기 소지 요소를 확인하는 인증",
    "인증 앱에서 30초마다 새로 생성되는 6자리 TOTP 코드를 입력하게 해 본인을 확인하는 인증",
    "등록된 이메일 주소로 전달되는 일회성 인증 링크를 눌러 본인임을 확인하는 방식의 이차 인증",
    "오리진에 바인딩되어 가짜 도메인에서는 서명이 생성되지 않는 WebAuthn/FIDO2 기반 인증" ] },
  { k: "논스·IV 관리", o: [
    "치명적이다. 두 평문의 XOR이 드러나 기밀성이 붕괴되고, 인증 서브키가 노출되어 태그 위조가 가능해진다",
    "복호화가 불가능해져 서비스는 멈추지만 평문이나 키에서 얻을 정보는 없어 가용성 문제로만 끝난다",
    "nonce 는 애초에 암호문과 함께 공개되는 값이므로 같은 키에서 재사용해도 보안상 아무 문제가 없다",
    "연산 캐시가 어긋나 암호화 성능이 조금 떨어질 뿐 기밀성이나 무결성에는 아무 영향이 없다" ] },
  { k: "sec6-secret-store", o: [
    "소스 코드에 하드코딩하는 방식이 배포 절차가 단순하고 외부 시스템에 기대지 않아 가장 안전하다",
    "환경변수에 두는 방식과 코드에 하드코딩하는 방식은 결국 프로세스 메모리에 있으므로 위험도가 사실상 같다",
    "시크릿 관리 시스템에서 런타임에 주입하는 방식이 가장 안전하고, 코드에 하드코딩하는 방식이 가장 위험하다",
    "설정 파일에 base64 로 넣어 두면 암호화된 것으로 볼 수 있어 저장소에 커밋해도 충분히 안전하다" ] },
  { k: "sec4-aead-def", o: [
    "압축과 암호화를 한 번의 연산으로 함께 수행해 주고받는 전송량을 줄이면서 기밀성까지 확보해 준다",
    "기밀성과 무결성·인증을 함께 제공하고, 암호화하지 않지만 함께 인증하고 싶은 값을 AAD로 묶을 수 있다",
    "기밀성만 제공하므로 무결성을 보장하려면 별도의 MAC 을 붙여야 하고, 그 순서를 잘못 잡으면 취약해진다",
    "키 교환과 전자 서명을 함께 제공해 세션 수립까지 처리해 주므로 TLS 핸드셰이크를 대신할 수 있다" ] },
  { k: "sec6-scanning", o: [
    "개발자 전원을 대상으로 한 보안 교육을 정기적으로 열고, 사람이 저지르는 실수를 줄이는 데 집중해 유출 자체가 일어나지 않게 한다",
    "코드 리뷰 단계에서 사람이 육안으로 시크릿이 섞였는지 확인하고, 발견하면 커밋을 되돌리게 해 저장소에 남지 않도록 한다",
    "사전 커밋 훅과 CI의 시크릿 스캐닝, 저장소 푸시 보호를 함께 적용하고 탐지 시 자동 회전 절차를 연결한다",
    "분기마다 저장소 전체를 수동으로 감사해 노출된 값을 찾아 회수하고, 감사 결과를 문서로 남겨 다음 감사의 기준으로 삼는다" ] }
] },
{ track: "sysd", fixes: [
  { k: "다중 AZ/리전", o: [
    "다중 리전은 비용만 늘 뿐 가용성에는 도움이 되지 않는다 — 어차피 쓰기를 받는 쪽은 한 리전뿐이고 나머지는 대기 상태로 놀고 있으므로, 장애가 나도 곧바로 넘어가지 못해 실익이 없다. 다중 AZ 도 같은 이유로 AZ 하나만 쓰는 것과 차이가 없다",
    "다중 AZ는 같은 지역 내 물리적으로 분리된 영역에 분산해 지연 손실이 거의 없이 가용성을 올리는 기본 구성이고, 다중 리전은 재해 복구와 지역 지연 개선까지 얻지만 데이터 복제 지연과 정합성, 비용 문제가 크게 늘어난다",
    "다중 AZ 구성만으로 리전 전체 장애를 견딜 수 있다 — AZ 는 서로 다른 데이터센터이므로 리전에 문제가 생겨도 AZ 중 하나는 남아 있고, 그래서 다중 리전은 규제 요구가 있는 금융권 같은 특수한 경우가 아니면 고려할 필요가 없다",
    "다중 AZ와 다중 리전은 같은 말이다 — 제공자마다 부르는 이름이 다를 뿐 둘 다 물리적으로 떨어진 데이터센터에 복제하는 구성이고, 복제 지연이나 정합성 문제도 같은 크기로 생기므로 설계 면접에서 둘을 구분해 답할 이유가 없다" ] },
  { k: "관계형 유지", o: [
    "둘을 함께 쓰면 운영이 복잡해지므로 하나로 통일해야 한다 — 저장소가 둘이면 백업·모니터링·장애 대응 절차가 두 벌이 되고 팀의 학습 비용도 두 배가 되므로, 어느 쪽이든 하나를 골라 모든 도메인을 그 위에 올리는 편이 낫다",
    "관계형 DB 는 확장이 불가능하므로 언젠가는 바꿔야 한다 — 단일 노드 한계가 명확하고 샤딩도 직접 구현해야 하므로, 처음부터 수평 확장이 되는 저장소로 시작하는 편이 나중에 옮기는 비용을 아끼는 길이고, 그래서 신규 서비스는 NoSQL 이 기본이다",
    "대부분의 서비스 규모에서는 관계형 DB 한 대로 충분하고, 트랜잭션·조인·임의 질의·성숙한 운영 도구라는 이점이 크다. 정말 필요한 지점(쓰기 폭증, 단일 노드 용량 초과)이 확인되면 그 도메인만 옮기겠다",
    "맞다. 신규 서비스는 NoSQL 로 시작하는 것이 정석이다 — 스키마를 미리 정하지 않아도 되어 초기 개발이 빠르고, 트래픽이 늘어도 노드를 더하면 되므로 관계형 DB 를 고르는 것은 나중에 마이그레이션 비용을 떠안는 선택이다" ] },
  { k: "선택 기준", q: "NoSQL을 선택", o: [
    "접근 패턴이 키 기반 단건 조회로 고정되어 있고 쓰기량이 단일 마스터 한계를 넘을 것으로 추정돼, 수평 확장이 쉬운 대신 임의 질의와 교차 엔티티 트랜잭션을 포기하는 거래가 타당하다고 판단했다",
    "스키마를 정의하지 않아도 되어 개발이 빠르기 때문이다 — 필드가 자주 바뀌는 초기 단계에서는 마이그레이션 없이 문서 구조를 바꿀 수 있고, 그래서 개발 속도가 관계형보다 몇 배 빠르며 나중에 스키마가 굳어지면 그때 정리하면 된다",
    "관계형보다 빠르고 요즘 표준이기 때문이다 — 조인이 없어 조회가 단순하고 메모리 기반 캐시처럼 동작하므로 응답이 빠르며, 대부분의 대형 서비스가 쓰고 있어 채용과 운영 사례를 구하기도 쉽다는 점에서 기본 선택으로 잡았다",
    "조인이 필요 없는 시스템에는 관계형 DB 를 쓸 수 없기 때문이다 — 테이블 구조 자체가 조인을 전제로 만들어져 있어 단건 조회만 하는 서비스에서는 오히려 오버헤드가 되고, 그래서 후보에서 제외하고 문서 저장소를 골랐다" ] },
  { k: "개략 설계 범위", o: [
    "클라이언트부터 저장소까지의 요청 흐름, 주요 컴포넌트(LB, API 서버, 캐시, DB, 큐, 오브젝트 스토리지)와 그 사이 데이터 이동, 그리고 핵심 API 2~3개와 데이터 모델",
    "각 서비스의 클래스 다이어그램과 함수 시그니처 — 어떤 객체가 어떤 메서드를 갖는지까지 그려야 면접관이 구현 능력을 확인할 수 있고, 데이터 흐름은 그 뒤 심화 단계에서 자연스럽게 나온다",
    "장애 대응 절차와 온콜 로테이션 — 시스템은 결국 장애가 나므로 누가 언제 대응하는지를 먼저 정해 두어야 하고, 컴포넌트 배치는 운영 절차가 정해진 뒤에 그에 맞춰 그리는 편이 순서상 맞다",
    "선택한 클라우드 서비스의 정확한 제품명과 인스턴스 타입을 적는다 — 어떤 인스턴스를 몇 대 쓸지까지 적어 두면 비용 계산도 함께 되므로 구체적일수록 좋고, 추상적인 상자 그림은 실무 경험이 없어 보인다" ] },
  { k: "성능 저하 모드", o: [
    "DB가 즉시 포화될 수 있으므로, 캐시 장애를 감지하면 회로를 차단하고 요청 수를 제한하며 비핵심 기능을 끄는 성능 저하 모드로 전환해 핵심 기능만 유지한다",
    "모든 요청이 DB 로 가지만 DB 가 감당할 것이므로 문제 없다 — 캐시는 어디까지나 속도용 장치이고 DB 는 애초에 전체 부하를 처리하도록 용량을 잡아 두었으므로, 응답이 조금 느려질 뿐 준비할 것이 없다",
    "서비스를 전면 중단하고 캐시를 복구한다 — 캐시 없이 돌리면 DB 가 무너져 데이터까지 위험해지므로 점검 페이지를 띄워 요청을 막고, 캐시 클러스터를 다시 세운 뒤 데이터를 예열해 정상으로 돌아오면 다시 연다",
    "캐시는 부가 기능이므로 아무 영향이 없다 — 캐시 미스는 원래 DB 조회로 처리되는 정상 경로이고, 캐시가 통째로 죽는 것은 모든 요청이 미스가 되는 것과 같으므로 설계상 이미 감당되는 상황이라 따로 대응하지 않는다" ] },
  { k: "순서와 중복", o: [
    "대화방 단위로 서버가 단조 증가하는 시퀀스나 정렬 가능한 ID를 부여해 순서를 정하고, 클라이언트가 만든 메시지 ID로 재전송 중복을 제거한다",
    "TCP 를 쓰면 순서와 중복이 모두 해결된다 — 전송 계층이 패킷 순서를 보장하고 재전송도 스스로 처리하므로, 애플리케이션이 시퀀스나 메시지 ID 를 따로 둘 필요가 없고 서버 여러 대여도 연결마다 순서가 지켜진다",
    "메시지는 도착 순서대로 보여 주면 되므로 별도 처리가 필요 없다 — 네트워크가 도착 순서를 지켜 주고 재전송은 드물기 때문에, 서버가 받은 순서대로 저장하고 그대로 내려 주면 사용자에게는 자연스러운 대화 순서로 보인다",
    "클라이언트 기기의 타임스탬프로 정렬하면 충분하다 — 보낸 시각이 곧 순서이고 기기 시계는 네트워크로 동기화되므로 오차가 무시할 만하며, 중복은 같은 타임스탬프의 메시지를 하나로 합치는 것으로 걸러낼 수 있다" ] },
  { k: "피크 QPS", o: [
    "피크는 예측 불가능하므로 무한 확장만 가정한다 — 어떤 값을 잡아도 틀릴 수 있으니 오토스케일링에 맡기고 용량 산정은 생략하는 편이 정직하다",
    "평균과 피크는 같다고 본다 — 여유를 두면 비용만 늘어나므로 2,300 QPS 로 용량을 잡고, 넘치는 순간이 오면 그때 인스턴스를 더하면 된다",
    "트래픽이 하루 중 특정 시간대에 몰리므로 평균의 2~3배인 약 5,000~7,000 QPS를 피크로 잡고 그 값으로 용량을 산정한다",
    "평균의 100배인 230,000 QPS 로 잡는다 — 어떤 이벤트가 와도 견디도록 넉넉히 잡아 두는 것이 안전하고, 남는 용량은 다른 서비스가 나눠 쓰면 된다" ] }
] },
{ track: "mobile", fixes: [
  { k: "무엇부터 할까", o: [
    "아무것도 하지 않는다 — 필요해질 때 한꺼번에 하는 것이 효율적이다. 지금 손대면 쓰지도 않을 언어를 위해 코드가 복잡해지고, 나중에 요구가 오면 그때 화면을 한 번에 훑어 고치는 편이 낫다",
    "번역 파일로 빼는 것만 하고 나머지는 나중에 한다 — 문구만 코드 밖으로 나가 있으면 번역은 언제든 붙일 수 있고, 배치나 형식 문제는 실제 언어가 정해진 뒤에야 무엇이 깨지는지 알 수 있으므로 미리 할 수 없다",
    "실제 언어를 하나 붙여 보는 것만 하면 된다 — 영어든 일본어든 한 언어를 실제로 지원해 보면 문구·배치·형식 문제가 한꺼번에 드러나므로, 습관을 따로 들이는 것보다 그 경험이 훨씬 확실한 준비다",
    "문구를 빼고, 배치를 견디게 하고, 형식을 지역 기능에 맡기는 것이다 — 나중에 하면 손댈 곳이 앱 전체로 퍼지지만 지금은 습관으로 해결된다" ] },
  { k: "암호화는 어디까지", o: [
    "인증 토큰 — 유출되면 남이 그 사용자로 행세할 수 있다. 일반 파일이나 평문 설정에 두면 안 된다",
    "앱 설정 — 사용자 취향이 드러난다. 설정 값에서 무엇을 좋아하는지 읽히므로 토큰과 같은 수준으로 보호해야 한다",
    "이미지 캐시 — 용량이 크므로 보호가 필요하다. 큰 파일일수록 유출 피해가 크니 안전한 저장소에 두어야 한다",
    "전부 안전한 저장소에 두어야 한다 — 어떤 데이터든 남에게 보이면 안 되므로 구분하지 말고 모두 키체인에 넣는다" ] },
  { k: "시계가 어긋나면", o: [
    "충돌로 표시되어 사용자에게 묻는다 — 타임스탬프가 어긋나면 LWW 는 어느 쪽도 고르지 않고 결정을 미룬다",
    "A 가 이긴다 — 서버가 실제 순서를 알고 있으므로 기기가 적은 시각과 무관하게 나중에 도착한 편집을 남긴다",
    "시계 오차는 동기화로 자동 보정되므로 A 가 이긴다 — 3초 정도의 차이는 기기가 스스로 맞춰 900 이 1008 로 고쳐진다",
    "B 가 이긴다 — 실제로 나중에 쓴 것이 진다. 시계를 믿는 규칙의 근본적인 약점이다" ] },
  { k: "무엇을 어디에 둘까", o: [
    "이미지 캐시 — 가장 크므로 먼저 지워지고, 다시 받으려면 데이터 요금과 시간이 들어 사용자가 곧바로 불편을 느낀다",
    "검색 인덱스 — 다시 만드는 데 시간이 걸리므로 지워지면 앱을 켤 때마다 검색이 느려져 캐시에 두면 안 된다",
    "전부 캐시에 두어도 된다 — OS 가 알아서 판단하므로 중요한 것은 나중에 지우고, 지워진 것은 앱이 다시 만들면 된다",
    "오프라인 초안 — 다시 만들 방법이 없으므로 지워지면 사용자가 쓴 내용이 영영 사라진다" ] },
  { k: "지운 것이 되살아난다", o: [
    "충돌로 표시되어 사용자에게 묻는다 — 한쪽에만 있는 항목은 합칠 수 없어 결정을 사용자에게 넘긴다",
    "계란이 사라진다 — 한쪽에 없으면 지운 것으로 보고 합집합이 아니라 교집합으로 합쳐지기 때문이다",
    "계란이 되살아난다 — '없다' 와 '지웠다' 를 구분할 수 없기 때문이다",
    "합칠 수 없어 오류가 난다 — 두 집합의 원소 수가 다르면 합치기 전에 검사에서 걸려 실패한다" ] },
  { k: "mob-image-memory", o: [
    "다크 모드를 함께 지원해 색상 자원이 두 벌 올라가는 것 — 테마마다 비트맵이 따로 생겨 메모리가 두 배가 된다",
    "작은 JSON 응답을 반복해서 파싱하는 것 — 파싱 결과 객체가 쌓여 가비지 컬렉터가 따라잡지 못한다",
    "화면 크기보다 훨씬 큰 원본 이미지를 그대로 디코딩해 메모리에 올리는 것",
    "디버그 로그를 많이 남기는 것 — 로그 문자열이 메모리에 누적되어 앱이 오래 켜져 있을수록 커진다" ] },
  { k: "mob-privacy-minimal", o: [
    "나중에 분석에 쓸 수 있으므로 광고 식별자는 제약 없이 공유해도 된다 — 식별자는 개인정보가 아니기 때문이다",
    "나중에 쓸지 모르니 최대한 많이 수집해 둔다 — 다시 수집하려면 사용자 동의를 또 받아야 하므로 처음에 넉넉히 받는다",
    "개인정보 처리방침은 심사에서 요구할 때만 작성한다 — 실제 동작과 달라질 수 있으니 미리 쓰면 오히려 위험하다",
    "목적에 필요한 최소한만 수집하고, 목적과 보관 기간을 명시하며 저장·전송 시 보호한다" ] },
  { k: "mob-request-count", o: [
    "같은 요청을 병렬로 60개 보내면 전체 대기 시간이 그만큼 짧아진다 — 왕복이 겹쳐 총 시간은 요청 하나 수준이 된다",
    "이미지 해상도를 두 배로 올려 화질을 높인다 — 요청 수는 그대로지만 한 번에 받는 데이터가 늘어 체감 품질이 좋아진다",
    "요청을 하나로 묶거나 필요한 필드만 담은 집계 API를 만들어 왕복 횟수를 줄인다",
    "네트워크 타임아웃을 넉넉히 늘린다 — 30번의 요청이 실패 없이 끝나기만 하면 화면은 결국 그려지므로 실패율이 문제다" ] }
] },
{ track: "math", fixes: [
  { k: "정리하면", q: "조건수가 되풀이", o: [
    "조건수가 계산 속도를 정하기 때문이다 — 조건수가 클수록 반복 횟수가 늘어 알고리즘이 느려지므로 성능 지표로 계속 등장한다",
    "우연히 여러 주제에 이름이 겹쳤을 뿐이다 — 행렬식의 조건수와 최소제곱의 조건수는 정의가 달라 같은 개념으로 묶을 수 없다",
    "조건수라는 값이 행렬의 크기와 저장하는 데 드는 메모리 양을 한 번에 알려 주기 때문에 자료 구조를 고를 때 기준이 된다",
    "조건수가 이 문제가 얼마나 어려운지를 재는 하나의 숫자이기 때문이다 — 알고리즘을 고르는 기준이 된다" ] },
  { k: "음수가 나온 분산", o: [
    "값들이 거의 같아서 분산이 0 에 가까우면 부호는 아무런 의미가 없다 — 절댓값을 취해 그대로 쓰면 되고, 계산 자체가 무너진 것은 아니므로 공식을 바꿀 이유도 없다",
    "값이 너무 커서 제곱하는 순간 표현 범위를 넘은 것이다 — 오버플로가 나면 부호 비트가 뒤집혀 음수가 되므로, 값을 미리 작은 단위로 나눠 두면 같은 공식으로도 맞는 답이 나온다",
    "n-1 로 나누는 표본 분산 공식을 잘못 적용한 것이다 — 모집단 공식과 표본 공식을 섞어 쓰면 빼는 항이 더하는 항보다 커져 음수가 나올 수 있으므로 분모를 다시 확인해야 한다",
    "제곱합과 합제곱이 둘 다 아주 커서 그 차이를 구할 때 자릿수가 통째로 지워진 것이다 — 분산은 음수가 될 수 없으므로 계산이 무너졌다는 확실한 신호다" ] },
  { k: "ab-test-small-sample", o: [
    "표본이 작아 우연일 수 있으므로 판단을 보류하고 표본을 늘린다",
    "A 가 안정적으로 낮으므로 B 를 폐기하고 A 를 유지하는 쪽이 안전하다",
    "전환율 차이가 2%p 이므로 전면 적용하면 매출도 그만큼 2% 늘어난다",
    "B 가 확실히 우수하므로 실험을 끝내고 즉시 전면 적용해 이득을 앞당긴다" ] },
  { k: "묻히는 값", o: [
    "덧셈 결과를 정수로 반올림하는 규칙 때문이다 — 실수끼리 더해도 결과가 정수 자리에서 끊기므로 소수 부분이 사라지고, 큰 값을 다룰수록 이 절삭이 자주 일어난다",
    "정수 표현의 최댓값을 넘어 값이 잘린 것이다 — 1e16 은 64비트 정수 범위 근처라 더하는 순간 넘쳐서 원래 값으로 되돌아오고, 그래서 더해도 늘지 않는 것처럼 보인다",
    "표현할 수 있는 자릿수를 넘어가 작은 값이 반올림에 묻혔다 — 큰 누적값에 작은 값을 계속 더하는 계산은 이 손해가 반복해서 쌓인다",
    "1e16 이라는 값이 특별해서 오직 이 경우에만 생기는 현상이며, 다른 크기의 수를 쓰면 이런 일은 전혀 일어나지 않는다 — 그래서 누적 계산에는 아무 영향이 없다" ] },
  { k: "딱 맞는 답이 없을 때", o: [
    "해가 없으므로 아무 답도 못 구한다 — 식이 미지수보다 많으면 연립방정식이 모순이라 풀이 자체가 정의되지 않고, 점을 두 개로 줄여야 직선이 나온다",
    "잔차 제곱합을 가장 작게 만드는 직선이다 — 여기서는 y = 1.9914x + 1.0381, 잔차 제곱합 0.1070",
    "잔차의 절댓값을 모두 더한 값을 정확히 가장 작게 만들어 주는 직선이다 — 그것이 바로 최소제곱이 하는 일이고, 제곱은 계산 편의를 위한 표기일 뿐이다",
    "여섯 점 중 두 점을 골라 그 직선을 쓴다 — 두 점이면 직선이 하나로 정해지므로 가장 멀리 떨어진 두 점을 고르면 나머지 점도 대체로 그 근처에 놓인다" ] },
  { k: "언제 정확한 산술로", o: [
    "계산 횟수가 많은가다 — 많으면 오차가 쌓이므로 정확한 산술을 쓰고, 몇 번 안 되는 계산은 실수로 두어도 오차가 눈에 띄지 않으므로 그대로 둔다",
    "결과가 정확히 맞아야 하는 요구가 있는가다 — 회계·법적 계산·비교 판정처럼 1의 차이가 사건이 되는 곳은 정확한 산술로 간다",
    "속도가 중요하지 않으면 정확한 산술을 쓰는 것이 낫다 — 느려도 괜찮은 곳에서는 굳이 오차를 감수할 이유가 없으니 기본을 정확한 산술로 두고 예외만 실수로 한다",
    "값의 크기가 큰가다 — 아주 큰 값을 다루면 표현할 수 있는 범위를 넘길 수 있으므로 언제나 정확한 산술을 써야 하고, 작은 값은 실수로도 충분히 정확하다" ] },
  { k: "거의 0 이면 더 나쁘다", o: [
    "행렬식이 작으면 답이 0에 가까워진다 — 나누는 수가 작아지므로 역행렬의 원소가 전부 0 근처로 몰려 의미 있는 값이 남지 않는다",
    "행렬식이 작으면 나눗셈이 늘어 계산이 느려질 뿐이고 얻는 답의 정확도 자체는 그대로 같다",
    "역행렬이 계산은 되지만 입력의 작은 오차가 답에서 크게 부풀려진다 — 조건수가 그 부풀림 배율이다",
    "0이 아니면 안전하므로 문제없다 — 역행렬이 존재한다는 것이 곧 풀 수 있다는 뜻이고, 크기는 이론상 아무 관계가 없다" ] },
  { k: "정규방정식의 함정", o: [
    "행렬 곱이 느려서다 — 정확도는 같지만 XᵀX 를 만드는 데 n² 의 곱셈이 들어 큰 데이터에서는 QR 분해보다 훨씬 오래 걸린다",
    "위험하지 않다 — 교과서에 나오는 공식이므로 그대로 옮겨 쓰면 아무 문제가 없고, 라이브러리도 내부적으로 같은 식을 계산한다",
    "역행렬이 항상 존재하지 않아서다 — XᵀX 는 정방행렬이지만 특이할 수 있으므로 역행렬을 구하기 전에 행렬식을 확인해야 한다",
    "XᵀX 를 만드는 순간 조건수가 제곱된다 — 원래 쓸 만하던 문제가 못 푸는 문제가 된다" ] }
] },
{ track: "stat", fixes: [
  { k: "CLT 예외", o: [
    "표본크기가 10000 으로 아주 커서 오히려 정규 근사가 과하게 적용되는 경우",
    "모집단이 0 과 1 두 값만 갖는 이산형 베르누이 분포라 연속 근사가 어려운 경우",
    "관측치들이 서로 독립이 아니고 강하게 자기상관된 시계열인 경우",
    "모집단이 균등분포라 종 모양과 거리가 멀어 평균도 종 모양이 안 되는 경우" ] },
  { k: "데이터 95% 오해", o: [
    "신뢰구간은 개별 관측값이 아니라 모수(예: 평균)의 위치에 대한 구간이기 때문",
    "95% 가 아니라 68% 가 맞기 때문 — 1 표준편차 안에 들어가는 비율이 신뢰구간의 정의다",
    "신뢰구간은 데이터가 퍼져 있는 전체 범위보다 넓게 잡히므로 95% 보다 훨씬 많이 들어가기 때문이다",
    "데이터가 정규분포일 때만 성립하는 말이라 일반적인 자료에서는 비율이 달라지기 때문" ] },
  { k: "효과 크기도 함께", o: [
    "표본이 아주 크면 p 값은 작아지게 되어 있으므로 이 결과는 결국 그냥 무시하는 것이 맞다 — 100만 명 실험에서 나온 p 값은 어떤 결론에도 쓸 수 없다",
    "p 가 매우 작으므로 적용해야 한다 — 0.001 은 우연히 나올 확률이 1000번에 한 번이라는 뜻이고, 그 정도로 확실한 차이를 두고 망설이면 실험을 한 의미가 없다",
    "차이가 작으므로 p 값이 잘못 계산된 것이다 — 0.02%p 차이에서 p = 0.001 이 나올 수는 없으니 검정 방법이나 데이터 집계에 오류가 있는지 먼저 확인해야 한다",
    "통계적으로는 확실하지만 실질적으로 의미 있는 크기인지는 별개다 — 개발·운영 비용과 견줘 판단해야 한다" ] },
  { k: "경보 피로", o: [
    "담당자 수를 크게 늘려서 하루 경보 1,099건을 빠짐없이 모두 직접 확인하도록 만든다",
    "여러 신호를 함께 봐서 사후 확률이 충분히 높을 때만 경보를 낸다 — 건수를 줄이고 적중률을 올린다",
    "경보를 더 눈에 띄게 표시하고 소리와 색을 바꿔 담당자가 무시할 수 없게 만든다",
    "민감도를 100% 로 올려 진짜 장애를 하나도 놓치지 않게 하면 담당자가 경보를 신뢰하게 된다" ] },
  { k: "p와 효과크기", o: [
    "통계적으로는 유의하지만 효과크기가 실무적으로 의미 있는 수준인지 따로 판단해야 한다",
    "표본이 아주 크면 p 값을 믿을 수 없게 되므로 이 결과는 무시하고 작은 표본으로 다시 실험해야 한다",
    "효과크기가 0.02%p 로 작으므로 p = 0.001 이 나올 수 없고, p 값 계산이 잘못된 것이다",
    "p 가 매우 작으니 효과도 매우 크다 — p 값이 작을수록 실제 차이가 크다는 뜻이기 때문이다" ] },
  { k: "p 값은 무엇이 아닌가", o: [
    "귀무가설이 참일 확률이 3% 라는 뜻이다 — 데이터를 본 뒤 '효과가 없다' 는 가설이 맞을 가능성이 그만큼 낮아졌다는 말이다",
    "효과 크기가 3% 라는 뜻이다 — A 와 B 의 전환율 차이가 3%p 라는 것을 확률 형태로 적은 값이므로 그대로 이득으로 읽으면 된다",
    "효과가 없다고 가정했을 때 이만한 결과가 나올 확률이 3% 라는 뜻이다 — 가설이 틀릴 확률이 3% 라는 뜻이 아니다",
    "이 실험을 처음부터 똑같이 다시 해 보면 97% 의 확률로 같은 결과가 그대로 재현된다는 뜻으로 읽으면 된다" ] },
  { k: "정리하면", q: "여러 사례가 공통으로", o: [
    "결국 검사 성능이 충분히 높기만 하면 거기서 나온 결과는 그대로 믿을 수 있다는 것 — 민감도와 특이도가 99% 면 양성 판정도 99% 맞는다",
    "표본을 늘리면 모든 문제가 해결된다 — 검사를 더 많이 하면 오양성이 평균으로 묻혀 사라지므로 결국 정확한 결론에 이른다",
    "확률 계산은 직관과 늘 일치한다 — 사례들이 놀라워 보이는 것은 계산이 틀렸기 때문이지 직관이 틀렸기 때문이 아니다",
    "드문 일을 찾을 때는 흔한 쪽의 숫자가 결과를 지배한다 — 검사 성능만으로는 결론이 안 난다" ] }
] },
{ track: "mleval", fixes: [
  { k: "FPR 정의", o: [
    "FP/(FP+TP) - 양성으로 예측한 것 중 틀린 비율",
    "FN/(FN+TP) - 실제 양성 중 놓친 것의 비율",
    "FP/전체 샘플 수 - 전체 중 거짓 양성이 차지하는 비율",
    "FP/(FP+TN) - 실제 음성 중 양성으로 잘못 판정한 비율" ] },
  { k: "선택 데이터", o: [
    "검증 세트(또는 교차검증 폴드)",
    "운영 트래픽 전체의 실제 분포",
    "최종 성능을 재는 테스트 세트",
    "모델을 학습시킨 훈련 세트" ] },
  { k: "SVM 마진", o: [
    "경사하강법의 보폭인 학습률(rate)",
    "분류에 참여하는 이웃의 개수 k",
    "결정경계와 데이터 사이의 마진",
    "결정 트리가 갈라지는 깊이" ] },
  { k: "파라미터", o: [
    "데이터 학습으로 추정되는 가중치·절편",
    "사람이 학습 전에 미리 넣는 설정값",
    "예측 결과를 정리한 혼동행렬의 각 칸",
    "학습이 끝난 뒤 평가에 쓰는 지표" ] },
  { k: "특이도", o: [
    "실제 음성 중 음성으로 맞힌 비율, TN/(TN+FP)",
    "양성 예측 중 실제로 맞힌 비율, TP/(TP+FP)",
    "실제 양성 중 양성으로 맞힌 비율, TP/(TP+FN)",
    "전체 예측 중 맞힌 비율, (TP+TN)/전체" ] },
  { k: "LOOCV", o: [
    "폴드 수가 가장 많으므로 어떤 상황에서도 가장 믿을 만한 성능 추정치를 내주는 방식이다",
    "훈련 데이터를 거의 다 쓰므로 편향은 작지만 계산 비용이 크고 추정치의 분산이 커질 수 있다",
    "회귀 문제에는 쓸 수 없다 — 샘플 하나로는 연속값 오차를 계산할 수 없기 때문이다",
    "가장 계산이 빠른 방식이다 — 폴드마다 검증 샘플이 하나뿐이라 평가 비용이 거의 없다" ] },
  { k: "층화 목적", o: [
    "같은 그룹의 샘플이 한 폴드에만 들어가도록 갈라 그룹 누수를 막는다",
    "데이터를 무작위로 섞기만 하고 클래스 비율은 신경 쓰지 않는다",
    "폴드를 시간 순서대로 나눠 미래 데이터가 훈련에 섞이지 않게 한다",
    "각 폴드의 클래스 비율을 원본 데이터와 비슷하게 유지한다" ] }
] },
{ track: "git", fixes: [
  { k: "추적 설정", o: [
    "이후 git push/pull 만 쳐도 되도록 추적 관계를 설정한다",
    "전송을 여러 갈래로 나눠서 업로드 속도를 크게 높인다",
    "푸시하기 전에 사용자 인증을 대신 처리해 자격증명을 저장한다",
    "원격 브랜치의 내용을 내 것으로 덮어쓰는 강제 푸시를 한다" ] },
  { k: "포맷 잡음", o: [
    "비교할 줄 자체가 줄어 병합 계산이 눈에 띄게 빨라진다",
    "포맷 브랜치를 따로 둘 필요가 없어 관리할 브랜치 개수가 줄어든다",
    "서로 같은 줄을 고치더라도 충돌이 아예 나지 않게 되어서",
    "사람마다 다른 포맷으로 생기는 의미 없는 줄 차이가 사라져 충돌이 준다" ] },
  { k: "pull 전략", o: [
    "같은 양의 커밋을 압축해 받아 오므로 더 빨리 끝난다",
    "원격 커밋 위에 내 커밋을 다시 얹어 불필요한 병합 커밋을 만들지 않는다",
    "내 커밋을 새로 만들기 때문에 합칠 때 충돌이 아예 나지 않는다",
    "받아서 합친 뒤 원격으로 자동 푸시까지 대신 해 주기 때문에" ] },
  { k: "실패 알림", o: [
    "알림을 아예 끄고 각자 필요할 때 상태 페이지에서 직접 확인하게 한다",
    "매일 아침 담당자 한 사람이 수동으로 파이프라인 화면을 훑어 확인한다",
    "실패 시 팀 채널로 알림을 보내고 main 실패를 최우선 과제로 삼는다",
    "실패한 잡을 정해진 횟수만큼 자동으로 다시 돌려 통과할 때까지 재시도한다" ] },
  { k: "릴리스 노트", o: [
    "커밋 메시지 규약(feat/fix 등)을 지키고 PR 제목을 잘 쓴다",
    "릴리스할 때마다 버전 태그를 빠짐없이 꼬박꼬박 달아 두기만 한다",
    "작업마다 브랜치를 넉넉히 많이 만들어 브랜치 이름에 내용을 담는다",
    "커밋을 큰 단위로 묶어 개수를 줄이면 노트에 적을 항목도 함께 줄어든다" ] },
  { k: "README", o: [
    "기여자별 커밋 통계와 활동 그래프를 눈에 띄게 보여 준다",
    "프로젝트에 기여해 온 사람들의 이름 목록을 빠짐없이 적는다",
    "적용되는 라이선스의 전체 문구를 첫 화면에 그대로 싣는다",
    "이 프로젝트가 무엇이고 어떤 문제를 푸는지 한두 문장" ] },
  { k: "정의", q: "Pull Request", o: [
    "원격 저장소를 내 계정으로 복사해 독립적으로 작업할 수 있게 한다",
    "내 브랜치를 합쳐 달라고 요청하고 리뷰를 받는 자리를 만든다",
    "조건이 맞으면 리뷰 없이 코드를 자동으로 합쳐 준다",
    "버그나 요청 사항을 기록하는 이슈를 만들어 담당자를 지정한다" ] }
] },
{ track: "php", fixes: [
  { k: "파일 포함", o: [
    "경로 조작으로 서버의 임의 파일을 읽히거나 실행시킬 수 있다(LFI) — 화이트리스트 매핑으로 바꿔야 한다",
    "애초에 PHP 8 에서는 이런 코드가 문법 오류로 처리되어 실행되지 않으므로 위험하지 않다 — 옛 버전에서만 문제였다",
    "포함되는 파일이 매번 달라져 opcache 가 결과를 캐시하지 못하므로 요청마다 파싱 비용이 들어 느려진다",
    "문자열 결합과 include 가 매 요청마다 디스크를 읽으므로 트래픽이 많으면 I/O 병목이 되어 응답이 느려진다" ] },
  { k: "용량 산정", o: [
    "워커 수를 CPU 코어 수와 정확히 같게 맞추는 것이 가장 좋은 방법이다",
    "가용 메모리 ÷ 요청당 평균 메모리 로 상한을 잡고, 대기 시간 비중을 보며 조정한다",
    "가능한 한 크게 잡아 두면 요청이 몰려도 대기 없이 처리되므로 안전하다",
    "예상 동시 접속자 수와 같게 맞추면 사용자마다 워커가 하나씩 돌아 대기가 없다" ] },
  { k: "함수 호출", o: [
    "그 함수의 오토로딩이 비활성화되어 클래스 로더를 거치지 않고 곧바로 불린다",
    "현재 네임스페이스의 같은 이름 함수가 아니라 전역 함수를 부르므로 동작이 달라진다",
    "그 함수가 전역 네임스페이스에 새로 등록되어 이후 어디서나 부를 수 있게 된다",
    "현재 네임스페이스를 먼저 찾는 조회 단계를 건너뛰어, 의도가 분명해지고 아주 약간 빠르다" ] },
  { k: "세션 고정", o: [
    "로그인 후에 같은 계정으로 여러 기기에서 동시에 접속하는 것을 허용해 주기 위해서",
    "세션 ID 가 더 짧은 새 값으로 바뀌어 쿠키 크기를 줄이고 매 요청의 전송량을 아끼려고",
    "로그인 전에 담아 둔 세션 데이터를 모두 지우고 완전히 깨끗한 세션으로 새로 시작하려고",
    "공격자가 미리 알려 준 세션 ID 가 로그인 후에도 유효한 상태(세션 고정)를 막으려고" ] },
  { k: "정렬 반환", o: [
    "정렬된 배열 — 원본 $arr 와 같은 배열이 그대로 돌아온다",
    "정렬된 새 배열 — 원본은 그대로 두고 복사본을 정렬해 돌려준다",
    "null — sort 는 반환값이 없는 함수라 아무것도 돌려주지 않는다",
    "true 또는 false — 정렬 성공 여부이며 배열은 원본이 참조로 바뀐다" ] },
  { k: "lock", o: [
    "모든 환경에서 정확히 같은 버전이 설치되도록 재현성을 보장하려고",
    "Composer 가 lock 파일이 없으면 install 을 거부하고 오류를 내기 때문에",
    "알려진 보안 취약점을 찾아내 고쳐 주도록 하기 위해서다",
    "vendor 디렉터리를 커밋하지 않아도 되어 저장소 크기를 줄이려고" ] }
] },
{ track: "pandas", fixes: [
  { k: "indicator 열", o: [
    "'_flag' 열에 매칭 여부를 뜻하는 True/False 두 가지 값",
    "'_join' 열에 조인 방식을 뜻하는 inner/outer/cross 세 가지 값",
    "'_merge' 열에 left_only/right_only/both 세 가지 값",
    "'_source' 열에 각 행이 나온 원본 DataFrame 의 변수명" ] },
  { k: "inplace 오해", o: [
    "대부분 내부 복사가 일어나 메모리 절감 효과가 보장되지 않는다",
    "None 대신 자기 자신을 돌려주므로 체이닝이 가능해져 가독성이 높아진다",
    "복사 단계를 건너뛰므로 어떤 연산이든 속도를 두 배 이상 빠르게 만든다",
    "새 객체를 만들지 않고 원본을 고치므로 복사를 피해 메모리를 절약해 준다" ] },
  { k: "안전한 조인 습관", o: [
    "조인이 끝난 뒤 drop_duplicates() 를 호출해 늘어난 행을 정리한다",
    "how='outer' 로 조인해 어느 쪽 행도 잃지 않도록 데이터 손실을 원천 차단한다",
    "inplace=True 를 붙여 조인 중간 결과의 메모리 사용을 줄인다",
    "조인 전 키 유일성을 확인하고 validate와 indicator를 함께 쓴다" ] },
  { k: "validate", o: [
    "MergeError 예외가 발생하며 조인이 중단된다",
    "중복 키에 해당하는 행이 경고 없이 조용히 결과에서 제외된다",
    "중복 행이 자동으로 제거된 뒤 하나만 남겨 조인이 진행된다",
    "경고만 출력하고 중복 키의 카티션 곱 결과를 그대로 반환한다" ] },
  { k: "apply 성능", o: [
    "반환 shape 이 항상 그룹 수와 같아 결과 형태를 예측하기 쉽다",
    "그룹마다 파이썬 함수를 호출하므로 agg/transform보다 일반적으로 느리다",
    "transform 과 동일한 결과를 더 짧게 쓸 수 있는 문법일 뿐이다",
    "내부적으로 C 로 최적화되어 있어 agg 보다 항상 빠르게 동작한다" ] }
] },
{ track: "compiler", fixes: [
  { k: "데드 코드 제거", o: [
    "실행 파일을 압축하면 링크 결과 자체가 줄어들어 안 쓰는 함수도 함께 사라진다",
    "-O3 로 올리면 컴파일러가 다른 파일의 미사용 함수까지 찾아 제거해 준다",
    "링커는 모든 심볼을 포함하므로 라이브러리를 함수 단위로 잘게 쪼개는 수밖에 없다",
    "함수·데이터를 각각 별도 섹션에 두고 링커에게 참조되지 않은 섹션을 버리게 한다" ] },
  { k: "cmp-lex-token2", o: [
    "파서는 문법 판단에 종류가 필요하고, 이후 단계는 실제 문자열 값이 필요하기 때문",
    "타입 검사를 어휘 분석 단계에서 끝내려면 종류와 값이 함께 있어야 하기 때문",
    "렉심 문자열만으로는 파일에 저장할 수 없어 종류 코드를 붙여야 하기 때문",
    "종류와 렉심이 같은 값이라 중복해서 저장해 두는 편이 안전하기 때문이다" ] },
  { k: "cmp-ast-tree3", o: [
    "원본 소스와 괄호·공백까지 똑같은 문자열",
    "스택 기반 VM에 바로 쓰기 좋은 후위 표기식",
    "연산자가 피연산자 사이에 오는 중위 표기식",
    "어휘 분석기가 만든 것과 같은 토큰 목록" ] },
  { k: "cmp-gen-reg2", o: [
    "두 값이 동시에 살아 있어 같은 레지스터를 쓸 수 없다",
    "두 값이 항상 같은 명령에서 함께 계산된다",
    "두 값의 타입이 같아 같은 레지스터 부류에 들어간다",
    "두 값이 같은 함수 안에 선언되어 있다는 것" ] },
  { k: "cmp-run-jit1", o: [
    "코드를 즉시 실행할 수 있어 시작이 빠르고 반복 개발·디버깅이 편하다",
    "실행하기 전에 기계어로 된 실행 파일을 만들어 두어야 하므로 배포가 단순하다",
    "실행 중 해석 오버헤드가 전혀 없어 같은 코드를 반복 실행해도 느려지지 않는다",
    "타입 검사를 컴파일 타임에만 수행하므로 실행 중에는 타입 오류가 날 수 없다" ] }
] },
{ track: "os", fixes: [
  { k: "코루틴", o: [
    "협력적(cooperative)으로 yield/await 지점에서만 제어를 양보하며, 전환이 유저 공간에서 일어난다",
    "코루틴은 병렬로 실행되어야 하므로 여러 코어를 동시에 점유하며, 어느 코어에 올릴지는 커널 스케줄러가 배정해 준다",
    "코루틴 하나가 각각 별도의 커널 스레드에 1:1 로 매핑되어 관리되므로 전환 비용은 스레드 전환과 같다",
    "커널 스케줄러가 타임 슬라이스마다 코루틴을 선점적으로 갈아 끼우며 실행하므로 양보 지점을 적을 필요가 없다" ] },
  { k: "cs2-ctxsw-mode-vs-context", o: [
    "모드 전환과 컨텍스트 스위칭은 짝을 이뤄 일어나므로, 두 가지의 비용을 따로 떼어서 볼 필요가 없다",
    "모드 전환은 같은 프로세스 내 유저↔커널 전환일 뿐, 반드시 다른 프로세스로의 컨텍스트 스위칭을 수반하지 않는다",
    "컨텍스트 스위칭이 모드 전환보다 더 저렴하게 끝난다 — 레지스터만 바꾸면 되고 특권 레벨은 건드리지 않기 때문이다",
    "모드 전환이 일어나면 실행 중이던 프로세스가 반드시 교체된다 — 커널이 실행되는 동안 다른 프로세스가 CPU 를 쓴다" ] },
  { k: "cs2-sched-priority-inversion", o: [
    "우선순위가 낮은 태스크가 높은 태스크보다 먼저 실행되도록 스케줄러가 순서를 정할 때",
    "실행 대기 중인 모든 태스크의 우선순위 값이 서로 같아 스케줄러가 순서를 정할 수 없을 때",
    "고우선 태스크가 저우선 태스크가 쥔 락을 기다리는데, 중우선 태스크가 저우선을 계속 선점할 때",
    "라운드로빈 스케줄러의 타임 퀀텀이 지나치게 커서 고우선 태스크의 차례가 늦게 돌아올 때" ] },
  { k: "osobs-container-vm", o: [
    "컨테이너는 자기 게스트 커널을 따로 부팅하고, VM 은 호스트의 커널을 그대로 공유한다",
    "컨테이너는 호스트 커널을 공유하는 격리된 프로세스이고, VM은 자기 게스트 커널을 따로 부팅한다",
    "컨테이너를 쓰려면 CPU 의 가상화 확장 기능이 반드시 필요하고, VM 은 그것 없이도 동작한다",
    "VM 은 호스트와 파일시스템을 그대로 공유해서 쓰고, 컨테이너는 자기 디스크 이미지를 따로 가진다" ] }
] },
{ track: "numpy", fixes: [
  { k: "ravel vs flatten", o: [
    "둘 다 항상 뷰를 주므로 결과를 바꾸면 원본도 바뀐다",
    "ravel()은 가능하면 뷰를 주고, flatten()은 항상 복사한다",
    "ravel()은 항상 복사하고, flatten()은 가능하면 뷰를 준다",
    "둘 다 항상 복사하므로 결과를 바꿔도 원본은 그대로다" ] },
  { k: "seed 오개념", o: [
    "이후 모든 난수 호출이 같은 값을 반복해서 내므로 배열을 만들면 원소가 모두 같다",
    "난수가 고정되어 배열의 크기나 호출 방식과 무관하게 언제나 같은 값이 나온다",
    "seed 는 성능 최적화 용도라 나오는 값에는 영향이 없고 생성 속도만 달라진다",
    "정해진 난수열을 앞에서부터 꺼내 쓰므로 호출 순서나 횟수가 바뀌면 값도 달라진다" ] },
  { k: "argmax 축", o: [
    "각 열의 최댓값 자체를 담은 값들",
    "각 열에서 최댓값이 있는 행 인덱스",
    "배열 전체에서 가장 큰 값 하나",
    "각 행 최댓값이 있는 열 인덱스" ] },
  { k: "샘플별 최댓값", o: [
    "Z.max(0, keepdims=True) — 결과 shape 가 (1, 10) 이 된다",
    "Z.max(axis=1) — 결과 shape 가 (32,) 로 1차원이 된다",
    "Z.max(axis=1, keepdims=True)",
    "Z.max(axis=0) — 결과 shape 가 (10,) 로 열별 값이 된다" ] }
] },
{ track: "ml", fixes: [
  { k: "테스트셋", o: [
    "하이퍼파라미터를 튜닝할 때마다 반복해서 성능을 확인하는 데 쓴다",
    "모든 개발이 끝난 뒤 최종 일반화 성능 평가에만 사용한다",
    "검증 세트를 대신해 에폭마다 성능을 확인하고 조기 종료를 결정한다",
    "훈련 세트에 합쳐 모델을 학습시켜 데이터를 최대한 활용한다" ] },
  { k: "L2 효과", o: [
    "계수를 무작위 값으로 바꿔 특정 특성에 치우치지 않게 한다",
    "값이 작은 계수를 정확히 0 으로 만들어 특성을 걸러 내 준다",
    "계수를 전체적으로 0에 가깝게 축소하지만 정확히 0으로 만들지는 않는다",
    "계수의 부호를 반대로 뒤집어 과적합된 방향을 상쇄한다" ] },
  { k: "스케일링 필요", o: [
    "규제와 스케일링은 사실 아무 관련이 없지만 관례상 함께 쓰기 때문",
    "벌점이 계수의 크기에 걸리므로 스케일이 다르면 특정 특성이 부당하게 더 억제된다",
    "미리 스케일링을 해 두어야 계산한 손실 값이 음수로 내려가지 않기 때문",
    "스케일링이 클래스 불균형을 해결해 규제가 소수 클래스를 덜 억제하기 때문" ] },
  { k: "드롭아웃 목적", o: [
    "훈련과 검증 데이터가 섞이는 데이터 누수를 차단한다",
    "소수 클래스의 뉴런을 더 자주 켜서 클래스 불균형을 해결한다",
    "매 스텝 일부 뉴런을 꺼서 학습 속도를 두 배 이상 빠르게 만든다",
    "뉴런들이 특정 조합에만 의존하는 공적응을 막아 과적합을 줄인다" ] }
] },
{ track: "ai", fixes: [
  { k: "aie2-quality-cost-latency", o: [
    "어떤 태스크에서든 가장 값이 싼 모델을 골라 쓰는 편이 낫다 — 품질 차이는 프롬프트로 메운다",
    "태스크별 품질 임계치를 정하고 이를 충족하는 가장 싼/빠른 모델을 고른다",
    "무조건 가장 큰 모델을 골라 쓴다 — 품질이 최우선이고 비용은 나중에 줄이면 된다",
    "지연은 무시하고 벤치마크 점수만 본다 — 점수가 높은 모델이 결국 제품에서도 낫다" ] },
  { k: "청킹 실패", o: [
    "관련 문장이 무관한 내용에 희석되어 임베딩 신호가 약해지고 검색 정밀도가 떨어진다",
    "검색 재현율(recall)이 어떤 질의를 넣어도 0 으로 나와 버려 아무 문서도 찾지 못한다",
    "청크가 크면 임베딩 모델의 출력 차원이 자동으로 늘어나 벡터 저장소와 맞지 않게 된다",
    "긴 청크는 토크나이저가 UNK 토큰을 더 많이 만들어 임베딩이 의미를 잃는다" ] },
  { k: "ai-serve-token-unit", o: [
    "토큰 하나가 단어 하나와 1:1 로 정확하게 대응하도록 규격에 정해져 있어 단어 수가 곧 토큰 수다",
    "토큰은 보통 서브워드 단위이며, 같은 의미의 문장도 언어와 표기에 따라 토큰 수가 다르다",
    "공백은 절대 토큰에 포함되지 않으므로 띄어쓰기를 어떻게 하든 토큰 수는 같다",
    "토큰 수는 언어와 상관없이 문자 수에 정비례하므로 글자 수를 세면 토큰 수를 알 수 있다" ] },
  { k: "연속 배칭", o: [
    "먼저 끝난 시퀀스의 슬롯을 기다리지 않고 새 요청으로 즉시 채워 GPU 유휴를 줄여서",
    "모든 요청의 출력 길이를 미리 똑같은 값으로 맞추도록 강제해 두고 있기 때문",
    "모델 가중치를 매 스텝 다시 로딩하지 않고 GPU 메모리에 상주시켜 두어서",
    "KV 캐시를 CPU 메모리로 옮겨 GPU 메모리를 비우고 더 큰 배치를 잡아서" ] }
] },
{ track: "fp", fixes: [
  { k: "fp-imm-nested1", o: [
    "{ ...state, city: '부산' } — 최상위에 city 를 두면 가장 안쪽 값까지 자동으로 덮인다",
    "{ ...state, user: { ...state.user, addr: { ...state.user.addr, city: '부산' } } } — 다른 키에 담는다",
    "state.user.profile.city = '부산'; — 경로가 깊어도 직접 대입이 가장 명확하다",
    "{ ...state, user: { ...state.user, profile: { ...state.user.profile, city: '부산' } } }" ] },
  { k: "상태를 값으로 넘기기", o: [
    "(상태, 사건) → 새 상태 모양으로 바꿔 상태를 인자로 받고 새 상태를 돌려준다. 실제로 값을 들고 바꾸는 일은 바깥의 얇은 층 한 곳이 맡는다",
    "상태가 필요한 프로그램은 함수형으로 쓸 수 없으므로 이 부분만 명령형으로 남겨 두고 나머지 부분만 함수형으로 바꾸는 것이 현실적인 절충이다",
    "전역 변수를 const 로 바꾸면 불변이 되므로 함수는 그대로 두어도 된다 — 재할당이 막히니 상태가 바뀌지 않고, 배열 메서드로 안을 고치는 것은 문제가 아니다",
    "함수마다 자기 사본을 들게 하면 서로 간섭하지 않으므로 그것으로 충분하다 — 각 함수가 장바구니를 복사해 갖고 있으면 순수성이 지켜지고 합칠 필요도 없다" ] },
  { k: "fp-partial-multi1", o: [
    "한 번에 여러 개의 인자를 동시에 고정할 수 있다",
    "인자를 한 번에 하나씩만 고정하는 것이 정의다",
    "새 함수를 만드는 대신 원본 함수를 변경한다",
    "돌려주는 값이 함수가 아니라 항상 원시값이다" ] }
] },
{ track: "devops", fixes: [
  { k: "cloud2-tf-state-lock", o: [
    "state 파일을 git 에 커밋해 두 사람이 같은 파일을 보게 한다",
    "원격 백엔드(S3 등)+상태 잠금(DynamoDB 락)으로 동시 실행을 직렬화",
    "apply 를 실행하기 전에 서로 연락해 겹치지 않게 조율한다",
    "로컬 state 를 각자 보관하고 나중에 수동으로 합친다" ] },
  { k: "docker-apt-cache", o: [
    "apt-get install 에 --no-update 옵션을 붙여 패키지 인덱스 갱신을 건너뛰고 설치만 하도록 바꿔라",
    "update 와 install 을 한 RUN 으로 합치고 rm -rf /var/lib/apt/lists/* 로 캐시를 지워라",
    "apt-get 을 yum 으로 교체하라 — yum 은 패키지 인덱스 파일을 따로 남기지 않아 결과 이미지가 훨씬 깨끗하다",
    "RUN 을 세 개로 더 나눠 update·install·정리를 각각 별도 레이어로 분리해 빌드 캐시가 잘 맞게 하라" ] },
  { k: "force-lease", o: [
    "원격 브랜치를 로컬로 fetch한 이후 다른 사람이 push했다면 거부하여 남의 커밋 덮어쓰기를 방지한다",
    "push 전에 원격 훅이 강제로 실행되어 CI 통과를 보장하므로 깨진 코드가 올라가지 않는다",
    "서버에 백업 ref 를 자동 생성하여 덮어쓴 커밋을 언제든 복구할 수 있게 만들어 준다",
    "force 와 달리 fast-forward 만 허용하므로 히스토리를 아예 재작성하지 않기 때문에 안전하다" ] }
] },
{ track: "dbt", fixes: [
  { k: "해시 조인이 맞는 자리", o: [
    "작은 표끼리 붙일 때만 쓸 수 있다 — 작은 표 두 개면 해시 표가 메모리에 넉넉히 들어가지만 표가 크면 해시 표를 만들 수 없어 중첩 루프로 되돌아간다",
    "큰 표끼리 등호로 붙일 때 유리하고, 인덱스가 없어도 된다. 다만 등호에서만 쓸 수 있고 해시 표가 메모리에 들어가야 한다",
    "양쪽이 정렬돼 있어야만 쓸 수 있다 — 해시 값을 순서대로 비교해 나가는 방식이라 정렬되지 않은 표에서는 같은 값을 만나지 못하고 지나친다",
    "부등호 조인에 가장 유리하고 메모리를 안 쓴다 — 해시 값의 크기 비교로 범위를 판정하므로 등호보다 부등호에서 이득이 크고 표를 디스크에서 바로 읽는다" ] },
  { k: "통계가 낡으면", o: [
    "실행 계획이 바뀌었는지 보고, 통계가 낡아 추정 행 수가 실제와 어긋났는지 확인한다",
    "인덱스를 하나 더 만들어 본다 — 인덱스를 하나 더 두면 그만큼 옵티마이저가 고를 후보가 늘어나 더 나은 계획을 찾는다",
    "쿼리를 여러 개로 쪼개 본다 — 조인이 적은 쿼리는 계획이 단순해 옵티마이저가 잘못 고를 여지가 줄어든다",
    "서버를 재시작한다 — 버퍼 캐시가 오염되어 디스크를 다시 읽고 있는 것이므로 캐시를 비우면 어제 속도로 돌아온다" ] },
  { k: "흔한 단어의 값어치", o: [
    "그 단어로는 검색이 되지 않는다는 뜻이다 — 계산 결과가 0 이면 그 단어는 완전히 색인에서 빠진 것으로 본다",
    "모든 문서에 있는 단어는 어느 문서를 고를지 정하는 데 아무 도움이 안 된다 — 변별력이 없으므로 점수에 기여하지 않는다",
    "그 단어가 색인에서 자동으로 제거되었다는 뜻이다 — 색인기가 모든 문서에 나오는 단어를 불용어로 보고 목록에서 뺀다",
    "계산에 오류가 있어 값이 잘못 나온 것이다 — 어떤 단어든 문서에 나오면 양의 점수를 가져야 하므로 0 은 나올 수 없다" ] },
  { k: "드문 단어일수록 이득", o: [
    "측정 오차다 — 두 경우 모두 실제로는 같은 배수의 이득을 준다. 두 방식 모두 결과 수와 무관하게 같은 시간이 걸리므로 차이는 잡음이다",
    "역색인은 단어 길이가 길수록 빨라지므로 긴 단어에서 이득이 크다 — 긴 단어는 해시 충돌이 적어 목록을 찾는 시간이 짧아지기 때문이다",
    "흔한 단어에서는 역색인이 손해이므로 전체 훑기로 처리해야 한다 — 목록이 길면 목록을 읽는 시간이 전체를 훑는 시간보다 길어진다",
    "역색인의 이득은 결과가 적을수록 커진다 — 전체를 훑는 쪽은 결과 수와 무관하게 일정하고, 역색인은 꺼내는 목록의 길이만큼만 일한다" ] },
  { k: "무엇이 다른가", o: [
    "행 지향은 한 행의 값들을 디스크에 나란히 두고, 열 지향은 같은 열의 값들을 모아 둔다. 논리적인 표는 같지만 무엇이 이웃해 있느냐가 반대다",
    "열 지향은 행과 열을 뒤집어 저장하므로 조회할 때도 행과 열을 바꿔 써야 한다 — 열 지향에서는 질의에 열과 행을 반대로 적어야 같은 결과가 나온다",
    "둘은 논리적 모델의 차이일 뿐 실제 파일 배치는 같다 — 저장 엔진은 어느 쪽이든 페이지 단위로 쓰므로 디스크에 놓이는 바이트 순서는 달라지지 않는다",
    "행 지향은 디스크에, 열 지향은 메모리에만 둘 수 있다 — 열 지향은 압축 열을 통째로 올려 두어야 동작하므로 디스크 기반 저장소로는 만들 수 없다" ] }
] },
{ track: "arduino", fixes: [
  { k: "짝수 번 뒤집히면", o: [
    "못 잡는다 — 같은 자리에서 두 번 뒤집히면 XOR 결과가 원래대로 돌아온다",
    "바이트 수가 짝수일 때만 잡는다 — 홀수 개를 XOR 하면 마지막 바이트가 짝 없이 남는다",
    "잡지만 어느 바이트가 깨졌는지는 모른다 — 체크섬은 위치 정보를 담지 않기 때문이다",
    "잡는다 — 비트가 하나라도 바뀌면 XOR 결과는 달라지기 때문에 어떤 오류든 걸린다" ] },
  { k: "uno-isr-forbidden-delay", o: [
    "정상적으로 0.5초를 기다린 뒤 ISR 이 끝나고 loop 로 돌아가므로 문제가 없다",
    "ISR 안에서는 delay 가 millis 를 쓰는 논블로킹 대기로 바뀌어 정상 동작한다",
    "ISR 안의 delay 는 컴파일러가 금지하므로 컴파일 오류가 나 업로드 자체가 되지 않는다",
    "ISR 안에서는 인터럽트가 꺼져 있어 delay가 제대로 동작하지 않고 시스템이 멈춘 것처럼 된다" ] },
  { k: "동기를 잃었을 때", o: [
    "길이 필드는 잡음에 강하게 부호화되어 있어 이런 일이 생기지 않는다",
    "200바이트가 정해진 시간 안에 안 오면 수신기가 자동으로 복구된다",
    "체크섬이 틀리게 나오므로 그 프레임 하나만 버리고 넘어가면 다음부터는 정상으로 돌아온다",
    "다음 프레임들을 페이로드로 잘못 삼켜 이후 전체가 어긋난다 — 동기를 다시 맞출 방법이 필요하다" ] }
] },
{ track: "net", fixes: [
  { k: "nettb-buffering", o: [
    "프록시의 응답 버퍼링(nginx의 proxy_buffering)과 읽기 타임아웃 — 스트리밍 경로는 버퍼링을 끄고 타임아웃을 늘려야 한다",
    "클라이언트 쪽의 keep-alive 설정 — 클라이언트가 연결을 오래 유지하지 못해 스트림이 끊기고, 재연결할 때마다 밀린 이벤트가 한꺼번에 도착한다",
    "전송에 적용된 gzip 압축 레벨 — 레벨이 높으면 압축기가 한 블록을 채울 때까지 기다리므로 이벤트가 모였다가 한 번에 나간다",
    "DNS 레코드의 TTL 값 — TTL 이 짧으면 프록시가 주소를 다시 찾는 동안 이벤트를 쌓아 두었다가 연결이 복구되면 한꺼번에 보낸다" ] },
  { k: "netco-httponly", o: [
    "네트워크로 전송되지 않고 브라우저에만 남는다 — 그래서 세션 값이 서버로 전달되지 않는다",
    "HTTPS 연결에서만 전송되고 HTTP 에서는 빠진다 — 평문 연결에서 탈취되는 것을 막는 장치다",
    "자바스크립트의 document.cookie로 읽을 수 없을 뿐, 요청에는 평소처럼 자동으로 실려 나간다",
    "다른 사이트에서 시작된 요청에는 실리지 않으므로 CSRF 공격까지 함께 막아 준다" ] },
  { k: "nettb-proxy-kind", o: [
    "방향만 다를 뿐 클라이언트가 직접 설정해야 한다 — 그래서 리버스 프록시도 브라우저 설정이 필요하다",
    "정방향 프록시는 클라이언트를 대신해 나가는 요청을 대리하고, 리버스 프록시는 서버 앞에서 들어오는 요청을 대리한다",
    "정방향 프록시는 TLS 를 종료할 수 없다 — 클라이언트와 서버 사이의 암호화된 터널만 이어 주기 때문이다",
    "리버스 프록시는 캐시 기능을 가질 수 없다 — 서버의 응답을 그대로 전달하는 것이 역할이라 저장하지 않는다" ] }
] },
{ track: "dl", fixes: [
  { k: "과적합", o: [
    "학습이 진행되지 않아 학습 데이터에서 재는 손실조차 줄어들지 않는 현상",
    "학습 데이터에는 잘 맞지만 새로운(검증·테스트) 데이터에서는 성능이 떨어지는 현상",
    "학습 성능과 검증 성능이 둘 다 낮게 머물러 모델이 패턴을 못 잡는 현상",
    "가중치 파라미터가 모두 0 으로 수렴해 어떤 입력에도 같은 출력을 내는 현상" ] },
  { k: "어텐션", o: [
    "입력을 왼쪽에서 오른쪽으로 한 칸씩 차례대로 훑으며 이전 상태를 넘겨 처리한다",
    "합성곱 연산만으로 이미지의 지역 특징을 뽑아 내고 위치 정보는 풀링으로 버린다",
    "시퀀스 내 모든 위치가 서로의 관련도(가중치)를 계산해 문맥을 반영한다",
    "가중치 없이 모든 위치의 값을 단순 평균해 넘기므로 문맥과 무관한 표현이 된다" ] }
] },
{ track: "rust", fixes: [
  { k: "빌림의 규칙", o: [
    "읽기만 하는 빌림은 동시에 여럿 가능하지만, 고칠 수 있는 빌림은 그 순간 하나뿐이고 그동안 읽기 빌림도 함께 있을 수 없다",
    "빌림을 몇 개 만들든 제한은 없고, 대신 실행 도중에 충돌이 생기는지 검사해서 문제가 있으면 그 자리에서 패닉으로 멈춘다",
    "고칠 수 있는 빌림은 동시에 여럿 만들 수 있지만 읽기만 하는 빌림은 그 순간 하나뿐이고, 둘을 함께 두는 것은 허용된다",
    "읽기 빌림도 고치는 빌림도 종류마다 각각 하나씩만 만들 수 있고 그 이상은 막히므로, 여러 곳에서 읽으려면 복사해야 한다" ] }
] },
{ track: "react", fixes: [
  { k: "rx-form-submit", o: [
    "이벤트가 부모 요소로 전파되는 버블링을 멈추기 위해",
    "브라우저의 기본 폼 전송과 페이지 이동을 막고 자바스크립트로 처리하기 위해",
    "React 의 상태 갱신을 동기적으로 만들어 제출 직후 최신 값을 읽기 위해",
    "브라우저의 기본 입력 검증을 비활성화해 빈 값도 제출되게 하기 위해" ] },
  { k: "react2-ref-scroll", o: [
    "메시지 배열을 의존성으로 하는 이펙트에서 DOM ref의 scrollTop을 조정한다",
    "메시지를 추가하는 이벤트 핸들러에서 곧바로 scrollTop 을 최대값으로 설정한다",
    "CSS 의 flex-direction: column-reverse 만으로 처리하고 JavaScript 는 쓰지 않는다",
    "렌더 본문에서 매번 scrollTop 을 최대값으로 맞춰 렌더마다 아래로 붙인다" ] },
  { k: "rx-memo-key", o: [
    "key 가 문자열이 아니면 React 가 경고 없이 그 항목의 렌더를 중단한다",
    "인덱스 key 는 전체 리스트를 재생성시켜 성능이 크게 나빠진다 — 위치가 바뀌기 때문",
    "항목이 추가·삭제·정렬되면 같은 인덱스에 다른 항목이 매칭되어 입력값 같은 내부 상태가 엉킨다",
    "인덱스 key 를 쓰면 항목이 다시 렌더되지 않아 바뀐 값이 화면에 반영되지 않는다" ] },
  { k: "react2-sus-meaning", o: [
    "컴포넌트 렌더를 다음 프레임으로 지연시켜 초기 표시 성능을 개선한다",
    "아래 트리가 아직 준비되지 않았을 때 보여 줄 fallback을 선언한다",
    "자손 컴포넌트에서 발생한 에러를 잡아 대신 폴백 UI 를 보여 준다",
    "데이터를 대신 불러와 캐싱해 주는 페칭 라이브러리 역할을 한다" ] }
] },
{ track: "cs", fixes: [
  { k: "point-one-plus-two-cause", o: [
    "0.1과 0.2를 2진 분수로 유한하게 표현할 수 없어 반올림 오차가 생기기 때문",
    "CPU 의 부동소수점 연산 유닛에 오래된 설계 결함이 남아 있어 덧셈이 틀리기 때문",
    "언어 구현에 있는 오래된 버그라 최신 버전에서는 true 가 나오기 때문",
    "덧셈 순서를 바꿔 0.2 + 0.1 로 계산하면 정확한 값이 나오기 때문" ] }
] },
{ track: "code", fixes: [
  { k: "정리하면", q: "성질 적기", o: [
    "테스트를 최대한 많이 써서 코드 한 줄마다 검사를 붙여 두자는 이야기로 받아들이면 된다",
    "무작위성이 결정론보다 낫다는 것을 보여 주려는 것이다 — 정해진 예제보다 무작위 입력이 언제나 더 많은 버그를 찾는다",
    "기존 예제 테스트를 전부 없애고 성질만 남기자는 것이다 — 예제는 성질의 특수한 경우일 뿐이라 중복이다",
    "입력을 고르는 일을 사람에게서 기계로 넘기는 것이다 — 사람은 성질을 적고 기계는 반례를 찾는다" ] }
] }
];
