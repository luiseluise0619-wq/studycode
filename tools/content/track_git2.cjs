/* Git/GitHub 트랙 후반부 — 유닛 7~12.
   원격·GitHub 협업·리뷰·CI·실무 운영·보안.
   _p 는 임시 저장소(필요하면 bare 저장소를 원격으로 삼아)에서 실제 git 을 돌려 확인한다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex,p)=>{ const o={t:"input",k,q,a,ex}; if(p) o._p=p; return o; };
const P=(sh,want)=>({sh,want});

const UNITS=[

/* ═══════════════ 유닛 7 ═══════════════ */
{ t:"원격 저장소 — 다른 컴퓨터와 맞춘다", l:[

  { t:"fetch 와 pull 은 다르다", xp:25,
    th:{ sum:"`pull` 이 `fetch` + `merge` 라는 사실만 알아도 원격에서 나는 사고의 절반이 이해된다. **받는 것**과 **합치는 것**은 별개다.",
      body:[
        {h:"원격 추적 브랜치라는 중간 지대", t:"`git fetch` 는 원격의 새 커밋을 받아 `origin/main` 같은 **원격 추적 브랜치**에 넣는다. 내 `main` 은 손대지 않는다. 그래서 fetch 는 언제 해도 안전하다. `git pull` 은 여기에 `merge`(또는 `rebase`)를 붙여 **내 브랜치까지 움직인다**. 무슨 일이 일어날지 먼저 보고 싶으면 `fetch` 후 `git log main..origin/main` 으로 확인한 다음 합치면 된다."},
        {h:"push 가 거부되는 이유는 거의 하나다", t:"`push` 가 거부되면 대개 '**원격에 내가 모르는 커밋이 있다**'는 뜻이다. Git 은 남의 커밋을 덮어쓰지 않으려고 막는 것이다. 답은 강제 푸시가 아니라 **먼저 받아서 합치는 것**이다. `git pull --rebase` 로 내 커밋을 원격 위에 다시 얹은 뒤 푸시하면 역사도 깔끔하게 정리된다."}],
      code:{ c:"git fetch origin                 # 받기만 한다 (안전)\ngit log main..origin/main        # 뭐가 새로 왔나\ngit pull --rebase origin main    # 내 커밋을 그 위에 얹는다\ngit push origin main", cap:"받는 것과 합치는 것을 분리해서 본다" },
      key:["fetch 는 받기만 — 언제나 안전","pull = fetch + merge(또는 rebase)","origin/main 은 원격의 상태를 비추는 사본","push 거부는 '먼저 받아라' 는 뜻"] },
    q:[
      I("새로 온 커밋만","<code>fetch</code> 후 원격에만 있는 커밋을 보는 표현은? (git log 뒤 범위 표현, main 기준)",
        ["git log main..origin/main","main..origin/main"],
        "정답은 `git log main..origin/main` 입니다. 점 두 개는 '오른쪽에는 있고 왼쪽에는 없는' 커밋을 뜻합니다.\n합치기 전에 무엇이 들어올지 먼저 읽는 습관을 만들어 주는 표현입니다."),
      C("차이","<code>git fetch</code> 와 <code>git pull</code> 의 차이는?",
        ["fetch 가 똑같은 양의 커밋을 훨씬 더 빠른 속도로 받아 온다","pull 은 fetch + 합치기",
         "fetch 는 브랜치를 빼고 태그만 골라 받아 온다","이름만 다를 뿐 하는 일이 완전히 같다"],1,
        "`pull` = `fetch` + `merge`(설정에 따라 `rebase`)입니다.\n`fetch` 는 원격 추적 브랜치만 갱신하므로 내 작업에 영향이 없어 **언제 해도 안전**합니다.\n무엇이 들어올지 먼저 보고 판단하고 싶을 때 `fetch` 로 나눠서 하는 것이 좋은 습관입니다."),
      C("origin/main","<code>origin/main</code> 이 뜻하는 것은?",
        ["원격 서버에서 지금 실제로 살아 움직이고 있는 브랜치 그 자체","마지막 fetch 시점의 사본","내 main 브랜치를 복사해 둔 백업","지금 작업하고 있는 브랜치를 가리키는 이름"],1,
        "`origin/main` 은 **내 컴퓨터에 저장된 사본**입니다. 실시간 원격이 아니라 마지막으로 받아 온 시점의 상태입니다.\n그래서 원격이 바뀌어도 `fetch` 하기 전까지는 `origin/main` 이 그대로입니다.\n'분명 동료가 푸시했다는데 안 보인다' 는 대부분 fetch 를 안 한 것입니다."),
      C("push 거부","<code>push</code> 가 <code>rejected (non-fast-forward)</code> 로 거부됐다. 올바른 대응은?",
        ["--force 로 밀어붙인다","먼저 pull 해서 원격 커밋을 합친 뒤 다시 push 한다",
         "다른 이름의 브랜치를 새로 하나 만들어 그쪽으로 대신 올린다","원격 저장소를 지우고 처음부터 다시 만든다"],1,
        "거부는 '**원격에 네가 모르는 커밋이 있다**' 는 안전장치입니다. `--force` 로 밀면 동료의 커밋이 사라집니다.\n`git pull --rebase` 로 내 커밋을 원격 위에 얹은 뒤 푸시하면 역사도 한 줄로 정리됩니다.\n정말 덮어써야 하는 상황(내 PR 브랜치 rebase 후)이라면 `--force-with-lease` 를 씁니다."),
      I("원격 목록","등록된 원격 저장소와 주소를 보는 명령은? (git 포함)",
        ["git remote -v","git remote --verbose"],
        "정답은 `git remote -v` 입니다. fetch 용과 push 용 주소가 각각 나옵니다.\n관례상 첫 원격의 이름은 `origin` 이지만 강제는 아닙니다. 포크에서 작업할 때는 원본을 `upstream` 으로 추가해 두는 것이 널리 쓰이는 관례입니다.",
        P("git remote add origin https://example.com/x.git && git remote -v | head -1 | awk '{print $1}'","origin")),
      C("추적 설정","<code>git push -u origin main</code> 에서 <code>-u</code> 의 역할은?",
        ["원격 내용을 덮어쓰는 강제 푸시","이후 git push/pull 만 쳐도 되도록 추적 관계를 설정한다",
         "전송을 여러 갈래로 나눠서 업로드 속도를 크게 높인다","푸시하기 전에 사용자 인증을 대신 처리해 준다"],1,
        "`-u`(--set-upstream)는 로컬 브랜치와 원격 브랜치를 **짝지어 기억**합니다.\n한 번 설정하면 이후에는 `git push`·`git pull` 만 쳐도 어디로 갈지 Git 이 압니다.\n`git status` 가 '원격보다 2커밋 앞서 있다' 같은 정보를 보여 주는 것도 이 관계가 있어야 가능합니다."),
      C("삭제된 원격 브랜치","원격에서는 지워졌는데 내 <code>git branch -r</code> 에 계속 남아 있다면?",
        ["오래된 Git 버전에서만 생기는 것으로 알려져 있는 버그다","git fetch --prune 으로 정리한다","저장소를 통째로 지우고 처음부터 다시 클론해 받아 온다","그냥 두면 얼마 뒤 자동으로 사라진다"],1,
        "Git 은 원격 추적 브랜치를 **자동으로 지우지 않습니다**. `git fetch --prune`(짧게 `-p`)이 사라진 것을 정리합니다.\n`git config --global fetch.prune true` 로 항상 켜 두는 사람이 많습니다.\nPR 이 병합되며 브랜치가 삭제되는 팀에서는 이걸 안 하면 목록이 금세 지저분해집니다."),
      C("클론","<code>git clone</code> 이 자동으로 해 주는 일이 <b>아닌</b> 것은?",
        ["전체 역사 복사","origin 이라는 이름으로 원격 등록","기본 브랜치 체크아웃","의존성 패키지 설치"],3,
        "`clone` 은 Git 의 일만 합니다 — 역사 복사, `origin` 등록, 기본 브랜치 체크아웃, 추적 관계 설정.\n`npm install` 같은 것은 프로젝트의 몫이라 별도로 해야 합니다.\n역사가 너무 클 때는 `--depth 1`(얕은 클론)로 최근 커밋만 받을 수 있습니다 — CI 에서 흔히 씁니다.")
    ] },

  { t:"인증과 접근 — HTTPS 냐 SSH 냐", xp:25,
    th:{ sum:"'푸시할 때마다 비밀번호를 묻는다', '권한이 없다고 한다' 의 원인은 거의 전부 **인증 방식**에 있다.",
      body:[
        {h:"비밀번호는 이제 안 쓴다", t:"GitHub 는 2021년부터 HTTPS 에서 **계정 비밀번호를 받지 않는다**. 대신 **개인 액세스 토큰(PAT)** 을 비밀번호 자리에 넣거나, **SSH 키**를 등록한다. 토큰은 만료와 권한 범위를 정할 수 있어, 'CI 에서 읽기만' 같은 최소 권한을 주기에 좋다."},
        {h:"SSH 키는 한 번 등록하면 끝", t:"`ssh-keygen -t ed25519` 로 키 쌍을 만들고 **공개키만** GitHub 에 등록한다. 개인키는 절대 밖으로 내보내지 않는다. 이후에는 입력 없이 인증된다. 회사 노트북과 개인 노트북에 각각 다른 키를 두면, 한 대를 잃어버렸을 때 그 키만 지우면 된다 — 이것이 비밀번호보다 나은 실질적인 이유다."}],
      code:{ c:"ssh-keygen -t ed25519 -C \"me@example.com\"\ncat ~/.ssh/id_ed25519.pub     # 이 공개키만 GitHub 에 등록\nssh -T git@github.com          # 연결 확인", cap:"개인키(.pub 없는 쪽)는 절대 공유하지 않는다" },
      key:["GitHub HTTPS 는 비밀번호 대신 토큰","SSH 는 공개키만 등록한다","개인키는 절대 밖으로 내보내지 않는다","토큰은 권한과 만료를 좁게"] },
    q:[
      I("연결 확인","SSH 키가 GitHub 에 제대로 등록됐는지 확인하는 명령은? (ssh 부터 전부)",
        ["ssh -T git@github.com","ssh -T git@github.com "],
        "정답은 `ssh -T git@github.com` 입니다. 성공하면 사용자 이름과 함께 '셸 접근은 제공하지 않는다' 는 안내가 나옵니다.\n그 메시지가 정상 응답입니다 — 실패로 오해하기 쉬운 자리입니다."),
      C("HTTPS 인증","GitHub 에 HTTPS 로 푸시할 때 비밀번호 자리에 넣는 것은?",
        ["계정 비밀번호","개인 액세스 토큰(PAT)","GitHub 계정에 등록한 이메일 주소","앱이 만들어 준 2단계 인증 코드"],1,
        "GitHub 는 2021년 8월부터 **계정 비밀번호로는 Git 작업을 받지 않습니다**. PAT 를 발급해 비밀번호 자리에 넣습니다.\n토큰은 권한 범위(repo 읽기만 등)와 만료일을 지정할 수 있어, 유출되어도 피해 범위가 제한됩니다.\n매번 입력하지 않으려면 자격 증명 도우미(`git config --global credential.helper`)를 설정합니다."),
      C("공개키","SSH 인증에서 GitHub 에 등록하는 것은?",
        ["확장자 없는 개인키 파일(id_ed25519)","공개키(id_ed25519.pub)","개인키와 공개키 파일을 둘 다","계정 비밀번호"],1,
        "**`.pub` 이 붙은 공개키만** 등록합니다. 개인키는 어떤 경우에도 내 컴퓨터를 벗어나면 안 됩니다.\n개인키를 실수로 저장소에 커밋하면 그 즉시 폐기하고 새로 만들어야 합니다 — 파일을 지워도 역사에 남습니다.\n개인키에 암호(passphrase)를 걸어 두면 노트북을 잃어버려도 한 겹 더 방어됩니다."),
      C("최소 권한","CI 파이프라인이 저장소를 클론만 하면 될 때 토큰 권한으로 적절한 것은?",
        ["조직의 모든 저장소에 대한 관리자 권한","해당 저장소 읽기 전용","조직 전체의 소유자 권한","권한 없음"],1,
        "**필요한 만큼만** 주는 것이 원칙(최소 권한)입니다. 읽기만 하면 되는 곳에 쓰기 권한을 주면, 토큰 유출이 곧 코드 변조로 이어집니다.\nGitHub Actions 안에서는 자동 발급되는 `GITHUB_TOKEN` 을 쓰고 `permissions:` 로 범위를 좁히는 것이 더 안전합니다 — 작업이 끝나면 만료됩니다.\n사람용 PAT 를 CI 에 넣으면 그 사람이 퇴사할 때 파이프라인이 멈춥니다."),
      I("키 생성","권장되는 방식으로 SSH 키를 만드는 명령의 앞부분은? (ssh-keygen -t 까지 포함해 알고리즘 이름까지)",
        ["ssh-keygen -t ed25519"],
        "정답은 `ssh-keygen -t ed25519` 입니다. RSA 보다 짧고 빠르며 안전합니다.\n아주 오래된 서버가 ed25519 를 모를 때만 `rsa -b 4096` 을 씁니다. 요즘 GitHub·GitLab 은 모두 ed25519 를 지원합니다."),
      C("원격 주소 변경","HTTPS 로 클론한 저장소를 SSH 로 바꾸려면?",
        ["다시 클론해야 한다","git remote set-url origin git@github.com:user/repo.git",
         "git config --global protocol ssh 를 실행해 연결 기본 방식을 통째로 바꾼다","불가능하다"],1,
        "`git remote set-url` 로 주소만 바꾸면 됩니다. 역사와 브랜치는 그대로입니다.\n반대로 회사 방화벽이 SSH(22번 포트)를 막을 때는 HTTPS 로 되돌리거나, GitHub 의 443 포트 SSH(`ssh.github.com`)를 씁니다.\n`git remote -v` 로 바뀐 주소를 확인하는 것을 잊지 마세요."),
      C("자격 증명 저장","<code>credential.helper</code> 를 설정하면 무엇이 달라지나?",
        ["전송이 줄어 푸시가 빨라진다","매번 입력하지 않아도 된다","권한이 넓어진다","저장해 둔 값으로 대신하므로 2단계 인증이 꺼진다"],1,
        "인증 정보를 캐시하거나 OS 키체인에 저장해 **반복 입력을 없애 줍니다**.\n다만 `store` 방식은 **평문 파일**에 저장하므로 공용 컴퓨터에서는 위험합니다. macOS 는 `osxkeychain`, Windows 는 `manager` 를 쓰는 것이 안전합니다.\n서버나 CI 에서는 아예 파일로 두지 말고 환경 변수 시크릿을 쓰는 것이 정석입니다."),
      C("서명","커밋에 GPG/SSH 서명을 붙이는 이유는?",
        ["용량을 줄이려고","작성자를 증명하려고",
         "충돌을 막으려고","보내야 할 전송량을 줄여 푸시를 더 빠르게 끝내려고"],1,
        "커밋의 작성자 이름과 이메일은 **누구나 자유롭게 적을 수 있습니다**. `git config user.name` 만 바꾸면 남의 이름으로 커밋할 수 있습니다.\n서명은 개인키로 그 커밋을 봉인해, GitHub 에서 'Verified' 배지로 확인됩니다.\n중요한 저장소는 브랜치 보호 규칙에서 **서명된 커밋만 허용**하도록 강제할 수 있습니다.")
    ] }
]},

/* ═══════════════ 유닛 8 ═══════════════ */
{ t:"GitHub — 코드가 만나는 장소", l:[

  { t:"Pull Request 는 대화의 단위다", xp:25,
    th:{ sum:"PR 은 '코드를 합쳐 달라' 는 요청이자, **왜 이렇게 바꿨는지 설명하고 합의하는 자리**다. 코드보다 설명이 부족한 PR 이 리뷰를 어렵게 만든다.",
      body:[
        {h:"작을수록 좋은 리뷰를 받는다", t:"400줄이 넘어가면 리뷰어의 발견율이 급격히 떨어진다는 연구가 여러 번 재현되었다. 큰 PR 은 '괜찮아 보임' 이라는 형식적 승인을 받는다. 기능이 크면 **리팩터링·인터페이스·구현**처럼 단계별로 쪼개 여러 PR 로 올리는 편이 결과적으로 빠르다."},
        {h:"설명이 리뷰의 절반", t:"좋은 PR 설명에는 **무엇을 왜 바꿨는지**, **어떻게 확인했는지**, **리뷰어가 특히 봐 줬으면 하는 곳**이 들어간다. 스크린샷이나 재현 절차 한 줄이 리뷰어의 30분을 아낀다. 저장소에 `.github/pull_request_template.md` 를 두면 이 항목들이 자동으로 채워질 틀이 된다."}],
      code:{ c:"## 무엇을\n결제 타임아웃을 3초 → 10초\n\n## 왜\n피크에 게이트웨이가 5초를 넘겨 결제 실패 (#142)\n\n## 확인 방법\n부하 테스트로 p99 4.8초 확인, 재시도 0건", cap:"리뷰어가 물어볼 것을 먼저 적는다" },
      key:["PR 은 합쳐 달라는 요청 + 설명","400줄 넘으면 리뷰 품질이 급락","무엇을·왜·어떻게 확인했는지","템플릿으로 틀을 고정한다"] },
    q:[
      I("경로별 리뷰어","경로마다 리뷰어를 자동 지정하는 파일의 이름은? (대문자, 확장자 없음)",
        ["CODEOWNERS"],
        "정답은 `CODEOWNERS` 입니다. `.github/` 나 저장소 루트에 둡니다.\n`/api/ @backend-team` 처럼 적으면 그 폴더가 바뀐 PR 에 해당 팀이 자동으로 붙어, 매번 '누가 봐야 하지' 를 묻지 않아도 됩니다."),
      C("정의","Pull Request 가 하는 일은?",
        ["조건이 맞으면 리뷰 없이 코드를 자동으로 합쳐 준다","내 브랜치를 합쳐 달라고 요청하고 리뷰를 받는 자리를 만든다",
         "저장소를 복사한다","이슈를 만든다"],1,
        "PR 은 **요청과 대화의 장**입니다. 합치는 것은 승인 후 사람이(또는 자동 병합이) 합니다.\nGitHub 이 만든 개념이라 Git 자체에는 없습니다 — GitLab 은 Merge Request 라 부르며 같은 것입니다.\n브랜치 보호 규칙과 결합하면 '리뷰 없이는 main 에 못 들어간다' 를 도구로 강제할 수 있습니다."),
      C("크기","PR 을 작게 유지해야 하는 가장 큰 이유는?",
        ["저장소마다 걸린 GitHub 용량 제한 때문에","커질수록 리뷰어의 결함 발견율이 떨어지기 때문",
         "충돌이 나서","CI 가 느려져서"],1,
        "리뷰 대상이 커질수록 사람의 집중력이 급격히 떨어져 **형식적 승인**이 됩니다. 400줄 근처가 흔히 인용되는 분기점입니다.\n큰 변경은 '리팩터링만 하는 PR' → '기능 추가 PR' 처럼 나누면 각 단계의 의도가 분명해집니다.\n작은 PR 은 충돌도 적고 되돌리기도 쉬워, 결국 전체 속도가 빨라집니다."),
      C("Draft PR","Draft(초안) PR 을 쓰는 목적은?",
        ["병합을 막기 위해","방향을 미리 확인받으려고",
         "아직 완성 전이므로 CI 가 돌지 않도록 아예 건너뛰기 위해서","비공개로 만들기 위해"],1,
        "'다 만든 뒤에 방향이 틀렸다' 는 최악을 막는 장치입니다. 초안 상태로 올려 접근 방식을 먼저 확인받습니다.\nDraft 는 병합 버튼이 비활성화되고 리뷰어 자동 지정도 되지 않아, 아직 볼 때가 아님을 분명히 알립니다.\nCI 는 대개 그대로 돌아 이른 피드백을 줍니다."),
      C("이슈 연결","PR 설명에 <code>Closes #142</code> 를 쓰면?",
        ["이미 같은 내용이라 아무 변화도 일어나지 않는다","병합 시 그 이슈가 닫힌다","연결된 이슈가 함께 삭제된다","PR 이 닫힌다"],1,
        "`Closes`·`Fixes`·`Resolves` 뒤에 이슈 번호를 쓰면 **병합 시 자동으로 닫힙니다**.\n왜 이 변경이 필요했는지의 맥락이 이슈에 있으므로, 나중에 `git log` 에서 PR 번호를 따라가면 전체 사연이 복원됩니다.\n닫지 않고 참조만 하려면 그냥 `#142` 라고 쓰면 됩니다."),
      C("병합 방식","GitHub 의 <b>Squash and merge</b> 를 선택하면?",
        ["커밋이 그대로 다 들어간다","커밋들이 하나로 합쳐진다",
         "양쪽 브랜치마다 병합 커밋이 각각 하나씩 따로 새로 생긴다","브랜치가 유지된다"],1,
        "PR 안의 지저분한 커밋('오타', 'WIP')을 **하나로 눌러** `main` 에 넣습니다. `main` 의 로그가 PR 단위로 깔끔해집니다.\n대신 개별 커밋의 맥락이 사라지므로, 의미 있게 나눈 커밋을 살리고 싶은 PR 에서는 'Rebase and merge' 나 일반 병합이 낫습니다.\n팀이 어느 방식을 기본으로 할지 저장소 설정에서 고정할 수 있습니다."),
      I("템플릿 위치","PR 설명의 기본 틀을 두는 파일 경로는? (.github 로 시작)",
        [".github/pull_request_template.md",".github/PULL_REQUEST_TEMPLATE.md"],
        "정답은 `.github/pull_request_template.md` 입니다. 대소문자 두 가지 모두 인식됩니다.\n이슈 쪽은 `.github/ISSUE_TEMPLATE/` 폴더에 여러 종류(버그·기능 제안)를 둘 수 있습니다. 틀이 있으면 '재현 절차가 없어 되묻는' 일이 크게 줄어듭니다."),
      C("포크 흐름","권한이 없는 오픈소스에 기여하는 표준 절차는?",
        ["직접 푸시한다","포크 → 브랜치 → 커밋 → 포크에 푸시 → 원본에 PR",
         "이슈만 남긴다","메인테이너에게 이메일로 고쳐 둔 코드를 그대로 직접 보낸다"],1,
        "**포크**는 내 계정으로 저장소를 복제하는 것입니다. 쓰기 권한 없이도 기여할 수 있는 통로입니다.\n원본을 `upstream` 원격으로 추가해 두고 주기적으로 `git pull upstream main` 해야 뒤처지지 않습니다.\n큰 변경은 먼저 이슈로 상의하는 것이 예의이자, 헛수고를 막는 방법입니다.")
    ] },

  { t:"코드 리뷰 — 사람을 리뷰하지 않는다", xp:25,
    th:{ sum:"리뷰의 목적은 **결함을 찾는 것**과 **지식을 퍼뜨리는 것**이다. 둘 다 사람을 평가하는 일이 아니다.",
      body:[
        {h:"코드에 말하고 사람에게 말하지 않는다", t:"'왜 이렇게 짰어요?' 대신 '**이 경우 `null` 이 들어오면 어떻게 되나요?**' 라고 묻는다. 전자는 방어를 부르고 후자는 답을 부른다. 지적에는 **근거와 대안**을 함께 적고, 취향과 결함을 구분해 표시한다 — `nit:` 접두사 하나가 '이건 안 고쳐도 된다' 를 분명히 해 준다."},
        {h:"자동으로 잡을 수 있는 것은 사람이 보지 않는다", t:"들여쓰기·따옴표·import 순서는 **포매터**가, 미사용 변수·위험한 패턴은 **린터**가, 회귀는 **테스트**가 잡는다. 사람은 그 위에서 **설계·경계 조건·이름·빠진 케이스**를 본다. 자동화로 걸러지지 않는 것에 리뷰 시간을 쓰는 것이 팀 전체의 이득이다."}],
      code:{ c:"# 좋은 리뷰 코멘트\nnit: 이 변수명은 count 보다 retryCount 가 명확할 것 같아요.\n\nq: 여기서 응답이 빈 배열이면 아래 [0] 에서 터지지 않나요?\n   재현: /api/items?q=존재하지않는값", cap:"nit(취향) 과 q(질문) 를 구분해 표시한다" },
      key:["코드에 말하고 사람에게 말하지 않는다","지적에는 근거와 대안을","취향과 결함을 표시로 구분","포매터·린터가 잡는 것은 리뷰하지 않는다"] },
    q:[
      I("자기 변경 다시 보기","PR 을 올리기 전에 내 브랜치의 전체 변경을 한눈에 보는 표현은? (git diff 와 기준 브랜치)",
        ["git diff main...HEAD","git diff main..HEAD","git diff main"],
        "정답은 `git diff main...HEAD` 입니다. 점 세 개는 <b>공통 조상 기준</b>이라 GitHub 이 PR 에서 보여 주는 것과 같은 diff 가 나옵니다.\n올리기 전에 자기 diff 를 한 번 읽으면, 디버그 코드나 빠뜨린 파일이 대부분 여기서 걸립니다."),
      C("리뷰의 목적","코드 리뷰의 목적으로 가장 거리가 <b>먼</b> 것은?",
        ["합치기 전에 결함을 조기에 발견","코드에 대한 지식을 팀 전체에 퍼뜨림","설계 대안을 논의","개인 실력 평가와 순위"],3,
        "평가 도구로 쓰이는 순간 사람들은 **작은 PR 을 올리지 않고 방어적으로 변합니다**. 리뷰의 효과가 통째로 사라집니다.\n리뷰는 '이 코드를 우리 것으로 만드는' 절차이지 시험이 아닙니다.\n결함을 찾은 리뷰어가 아니라 **결함이 통과한 프로세스**를 고치는 것이 팀의 태도여야 합니다."),
      C("표현","리뷰 코멘트로 가장 좋은 것은?",
        ["이건 틀렸어요","왜 이런 방식으로 짜셨는지 배경이 되는 이유를 알려 주실 수 있을까요?","응답이 빈 배열이면 아래 [0] 에서 터지지 않을까요? 재현: /api/items?q=없는값",
         "다시 하세요"],2,
        "**구체적인 상황과 재현 방법**이 함께 있으면 상대가 바로 확인하고 고칠 수 있습니다.\n'틀렸다'·'다시 하라' 는 무엇을 어떻게 고쳐야 하는지 알려 주지 않아 왕복만 늘립니다.\n질문형으로 쓰면 리뷰어가 놓친 맥락이 있을 가능성도 자연스럽게 열어 둘 수 있습니다."),
      C("nit","리뷰에서 <code>nit:</code> 접두사가 뜻하는 것은?",
        ["반드시 고쳐야 하고 고치기 전에는 승인하지 않음","사소한 제안이니 판단에 맡긴다","보안 문제","테스트를 더 붙여야 한다는 뜻"],1,
        "'nitpick(사소한 트집)' 의 줄임말로, **안 고쳐도 승인한다**는 신호입니다.\n이 표시가 없으면 작성자는 모든 코멘트를 필수 수정으로 받아들여 왕복이 길어집니다.\n반대로 꼭 고쳐야 할 것은 명확히 'blocking' 이라고 밝히는 것이 서로에게 친절합니다."),
      C("자동화 경계","리뷰어가 사람으로서 봐야 할 것에 가장 가까운 것은?",
        ["들여쓰기 폭","따옴표 종류","import 문이 미리 정해 둔 순서대로 정렬되어 있는지","실패했을 때 복구 가능한가"],3,
        "앞의 셋은 **포매터가 0초에 끝냅니다**. 사람이 그것을 지적하면 시간과 감정만 소모됩니다.\n사람만 볼 수 있는 것은 설계, 경계 조건, 이름의 적절함, 빠진 케이스, 실패 시 복구 가능성입니다.\n그래서 포매터·린터를 CI 에 물리는 것이 곧 **리뷰 품질을 올리는 일**입니다."),
      C("승인 후 변경","승인(Approve)받은 뒤 코드를 크게 고쳤다면?",
        ["승인이 있었으므로 그대로 병합한다","다시 리뷰를 요청한다","문제가 된 커밋을 되돌린다","무시한다"],1,
        "승인은 **그 시점의 코드**에 대한 것입니다. 크게 바뀌었다면 승인의 근거가 사라집니다.\nGitHub 브랜치 보호에서 'Dismiss stale approvals' 를 켜면 새 커밋이 올라올 때 승인을 자동 무효화합니다.\n오타 수정 정도라면 코멘트로 알리고 넘어가는 것이 실무적입니다."),
      I("리뷰 요청","GitHub PR 에서 특정 사람에게 리뷰를 부탁하는 기능의 이름은? (한국어 또는 영어)",
        ["리뷰어","reviewer","review request","리뷰 요청"],
        "정답은 **리뷰어(reviewer) 지정**입니다. `CODEOWNERS` 파일을 두면 경로별로 자동 지정할 수 있습니다.\n예를 들어 `/api/ @backend-team` 이라고 적으면 그 폴더가 바뀐 PR 에 백엔드 팀이 자동으로 붙습니다 — 누가 봐야 할지 매번 고민하지 않아도 됩니다."),
      C("리뷰 지연","리뷰가 며칠씩 밀릴 때 팀 차원의 대응으로 가장 효과적인 것은?",
        ["리뷰어를 늘린다","PR 크기를 줄이고 리뷰를 하루 안에 처리하는 규칙을 만든다",
         "리뷰를 생략한다","조건이 다 맞으면 바로 합쳐지도록 자동 병합을 켜 둔다"],1,
        "리뷰 지연의 원인은 대개 **PR 이 커서 시작하기 부담스럽기 때문**입니다. 사람을 늘려도 큰 PR 은 여전히 밀립니다.\n'PR 은 400줄 이하', '리뷰는 하루 안에' 같은 팀 규칙이 실제로 순환 시간을 줄입니다.\n대기 중인 PR 이 쌓이면 브랜치가 낡아 충돌까지 늘어나므로, 지연은 복리로 비용이 커집니다.")
    ] }
]},

/* ═══════════════ 유닛 9 ═══════════════ */
{ t:"GitHub Actions — 저장소가 스스로 일한다", l:[

  { t:"CI 의 첫 워크플로", xp:30,
    th:{ sum:"CI(지속적 통합)의 핵심은 '**합치기 전에 기계가 먼저 확인한다**' 이다. 사람이 잊어도 기계는 잊지 않는다.",
      body:[
        {h:"이벤트 → 잡 → 스텝", t:"워크플로는 `.github/workflows/*.yml` 에 둔다. **on** 에 언제 돌릴지(푸시·PR·일정), **jobs** 에 무엇을 할지 적는다. 잡은 각각 **깨끗한 가상 머신**에서 시작하므로, 내 컴퓨터에서만 되는 일이 걸러진다. 스텝은 위에서 아래로 돌고, 하나가 실패하면 그 잡은 거기서 멈춘다."},
        {h:"빠르지 않으면 아무도 안 기다린다", t:"CI 가 20분이면 사람들은 결과를 안 보고 다른 일을 하다 잊는다. **의존성 캐시**, **잡 병렬화**, **변경된 부분만 테스트**로 몇 분 안에 끝내는 것이 목표다. 느린 전체 테스트는 병합 후 야간에 돌리고, PR 에서는 빠른 것만 도는 2단 구성이 흔하다."}],
      code:{ c:"name: CI\non: [push, pull_request]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 20, cache: npm }\n      - run: npm ci\n      - run: npm test", cap:"체크아웃 → 환경 → 설치 → 실행" },
      key:["파일 위치는 .github/workflows/","on 이 언제, jobs 가 무엇을","잡마다 깨끗한 머신에서 시작","느린 CI 는 아무도 안 기다린다"] },
    q:[
      I("전체 역사 받기","워크플로에서 얕은 클론 대신 전체 역사를 받게 하는 설정 값은? (키: 값 형태)",
        ["fetch-depth: 0","fetch-depth:0"],
        "정답은 `fetch-depth: 0` 입니다. `actions/checkout` 의 `with:` 아래에 적습니다.\n기본은 최근 커밋 하나만 받으므로 `git describe`·`blame`·`bisect` 가 실패합니다. 필요할 때만 켜는 것이 좋습니다 — 큰 저장소에서는 느려집니다."),
      I("파일 위치","GitHub Actions 워크플로 파일을 두는 폴더 경로는? (.github 로 시작)",
        [".github/workflows",".github/workflows/"],
        "정답은 `.github/workflows` 입니다. 이 폴더 안의 `.yml`·`.yaml` 파일이 자동으로 인식됩니다.\n파일 이름은 자유이고, 여러 개를 두면 각각 독립적으로 돕니다 — 보통 `ci.yml`·`deploy.yml` 처럼 목적별로 나눕니다."),
      C("첫 스텝","거의 모든 워크플로가 <code>actions/checkout</code> 으로 시작하는 이유는?",
        ["로그인을 위해","실행 머신에 저장소 코드가 없어서 먼저 받아와야 하므로",
         "캐시를 위해","실행 머신에 필요한 접근 권한을 앞서 설정해 두기 위해서"],1,
        "잡은 **아무것도 없는 가상 머신**에서 시작합니다. 내 코드조차 없습니다.\n`actions/checkout` 이 저장소를 클론해 와야 그 다음 스텝들이 파일을 볼 수 있습니다.\n기본은 얕은 클론(depth 1)이라, `git describe` 처럼 역사가 필요하면 `fetch-depth: 0` 을 줘야 합니다."),
      C("잡의 격리","서로 다른 job 사이에 파일이 공유되지 않는 이유는?",
        ["실행 환경에 걸린 보안 정책 때문에","잡마다 다른 머신이라서","용량 제한 때문","YAML 문법 자체가 잡 사이의 파일 공유를 허용하지 않기 때문에"],1,
        "잡은 각각 **다른 머신**에서 돕니다. 빌드 잡의 결과물을 테스트 잡에서 쓰려면 `actions/upload-artifact` 와 `download-artifact` 로 명시적으로 주고받아야 합니다.\n같은 잡 안의 스텝끼리는 같은 머신이라 파일이 그대로 남습니다.\n이 격리 덕분에 잡을 병렬로 돌려 시간을 줄일 수 있습니다."),
      C("시크릿","API 키를 워크플로에서 쓰려면?",
        ["YAML 에 직접 적는다","저장소 Secrets 에 저장하고 ${{ secrets.NAME }} 으로 참조한다",
         "값을 채워 둔 환경 변수 파일을 그대로 저장소에 커밋해 둔다","README 에 적는다"],1,
        "YAML 은 저장소에 커밋되므로 **비밀을 적으면 곧 공개**입니다. Settings → Secrets 에 저장하고 참조합니다.\n시크릿은 로그에 나타나면 자동으로 `***` 로 가려지지만, 일부러 인코딩해 출력하면 새어 나갈 수 있어 신뢰 경계를 넘는 곳에서는 조심해야 합니다.\n포크에서 온 PR 에는 기본적으로 시크릿이 전달되지 않습니다 — 악의적인 PR 이 시크릿을 빼돌리는 것을 막기 위해서입니다."),
      C("캐시","<code>actions/setup-node</code> 의 <code>cache: npm</code> 이 하는 일은?",
        ["코드를 캐시한다","설치 시간을 줄인다","지난 실행에서 나온 테스트 결과를 캐시해 둔다","빌드에 쓰는 도커 이미지를 캐시한다"],1,
        "`node_modules` 를 만드는 데 필요한 다운로드를 재사용해 **매번 인터넷에서 받는 시간**을 없앱니다.\n잠금 파일(`package-lock.json`)의 해시를 키로 쓰므로, 의존성이 바뀌면 자동으로 새로 받습니다.\nCI 시간의 상당 부분이 설치에 쓰이는 프로젝트에서는 이 한 줄이 가장 큰 개선입니다."),
      C("필수 검사","PR 이 CI 를 통과해야만 병합되게 하려면?",
        ["워크플로 파일 안에 required: true 한 줄을 적어 둔다","브랜치 보호에서 필수 지정",
         "리뷰어가 확인한다","워크플로를 만들면 자동으로 그렇게 된다"],1,
        "워크플로는 **결과를 보고할 뿐**이고, 그것을 필수로 만드는 것은 저장소의 **브랜치 보호 규칙**입니다.\n검사 이름을 지정해 두면 실패하거나 아직 안 끝난 PR 은 병합 버튼이 잠깁니다.\n이 설정이 없으면 CI 가 빨간불이어도 누구나 병합할 수 있어, 사실상 장식이 됩니다."),
      C("행렬","<code>strategy.matrix</code> 를 쓰는 목적은?",
        ["여러 개의 잡을 미리 정해진 순서대로 하나씩 실행하려고","여러 버전·OS 조합에서 같은 잡을 병렬로 돌리려고",
         "재시도하려고","캐시를 나누려고"],1,
        "Node 18·20·22 × Ubuntu·macOS 처럼 **조합을 자동으로 펼쳐** 병렬 실행합니다.\n라이브러리처럼 여러 환경을 지원해야 하는 프로젝트에서 특히 유용합니다.\n조합이 늘면 실행 시간(과 비용)이 곱으로 늘어나므로, 대표 조합만 PR 에서 돌리고 전체는 야간에 돌리는 구성이 흔합니다.")
    ] },

  { t:"자동화의 경계 — 무엇을 기계에 맡길까", xp:30,
    th:{ sum:"자동화는 **반복되고 판단이 필요 없는 것**부터다. 판단이 필요한 곳에 자동화를 넣으면 사람이 기계를 속이기 시작한다.",
      body:[
        {h:"맡기기 좋은 것", t:"포맷 검사, 린트, 테스트, 빌드, 취약점 스캔, 라벨 붙이기, 릴리스 노트 생성, 의존성 업데이트 PR. 전부 **정답이 하나로 정해지는 일**이다. 이런 것을 사람이 하면 실수하고, 지적하면 감정이 상한다. 기계가 하면 아무도 기분 나쁘지 않다."},
        {h:"맡기면 안 되는 것", t:"'커버리지 80% 미만이면 병합 차단' 같은 **숫자 기준**은 종종 역효과를 낸다. 사람들이 의미 없는 테스트를 써서 숫자를 채운다. 측정값은 **보여 주되 차단하지 않는** 편이 대개 낫다. 자동 병합도 마찬가지로, 테스트가 못 잡는 종류의 실수를 그대로 통과시킨다."}],
      code:{ c:"# 좋은 자동화: 판단이 필요 없다\n- run: npm run lint\n- run: npm test\n- uses: actions/dependency-review-action@v4\n\n# 위험한 자동화: 숫자로 사람을 압박한다\n- run: coverage --fail-under=80", cap:"측정은 하되 함부로 차단하지 않는다" },
      key:["정답이 하나인 일부터 자동화","자동화는 감정 소모를 없앤다","숫자 기준은 우회를 부른다","측정값은 보여 주되 차단은 신중히"] },
    q:[
      I("수동 실행","워크플로를 버튼으로 직접 돌릴 수 있게 하는 이벤트 이름은? (on 아래 키워드)",
        ["workflow_dispatch"],
        "정답은 `workflow_dispatch` 입니다. Actions 화면에 실행 버튼이 생깁니다.\n배포처럼 '자동으로 돌면 곤란하지만 손으로는 돌리고 싶은' 작업에 씁니다. 입력값을 받도록 정의할 수도 있습니다."),
      C("우선순위","가장 먼저 자동화하기 좋은 것은?",
        ["코드 설계 검토","코드 포맷 검사","무엇을 만들지 기능 요구사항 확정","리뷰어 배정 판단"],1,
        "포맷은 **정답이 하나로 정해지고 반복**됩니다. 자동화의 교과서적인 대상입니다.\n사람이 포맷을 지적하면 시간과 감정이 소모되지만, 포매터가 하면 아무 갈등이 없습니다.\n설계 검토처럼 판단이 필요한 일은 자동화 대상이 아니라 **사람이 집중할 자리**입니다."),
      C("커버리지 기준","'커버리지 80% 미만이면 병합 차단' 이 역효과를 내는 이유는?",
        ["CI 가 느려져서","의미 없는 테스트를 쓰게 되어서",
         "프로젝트에서 커버리지 측정 자체가 불가능해서","GitHub Actions 가 아직 그 기능을 지원하지 않고 있어서"],1,
        "측정값이 목표가 되면 **측정값만 좋아지고 실제 품질은 그대로**입니다(굿하트의 법칙).\n어서션 없는 테스트로도 커버리지는 올라갑니다 — 코드가 실행되기만 하면 되기 때문입니다.\n'새로 추가된 코드의 커버리지를 PR 에 표시' 처럼 **보여 주는 방식**이 대개 더 건강합니다."),
      C("의존성 자동 업데이트","Dependabot 같은 도구가 PR 을 올려 주는 것의 가장 큰 이점은?",
        ["쓰지 않는 불필요한 코드가 함께 정리되면서 실행이 더 빨라진다","패치를 작게 자주 따라간다",
         "테스트가 필요 없어진다","중복된 의존성이 사라져 저장 용량이 크게 준다"],1,
        "취약점 패치는 **미룰수록 한꺼번에 올려야 해서 위험**해집니다. 작게 자주 올리면 문제가 생겨도 원인이 분명합니다.\n자동 PR 은 CI 가 통과하는지까지 보여 주므로, 안전한 것부터 빠르게 병합할 수 있습니다.\n다만 PR 이 쏟아지면 무시하게 되므로, 묶음 주기와 대상 범위를 조절하는 것이 실무의 요령입니다."),
      C("포크 PR 보안","외부 포크에서 온 PR 에 시크릿을 주지 않는 이유는?",
        ["용량 절약","시크릿을 빼돌릴 수 있어서",
         "실행 속도가 눈에 띄게 빨라져서","GitHub 정책상 아예 허용되지 않는 종류의 동작으로 막혀 있어서"],1,
        "PR 은 **워크플로 파일 자체도 바꿀 수 있습니다**. 시크릿이 그대로 전달되면 '시크릿을 출력하는 스텝' 을 넣어 훔쳐 갈 수 있습니다.\n그래서 `pull_request` 이벤트는 포크에서 올 때 시크릿 없이, 읽기 권한으로만 돕니다.\n배포처럼 시크릿이 필요한 작업은 **병합 후** 또는 승인이 붙은 별도 이벤트에서 돌려야 합니다."),
      C("자동 병합","자동 병합(auto-merge)을 켤 때 반드시 함께 있어야 하는 것은?",
        ["한 번도 끊기지 않는 아주 빠른 인터넷 연결","믿을 수 있는 자동 테스트와 필수 리뷰 규칙","넉넉한 용량","여러 리뷰어"],1,
        "자동 병합은 '조건이 갖춰지면 사람 없이 합친다' 는 뜻이므로, **그 조건이 곧 품질의 전부**가 됩니다.\n테스트가 부실하면 자동 병합은 결함을 더 빨리 배포하는 장치가 됩니다.\n반대로 테스트가 튼튼한 팀에서는 리뷰 승인 직후 대기 없이 합쳐져 순환 시간을 크게 줄여 줍니다."),
      I("일정 실행","워크플로를 정해진 시각에 주기적으로 돌릴 때 <code>on:</code> 아래에 쓰는 키워드는?",
        ["schedule"],
        "정답은 `schedule` 이고, 그 아래 `cron:` 으로 시각을 적습니다. 시각은 **UTC 기준**입니다.\n야간 전체 테스트, 링크 점검, 만료 예정 인증서 확인 같은 일에 씁니다. 다만 실행 시각이 정확히 보장되지는 않아 몇 분 밀릴 수 있습니다."),
      C("실패 알림","CI 가 실패했는데 아무도 모르는 상황을 막는 가장 실용적인 방법은?",
        ["매일 수동으로 확인한다","실패 시 팀 채널로 알림을 보내고 main 실패를 최우선 과제로 삼는다",
         "실패한 잡을 정해진 횟수만큼 자동으로 다시 돌려 재시도한다","알림을 끈다"],1,
        "**깨진 `main` 을 방치하는 것**이 가장 비싼 상태입니다. 다음 사람들이 그 실패를 자기 것이라 착각하며 시간을 낭비합니다.\n알림을 채널로 보내고 '`main` 이 빨간불이면 다른 일보다 먼저 고친다' 는 규칙을 두는 팀이 가장 건강합니다.\n무턱대고 재시도하면 플레이키(간헐 실패) 테스트를 방치하게 되어 신뢰가 더 무너집니다.")
    ] }
]},

/* ═══════════════ 유닛 10 ═══════════════ */
{ t:"실무 운영 — 태그 · 릴리스 · 큰 저장소", l:[

  { t:"버전과 태그", xp:30,
    th:{ sum:"태그는 **움직이지 않는 이름표**다. 브랜치가 흐르는 강이라면 태그는 강가에 박은 말뚝이다.",
      body:[
        {h:"경량 태그와 주석 태그", t:"`git tag v1.0` 은 **경량 태그** — 그냥 이름표다. `git tag -a v1.0 -m \"…\"` 는 **주석 태그**로, 만든 사람·시각·메시지를 담은 **객체**가 생기고 서명도 붙일 수 있다. 릴리스에는 주석 태그를 쓰는 것이 관례다. `git describe` 가 기본적으로 주석 태그만 보는 것도 이 때문이다."},
        {h:"태그는 자동으로 푸시되지 않는다", t:"`git push` 는 브랜치만 보낸다. 태그는 `git push origin v1.0` 또는 `--tags` 로 따로 보내야 한다. 이걸 몰라 '태그를 달았는데 배포가 안 된다' 는 일이 흔하다. 반대로 이미 푸시한 태그를 옮기는 것은 **하지 않는 것이 원칙**이다 — 이미 받아 간 사람의 태그는 갱신되지 않아 같은 이름이 서로 다른 커밋을 가리키게 된다."}],
      code:{ c:"git tag -a v1.2.0 -m \"결제 재시도 추가\"\ngit push origin v1.2.0        # 태그는 따로 보낸다\ngit describe --tags           # v1.2.0-3-gabc1234 (태그 이후 3커밋)", cap:"태그 이후 몇 커밋인지까지 이름에 담긴다" },
      key:["릴리스에는 주석 태그(-a)","태그는 따로 푸시해야 한다","푸시된 태그는 옮기지 않는다","describe 로 빌드 버전을 만든다"] },
    q:[
      I("태그 전부 보내기","만든 태그를 한 번에 모두 원격으로 보내는 옵션은? (git push 뒤 옵션만)",
        ["--tags","-tags"],
        "정답은 `--tags` 입니다. 일반 `git push` 는 태그를 보내지 않습니다.\n다만 로컬에 실험용 태그가 남아 있으면 그것까지 함께 나가므로, 하나만 보낼 때는 `git push origin v1.2.0` 이 더 안전합니다."),
      C("브랜치와의 차이","태그가 브랜치와 다른 점은?",
        ["용량이 작다","커밋이 쌓여도 움직이지 않는다","내 컴퓨터에는 없고 원격 저장소에만 존재한다","한 번 만들면 삭제할 수 없다"],1,
        "브랜치는 커밋할 때마다 **앞으로 나아가지만** 태그는 그 자리에 고정됩니다.\n그래서 'v1.2.0 이 정확히 어떤 코드였는가' 를 영구히 가리킬 수 있습니다.\n장애가 났을 때 '지난 릴리스로 되돌리기' 가 가능한 것도 태그가 움직이지 않기 때문입니다."),
      C("주석 태그","릴리스에 <code>-a</code> 를 붙인 주석 태그를 권장하는 이유는?",
        ["태그를 다는 같은 일을 더 빠르게 끝낸다","객체로 남고 서명할 수 있다",
         "용량이 작다","커밋이 쌓이면 브랜치처럼 앞으로 자동으로 따라 움직인다"],1,
        "주석 태그는 **독립된 객체**로 저장되어 누가 언제 왜 이 릴리스를 찍었는지가 남습니다.\n경량 태그는 그냥 해시를 가리키는 이름일 뿐이라 그 정보가 없습니다.\nGPG 서명을 붙이면 '이 릴리스는 진짜 우리가 만든 것' 을 증명할 수 있어, 공급망 보안에서 중요해집니다."),
      C("푸시","<code>git push</code> 만 했는데 태그가 원격에 없는 이유는?",
        ["권한 부족","push 는 태그를 안 보낸다","태그 객체가 손상되는 바람에 전송되지 못했다","원격에 반영되기까지 시간이 걸린다"],1,
        "`git push` 는 **브랜치만** 보냅니다. 태그는 `git push origin 태그이름` 이나 `--tags` 로 명시해야 합니다.\n의도치 않은 태그가 함께 나가는 것을 막으려는 설계입니다.\n릴리스 파이프라인이 태그를 신호로 삼는 경우, 이걸 빠뜨리면 배포가 시작되지 않아 한참 헤매게 됩니다.",
        P("echo a>f && git add f && git commit -qm c && git tag -a v1 -m r && git tag -l | head -1","v1")),
      C("시맨틱 버전","<code>1.4.2</code> 에서 <b>가운데 숫자</b>가 올라가는 경우는?",
        ["버그만 고쳤을 때","호환되는 기능이 추가됐을 때","호환이 깨지는 변경이 있을 때","코드 없이 문서만 바뀌었을 때"],1,
        "시맨틱 버전은 `MAJOR.MINOR.PATCH` 입니다. 가운데(MINOR)는 **하위 호환을 지키면서 기능이 추가**됐을 때 올립니다.\n호환이 깨지면 MAJOR, 버그 수정만이면 PATCH 입니다.\n이 약속이 있어야 의존하는 쪽이 '`^1.4.0` 이면 안전하게 올려도 된다' 고 판단할 수 있습니다."),
      I("버전 문자열","가장 가까운 태그를 기준으로 현재 커밋의 버전 문자열을 만드는 명령은? (git 포함)",
        ["git describe","git describe --tags"],
        "정답은 `git describe` 입니다. `v1.2.0-3-gabc1234` 처럼 **태그 이후 3커밋, 해시 abc1234** 라는 정보를 담아 줍니다.\n빌드 산출물에 이 문자열을 박아 두면 '지금 서버에 뜬 게 어느 커밋인지' 를 즉시 알 수 있어, 장애 대응에서 큰 도움이 됩니다."),
      C("잘못 단 태그","이미 푸시한 태그를 다른 커밋으로 옮기면 생기는 문제는?",
        ["아무 문제 없다","이미 받아 간 사람의 태그는 갱신되지 않아 같은 이름이 서로 다른 커밋을 가리킨다",
         "가리키는 객체가 어긋나면서 저장소 전체가 못 쓰게 손상된다","옮긴 순간 원래 태그가 사라진다"],1,
        "Git 은 이미 있는 태그를 **자동으로 덮어쓰지 않습니다**. 결과적으로 사람마다 `v1.2.0` 이 다른 코드를 가리키게 됩니다.\n재현 불가능한 버그 보고의 전형적인 원인입니다.\n잘못 찍었다면 옮기지 말고 **새 버전을 발행**하는 것이 원칙입니다 — 버전 숫자는 싸고 혼란은 비쌉니다."),
      C("릴리스 노트","릴리스 노트를 자동 생성하기 쉽게 만드는 습관은?",
        ["커밋을 큰 단위로 묶어 적게 한다","커밋 메시지 규약(feat/fix 등)을 지키고 PR 제목을 잘 쓴다",
         "릴리스할 때마다 버전 태그를 빠짐없이 꼬박꼬박 달아 둔다","작업마다 브랜치를 넉넉히 많이 만든다"],1,
        "`feat:`·`fix:` 같은 **관례적 커밋(Conventional Commits)** 규약을 지키면 도구가 변경 사항을 분류해 노트를 만들어 줍니다.\nGitHub 의 자동 릴리스 노트는 PR 제목과 라벨을 씁니다 — 그래서 PR 제목이 곧 사용자에게 보이는 문장이 됩니다.\n사람이 손으로 정리하는 노트는 결국 밀리고, 밀리면 아무도 안 읽습니다.")
    ] },

  { t:"저장소가 무거워질 때", xp:30,
    th:{ sum:"Git 은 텍스트에 최적화되어 있다. 큰 바이너리·긴 역사·거대한 모노레포는 각각 다른 처방이 필요하다.",
      body:[
        {h:"큰 파일은 애초에 넣지 않는다", t:"바이너리는 조금만 바뀌어도 **통째로 새 객체**가 되어 저장소가 급격히 부푼다. 게다가 한 번 커밋하면 지워도 역사에 남아 클론 시간이 영구히 늘어난다. 디자인 원본·데이터셋·빌드 산출물은 **오브젝트 스토리지에 두고 주소만** 커밋하거나 Git LFS 를 쓴다."},
        {h:"얕게 받고 부분만 받는다", t:"CI 처럼 역사가 필요 없는 곳은 `--depth 1`(얕은 클론)이면 충분하다. 모노레포에서 내가 쓰는 폴더만 받고 싶으면 **sparse-checkout** 을, 큰 파일을 필요할 때만 받으려면 **partial clone**(`--filter=blob:none`)을 쓴다. 셋 다 저장소를 바꾸지 않고 **받는 쪽에서** 정하는 방법이라 도입이 쉽다."}],
      code:{ c:"git clone --depth 1 URL              # 최근 커밋만\ngit clone --filter=blob:none URL     # 파일 내용은 필요할 때\ngit sparse-checkout set apps/web     # 이 폴더만 펼친다", cap:"받는 쪽에서 범위를 정한다" },
      key:["큰 바이너리는 역사에 영구히 남는다","LFS 또는 외부 스토리지 + 주소","CI 는 --depth 1 로 충분","sparse-checkout 으로 폴더만"] },
    q:[
      I("저장소 크기","저장소가 실제로 얼마나 큰지 사람이 읽기 좋은 단위로 보는 명령은? (git 과 옵션까지)",
        ["git count-objects -vH","git count-objects -v -H"],
        "정답은 `git count-objects -vH` 입니다. 느슨한 객체와 팩 파일의 크기를 각각 보여 줍니다.\n`size-pack` 이 유난히 크면 대용량 파일이 역사에 들어 있다는 신호이고, 그때는 어떤 객체가 범인인지 따로 찾아야 합니다."),
      C("바이너리 문제","큰 이미지 파일을 자주 커밋하면 생기는 문제는?",
        ["같은 파일을 건드릴 때 충돌이 늘어난다","조금만 바뀌어도 통째로 새로 저장되어 저장소가 급격히 커진다",
         "브랜치를 옮길 때마다 눈에 띄게 느려지는 문제까지 생긴다","해당 커밋에 태그를 달 수 없게 된다"],1,
        "Git 은 텍스트에는 델타 압축이 잘 듣지만, 압축된 바이너리(PNG·MP4)에는 거의 듣지 않습니다.\n버전마다 사실상 **전체 사본**이 쌓이고, 지워도 역사에 남아 클론 시간이 영구히 늘어납니다.\n그래서 '나중에 정리하지' 가 통하지 않는 대표적인 영역입니다."),
      C("LFS","Git LFS 가 저장소에 실제로 커밋하는 것은?",
        ["커밋 시점의 파일 내용 전체","짧은 포인터 텍스트","원본 파일을 압축해 크기를 줄여 둔 사본","직전 버전과 비교해서 만든 diff 조각"],1,
        "LFS 는 실제 내용을 별도 서버에 두고, 저장소에는 **해시가 적힌 몇 줄짜리 포인터**만 커밋합니다.\n덕분에 역사가 가벼워지고, 체크아웃할 때 필요한 버전만 내려받습니다.\n대신 LFS 서버가 필요하고 대역폭 비용이 붙으므로, 도입 전에 그 운영을 누가 할지 정해야 합니다."),
      C("얕은 클론","CI 에서 <code>--depth 1</code> 을 쓰는 이유는?",
        ["오래된 커밋에 그대로 남아 있는 보안 정보를 감추기 위해서","클론 시간을 줄이려고",
         "전체 역사를 받을 권한이 없어서","저장소에 브랜치가 하나뿐이어서"],1,
        "CI 는 대개 **현재 코드만** 있으면 됩니다. 10년치 역사를 받을 이유가 없습니다.\n큰 저장소에서는 이 옵션 하나가 파이프라인 시작을 몇 분 앞당깁니다.\n단, `git describe`·`git blame`·`bisect` 처럼 역사가 필요한 작업은 실패하므로, 그때는 `fetch-depth: 0` 으로 되돌립니다."),
      C("역사에서 삭제","이미 커밋된 대용량 파일을 역사에서 완전히 지우려면?",
        ["git rm 으로 지우고 새로 커밋","git filter-repo 등으로 역사를 다시 쓰고 전원이 다시 클론",
         "gitignore 에 그 파일을 적어 넣은 뒤 지우고 다시 커밋한다","그 시점에 태그를 달아 표시한다"],1,
        "`git rm` 은 **앞으로만** 없앨 뿐 과거 커밋에는 그대로 남습니다.\n완전히 지우려면 역사를 다시 써야 하는데, 모든 커밋 해시가 바뀌므로 **전원이 다시 클론**해야 합니다. 열려 있던 PR 도 깨집니다.\n그래서 이것은 최후의 수단이고, 애초에 넣지 않는 것이 유일하게 값싼 방법입니다."),
      C("서브모듈","<code>git submodule</code> 이 상위 저장소에 기록하는 것은?",
        ["하위 저장소의 파일과 코드 전체","하위 저장소의 특정 커밋 해시","하위 저장소가 따라갈 브랜치의 이름","폴더 경로만 두고 그 밖에는 아무것도 기록하지 않는다"],1,
        "서브모듈은 '이 폴더는 저 저장소의 **이 커밋**' 이라는 고정된 참조입니다. 코드 자체는 들어오지 않습니다.\n그래서 클론 후 `git submodule update --init` 을 해야 내용이 채워집니다 — 이걸 잊어 빈 폴더를 보고 당황하는 일이 아주 흔합니다.\n버전이 고정되는 장점이 있지만 다루기가 까다로워, 요즘은 패키지 매니저나 모노레포를 택하는 경우가 많습니다."),
      I("일부만 받기","모노레포에서 원하는 폴더만 펼쳐 쓰는 기능의 이름은? (git 하위 명령)",
        ["sparse-checkout","git sparse-checkout"],
        "정답은 `git sparse-checkout` 입니다. 저장소 전체를 받되 **작업 폴더에는 지정한 경로만** 펼칩니다.\n`--filter=blob:none` 과 함께 쓰면 필요 없는 파일의 내용은 아예 받지 않아, 거대한 모노레포에서도 가볍게 일할 수 있습니다."),
      C("성능 점검","저장소가 왜 무거운지 확인하는 첫 단계로 적절한 것은?",
        ["브랜치가 몇 개나 남아 있는지 센다","git count-objects -vH 로 크기를 확인하고 큰 객체를 찾는다",
         "전체 커밋이 몇 개인지 세어 본다","처음부터 다시 클론한다"],1,
        "`git count-objects -vH` 는 느슨한 객체와 팩의 실제 크기를 알려 줍니다.\n어떤 파일이 범인인지는 `git rev-list --objects --all` 결과를 크기순으로 정렬해 찾습니다.\n원인을 모른 채 `gc` 만 반복하면, 진짜 원인인 대용량 파일이 그대로 남아 다시 부풀어 오릅니다.")
    ] }
]},

/* ═══════════════ 유닛 11 ═══════════════ */
{ t:"협업의 규율 — 사람이 여럿일 때", l:[

  { t:"충돌을 줄이는 습관", xp:30,
    th:{ sum:"충돌 해결법을 배우는 것보다 **충돌이 덜 나게 일하는 법**이 훨씬 값어치 있다.",
      body:[
        {h:"거리와 시간이 충돌을 만든다", t:"충돌은 '같은 곳을 다르게 고쳤다' 는 사건이다. **브랜치를 짧게 유지**하고 **자주 `main` 을 끌어오면** 서로가 멀어질 시간이 줄어든다. 큰 리팩터링은 남들과 시간을 맞춰 **먼저 합치고 시작**하는 것이 정석이다 — 조용히 3주간 이름을 바꾸면 팀 전체가 그 대가를 치른다."},
        {h:"자동 포맷은 충돌 예방책이기도 하다", t:"들여쓰기·따옴표가 사람마다 다르면 **의미 없는 줄 차이**가 잔뜩 생겨 충돌을 만든다. 포매터를 저장소에 고정하고 커밋 훅으로 강제하면 이 잡음이 통째로 사라진다. 대신 포맷을 처음 도입한 커밋은 파일 전체를 건드리므로, **그 커밋만 blame 에서 제외**하도록 `.git-blame-ignore-revs` 에 적어 둔다."}],
      code:{ c:"# .git-blame-ignore-revs\n# 전체 포맷 적용 커밋 — blame 에서 무시\n8a3f2c1d9e0b4a5c6d7e8f9012345678abcdef01\n\ngit config blame.ignoreRevsFile .git-blame-ignore-revs", cap:"대량 포맷 커밋이 blame 을 가리지 않게" },
      key:["브랜치를 짧게, main 을 자주 끌어온다","큰 리팩터링은 먼저 합치고 시작","포매터 고정이 충돌을 줄인다","대량 포맷 커밋은 blame 에서 제외"] },
    q:[
      I("훅 경로 바꾸기","훅 스크립트를 저장소 안 폴더에서 찾게 하는 설정 항목은? (git config 뒤 이름만)",
        ["core.hooksPath","hooksPath"],
        "정답은 `core.hooksPath` 입니다. `.git/hooks` 는 커밋되지 않아 팀에 배포되지 않기 때문입니다.\n저장소 안 `.githooks/` 를 가리키게 하면 훅도 버전 관리 대상이 되어, 새로 합류한 사람도 같은 검사를 받게 됩니다."),
      C("예방","병합 충돌을 줄이는 가장 효과적인 습관은?",
        ["충돌을 빨리 푸는 병합 도구의 사용법을 익혀 둔다","브랜치를 짧게 유지하고 main 을 자주 끌어온다",
         "파일을 최대한 잘게 나눠 둔다","한 사람만 커밋하도록 순번을 정한다"],1,
        "충돌은 **떨어져 있던 시간**에 비례합니다. 자주 합칠수록 각 병합이 작아집니다.\n도구는 이미 난 충돌을 다루는 것이고, 습관은 충돌 자체를 줄입니다.\n하루에 한 번 `git pull --rebase origin main` 만 해도 체감이 크게 달라집니다."),
      C("대규모 리팩터링","파일 이름을 대량으로 바꾸는 작업을 할 때 옳은 순서는?",
        ["다른 사람 모르게 혼자 3주간 작업한 뒤 한 번에 큰 PR 을 올린다","알리고 짧게 끝내 먼저 합친다",
         "여러 사람이 브랜치를 나눠 맡아 같은 이름을 동시에 바꾼다","금요일 저녁에 병합"],1,
        "이름 변경은 **거의 모든 파일**을 건드려 다른 사람의 모든 브랜치와 충돌합니다.\n짧게 끝내고 먼저 합치면 다른 사람들은 한 번만 `main` 을 끌어오면 됩니다. 오래 끌면 각자 매번 충돌을 겪습니다.\n금요일 저녁 병합은 문제가 났을 때 대응할 사람이 없어 피하는 것이 관례입니다."),
      C("포맷 잡음","포매터를 저장소에 고정하면 병합에 어떤 이점이 있나?",
        ["비교할 줄이 줄어 병합이 빨라진다","사람마다 다른 포맷으로 생기는 의미 없는 줄 차이가 사라져 충돌이 준다",
         "서로 같은 줄을 고치더라도 충돌이 아예 나지 않게 되어서","관리할 브랜치 개수가 줄어든다"],1,
        "포맷 차이는 **실질적 변경이 없는데도 줄이 다르게** 보이게 만들어 충돌과 리뷰 잡음을 늘립니다.\n포매터를 고정하고 CI 에서 검사하면 이 범주의 문제가 통째로 사라집니다.\n의미 있는 변경만 diff 에 남으므로 리뷰의 질도 함께 올라갑니다."),
      C("blame 오염","전체 포맷 적용 커밋 때문에 <code>git blame</code> 이 쓸모없어지는 것을 막으려면?",
        ["blame 을 쓰지 않고 커밋 목록을 하나씩 훑어 작성자를 직접 찾는다",".git-blame-ignore-revs 에 적는다",
         "전체 포맷 적용을 아예 하지 않는다","그 포맷 커밋을 역사에서 지운다"],1,
        "`.git-blame-ignore-revs` 에 해시를 적고 `git config blame.ignoreRevsFile` 로 지정하면 blame 이 그 커밋을 **건너뛰고 그 이전 작성자**를 보여 줍니다.\nGitHub 도 이 파일을 자동으로 인식합니다.\n대량 변경 커밋에는 **다른 변경을 섞지 않는 것**이 전제입니다 — 섞여 있으면 통째로 무시할 수 없습니다."),
      C("커밋 훅","<code>pre-commit</code> 훅으로 하기에 적절한 것은?",
        ["전체 통합 테스트 실행","빠른 포맷·린트 검사","배포","프로젝트 의존성 패키지 설치"],1,
        "커밋 훅은 **매번, 사람이 기다리는 동안** 돕니다. 몇 초를 넘으면 사람들이 `--no-verify` 로 우회하기 시작합니다.\n무거운 검사는 CI 에 맡기고, 훅에서는 즉시 끝나는 것만 합니다.\n훅은 저장소에 자동 배포되지 않으므로, `husky`·`pre-commit` 같은 도구로 설치를 자동화하는 것이 보통입니다."),
      C("공유 브랜치","여러 명이 함께 쓰는 브랜치에서 <b>하면 안 되는</b> 일은?",
        ["자주 커밋하기","작업한 것을 자주 푸시하기","rebase 후 강제 푸시","지금 브랜치에서 새 브랜치 만들기"],2,
        "공유 브랜치를 rebase 하면 해시가 바뀌어 **다른 사람의 로컬과 어긋납니다**. 다음 pull 에서 중복 커밋과 이상한 충돌이 생깁니다.\n황금률은 '내 것은 마음껏, 공유된 것은 절대' 입니다.\n꼭 필요하면 팀에 미리 알리고 모두가 동시에 `git reset --hard origin/브랜치` 하도록 조율해야 합니다."),
      I("훅 폴더","Git 훅 스크립트가 들어가는 기본 폴더 경로는? (.git 으로 시작)",
        [".git/hooks",".git/hooks/"],
        "정답은 `.git/hooks` 입니다. 이 폴더는 **저장소에 커밋되지 않으므로** 팀원에게 자동으로 배포되지 않습니다.\n그래서 `core.hooksPath` 를 저장소 안의 폴더로 바꾸거나 husky 같은 도구를 쓰는 것이 표준적인 해법입니다.")
    ] },

  { t:"오픈소스와 라이선스", xp:30,
    th:{ sum:"공개 저장소는 코드만 공개하는 것이 아니다. **어떻게 쓸 수 있는지**를 함께 정해야 남이 쓸 수 있다.",
      body:[
        {h:"라이선스가 없으면 쓸 수 없다", t:"많은 사람이 'GitHub 에 공개했으니 자유롭게 쓰라는 뜻' 이라고 오해한다. 법적으로는 반대다 — **라이선스가 없으면 기본적으로 저작권이 전부 유보**되어 복제·수정·배포가 허용되지 않는다. `LICENSE` 파일 하나가 프로젝트가 쓰일 수 있는지를 정한다."},
        {h:"허용형과 카피레프트", t:"**MIT·Apache-2.0** 은 저작권 표시만 남기면 상업적 사용도 자유롭다(허용형). **GPL** 계열은 그것을 포함한 결과물도 같은 라이선스로 공개해야 한다(카피레프트). Apache-2.0 은 여기에 **특허 사용권**을 명시해 기업이 선호한다. 회사에서 의존성을 추가할 때 라이선스를 먼저 보는 이유가 이것이다."}],
      code:{ c:"repo/\n  LICENSE          # 이게 없으면 아무도 못 쓴다\n  README.md        # 무엇인지, 어떻게 쓰는지\n  CONTRIBUTING.md  # 어떻게 기여하는지\n  CODE_OF_CONDUCT.md", cap:"공개 저장소의 최소 구성" },
      key:["라이선스가 없으면 권리는 유보된다","MIT·Apache 는 허용형","GPL 은 같은 라이선스로 공개 요구","Apache-2.0 은 특허 조항을 명시"] },
    q:[
      I("서명 검증","커밋에 붙은 서명을 함께 확인하며 로그를 보는 옵션은? (git log 뒤 옵션만)",
        ["--show-signature","-show-signature"],
        "정답은 `--show-signature` 입니다. 서명이 유효한지, 누구의 키인지 함께 보여 줍니다.\n작성자 이름과 이메일은 누구나 자유롭게 적을 수 있으므로, 진짜 확인은 서명으로만 가능합니다."),
      C("무라이선스","LICENSE 파일이 없는 공개 저장소의 코드를 쓰려면?",
        ["공개되어 있는 코드이므로 누구나 자유롭게 가져다 쓸 수 있다","원칙적으로 쓸 수 없다 — 권리가 유보된 상태다",
         "출처와 원저자 이름만 밝히면 쓸 수 있다","돈을 받지 않는 비상업적 용도에 한해서만 쓸 수 있다"],1,
        "공개는 **볼 수 있게** 한 것이지 **쓸 수 있게** 한 것이 아닙니다. 기본값은 저작권 전부 유보입니다.\n실무에서는 저자에게 라이선스를 물어보거나, 답이 없으면 쓰지 않는 것이 안전합니다.\n내 프로젝트를 남이 쓰길 바란다면 `LICENSE` 파일 하나를 추가하는 것이 가장 중요한 한 걸음입니다."),
      C("MIT","MIT 라이선스의 핵심 조건은?",
        ["포함한 결과물의 소스를 공개해야 한다","저작권 표시와 라이선스 전문을 포함하면 된다",
         "제품을 팔아 돈을 버는 상업적 사용을 금지한다","파생물도 반드시 같은 라이선스로만 배포해야 한다"],1,
        "MIT 는 **저작권 고지만 유지**하면 상업적 사용·수정·비공개 배포까지 허용하는 대표적 허용형 라이선스입니다.\n짧고 명확해서 널리 쓰이지만, 특허에 대한 언급이 없다는 점이 Apache-2.0 과의 차이입니다.\n보증도 없음을 명시하므로, 쓰다 생긴 문제의 책임은 사용자에게 있습니다."),
      C("GPL","GPL 계열 라이브러리를 제품에 포함할 때 주의할 점은?",
        ["포함하든 고치든 아무 제약이 없다","조건에 따라 결과물도 같은 라이선스로 소스를 공개해야 할 수 있다",
         "어떤 값도 받지 않고 언제나 무료로만 배포하도록 강하게 요구한다","제품 이름을 원본과 다르게 바꿔야 한다"],1,
        "GPL 은 **카피레프트**입니다. 파생물도 같은 자유를 유지하도록 요구합니다.\n'무료' 가 아니라 '**자유**' 에 관한 조건이라, 상업적 판매 자체는 금지되지 않습니다.\n네트워크로 제공하는 서비스까지 포함하는 AGPL 은 특히 SaaS 기업이 조심해서 검토합니다."),
      C("CONTRIBUTING","<code>CONTRIBUTING.md</code> 가 있으면 좋은 이유는?",
        ["저장소가 검색 결과에서 더 높은 순위에 오르게 된다","절차를 미리 알려 왕복을 줄인다",
         "LICENSE 파일이 따로 없어도 그 역할을 대신 해 주기 때문에","자동화 설정이 붙어 CI 가 빨라진다"],1,
        "'브랜치 이름 규칙', '테스트 실행법', '커밋 메시지 형식' 을 미리 적어 두면 기여자가 처음부터 맞춰 옵니다.\n없으면 메인테이너가 매번 같은 말을 반복하게 되고, 기여자는 다 만든 뒤 되돌리게 됩니다.\nGitHub 은 이 파일이 있으면 PR 작성 화면에 링크를 띄워 줍니다."),
      I("보안 신고","취약점 신고 방법을 적어 두는 표준 파일 이름은? (확장자 포함)",
        ["SECURITY.md"],
        "정답은 `SECURITY.md` 입니다. 취약점을 **공개 이슈로 올리지 말고** 어디로 알릴지 안내하는 파일입니다.\n공개 이슈로 올라오면 패치 전에 공격자에게 먼저 알려지는 셈이 됩니다. GitHub 의 비공개 보안 신고 기능을 함께 켜 두는 것이 좋습니다."),
      C("기여자 라이선스","큰 프로젝트가 기여자에게 CLA/DCO 에 동의하도록 하는 이유는?",
        ["메인테이너가 감당할 수 있는 정도로만 기여 건수를 줄이기 위해서","기여된 코드의 권리 관계를 명확히 해 나중의 법적 분쟁을 막으려고",
         "기여되는 코드의 품질을 끌어올리려고","병합까지 걸리는 속도를 높이려고"],1,
        "코드의 저작권은 기본적으로 **작성자**에게 있습니다. 권리 관계가 불분명하면 라이선스 변경이나 재배포가 막힐 수 있습니다.\nDCO 는 커밋에 `Signed-off-by` 한 줄을 붙이는 가벼운 방식이고, CLA 는 별도 문서에 서명하는 무거운 방식입니다.\n기여 장벽이 되므로 프로젝트 성격에 따라 선택이 갈립니다."),
      C("README","공개 저장소 README 에 가장 먼저 있어야 할 것은?",
        ["프로젝트에 기여해 온 사람들의 이름 목록","이 프로젝트가 무엇이고 어떤 문제를 푸는지 한두 문장",
         "적용되는 라이선스의 전체 문구","기여자별 커밋 통계와 그래프"],1,
        "방문자가 **3초 안에** '나에게 필요한 것인가' 를 판단할 수 있어야 합니다.\n그 다음이 설치·최소 사용 예제입니다. 예제 코드 한 덩어리가 설명 열 줄보다 잘 통합니다.\n기여자 목록과 라이선스 전문은 아래쪽이나 별도 파일로 충분합니다.")
    ] }
]},

/* ═══════════════ 유닛 12 ═══════════════ */
{ t:"직접 해 보는 Git — 상황별 처방", l:[

  { t:"막혔을 때의 판단 순서", xp:35,
    th:{ sum:"Git 사고의 90%는 **다섯 가지 질문**으로 좁혀진다. 명령을 외우는 대신 이 순서를 익힌다.",
      body:[
        {h:"① 커밋했는가 ② 푸시했는가", t:"**커밋하지 않은 것**은 Git 이 모르므로 되살릴 수 없다 — 그래서 애매하면 `git stash` 나 임시 커밋이 최선의 보험이다. **푸시했는가**는 '역사를 고쳐도 되는가' 를 정한다. 푸시 전이면 `amend`·`rebase`·`reset` 이 모두 열려 있고, 푸시 후면 `revert` 가 기본이다."},
        {h:"③ 지금 어디인가 ④ 무엇이 달라졌나 ⑤ 되돌릴 대상은", t:"`git status` 로 브랜치와 세 공간의 상태를, `git log --oneline --graph --all` 로 갈래 모양을, `git diff` 로 실제 차이를 본다. 이 셋을 보고 나면 '**작업 폴더의 수정**' 인지 '**스테이지**' 인지 '**커밋**' 인지가 정해지고, 명령은 자동으로 하나로 좁혀진다."}],
      code:{ c:"git status                       # 어디에, 무엇이\ngit log --oneline --graph --all  # 갈래 모양\ngit reflog                       # 내가 거쳐 온 자리\ngit diff / git diff --staged     # 실제 차이", cap:"명령을 치기 전에 상태를 먼저 본다" },
      key:["커밋 전이면 되살릴 수 없다","푸시했는지가 역사 수정 가능 여부를 정한다","status·log·diff 로 먼저 확인","되돌릴 대상이 정해지면 명령은 하나"] },
    q:[
      I("범위로 되돌리기","최근 두 커밋을 각각 취소하는 revert 표현은? (git revert 와 범위)",
        ["git revert HEAD~2..HEAD","git revert HEAD~1..HEAD "],
        "정답은 `git revert HEAD~2..HEAD` 입니다. 범위 안의 커밋을 <b>역순으로</b> 하나씩 되돌립니다.\n한 커밋으로 묶고 싶으면 `--no-commit` 을 붙여 전부 스테이지에 모은 뒤 직접 커밋하면 됩니다."),
      C("판단 1","되돌리기 명령을 고를 때 <b>가장 먼저</b> 확인할 것은?",
        ["설치된 Git 의 버전 번호","되돌릴 대상이 커밋인지, 스테이지인지, 작업 폴더의 수정인지",
         "지금 작업하고 있는 브랜치의 이름과 위치","연결된 원격 저장소의 주소"],1,
        "명령이 헷갈리는 이유는 **대상이 정해지지 않았기 때문**입니다. 대상이 정해지면 명령은 거의 하나로 좁혀집니다.\n작업 폴더의 수정이면 `restore`, 스테이지면 `restore --staged`, 커밋이면 `reset`/`revert` 입니다.\n`git status` 한 번이면 이 셋 중 어디에 있는지가 바로 보입니다."),
      C("판단 2","'역사를 고쳐도 되는가' 를 결정하는 기준은?",
        ["브랜치에 지금까지 쌓인 커밋의 개수","이미 푸시해서 남과 공유했는지","지금 작업하고 있는 브랜치의 이름과 위치","되돌리려는 파일의 크기"],1,
        "**공유되지 않은 역사는 마음껏, 공유된 역사는 절대** — 이 한 줄이 `amend`·`rebase`·`reset` 사용 여부를 전부 결정합니다.\n공유된 뒤에는 `revert` 로 새 커밋을 쌓는 것이 안전합니다.\n예외는 '내 PR 브랜치는 강제 푸시해도 된다' 고 팀이 합의한 경우뿐입니다."),
      C("상황 처방 1","커밋 메시지 오타를 발견했다. <b>아직 푸시 전</b>이다.",
        ["git revert HEAD","git commit --amend","git reset --hard HEAD","새 커밋으로 설명"],1,
        "`git commit --amend` 로 직전 커밋을 새로 만들어 갈아 끼웁니다. 푸시 전이므로 해시가 바뀌어도 문제없습니다.\n`--no-edit` 를 붙이면 메시지를 그대로 두고 파일만 추가할 수 있습니다.\n푸시 후였다면 강제 푸시가 필요해지므로, 사소한 오타라면 그냥 두는 편이 나을 때도 많습니다."),
      C("상황 처방 2","<b>이미 배포된</b> 커밋이 장애를 일으켰다. 가장 안전한 대응은?",
        ["git reset --hard 로 지운 뒤 원격에 강제 푸시한다","git revert 로 취소 커밋을 만들어 배포",
         "문제가 된 파일을 직접 손으로 되돌려 새로 커밋한다","저장소 전체를 이전 시점으로 롤백한다"],1,
        "`revert` 는 **역사를 건드리지 않고** 반대 변경을 새로 쌓으므로 다른 사람의 저장소가 어긋나지 않습니다.\n무엇보다 '언제 무엇을 왜 되돌렸는가' 가 기록으로 남아 사후 분석에 쓰입니다.\n장애 상황에서는 원인 규명보다 **먼저 되돌려 안정화**하는 것이 순서입니다."),
      C("상황 처방 3","브랜치를 잘못 지웠는데 아직 병합하지 않은 커밋이 있었다.",
        ["가리키는 이름이 없어 복구할 수 없다","reflog 에서 해시를 찾아 되살린다",
         "원격 저장소에서 지운 브랜치를 다시 받아온다","git fsck 를 돌리면 잃어버렸던 브랜치가 알아서 자동으로 복구된다"],1,
        "`git reflog` 에 그 브랜치가 가리키던 마지막 해시가 남아 있습니다. `git switch -c 이름 해시` 로 되살립니다.\n`git fsck --lost-found` 로 도달 불가 객체를 뒤지는 방법도 있지만, reflog 가 훨씬 빠르고 확실합니다.\n다만 `git gc` 가 정리한 뒤라면 되살릴 수 없으니, 사고 직후 바로 시도해야 합니다."),
      I("상황 처방 4","다른 브랜치의 커밋 하나만 지금 브랜치로 가져오는 명령은? (git 포함, 해시는 제외)",
        ["git cherry-pick"],
        "정답은 `git cherry-pick` 입니다. 그 커밋의 변경만 현재 브랜치에 새 커밋으로 얹습니다.\n긴급 수정을 `main` 과 릴리스 브랜치 양쪽에 넣을 때 표준적인 방법입니다. 범위로 여러 개(`A..B`)를 가져올 수도 있습니다."),
      C("상황 처방 5","<code>main</code> 에서 작업하다 커밋까지 해 버렸다. 브랜치로 옮기려면?",
        ["파일을 전부 복사해 둔 다음 새 브랜치를 만들어 처음부터 똑같이 다시 만든다","git switch -c 새브랜치 후 main 을 git reset --hard origin/main 으로 되돌린다",
         "git stash 로 옮긴다","main 인 채로 그냥 원격에 푸시한다"],1,
        "커밋은 **브랜치가 아니라 그래프에 있으므로**, 새 브랜치를 지금 자리에 만들면 그 커밋들이 따라옵니다.\n그 다음 `main` 만 원격 상태로 되돌리면 깔끔하게 정리됩니다.\n`main` 을 되돌리기 전에 새 브랜치를 **먼저** 만드는 순서가 중요합니다 — 순서를 바꾸면 커밋을 가리키는 이름이 없어져 reflog 를 뒤져야 합니다.")
    ] },

  { t:"혼자서도 Git 을 제대로 쓰기", xp:35,
    th:{ sum:"혼자 하는 프로젝트에서도 Git 은 값어치가 있다. **되돌아갈 자리**와 **왜 그랬는지의 기록**은 팀이 없어도 필요하다.",
      body:[
        {h:"과감해지기 위해 커밋한다", t:"커밋을 자주 하면 **마음 놓고 부술 수 있다**. '이거 갈아엎어 볼까' 를 망설이지 않게 되는 것이 혼자 하는 프로젝트에서 Git 의 가장 큰 효용이다. 실험은 브랜치에서 하고, 실패하면 브랜치째 버리면 된다 — 잃는 것이 없다."},
        {h:"미래의 나는 남이다", t:"3개월 뒤의 자신은 오늘의 맥락을 전혀 기억하지 못한다. '왜 이 라이브러리를 버전 고정했는지', '왜 이 이상한 우회 코드가 있는지' 는 커밋 메시지에만 남는다. 혼자여도 **이슈를 쓰고 PR 을 여는** 사람들이 있는데, 그 기록이 나중에 자신을 구하기 때문이다."}],
      code:{ c:"git switch -c try/새구조     # 실험은 브랜치에서\n# ... 마음껏 부순다\ngit switch main              # 실패하면 그냥 돌아온다\ngit branch -D try/새구조     # 미련 없이 버린다", cap:"실패의 비용이 0이면 과감해진다" },
      key:["자주 커밋해야 과감해진다","실험은 브랜치에서, 실패하면 버린다","미래의 나는 맥락을 기억 못 한다","혼자여도 기록은 남긴다"] },
    q:[
      I("보기 좋은 로그","갈래 모양까지 그려 주는 로그 표현으로 널리 쓰이는 옵션 묶음은? (git log 뒤 옵션 세 개)",
        ["--oneline --graph --all","--graph --oneline --all","--oneline --all --graph"],
        "정답은 `git log --oneline --graph --all` 입니다. 한 줄씩, 갈래 모양으로, 모든 브랜치를 보여 줍니다.\n너무 길어 매번 치기 어려우므로 `git config --global alias.lg` 로 별칭을 만들어 두는 사람이 많습니다 — 치기 쉬워야 실제로 보게 됩니다."),
      C("혼자일 때 효용","혼자 하는 프로젝트에서 Git 의 가장 큰 효용은?",
        ["쓰지 않는 불필요한 코드가 함께 정리되면서 실행이 더 빨라진다","되돌아갈 자리가 있어 과감하게 시도할 수 있다",
         "여러 사람이 함께 나눠서 작업할 수 있다","중복된 의존성이 사라져 저장 용량이 크게 준다"],1,
        "'망치면 어쩌지' 가 사라지면 **더 나은 구조를 시도**하게 됩니다. 이것이 혼자 쓰는 Git 의 핵심 가치입니다.\n협업 기능은 사람이 늘어야 의미가 있지만, 되돌리기는 첫날부터 값어치가 있습니다.\n그래서 초보자에게도 '일단 `git init`' 을 권합니다."),
      C("실험","새 구조를 시도해 보고 싶은데 실패할 것 같다면?",
        ["고치기 전 파일을 통째로 복사해 둔다","브랜치를 만들어 실험하고 실패하면 브랜치째 버린다",
         "완성될 때까지 해당 코드를 주석 처리해 둔다","날짜를 붙인 백업 폴더를 프로젝트 옆에 따로 만들어 둔다"],1,
        "브랜치는 **거의 공짜**이고, 버리는 것도 명령 한 줄입니다. 파일 복사는 어느 것이 최신인지 금세 알 수 없게 됩니다.\n성공하면 병합하고 실패하면 삭제 — 어느 쪽이든 `main` 은 깨끗하게 유지됩니다.\n이 습관 하나가 '큰 리팩터링을 못 하는' 상태에서 벗어나게 해 줍니다."),
      C("기록","혼자 하는 프로젝트에서도 커밋 메시지를 잘 써야 하는 이유는?",
        ["나중에 다른 사람이 볼 수도 있어서","3개월 뒤의 자신이 오늘의 맥락을 기억하지 못하기 때문",
         "커밋 메시지가 GitHub 활동 순위에 반영되기 때문에","저장소 용량을 아끼기 위해서"],1,
        "미래의 자신은 사실상 **남**입니다. '왜 이 버전을 고정했는지', '왜 이 우회 코드가 있는지' 는 코드에 남지 않습니다.\n그 정보가 없으면 나중에 '필요 없어 보인다' 며 지웠다가 같은 버그를 다시 만나게 됩니다.\n한 줄이면 충분합니다 — 없는 것과 있는 것의 차이가 큽니다."),
      C("백업","내 컴퓨터가 고장 나도 작업을 지키려면?",
        ["작업을 stash 에 넣어 보관한다","원격 저장소에 자주 푸시한다","작업마다 브랜치를 넉넉히 많이 만든다","그 시점에 태그를 달아 표시한다"],1,
        "Git 은 **내 컴퓨터 안**에서만 동작합니다. 디스크가 고장 나면 역사도 함께 사라집니다.\n원격에 푸시하는 것이 유일한 진짜 백업입니다. 비공개 저장소는 무료로 만들 수 있습니다.\nstash 와 브랜치는 전부 로컬이라 백업이 되지 않습니다."),
      C("초기 설정","새 컴퓨터에서 Git 을 쓰기 전 반드시 해야 할 설정은?",
        ["커밋 메시지를 작성할 기본 에디터 지정","user.name 과 user.email","출력에 쓰이는 색상 설정","자주 쓰는 긴 명령의 짧은 별칭 등록"],1,
        "커밋에는 **작성자 정보**가 박히므로, 설정하지 않으면 Git 이 커밋을 거부하거나 이상한 값이 들어갑니다.\n`git config --global user.name`·`user.email` 두 줄이면 됩니다.\nGitHub 의 커밋이 내 계정에 연결되려면 여기 적은 이메일이 계정에 등록되어 있어야 합니다."),
      I("별칭","<code>git st</code> 로 <code>git status</code> 를 부르게 하는 설정 항목의 앞부분은? (config 뒤에 오는 이름)",
        ["alias.st","alias"],
        "정답은 `alias` 입니다. `git config --global alias.st status` 처럼 씁니다.\n`lg = log --oneline --graph --all --decorate` 같은 긴 명령을 줄여 두면 실제로 자주 쓰게 됩니다 — 치기 귀찮아서 안 보던 것을 보게 만드는 효과가 있습니다."),
      C("다음 단계","Git 을 기본적으로 익힌 뒤 가장 실용적인 다음 단계는?",
        ["git 명령마다 붙는 옵션을 하나하나 전부 외워 두기","실제 프로젝트를 원격에 올리고 PR 로 자신의 변경을 리뷰해 보기",
         "Git 내부 객체 구현 코드를 읽어 보기","SVN·Mercurial 같은 다른 버전 관리 도구까지 함께 배워 두기"],1,
        "Git 은 **쓰면서** 익혀집니다. 혼자여도 브랜치를 따고 PR 을 열어 자기 diff 를 읽어 보면, 리뷰어의 시선이 생깁니다.\n옵션 암기는 필요할 때 `git help` 로 찾으면 되고, 내부 구조는 문제를 겪은 뒤에 읽어야 남습니다.\n실제 저장소 하나를 끝까지 운영해 보는 것이 어떤 문서보다 빠른 길입니다.")
    ] }
]}

];

module.exports=shuffleUnits(UNITS);
