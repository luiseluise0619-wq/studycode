/* 4차 이론 — 재 보니 가장 어려웠던 레슨들.

   audit_theory 가 매긴 점수 상위부터 골랐다. 대부분 '한 문단에 개념 서넛',
   '왜 그런지 없음', '용어를 안 풀고 씀' 세 가지가 겹쳐 있었다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기
   문단 순서: 이게 뭔지 → 어떻게 쓰는지 → 어디서 틀리는지 */
module.exports = {

sql: [
{
  unit: "SQL 심화 — 실행 채점 (SQLite)", lesson: "서브쿼리·조인 심화",
  th: {
    sum: "조회 안에 조회를 넣은 것이 **서브쿼리**다. 표를 옆으로 이어 붙이는 것이 **조인**이다.",
    body: [
      { h: "서브쿼리는 '먼저 구해야 하는 값'", t: "'평균보다 많이 받는 사람' 을 찾으려면 평균을 먼저 알아야 한다. 그 평균을 구하는 조회를 괄호로 감싸 안에 넣는 것이 서브쿼리다. 값을 손으로 적어 넣으면 자료가 바뀔 때마다 쿼리를 고쳐야 한다." },
      { h: "바깥 행을 참조하면 행마다 다시 돈다", t: "안쪽 조회에서 바깥 표의 열을 쓰면, 바깥 행 하나마다 안쪽이 새로 계산된다. '자기 부서 평균' 처럼 행마다 답이 달라야 할 때 이렇게 쓴다. 연결하지 않으면 전체 평균 하나가 모든 행에 쓰인다." },
      { h: "조인은 '어느 열끼리 같은가' 를 적는 것", t: "`ON a.id = b.직원` 은 두 표를 어떤 열로 맞출지 정하는 자리다. 이 조건을 빠뜨리면 모든 행이 모든 행과 짝지어져 행 수가 곱하기로 늘어난다. 결과가 갑자기 수천 줄이면 이걸 먼저 의심한다." },
      { h: "LEFT JOIN 은 없는 짝을 NULL 로 채운다", t: "짝이 없는 행도 남기고 싶으면 `LEFT JOIN` 이다. 짝이 없는 자리는 빈 값(NULL)이 된다. 그런데 `WHERE` 에서 그 열을 조건으로 쓰면 그 행들이 다시 걸러져, 결국 안쪽 조인과 같아진다." },
    ],
    code: { c: "SELECT 이름 FROM 직원 d\nWHERE 급여 > (SELECT AVG(급여) FROM 직원 WHERE 부서 = d.부서);\n\nSELECT d.이름, s.금액\nFROM 직원 d LEFT JOIN 실적 s ON s.직원 = d.id;", cap: "안에서 구하고, 옆으로 붙인다" },
    key: ["서브쿼리는 먼저 구할 값", "바깥 열을 쓰면 행마다 다시 계산", "`ON` 을 빼면 행이 곱해진다"],
  },
},
{
  unit: "SQL 심화 — 실행 채점 (SQLite)", lesson: "집계·NULL·날짜",
  th: {
    sum: "여러 행을 하나로 줄이는 것이 **집계**다. 빈 값과 날짜는 집계에서 특히 자주 어긋난다.",
    body: [
      { h: "집계는 행을 줄인다", t: "`COUNT`·`SUM`·`AVG` 는 여러 행을 하나로 만든다. `GROUP BY` 를 적으면 그 기준마다 하나씩 나온다. 기준을 안 적으면 표 전체가 한 줄로 줄어든다." },
      { h: "빈 값은 집계에서 빠진다", t: "`AVG(금액)` 은 비어 있는 행을 아예 세지 않는다. 그래서 '0으로 치고 낸 평균' 보다 값이 커진다. 어느 쪽이 맞는지는 상황마다 다르니, 0으로 봐야 한다면 `COALESCE(금액, 0)` 으로 먼저 채운다." },
      { h: "COUNT(*) 와 COUNT(열) 은 다르다", t: "`COUNT(*)` 는 행 수를 세고, `COUNT(금액)` 은 금액이 **있는** 행만 센다. 둘을 나란히 놓으면 빈 값이 몇 개인지 바로 보인다. 헷갈려 쓰면 '왜 개수가 다르지' 로 시간을 쓴다." },
      { h: "날짜는 글자가 아니다", t: "SQLite 는 날짜를 `'2026-01-31'` 같은 글자로 담는다. 이 형식이면 글자 비교가 날짜 비교와 같아서 편하다. 하지만 `'2026-1-5'` 처럼 자리를 안 맞추면 순서가 어긋나므로, 넣을 때부터 형식을 지켜야 한다." },
    ],
    code: { c: "SELECT COUNT(*) AS 행수, COUNT(금액) AS 값있음\nFROM 실적;\n\nSELECT AVG(COALESCE(금액, 0)) FROM 실적;   -- 빈 값을 0으로 치고", cap: "빈 값을 어떻게 볼지 먼저 정한다" },
    key: ["집계는 행을 줄인다", "빈 값은 집계에서 빠진다", "날짜는 형식을 지켜 담는다"],
  },
},
{
  unit: "SQL 심화 — 실행 채점 (SQLite)", lesson: "윈도우·CTE·집합",
  th: {
    sum: "**윈도우 함수**는 행을 줄이지 않고 옆에 계산을 붙인다. **CTE** 는 긴 쿼리를 이름 붙여 쪼갠다.",
    body: [
      { h: "줄이지 않고 옆에 붙인다", t: "`GROUP BY` 는 여러 행을 하나로 만들지만, `AVG(급여) OVER (PARTITION BY 부서)` 는 행을 그대로 두고 옆에 부서 평균을 적어 준다. 개인 값과 무리 값을 나란히 보여 주는 표는 대개 이 방식으로 만든다." },
      { h: "순서를 주면 '지금까지' 가 된다", t: "`SUM(금액) OVER (ORDER BY 월)` 은 처음부터 그 행까지의 누적이다. `ORDER BY` 를 빼면 범위가 전체가 되어 모든 행에 같은 총합이 붙는다. 누적을 원했다면 완전히 다른 표가 된다." },
      { h: "CTE 는 이름 붙인 임시 결과", t: "`WITH 최근 AS (SELECT ...)` 로 앞부분에 이름을 붙이면, 뒤에서 표처럼 쓸 수 있다. 서브쿼리를 여러 겹 중첩하는 것보다 읽기 쉽고, 같은 결과를 두 번 쓸 때 한 번만 적으면 된다." },
      { h: "집합 연산자는 행 단위로 합친다", t: "`UNION` 은 두 결과를 합치되 **똑같은 행을 하나로 줄인다.** 개수를 세는 일에 쓰면 답이 작아지므로 그때는 `UNION ALL` 이다. 겹치는 것만 원하면 `INTERSECT`, 빼려면 `EXCEPT` 다." },
    ],
    code: { c: "WITH 월별 AS (\n  SELECT 직원, 월, 금액 FROM 실적\n)\nSELECT 월, 금액,\n  SUM(금액) OVER (ORDER BY 월) AS 누적\nFROM 월별;", cap: "옆에 붙이고, 이름을 붙여 쪼갠다" },
    key: ["윈도우는 행을 줄이지 않는다", "`ORDER BY` 가 있으면 누적", "`UNION` 은 중복을 없앤다"],
  },
},
],

javascript: [
{
  unit: "이벤트 루프와 태스크 큐", lesson: "매크로태스크 vs 마이크로태스크",
  th: {
    sum: "자바스크립트는 한 번에 **한 가지 일만** 한다. 기다리는 일들은 줄을 서는데, 그 줄이 **두 개**다.",
    body: [
      { h: "줄이 두 개인 이유", t: "`setTimeout` 으로 맡긴 일은 '큰 줄'(매크로태스크)에 선다. 프라미스가 끝나고 이어서 할 일은 '작은 줄'(마이크로태스크)에 선다. 프라미스 뒷정리는 최대한 빨리 해 줘야 값이 오래 붕 떠 있지 않기 때문이다." },
      { h: "작은 줄이 항상 먼저다", t: "지금 하던 일이 끝나면, 엔진은 **작은 줄을 완전히 비운 뒤에** 큰 줄에서 하나를 꺼낸다. 그래서 `setTimeout(f, 0)` 보다 `Promise.resolve().then(g)` 가 먼저 돈다. 0초로 맡겼는데 왜 나중이지 — 이 순서 때문이다." },
      { h: "0초는 '즉시' 가 아니다", t: "`setTimeout(f, 0)` 은 '지금 하던 일이 다 끝나고, 작은 줄도 다 비운 다음' 이라는 뜻이다. 화면을 그리는 일이 끼어들 수도 있다. '바로 다음' 을 원하면 프라미스 쪽을 쓴다." },
      { h: "긴 계산은 줄 전체를 막는다", t: "일꾼이 하나뿐이라, 오래 걸리는 계산이 돌면 그동안 클릭도 화면 갱신도 아무것도 못 한다. 화면이 멎은 것처럼 보이는 대부분이 이것이다. 잘게 쪼개 맡기거나 워커에게 넘겨야 한다." },
    ],
    code: { c: "setTimeout(() => console.log('큰 줄'), 0);\nPromise.resolve().then(() => console.log('작은 줄'));\nconsole.log('지금');\n// 지금 → 작은 줄 → 큰 줄", cap: "작은 줄을 다 비운 뒤 큰 줄 하나" },
    key: ["일꾼은 하나뿐", "작은 줄(프라미스)이 먼저", "0초는 즉시가 아니다"],
  },
},
],

python: [
{
  unit: "직접 코딩 — 실행 채점 (Python)", lesson: "파이썬 구현 챌린지 I",
  th: {
    sum: "여기서는 **직접 짜서 돌려 본다.** 읽어서 아는 것과 짜서 되는 것은 다르다.",
    body: [
      { h: "돌려 보면 안다", t: "고른 답이 맞았다고 짤 수 있는 것은 아니다. 직접 쓰면 빈 목록·0·음수 같은 경우를 스스로 떠올려야 한다. 그 과정에서 '알고 있다고 생각했던 것' 과 '진짜 아는 것' 이 갈린다." },
      { h: "빈 입력을 먼저 생각한다", t: "목록이 비어 있으면 `max()` 는 오류를 낸다. 합계는 0이 맞지만 평균은 0으로 나누게 된다. 이 경우를 빼먹으면 평소에는 잘 돌다가, 자료가 비는 날 그 자리에서 멈춘다. 값이 하나도 없거나 딱 하나일 때를 먼저 떠올리는 습관이 좋다." },
      { h: "이름이 절반이다", t: "`f`, `tmp`, `data2` 같은 이름은 사흘 뒤의 나에게 아무것도 알려 주지 않는다. `unread_count` 처럼 무엇인지 적어 두면 주석이 필요 없어진다. 읽는 시간이 쓰는 시간보다 훨씬 길다." },
      { h: "막히면 작게 쪼갠다", t: "한 번에 다 하려 하면 어디서 틀렸는지도 알기 어렵다. 중간 값을 변수에 담아 하나씩 확인하면 틀린 자리가 바로 보인다. 다 되고 나서 다시 합쳐도 늦지 않다." },
    ],
    code: { c: "def average(xs):\n    if not xs:        # 빈 목록을 먼저 거른다\n        return 0.0\n    return sum(xs) / len(xs)", cap: "없을 때를 먼저 정한다" },
    key: ["읽는 것과 짜는 것은 다르다", "빈 입력과 한 개를 먼저", "이름이 주석을 대신한다"],
  },
},
{
  unit: "직접 코딩 — 실행 채점 (Python)", lesson: "파이썬 구현 챌린지 II",
  th: {
    sum: "앞보다 조금 더 길다. 여기서부터는 **자료구조를 고르는 눈**이 결과를 가른다.",
    body: [
      { h: "'안에 있나' 는 집합에게", t: "리스트에 `x in xs` 를 하면 처음부터 하나씩 훑는다. 목록이 10배 커지면 10배 느려진다. 집합(`set`)은 몇 개가 들었든 거의 한 번에 답한다. 순서가 필요 없다면 집합이다." },
      { h: "세기와 묶기는 이미 만들어져 있다", t: "개수를 셀 때는 `collections.Counter`, 키마다 목록에 담을 때는 `setdefault` 나 `defaultdict` 가 있다. 직접 만들면 '없으면 만들기' 같은 if 가 늘어나고, 그 자리에서 실수가 난다." },
      { h: "한 번만 훑는다", t: "같은 목록을 여러 번 도는 코드는 대개 한 번에 끝낼 수 있다. 최댓값과 합계를 함께 구하는 식이다. 자료가 작을 때는 티가 안 나지만, 커지면 이 차이가 그대로 시간이 된다." },
      { h: "고치기 전에 재 본다", t: "느린 것 같다고 짐작으로 고치면 엉뚱한 데를 고친다. 시간이 오래 걸리는 자리를 먼저 재고, 거기만 손댄다. 대부분은 자료구조 하나를 바꾸는 것으로 끝난다." },
    ],
    code: { c: "seen = set()          # '봤나' 는 집합에게\nfrom collections import Counter\nCounter(words).most_common(3)", cap: "고르는 것만으로 짧고 빨라진다" },
    key: ["'있나' 는 집합", "세기·묶기는 이미 있다", "짐작 말고 재 보고 고친다"],
  },
},
],

compiler: [
{
  unit: "언어는 어떻게 실행되나", lesson: "번역과 실행",
  th: {
    sum: "사람이 쓴 글자를 기계가 이해하는 말로 **옮기는 일**이 번역이다. 옮기는 시점이 언어마다 다르다.",
    body: [
      { h: "기계는 우리 글자를 모른다", t: "CPU 는 아주 단순한 명령만 안다. 우리가 쓴 `if` 나 `for` 는 그대로는 아무 뜻이 없다. 누군가 이것을 CPU 가 아는 명령으로 바꿔 줘야 하고, 그 일을 하는 것이 컴파일러나 인터프리터다." },
      { h: "미리 옮기기 (컴파일)", t: "C 나 Go 는 **실행하기 전에** 통째로 옮겨 둔다. 그래서 돌 때는 빠르고, 문법이 틀리면 실행도 못 해 보고 그 자리에서 걸린다. 잘못을 일찍 발견한다는 것이 가장 큰 장점이다." },
      { h: "그때그때 옮기기 (인터프리터)", t: "파이썬은 한 줄씩 읽어 가며 실행한다. 바로 고쳐 보고 바로 돌려 볼 수 있어 편하다. 대신 실행해 보기 전에는 문법이 틀린 줄도 모르고, 같은 일을 반복해도 매번 다시 해석한다." },
      { h: "요즘은 섞어 쓴다", t: "자바와 자바스크립트는 일단 돌려 보다가, 자주 도는 자리를 그때 기계어로 바꿔 둔다(JIT). 처음엔 느리다가 점점 빨라지는 이유가 이것이다. '컴파일이냐 인터프리터냐' 로 딱 나뉘지 않는다." },
    ],
    code: { c: "소스 코드\n  → (컴파일) → 실행 파일 → 실행\n  → (인터프리터) → 한 줄씩 바로 실행", cap: "옮기는 시점이 다를 뿐이다" },
    key: ["기계는 우리 글자를 모른다", "미리 옮기면 빠르고 일찍 걸린다", "그때그때 옮기면 편하다"],
  },
},
{
  unit: "언어는 어떻게 실행되나", lesson: "컴파일 단계",
  th: {
    sum: "번역은 한 번에 되지 않는다. **글자 → 구조 → 뜻 → 기계어** 순서로 몇 단계를 거친다.",
    body: [
      { h: "먼저 낱말로 자른다", t: "`x = 1 + 2` 를 `x`, `=`, `1`, `+`, `2` 로 쪼갠다. 사람이 문장을 낱말로 읽는 것과 같다. 여기서 걸리는 오류는 대개 오타나 닫지 않은 따옴표다." },
      { h: "다음은 구조를 만든다", t: "낱말을 문법에 맞춰 나무 모양으로 엮는다. '무엇에 무엇을 대입한다', '무엇과 무엇을 더한다' 같은 뼈대다. 괄호를 안 닫았거나 순서가 어긋나면 여기서 걸린다." },
      { h: "그다음 뜻을 본다", t: "구조가 맞아도 뜻이 안 맞을 수 있다. 없는 변수를 쓰거나, 숫자에 문자열을 더하는 식이다. 문법은 멀쩡한데 오류가 나는 경우가 대부분 이 단계다." },
      { h: "마지막에 기계어로", t: "뜻까지 확인되면 CPU 가 아는 명령으로 옮기고, 쓸데없는 부분을 줄인다(최적화). 그래서 컴파일러가 만든 코드는 우리가 쓴 것과 줄 순서가 다를 수 있다 — 디버거에서 줄이 튀는 이유다." },
    ],
    code: { c: "x = 1 + 2\n  ① 낱말: x  =  1  +  2\n  ② 구조: (= x (+ 1 2))\n  ③ 뜻  : x 는 숫자다 — 괜찮다\n  ④ 기계어", cap: "단계마다 걸리는 오류가 다르다" },
    key: ["낱말 → 구조 → 뜻 → 기계어", "오류 종류로 단계를 알 수 있다", "최적화가 줄 순서를 바꾼다"],
  },
},
],

algo: [
{
  unit: "해시·투포인터", lesson: "해시맵으로 O(1) 조회 만들기",
  th: {
    sum: "해시맵은 **값을 자리로 바꿔** 바로 찾아가는 자료구조다. 몇 개가 들었든 찾는 시간이 거의 같다.",
    body: [
      { h: "왜 빠른가", t: "목록에서 찾으려면 처음부터 하나씩 본다. 10만 개면 최악에 10만 번이다. 해시맵은 값을 계산해 '몇 번 칸' 인지 바로 구하고 거기만 본다. 그래서 개수가 늘어도 찾는 시간이 거의 그대로다." },
      { h: "무엇을 열쇠로 삼을지 정한다", t: "'이 값을 본 적 있나' 면 값 자체가 열쇠다. '이 값이 몇 번째였나' 면 값이 열쇠, 자리가 내용이다. 무엇을 물을지 먼저 정하면 무엇을 담을지가 따라 정해진다." },
      { h: "두 수의 합 문제", t: "합이 `t` 인 두 수를 찾을 때, 모든 짝을 다 보면 오래 걸린다. 대신 한 번 훑으면서 '지금 값의 짝인 `t - x` 를 본 적 있나' 를 해시맵에 물으면 된다. 한 번만 돌고 끝난다." },
      { h: "순서는 지켜 주지 않는다", t: "해시맵은 빨리 찾는 대신 '어떤 순서로 들어왔는지' 를 기본으로 보장하지 않는 언어가 많다. 순서가 중요하면 따로 목록에 담거나, 순서를 지키는 자료구조를 쓴다." },
    ],
    code: { c: "seen = {}\nfor i, x in enumerate(nums):\n    if t - x in seen:      # 짝을 본 적 있나\n        return [seen[t - x], i]\n    seen[x] = i", cap: "한 번 훑으면서 물어본다" },
    key: ["값을 자리로 바꿔 바로 찾는다", "무엇을 물을지가 열쇠를 정한다", "순서는 보장하지 않는다"],
  },
},
{
  unit: "해시·투포인터", lesson: "누적합·슬라이딩 윈도우",
  th: {
    sum: "구간의 합을 매번 다시 더하지 않는 방법이 둘 있다. **미리 더해 두기**와 **창을 밀기**다.",
    body: [
      { h: "미리 더해 두면 뺄셈 한 번", t: "앞에서부터의 합을 미리 구해 두면, 어떤 구간의 합도 '끝까지의 합 − 시작 앞까지의 합' 한 번으로 나온다. 구간을 여러 번 물어볼 때 특히 크게 이긴다 — 매번 다시 더하면 물어볼 때마다 처음부터 훑는다." },
      { h: "창을 밀면 두 번만 고친다", t: "길이가 정해진 구간을 한 칸씩 옮길 때, 새로 들어온 값을 더하고 빠져나간 값을 빼면 된다. 구간 전체를 다시 더할 필요가 없다. 이 방법을 창(윈도우)을 민다고 부른다." },
      { h: "길이가 정해지지 않은 창", t: "'합이 t 이하인 가장 긴 구간' 처럼 길이가 정해지지 않으면, 오른쪽을 늘리다가 조건이 깨지면 왼쪽을 줄인다. 양쪽이 각각 한 번씩만 지나가므로 전체를 한 번 훑는 것과 비슷하게 끝난다." },
      { h: "음수가 있으면 조심한다", t: "창을 미는 방법은 대개 '늘리면 합이 커진다' 는 가정 위에 있다. 음수가 섞이면 그 가정이 깨져 답이 어긋난다. 그때는 누적합과 해시맵을 함께 쓰는 다른 방법을 쓴다." },
    ],
    code: { c: "# 미리 더해 두기\npre = [0]\nfor x in xs: pre.append(pre[-1] + x)\nsum(xs[i:j]) == pre[j] - pre[i]\n\n# 창 밀기\ns += xs[r]; s -= xs[l]", cap: "다시 더하지 않는다" },
    key: ["미리 더해 두면 뺄셈 한 번", "창은 두 값만 고친다", "음수가 섞이면 가정이 깨진다"],
  },
},
],

react: [
{
  unit: "렌더링과 재조정", lesson: "Reconciliation과 메모이제이션",
  th: {
    sum: "React 는 화면을 통째로 다시 그리지 않는다. **무엇이 달라졌는지 견주어** 달라진 곳만 고친다.",
    body: [
      { h: "견주고 나서 고친다", t: "상태가 바뀌면 React 는 새 화면 설계도를 만들고 이전 것과 견준다. 달라진 부분만 실제 화면에 반영한다. 화면 조작은 느린 일이라, 이 견주기가 통째로 다시 그리는 것보다 훨씬 싸다." },
      { h: "key 는 '같은 것' 이라는 표시", t: "목록에서 `key` 는 '이 항목은 아까 그 항목' 이라고 알려 주는 이름표다. 순번을 key 로 쓰면 중간에 하나를 지웠을 때 이름표가 밀려, 엉뚱한 항목이 남거나 입력값이 다른 줄로 옮겨 간다." },
      { h: "새 객체는 매번 '달라진 것'", t: "함수 안에서 만든 객체나 함수는 그릴 때마다 새로 만들어진다. 겉보기 내용이 같아도 React 는 다른 것으로 본다. 그래서 자식에게 그대로 넘기면 자식이 매번 다시 그려진다." },
      { h: "메모이제이션은 마지막 수단", t: "`useMemo`·`useCallback`·`memo` 는 '이전 것을 그대로 쓰라' 는 표시다. 공짜가 아니라 비교하는 비용이 든다. 느린 것을 먼저 재 보고, 정말 자주 다시 그려지는 곳에만 붙인다 — 습관처럼 다 붙이면 코드만 복잡해진다." },
    ],
    code: { c: "{items.map(it => <Row key={it.id} />)}   // 순번 말고 고유 id\n\nconst onPick = React.useCallback(fn, [dep]);  // 정말 필요할 때만", cap: "달라진 곳만 고친다" },
    key: ["견주고 달라진 곳만 고친다", "`key` 는 같은 것이라는 표시", "메모이제이션은 재 보고 붙인다"],
  },
},
],

net: [
{
  unit: "TCP vs UDP", lesson: "신뢰성의 대가",
  th: {
    sum: "TCP 는 **빠뜨리지 않는 대신 기다린다.** UDP 는 **기다리지 않는 대신 빠뜨린다.**",
    body: [
      { h: "TCP 가 하는 일", t: "보낸 것이 도착했는지 확인하고, 안 왔으면 다시 보낸다. 순서가 뒤바뀌면 바로잡아 준다. 파일이나 웹 페이지처럼 한 글자만 빠져도 못 쓰는 자료에 쓴다." },
      { h: "그 대가는 기다림", t: "빠진 조각 하나를 다시 받는 동안, 이미 도착한 뒤 조각들도 **줄 서서 기다린다.** 순서를 지켜 줘야 하기 때문이다. 그래서 한 번 놓치면 그 순간 전체가 잠깐 멎은 것처럼 보인다." },
      { h: "UDP 는 그냥 보낸다", t: "확인도 재전송도 순서 정리도 하지 않는다. 잃어버린 조각은 그냥 없는 채로 간다. 대신 기다리는 시간이 없어서, 늦게 오느니 없는 게 나은 자료에 맞는다." },
      { h: "그래서 어디에 쓰나", t: "실시간 통화나 게임은 0.5초 전 목소리를 지금 받아 봐야 소용이 없다. 잠깐 지직거려도 지금 것을 받는 편이 낫다. 반대로 파일 전송에서 조각이 빠지면 그 파일은 쓸 수 없게 된다." },
    ],
    code: { c: "TCP  보냄 → 확인 → (없으면) 다시 보냄 → 순서 정리\nUDP  보냄", cap: "무엇을 포기할지가 다르다" },
    key: ["TCP 는 빠뜨리지 않는다", "그 대가로 기다린다", "UDP 는 늦느니 없는 게 나을 때"],
  },
},
],

numpy: [
{
  unit: "ndarray 기초", lesson: "배열 생성과 shape/ndim/dtype",
  th: {
    sum: "넘파이 배열은 **같은 종류의 숫자를 한 덩어리로** 담는다. 그래서 파이썬 리스트보다 훨씬 빠르다.",
    body: [
      { h: "왜 리스트보다 빠른가", t: "파이썬 리스트는 칸마다 다른 종류가 들어갈 수 있어, 값 하나를 볼 때마다 '이게 뭐지' 를 확인한다. 넘파이 배열은 전부 같은 종류라 그 확인이 필요 없고, 한 번에 여러 개를 계산할 수도 있다." },
      { h: "shape 는 '몇 줄 몇 칸'", t: "`a.shape` 는 각 방향의 길이를 튜플로 알려 준다. `(2, 3)` 이면 2줄 3칸이다. 계산이 안 될 때 대부분은 이 모양이 안 맞아서이므로, 오류를 만나면 먼저 `shape` 를 찍어 본다." },
      { h: "ndim 은 '방향이 몇 개'", t: "`a.ndim` 은 차원 수다. 한 줄짜리는 1, 표 모양은 2다. `(3,)` 과 `(3, 1)` 은 값은 같아 보여도 방향 수가 달라, 계산 결과 모양이 달라진다." },
      { h: "dtype 은 '어떤 숫자'", t: "`a.dtype` 이 `int64` 면 정수만 담는다. 여기에 0.5를 넣으면 소수점이 잘려 0이 된다. 오류도 안 나고 값만 조용히 바뀌므로, 소수가 필요하면 만들 때부터 `float` 로 정한다." },
    ],
    code: { c: "a = np.array([[1, 2, 3], [4, 5, 6]])\na.shape   # (2, 3)  — 2줄 3칸\na.ndim    # 2\na.dtype   # int64", cap: "모양과 종류를 먼저 확인한다" },
    key: ["같은 종류라 빠르다", "`shape` 는 몇 줄 몇 칸", "`dtype` 이 정수면 소수가 잘린다"],
  },
},
{
  unit: "ndarray 기초", lesson: "배열 생성 함수와 reshape",
  th: {
    sum: "값을 하나하나 적지 않고 **만들어 주는 함수들**이 있다. 그리고 모양은 나중에 바꿀 수 있다.",
    body: [
      { h: "빈 판을 먼저 만든다", t: "`np.zeros((2, 3))` 은 0으로 채운 2줄 3칸을 만든다. `ones` 는 1로 채운다. 결과를 담을 자리를 미리 만들어 두면, 반복문에서 조금씩 이어 붙이는 것보다 빠르고 코드도 짧다." },
      { h: "규칙적인 수열", t: "`np.arange(0, 10, 2)` 는 0부터 2씩 건너뛰며 10 **앞까지** 만든다. 끝값이 포함되지 않는 것은 파이썬 `range` 와 같다. 끝값까지 정확히 나눠 담고 싶으면 `linspace` 를 쓴다." },
      { h: "reshape 는 값을 그대로 두고 모양만 바꾼다", t: "`a.reshape(3, 2)` 는 같은 값을 3줄 2칸으로 다시 본다. 그래서 **개수가 맞아야** 한다 — 6개짜리를 4칸으로 바꾸려 하면 오류가 난다. 개수가 안 맞으면 무엇을 버릴지 넘파이가 알 수 없기 때문이다." },
      { h: "-1 은 '알아서 계산해' ", t: "`a.reshape(2, -1)` 이라고 쓰면 남은 자리를 넘파이가 계산해 준다. 줄 수만 정하고 칸 수는 맡기고 싶을 때 쓴다. 두 자리를 다 -1 로 두면 답이 하나로 정해지지 않아 오류가 난다." },
    ],
    code: { c: "np.zeros((2, 3))\nnp.arange(0, 10, 2)     # [0 2 4 6 8] — 10 은 빠진다\na.reshape(2, -1)        # 칸 수는 알아서", cap: "만들어 주는 함수와 모양 바꾸기" },
    key: ["담을 자리를 미리 만든다", "`arange` 는 끝값을 뺀다", "`reshape` 는 개수가 맞아야 한다"],
  },
},
],

};
