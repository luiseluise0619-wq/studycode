module.exports = {
  track: "numpy", guide: "", xp: 80,
  unit: "실행형 실전 — 모양은 맞는데 값이 틀린 코드 고치기",
  source: "./dbg_numpy.cjs",
  lessons: [
    { t: "축과 브로드캐스트", n: 6,
      th: { sum: "넘파이 버그의 절반은 **정사각 배열에서 통과한다** — 축을 바꿔도, 브로드캐스트가 어긋나도 모양이 같아 보인다.",
        body: [
          { h: "axis 는 없어지는 축", t: "`(n, m)` 에서 `axis=0` 은 `(m,)`, `axis=1` 은 `(n,)` 이다. 열마다 평균이면 **행 방향으로 접어야** 하므로 `axis=0` 이다. 집계한 값을 다시 나눌 때는 `keepdims=True` 로 축을 남겨야 브로드캐스트가 의도대로 걸린다 — 안 그러면 뒤쪽 축끼리 맞추려 해서 조용히 열 방향으로 나눠진다." },
          { h: "뷰인가 복사인가", t: "넘파이의 기본 슬라이싱은 **뷰**라서 파이썬 리스트의 `a[:]` 습관이 그대로 원본을 고친다. `+=` 같은 제자리 연산도 dtype 을 강요해 소수 부분을 잘라 낸다. 둘 다 반환값만 보는 테스트로는 절대 안 잡히므로 '**원본이 그대로인가**' 를 따로 확인해야 한다." }],
        code: { c: "a / a.sum(axis=1, keepdims=True)   # (n,1) 로 남긴다\nb = a.copy()                        # 슬라이싱은 뷰다", cap: "행·열 개수가 다른 배열로 테스트한다" },
        key: ["axis 는 사라지는 축", "집계 후 나누기엔 keepdims", "기본 슬라이싱은 뷰", "원본 보존을 따로 테스트"] } },
    { t: "조용히 번지는 값", n: 6,
      th: { sum: "넘파이는 **예외를 잘 던지지 않는다** — 0 으로 나눠도, 정수가 넘쳐도 경고만 내고 계속 간다.",
        body: [
          { h: "inf·nan·감김", t: "0 으로 나누면 `inf`·`nan` 이 되고 `nan` 은 이후 모든 집계를 오염시킨다. `int8` 은 넘쳐도 조용히 감겨 200 이 −56 이 된다. `np.errstate` 로 경고를 오류로 올리고, 파이프라인 끝에서 `np.isfinite(x).all()` 을 확인하면 멀리 퍼지기 전에 잡힌다." },
          { h: "배열은 참거짓이 아니다", t: "`if a > 0` 은 불리언 **배열**이라 넘파이가 '모호하다' 며 거절한다 — 개수는 `np.count_nonzero`, 판정은 `.any()`·`.all()` 이다. `np.where(cond)` 는 값이 아니라 **자리**를 주고, `np.argmax` 는 axis 없이 부르면 평탄화된 인덱스를 준다. 축과 반환값의 뜻을 언제나 명시하세요." }],
        code: { c: "np.divide(a, b, out=out, where=(b != 0))\nreturn a[a > t]          # 값이 필요하면 불리언 인덱싱", cap: "경고를 오류로 올려 개발 중에 잡는다" },
        key: ["0 나누기·오버플로는 조용하다", "배열을 if 에 넣지 않는다", "where 는 자리를 준다", "argmax 는 axis 를 적는다"] } },
  ],
};
