module.exports = {
  track: "net", guide: "", xp: 80,
  unit: "실행형 실전 — 규약을 반쯤만 지킨 코드 고치기",
  source: "./dbg_net.cjs",
  lessons: [
    { t: "주소·비트·조각에서 새는 곳", n: 6,
      th: { sum: "네트워크 코드의 버그는 **작은 값에서는 맞고 큰 값에서 틀린다** — 비트가 넘치거나, 진법이 다르거나, 단위가 8의 배수가 아니거나.",
        body: [
          { h: "시프트와 자리올림", t: "자바스크립트의 시프트는 자릿수를 **32 로 나눈 나머지**만 쓴다. `-1 << 32` 는 `-1 << 0` 과 같아서 `/0` 마스크가 '전부 0' 이 아니라 '전부 1' 이 된다. 1의 보수 체크섬은 반대로 **넘친 자리를 버리면 안 되고** 하위로 되돌려 더해야 한다(end-around carry). 둘 다 작은 입력에서는 통과한다." },
          { h: "단위가 곧 계약이다", t: "IPv4 조각 오프셋은 **8바이트 단위**라 마지막이 아닌 조각의 페이로드는 8의 배수여야 한다. 헤더가 20바이트일 때 1480 이 우연히 8의 배수라서, 옵션이 붙은 헤더에서만 재조립이 깨진다. TTL 처럼 **음수가 뜻을 갖지 않는 값**은 만드는 자리에서 0 으로 깎아 둔다." }],
        code: { c: "const mask = bits === 0 ? 0 : (-1 << (32 - bits)) >>> 0;\nwhile (sum > 0xffff) sum = (sum & 0xffff) + (sum >>> 16);", cap: "0 과 32, 넘치기 전과 뒤를 함께 테스트한다" },
        key: ["시프트 자릿수가 변수면 0 과 32 를 테스트", "체크섬은 자리올림을 접는다", "조각 페이로드는 8의 배수", "남은 시간은 음수가 될 수 없다"] } },
    { t: "헤더와 문자열을 반쯤만 읽은 곳", n: 6,
      th: { sum: "HTTP 헤더는 **특수값과 두 번째 형식**을 함께 규정한다 — 그 예외를 안 읽으면 개발 서버에서는 전부 통과한다.",
        body: [
          { h: "0 과 '두 가지 형식'", t: "`Accept` 의 **`q=0` 은 명시적 거부**지 낮은 우선순위가 아니다. `Retry-After` 는 **초이거나 HTTP 날짜**이고, `Number()` 는 날짜를 `NaN` 으로, 빈 문자열을 0 으로 뭉갠다. '없음' 과 '0' 이 서로 다른 뜻인 자리에서는 변환 전에 존재 여부를 먼저 본다 — 포트가 비어 있으면 0 이 아니라 **스킴의 기본 포트**다." },
          { h: "경계 문자와 진법", t: "도메인 접미사 검사에는 **점 경계**가 있어야 `notexample.com` 이 통과하지 않는다. 질의 문자열의 `+` 는 빈칸이고(폼 인코딩), 청크 길이 줄은 **16진수**다 — 9 이하에서는 10진수와 같아 오래 숨는다. 바깥 입력을 다루는 함수가 **예외를 던질 수 있는지**도 계약의 일부다." }],
        code: { c: "host === cookieDomain || host.endsWith('.' + cookieDomain)\nparseInt(sizeLine, 16)   // 진법은 언제나 적는다", cap: "경계 문자 한 개, 진법 한 인자" },
        key: ["q=0 은 거부", "한 헤더가 두 형식을 가질 수 있다", "접미사 검사에는 점 경계", "16진수 자리에는 a~f 를 테스트"] } },
  ],
};
