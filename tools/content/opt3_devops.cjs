/* devops — 정답률 15% 아래 끝맺음의 오답 12개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "devops", fixes: [
  { k: "cloud2-k8s-liveness-vs-readiness", from: "둘 다 필요 없다", to: "프로브 없이 replicas 만 늘린다" },
  { k: "cloud2-k8s-oomkilled", from: "메모리 설정은 손대지 않아도 된다", to: "재시작 정책을 Never 로 바꿔 반복 재시작을 막는다. 죽은 파드는 그대로 두고 새로 배포하면 되므로 메모리 설정은 그대로 둔다" },
  { k: "cloud2-cicd-artifact-immutable", from: "불일치 자체가 생기지 않는다", to: "테스트를 건너뛴다 — 테스트 단계가 없으면 '테스트한 것과 다른 산출물' 이라는 불일치 자체가 성립하지 않는다" },
  { k: "cloud2-obs-cardinality", from: "너무 길게 잡혀 있기 때문이다", to: "로그 보존 기간이 너무 길게 잡혀 있는 탓이다" },
  { k: "migration-order", from: "마이그레이션 순서를 신경 쓸 필요가 없다", to: "구버전 파드를 먼저 전부 죽인다 — 롤링 대신 재생성 전략으로 바꾸면 두 버전이 공존하는 구간이 없어 마이그레이션 순서는 아무래도 좋다" },
  { k: "cpu-throttle", from: "일부 요청이 차단되고 있기 때문이다", to: "네트워크 정책에 걸려 일부 요청이 차단되고 있는 탓이다" },
  { k: "제약", from: "그대로 유지하고 있기 때문이다", to: "앞단의 로드밸런서가 옛 응답 캐시를 계속 그대로 유지하고 있어서다" },
  { k: "트레이스", from: "같은 정보를 두 번 모을 필요가 없다", to: "트레이스가 메트릭을 대체하므로 메트릭 수집을 꺼서 비용을 줄여야 한다. 같은 정보를 두 번 모으는 것은 낭비다" },
  { k: "롤백안전", from: "기본값이 채워지므로 문제가 없다", to: "롤백은 안전하므로 그냥 이전 이미지를 다시 배포하면 된다. 스키마는 코드와 독립적이라 v1 이 돌아와도 새 컬럼을 무시하고 그대로 동작한다 새 컬럼은 기본값이 채워지므로 그대로 돈다" },
  { k: "의존성 설치", from: "테스트를 함께 실행해 주기 때문", to: "테스트를 함께 실행해 주어서" },
  { k: "운영", from: "아무 조치도 필요 없다", to: "콘솔 변경은 IaC 가 자동으로 감지해 코드에 반영한다" },
  { k: "원칙", from: "설정과 시크릿은 구분할 필요가 없다", to: "설정과 시크릿은 같은 파일에 함께 둔다" }
] };
