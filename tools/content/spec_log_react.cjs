module.exports = {
  track: "react", guide: "📜", xp: 70,
  unit: "로그 분석 — 콘솔과 프로파일러 읽기",
  source: "./log_react.cjs",
  lessons: [
    { t: "다시 그려지는 이유", n: 6,
      th: { sum: "리액트의 사고는 대개 **언제 다시 그리는가**를 잘못 말해 준 결과다.",
        body: [
          { h: "효과와 의존성", t: "의존성 배열이 없으면 렌더마다 효과가 돌고, 그 안에서 상태를 바꾸면 무한 고리가 된다. 반대로 빈 배열로 한 번만 등록한 콜백은 <b>그때의 값에 갇혀</b> 영원히 옛 상태를 본다. 콘솔의 '초당 렌더 수' 와 'Maximum update depth' 는 원인이 아니라 결과다." },
          { h: "같아 보이는데 다른 값", t: "Provider 의 value 를 렌더마다 새 객체로 주면 내용이 같아도 참조가 달라 모든 구독자가 다시 그려진다. <code>React.memo</code> 는 props 만 비교하므로 컨텍스트에는 듣지 않는다. 목록의 <code>key={index}</code> 도 같은 종류의 거짓말이다 — 자리로 신원을 말하면 순서가 바뀔 때 상태가 엉킨다." }],
          code: { c: "useEffect(fn)        → 매 렌더\nuseEffect(fn, [])    → 한 번, 값은 그때로 고정\nuseEffect(fn, [x])   → x 가 바뀔 때\nvalue={{a,b}}        → 매번 새 참조", cap: "다시 그리는 조건을 정확히 말해 준다" },
        key: ["의존성 배열이 계약이다", "빈 배열은 오래된 클로저를 만든다", "새 객체 참조는 전부 다시 그린다", "key 는 자리가 아니라 신원"] } },
    { t: "느린 화면의 증거", n: 6,
      th: { sum: "느림의 원인은 로그에서 **시간이 어디에 쌓였는지**로 갈린다 — 계산인가, 네트워크인가, DOM 인가.",
        body: [
          { h: "쪼개서 본다", t: "프로파일러의 커밋 시간이 180ms 인데 DOM 갱신이 9ms 라면 병목은 리액트가 아니라 렌더 본문의 계산이다. 네트워크 탭에서 요청이 <b>겹치지 않고 계단</b>으로 보이면 폭포수이고, DOM 노드가 수만 개면 스타일 재계산이 프레임을 먹는다. <b>총 시간이 아니라 구간별 시간</b>을 봐야 처방이 갈린다." },
          { h: "늦게 온 응답과 두 번 도는 효과", t: "먼저 보낸 요청이 나중에 도착해 최신 결과를 덮어쓰는 일은 흔하다 — 취소하지 않으면 화면과 입력이 어긋난다. StrictMode 가 효과를 두 번 실행해 드러나는 문제도 개발 모드의 잘못이 아니라 <b>다시 실행되면 안 되는 부수효과</b>를 효과에 담은 탓이다." }],
        code: { c: "커밋 180ms = 계산 162ms + DOM 9ms → useMemo\n요청 3건이 순차 → Promise.all / 상위에서 함께\nAbortController 로 이전 요청 취소", cap: "시간이 쌓인 자리를 먼저 찾는다" },
        key: ["구간별로 재야 처방이 나온다", "폭포수는 계단 모양으로 보인다", "늦게 온 응답을 버려야 한다", "효과는 두 번 돌아도 안전해야"] } },
  ],
};
