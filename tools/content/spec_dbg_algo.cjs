module.exports = {
  track: "algo", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 예제는 통과하는 구현 고치기",
  source: "./dbg_algo.cjs",
  lessons: [
    { t: "경계와 초기값이 어긋난 곳", n: 7,
      th: { sum: "알고리즘 버그의 대부분은 논리가 아니라 **경계 한 칸과 초기값 하나**에 있다 — 그리고 그 둘은 교과서 예제에서 절대 드러나지 않는다.",
        body: [
          { h: "부등호 하나가 답을 바꾼다", t: "이진 탐색은 '찾는다' 가 아니라 '<b>어느 경계를 돌려줄 것인가</b>' 를 정하는 일이고, 모노토닉 스택의 <code>&lt;</code> 와 <code>&lt;=</code> 는 '같은 값을 하나로 볼 것인가' 라는 서로 다른 문제를 푼다. 누적합 배열은 원본보다 <b>하나 길다</b> — 포함 구간 <code>[i, j]</code> 의 합은 언제나 <code>pre[j+1] - pre[i]</code> 다. 문제 문장의 '더', '이상', '포함' 을 그대로 부등호로 옮겨 적는 습관이 이 세 가지를 한꺼번에 막는다." },
          { h: "초기값과 기준은 후보 안에서 고른다", t: "누적 최댓값의 초기값은 0 이 아니라 <b>실제 후보 하나</b>여야 한다 — 0 으로 두면 '아무것도 고르지 않는 선택' 을 몰래 후보에 넣은 것이라 전부 음수인 입력에서 무너진다. 탐욕도 같다. 회의실 배정에서 <b>무엇을 아끼려는가</b>(남은 시간)를 정하면 정렬 기준이 '끝나는 시각' 으로 저절로 정해진다. 투 포인터의 왼쪽 끝은 <b>절대 되감기지 않는다</b> — 그 단조성이 O(n) 의 근거다." }],
        code: { c: "if (a[mid] < x) lo = mid + 1; else hi = mid;  // 경계 하나만 남긴다\nleft = Math.max(left, last.get(ch) + 1);      // 되감기지 않는다", cap: "부등호와 단조성이 곧 정답의 근거다" },
        key: ["중복이 있는 입력을 첫 테스트로", "누적합은 원본보다 한 칸 길다", "최댓값 초기값은 실제 후보로", "탐욕 기준은 아끼려는 자원에서 나온다"] } },
    { t: "아껴 둔 상태를 잘못 되쓴 곳", n: 6,
      th: { sum: "표·메모·집합·큐는 전부 **계산을 아끼려고 둔 상태**다. 아끼는 순간부터 '이 값이 지금 유효한가' 가 새로운 버그의 자리가 된다.",
        body: [
          { h: "읽는 칸이 이전 값인가 이번 값인가", t: "0/1 배낭을 1차원으로 접을 때 용량을 작은 쪽부터 훑으면 <code>dp[c-w]</code> 가 <b>같은 물건을 이미 넣은 뒤의 값</b>이라 무한 배낭이 된다. 메모이제이션의 열쇠는 <b>상태를 유일하게 결정하는 값 전부</b>여야 한다 — <code>r + c</code> 는 <code>(0,1)</code> 과 <code>(1,0)</code> 을 뭉개고, 정사각 격자에서는 우연히 통과한다. 유니온-파인드에서 <code>p[...]</code> 에 대입하는 왼쪽 자리는 언제나 <b>find 를 거친 뿌리</b>여야 한다." },
          { h: "순서가 곧 보장이다", t: "BFS 의 최단 거리 보장은 오직 '<b>가까운 것부터 꺼낸다</b>' 에서 나온다. <code>pop()</code> 으로 바꾸는 순간 근거가 사라지고, 방문 표시가 담을 때 찍히므로 <b>먼 길의 거리가 그대로 못 박힌다</b>. 반대로 정렬은 순서에 기대면 안 된다 — 비교 함수는 임의의 두 원소에 순서를 주는 <b>전순서</b>여야 하고, 동점이 남으면 답이 입력 순서에 딸려 가 재현이 안 된다." }],
        code: { c: "for (let c = cap; c >= w; c -= 1) ...        // 거꾸로 훑어야 한 번만 담긴다\nsort((a,b) => b[1]-a[1] || cmp(a[0],b[0]))  // 동점에도 순서를 준다", cap: "아껴 둔 값에는 유효 범위가 붙어 있다" },
        key: ["1차원으로 접을 때는 훑는 방향을 확인한다", "메모 열쇠에는 구분자를 넣는다", "합칠 것은 노드가 아니라 뿌리다", "비교 함수는 전순서로 만든다"] } },
  ],
};
