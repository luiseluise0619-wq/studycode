module.exports = {
  track: "php", guide: "", xp: 70,
  unit: "로그 분석 — 웹 서버가 남긴 것",
  source: "./log_ops2.cjs", slice: [0, 8],
  lessons: [
    { t: "설정이 만든 증상", n: 4,
      th: { sum: "PHP 사고의 상당수는 코드가 아니라 **설정 한 줄**에서 온다 — 그리고 대개 조용히 실패한다.",
        body: [
          { h: "보여 줄지와 남길지는 다르다", t: "운영에서 오류를 화면에 띄우지 않는 것은 옳지만, 그것과 **기록을 남기는 것**은 다른 설정이다. 둘을 함께 꺼 두면 치명적 오류가 나도 남는 것이 500 하나뿐이라 원인을 찾을 자리가 없다. 한도도 여러 개가 겹쳐 있고 언제나 **작은 쪽이 이긴다** — 업로드 한도가 넉넉해도 요청 전체 한도가 작으면 요청은 본문째 버려지고, 코드에는 빈 배열이 도착해 아무 일도 없었다는 듯 성공 화면이 나간다." },
          { h: "서버가 둘이면 상태가 갈린다", t: "세션을 각 서버의 로컬 파일에 담으면, 로드밸런서가 요청을 다른 서버로 보내는 순간 '모르는 세션' 이 된다. 같은 세션 번호로 어떤 요청은 로그인 상태이고 어떤 요청은 아닌 것이 그 표식이다. 메모리 한도도 비슷하게 **화면 하나만** 터뜨린다 — 결과를 통째로 배열에 담는 코드가 있으면 데이터가 늘어날수록 그 화면만 죽고, 한도를 올려도 시간을 벌 뿐이다. 흘려보내는 방식으로 바꿔야 크기와 무관해진다." }],
        code: { c: "log_errors = On          ; 화면에 안 띄우는 것과 별개다\npost_max_size >= upload_max_filesize\nsession.save_handler = redis   ; 서버가 둘 이상이면", cap: "작은 쪽 한도가 이긴다" },
        key: ["보여 주기와 남기기는 다른 설정", "겹친 한도는 작은 쪽이 이긴다", "넘긴 요청은 본문째 버려진다", "세션은 서버 밖에 둔다"] } },
    { t: "느림과 배포", n: 4,
      th: { sum: "**일꾼 수**와 **한 요청이 잡고 있는 시간**의 곱이 처리량이다. 느려짐도 배포 사고도 대개 이 둘로 설명된다.",
        body: [
          { h: "느려진 것은 바깥일 때가 많다", t: "일꾼이 다섯인데 하나가 3초씩 붙잡히면 초당 두 요청도 못 받는다. 늘어난 것이 접속 수가 아니라 **한 요청이 잡고 있는 시간**이면, 일꾼 수를 올려도 메모리가 먼저 바닥난다. 처방은 바깥 호출에 짧은 제한 시간을 걸고 아픈 곳을 잠시 끊는 것이다. 반대로 느린 쿼리 기록이 비어 있는데 화면이 4초라면 **하나가 느린 것이 아니라 100번 도는 것**이다 — 요청당 쿼리 수를 함께 남겨야 눈에 띈다." },
          { h: "파일이 바뀌어도 코드는 그대로", t: "바이트코드 캐시에서 시간표 확인을 꺼 두면 빨라지는 대신 **다시 읽을 때를 우리가 알려 줘야** 한다. 배포가 재시작도 캐시 비우기도 하지 않으면 옛 코드가 그대로 돌고, 더 나쁘게는 어떤 파일만 새것이라 반쯤 섞인 상태가 된다. 시각이 어긋나는 것도 비슷하게 설정의 문제다 — 저장은 협정 세계시로, 표시할 때만 지역 시간으로 바꾸고 그 지역을 애플리케이션에 명시해 둔다." }],
        code: { c: "# 처리량 = 일꾼 수 / 한 요청이 잡는 시간\ncurl --max-time 2 ...          # 바깥 호출에는 제한 시간\nopcache_reset() 또는 부드러운 재시작을 배포 절차에", cap: "붙잡힌 시간이 처리량을 정한다" },
        key: ["일꾼 수보다 잡는 시간", "느린 쿼리가 없어도 느릴 수 있다", "배포에 캐시 비우기를 넣는다", "저장은 UTC, 표시만 지역"] } },
  ],
};
