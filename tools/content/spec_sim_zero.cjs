const UNIT="시뮬레이션 — 되감아 보며 이해한다";
module.exports={
  source:"./sim_zero.cjs", xp:70,
  entries:[
    { track:"numpy", guide:"🔢", unit:UNIT, lesson:"모양과 순회를 눈으로 본다", slice:[0,2],
      th:{ sum:"NumPy 의 두 기초 개념은 **한 단계씩 되감아 볼 때** 규칙이 아니라 구조로 남는다.",
        body:[
          {h:"뒤에서부터 맞춰지는 이유", t:"브로드캐스팅은 '뒤에서부터 맞추고, 같거나 1' 이 전부인데, 뒤에서부터인 것은 관례가 아니라 **메모리 배치** 때문이다 — C 순서에서 마지막 축이 연속이므로 뒤쪽 축을 맞추면 복사 없이 stride 를 0 으로 두어 확장할 수 있다. 그래서 `(3,5)` 에 `(5,)` 는 더할 수 있고 `(3,)` 은 못 더하며, `v[:, None]` 로 모양을 바꿔야 한다."},
          {h:"어느 축이 빨리 변하는가", t:"C 순서 순회는 **마지막 축이 가장 빠르게** 변한다. 이것이 행 방향 순회가 열 방향보다 빠른 이유(캐시 라인)이고, `reshape` 이 전치와 다른 이유이기도 하다 — `(2,3)` 을 `(3,2)` 로 바꾸면 데이터는 그대로 두고 읽는 방식만 바뀐다."}],
        code:{ c:"# (n,1) + (n,) 은 오류가 아니라 (n,n) 이 된다\nassert pred.shape == y.shape", cap:"모양을 단정하지 않으면 엉뚱한 손실이 나온다" },
        key:["브로드캐스팅은 뒤에서부터","크기 1 인 축은 stride 0 으로 확장","C 순서는 마지막 축이 가장 빠르다","reshape 은 전치가 아니다"] } },

    { track:"pandas", guide:"🐼", unit:UNIT, lesson:"groupby 와 조인을 단계로 본다", slice:[2,4],
      th:{ sum:"pandas 의 한 줄 뒤에는 여러 단계가 있고, **행 수가 조용히 변하는 지점**이 그 안에 있다.",
        body:[
          {h:"split · apply · combine", t:"`groupby` 는 키로 나누고, 그룹마다 함수를 적용하고, 결과를 합치는 세 단계다. 이 구조를 알면 '왜 `apply` 가 `agg` 보다 느린가' 가 자명해진다 — apply 는 그룹마다 파이썬 함수를 호출하므로 그룹 수만큼 호출이 발생한다. 그리고 집계 결과에는 거의 항상 **개수를 함께** 붙여야 한다: 샘플 3개의 평균과 1만 개의 평균은 다른 이야기다."},
          {h:"불어나고 사라진다", t:"조인은 오른쪽에 같은 키가 여러 개면 왼쪽 행을 **복제**하고, 짝이 없으면(내부 조인) **버린다**. 두 오차가 상쇄되어 총 행 수가 그럴듯해 보이는 것이 최악의 경우다. 방어는 조인 전후 행 수를 단정하거나 `validate=` 로 관계를 선언해 어기면 예외가 나게 하는 것이다."}],
        code:{ c:"before = len(df)\ndf = df.merge(other, on='id', validate='many_to_one')\nassert len(df) == before", cap:"'조인 뒤 SUM' 은 항상 의심 신호" },
        key:["groupby 는 split·apply·combine","집계에는 count 를 함께","조인은 행을 복제하거나 버린다","관계를 선언하고 행 수를 단정한다"] } },

    { track:"dl", guide:"🧠", unit:UNIT, lesson:"손실이 내려가는 길을 본다", slice:[4,6],
      th:{ sum:"학습률 하나가 **수렴과 발산을 가른다**. 궤적을 되감아 보면 그 경계가 계산으로 설명된다.",
        body:[
          {h:"걸음은 저절로 줄어든다", t:"경사 하강은 최소점에 가까워질수록 기울기가 작아져 **학습률을 그대로 둬도 걸음이 짧아진다**. 그래서 학습 후반에 손실이 천천히 줄어드는 것은 모델이 나빠서가 아니고, 그 구간에서 학습률을 더 줄이면 잡음에 덜 흔들린다."},
          {h:"경계는 계산으로 나온다", t:"이 손실에서 갱신식을 정리하면 `w − 3 ← (1 − 2lr)(w − 3)` 이므로 배율 `|1 − 2lr|` 이 모든 것을 정한다 — 1 보다 작으면 수렴, 0 이면 한 걸음에 도달, 음수면 양쪽을 번갈아 넘고, 절댓값이 1 을 넘으면 발산한다. 그래서 발산은 단순히 커지는 것이 아니라 **부호가 매 걸음 뒤집히는** 모양으로 나타난다 — 손실 곡선의 모양으로 학습률 문제와 기울기 부호 오류를 구별할 수 있다."}],
        code:{ c:"w = w - lr * 2 * (w - 3)\n# |1 - 2*lr| < 1 이면 수렴, > 1 이면 발산", cap:"톱니처럼 튀며 커지면 학습률 문제" },
        key:["기울기가 작아지면 걸음도 짧아진다","발산은 부호가 뒤집히며 커진다","손실 곡선 모양으로 원인을 구별한다","LR finder 로 구간을 찾고 조금 작게 잡는다"] } },

    { track:"ai", guide:"🤖", unit:UNIT, lesson:"어텐션과 토크나이저를 단계로 본다", slice:[6,8],
      th:{ sum:"트랜스포머의 두 부품 — **왜 √d 로 나누는가**, 그리고 **어휘는 어떻게 만들어지는가**.",
        body:[
          {h:"뾰족해지면 학습이 멈춘다", t:"차원이 커지면 내적의 분산이 커져 softmax 가 거의 원-핫이 되고, 그러면 미분값이 0 에 가까워져 **그 층에서 학습이 멈춘다**. 실제 헤드 차원이 64 이므로 스케일이 없으면 내적이 수십에 이른다 — '손실 곡선이 평평한데 성능이 안 나오는' 증상이 이것이다. `√d` 로 나누면 차원이 커져도 분포가 급격히 뾰족해지지 않는다."},
          {h:"자주 붙는 것을 합친다", t:"BPE 학습은 '가장 자주 붙어 나오는 두 조각을 합치기' 의 반복이다. 이 과정을 알면 실무 현상이 설명된다 — 한글이 토큰을 많이 먹는 이유(코퍼스에 한글 쌍이 적어 병합이 덜 일어남 → 비용과 컨텍스트 길이에 직결), 숫자 계산을 틀리는 이유(자릿수 구조가 빈도에 따라 임의로 쪼개짐). 동점 처리 규칙이 없으면 같은 코퍼스로도 **매번 다른 어휘**가 나와 재현이 불가능해진다."}],
        code:{ c:"raw = [dot(q, k) / sqrt(len(q)) for k in K]\n# 나누지 않으면 뾰족해지고, 뾰족하면 기울기가 죽는다", cap:"성능이 안 나오는데 손실 곡선만 평평하다면" },
        key:["√d 로 나누지 않으면 기울기가 죽는다","마스킹은 -inf 로, 큰 음수로 대체하지 않는다","BPE 는 빈번한 인접 쌍을 합친다","동점 규칙이 없으면 어휘가 재현되지 않는다"] } },

    { track:"mleval", guide:"📐", unit:UNIT, lesson:"임계값과 확률을 움직여 본다", slice:[8,10],
      th:{ sum:"모델을 배포할 때 실제로 정하는 것은 가중치가 아니라 **어디서 자를지, 확률을 믿을지**다.",
        body:[
          {h:"재현율만 단조롭다", t:"임계값을 내리면 양성 예측이 늘어 TP·FP 가 둘 다 줄지 않으므로 **재현율은 단조 증가**하지만, 정밀도는 새로 들어온 예측의 정체에 따라 오르내린다. 그래서 '목표 정밀도를 만족하는 임계값' 을 **이분 탐색으로 찾으면 틀린다** — 후보를 다 훑어야 한다. 그리고 이 임계값은 평가셋이 아니라 별도의 검증셋에서 골라야 한다."},
          {h:"확신과 실제가 어긋난다", t:"보정은 정확도와 다른 문제다 — '90% 확신' 이라고 말한 예측 중 90%가 맞아야 그 확률로 기대값을 계산할 수 있는데, 현대 신경망은 대체로 **과신**한다. ECE 는 그 어긋남을 재며, 칸마다 든 개수가 다르므로 **가중 평균**이어야 한다(1건뿐인 칸이 결론을 좌우하면 지표가 잡음을 재는 것이 된다)."}],
        code:{ c:"# 제약 안에서 재현율을 최대로 — 가장 낮은 임계값\nfor t in sorted(set(scores)):\n    if precision_at(t) >= target: return t", cap:"정밀도는 단조롭지 않다 — 이분 탐색은 틀린다" },
        key:["재현율은 단조, 정밀도는 아니다","임계값은 검증셋에서 고른다","ECE 는 칸 크기로 가중 평균","보정은 검증셋에서 학습해 붙인다"] } },

    { track:"php", guide:"🐘", unit:UNIT, lesson:"요청 수명과 워커 풀을 본다", slice:[10,12],
      th:{ sum:"PHP 를 이해하는 데 필요한 사실 둘 — **요청마다 새로 시작한다**, 그리고 **워커 하나가 요청 하나를 붙잡는다**.",
        body:[
          {h:"공유하지 않는다", t:"요청이 끝나면 변수·객체·연결이 전부 사라진다(shared-nothing). 그래서 메모리 누수가 서비스를 죽이지 않고 배포가 파일 교체로 끝나지만, 요청 사이에 남길 것은 **세션·Redis·DB** 로 보내야 하고 초기화가 매 요청 반복된다 — 그 비용을 줄이는 것이 OPcache 다. Swoole·RoadRunner 로 옮기면 이 안전망이 사라져 전역 상태가 다음 요청으로 샌다."},
          {h:"기다리는 동안에도 점유한다", t:"워커는 DB·외부 API 응답을 기다리는 동안에도 **점유된 채 놀고 있다**. 그래서 외부 API 가 느려지면 워커가 고갈되어 그 API 를 쓰지 않는 페이지까지 멈춘다 — CPU 는 한가한데 502 가 뜨는 상황이 이것이다. 방어는 짧은 타임아웃, 서킷 브레이커, 그리고 즉시 응답이 필요 없는 호출을 큐로 미루기다."}],
        code:{ c:"; 동시 처리 상한이 곧 이 값이다\npm.max_children = 40\n; max_children × memory_limit 이 서버 메모리를 넘지 않게", cap:"늘리는 것은 마지막 수단" },
        key:["요청마다 프로그램이 새로 시작한다","요청 간 상태는 바깥 저장소에","워커는 대기 중에도 점유된다","동시 처리 상한은 pm.max_children"] } },
  ]
};
