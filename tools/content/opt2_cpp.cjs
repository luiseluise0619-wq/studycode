/* cpp 트랙 — 정답이 5자 이상 단독 최장인 24문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "cpp", fixes: [
  { k: "cpp-tpl-vs-virtual", o: [
    "가상 함수 쪽이 코드 팽창을 유발한다 — 파생 타입마다 vtable 과 함수 본문이 따로 생성되어 바이너리가 커지고, 템플릿은 하나의 정의만 남긴다",
    "템플릿은 타입이 컴파일 시점에 정해져야 하고 간접 호출이 없으며, 가상 함수는 런타임에 타입이 정해지고 vtable 간접 호출이 있다",
    "템플릿도 인스턴스마다 vtable 을 만들어 호출을 간접적으로 해석하므로, 두 방식의 호출 비용은 같고 타입이 정해지는 시점만 다르다",
    "둘은 같은 메커니즘을 문법만 다르게 적은 것이라, 컴파일러가 템플릿 인자를 가상 함수 표로 바꿔 두 방식의 결과 코드가 완전히 같아진다" ] },
  { k: "cpp-elision-param", o: [
    "NRVO 가 정상적으로 적용된다 — 매개변수도 이름 있는 지역 객체이므로 반환값 자리에 직접 만들어져 복사도 이동도 없다",
    "컴파일 에러가 발생한다 — 매개변수는 호출자 소유라 값으로 반환하려면 먼저 지역 변수에 복사해 두어야 한다",
    "NRVO가 적용되지 않지만 이동은 일어난다 — 매개변수는 호출자 프레임과 연관되어 생략할 수 없다",
    "반드시 복사가 일어난다 — 매개변수는 lvalue 이므로 반환문에서도 이동 생성자가 아니라 복사 생성자만 선택된다" ] },
  { k: "cpp-elision-badmove", o: [
    "반환 표현식이 이름 있는 지역 변수가 아니게 되어 NRVO가 막히고, 생략될 수 있었던 이동이 강제된다",
    "std::move 는 xvalue 를 만들고 반환문은 lvalue 만 받으므로 그런 return 문은 컴파일 에러가 되기 때문",
    "std::move 가 지역 변수의 소유권을 먼저 넘겨 local 이 반환 전에 파괴되므로 반환값이 빈 객체가 되기 때문",
    "std::move 가 붙은 반환값에는 이동 생성자가 선택되지 않아 오히려 복사가 일어나 비용이 커지기 때문" ] },
  { k: "cpp-tpl-ifconstexpr", o: [
    "조건에 상수식만 쓸 수 있다는 문법 제약일 뿐이며, 두 가지 모두 컴파일되고 검사되는 것은 일반 if 와 똑같다",
    "조건이 컴파일 타임에 확정되어 분기 명령이 사라지므로 런타임에 더 빠르게 분기한다는 것이 유일한 차이다",
    "조건이 컴파일 타임에 거짓이면 그 가지를 아예 컴파일하지 않아, 특정 타입에서만 유효한 코드를 담을 수 있다",
    "반드시 else 가 있어야 하고 두 가지 모두 같은 타입을 반환해야 하므로, 타입별 분기에는 오히려 쓰기 어렵다" ] },
  { k: "cpp-move-noexcept", o: [
    "std::vector가 재할당할 때 강한 예외 보장을 위해 noexcept가 아닌 이동 대신 복사를 선택하기 때문",
    "noexcept 가 붙으면 컴파일러가 예외 검사 코드를 생략해 이동 자체가 눈에 띄게 빨라지기 때문",
    "noexcept 가 붙은 이동 생성자는 원본의 소멸자 호출을 생략해 주므로 정리 비용이 사라지기 때문",
    "noexcept 없는 이동 생성자는 표준 컨테이너 원소로 쓸 수 없다는 규칙이 있어 컴파일되지 않기 때문" ] },
  { k: "cpp-vf-vtable", o: [
    "객체 안의 숨은 포인터(vptr)가 클래스별 vtable을 가리키고, 그 표에서 함수 포인터를 찾아 호출한다",
    "호출할 때마다 런타임 타입 정보(RTTI)의 타입 이름 문자열을 비교해 실제 클래스를 알아낸 뒤 그 구현으로 간다",
    "함수 이름을 문자열 키로 해시 표에서 검색해 그 클래스의 구현을 찾으므로 이름이 길수록 호출이 느려진다",
    "컴파일 시점에 정적 타입으로 함수가 결정되고, 파생 클래스의 구현은 포인터 타입을 파생으로 바꿔야만 호출된다" ] },
  { k: "cpp-stl-map", o: [
    "unordered_map 은 O(1) 을 보장하고 map 은 O(log n) 이라 순서가 필요 없으면 언제나 unordered_map 이 빠르다",
    "map 이 해시 기반이고 unordered_map 이 트리 기반이라 이름이 반대로 붙어 있어 초보가 헷갈리기 쉽다",
    "map은 정렬 순서를 유지하며 O(log n), unordered_map은 순서가 없고 평균 O(1)·최악 O(n)이다",
    "둘 다 키의 정렬 순서를 유지하며, unordered_map 은 삽입 순서까지 함께 기억한다는 점만 다르다" ] },
  { k: "cpp-const-ptr", o: [
    "둘 다 대상과 포인터가 모두 const 라 *p 도 q 도 바꿀 수 없고, 표기만 옛 방식과 새 방식으로 다르다",
    "p 는 포인터 자체가 const 라 다른 곳으로 옮길 수 없고, q 는 가리키는 대상이 const 라 *q 를 바꿀 수 없다",
    "둘은 완전히 같은 뜻이며 const 의 위치는 읽기 편의일 뿐이라 어느 쪽을 써도 컴파일러가 같게 취급한다",
    "p는 가리키는 대상이 const라 *p를 바꿀 수 없고, q는 포인터 자체가 const라 q를 다른 곳으로 옮길 수 없다" ] },
  { k: "cpp-raii-lockguard", o: [
    "생성자에서 뮤텍스를 잠그고 소멸자에서 자동으로 풀어 예외 경로에서도 데드락을 막는다",
    "프로세스 전체에 전역 락을 하나만 유지해 어떤 스레드가 잡아도 같은 뮤텍스를 쓰기 때문",
    "뮤텍스를 아예 사용하지 않고 원자 변수로 임계 구역을 보호하므로 잠금 해제가 필요 없기 때문",
    "잠금은 자동이지만 해제는 unlock() 을 직접 불러야 하므로 소유권을 프로그래머가 관리하기 때문" ] },
  { k: "cpp-tpl-header", o: [
    "링커가 템플릿 심볼을 만들 수 없어 .cpp 에 두면 링크 단계에서 항상 실패하기 때문",
    "인스턴스화하는 지점에서 정의 전체가 보여야 컴파일러가 코드를 생성할 수 있기 때문",
    "헤더는 전처리 단계에서 한 번만 읽히므로 .cpp 보다 컴파일이 빨라지기 때문",
    "표준이 템플릿 정의는 헤더에만 둘 수 있다고 문법적으로 강제하기 때문" ] },
  { k: "cpp-sptr-makeshared", o: [
    "weak_ptr 을 만들 수 없게 막아 주므로 제어 블록이 객체보다 오래 남는 문제가 생기지 않는다",
    "참조 카운트를 원자적으로 세지 않아 더 빠르며, 단일 스레드 전용이라는 점만 감수하면 된다",
    "객체를 힙이 아니라 스택에 할당하므로 할당 비용이 없고 함수가 끝나면 자동으로 사라진다",
    "객체와 제어 블록을 한 번의 할당으로 만들어 메모리 할당 횟수와 캐시 미스를 줄인다" ] },
  { k: "cpp-uptr-makeunique", o: [
    "make_unique 가 객체를 스택에 할당하므로 힙 할당 비용이 없고 소멸도 자동이기 때문",
    "make_unique 는 new 보다 항상 빠르며 할당기를 직접 호출해 오버헤드가 없기 때문",
    "make_unique 만 사용자 정의 삭제자를 지원하므로 파일이나 소켓 같은 자원을 감쌀 수 있기 때문",
    "타입을 두 번 쓰지 않아도 되고, 함수 인자에서 발생할 수 있는 예외 누수 문제를 피할 수 있다" ] },
  { k: "cpp-erase-loop", o: [
    "if (조건) it = m.erase(it); else ++it; — erase가 반환한 다음 반복자를 받는다",
    "for (auto&amp; kv : m) if (조건) m.erase(kv.first); — 범위 기반 for 가 반복자를 다시 얻는다",
    "if (조건) m.erase(it); ++it; — 지운 뒤 증가시키면 다음 원소로 자연스럽게 넘어간다",
    "if (조건) m.erase(it++); else m.clear(); — 후위 증가가 옛 반복자를 넘겨 안전하다" ] },
  { k: "cpp-uptr-release", o: [
    "release 가 객체를 해제하고 reset 이 원시 포인터를 반환한다 — 이름 그대로 동작한다",
    "둘 다 객체를 해제하며, release 는 해제 뒤 nullptr 을 넣고 reset 은 새 객체를 넣는다",
    "release는 소유권을 포기하고 원시 포인터를 반환하며 해제하지 않고, reset은 기존 객체를 해제한다",
    "둘 다 객체를 해제하지 않으며, 실제 해제는 unique_ptr 의 소멸자에서만 한 번 일어난다" ] },
  { k: "cpp-elision-nrvo", o: [
    "C++17 부터 표준이 보장한다 — 복사 생략이 의무로 바뀌어 지역 변수 반환에서도 생략이 항상 일어난다",
    "반환 타입이 포인터나 참조일 때만 적용되며, 값 타입을 반환하면 반드시 복사나 이동이 일어난다",
    "컴파일러가 절대 수행하지 않는 이론상의 최적화라 지역 변수 반환에는 항상 이동 생성자가 불린다",
    "지역 변수를 이름으로 반환할 때의 최적화로, 표준이 허용하지만 강제하지는 않는다" ] },
  { k: "cpp-stl-list", o: [
    "원소를 항상 정렬 상태로 유지하므로 삽입마다 자리를 찾아 비교하는 비용이 들기 때문",
    "링크 조작 자체가 O(n) 이라 노드를 끊고 잇는 데 원소 수만큼 시간이 들기 때문",
    "임의 접근이 O(1) 이지만 그 대신 순차 접근이 느려 반복문에서 손해를 보기 때문",
    "노드가 메모리에 흩어져 캐시 지역성이 나쁘고 노드마다 할당 비용이 들기 때문" ] },
  { k: "cpp-iter-realloc", o: [
    "push_back 이 재할당에 실패하면 bad_alloc 을 던지는데 그 예외를 잡지 않기 때문",
    "push_back이 재할당을 일으키면 p가 해제된 버퍼를 가리켜 미정의 동작이 된다",
    "p 가 const 포인터가 아니라 벡터 내부 원소를 밖에서 바꿀 수 있게 열어 두기 때문",
    "벡터가 정렬되지 않은 상태라 p 가 가리키는 원소의 위치를 믿을 수 없기 때문" ] },
  { k: "cpp-vf-constmismatch", o: [
    "시그니처가 달라 재정의가 아니라 별개의 함수가 되고, 기반 포인터 호출은 기반 구현으로 간다",
    "const 는 시그니처에 들어가지 않으므로 정상 재정의가 되어 런타임에 파생 버전이 호출된다",
    "컴파일러가 기반의 const 를 파생 선언에 자동으로 보정해 넣어 정상 재정의로 취급한다",
    "기반과 이름은 같은데 시그니처가 다른 가상 함수는 허용되지 않아 컴파일 에러가 발생한다" ] },
  { k: "cpp2-cc-volatile", o: [
    "volatile 은 접근마다 내부적으로 뮤텍스를 잡았다 놓으므로 동기화는 되지만 너무 느려 실용성이 없기 때문",
    "volatile 은 C++11 에서 삭제된 키워드라 새 표준으로 컴파일하면 문법 오류가 나고 atomic 으로 바꿔야 하기 때문",
    "volatile 변수는 여러 스레드에서 동시에 접근할 수 없도록 컴파일러가 접근 자체를 막아 주어 공유가 불가능하기 때문",
    "volatile은 원자성과 스레드 간 메모리 순서를 전혀 보장하지 않으며, 메모리 맵 하드웨어 접근용 기능이기 때문" ] },
  { k: "cpp3-constcast-ub", o: [
    "const_cast 로 벗겨도 대상이 원래 const 임을 컴파일러가 알고 있어 대입문에서 컴파일 에러가 나 막아 준다",
    "c 는 읽기 전용 메모리 페이지에 놓이므로 쓰기 순간 접근 위반이 발생해 프로그램이 즉시 죽어 버린다",
    "원래부터 const인 객체를 수정하는 것이므로 정의되지 않은 동작이다",
    "const_cast 가 const 속성을 걷어내므로 c 가 20 으로 정상적으로 바뀌고 이후 읽기도 20 을 돌려준다" ] },
  { k: "cpp3-reinterpret-roundtrip", o: [
    "정수와 포인터 사이의 변환 결과가 모든 플랫폼에서 동일하다고 표준이 정해 두어 이식성이 보장된다",
    "원래 타입으로 다시 되돌리면 원래 포인터 값을 얻는다는 왕복 정도가 보장된다",
    "변환된 포인터로 다른 타입의 값을 읽는 것이 정의된 동작이라 바이트 재해석에 안전하게 쓸 수 있다",
    "어떤 타입으로 바꿔도 그 타입으로 읽는 것이 항상 안전하며, 정렬이 어긋나도 컴파일러가 보정한다" ] },
  { k: "cpp3-virtual-base-cost", o: [
    "가상 상속을 쓰면 힙 할당이 강제되므로 스택 위에는 그런 객체를 만들어 둘 수 없게 된다",
    "가상 기반 부분의 위치를 간접적으로 찾아야 해서 접근 비용과 객체 크기가 늘고 레이아웃이 복잡해진다",
    "가상 상속을 쓰면 그 계층의 모든 멤버 함수가 가상 함수로 바뀌어 모든 호출이 vtable 을 거쳐 느려진다",
    "비용이 전혀 없으며 다이아몬드가 생길지 미리 알 수 없으므로 상속은 기본적으로 virtual 로 두는 것이 좋다" ] },
  { k: "cpp3-placement-align", o: [
    "버퍼가 0 으로 초기화되어 있어야 생성자가 멤버를 안전하게 덮어쓸 수 있다",
    "버퍼가 char 배열이 아니라 T 타입 배열로 선언되어 있어야 정렬이 맞는다",
    "버퍼 크기가 sizeof(T) 이상이고 주소가 T의 정렬 요구를 만족해야 한다",
    "버퍼가 힙에 있어야 하며 스택 배열에는 placement new 를 쓸 수 없다" ] },
  { k: "cpp2-bld-odr-header-func", o: [
    "#pragma once 가 들어 있으므로 중복 포함이 막혀 두 .cpp 어디에서도 add 가 두 번 정의되지 않아 링크가 정상이다",
    "add의 정의가 두 오브젝트 파일에 각각 생겨 duplicate symbol 링크 오류가 난다(ODR 위반)",
    "같은 함수가 두 번역 단위에 정의된 것을 컴파일러가 감지해 두 번째 .cpp 를 컴파일하는 단계에서 에러가 난다",
    "링커가 같은 이름의 정의 중 하나를 자동으로 골라 나머지를 버리므로 경고 없이 정상적으로 링크된다" ] }
] };
