module.exports = {
  track: "math", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 식은 맞는데 수가 어긋난 코드 고치기",
  source: "./dbg_math.cjs",
  lessons: [
    { t: "부호·경계·끝점이 어긋난 곳", n: 6,
      th: { sum: "수치 버그는 **양수·정상 범위·나누어떨어지는 값**에서 전부 통과한다 — 부호와 끝점을 넣어야 드러난다.",
        body: [
          { h: "방향과 경계", t: "`Math.round` 는 언제나 +∞ 쪽으로 반올림해 −2.5 가 −2 가 된다. 경계 두 개를 인자로 받는 함수는 **뒤집혀 들어오는 경우**를 정해야 하고, 분모가 0 인 순간(신규 지표·새 고객·첫 배포)은 반드시 온다. 셋 다 '알 수 없음' 을 0 으로 뭉개지 말고 **명시적으로** 표현해야 한다." },
          { h: "점의 수와 간격의 수", t: "점이 n 개면 간격은 **n−1 개**다. `(b-a)/n` 으로 나누면 마지막 점이 끝에 못 미친다. 그리고 실수 비교는 **크기에 비례한** 허용 오차가 필요하다 — 10억 근처에서는 표현 간격 자체가 1e−7 보다 크다." }],
        code: { c: "const sign = x < 0 ? -1 : 1;\nreturn sign * Math.round(Math.abs(x));\nconst step = (b - a) / (n - 1);", cap: "부호를 떼고, 간격은 하나 적게" },
        key: ["반올림 규칙은 도메인이 정한다", "뒤집힌 경계를 정한다", "점 n 개 → 간격 n−1 개", "실수 비교는 상대 오차로"] } },
    { t: "표현 범위와 평균의 종류", n: 6,
      th: { sum: "값이 커지거나 둥글어질 때 무너지는 셋 — **큰 수를 먼저 만들기**, **잘못된 평균**, **접지 않은 각도**.",
        body: [
          { h: "작게 유지하며 계산한다", t: "30! 은 정수 정확도(2⁵³)를 넘겨 조합 계산의 끝자리를 망친다. 팩토리얼을 만들지 말고 **곱하며 바로 나누면** 중간값이 언제나 작고 정확하다. 같은 원칙이 나머지 연산·확률의 곱에도 적용된다." },
          { h: "무엇이 고정되어 있는가", t: "거리가 같으면 속도의 평균은 **조화평균**이다(60·30 의 평균은 45 가 아니라 40). 표본에서 계산한 분산은 자유도 **n−1** 로 나눈다. 각도처럼 **둥근 값**은 −180~180 으로 접어야 0도 경계에서 회전 방향이 뒤집히지 않는다." }],
        code: { c: "r = r * (n - t + i) / i;                    // 단계마다 정수 유지\nconst d = ((a - b) % 360 + 540) % 360 - 180;", cap: "접고 나서 견준다" },
        key: ["팩토리얼을 만들지 않는다", "속도의 평균은 조화평균", "표본분산은 n−1", "둥근 값은 접는다"] } },
  ],
};
