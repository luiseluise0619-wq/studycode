/* python 트랙 — 정답이 5자 이상 단독 최장인 48문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "python", fixes: [
  { k: "is vs ==", o: [
    "둘 다 True — 원인: 값이 같으면 인터프리터가 같은 객체로 합쳐 두므로 id 도 같아짐 / 해결: 리스트처럼 값이 같은 것은 어느 연산자로 비교해도 결과가 같으니 둘을 구분해 쓸 필요가 없고 취향대로 고른다",
    "== 는 False, is 는 True — 원인: 두 리스트가 같은 id 를 가리키지만 원소 비교는 참조를 따라가지 않아 값이 다르다고 판정됨 / 해결: 동일성이 값보다 우선하므로 리스트 비교에는 is 만 믿고 쓰면 된다",
    "둘 다 False — 원인: 리스트는 가변 객체라 값 비교가 정의되지 않고 서로 다른 객체이기도 함 / 해결: 리스트를 비교하려면 tuple 로 바꾸거나 원소를 하나씩 순회해 직접 견주는 함수를 따로 써야 한다",
    "a == b는 True(값 동등), a is b는 False(다른 객체) — 원인: ==는 __eq__로 값 비교, is는 동일 객체(id) 비교 / 해결: 값 비교엔 ==, 동일성 비교엔 is를 구분해 사용" ] },
  { k: "롤백 불가 상황", o: [
    "마이그레이션을 코드와 같은 배포에 강하게 묶는다 — 원인: 한 번에 처리하는 편이 편리하고 배포 단위가 하나라 추적이 쉬움 / 해결: 스키마와 코드를 같은 배포로 한꺼번에 반영하고, 문제가 나면 둘을 함께 되돌리는 절차를 문서로 둔다",
    "롤백을 포기하고 앞으로만 고친다 — 원인: 파괴적 변경은 어차피 되돌릴 수 없으므로 롤백 절차 자체가 무의미함 / 해결: 장애가 나면 핫픽스를 앞으로 배포해 견디고, 롤백 절차는 아예 만들지 않아 팀의 혼선을 줄인다",
    "배포 직전마다 DB 전체를 백업한다 — 원인: 컬럼 삭제는 데이터 유실을 뜻하므로 되돌릴 사본이 있어야 함 / 해결: 스냅샷을 떠 두고 문제가 나면 코드와 함께 DB 를 통째로 복원해 배포 전 상태로 되돌리고 그 사이 쓰기는 포기한다",
    "확장-수축(expand-contract)으로 스키마 변경을 하위 호환 단계로 쪼갠다 — 원인: 파괴적 변경이 롤백을 막음 / 해결: 먼저 추가만 하고 양쪽 호환 배포 후, 안정화되면 제거해 언제든 되돌릴 수 있게 함" ] },
  { k: "type: ignore", o: [
    "구체적 오류 코드를 지정하고(예: # type: ignore[arg-type]) 이유를 주석으로 남긴 뒤 최소 범위로만 쓴다",
    "mypy 설정에서 검사를 끄는 것과 같은 효과이므로 오류가 나는 줄마다 자유롭게 붙여 초록불을 유지하고 넘어간다",
    "임시 방편이므로 코드 리뷰 대상에서 제외하고, 나중에 타입을 정리하는 작업을 따로 잡아 한꺼번에 걷어내면 된다",
    "오류가 많은 파일은 상단에 # type: ignore 를 통째로 붙여 조용하게 만들고 오류가 적은 다른 파일부터 고친다" ] },
  { k: "암묵적 Optional", o: [
    "기본값 None이 int 선언과 맞지 않으므로 timeout: int | None = None 으로 명시해야 한다",
    "int 처럼 불변 기본형은 기본값을 가질 수 없어 호출할 때마다 반드시 값을 넘겨야 하기 때문",
    "기본값에는 None 을 쓸 수 없고 0 같은 같은 타입의 값을 써야 검사기가 통과시키기 때문",
    "호출할 때 인자를 생략하면 int 자리에 None 이 들어가 런타임에 곧바로 TypeError 가 나기 때문" ] },
  { k: "컴프리헨션 vs 제너레이터", o: [
    "앞쪽은 1천만 개짜리 리스트를 메모리에 만들고, 뒤쪽은 값을 하나씩 만들어 넘기므로 메모리 사용이 훨씬 적다",
    "둘 다 sum 에 넘기기 전에 1천만 개짜리 리스트를 만들며, 괄호와 대괄호는 같은 리스트 표기의 문법 차이일 뿐이다",
    "뒤쪽은 sum 안에 괄호가 한 겹뿐이라 함수 호출로 해석되지 않아 문법 오류가 나고, 앞쪽만 정상 실행된다",
    "뒤쪽은 값을 하나씩 만드는 상태 객체를 유지하므로 메모리를 더 쓰는 대신, 리스트를 만들지 않아 실행 속도는 더 빠르다" ] },
  { k: "CPU 작업 선택", o: [
    "단일 스레드에서 async def 로 감싸 이벤트 루프에 올리면 픽셀 계산이 서로 겹쳐 처리되어 빨라진다",
    "multiprocessing(또는 ProcessPoolExecutor)으로 코어 수만큼 프로세스를 나눈다",
    "threading 으로 스레드를 코어 수만큼 만들어 나눠 처리하면 스레드가 코어마다 하나씩 병렬로 돈다",
    "asyncio 로 이미지마다 코루틴을 만들어 gather 하면 대기 없이 여러 이미지가 동시에 계산된다" ] },
  { k: "최솟값 사용", o: [
    "최솟값이 실제 서비스에서 사용자가 겪는 지연 시간과 가장 가깝기 때문에 그 값을 기준으로 삼는다",
    "다른 프로세스나 스케줄링 방해는 시간을 늘리는 쪽으로만 작용하므로 최솟값이 잡음에 덜 오염된다",
    "평균은 모든 표본을 더하고 나누는 계산 비용이 커서 반복 횟수가 많으면 측정 자체를 느리게 만든다",
    "최솟값은 표본 수와 무관하게 수렴하는 통계량이라 반복을 조금만 해도 평균보다 정확하기 때문" ] },
  { k: "__slots__ 효과", o: [
    "메모리 사용량은 그대로지만 속성 접근이 딕셔너리 조회를 거치지 않아 언제나 두 배 빨라진다",
    "클래스 변수를 선언할 수 없게 되고 그 클래스를 상속하는 것도 금지되어 확장이 막힌다",
    "인스턴스 __dict__가 사라져 메모리가 줄지만, 선언하지 않은 속성을 나중에 붙일 수 없다",
    "인스턴스마다 딕셔너리가 사라져 메모리가 줄고, 동적 속성 추가도 그대로 가능해 대가가 없다" ] },
  { k: "타입 좁히기", o: [
    "런타임에 u 가 None 이 아님을 확인한 뒤에야 mypy 가 실행 결과를 보고 통과시키기 때문",
    "return 문이 있는 함수는 mypy 가 반환 이후의 문장을 검사하지 않고 건너뛰기 때문",
    "mypy가 is None 분기와 조기 반환을 이해해 그 아래에서 u를 User로 좁히기 때문",
    "속성 접근은 동적으로 해석되므로 mypy 가 타입 검사 대상에서 아예 제외하기 때문" ] },
  { k: "slots트레이드", o: [
    "클래스 메서드와 정적 메서드를 만들 수 없게 되고, 상속까지 제한되어 데이터 클래스로만 쓸 수 있다",
    "속성 접근이 슬롯 인덱스 계산을 거치므로 인스턴스가 더 느려지고, 메모리를 아낀 만큼 시간을 잃는다",
    "__slots__ 를 정의한 클래스는 상속이 전면 금지되어 파생 클래스를 만들면 TypeError 가 난다",
    "선언되지 않은 임의 속성을 동적으로 추가할 수 없고, __dict__/약한참조 등에 제약이 생긴다" ] },
  { k: "메모리 누수 진단", o: [
    "시간에 따른 메모리 추이를 모니터링해 계속 증가하는지(누수) 확인하고, tracemalloc·객체 수 스냅샷으로 어떤 객체가 안 줄어드는지 좁힌다 — 원인: 시간 비례 증가는 누수 신호 / 해결: 증가 패턴 확인 후 원인 객체를 추적해 참조를 끊음",
    "이전 버전으로 즉시 롤백한다 — 원인: 배포 직후 시작됐으니 새 코드가 원인이며, 되돌리면 증상이 사라지므로 더 볼 것이 없음 / 해결: 롤백으로 끝내고, 어느 커밋이 문제였는지는 다음 배포 때 하나씩 다시 배포해 보며 증상이 재현되는지로 확인한다",
    "일단 인스턴스 메모리를 두 배로 늘린다 — 원인: 단순히 할당된 메모리가 부족해서 생긴 문제이며 코드 자체에는 이상이 없음 / 해결: 서버 스펙을 증설해 OOM 이 나지 않는 선까지 올리고 재시작 주기를 늘려 운영으로 흡수하며 코드는 손대지 않는다",
    "로그 파일을 지우고 로그 레벨을 낮춘다 — 원인: 3시간 동안 쌓인 로그 버퍼와 파일 핸들이 메모리를 차지해 OOM 을 부름 / 해결: 로그를 ERROR 만 남기고 파일을 주기적으로 비워 메모리를 회수하며, 그래도 부족하면 로깅을 아예 끈다" ] },
  { k: "프로세스 메모리", o: [
    "리스트에 담긴 값이 피클 가능하지 않아 자식에서 부모로 돌아오지 못하기 때문",
    "자식 프로세스에서는 전역 객체가 읽기 전용이라 append 호출이 조용히 무시되기 때문",
    "부모가 자식보다 먼저 끝나 자식이 추가한 값이 반영될 시점이 없었기 때문",
    "프로세스마다 메모리 공간이 분리되어 있어 자식의 변경이 부모에 반영되지 않기 때문" ] },
  { k: "raise from", q: "구문의 목적", o: [
    "두 예외를 하나의 예외 객체로 합쳐 던진다",
    "err 를 처리된 것으로 표시해 조용히 무시한다",
    "err 의 트레이스백을 지우고 새 예외만 남긴다",
    "원래 예외(err)를 원인으로 연결해 추적을 남긴다" ] },
  { k: "asgi-vs-wsgi", o: [
    "ASGI 위에서 돌며 async/await 기반 단일 이벤트 루프로 동시 I/O를 처리한다",
    "WSGI 위에서 돌며 요청마다 스레드를 하나씩 배정하는 방식으로만 동시성을 낸다",
    "CGI 방식으로 요청마다 프로세스를 새로 띄우므로 동시성은 프로세스 수로 정해진다",
    "ASGI 위에서 돌지만 내부적으로는 WSGI 로 변환해 실행하므로 결국 Flask 와 같다" ] },
  { k: "middleware-blocking", o: [
    "미들웨어는 워커 프로세스를 하나만 쓰도록 되어 있어 부하가 몰리면 그 워커 하나가 병목이 되기 때문에",
    "requests 라이브러리는 미들웨어 안에서 사용이 금지되어 있어 호출마다 경고와 재시도 지연이 붙기 때문에",
    "미들웨어는 요청을 순차 처리하도록 설계돼 있어 앞 요청의 감사 로그가 끝나야 다음 요청이 들어오기 때문에",
    "미들웨어가 이벤트 루프 스레드에서 실행되는데 동기 blocking 호출이 루프를 막아서" ] },
  { k: "GIL과 CPU 바운드", o: [
    "스레드마다 인터프리터 상태가 복제되어 메모리가 스레드 수만큼 는다",
    "GIL 을 두 스레드가 동시에 요구하면 교착이 나 프로그램이 종료된다",
    "GIL 이 잡혀 있는 동안에는 새 스레드가 아예 생성되지 않는다",
    "코어가 여러 개여도 병렬 계산 이득이 거의 없다" ] },
  { k: "gather", o: [
    "코루틴 하나에서 예외가 나도 gather 가 삼켜 주므로 나머지 결과만 안전하게 받는다",
    "여러 대기 시간이 겹쳐 전체 소요 시간이 가장 느린 작업 정도로 줄어든다",
    "CPU 코어를 여러 개 쓰게 되어 계산 자체가 빨라지고 총 CPU 시간도 줄어든다",
    "각 코루틴이 별도 프로세스에서 실행되어 GIL 의 영향을 받지 않게 된다" ] },
  { k: "5xx 롤백 판단", o: [
    "트래픽을 두 배로 늘려 재현한다 — 원인: 8% 는 특정 조건에서만 나는 산발적 오류라 부하를 높여야 패턴이 보임 / 해결: 부하 테스트 도구로 프로덕션에 요청을 더 보내 오류 로그를 충분히 모은 뒤 원인을 좁힌다",
    "원인을 완전히 규명할 때까지 프로덕션을 그대로 둔다 — 원인: 조사가 먼저라는 판단이며 롤백하면 재현 환경이 사라짐 / 해결: 며칠에 걸쳐 로그를 보며 디버깅하고, 롤백은 원인을 알고 난 뒤에 결정해도 늦지 않다",
    "먼저 즉시 롤백해 사용자 영향을 멈춘 뒤, 안전한 상태에서 로그·트레이스로 원인을 분석한다 — 원인: 배포와 급증의 시간적 상관 / 해결: 영향 차단(롤백)을 조사보다 앞세우고 근본원인은 이후 분석",
    "에러 로그의 레벨을 올려 조용하게 만든다 — 원인: 새 버전이 경고성 오류를 5xx 로 잘못 분류해 수치만 부풀림 / 해결: 로깅 설정을 고쳐 지표를 정상으로 돌리고, 실제 사용자 불만이 들어오면 그때 조사한다" ] },
  { k: "MRO 순서", o: [
    "['D', 'A', 'B', 'C', 'object'] — 원인: 공통 부모를 먼저 확정해야 하위 클래스가 안전함 / 해결: A 를 D 바로 뒤에 두고 B·C 는 그 뒤에 선언 순서대로 늘어놓는다",
    "['D', 'B', 'A', 'C', 'object'] — 원인: 깊이 우선 탐색으로 왼쪽 가지를 끝까지 훑은 뒤 오른쪽으로 넘어감 / 해결: B 다음에 B 의 부모 A 를 두고, 그 뒤에 C 를 이어 붙인다",
    "['D', 'C', 'B', 'A', 'object'] — 원인: 오른쪽에 선언된 부모가 더 나중에 정의되어 우선순위가 높음 / 해결: C 를 앞에 두고 B, 그다음 공통 부모 A 순서로 정한다",
    "['D', 'B', 'C', 'A', 'object'] — 원인: C3 선형화가 자식→부모, 좌→우 순서와 단조성을 지킴 / 해결: A는 B·C 뒤에 한 번만 오고 마지막에 object" ] },
  { k: "__slots__ 무력화", o: [
    "__slots__ 를 튜플이 아니라 리스트로 선언하면 무시되고 __dict__ 가 그대로 만들어지기 때문",
    "클래스에 메서드가 하나라도 있으면 메서드 바인딩을 위해 __dict__ 가 반드시 필요하기 때문",
    "__init__ 에서 self 에 값을 대입하는 순간 __dict__ 가 만들어져 슬롯이 무시되기 때문",
    "부모 클래스가 __slots__를 정의하지 않아 상속된 __dict__가 그대로 남기 때문" ] },
  { k: "라이브러리 basicConfig", o: [
    "루트 로거에 핸들러를 붙여 애플리케이션이 의도한 로깅 설정을 덮어쓰거나 중복 출력하게 만든다",
    "호출할 때마다 핸들러 목록을 다시 훑으므로 성능이 조금 느려질 뿐 출력에는 부작용이 없다",
    "라이브러리 임포트 시점에 로깅 설정이 두 번 이루어져 ImportError 가 나고 모듈 임포트가 실패한다",
    "루트 로거가 라이브러리 소유가 되어 애플리케이션 쪽 핸들러가 붙지 않고 라이브러리 로그만 남는다" ] },
  { k: "cumtottime", o: [
    "함수 자체 연산이 병목이므로 이 함수 내부의 루프와 계산 로직을 최적화해야 한다",
    "tottime 과 cumtime 이 크게 다르면 프로파일러 측정 오류이므로 다시 재고 무시한다",
    "재귀 호출이 없는 함수라는 뜻이므로 하위 호출을 볼 필요 없이 본문만 살피면 된다",
    "함수 자체는 빠르고, 이 함수가 호출하는 하위 함수들이 대부분의 시간을 쓴다" ] },
  { k: "코루틴 호출 결과", o: [
    "코루틴 객체 (&lt;coroutine object ...&gt;)",
    "TypeError — await 없이 코루틴을 호출할 수 없다",
    "1 — 본문이 실행되어 반환값이 그대로 출력된다",
    "None — 실행이 예약만 되고 값은 아직 없다" ] },
  { k: "타임아웃", o: [
    "재시도를 즉시 무한 반복한다 — 원인: 일시적 실패는 곧 회복되므로 포기하는 쪽이 손해 / 해결: 실패한 호출을 간격 없이 계속 다시 보내 응답이 오는 순간을 놓치지 않게 하고, 재시도 횟수 제한은 두지 않는다",
    "그 API 를 신뢰하고 무한히 기다린다 — 원인: 언젠가는 응답이 올 것이라는 판단이며 끊으면 결과를 잃음 / 해결: 타임아웃 없이 계속 기다리고, 응답이 오면 정상 처리되므로 대기 자체는 문제로 보지 않는다",
    "워커 수를 크게 늘린다 — 원인: 대기 중인 워커가 많아 새 요청을 받을 워커가 모자람 / 해결: 스레드와 프로세스를 수 배 증설해 몇 개가 외부 호출에 묶여 있어도 나머지가 요청을 계속 받게 한다",
    "모든 외부 호출에 합리적인 타임아웃을 설정한다 — 원인: 타임아웃 없는 호출이 스레드를 영구 점유 / 해결: connect·read 타임아웃으로 실패를 빨리 확정해 리소스를 회수" ] },
  { k: "raise from", q: "ConfigError", o: [
    "새 예외를 만드는 순간 원본 예외 e 를 자동으로 로그 파일에 기록한다",
    "새 예외의 __cause__에 원본 예외를 연결해 원인 관계를 명시한다",
    "원본 예외 e 를 그 자리에서 처리한 것으로 보고 호출자에게 전달하지 않는다",
    "원본 예외의 트레이스백을 지우고 새 예외 하나만 남겨 출력을 짧게 만든다" ] },
  { k: "BaseException", o: [
    "BaseException 은 인스턴스를 만들 수 없는 최상위 클래스라 except 절에 쓰면 문법 오류가 나서 실행되지 않는다",
    "BaseException 은 ValueError 같은 일반 예외를 잡지 못해 정작 필요한 오류를 놓치고 프로그램이 죽게 된다",
    "BaseException 은 예외 계층 전체를 훑으며 매칭하므로 예외 처리 속도가 눈에 띄게 느려져 성능이 나빠진다",
    "BaseException은 KeyboardInterrupt와 SystemExit까지 잡아 프로세스 중단·종료를 막아 버린다" ] },
  { k: "예외 계층", q: "베이스 예외", o: [
    "공통 부모를 두면 예외 객체가 슬롯을 공유해 생성 비용과 메모리가 줄어든다",
    "타입 검사기가 예외 발생 여부를 강제로 검사해 주므로 처리 누락을 막을 수 있다",
    "호출자가 except AppError 하나로 우리 코드가 낸 오류만 골라 처리할 수 있다",
    "베이스 예외에 핸들러가 붙어 하위 예외가 모두 자동으로 로깅된다" ] },
  { k: "cpu-task-offload", o: [
    "uvicorn 의 keep-alive 시간을 늘려 연결이 끊기지 않게 하면 느린 응답도 완료된다",
    "무거운 CPU 작업을 워커큐로 오프로딩하고 API는 작업 접수 후 즉시 응답한다",
    "핸들러를 async def 로만 바꾸면 CPU 작업도 이벤트 루프가 겹쳐 처리해 빨라진다",
    "리사이즈를 미들웨어로 옮기면 핸들러 앞에서 처리되어 응답 지연이 사라진다" ] },
  { k: "layered-direction", o: [
    "SQL 은 화면과 가장 가까운 프레젠테이션 계층에 있어야 하므로 위치가 틀렸다",
    "문제 없다 — 도메인이 직접 쿼리하면 계층을 거치지 않아 성능에 유리하다",
    "영속성 관심사가 도메인 계층으로 새어들어 계층 분리가 깨졌다",
    "도메인 계층은 데이터의 주인이므로 DB 를 직접 다루는 것이 정상이다" ] },
  { k: "지연 포매팅", o: [
    "f-string 방식과 달리 인자를 자동으로 이스케이프해 주므로 더 안전하다",
    "해당 레벨이 비활성일 때 문자열 포매팅 비용 자체를 치르지 않는다",
    "%s 인자 방식은 로그 인젝션을 원천 차단하도록 값을 검증해 준다",
    "f-string 과 완전히 동일한 시점에 포매팅되며 표기 취향 차이일 뿐이다" ] },
  { k: "__name__ 로거", o: [
    "이름이 짧아져 로그 출력 속도가 눈에 띄게 빨라지고 메모리도 줄어든다",
    "모듈 이름과 같은 파일에 핸들러가 자동으로 연결되어 설정이 필요 없다",
    "모듈 경로 기반 계층이 생겨 패키지 단위로 레벨과 필터를 조정할 수 있다",
    "루트 로거를 대체해 다른 모듈의 로거를 전부 무력화해 우리 로그만 남는다" ] },
  { k: "iterable vs iterator", o: [
    "iterator 만 __iter__ 를 가지고 iterable 은 __next__ 만 가져 역할이 서로 바뀌어 있다",
    "iterable 은 __next__ 만 가지고 for 문이 그 메서드를 반복 호출해 값을 하나씩 꺼낸다",
    "둘은 완전히 같은 개념이며 문맥에 따라 이름만 다르게 부를 뿐 메서드 구성도 같다",
    "iterable은 __iter__를, iterator는 __iter__와 __next__를 모두 가진다" ] },
  { k: "bare except", o: [
    "bare except는 KeyboardInterrupt/SystemExit까지 모든 예외를 삼켜 원인 파악과 정상 종료를 막는다. 잡을 예외를 구체화하고 로깅 후 적절히 재발생/처리한다",
    "try 블록이 한 줄뿐이라 except 가 무의미하므로 try 를 제거하면 예외가 그대로 올라가 로그에 남고, 결제 실패도 호출자가 알게 되어 조용한 실패가 저절로 사라진다",
    "return None 대신 return False 를 반환하면 호출자가 실패를 구분할 수 있고, 거짓 값을 로그 핸들러가 잡아 기록하므로 예외를 다시 던지거나 잡는 범위를 좁힐 필요가 없어진다",
    "except 가 너무 좁아 일부 예외를 놓치는 것이므로 except (ValueError, KeyError) 처럼 잡는 범위를 넓히면 로그가 남고 조용한 실패도 함께 사라진다" ] },
  { k: "plaintext secret", o: [
    "비밀을 소스/저장소에서 제거해 환경변수·시크릿 매니저로 옮기고, 이미 노출됐으므로 해당 자격증명을 즉시 폐기·교체(로테이션)한다",
    "변수 이름을 _DATABASE_PASSWORD 로 바꿔 눈에 덜 띄게 감추고, 커밋 메시지에서도 언급하지 않도록 해 저장소를 훑는 사람이 찾기 어렵게 만든다",
    "비밀번호를 rot13 이나 base64 로 변환해 커밋하고 앱이 복원해 쓰게 하면, 저장소를 보는 사람이 평문을 바로 읽을 수 없으므로 노출 위험이 사라진다",
    "settings.py 를 .gitignore 에 넣고 저장소에서 파일만 지우면 이력에서도 사라지므로, 비밀번호는 그대로 두고 파일 관리만 바꾸면 조치가 끝난다" ] },
  { k: "누수 흔한 원인", o: [
    "요청마다 스레드가 새로 생기고 끝나지 않아서 — 원인: 워커 스레드가 캐시를 잡은 채 종료되지 않음 / 해결: 스레드 풀 크기를 줄이고 요청이 끝나면 스레드를 강제로 정리한다",
    "무한 성장 캐시가 도달 가능한 상태로 남아 GC 대상이 아님 — 원인: 참조가 살아 있으면 수거 불가 / 해결: 크기 제한·TTL 캐시(LRU)로 오래된 항목을 축출",
    "GC 가 순환 참조를 못 풀어서 — 원인: 딕셔너리 값이 서로를 가리키면 세대별 수집기가 회수를 미룸 / 해결: gc.collect() 를 주기적으로 호출해 강제로 수거한다",
    "딕셔너리 자체가 무거워서 — 원인: dict 는 해시 표를 미리 넉넉히 잡아 키가 늘수록 두 배씩 커짐 / 해결: 지우는 로직 없이 list 나 tuple 로 바꿔 저장하면 늘지 않는다" ] },
  { k: "Any 전염", o: [
    "Any 로 표시된 값은 런타임에 실제 타입으로 자동 변환되어 검사 없이도 안전하게 쓰인다",
    "해당 변수만 영향을 받고 파생된 값은 정확히 추론되므로 오염이 다른 곳으로 번지지 않는다",
    "Any 값을 거쳐 파생된 표현식도 Any가 되어 그 경로의 타입 검사가 조용히 사라진다",
    "mypy 가 Any 를 발견하는 즉시 오류를 내며 실행을 막으므로 반드시 구체 타입으로 바꿔야 한다" ] },
  { k: "defaultdict 그룹", o: [
    "키를 넣는 순서대로 자동 정렬해 주어 city 별로 모아 보기 편해서",
    "없는 키에 자동으로 빈 리스트가 생겨 append 가능",
    "키가 없을 때 값을 자동으로 합산해 주어 집계가 한 줄로 끝나서",
    "같은 city 를 가진 중복 레코드를 자동으로 제거해 주어서" ] },
  { k: "most_common 형태", o: [
    "(값, 개수) 튜플 2개가 담긴 리스트",
    "개수가 가장 많은 값 하나만 돌려준다",
    "상위 2개 항목의 개수를 합한 정수",
    "빈도순으로 정렬된 값만 담긴 리스트" ] },
  { k: "parametrize 이점", o: [
    "첫 실패에서 즉시 나머지 케이스를 건너뛰어 전체 실행 시간을 아끼고 실패 원인을 하나로 좁혀 준다",
    "테스트 실행 시간이 케이스 수와 무관하게 상수 시간으로 줄어들어 전체 스위트가 빨라진다",
    "케이스마다 필요한 픽스처를 인자 값에서 자동으로 생성해 주므로 픽스처 함수를 따로 만들 필요가 없다",
    "케이스마다 독립된 테스트로 수집되어 하나가 실패해도 나머지 케이스의 결과를 모두 확인할 수 있다" ] },
  { k: "gunicorn-uvicorn-worker", o: [
    "--workers 4 (기본 sync 워커) — 코어마다 프로세스가 하나씩 돈다",
    "--workers 4 --worker-class uvicorn.workers.UvicornWorker",
    "--threads 8 (worker 1개) — 스레드가 이벤트 루프를 나눠 쓴다",
    "--worker-class gthread --workers 1 로 스레드 워커 하나만 띄운다" ] },
  { k: "middleware-vs-depends", o: [
    "미들웨어는 요청 진입부터 응답 반환까지 전 과정을 감싸 응답 객체를 수정할 수 있어서",
    "미들웨어는 라우팅 앞에서 실행되어 Depends 보다 항상 빠르고 오버헤드가 적어서",
    "Depends 는 async 함수를 지원하지 않아 응답이 끝나는 시점을 잴 수 없어서",
    "Depends 는 요청 헤더를 읽을 수 없어 처리 시간을 기록할 자리가 없어서" ] },
  { k: "GIL과 I/O", o: [
    "I/O 가 끝날 때까지 GIL 을 잡고 있어 다른 스레드가 돌지 못한다",
    "I/O 를 만나면 자동으로 새 프로세스를 만들어 그쪽에서 처리한다",
    "대기 중 GIL을 놓아 다른 스레드가 실행될 수 있음",
    "GIL 이 I/O 자체를 막아 한 번에 한 스레드만 파일을 열 수 있다" ] },
  { k: "to_thread", o: [
    "함수 실행을 취소 가능한 태스크로 이벤트 루프에 등록만 하고 실행은 나중으로 미룬다",
    "새 프로세스를 띄워 CPU 병렬 처리를 하고 결과를 피클로 돌려받아 await 로 받는다",
    "함수를 코루틴으로 자동 변환해 단일 스레드에서 실행하므로 스레드를 새로 만들지 않는다",
    "블로킹 함수를 별도 스레드에서 실행하고 그 결과를 await로 받아 이벤트 루프를 막지 않게 한다" ] },
  { k: "string concat join", o: [
    "+= 대신 result = result + token 으로 쓰면 최적화된다 — 복합 대입은 제자리 연산을 시도하다 실패해 두 번 복사하므로 일반 대입이 복사를 한 번으로 줄인다",
    "result 를 전역으로 두면 재할당이 없어 빨라진다 — 지역 변수는 루프마다 프레임에 다시 묶이므로 복사가 한 번 더 일어나고, 전역은 한 자리에 고정된다",
    "token 이 유니코드 문자열이라 느린 것이므로 encode() 로 바이트열로 바꾼 뒤 이어붙이면 복사 비용이 사라져 빨라지고 마지막에 decode 만 하면 된다",
    "문자열은 불변이라 += 마다 새 문자열을 만들어 누적 복사가 O(n^2) 에 가깝다. ''.join(tokens) 로 한 번에 이어붙이면 O(n) 이 된다" ] },
  { k: "로그로 병목", o: [
    "로그 레벨을 ERROR 로만 낮춰 둔다 — 원인: 로그 양이 너무 많아 정작 중요한 줄을 찾기 어려움 / 해결: 에러만 기록하도록 줄이면 남은 로그만 보고도 느린 구간을 바로 특정할 수 있고, 남은 줄만 보면 병목이 바로 드러난다",
    "의심 가는 곳마다 print 로 시각을 찍는다 — 원인: 어디가 느린지 눈에 보이지 않음 / 해결: 함수 앞뒤에 print(time.time()) 을 흩뿌려 콘솔 출력을 눈으로 훑고, 느린 요청이 다시 올 때까지 기다렸다가 출력을 비교한다",
    "요청 처리 중에는 로그를 끈다 — 원인: 로그 기록 자체가 I/O 라 지연을 만들고 있을 가능성이 큼 / 해결: 로깅을 비활성화한 뒤 지연이 사라지는지 보고, 사라지면 로그가 원인이었으므로 그대로 꺼 둔 채 운영한다",
    "요청마다 correlation id를 붙이고 주요 단계별 소요 시간을 구조화 로그로 남겨, 느린 요청의 어느 구간이 오래 걸렸는지 집계한다 — 원인: 어느 단계가 느린지 불명 / 해결: 단계별 타이밍과 추적 id로 병목 구간을 데이터로 특정" ] },
  { k: "N+1 쿼리", o: [
    "DB 서버가 약해서 — 원인: 101 번의 쿼리 자체는 정상이고 처리할 하드웨어가 모자란 것 / 해결: CPU 와 메모리를 증설해 초당 처리 가능한 쿼리 수를 늘리고 커넥션 풀도 함께 키운다",
    "각 항목마다 연관 데이터를 개별 조회하는 N+1 문제 — 원인: 루프 안에서 행마다 추가 쿼리 발생 / 해결: eager loading·JOIN·IN 절로 한 번에 모아 조회",
    "인덱스가 너무 많아서 생긴 문제 — 원인: 인덱스 과다로 조회 계획이 꼬여 쿼리마다 잘못된 인덱스를 고름 / 해결: 불필요한 인덱스를 지워 쿼리 계획을 단순하게 만들고 통계를 갱신한다",
    "네트워크가 느려서 — 원인: 앱 서버와 DB 사이의 왕복 지연이 쿼리마다 더해져 101 번이 누적됨 / 해결: DB 를 같은 호스트로 옮기거나 회선을 증설해 왕복 시간을 줄인다" ] },
  { k: "서킷 브레이커", o: [
    "실패율이 임계치를 넘으면 회로를 열어 즉시 실패시키고, 일정 시간 뒤 반열림에서 소수 요청으로 회복을 탐지해 닫는다 — 원인: 죽은 의존성에 계속 호출하면 자원 낭비·전파 / 해결: 회로를 열어 빠른 실패로 보호하고 점진 재개",
    "회로는 호출마다 상태를 검사하므로 성능만 떨어뜨린다 — 원인: 정상일 때도 카운터와 타이머를 갱신하는 오버헤드가 붙음 / 해결: 재시도와 타임아웃만 두고 브레이커는 쓰지 않으며, 장애는 모니터링 경보로 사람이 대응한다",
    "실패가 이어지면 전체 서버를 종료한다 — 원인: 하위 서비스가 계속 실패하면 우리 서버도 정상이 아니라고 보아야 함 / 해결: 프로세스를 kill 해 재시작을 유도하고 오케스트레이터가 다시 띄우게 맡겨 깨끗한 상태로 시작한다",
    "실패해도 호출을 계속 보낸다 — 원인: 하위 서비스가 언젠가 회복되면 그때부터는 성공하므로 시도를 멈출 이유가 없음 / 해결: 짧은 간격으로 계속 재시도해 회복 순간을 놓치지 않고, 실패 응답은 로그만 남기고 사용자에게는 감춘다" ] },
  { k: "메서드도 디스크립터", o: [
    "클래스가 호출 때마다 self 를 복사해 넣는다 — 원인: 인스턴스마다 메서드 본체를 따로 두어야 상태가 격리됨 / 해결: 클래스 생성 시 메서드를 인스턴스 수만큼 복제하고 각 사본에 self 를 박아 둔 뒤 호출 때 그 사본을 고른다",
    "함수 객체가 __get__ 을 가진 비데이터 디스크립터라, 인스턴스로 접근하면 self 가 묶인 메서드 객체를 돌려준다 — 원인: 함수 접근을 가로채 바인딩이 필요 / 해결: 디스크립터 프로토콜이 bound method 를 생성",
    "self 는 인터프리터가 관리하는 전역 변수다 — 원인: 현재 실행 중인 인스턴스를 어디서나 알아야 하므로 공유 저장소가 필요함 / 해결: 메서드에 진입할 때 전역 self 를 갱신하고 본문은 그 값을 읽으며, 빠져나올 때 이전 값으로 되돌린다",
    "파이썬이 self 를 마법처럼 넣어 준다 — 원인: 인스턴스 메서드 호출에는 언어 차원의 특별한 규칙이 따로 적용되기 때문 / 해결: 인터프리터가 호출 구문을 가로채 첫 인자 자리에 인스턴스를 예외적으로 끼워 넣어 주며, 함수 객체는 관여하지 않는다" ] }
] };
