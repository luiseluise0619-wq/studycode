/* python — 40자 미만 해설을 '왜 그런가 + 나머지는 왜 아닌가' 로 다시 쓴다 (1차, 1~87).
   node ex_short.cjs --apply ex_python.cjs */
module.exports = { track: "python", fixes: [
  { k: "출력 결과", ex0: "따옴표는 문자열을 감쌀 뿐", ex: "따옴표는 '여기부터 여기까지가 글자' 라는 표시일 뿐 화면에는 나오지 않는다. 그래서 Hello 만 찍힌다. \"Hello\" 처럼 따옴표까지 나오려면 문자열 안에 따옴표를 따로 넣어야 하고, print Hello 는 파이썬 3 에서 문법 오류다." },
  { k: "빈칸 채우기", ex0: "print(\"world\")", ex: "화면에 무엇을 보여 주는 함수는 print 하나뿐이다. 괄호 안에 넣은 것을 그대로 찍는다. echo 나 printf 는 다른 언어의 이름이라 파이썬에서는 '정의되지 않은 이름' 오류가 난다." },
  { k: "직접 입력", ex0: "숫자는 따옴표 없이", ex: "숫자는 따옴표 없이 print(7) 로 적는다. print(\"7\") 도 화면에는 똑같이 7 이 보이지만 그것은 글자 7 이라 나중에 더하기를 하면 이어 붙이기가 되어 버린다. 괄호를 빼면 파이썬 3 에서는 문법 오류다." },
  { k: "개념", ex0: "파이썬은 print() 로 출력", ex: "파이썬에서 화면에 글자를 내보내는 함수는 print() 다. printf() 는 C, cout 은 C++, echo 는 PHP 와 셸에서 쓰는 이름이라 파이썬에서 부르면 이름을 찾을 수 없다는 오류가 난다." },
  { k: "문자열", ex0: "문자열은 따옴표로 감싸", ex: "글자를 값으로 다루려면 따옴표로 감싼다. \"Hello\" 나 'Hello' 둘 다 된다. (Hello) 는 Hello 라는 이름의 변수를 찾으려 하고, [Hello] 는 리스트, 따옴표 없는 Hello 는 변수 이름이라 셋 다 '정의되지 않은 이름' 오류가 난다." },
  { k: "개념", ex0: "변수(variable)에 값을 저장", ex: "값에 이름을 붙여 담아 두는 것이 변수다. age = 20 처럼 쓰면 이후 age 라는 이름으로 20 을 꺼내 쓸 수 있다. 조건과 반복은 흐름을 바꾸는 문장이고, 함수는 코드 묶음에 이름을 붙이는 것이라 값 저장과는 다르다." },
  { k: "빈칸", ex0: "= 는 대입(저장) 연산자", ex: "= 는 오른쪽 값을 왼쪽 이름에 저장하는 대입 연산자다. age = 20 은 'age 에 20 을 넣어라' 는 뜻이고 수학의 '같다' 가 아니다. 같은지 비교하려면 == 를 쓴다. 이 둘을 헷갈리면 조건문에서 값이 바뀌어 버리는 버그가 난다." },
  { k: "자료형", ex0: "소수점이 있으면 float", ex: "3.14 처럼 소수점이 있는 수는 float(실수)다. 3 이면 int(정수)이고, \"3.14\" 처럼 따옴표가 있으면 str(문자열)이라 계산에 쓸 수 없다. bool 은 True/False 두 값뿐이다. type(3.14) 를 찍어 보면 float 이 나온다." },
  { k: "출력 결과", ex0: "숫자끼리 더하면 8", ex: "x 와 y 에 든 것이 숫자이므로 + 는 덧셈이고 결과는 8 이다. 53 은 \"5\" + \"3\" 처럼 문자열끼리 더했을 때 나오는 이어 붙이기 결과이고, x+y 라는 글자가 그대로 나오려면 따옴표로 감싸야 한다. 오류는 나지 않는다." },
  { k: "문자열 덧셈", ex0: "문자열 + 는 이어붙이기", ex: "따옴표로 감싼 \"2\" 와 \"3\" 은 숫자가 아니라 글자다. 글자끼리의 + 는 이어 붙이기라 23 이 된다. 5 가 나오려면 int(\"2\") + int(\"3\") 처럼 숫자로 바꿔야 하고, 2+3 이라는 글자가 나오려면 \"2+3\" 하나의 문자열이어야 한다." },
  { k: "빈칸", ex0: "else 는 그 외의 경우", ex: "if 의 조건이 거짓일 때 실행할 블록은 else 로 시작한다. elif 는 '그 외에 또 다른 조건' 을 검사할 때 쓰고, 조건이 없는 마지막 갈래가 else 다. else 뒤에는 조건을 적지 않고 콜론만 붙인다." },
  { k: "들여쓰기", ex0: "파이썬은 들여쓰기로 블록", ex: "파이썬은 중괄호 대신 들여쓰기(보통 공백 4칸)로 블록을 나눈다. 같은 깊이로 들여 쓴 줄들이 한 덩어리다. 중괄호 {} 는 파이썬에서 딕셔너리와 집합을 만드는 기호이고, 세미콜론은 한 줄에 여러 문장을 적을 때만 쓴다." },
  { k: "개념", ex0: "if 는 조건 분기", ex: "if 는 조건이 참일 때만 그 아래 블록을 실행한다. for 는 여러 값을 차례로 도는 반복, def 는 함수를 정의하는 키워드, print 는 화면에 찍는 함수라 조건 분기와는 역할이 다르다." },
  { k: "비교", ex0: "== 는 비교, = 는 대입", ex: "'같다' 를 물을 때는 == 를 쓴다. = 하나는 대입이라 a = 5 는 a 에 5 를 넣는 문장이고 참·거짓을 돌려주지 않는다. equals 는 자바의 메서드 이름이고 => 는 파이썬에 없는 기호다. 조건문에 = 를 쓰면 문법 오류가 난다." },
  { k: "출력 결과", ex0: "range(3) 은 3번 반복", ex: "range(3) 은 0, 1, 2 세 값을 만들므로 for 문이 세 번 돈다. 끝 값 3 은 포함되지 않는다. 그래서 1번도 4번도 아니고, 값이 정해져 있으니 무한 반복도 아니다. range(n) 은 n 번 반복이라고 기억하면 된다." },
  { k: "빈칸", ex0: "for x in ... 형태", ex: "리스트의 값을 하나씩 꺼내 도는 반복문은 for 다. for x in 리스트: 꼴로 쓰면 x 에 값이 차례로 들어온다. while 은 조건이 참인 동안 도는 반복이라 값을 하나씩 꺼내 주지 않고, 인덱스를 직접 다뤄야 한다." },
  { k: "개념", ex0: "range(5) 는 0~4", ex: "range(5) 는 0 부터 4 까지 다섯 개의 정수를 순서대로 만든다. 끝 값 5 는 들어가지 않는다. loop(5), for(5), count(5) 는 파이썬에 없는 함수라 이름 오류가 난다. 1 부터 시작하려면 range(1, 6) 처럼 시작값을 준다." },
  { k: "while", ex0: "while 은 조건 기반 반복", ex: "while 은 조건이 참인 동안 계속 도는 반복문이다. if 는 한 번만 검사하고 지나가고, do 와 repeat 는 파이썬에 없는 키워드다. while 안에서 조건을 바꾸지 않으면 무한 반복이 되므로 끝나는 조건을 반드시 만들어야 한다." },
  { k: "길이", ex0: "len(리스트) 로 개수", ex: "리스트의 원소 개수는 len() 으로 센다. length(), size(), count() 는 다른 언어의 이름이라 파이썬에서는 함수로 존재하지 않는다. 리스트의 count() 메서드는 있지만 특정 값이 몇 번 나오는지 세는 것이라 전체 개수와는 다르다." },
  { k: "빈칸", ex0: "append() 로 끝에 추가", ex: "arr.append(5) 는 리스트 끝에 5 를 하나 붙인다. add 는 집합(set)의 메서드이고 push 는 자바스크립트의 이름이다. 중간에 넣으려면 insert(위치, 값), 여러 개를 한꺼번에 붙이려면 extend() 를 쓴다." },
  { k: "개념", ex0: "리스트는 [1,2,3] 처럼", ex: "여러 값을 순서대로 담는 자료형은 리스트다. [1, 2, 3] 처럼 대괄호로 만들고 인덱스로 꺼낸다. 정수와 불리언은 값 하나이고, 문자열은 글자를 순서대로 담기는 하지만 글자만 담을 수 있어 여러 종류의 값을 담는 용도로는 리스트를 쓴다." },
  { k: "인덱스", ex0: "인덱스는 0부터 시작", ex: "파이썬의 인덱스는 0 부터 시작하므로 nums[0] 은 첫 번째 값 10 이다. nums[1] 이 20, nums[2] 가 30 이다. 0 이 나오는 것이 아니라 0 번째 자리의 값이 나온다는 점을 헷갈리기 쉽다. nums[3] 은 범위를 벗어나 오류가 난다." },
  { k: "출력 결과", ex0: "upper()는 모든 문자를 대문자로", ex: "upper() 는 문자열의 모든 글자를 대문자로 바꾼 새 문자열을 돌려준다. 그래서 HELLO 가 나온다. 첫 글자만 대문자로 하려면 capitalize(), 소문자로 바꾸려면 lower() 다. 원본 문자열은 바뀌지 않으므로 결과를 변수에 받아야 한다." },
  { k: "메서드", ex0: "lower()는 모든 문자를 소문자로", ex: "lower() 는 모든 글자를 소문자로 바꾼 새 문자열을 돌려주므로 python 이 된다. 대소문자를 무시하고 비교할 때 양쪽을 lower() 로 맞추는 용도로 자주 쓴다. 원본 \"Python\" 은 그대로 남는다." },
  { k: "빈칸", ex0: "len() 함수로 문자열 길이", ex: "문자열의 글자 수도 리스트처럼 len() 으로 센다. len(\"hello\") 는 5 다. length 나 size 라는 이름의 함수는 없고, 문자열에 .length 속성도 없다. 한글도 글자 하나를 1 로 센다." },
  { k: "출력 결과", ex0: "join은 구분자로 요소들을", ex: "join 은 구분자 문자열의 메서드다. \"-\".join([\"a\", \"b\", \"c\"]) 는 a, b, c 사이에 - 를 끼워 a-b-c 를 만든다. 구분자가 앞에 오는 순서가 낯설지만, 리스트가 아니라 구분자가 주인공이라고 기억하면 된다. 리스트 안에 숫자가 섞여 있으면 오류가 난다." },
  { k: "메서드", ex0: "split() 메서드가 문자열을 나눠", ex: "split() 은 문자열을 구분자 기준으로 잘라 리스트로 돌려준다. \"a,b\".split(\",\") 는 [\"a\", \"b\"] 다. join 이 그 반대 방향이다. 인자를 주지 않으면 공백을 기준으로 나누고 연속된 공백도 하나로 본다." },
  { k: "개념", ex0: "인자가 없으면 공백을 기준으로", ex: "split() 에 인자를 주지 않으면 공백(스페이스·탭·줄바꿈)을 기준으로 나누고, 연속된 공백은 하나로 본다. 쉼표나 마침표로 나누려면 split(\",\") 처럼 구분자를 넣어야 한다. 줄바꿈으로만 나누려면 splitlines() 가 따로 있다." },
  { k: "출력 결과", ex0: "인덱스 0,1,2에 해당하는 'Pyt'", ex: "s[0:3] 은 인덱스 0, 1, 2 세 글자를 가져온다. 끝 인덱스 3 은 포함되지 않아 Pyt 다. Pyth 가 나오려면 s[0:4], yth 는 s[1:4] 다. '끝은 빠진다' 는 규칙은 range 와 같다." },
  { k: "출력 결과", ex0: "-1은 마지막 문자를 가리키므로 'n'", ex: "음수 인덱스는 뒤에서부터 센다. s[-1] 은 마지막 글자 n, s[-2] 는 o 다. len(s) - 1 을 계산하지 않아도 마지막 글자를 꺼낼 수 있어 자주 쓴다. s[6] 처럼 길이와 같은 인덱스는 범위를 벗어나 오류가 난다." },
  { k: "출력 결과", ex0: "f-string은 {} 안의 변수 값을", ex: "문자열 앞에 f 를 붙이면 {} 안의 식이 값으로 바뀌어 들어간다. name 이 Kim 이므로 Hi Kim 이 된다. f 가 없으면 {name} 이라는 글자가 그대로 찍힌다. {} 안에는 변수뿐 아니라 {age + 1} 같은 식도 쓸 수 있다." },
  { k: "출력 결과", ex0: "인덱스 2부터 끝까지 잘라 'cdef'", ex: "s[2:] 는 인덱스 2 부터 끝까지다. a(0), b(1) 을 건너뛰고 c 부터 cdef 가 된다. 끝을 비우면 마지막까지라는 뜻이다. cde 가 나오려면 s[2:5], ab 는 s[:2] 다." },
  { k: "합계 출력", ex0: "제너레이터로 각 레코드의 age를", ex: "sum(r['age'] for r in rows) 는 레코드마다 age 를 꺼내 더한다. 25 + 30 + 25 = 80 이다. 괄호 안의 식은 리스트를 만들지 않고 값을 하나씩 흘려보내는 제너레이터라 큰 데이터에서도 메모리를 아낀다." },
  { k: "필드 접근", ex0: "레코드는 dict이므로 대괄호 키 접근", ex: "레코드가 딕셔너리이므로 값은 r['name'] 처럼 대괄호와 키로 꺼낸다. r.name 은 객체의 속성 접근이라 딕셔너리에는 없고, r->name 은 C 문법이다. 키가 없을 때 오류 대신 기본값을 받으려면 r.get('name') 을 쓴다." },
  { k: "필터 개수", ex0: "city가 'X'인 레코드는 a와 c", ex: "조건 r['city'] == 'X' 를 만족하는 레코드는 a 와 c 둘이다. len() 이 그 개수를 세어 2 가 된다. b 는 city 가 다르므로 걸러진다. 컴프리헨션의 if 는 조건에 맞는 것만 남긴다는 뜻이다." },
  { k: "열 추출", ex0: "각 레코드에서 name만 뽑은 리스트", ex: "[r['name'] for r in rows] 는 레코드마다 name 값만 꺼내 새 리스트를 만든다. 순서는 원래 rows 순서 그대로라 ['a', 'b', 'c'] 다. 표에서 한 열만 뽑는 것과 같은 일이다." },
  { k: "필터 표현", ex0: "if 조건을 붙인 리스트 컴프리헨션", ex: "리스트 컴프리헨션에 조건을 붙이는 문법은 [값 for 변수 in 목록 if 조건] 이다. where 는 SQL 의 말이라 문법 오류이고, [rows if ...] 는 for 가 없어 컴프리헨션이 아니다. filter(rows, ...) 는 인자 순서가 반대이고 조건도 함수여야 한다." },
  { k: "lambda 역할", ex0: "key 함수는 각 원소를 받아 비교에 쓸", ex: "key 에 넘긴 함수는 정렬할 때 각 원소를 받아 '무엇으로 비교할지' 를 돌려준다. r['age'] 를 돌려주므로 나이순으로 정렬된다. 정렬 방향은 reverse 인자가 정하고, lambda 는 새 딕셔너리를 만들거나 지우지 않는다." },
  { k: "최솟값", ex0: "min에 key를 주면 그 기준으로 최소인", ex: "min(rows, key=lambda r: r['age']) 는 각 레코드의 age 를 비교해 가장 작은 레코드 자체를 돌려준다. min(rows, 'age') 는 두 번째 인자를 비교 대상 값으로 여겨 오류가 나고, rows 는 리스트라 .min() 메서드도 ['age'] 인덱싱도 없다." },
  { k: "그룹 크기", ex0: "city 'X' 그룹에는 a와 c가 담겨", ex: "city 별로 묶으면 'X' 그룹에는 a 와 c 두 레코드가 들어간다. 그래서 len(groups['X']) 는 2 다. b 는 다른 city 라 다른 그룹에 있다. 그룹을 만들 때 setdefault 나 defaultdict(list) 를 쓰면 키가 없을 때의 처리를 줄일 수 있다." },
  { k: "그룹 내용", ex0: "X 그룹에는 a와 c가 순서대로", ex: "'X' 그룹에는 rows 를 훑은 순서대로 a, c 가 담긴다. 그래서 ['a', 'c'] 다. 그룹 안의 순서는 원본 순서를 따르므로 정렬은 따로 하지 않는 한 바뀌지 않는다." },
  { k: "평균 패턴", ex0: "합을 제너레이터로 구한 뒤 레코드 개수로", ex: "평균은 합을 개수로 나눈 것이다. sum(r['age'] for r in rows) 로 합을 구하고 len(rows) 로 나눈다. mean 과 avg 는 내장 함수가 아니고, rows 는 리스트라 rows.age 나 rows['age'] 로는 접근할 수 없다. 통계 함수가 필요하면 statistics.mean 을 쓴다." },
  { k: "출력 결과", ex0: "요소가 3개이므로 길이는 3", ex: "튜플도 리스트처럼 len() 으로 원소 개수를 센다. (1, 2, 3) 은 세 개이므로 3 이다. 튜플은 값을 바꿀 수 없다는 점만 리스트와 다르고 길이·인덱싱·순회는 같은 방식으로 한다." },
  { k: "개념", ex0: "요소 1개 튜플은 뒤에 쉼표를 붙여", ex: "튜플을 만드는 것은 괄호가 아니라 쉼표다. (5) 는 그냥 정수 5 이고, (5,) 라야 원소 하나짜리 튜플이다. tuple(5) 는 5 가 반복 가능한 값이 아니라 오류가 나고, [5] 는 리스트, {5} 는 집합이다." },
  { k: "개념", ex0: "튜플은 불변(immutable) 자료형", ex: "튜플은 한 번 만들면 원소를 바꾸거나 더하거나 지울 수 없다. 그래서 바뀌면 안 되는 값(좌표, 설정)을 담는 데 쓴다. 숫자만 담는 것은 아니고 어떤 값이든 섞어 담을 수 있으며, 비어 있을 필요도 없다." },
  { k: "개념", ex0: "튜플은 소괄호 ( )로 만든다", ex: "튜플은 (1, 2) 처럼 소괄호로 적는다. [ ] 는 리스트, { } 는 딕셔너리나 집합이다. 사실 튜플을 만드는 것은 쉼표라서 1, 2 라고만 적어도 튜플이 되지만, 읽기 쉽게 괄호를 붙이는 것이 관례다." },
  { k: "출력 결과", ex0: "집합은 중복을 허용하지 않아 {1,2,3}", ex: "set([1, 2, 2, 3]) 처럼 집합으로 바꾸면 같은 값은 하나만 남는다. 그래서 {1, 2, 3} 이 되고 길이는 3 이다. 원래 리스트의 개수 4 가 아니다. 중복을 없앨 때 set 으로 바꿨다가 다시 list 로 돌리는 방법을 자주 쓴다." },
  { k: "빈칸", ex0: "set()으로 변환하면 중복이 제거", ex: "리스트를 set() 으로 바꾸면 같은 값은 하나만 남는다. 다시 리스트가 필요하면 list(set(arr)) 로 돌린다. 다만 집합은 순서를 지키지 않으므로 원래 순서를 유지하며 중복만 없애려면 dict.fromkeys(arr) 를 쓴다." },
  { k: "개념", ex0: "집합은 중복이 없고 순서가 없는", ex: "집합은 같은 값을 두 번 담지 않고 순서도 없다. 그래서 s[0] 처럼 인덱스로 꺼낼 수 없다. 값을 바꿀 수 없는 것은 튜플의 성질이고, 집합은 add 와 remove 로 원소를 넣고 뺄 수 있다. 어떤 값이 있는지 확인하는 in 검사가 빠른 것이 집합의 장점이다." },
  { k: "메서드", ex0: "set의 add() 메서드로 원소를 추가", ex: "집합에 원소 하나를 넣는 메서드는 add() 다. append() 는 리스트의 것이라 집합에는 없다. 여러 개를 한꺼번에 넣으려면 update() 를 쓴다. 이미 있는 값을 add 하면 아무 일도 일어나지 않는다." },
  { k: "연산자", ex0: "& 연산자로 교집합", ex: "두 집합에 모두 있는 원소만 남기는 교집합은 & 다. | 는 합집합(둘 중 하나라도 있는 것), - 는 차집합(앞에만 있는 것)이다. + 는 집합에 정의되어 있지 않아 오류가 난다. a.intersection(b) 도 같은 결과다." },
  { k: "출력 결과", ex0: "이미 있는 값은 중복 추가되지 않아", ex: "s 에 이미 2 가 있으므로 s.add(2) 는 아무것도 바꾸지 않는다. 집합은 같은 값을 두 번 담지 않기 때문이다. 그래서 len(s) 는 그대로 2 다. 오류도 나지 않고 조용히 무시된다." },
  { k: "빈칸", ex0: "class 키워드로 클래스를 정의", ex: "클래스는 class 키워드로 정의한다. class Dog: 처럼 이름 뒤에 콜론을 붙이고 본문을 들여 쓴다. def 는 함수를 만드는 키워드라 클래스에는 쓰지 않는다. 클래스 이름은 관례상 대문자로 시작한다." },
  { k: "메서드", ex0: "__init__ 메서드가 객체 생성 시", ex: "객체를 만들 때 자동으로 불리는 메서드는 __init__ 이다. 앞뒤에 밑줄이 두 개씩 붙는다. Dog(\"바둑이\") 처럼 부르면 파이썬이 __init__(self, \"바둑이\") 를 대신 호출한다. constructor 나 new 는 다른 언어의 이름이다." },
  { k: "출력 결과", ex0: "생성자에서 self.name에 '바둑이'", ex: "Dog(\"바둑이\") 를 만들 때 __init__ 이 self.name = name 으로 '바둑이' 를 저장한다. 그 뒤 d.name 을 찍으면 저장된 값 '바둑이' 가 나온다. self 나 name 이라는 글자 자체가 아니라 그 안에 든 값이 출력된다는 점이 핵심이다." },
  { k: "개념", ex0: "파이썬에서는 관례적으로 self를 첫 매개변수", ex: "인스턴스 메서드의 첫 매개변수는 관례상 self 라고 이름 짓는다. 파이썬은 메서드를 부를 때 호출한 객체를 첫 인자로 자동으로 넘기는데, 그 자리 이름이 self 다. this 는 자바·자바스크립트의 키워드이고 파이썬에서는 아무 뜻이 없다." },
  { k: "개념", ex0: "클래스로 만든 실체를 인스턴스 또는 객체", ex: "클래스는 설계도이고, 그 설계도로 실제로 만들어 낸 것이 인스턴스(객체)다. Dog 클래스가 하나여도 Dog(\"바둑이\"), Dog(\"흰둥이\") 처럼 인스턴스는 여러 개 만들 수 있다. 변수는 값에 붙인 이름이고 모듈은 파일 단위라 다른 개념이다." },
  { k: "빈칸", ex0: "self를 통해 인스턴스 변수에 접근", ex: "메서드 안에서 이 객체의 변수를 쓰려면 self.name 처럼 self 를 앞에 붙인다. self 없이 name 이라고만 적으면 지역 변수를 찾으려 하고, 없으면 이름 오류가 난다. self 는 '지금 이 객체' 를 가리키는 이름이다." },
  { k: "개념", ex0: "self는 메서드를 호출한 인스턴스 자신", ex: "self 는 메서드를 부른 바로 그 객체를 가리킨다. d.bark() 라고 부르면 bark 안의 self 는 d 다. 클래스 이름이나 부모 클래스가 아니고, 전역 변수와도 관계가 없다. 그래서 같은 메서드라도 어느 객체가 불렀느냐에 따라 self.name 이 다르다." },
  { k: "개념", ex0: "자식 클래스 이름 옆 괄호 안에 부모", ex: "상속은 class 자식(부모): 꼴로 적는다. 괄호 안이 부모다. class Animal(Dog): 는 방향이 반대라 Animal 이 Dog 를 상속하게 된다. 대괄호나 콜론 뒤에 적는 문법은 파이썬에 없다." },
  { k: "빈칸", ex0: "super()로 부모 클래스의 메서드에 접근", ex: "자식에서 부모의 메서드를 부를 때 super() 를 쓴다. super().__init__() 은 부모의 생성자를 실행해 부모가 준비하는 변수까지 채워 준다. 부모 이름을 직접 적어도 되지만 super() 는 부모 이름이 바뀌어도 고칠 필요가 없다." },
  { k: "개념", ex0: "메서드를 재정의하는 것을 오버라이딩", ex: "부모에 있는 메서드를 자식에서 같은 이름으로 다시 정의해 동작을 바꾸는 것이 오버라이딩이다. 오버로딩은 같은 이름에 다른 인자 개수의 함수를 여럿 두는 것으로 파이썬에는 기본으로 없다. 캡슐화는 내부를 감추는 것, 인스턴스화는 객체를 만드는 것이다." },
  { k: "출력 결과", ex0: "소수점 둘째 자리로 반올림하면 3.14", ex: "round(3.14159, 2) 는 소수점 아래 둘째 자리까지 남기고 반올림한다. 셋째 자리가 1 이라 그대로 잘려 3.14 다. round(x) 처럼 자릿수를 주지 않으면 정수 3 이 되고, 3.15 는 셋째 자리가 5 이상일 때만 나온다." },
  { k: "내장함수", ex0: "sum()은 원소들의 합을 반환", ex: "리스트의 모든 원소를 더하는 내장 함수는 sum() 이다. sum(nums) 처럼 리스트를 통째로 넘긴다. total 이나 add 라는 이름의 내장 함수는 없다. 문자열 리스트에는 쓸 수 없고 숫자에만 쓴다." },
  { k: "출력 결과", ex0: "2부터 8 전까지 2씩 증가해", ex: "range(2, 8, 2) 는 2 부터 시작해 2 씩 늘리며 8 전까지 만든다. 2, 4, 6 이고 8 은 끝 값이라 포함되지 않는다. 세 번째 인자가 간격이다. [2, 4, 6, 8] 이 되려면 끝을 9 나 10 으로 주어야 한다." },
  { k: "내장함수", ex0: "len()은 길이(원소 개수)를 반환", ex: "문자열의 글자 수든 리스트의 원소 수든 len() 이 센다. len(\"abc\") 는 3, len([1, 2]) 는 2 다. count 는 특정 값의 개수를 세는 메서드이고 size 나 length 는 파이썬 내장 함수가 아니다." },
  { k: "개념", ex0: "start를 지정하지 않으면 0부터", ex: "enumerate(리스트) 는 (0, 첫 값), (1, 둘째 값) 처럼 인덱스를 0 부터 붙인다. 1 부터 세고 싶으면 enumerate(리스트, start=1) 로 시작값을 준다. 리스트 길이나 -1 에서 시작하는 일은 없다." },
  { k: "내장함수", ex0: "zip()은 여러 반복 객체를 짝지어", ex: "zip(a, b) 는 두 리스트의 같은 위치 원소를 (a[0], b[0]), (a[1], b[1]) 처럼 짝지어 준다. 길이가 다르면 짧은 쪽에서 멈춘다. enumerate 는 인덱스를 붙이는 것이라 두 리스트를 묶지는 않고, map 은 함수를 적용하는 것이다." },
  { k: "개념", ex0: "enumerate는 (인덱스, 값) 쌍을", ex: "enumerate 는 순회하면서 몇 번째인지(인덱스)와 값을 함께 준다. for i, x in enumerate(리스트): 꼴로 받는다. 두 리스트를 묶는 것은 zip, 합계는 sum, 정렬은 sorted 의 일이다." },
  { k: "global 누적", ex0: "global 선언으로 전역 count를 갱신", ex: "함수 안에서 global count 라고 선언했으므로 count += 1 은 바깥의 전역 count 를 바꾼다. 두 번 부르면 0 → 1 → 2 가 되어 2 가 찍힌다. global 이 없었다면 count 가 지역 변수로 취급되어 '할당 전에 참조' 오류가 났을 것이다." },
  { k: "읽기문자", ex0: "'r'(read)은 읽기 모드이며 open()의 기본값", ex: "open() 의 모드 문자 r 은 read, 즉 읽기 전용이다. 모드를 적지 않으면 이 r 이 기본값이다. w 는 쓰기(기존 내용 삭제), a 는 이어 쓰기, b 를 붙이면 이진 모드다. 읽기 모드로 열었는데 파일이 없으면 오류가 난다." },
  { k: "기본모드", ex0: "모드를 생략하면 읽기 모드 'r'", ex: "open('data.txt') 처럼 모드를 생략하면 읽기 모드 'r' 이 된다. 실수로 파일을 지우는 일이 없도록 안전한 쪽이 기본값이다. 쓰려면 'w', 뒤에 덧붙이려면 'a', 이진 파일이면 'rb' 처럼 명시해야 한다." },
  { k: "전체읽기", ex0: "read()는 파일 전체를 하나의 문자열", ex: "read() 는 파일 내용 전체를 하나의 문자열로 돌려준다. readline() 은 한 줄만, readlines() 는 줄마다 나눈 리스트를 준다. readall() 이라는 메서드는 없다. 파일이 아주 크면 read() 는 메모리를 많이 쓰므로 줄 단위로 도는 편이 낫다." },
  { k: "줄리스트", ex0: "readlines()는 각 줄을 요소로 갖는 리스트", ex: "readlines() 는 줄마다 하나의 원소인 리스트를 돌려준다. 각 원소 끝에 줄바꿈 문자가 붙어 있다. read() 는 통째로 한 문자열이고, readline(-1) 은 한 줄만 읽으며, split() 은 문자열의 메서드라 파일 객체에는 없다." },
  { k: "블록종료", ex0: "with 블록이 끝나면 파일은 자동으로", ex: "with open(...) as f: 블록을 벗어나면 파이썬이 f.close() 를 대신 불러 준다. 예외가 나도 닫힌다. f 변수 자체는 남아 있지만 닫힌 파일이라 읽으면 오류가 난다. 이 자동 닫기 때문에 파일은 with 로 여는 것이 관례다." },
  { k: "기본쓰기", ex0: "'hello'를 쓴 뒤 다시 읽으면 그대로", ex: "'w' 로 열어 hello 를 쓰고 닫은 뒤, 'r' 로 다시 열어 read() 하면 방금 쓴 hello 가 그대로 나온다. 쓰기와 읽기를 같은 열기에서 하려면 모드를 바꾸거나 파일 위치를 되돌려야 하므로, 따로 여는 것이 단순하다." },
  { k: "덮어쓰기", ex0: "'w'는 열 때마다 기존 내용을 지우므로", ex: "'w' 모드는 열 때마다 파일을 비우고 처음부터 쓴다. A 를 쓴 뒤 다시 'w' 로 열어 B 를 쓰면 A 는 사라지고 B 만 남는다. 뒤에 덧붙이려면 'a' 모드를 써야 한다." },
  { k: "이어쓰기", ex0: "'a'는 기존 내용을 유지하고 뒤에 추가", ex: "'a' 모드는 기존 내용을 지우지 않고 끝에 이어 쓴다. AAA 가 있던 파일에 B 를 덧붙이면 AAAB 가 된다. 'w' 였다면 B 만 남았을 것이다. 로그처럼 계속 쌓는 파일에 'a' 를 쓴다." },
  { k: "글자세기", ex0: "'banana'에는 a가 3번 등장하므로", ex: "Counter('banana') 는 글자마다 몇 번 나오는지 센 딕셔너리 같은 객체다. a 는 세 번 나오므로 c['a'] 는 3 이다. 없는 글자를 물으면 오류 대신 0 을 돌려주는 것이 일반 딕셔너리와 다른 점이다." },
  { k: "int기본값", ex0: "int()의 기본값이 0이므로 없는 키는 0", ex: "defaultdict(int) 는 없는 키에 접근하는 순간 int() 를 불러 기본값을 만든다. int() 는 0 이므로 없는 키의 값은 0 이다. 일반 딕셔너리라면 KeyError 가 났을 자리다. list 를 넘기면 빈 리스트가, str 을 넘기면 빈 문자열이 기본값이 된다." },
  { k: "자동생성", ex0: "없는 키 'new'를 접근하는 순간 기본값 0", ex: "d['new'] 를 읽는 순간 defaultdict 가 int() 로 0 을 만들어 그 키에 넣고 돌려준다. 그래서 0 이 찍히고, 그 뒤로는 'new' 키가 실제로 딕셔너리에 존재한다. 읽기만 했는데 키가 생긴다는 점을 기억해야 한다." },
  { k: "왼쪽삽입", ex0: "appendleft(0)은 맨 앞에 0을 넣으므로", ex: "deque 의 appendleft() 는 왼쪽 끝, 즉 맨 앞에 값을 넣는다. 0 을 앞에 넣었으니 d[0] 은 0 이다. 리스트의 insert(0, x) 와 결과는 같지만 deque 는 앞쪽 삽입이 상수 시간이라 큐를 만들 때 쓴다." },
  { k: "소수2자리", ex0: ":.2f는 소수점 아래 둘째 자리까지", ex: "f\"{x:.2f}\" 의 .2f 는 소수점 아래 두 자리까지 반올림해 고정 소수 표기로 찍으라는 뜻이다. 3.14159 는 3.14 가 된다. .0f 면 정수처럼 3, .3f 면 3.142 다. 값 자체는 바뀌지 않고 보이는 모양만 바뀐다." },
  { k: "자리채움", ex0: ":03d는 전체 3자리로 맞추고 빈 자리를 0", ex: "f\"{n:03d}\" 의 03d 는 정수를 전체 3 자리로 맞추고 빈 자리를 0 으로 채우라는 뜻이다. 5 는 005 가 된다. 0 을 빼고 3d 만 쓰면 공백으로 채워 '  5' 가 되고, 값이 이미 3 자리 이상이면 그대로 나온다." },
  { k: "천단위", ex0: ":, 포맷은 천 단위마다 쉼표", ex: "f\"{n:,}\" 의 콤마는 세 자리마다 쉼표를 넣으라는 지시다. 1000000 은 1,000,000 이 된다. 소수와 함께 쓰려면 :,.2f 처럼 이어 붙인다. 숫자 자체에 쉼표가 들어가는 것이 아니라 출력 모양만 바뀐다." },
  { k: "퍼센트정수", ex0: "%d는 정수 자리를 5로 채우므로", ex: "'%d개' % 5 에서 %d 는 정수가 들어갈 자리다. 5 가 그 자리에 들어가 5개 가 된다. 옛 방식의 포매팅이지만 아직 많이 보인다. %s 는 문자열, %f 는 실수 자리이며 요즘은 f-string 을 더 권한다." },
  { k: "퍼센트실수", ex0: "%.2f는 소수점 둘째 자리까지 표시", ex: "'%.2f' % 3.14159 의 .2f 는 실수를 소수점 아래 두 자리까지 반올림해 찍으라는 뜻이라 3.14 가 된다. f-string 의 {x:.2f} 와 같은 규칙이다. %f 만 쓰면 기본 여섯 자리 3.141590 이 나온다." },
  /* 2차 — 88~153 */
  { k: "reduce 초기값", ex0: "세 번째 인자 100이 초기값", ex: "reduce(함수, 목록, 초기값) 에서 세 번째 인자 100 이 누적의 출발점이다. 100 에 1, 2, 3 을 차례로 더해 106 이 된다. 초기값이 없었다면 첫 원소 1 부터 시작해 6 이 나오고, 빈 목록에서는 초기값이 없으면 오류가 난다." },
  { k: "chain", ex0: "chain은 여러 이터러블을 하나로", ex: "itertools.chain([1, 2], [3], [4, 5]) 는 세 목록을 차례로 이어 하나의 흐름으로 만든다. list() 로 감싸면 [1, 2, 3, 4, 5] 다. 중첩 리스트 [[1, 2], [3], [4, 5]] 가 되지 않는 것이 핵심이다. 리스트끼리 + 로 붙인 결과와 같지만 chain 은 중간 리스트를 만들지 않는다." },
  { k: "combinations 개수", ex0: "C(4,2)=4!/(2!2!)=6", ex: "combinations(4개, 2) 는 순서를 따지지 않고 2 개를 고르는 모든 경우다. 4 개에서 2 개를 고르는 조합의 수는 4×3÷2 = 6 이다. (1,2) 와 (2,1) 은 같은 것으로 세어 한 번만 나온다. 순서까지 구분하는 permutations 였다면 12 개다." },
  { k: "합 재귀", ex0: "s(4)=4+3+2+1+0=10", ex: "s(n) 은 n 에 s(n-1) 을 더하고 n 이 0 이면 0 을 돌려준다. s(4) = 4 + s(3) = 4 + 3 + 2 + 1 + 0 = 10 이다. 기저 조건 n == 0 이 있어 재귀가 멈춘다. 이 조건이 없었다면 음수로 끝없이 내려가 RecursionError 가 났을 것이다." },
  { k: "무한 재귀", ex0: "파이썬은 재귀 깊이 한계를 넘으면", ex: "기저 조건 없이 자기 자신을 계속 부르면 호출 스택이 쌓이다가 파이썬이 정한 한계(기본 1000)를 넘는 순간 RecursionError 가 난다. 무한히 정상 동작하지는 않고, KeyError 나 IndexError 는 딕셔너리나 리스트 접근 오류라 이 상황과 무관하다." },
  { k: "median 짝수", ex0: "중앙 두 값 2와 3의 평균 2.5", ex: "데이터 개수가 짝수면 가운데 값이 하나가 아니라 둘이다. 정렬한 뒤 가운데 두 값 2 와 3 의 평균 (2+3)/2 = 2.5 가 중앙값이다. 그래서 median 은 데이터에 없는 값을 돌려줄 수도 있다. 홀수 개면 가운데 하나를 그대로 돌려준다." },
  { k: "median 홀수", ex0: "홀수개면 정렬 후 가운데 값을", ex: "개수가 홀수면 정렬한 뒤 정확히 가운데 있는 값이 중앙값이다. 이 데이터에서는 20 이다. 평균과 달리 양 끝의 극단값에 흔들리지 않으므로, 몇 개의 큰 값이 섞인 데이터의 '보통 값' 을 볼 때 median 을 쓴다." },
  { k: "mode 출력", ex0: "가장 자주 나온 값(최빈값)은 1", ex: "mode 는 가장 자주 나온 값을 돌려준다. 1 이 다른 값보다 많이 등장하므로 1 이다. 평균이나 중앙값과 달리 계산이 아니라 개수 세기이므로 숫자가 아닌 데이터에도 쓸 수 있다. 최빈값이 여럿이면 처음 만난 것을 돌려준다." },
  { k: "variance 출력", ex0: "표본분산은 편차제곱합 10을 (n-1)=4로", ex: "variance 는 표본분산이라 편차 제곱의 합을 n 이 아니라 n-1 로 나눈다. 편차 제곱합 10 을 4 로 나눠 2.5 다. 모집단 전체를 가진 것으로 보고 n 으로 나누려면 pvariance 를 써야 하고, 그러면 2.0 이 된다." },
  { k: "pvariance 출력", ex0: "평균 5, 편차제곱합 32를 n=8로", ex: "pvariance 는 모분산이라 편차 제곱합을 n 으로 나눈다. 평균이 5 이고 편차 제곱합이 32 이므로 32 ÷ 8 = 4.0 이다. 표본분산 variance 였다면 7 로 나눠 약 4.57 이 나온다. p 가 붙은 쪽이 '전체(population)' 라고 기억하면 된다." },
  { k: "mode 정의", ex0: "mode는 빈도가 가장 높은 값을 반환", ex: "mode 는 가장 자주 나온 값, 즉 최빈값이다. 평균은 mean, 중앙값은 median, 최댓값은 max 가 따로 있다. 셋은 계산으로 구하지만 mode 는 개수를 세므로 문자열 데이터에도 쓸 수 있다는 점이 다르다." },
  { k: "pstdev 출력", ex0: "모분산 4.0의 제곱근이므로 모표준편차는 2.0", ex: "표준편차는 분산의 제곱근이다. pstdev 는 모분산 pvariance 의 제곱근이므로 4.0 의 제곱근 2.0 이다. 분산은 단위가 제곱이라 감이 안 오는데, 제곱근을 취한 표준편차는 원래 데이터와 같은 단위라 '평균에서 보통 얼마나 떨어져 있나' 로 읽을 수 있다." },
  { k: "n-1 효과", ex0: "stdev는 n-1로 나눠 분모가 작아지므로", ex: "stdev 는 표본표준편차라 n-1 로 나누고, pstdev 는 n 으로 나눈다. 같은 합을 더 작은 수로 나누니 stdev 가 더 크다. 표본은 모집단보다 퍼짐이 작게 잡히는 경향이 있어 이를 보정하는 것이다. 제곱이나 절댓값의 차이는 아니다." },
  { k: "개념", ex0: "def 이름(): 형태로 정의", ex: "함수는 def 이름(매개변수): 로 정의한다. function 은 자바스크립트, fn 은 러스트, func 는 Go 의 키워드라 파이썬에서는 문법 오류다. def 다음 줄부터 들여 쓴 부분이 함수의 몸통이다." },
  { k: "빈칸", ex0: "return x 로 결과를 돌려준다", ex: "함수가 값을 밖으로 내보내는 문장은 return 이다. return x 를 만나면 함수가 끝나고 x 가 호출한 쪽에 전달된다. print 는 화면에 보여 줄 뿐 값을 돌려주지 않아, print 만 쓴 함수의 결과를 변수에 받으면 None 이 된다." },
  { k: "용어", ex0: "함수가 받는 값을 매개변수(파라미터)", ex: "함수에 넘겨 주는 입력값을 매개변수 또는 인자라 한다. def f(a, b) 의 a, b 가 매개변수, f(1, 2) 의 1, 2 가 인자다. 반환값은 함수가 되돌려 주는 결과라 방향이 반대이고, 변수와 키는 다른 개념이다." },
  { k: "안전 접근", ex0: "d.get('x') 는 없으면 None 을 돌려줘", ex: "d['x'] 는 키가 없으면 KeyError 를 내지만 d.get('x') 는 None 을 돌려주고, d.get('x', 0) 처럼 기본값도 정할 수 있다. pick, find, key 라는 메서드는 딕셔너리에 없다. 키가 있는지 모를 때는 get 이 안전하다." },
  { k: "메서드", ex0: "keys() 는 키 목록, values() 는 값 목록", ex: "딕셔너리의 키만 모으려면 keys(), 값만 모으려면 values(), (키, 값) 쌍을 함께 얻으려면 items() 다. all() 은 모든 원소가 참인지 검사하는 내장 함수라 키와 관계가 없다. for k in d: 처럼 딕셔너리를 바로 돌려도 키가 나온다." },
  { k: "개념", ex0: "딕셔너리는 {\"a\":1} 처럼 키:값", ex: "키와 값을 짝지어 저장하는 자료형은 딕셔너리다. {\"a\": 1} 처럼 중괄호 안에 키: 값 을 적는다. 리스트와 튜플은 순서로만 찾고, 집합은 값만 담고 짝이 없다. 이름으로 찾아야 하는 데이터(전화번호부 같은 것)에 딕셔너리를 쓴다." },
  { k: "접근", ex0: "키 'a' 에 해당하는 값 1 을 꺼낸다", ex: "d[\"a\"] 는 키 \"a\" 에 짝지어진 값을 꺼내므로 1 이다. 키 자체인 a 나 key 라는 글자가 나오는 것이 아니다. 없는 키를 넣으면 KeyError 가 나고, 있는 키이므로 오류도 아니다." },
  { k: "개념", ex0: "파이썬은 except 로 오류를 잡는다", ex: "try 블록에서 난 오류를 받아 처리하는 블록은 except 다. catch 는 자바·자바스크립트의 키워드라 파이썬에는 없다. finally 는 오류가 났든 안 났든 마지막에 항상 실행되는 블록이고, error 는 키워드가 아니다." },
  { k: "출력 결과", ex0: "range(3)=0,1,2 각각 ×2", ex: "range(3) 은 0, 1, 2 를 만들고, 컴프리헨션이 각각을 2 배 해 [0, 2, 4] 가 된다. [2, 4, 6] 은 range(1, 4) 였을 때의 결과다. range 가 0 부터 시작한다는 것과 끝 값이 빠진다는 것 두 가지를 함께 떠올려야 한다." },
  { k: "개념", ex0: "try: 블록으로 위험한 코드를", ex: "오류가 날 수 있는 코드는 try: 블록 안에 둔다. 오류가 나면 except 로 넘어간다. catch 는 다른 언어의 이름이고 test 와 safe 는 키워드가 아니다. try 는 반드시 except 나 finally 와 짝을 이루어야 한다." },
  { k: "용어", ex0: "리스트 컴프리헨션(list comprehension)이다", ex: "[식 for x in 목록] 처럼 한 줄로 리스트를 만드는 문법이 리스트 컴프리헨션이다. 람다는 이름 없는 함수, 데코레이터는 함수를 감싸는 문법, 제너레이터 표현식은 소괄호 (식 for ...) 로 값을 하나씩 흘려보내는 것이라 각각 다르다." },
  { k: "빈칸", ex0: "lambda는 콜론 뒤의 식을 자동으로 반환", ex: "lambda a, b: a + b 에서 콜론 뒤의 식이 곧 반환값이다. return 을 적지 않아도 되고, 적으면 오히려 문법 오류다. def 로 쓰면 세 줄이 되는 것을 한 줄로 줄인 것이라 간단한 함수를 인자로 넘길 때 쓴다." },
  { k: "출력 결과", ex0: "8 이 거짓이므로 else 값인 8", ex: "lambda a, b: a if a > b else b 는 큰 쪽을 돌려주는 식이다. 3 > 8 이 거짓이라 else 쪽 값 8 이 나온다. 조건식 A if 조건 else B 는 조건이 참이면 A, 거짓이면 B 다. 3 이나 11 은 다른 계산을 가정한 값이다." },
  { k: "빈칸", ex0: "n의 제곱을 돌려주면 된다", ex: "제곱은 n ** 2 또는 n * n 으로 쓴다. ** 가 거듭제곱 연산자이고 ^ 는 파이썬에서 비트 XOR 이라 n ^ 2 는 전혀 다른 값이 나온다. pow(n, 2) 도 같은 결과다." },
  { k: "개념", ex0: "lambda는 이름 없이 간단히 정의하는 익명 함수", ex: "lambda x: x + 1 은 이름 없는 함수 하나를 만든다. 변수에 담거나 map, sorted 의 key 처럼 함수를 받는 자리에 바로 넘긴다. 반복문·클래스·리스트를 만드는 것이 아니고, def 로 이름을 붙여 쓰는 함수와 하는 일은 같다." },
  { k: "개념", ex0: "map은 모든 원소에 함수를 적용해 변환", ex: "map(f, 목록) 은 목록의 원소마다 f 를 적용한 결과를 만든다. 원소 수는 그대로이고 값만 바뀐다. 조건에 맞는 것만 남기는 것은 filter, 정렬은 sorted, 합계는 sum 이다. map 은 게으르게 계산하므로 결과를 보려면 list() 로 감싼다." },
  { k: "내장함수", ex0: "list()로 감싸면 결과를 리스트로", ex: "map 과 filter 는 결과를 미리 만들지 않고 필요할 때 하나씩 내놓는 객체를 돌려준다. 그래서 print 하면 <map object ...> 처럼 보인다. list() 로 감싸면 전부 계산해 리스트로 만든다. tuple() 이나 set() 으로 감싸도 된다." },
  { k: "개념", ex0: "filter는 조건 함수가 True인 원소만", ex: "filter(조건함수, 목록) 은 조건 함수가 참을 돌려주는 원소만 남긴다. 원소를 바꾸는 것은 map, 뒤집는 것은 reversed, 최댓값은 max 다. filter 의 결과도 list() 로 감싸야 리스트가 된다." },
  { k: "내장함수", ex0: "sorted()는 원본을 바꾸지 않고 새 정렬 리스트", ex: "sorted(nums) 는 원본은 그대로 두고 정렬된 새 리스트를 돌려준다. nums.sort() 는 원본을 제자리에서 정렬하고 None 을 돌려주므로 결과를 변수에 받으면 안 된다. 원본을 지키고 싶으면 sorted, 메모리를 아끼려면 sort 다." },
  { k: "개념", ex0: "key에 함수를 주면 그 결과를 기준으로 정렬", ex: "key 에 함수를 넘기면 각 원소 대신 그 함수의 결과로 비교한다. key=len 이면 길이순, key=str.lower 면 대소문자 무시 순이다. 방향은 reverse 인자가 정하고, 중복 제거나 뒤집기는 key 의 일이 아니다." },
  { k: "개념", ex0: "reverse=True는 큰 값부터 정렬", ex: "reverse=True 는 정렬 결과를 내림차순, 즉 큰 값부터 작은 값 순으로 만든다. 기본값 False 는 오름차순이다. sorted 는 원본을 바꾸지 않으므로 '원본 변경' 은 아니고, 오류도 나지 않는다." },
  { k: "문자클래스", ex0: "\\d=숫자, \\s=공백, \\w=단어문자", ex: "\\w 는 단어를 이루는 글자, 즉 영문자·숫자·밑줄에 매치된다. 숫자만은 \\d, 공백만은 \\s, 모든 문자는 . 이다. 유니코드 모드에서는 한글도 \\w 에 포함된다. 하이픈이나 공백은 \\w 가 아니므로 '단어 하나' 를 잡을 때 \\w+ 를 쓴다." },
  { k: "앵커", ex0: "^는 시작 위치, $는 끝 위치를 나타내는 앵커", ex: "^ 는 문자열(또는 줄) 시작, $ 는 끝을 뜻하는 앵커다. 글자를 소비하지 않고 위치만 확인한다. ^abc$ 는 정확히 abc 인 문자열에만 맞는다. 대괄호 안의 ^ 는 부정([^0-9]) 이라 뜻이 달라지는데, 이 문제는 대괄호 밖의 ^ 를 묻고 있다." },
  { k: "필드 접근", ex0: "자동 __init__이 x=10, y=20을 대입", ex: "@dataclass 는 필드 선언을 보고 __init__ 을 자동으로 만들어 준다. P(10, 20) 은 x=10, y=20 을 대입하고, p.x + p.y 는 30 이다. 직접 __init__ 을 쓰지 않았는데도 생성자가 있는 것이 데이터클래스의 핵심 편의다." },
  { k: "자동 메서드", ex0: "기본값으로 생성자·표현·동등비교 세 메서드", ex: "@dataclass 는 __init__(생성자), __repr__(출력용 표현), __eq__(같은 필드면 같다고 비교) 세 가지를 기본으로 만든다. 정렬 비교는 order=True 를 줘야 생기고, __call__·__len__·__enter__·__add__ 같은 것은 만들어 주지 않는다." },
  { k: "int 변환", ex0: "문자열 '3','4'를 int로 바꿔 더하면 7", ex: "CSV 에서 읽은 '3' 과 '4' 는 문자열이다. 그대로 더하면 '34' 가 되지만 int() 로 바꾼 뒤 더하면 7 이다. 숫자처럼 보이는 문자열을 계산에 쓸 때는 반드시 int 나 float 로 바꾸는 습관이 필요하다." },
  { k: "문자열 합계", ex0: "각 문자열을 int로 바꿔 합하면 1+2+3=6", ex: "['1', '2', '3'] 은 문자열 리스트라 sum() 을 바로 쓰면 오류가 난다. 각 원소를 int() 로 바꾼 뒤 더해야 1 + 2 + 3 = 6 이다. sum(int(x) for x in 목록) 꼴이 흔한 관용구다." },
  { k: "형변환 필요", ex0: "csv 모듈은 셀을 문자열로 읽으므로", ex: "csv 모듈은 파일의 모든 셀을 문자열로 읽는다. '10' 과 '20' 을 더하면 '1020' 이 되므로 계산 전에 int 나 float 로 바꿔야 한다. csv 가 숫자를 못 읽는 것이 아니라 숫자인지 판단하지 않는 것이고, int 와 float 모두 더할 수 있다." },
  { k: "빈도 접근", ex0: "x가 2번 등장하므로 c['x']는 2", ex: "Counter 는 값마다 등장 횟수를 센다. x 가 두 번 나왔으므로 c['x'] 는 2 다. 일반 딕셔너리와 달리 없는 키를 물으면 오류 대신 0 을 돌려주므로, 있는지 확인하지 않고 바로 c[키] 를 써도 된다." },
  { k: "most_common 형태", ex0: "빈도 내림차순으로 (요소, 개수) 튜플들의", ex: "most_common(2) 는 빈도가 높은 순으로 (값, 개수) 튜플 두 개를 담은 리스트를 돌려준다. [('a', 3), ('b', 2)] 같은 모양이다. 값만 주거나 개수의 합을 주지 않으며, 인자를 생략하면 전부 빈도순으로 나온다." },
  { k: "고유 개수", ex0: "고유 값은 a, b, c 세 종류이므로", ex: "set() 으로 바꾸면 중복이 사라져 서로 다른 값만 남는다. a, b, c 세 종류이므로 len 은 3 이다. 원래 리스트의 길이가 아니라 '몇 종류인가' 를 묻는 것이다. Counter 를 썼다면 len(c) 도 같은 3 이다." },
  { k: "Counter 용도", ex0: "Counter는 요소별 개수를 세는 딕셔너리형", ex: "Counter 는 값마다 몇 번 나왔는지 세는 딕셔너리다. 정렬은 sorted, 평균은 statistics.mean 의 일이고, 중복 제거는 set 이 한다. Counter 로도 키만 보면 중복이 없지만 목적은 '개수 세기' 다." },
  { k: "round 나눗셈", ex0: "10/3=3.333..., 소수 둘째 자리로", ex: "10 / 3 은 3.3333… 이고 round(값, 2) 는 소수 둘째 자리까지 반올림해 3.33 이 된다. 셋째 자리가 3 이라 올라가지 않는다. 정수 나눗셈 10 // 3 이었다면 3 이다." },
  { k: "평균 반올림", ex0: "합 270을 3으로 나누면 90.0", ex: "270 / 3 은 90.0 이다. 나눗셈 / 은 결과가 정수여도 실수(float)로 돌려준다. round(90.0, 2) 를 해도 90.0 그대로다. 정수 90 이 나오려면 // 를 쓰거나 int() 로 바꿔야 한다." },
  { k: "zip 열 묶기", ex0: "zip은 여러 열을 인덱스별로 묶어", ex: "zip(names, ages) 는 같은 위치의 원소끼리 (이름, 나이) 튜플로 묶는다. 두 열을 행으로 바꾸는 셈이다. 앞뒤로 이어 붙이는 것은 + 나 chain, 정렬은 sorted, 중복 제거는 set 의 일이다." },
  { k: "zip 결과", ex0: "같은 위치끼리 묶여 [('a', 1), ('b', 2)]", ex: "zip(['a', 'b'], [1, 2]) 를 list() 로 감싸면 [('a', 1), ('b', 2)] 다. 첫 번째끼리, 두 번째끼리 짝을 짓는다. dict() 로 감싸면 {'a': 1, 'b': 2} 가 되어 두 리스트로 딕셔너리를 만들 때도 쓴다." },
  { k: "round 인자", ex0: "두 번째 인자는 남길 소수 자릿수", ex: "round(x, 2) 의 2 는 소수점 아래 몇 자리까지 남길지다. round(3.14159, 2) 는 3.14 다. 음수를 주면 정수 자리에서 반올림해 round(1234, -2) 는 1200 이 된다. 곱하거나 반복하는 횟수가 아니다." },
  { k: "heap[0]", ex0: "최소힙이므로 push 후 h[0]에는 가장 작은 값", ex: "heapq 는 최소 힙이라 어떤 순서로 push 하든 h[0] 에는 항상 가장 작은 값이 온다. 1 이 가장 작으므로 h[0] 은 1 이다. 나머지 원소는 정렬되어 있지 않으므로 h[1] 이 두 번째로 작다는 보장은 없다. 꺼내려면 heappop 을 쓴다." },
  { k: "케이스 수 계산", ex0: "겹친 parametrize는 데카르트 곱", ex: "parametrize 를 두 개 겹치면 모든 조합이 만들어진다. 4 개 값과 3 개 값의 조합은 4 × 3 = 12 개다. 더하기가 아니라 곱하기이므로 데코레이터를 세 개 겹치면 케이스가 급격히 늘어난다." },
  { k: "코루틴 정의", ex0: "async def로 정의한 함수를 호출하면 코루틴 객체", ex: "코루틴 함수는 async def 로 정의한다. 부르면 바로 실행되지 않고 코루틴 객체가 만들어지며, await 하거나 이벤트 루프에 넘겨야 돈다. coroutine 키워드는 없고, await def 는 문법 오류이며, 그냥 def 는 일반 함수다." },
  { k: "함수 정의 키워드", ex0: "async def로 코루틴 함수를 정의", ex: "def 앞에 async 를 붙이면 코루틴 함수가 된다. 그 안에서만 await 를 쓸 수 있다. await 는 코루틴을 기다리는 키워드이지 정의하는 키워드가 아니다. asyncio.run() 으로 최상위 코루틴을 실행한다." },
  { k: "개념", ex0: "대괄호 [] 안에 식과 for를 쓰면 리스트", ex: "리스트 컴프리헨션은 [식 for 변수 in 반복] 이다. 소괄호 () 면 제너레이터 표현식, 중괄호 {} 면 집합 컴프리헨션이 된다. for 변수 in 반복: 은 일반 반복문이라 리스트를 만들지 않는다." },
  { k: "빈칸", ex0: "range(5)는 0부터 4까지 만든다", ex: "0 부터 4 까지 다섯 개는 range(5) 다. 끝 값 5 는 빠진다. [x for x in range(5)] 는 [0, 1, 2, 3, 4] 이고 list(range(5)) 와 같다." },
  { k: "개념", ex0: "뒤쪽 if는 조건에 맞는 원소만 남기는 필터", ex: "for 뒤에 붙는 if 는 조건에 맞는 원소만 통과시키는 필터다. 값을 바꾸는 것은 for 앞의 식이 한다. [x*2 for x in nums if x > 0] 이면 양수만 골라 2 배 한다. 정렬이나 반복 횟수와는 관계가 없다." },
  { k: "빈칸", ex0: "뒤쪽 if로 조건을 걸어 필터링", ex: "[n for n in nums if n > 0] 처럼 for 절 뒤에 if 조건을 붙이면 조건이 참인 원소만 남는다. where 나 when 은 파이썬 문법이 아니다. 조건에 else 를 붙이고 싶으면 for 앞으로 옮겨 [a if 조건 else b for ...] 꼴로 써야 한다." },
  { k: "개념", ex0: "집합(set)은 중복을 허용하지 않는다", ex: "{x for x in 목록} 은 집합을 만드므로 같은 값은 하나만 남는다. 순서는 보장되지 않고, 키-값 쌍은 딕셔너리 컴프리헨션의 것이며, 결과는 리스트가 아니라 set 이다. 중복을 없애며 변환할 때 유용하다." },
  { k: "개념", ex0: "딕셔너리 컴프리헨션은 키와 값을 함께", ex: "중괄호 안에 키: 값 꼴이면 딕셔너리 컴프리헨션이다. {k: v for k, v in 쌍들} 처럼 쓴다. 중괄호라도 콜론이 없으면 집합 컴프리헨션이 된다. 이 콜론 하나가 두 자료형을 가른다." },
  { k: "개념", ex0: "중괄호 안에 '키: 값' 형태를 쓰면 딕셔너리", ex: "딕셔너리 컴프리헨션은 {키: 값 for 변수 in 반복} 이다. {값 for ...} 는 집합, (키: 값 for ...) 와 [키: 값 for ...] 는 문법 오류다. 키와 값 사이의 콜론이 딕셔너리라는 표시다." },
  { k: "음수 인덱스", ex0: "음수 인덱스는 뒤에서부터 센다. -1은 마지막", ex: "음수 인덱스는 끝에서부터 센다. -1 은 마지막 글자 n, -2 는 o, -6 은 첫 글자 P 다. len(s) - 1 을 계산하지 않아도 마지막을 꺼낼 수 있어 편리하다. -7 처럼 길이를 넘는 음수는 오류다." },
  { k: "앞부분", ex0: "s[:2]는 처음부터 인덱스 1까지", ex: "s[:2] 는 시작을 생략해 처음부터, 인덱스 2 직전까지다. 인덱스 0 과 1 의 두 글자 Py 가 나온다. 끝 인덱스 2 는 포함되지 않는다. 슬라이스는 '시작은 포함, 끝은 제외' 라는 규칙을 항상 따른다." },
  { k: "끝까지", ex0: "s[-3:]은 뒤에서 3번째부터 끝까지", ex: "s[-3:] 은 뒤에서 세 번째 글자부터 끝까지다. Python 의 뒤 세 글자 hon 이 나온다. Pyt 는 s[:3], tho 는 s[2:5], ython 은 s[1:] 이다. 마지막 n 글자를 꺼낼 때 s[-n:] 을 쓴다." },
  { k: "리스트 step", ex0: "인덱스 0,2,4의 요소 = [1, 3, 5]", ex: "nums[::2] 는 처음부터 끝까지 두 칸씩 건너뛰며 고른다. 인덱스 0, 2, 4 의 값 1, 3, 5 가 나온다. 세 번째 자리가 간격(step)이고, [1::2] 면 인덱스 1 부터 시작해 짝수 번째 값들이 나온다." },
  { k: "count_safe · 세는 딕셔너리", ex0: "get 으로 꺼내기만 하고 1 을 더하지 않았다", ex: "counts.get(x, 0) 으로 지금 값을 꺼내기만 하고 다시 저장하지 않았다. 세는 일은 counts[x] = counts.get(x, 0) + 1 처럼 '꺼낸 값에 1 을 더해 다시 넣는' 두 단계가 모두 있어야 한다. 그래서 모든 값이 0 으로 남는다. 원본 목록은 읽기만 하므로 부작용은 없다." },
] };
