/* arduino · os · mleval · devops · ai · backend · compiler · cs · dbt 짧은 해설 다시 쓰기 */
module.exports = [
{ track: "arduino", fixes: [
  { k: "언어", ex0: "아두이노 스케치는 C/C++ 문법", ex: "아두이노 스케치는 C/C++ 문법으로 쓰고 avr-gcc 같은 컴파일러가 기계어로 바꿔 보드에 올린다. Python 은 MicroPython 보드에서나 쓰이고, JavaScript 는 브라우저와 Node 의 언어이며, SQL 은 데이터베이스 질의 언어라 마이크로컨트롤러 펌웨어에 쓰지 않는다." },
  { k: "구조", ex0: "setup() 에서 초기 설정(핀 모드 등)", ex: "보드에 전원이 들어오면 setup() 이 딱 한 번 실행되어 pinMode 나 Serial.begin 같은 초기 설정을 한다. 그 뒤 loop() 가 끝없이 반복된다. main() 은 아두이노 코어가 숨겨 두고 대신 setup 과 loop 를 불러 주며, init() 이라는 함수는 스케치 구조에 없다." },
  { k: "개념", ex0: "아두이노는 센서·모터·LED 등을 제어하는 작은 컴퓨터 보드", ex: "아두이노는 마이크로컨트롤러가 달린 작은 보드로, 핀에 연결한 센서와 모터와 LED 를 프로그램으로 제어한다. 운영체제 없이 스케치 하나가 바로 돈다. 브라우저나 데이터베이스는 소프트웨어이고, 운영체제는 PC 급 컴퓨터를 관리하는 것이라 아두이노의 정체와 다르다." },
  { k: "구조", ex0: "loop() 안의 코드가 무한 반복", ex: "setup() 이 끝나면 loop() 가 계속 반복 호출된다. 센서를 읽고 출력을 바꾸는 코드는 여기 둔다. setup() 은 한 번만 실행되고, repeat() 와 again() 은 아두이노에 없는 함수다. loop 안에서 delay 를 길게 걸면 그동안 다른 입력을 못 읽는다는 점을 기억해 두자." },
  { k: "함수", ex0: "pinMode(13, OUTPUT) 처럼 핀의 역할", ex: "핀을 입력으로 쓸지 출력으로 쓸지는 pinMode(핀, INPUT 또는 OUTPUT) 으로 정하고 보통 setup() 에서 한다. digitalWrite 는 이미 출력으로 정한 핀에 값을 내보내는 함수이고, begin() 은 Serial 같은 라이브러리를 시작하는 것이며, setPin() 은 존재하지 않는다." },
  { k: "함수", ex0: "delay(1000) 은 1초 동안 멈춘다", ex: "delay(ms) 는 지정한 밀리초 동안 프로그램을 멈춘다. delay(1000) 이 1 초다. wait(), halt(), stop() 은 아두이노 기본 함수가 아니다. 멈춘 동안 버튼 입력을 놓치므로 반응이 중요한 코드에서는 millis() 로 시간을 재는 방식을 쓴다." },
  { k: "함수", ex0: "digitalWrite(13, HIGH) 로 LED 를 켠다", ex: "디지털 핀에 HIGH(5V 또는 3.3V) 나 LOW(0V) 를 내보내는 함수는 digitalWrite(핀, 값) 이다. analogRead 는 값을 읽는 것이고, delayMicroseconds 는 아주 짧게 멈추는 것이며, print 는 Serial 로 글자를 보내는 것이라 핀 출력과 다르다. 먼저 pinMode 로 OUTPUT 을 정해야 한다." },
  { k: "제어", ex0: "HIGH 는 전압을 걸어 켜고, LOW 는 끈다", ex: "LED 를 켜려면 핀에 HIGH 를 써서 전압을 걸어야 한다. LOW 나 0 은 끄는 값이다. OUTPUT 은 pinMode 에 주는 핀 역할 설정이지 출력 값이 아니고, GROUND 는 회로의 0V 기준선 이름이라 digitalWrite 에 넣는 값이 아니다." },
  { k: "함수", ex0: "analogRead(A0) 은 0~1023 범위 값", ex: "온도나 빛처럼 연속적인 값을 내는 센서는 analogRead(A0) 으로 읽는다. 결과는 0 부터 1023 사이 정수다. pinMode 는 핀 역할 설정, delay 는 멈춤이고, readPin() 은 존재하지 않는다. 켜짐과 꺼짐만 있는 버튼은 digitalRead 로 읽는다." },
  { k: "개념", ex0: "10비트라 0부터 1023까지", ex: "아두이노 우노의 ADC 는 10 비트라 2¹⁰ = 1024 단계, 즉 0 부터 1023 까지를 돌려준다. 0~255 는 8 비트(analogWrite 의 PWM 범위)이고, 0~511 은 9 비트, 0~1 은 digitalRead 의 결과다. 전압으로 바꾸려면 값 × 5.0 / 1023 을 계산한다." },
  { k: "함수", ex0: "digitalRead(핀) 은 HIGH 또는 LOW 를 돌려준다", ex: "버튼처럼 켜짐과 꺼짐만 있는 입력은 digitalRead(핀) 으로 읽고 HIGH 또는 LOW 를 받는다. analogRead 는 0~1023 의 연속 값을 읽는 것이고, print 는 Serial 출력, loop 는 반복 구조라 입력 함수가 아니다. 버튼에는 풀업이나 풀다운 저항이 필요하다." },
  { k: "부품", ex0: "조도(빛) 센서로 밝기를 읽어 제어", ex: "빛의 밝기를 감지하는 대표 부품은 조도 센서(CDS, 광저항)다. 밝을수록 저항이 줄어 analogRead 값이 바뀐다. LED 는 빛을 내는 쪽이고, 버저는 소리를 내며, 저항은 전류를 제한하는 부품이라 감지 역할을 하지 않는다." },
  { k: "통신", ex0: "Serial.print() 로 PC 화면에 값을 출력", ex: "아두이노와 PC 가 USB 로 값을 주고받는 통로는 Serial 이다. Serial.print() 로 보낸 값을 IDE 의 시리얼 모니터에서 보며 디버깅한다. HDMI 는 화면 출력 규격, LCD 는 보드에 붙이는 표시 장치이고, WiFi 는 별도 모듈이 있어야 하며 기본 디버깅 통로가 아니다." },
  { k: "함수", ex0: "setup() 에서 Serial.begin(9600) 처럼 시작", ex: "시리얼 통신은 Serial.begin(9600) 처럼 속도(보드레이트)를 정해 시작하며 setup() 에 둔다. PC 쪽 시리얼 모니터도 같은 속도로 맞춰야 글자가 깨지지 않는다. Serial.start(), open(), on() 은 존재하지 않는 함수다." },
  { k: "과정", ex0: "아두이노 IDE 에서 업로드하면 보드에서 실행", ex: "작성한 스케치를 컴파일해 보드의 플래시 메모리에 써 넣는 과정을 업로드라고 한다. 컴파일만 하면 오류 검사는 되지만 보드에는 아무것도 옮겨지지 않는다. 다운로드는 반대 방향이고 백업은 파일을 따로 저장해 두는 것이다. 업로드 후 보드는 전원만 있으면 그 코드를 반복 실행한다." },
  { k: "개념", ex0: "버저로 소리(알람·멜로디)를 낼 수 있다", ex: "소리를 내는 부품은 버저(피에조)다. tone(핀, 주파수) 로 멜로디도 만들 수 있다. 브레드보드는 부품을 꽂아 연결하는 판, 점퍼선은 연결용 전선, 저항은 전류를 제한하는 부품이라 소리를 내지 못한다." }
] },
{ track: "os", fixes: [
  { k: "개념", ex0: "스레드는 프로세스 내부의 실행 흐름", ex: "한 프로세스 안에서 메모리를 공유하며 따로 실행되는 흐름이 스레드다. 프로세스보다 가볍게 만들고 전환한다. 파일은 저장 단위, 패키지는 코드 배포 단위, 비트는 정보의 최소 단위라 실행 단위가 아니다. 스레드끼리 메모리를 공유하므로 동기화가 필요하다." },
  { k: "개념", ex0: "문맥 교환으로 여러 작업이 동시에 도는 것처럼", ex: "CPU 가 한 작업에서 다른 작업으로 넘어갈 때 레지스터와 프로그램 카운터 같은 상태를 저장하고 복원하는 것이 문맥 교환이다. 시분할은 그 결과로 CPU 시간을 나누는 정책이고, 선점 스케줄링은 언제 바꿀지 정하는 방식이며, 인터럽트 처리는 교환을 일으키는 계기일 뿐 상태 저장 자체는 아니다." },
  { k: "개념", ex0: "디스크의 프로그램이 실행되면 프로세스", ex: "디스크에 있는 실행 파일이 메모리에 올라가 실행되는 상태를 프로세스라 한다. 자기 메모리 공간과 실행 상태를 가진다. 실행 파일은 아직 실행되기 전의 정적인 파일이고, 소스 코드는 컴파일 전의 글이며, 작업 폴더는 파일이 놓인 위치라 실행 중인 것이 아니다." },
  { k: "개념", ex0: "스케줄러가 실행 순서를 결정", ex: "여러 프로세스 중 CPU 를 누구에게 얼마나 줄지 정하는 일이 스케줄링이고 스케줄러가 맡는다. 컴파일링은 소스를 기계어로 바꾸는 것, 인터럽트는 하드웨어가 CPU 의 주의를 끄는 신호, 가비지 컬렉션은 안 쓰는 메모리를 회수하는 것이라 실행 순서와 관계없다." },
  { k: "개념", ex0: "페이징에서는 메모리를 페이지 단위로 관리", ex: "가상 메모리는 주소 공간을 보통 4KB 크기의 페이지로 나눠 관리하고 물리 메모리의 프레임에 대응시킨다. 비트는 너무 작아 관리 단위가 못 되고, 디스크 섹터는 저장 장치의 단위이며, 토큰은 컴파일러나 인증에서 쓰는 말이라 메모리 관리 단위가 아니다." },
  { k: "속도", ex0: "레지스터 > 캐시 > RAM > 디스크 순으로", ex: "CPU 안에 있는 레지스터가 가장 빠르고 그다음 캐시, RAM, 디스크 순으로 느려진다. 하드디스크는 가장 느린 쪽이고 USB 는 그보다도 느린 외부 장치이며, 클라우드는 네트워크 너머라 비교가 안 된다. 빠를수록 용량이 작고 비싸서 계층으로 나눠 쓴다." },
  { k: "개념", ex0: "가상 메모리는 디스크를 메모리처럼 확장", ex: "가상 메모리는 각 프로세스에 실제 RAM 보다 큰 주소 공간을 주고 당장 안 쓰는 페이지는 디스크(스왑)에 내려 두는 기법이다. 캐시와 레지스터는 CPU 가까이의 작은 고속 저장소라 공간을 늘리지 않고, 버퍼 풀은 데이터베이스가 디스크 페이지를 메모리에 두는 것이라 OS 의 주소 공간 확장과 다르다." },
  { k: "문제", ex0: "메모리 누수는 성능 저하·다운의 원인", ex: "할당한 메모리를 다 쓰고도 반납하지 않아 사용량이 계속 늘어나는 문제가 메모리 누수다. 오래 돌수록 느려지다가 결국 죽는다. 단편화는 빈 공간이 조각나 큰 덩어리를 못 잡는 것, 오버플로는 정해진 크기를 넘어 쓰는 것이고, 가비지 컬렉션은 누수를 줄이려는 회수 기법이지 문제가 아니다." },
  { k: "개념", ex0: "경쟁 상태는 실행 순서에 따라 결과가 달라지는 버그", ex: "여러 스레드가 같은 자원을 동시에 바꿔 실행 순서에 따라 결과가 달라지는 것이 경쟁 상태다. 교착 상태는 서로를 기다리며 멈추는 것, 기아 상태는 특정 작업이 계속 밀려 실행되지 못하는 것, 우선순위 역전은 낮은 우선순위가 높은 쪽을 막는 것이다. 락으로 임계 구역을 보호해 막는다." },
  { k: "개념", ex0: "deadlock 은 서로 무한정 대기하는 상태", ex: "A 는 B 가 가진 자원을, B 는 A 가 가진 자원을 기다리며 둘 다 영원히 멈추는 상황이 교착 상태(deadlock)다. 경쟁 상태는 멈추지 않고 결과가 꼬이는 것, 기아 상태는 한쪽만 계속 못 받는 것이고, 문맥 교환은 정상적인 작업 전환이다. 자원 획득 순서를 통일하면 예방된다." },
  { k: "개념", ex0: "락으로 임계 구역을 보호해 충돌을 막는다", ex: "한 번에 한 스레드만 들어가게 막는 장치는 락(뮤텍스)이다. 잠근 스레드만 풀 수 있다. 카운팅 세마포어는 여러 개를 동시에 허용할 수 있고, 조건 변수는 어떤 조건이 될 때까지 기다리게 하는 것이며, 파이프는 프로세스 사이에 데이터를 전달하는 통로라 상호 배제 장치가 아니다." },
  { k: "이유", ex0: "동시 접근으로 데이터가 깨지는 것을 막기 위함", ex: "임계 구역을 보호하는 이유는 여러 스레드가 동시에 같은 데이터를 바꿔 값이 깨지는 것을 막고 일관성을 지키기 위해서다. 락은 오히려 속도를 조금 늦추므로 속도 향상이 목적이 아니고, 디스크 공간이나 화면 표시와는 아무 관계가 없다." },
  { k: "개념", ex0: "파일 시스템이 저장장치의 파일을 조직", ex: "저장 장치 위에 파일과 디렉터리 구조를 만들어 이름으로 읽고 쓰게 해 주는 것이 파일 시스템이다(ext4, NTFS 등). 레지스터와 캐시는 CPU 근처의 고속 저장소이고, 스택 메모리는 함수 호출 정보를 담는 실행 중 메모리라 파일을 저장하지 않는다." },
  { k: "개념", ex0: "시스템 콜로 사용자 프로그램이 커널 기능을 쓴다", ex: "사용자 프로그램이 파일 읽기나 네트워크 같은 커널 기능을 요청하는 공식 통로가 시스템 콜이다. 디바이스 드라이버는 커널이 하드웨어를 다루는 코드이고, 인터럽트는 하드웨어나 타이머가 CPU 에 보내는 신호라 프로그램이 요청하는 통로가 아니다. read, write, open 이 대표적인 시스템 콜이다." },
  { k: "개념", ex0: "커널은 OS 의 심장부", ex: "운영체제의 핵심으로 CPU, 메모리, 장치를 직접 관리하는 부분이 커널이다. 셸은 사용자가 명령을 입력하는 프로그램이고, 부트로더는 커널을 메모리에 올리는 시작 프로그램이며, 디바이스 드라이버는 커널이 특정 장치를 다루려고 붙이는 부품이라 핵심 그 자체는 아니다." },
  { k: "개념", ex0: "버퍼는 속도 차이를 완충", ex: "빠른 CPU 와 느린 디스크처럼 속도가 다른 두 장치 사이에 데이터를 잠시 담아 두는 곳이 버퍼(캐시)다. 덕분에 빠른 쪽이 느린 쪽을 매번 기다리지 않는다. 페이지 테이블은 가상 주소 변환표, 인터럽트 벡터는 인터럽트 처리 함수 목록, 레지스터 파일은 CPU 내부 저장소라 완충 역할이 아니다." }
] },
{ track: "mleval", fixes: [
  { k: "시그모이드", ex0: "시그모이드 함수가 실수 입력을 0과 1 사이 확률로", ex: "로지스틱 회귀는 선형 결합 결과를 시그모이드 1/(1+e^-z) 에 넣어 0 과 1 사이의 확률로 만든다. 계단 함수는 0 또는 1 만 내서 미분이 안 되고, ReLU 는 0 이상이면 그대로 내보내 상한이 없으며, 코사인은 음수도 나와 확률이 될 수 없다." },
  { k: "앙상블 용어", ex0: "랜덤포레스트, 부스팅 등이 앙상블", ex: "여러 모델의 예측을 합쳐 하나보다 나은 성능을 내는 기법을 앙상블(ensemble)이라 한다. 배깅(랜덤 포레스트), 부스팅(XGBoost), 스태킹이 대표적이다. 서로 다른 실수를 하는 모델을 합치면 분산이 줄어 일반화가 좋아진다." },
  { k: "SVM 마진", ex0: "SVM은 두 클래스 사이의 여백(마진)을 최대로", ex: "SVM 은 두 클래스를 나누는 경계 중 가장 가까운 데이터(서포트 벡터)까지의 거리인 마진이 최대인 것을 고른다. 마진이 넓을수록 새 데이터에도 잘 맞는다. k 는 kNN 의 이웃 수, 깊이는 결정 트리, 학습률은 경사하강법의 하이퍼파라미터라 SVM 의 목적과 무관하다." },
  { k: "TP 정의", ex0: "True Positive: 실제도 양성, 예측도 양성", ex: "실제 양성을 양성으로 맞힌 것이 TP(참 양성)다. 앞 글자는 예측이 맞았는지(True/False), 뒤 글자는 예측한 쪽(Positive/Negative)이다. FN 은 양성을 음성으로 놓친 것, FP 는 음성을 양성으로 잘못 잡은 것, TN 은 음성을 음성으로 맞힌 것이다." },
  { k: "FP 정의", ex0: "False Positive: 음성을 양성으로 오판", ex: "실제 음성을 양성이라고 잘못 예측한 것이 FP(거짓 양성)다. 통계의 1 종 오류에 해당한다. TP 는 양성을 맞힌 것, TN 은 음성을 맞힌 것, FN 은 양성을 놓친 것이다. FP 가 늘면 정밀도가 떨어지고, FN 이 늘면 재현율이 떨어진다." },
  { k: "재현율 중시", ex0: "놓친 환자(FN)를 줄이는 것이 핵심이므로 재현율", ex: "암 진단에서는 실제 환자를 놓치는 FN 이 가장 치명적이므로 실제 양성 중 얼마나 잡아냈는지를 재는 재현율 TP/(TP+FN) 을 우선한다. 정밀도는 양성 예측이 얼마나 맞았는지라 FP 를 줄이는 데 관심이 있고, 정확도는 환자가 드물면 전부 음성이라 해도 높게 나오며, AUC 는 임계값 전체의 종합 지표라 특정 상황의 결정 기준이 아니다." },
  { k: "학습률", ex0: "학습률(learning rate)이 각 갱신 단계의 보폭", ex: "경사하강법에서 기울기 방향으로 한 번에 얼마나 움직일지를 정하는 하이퍼파라미터가 학습률(learning rate)이다. 너무 크면 최소점을 지나쳐 발산하고, 너무 작으면 수렴이 느리다. 학습으로 정해지는 파라미터(가중치)와 달리 사람이 정한다." },
  { k: "큰 학습률", ex0: "보폭이 너무 크면 최소점을 넘나들며 손실이 발산", ex: "학습률이 너무 크면 한 걸음이 골짜기 폭보다 커서 최소점을 좌우로 넘나들다가 손실이 오히려 커지며 발산할 수 있다. 더 빠르고 정확히 수렴하는 것이 아니고, 기울기가 0 이 되는 것은 최소점에 도달했을 때의 이야기이며, 과소적합은 모델 용량이나 학습 부족의 문제다." },
  { k: "작은 학습률", ex0: "보폭이 너무 작으면 조금씩만 움직여 최소점 도달까지", ex: "학습률이 너무 작으면 매 단계 아주 조금만 움직여 최소점까지 가는 데 오래 걸리고 지역 최소점에 갇히기도 한다. 발산은 학습률이 클 때의 문제이고, 기울기 부호가 뒤집히거나 회귀가 분류로 바뀌는 일은 학습률과 무관하다." },
  { k: "ROC 축", ex0: "y축 정밀도, x축 재현율인 것은 PR 곡선", ex: "ROC 곡선은 임계값을 바꿔 가며 y 축에 TPR(재현율), x 축에 FPR 을 찍은 것이다. 왼쪽 위로 붙을수록 좋다. y 정밀도, x 재현율은 PR 곡선이고, 정확도 대 임계값이나 F1 대 정밀도는 표준 곡선이 아니다." },
  { k: "FPR 정의", ex0: "FPR의 분모는 실제 음성 전체", ex: "FPR 은 실제 음성 가운데 양성으로 잘못 판정한 비율 FP/(FP+TN) 이다. 1 − 특이도와 같다. FP/(FP+TP) 는 1 − 정밀도이고, FN/(FN+TP) 는 1 − 재현율(놓친 비율)이며, FP 를 전체 샘플로 나누면 클래스 비율에 따라 의미가 달라져 쓰지 않는다." },
  { k: "AUC 확률 해석", ex0: "AUC는 임계값과 무관한 순위 품질 지표", ex: "ROC-AUC 는 무작위로 뽑은 양성 하나의 점수가 무작위로 뽑은 음성 하나보다 높을 확률과 같다. 임계값을 정하지 않고 점수의 순위만 보기 때문에 임계값에 독립적이다. 정확도는 특정 임계값에서의 값이라 다르고, 정밀도와 재현율의 평균은 AUC 와 관계없는 계산이다." }
] },
{ track: "devops", fixes: [
  { k: "개념", ex0: "Git 은 세계에서 가장 널리 쓰이는 버전 관리", ex: "코드 변경 이력을 기록하고 되돌리고 나누어 작업하게 해 주는 버전 관리 도구가 Git 이다. Docker 는 컨테이너로 실행 환경을 묶는 도구, Zip 은 압축 형식, FTP 는 파일 전송 프로토콜이라 이력을 관리하지 못한다. GitHub 는 Git 저장소를 호스팅하는 서비스다." },
  { k: "명령", ex0: "git commit 으로 스냅샷을 기록", ex: "변경 사항을 하나의 기록으로 남기는 명령은 git commit 이다. 메시지와 함께 그 시점의 스냅샷이 저장된다. save, keep, record 는 Git 명령이 아니다. commit 전에 git add 로 어떤 파일을 기록에 넣을지 스테이징해야 한다." },
  { k: "명령", ex0: "git push 로 원격에 반영", ex: "로컬 커밋을 원격 저장소에 올리는 명령은 git push 다. pull 은 반대로 원격 변경을 가져오는 것이고, clone 은 원격 저장소를 통째로 복사해 오는 첫 작업이며, fork 는 GitHub 에서 남의 저장소를 내 계정으로 복사하는 기능이라 Git 명령이 아니다." },
  { k: "명령", ex0: "git pull 로 원격 변경을 가져온다", ex: "원격의 최신 커밋을 내려받아 현재 브랜치에 합치는 명령은 git pull 이다(fetch 와 merge 를 합친 것). push 는 반대 방향, commit 은 로컬 기록, branch 는 브랜치를 만들거나 나열하는 명령이다. 팀 작업에서는 push 전에 pull 로 먼저 맞춘다." },
  { k: "개념", ex0: "merge(또는 rebase)로 브랜치를 통합", ex: "두 브랜치의 변경을 하나로 합치는 것은 merge 다. 같은 부분을 다르게 고쳤으면 충돌을 직접 풀어야 한다. clone 은 저장소 복사, reset 은 커밋을 되돌리는 것이고, split 은 Git 명령이 아니다. rebase 는 커밋을 다른 브랜치 위로 옮겨 붙이는 또 다른 통합 방법이다." },
  { k: "개념", ex0: "CI/CD 파이프라인으로 개발-배포를 자동화", ex: "테스트를 통과한 코드를 사람 손 없이 서버에 배포하는 것이 CD(지속적 배포)다. CI 가 빌드와 테스트를 자동화하면 CD 가 그 결과를 내보낸다. OS 는 운영체제, DB 는 데이터베이스, CSS 는 스타일시트라 배포 절차와 무관하다." },
  { k: "개념", ex0: "브랜치로 기능별 병렬 개발이 가능", ex: "메인 코드에 영향을 주지 않고 따로 개발하려고 갈라 두는 것이 브랜치다. 기능마다 브랜치를 만들고 끝나면 merge 한다. 커밋은 기록 한 개, 태그는 특정 커밋에 붙이는 이름표, 스태시는 작업 중인 변경을 잠시 치워 두는 것이라 독립 개발 공간이 아니다." },
  { k: "개념", ex0: "CI 로 문제를 빨리 발견", ex: "코드가 올라올 때마다 자동으로 빌드하고 테스트하는 것이 CI(지속적 통합)다. 깨진 코드를 바로 알아채 통합 비용을 줄인다. API 는 프로그램 사이 접점, SQL 은 질의 언어, GUI 는 그래픽 화면이라 자동 검증과 관계없다. GitHub Actions 가 대표 도구다." },
  { k: "용어", ex0: "멱등성 덕분에 선언형 IaC와 구성 관리 도구는 반복 적용이 안전", ex: "같은 작업을 한 번 하든 여러 번 하든 결과 상태가 같은 성질이 멱등성(idempotency)이다. Terraform 이나 Ansible 은 '원하는 상태' 를 선언하고 현재와의 차이만 적용하므로 재실행이 안전하다. 멱등하지 않은 스크립트는 두 번 돌리면 중복 생성이나 오류가 난다." }
] },
{ track: "ai", fixes: [
  { k: "개념", ex0: "머신러닝은 데이터로 패턴을 학습", ex: "규칙을 사람이 일일이 적지 않고 데이터에서 패턴을 스스로 찾아내는 기술이 머신러닝이다. 데이터베이스는 데이터를 저장하고 찾는 도구, 엑셀은 표 계산 프로그램, HTML 은 웹 문서 언어라 스스로 배우지 않는다. 스팸 필터와 추천 시스템이 머신러닝의 예다." },
  { k: "상식", ex0: "LLM = Large Language Model", ex: "ChatGPT 같은 모델은 LLM(Large Language Model, 거대 언어 모델)이다. 방대한 글로 학습해 다음 단어를 예측하며 문장을 만든다. CPU 는 프로세서, SQL 은 데이터베이스 질의 언어, CSS 는 웹 스타일 언어라 언어 모델과 무관하다." },
  { k: "개념", ex0: "딥러닝은 심층 신경망을 사용", ex: "신경망을 여러 층으로 깊게 쌓아 학습하는 머신러닝의 한 분야가 딥러닝이다. 이미지 인식과 언어 모델이 이 방식이다. 블록체인은 분산 장부, 클라우드는 원격 컴퓨팅 자원이라 학습 방법이 아니고, 표층학습이라는 분야는 없다(얕은 모델을 빗대 부르는 말일 뿐이다)." },
  { k: "핵심", ex0: "좋은 데이터가 좋은 모델을 만든다", ex: "머신러닝은 데이터에서 규칙을 배우므로 데이터의 양과 질이 결과를 좌우한다. 잘못된 데이터로 배우면 잘못된 모델이 나온다. 마우스, 폰트, 색깔은 화면과 입력 장치의 문제라 학습 재료가 아니다. 데이터 수집과 정제에 시간 대부분이 들어간다." },
  { k: "개념", ex0: "지도학습은 정답이 붙은 데이터로 학습", ex: "입력과 정답(라벨)이 짝지어진 데이터로 배우는 방식이 지도학습이다. 사진과 '고양이' 라벨을 함께 주는 식이다. 비지도학습(무감독 학습)은 라벨 없이 구조를 찾고, 강화학습은 행동에 대한 보상으로 배운다. 분류와 회귀가 지도학습의 대표 문제다." },
  { k: "도구", ex0: "Django 는 웹 프레임워크", ex: "Django 는 파이썬으로 웹 사이트를 만드는 프레임워크라 딥러닝과 무관하다. TensorFlow 와 PyTorch 는 신경망을 만들고 학습시키는 딥러닝 라이브러리이고, Keras 는 TensorFlow 위에서 쉽게 쓰게 해 주는 고수준 API 다." },
  { k: "개념", ex0: "비지도학습은 라벨 없이 구조를 찾는다", ex: "정답 없이 데이터 자체의 패턴이나 군집을 찾는 방식이 비지도학습이다. 고객을 비슷한 그룹으로 묶는 클러스터링이 예다. 지도학습은 정답이 있어야 하고, 전이학습은 이미 배운 모델을 다른 문제에 옮겨 쓰는 것이며, 암기식 학습은 머신러닝 용어가 아니다." },
  { k: "개념", ex0: "테스트 데이터로 성능을 평가", ex: "모델이 처음 보는 데이터에서도 잘 맞히는지 확인하려고 학습에 쓰지 않고 따로 떼어 둔 것이 테스트 데이터다. 학습에 쓴 데이터로 평가하면 외운 것을 확인하는 셈이라 의미가 없다. 백업은 데이터 보관용이고, 버리는 데이터가 아니라 평가에 꼭 쓰는 데이터다." }
] },
{ track: "backend", fixes: [
  { k: "메서드", ex0: "GET 은 조회, POST 는 전송", ex: "데이터를 가져올 때는 GET 을 쓴다. 서버 상태를 바꾸지 않아 캐시와 재시도가 안전하다. POST 는 새 데이터를 만들거나 보낼 때, PUT 은 기존 데이터를 통째로 바꿀 때, DELETE 는 지울 때 쓴다. 브라우저 주소창 입력은 모두 GET 요청이다." },
  { k: "상태 코드", ex0: "200 은 성공, 404 는 없음, 500 은 서버 오류", ex: "요청이 정상 처리되면 200 OK 다. 2xx 는 성공, 3xx 는 다른 곳으로 가라는 리다이렉트(301 은 영구 이동), 4xx 는 클라이언트 잘못(404 는 없는 주소), 5xx 는 서버 오류(500)다. 첫 자리 숫자만 봐도 어느 쪽 문제인지 알 수 있다." },
  { k: "개념", ex0: "클라이언트가 요청하면 서버가 응답", ex: "브라우저(클라이언트)가 서버에 보내는 것은 요청(Request)이고 서버가 돌려주는 것이 응답(Response)이다. 패킷은 요청이 네트워크에서 쪼개져 가는 단위이고, 쿠키는 요청에 실려 가는 작은 데이터 조각이라 '보내는 것' 의 이름으로는 요청이 맞다." },
  { k: "메서드", ex0: "POST 로 서버에 데이터를 보낸다", ex: "새 데이터를 만들어 달라고 보낼 때는 POST 를 쓴다. 회원가입이나 글 작성이 그렇다. GET 은 조회용이라 본문에 데이터를 싣지 않고, READ 와 FIND 는 HTTP 메서드가 아니다(CRUD 의 R 을 HTTP 로 옮기면 GET 이다)." },
  { k: "형식", ex0: "JSON 은 가볍고 읽기 쉬운 데이터 형식", ex: "웹 API 는 대개 JSON 으로 데이터를 주고받는다. {\"name\": \"kim\"} 처럼 사람도 읽기 쉽고 모든 언어에서 다루기 쉽다. PNG, MP3, EXE 는 이미지, 소리, 실행 파일이라 구조화된 데이터 교환에 쓰지 않는다. 과거에는 XML 을 많이 썼다." },
  { k: "프레임워크", ex0: "Flask/Django 는 파이썬 백엔드 프레임워크", ex: "파이썬의 대표적인 경량 웹 프레임워크는 Flask 다. 몇 줄로 서버를 띄울 수 있다. React, Vue, jQuery 는 브라우저에서 도는 자바스크립트 프런트엔드 도구라 파이썬 서버가 아니다. 더 많은 기능이 기본으로 들어 있는 파이썬 프레임워크는 Django 다." },
  { k: "개념", ex0: "API 로 다른 프로그램의 기능을 사용", ex: "프로그램끼리 정해진 방식으로 정보를 주고받는 규약이 API 다. 날씨 API 를 부르면 날씨 데이터가 온다. URL 은 자원의 주소, IP 는 기기의 주소, HTML 은 화면 문서라 프로그램 사이의 약속이 아니다. 웹 API 는 보통 HTTP 와 JSON 으로 만든다." },
  { k: "저장", ex0: "데이터베이스(DB)에 데이터를 저장", ex: "데이터를 영구적으로 두는 곳은 데이터베이스다. 서버가 꺼져도 남는다. 캐시는 빠르게 다시 쓰려고 잠시 두는 것이라 사라져도 되고, 브라우저 탭은 닫으면 없어지며, CPU 는 계산 장치라 저장소가 아니다. MySQL, PostgreSQL 이 대표적이다." }
] },
{ track: "compiler", fixes: [
  { k: "구분", ex0: "C 는 컴파일해서 실행 파일을 만든다", ex: "C 는 소스 전체를 컴파일러가 미리 기계어로 번역해 실행 파일을 만든 뒤 그것을 실행한다. 그래서 빠르지만 고칠 때마다 다시 컴파일해야 한다. 한 줄씩 해석하며 실행하는 인터프리트 방식은 Python 쪽이고, 압축은 실행 방식이 아니다." },
  { k: "구분", ex0: "Python 은 대표적인 인터프리터 언어", ex: "Python 은 인터프리터가 코드를 읽어 바로 실행한다(내부적으로 바이트코드로 바꾼 뒤 가상 머신이 돌린다). 실행 파일을 미리 만들지 않아 바로 고쳐 볼 수 있지만 C 보다 느리다. 사람이 손으로 번역하거나 기계어를 직접 쓰는 것은 실행 방식이 아니다." },
  { k: "개념", ex0: "컴파일러는 실행 전에 통째로 번역", ex: "소스 코드 전체를 실행 전에 기계어로 번역하는 프로그램이 컴파일러다(gcc, javac). 링커는 컴파일된 조각들을 하나의 실행 파일로 붙이는 다음 단계이고, 에디터는 코드를 적는 도구라 번역하지 않는다. 컴파일 오류는 실행 전에 잡힌다." },
  { k: "개념", ex0: "인터프리터는 번역과 실행을 함께", ex: "코드를 한 줄씩 해석해 그 자리에서 실행하는 프로그램이 인터프리터다. Python 과 JavaScript 엔진이 그렇다. 컴파일러는 미리 전부 번역해 두고, 디버거는 실행을 멈춰 가며 살펴보는 도구, 포매터는 코드 모양을 정리하는 도구라 실행 자체를 맡지 않는다." },
  { k: "용어", ex0: "AST 는 코드의 문법 구조를 나무 형태로", ex: "파서가 토큰을 문법에 맞춰 조립한 결과가 AST(추상 구문 트리)다. 식 a + b * c 가 연산자를 노드로 하는 나무가 된다. B-트리는 데이터베이스 인덱스 구조, 힙은 우선순위 큐용 트리, 리스트는 일렬 구조라 문법 구조를 담는 데 쓰지 않는다." },
  { k: "단계", ex0: "파서(parser)가 문법 구조를 분석", ex: "토큰 열을 문법 규칙에 맞춰 트리로 조립하는 단계가 구문 분석(parsing)이다. 여기서 괄호 짝이 안 맞는 식의 문법 오류가 잡힌다. 정렬, 해싱, 암호화는 컴파일 과정과 무관한 일반 알고리즘이다. 파싱 전 단계가 토큰을 만드는 어휘 분석이다." },
  { k: "용어", ex0: "문법(grammar)이 올바른 코드 구조를 규정", ex: "어떤 토큰 배열이 올바른 프로그램인지 규칙으로 정의한 것이 문법(grammar)이다. 보통 BNF 형태로 적고 파서가 이를 따른다. 표준은 언어 전체의 명세, 라이브러리는 미리 만든 코드 묶음, 주석은 컴파일러가 무시하는 설명이라 문법 규칙 자체가 아니다." },
  { k: "단계", ex0: "렉서(lexer)가 코드를 토큰 열로 나눈다", ex: "소스 문자열을 키워드, 식별자, 숫자, 연산자 같은 최소 단위인 토큰으로 쪼개는 첫 단계가 어휘 분석(lexing)이다. int x = 5; 는 다섯 개의 토큰이 된다. 실행, 압축, 배포는 번역 과정의 단계가 아니다. 토큰이 나온 뒤에야 파서가 구조를 만든다." }
] },
{ track: "cs", fixes: [
  { k: "개념", ex0: "컴퓨터는 0과 1, 즉 2진수로 동작", ex: "컴퓨터 회로는 전압이 있음과 없음 두 상태만 구별하므로 내부에서는 0 과 1 의 2 진수를 쓴다. 16 진수는 사람이 2 진수를 짧게 읽으려고 쓰는 표기이고, 10 진수는 사람의 셈법이며, 문자 코드는 글자를 숫자에 대응시킨 약속이라 수 체계가 아니다." },
  { k: "부품", ex0: "CPU 가 연산을 담당하는 핵심 장치", ex: "명령을 해석하고 계산을 수행하는 컴퓨터의 두뇌는 CPU 다. RAM 은 실행 중 데이터를 담는 메모리, HDD 는 데이터를 보관하는 저장 장치이고, GPU 는 그래픽과 병렬 계산에 특화된 보조 처리 장치라 '만' 이라고 하면 틀리다." },
  { k: "단위", ex0: "비트(bit)가 정보의 최소 단위", ex: "0 또는 1 하나를 나타내는 가장 작은 정보 단위가 비트다. 바이트는 비트 8 개를 묶은 것이고, 픽셀은 화면의 점 하나, 헤르츠는 초당 진동수라 정보량 단위가 아니다. 1 비트로 두 가지, n 비트로 2ⁿ 가지를 구별한다." },
  { k: "단위", ex0: "8비트 = 1바이트", ex: "비트 8 개를 묶은 것이 1 바이트다. 영문자 하나가 대략 1 바이트다. 킬로바이트는 1,024 바이트, 메가바이트는 그 1,024 배이고, 워드는 CPU 가 한 번에 다루는 크기(32 나 64 비트)라 값이 정해져 있지 않다." },
  { k: "개념", ex0: "Windows, macOS, Linux 가 운영체제", ex: "하드웨어를 관리하고 프로그램을 실행시켜 주는 기본 소프트웨어가 운영체제(OS)다. Windows, macOS, Linux 가 그것이다. 컴파일러는 코드를 번역하는 도구, 백신은 보안 프로그램, 브라우저는 웹을 보는 프로그램이라 모두 운영체제 위에서 도는 응용 프로그램이다." },
  { k: "구분", ex0: "Python 은 프로그래밍 언어", ex: "Python 은 프로그램을 짜는 언어이지 운영체제가 아니다. Linux 는 서버와 안드로이드의 바탕이 되는 운영체제, macOS 는 애플 컴퓨터, iOS 는 아이폰의 운영체제다. Python 은 이 운영체제들 위에서 실행된다." },
  { k: "저장", ex0: "RAM 은 빠르지만 전원이 꺼지면 사라진다", ex: "실행 중인 프로그램과 데이터를 임시로 담는 빠른 장치가 RAM(메모리)이다. 전원이 꺼지면 내용이 사라진다. 하드디스크, CD, USB 는 전원이 없어도 내용이 남는 대신 훨씬 느려서 보관용으로 쓴다." },
  { k: "저장", ex0: "SSD·HDD 는 영구 저장장치", ex: "전원을 꺼도 데이터가 남는 것은 SSD 와 하드디스크 같은 보조 기억 장치다. RAM, CPU 캐시, 레지스터는 모두 전원이 있을 때만 값을 유지하는 휘발성 저장소라 껐다 켜면 비어 있다. 그래서 문서를 '저장' 해야 디스크에 남는다." }
] },
{ track: "dbt", fixes: [
  { k: "키", ex0: "기본키(Primary Key)는 중복·NULL 이 없어야", ex: "각 행을 유일하게 식별하도록 정한 키가 기본키(PK)다. 중복과 NULL 이 허용되지 않는다. 후보키는 기본키가 될 수 있는 후보들이고 그중 하나를 골라 기본키로 삼는다. 외래키는 다른 테이블을 참조하는 키이며, 보조 인덱스는 조회 속도용 구조라 식별 역할이 아니다." },
  { k: "키", ex0: "외래키로 테이블 간 관계(참조 무결성)", ex: "다른 테이블의 기본키 값을 담아 관계를 맺는 열이 외래키(FK)다. 주문 테이블의 고객 ID 가 고객 테이블의 기본키를 가리키는 식이다. 참조 대상이 없는 값은 넣을 수 없어 무결성이 지켜진다. 슈퍼키와 대체키는 한 테이블 안의 식별 개념이고 기본키는 참조되는 쪽이다." },
  { k: "성능", ex0: "인덱스는 책의 색인처럼 원하는 행을 빨리", ex: "특정 열의 값으로 행을 빨리 찾도록 따로 만드는 정렬된 자료구조가 인덱스다. 책 뒤의 색인과 같다. 구체화 뷰는 질의 결과를 저장해 둔 것, 트리거는 변경 때 자동 실행되는 코드, 커서는 행을 하나씩 순회하는 도구라 조회를 빠르게 하는 구조가 아니다. 인덱스는 쓰기를 조금 느리게 한다." },
  { k: "개념", ex0: "정규화로 중복과 이상 현상을 줄인다", ex: "같은 데이터가 여러 곳에 반복되지 않도록 테이블을 나누는 설계 과정이 정규화다. 중복이 있으면 한 곳만 고쳤을 때 값이 어긋나는 갱신 이상이 생긴다. 캐싱과 인덱스는 성능 기법, 암호화는 보안 기법이라 설계 구조를 바꾸지 않는다." },
  { k: "개념", ex0: "트랜잭션은 전부 성공하거나 전부 취소", ex: "여러 SQL 작업을 하나의 논리적 단위로 묶어 전부 성공하거나 전부 취소되게 하는 것이 트랜잭션이다. 계좌 이체에서 출금과 입금이 함께 묶인다. 커밋은 트랜잭션을 확정하는 명령이고, 스키마는 테이블 구조 정의, 인덱스 스캔은 조회 방법이라 묶음 단위가 아니다." },
  { k: "개념", ex0: "원자성·일관성·고립성·지속성 = ACID", ex: "트랜잭션이 지켜야 할 네 성질인 원자성, 일관성, 고립성, 지속성의 머리글자가 ACID 다. CRUD 는 생성, 읽기, 갱신, 삭제라는 기본 연산의 약자이고, REST 는 웹 API 설계 방식, JSON 은 데이터 형식이라 트랜잭션 성질과 무관하다." },
  { k: "개념", ex0: "원자성 덕분에 중간에 실패하면 전체가 취소", ex: "'전부 되거나 전부 안 됨' 은 원자성(Atomicity)이다. 중간에 실패하면 이미 한 작업도 롤백된다. 일관성은 규칙이 깨지지 않는 것, 고립성은 동시에 도는 트랜잭션이 서로 간섭하지 않는 것, 지속성은 커밋된 결과가 장애 후에도 남는 것이다." },
  { k: "구조", ex0: "B-트리는 정렬을 유지하며 빠른 검색·범위 조회", ex: "관계형 데이터베이스 인덱스는 대개 B-트리(B+트리)다. 정렬을 유지해 같음 비교뿐 아니라 범위 조회와 정렬에도 쓰이고 디스크 블록에 맞게 넓고 얕다. 해시는 같음 비교만 되고, 연결 리스트는 찾는 데 O(n) 이며, 이진 힙은 최솟값만 빨리 꺼내는 구조라 인덱스로 부적합하다." }
] }
];
