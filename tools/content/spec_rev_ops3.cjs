const UNIT = "코드 리뷰 — 무엇을 결함이라 부를 것인가";
module.exports = {
  source: "./rev_ops3.cjs", xp: 70,
  entries: [

    { track: "arch", guide: "", unit: UNIT, lesson: "설계 문서와 계약을 읽는다", slice: [0, 4],
      th: { sum: "설계 리뷰에서 볼 것은 코드가 아니라 **약속**이다 — 서비스의 경계, API 의 계약, 이벤트의 모양, 캐시의 규칙.",
        body: [
          { h: "나눈 값을 스스로 버리지 마라", t: "서비스를 나누는 이유의 절반은 **따로 바꿀 수 있다**는 것이다. 그런데 데이터베이스를 공유하거나 넷을 한꺼번에 배포하면 그 이점이 사라지고 나눈 비용만 남는다. 호출의 모양도 같은 자리에서 본다 — 동기로 줄줄이 부르면 지연이 더해지고 **가용성은 곱해져** 떨어진다. 각각 99.9% 인 셋을 이어 부르면 99.7% 다. 늦어도 되는 일은 메시지로 흘려보내면 그 곱셈에서 빠진다." },
          { h: "바깥이 읽는 신호와 우리 취향", t: "HTTP 상태 코드는 우리끼리의 약속이 아니라 프록시·캐시·재시도·경보가 모두 읽는 **공용 신호**다. 오류를 200으로 돌려주면 그 장치들이 전부 눈을 감는다. 이벤트도 공용 계약이라 한 번에 다 같이 바꿀 수 없으니 판을 두고, 여러 곳에 복제되어 오래 남으므로 **개인정보는 싣지 않는다.** 반대로 동사 경로나 판을 어디에 적을지는 팀이 정할 취향이다. 리뷰에서 이 둘을 섞어 적으면 진짜 문제가 묻힌다." }],
        code: { c: "GET /orders?limit=50&cursor=...   # 상한은 처음부터\n{\"type\":\"OrderPaid\",\"v\":2,\"eventId\":\"...\",\"userId\":41}\n# 캐시는 없어도 돌아야 한다", cap: "공용 신호와 우리 취향을 나눈다" },
        key: ["나눴으면 따로 배포한다", "가용성은 곱해진다", "상태 코드는 공용 신호다", "이벤트에 개인정보를 싣지 않는다"] } },

    { track: "cs", guide: "", unit: UNIT, lesson: "자료구조와 동시성 코드를 읽는다", slice: [4, 8],
      th: { sum: "기초 자료구조의 리뷰는 **약속이 지켜지는가**를 묻는 일이다 — 상수 시간, 반드시 줄어드는 구간, 지켜지는 불변식.",
        body: [
          { h: "성능은 전제 위에 서 있다", t: "해시 테이블의 상수 시간은 '키가 고르게 흩어지고 사슬이 짧게 유지된다' 는 **전제 위에** 있다. 해시가 키 길이만 보거나 칸을 늘리지 않으면 그 전제가 무너져 사실상 배열을 훑는 것이 된다. 이진 탐색의 정확성도 마찬가지다 — '매번 구간이 반드시 줄어든다' 가 깨지면 멈추지 않고, '정렬돼 있다' 가 깨지면 **틀린 답을 조용히** 돌려준다. 함수가 확인할 수 없는 전제는 반드시 약속으로 적어 둔다." },
          { h: "지운 것과 갈라진 것", t: "C 에서 가장 다루기 어려운 고장은 반환한 메모리를 읽는 것이다. 곧바로 죽지 않고 한참 뒤 엉뚱한 자리에서 터지므로, 다음 자리는 **지우기 전에** 담아 둔다. 동시성에서는 <b>확인과 변경이 갈라진 자리</b>가 같은 역할을 한다 — 조건을 본 시점과 값을 바꾸는 시점 사이에 남이 끼어들 수 있으면 그 조건은 이미 낡았다. 잠금은 손으로 풀지 말고 범위를 벗어날 때 저절로 풀리게 하며, **무엇을 지키는 잠금인지** 데이터와 함께 묶어 둔다." }],
        code: { c: "Node *next = cur->next;  free(cur);  cur = next;\nwith lock:            # 확인부터 차감까지 통째로\n    if balance >= n: balance -= n", cap: "약속이 지켜지는지 묻는다" },
        key: ["상수 시간은 전제 위에 있다", "구간은 반드시 줄어야 한다", "지우기 전에 다음을 담는다", "확인과 변경은 한 잠금 안에"] } },

    { track: "stat", guide: "", unit: UNIT, lesson: "분석 절차와 보고를 읽는다", slice: [8, 12],
      th: { sum: "통계 리뷰는 코드보다 **절차**를 본다. 계산은 맞는데 절차가 틀린 분석이 가장 흔하고 가장 위험하다.",
        body: [
          { h: "언제 멈출지 미리 정하라", t: "실험을 매일 들여다보며 선을 넘는 순간 멈추면, 효과가 **전혀 없어도** 여러 날 중 언젠가는 넘는다. 볼 때마다 우연히 넘을 기회가 새로 생기기 때문이다. 같은 함정이 여러 실험을 한꺼번에 돌릴 때도 있다 — 40번 시도하면 5% 기준에서 평균 두 개는 우연히 유의해지므로, 이긴 것만 골라 보고하면 그 셋은 우연과 구별되지 않는다. 처방은 하나다: **표본 크기와 멈출 규칙을 미리 정하고**, 몇 번 시험했는지 함께 적는다." },
          { h: "무엇을 재고 무엇을 말하는가", t: "유의성은 '차이가 0이 아니다' 까지만 말한다. 표본이 커지면 0.01퍼센트 차이도 유의해지므로 **효과의 크기와 구간**을 함께 봐야 배포할 값어치가 있는지 알 수 있다. 회귀 계수도 그렇다 — 관측 데이터에서 나온 계수는 함께 움직인 정도이지 '늘리면 어떻게 되는지' 가 아니다. 표본을 모으는 방식도 결과를 정한다: 스스로 참여한 사람만 모으고 **이미 떠난 사람은 물어볼 수조차 없으면**, 만족도는 반드시 실제보다 높게 나온다." }],
        code: { c: "# 미리 정한다\nn = required_sample(mde=0.02, power=0.8, alpha=0.05)\n# 그리고 함께 적는다: 시도 40회 중 유의 3건", cap: "계산이 아니라 절차가 틀린다" },
        key: ["멈출 규칙을 미리 정한다", "여러 번 시험하면 기준도 바뀐다", "유의와 값어치는 다른 말", "떠난 사람은 응답하지 않는다"] } },

    { track: "math", guide: "", unit: UNIT, lesson: "수치 코드를 읽는다", slice: [12, 16],
      th: { sum: "수치 코드의 결함은 **수학적으로는 맞는데 컴퓨터에서 틀리는** 자리에 모여 있다.",
        body: [
          { h: "실수는 정확하지 않다", t: "0.1 은 이진법으로 딱 떨어지지 않아 열 번 더해도 정확히 1이 되지 않는다. 그래서 실수를 등호로 견주면 안 되고, 특히 **잔액이 0인지**를 등호로 묻는 코드는 아주 작은 오차 하나로 영영 닫히지 않는 계좌를 만든다. 근본 처방은 애초에 실수로 다루지 않는 것이다 — 통화는 최소 단위의 정수나 십진 자료형으로 담는다. 같은 결의 함정이 <b>자릿수 소실</b>이다: 큰 수에서 큰 수를 빼는 분산 공식은 수학적으로는 맞지만, 값이 크고 서로 가까우면 의미 있는 자릿수가 사라져 음수까지 나온다." },
          { h: "단위와 무작위", t: "삼각함수는 라디안을 받는다. 도를 그대로 넣으면 오류 없이 **전혀 다른 곳**으로 돈다. 각을 구하는 쪽은 도를 내놓고 회전하는 쪽은 라디안을 기대하면, 둘을 이어 붙이는 순간 조용히 틀린다. 단위는 이름에 적거나 한쪽으로 통일한다. 무작위에도 두 함정이 있다 — 언어가 기본으로 주는 난수는 **암호에 쓸 수 없고**, 비교 함수 자리에 난수를 넣어 섞는 방법은 정렬의 전제를 어겨 고르게 섞이지 않는다." }],
        code: { c: "Math.abs(a - b) < 1e-9        // 등호 대신\nMath.atan2(dy, dx)            // 사분면을 살린다\ncrypto.getRandomValues(...)   // 토큰에는 이쪽", cap: "수학적으로 맞아도 컴퓨터에서 틀린다" },
        key: ["실수는 등호로 견주지 않는다", "돈은 정수로 담는다", "삼각함수는 라디안을 받는다", "토큰에 일반 난수를 쓰지 않는다"] } },
  ],
};
