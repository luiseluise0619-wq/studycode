/* fp·math·ai·cs·os·ml·test·arduino·net·numpy·php·stat·web — 남은 26문항. */
module.exports = [
{ track: "fp", fixes: [
  { k: "던지기와 돌려주기", o: [
    "값으로 돌려주면 실패 가능성이 반환 타입에 드러나 부르는 쪽이 처리하지 않고는 값을 꺼낼 수 없다. 예외는 시그니처에 흔적이 없어 빠뜨려도 컴파일 단계에서 드러나지 않는다",
    "값으로 돌려주는 쪽이 빠르므로 순전히 성능 때문에 선택한다 — 예외는 스택을 풀어야 하므로 실패가 잦은 경로에서는 결과 값을 만드는 비용이 훨씬 싸고, 그 밖의 차이는 없다",
    "둘은 완전히 같으며 문법적 취향의 차이일 뿐이다 — 어느 쪽이든 실패는 호출자에게 전달되고, 처리하지 않으면 프로그램이 멈추므로 안전성은 같고 팀 규약으로 정하면 된다",
    "예외는 함수형 언어에 존재하지 않으므로 쓸 수 없다 — 순수 함수는 반환값 말고는 바깥에 영향을 줄 수 없으므로 던지기 자체가 불가능하고, 값으로 돌려주는 것이 유일한 길이다" ] },
  { k: "경계에서만 감싼다", o: [
    "Optional 은 반환값에만 쓰고 인자에는 쓰지 않으면 모든 문제가 해결된다 — 인자로 받은 값은 호출자가 이미 확인한 것이므로 함수 안에서는 벗길 일이 없고, 반환값만 감싸면 껍질이 한 겹으로 줄어든다",
    "널을 쓰지 않으면 Optional 도 필요 없으므로 빈 문자열과 0 같은 기본값으로 대신한다 — '없음' 을 특별한 값으로 표현하면 타입이 단순해지고 감싸고 벗기는 코드가 통째로 사라진다",
    "모든 함수가 Optional 을 주고받아야 안전하므로 지저분해도 그대로 두는 것이 옳다 — 어디서든 값이 없을 수 있다는 전제를 지켜야 널 접근이 원천 차단되고, 잡음은 안전의 대가다",
    "값이 없을 수 있는 경계에서 한 번 감싸고, 안쪽 로직은 값이 있다는 전제로 단순하게 쓴 뒤 내보낼 때 한 번 벗긴다. 모든 함수가 감싸진 값을 주고받으면 껍질을 벗기고 씌우는 잡음만 늘어난다" ] },
  { k: "나누면 손해인 경우", o: [
    "나누고 각 조각을 넘기고 결과를 모으는 고정 비용이 실제 계산보다 크기 때문이다 — 원소가 적거나 원소당 계산이 가벼우면 순차가 빠르다",
    "병렬 실행은 언제나 더 느리며 아주 큰 데이터에서만 예외적으로 빠르다 — 스레드를 만들고 동기화하는 비용이 워낙 커서 수백만 원소는 되어야 본전을 찾는다",
    "자바스크립트는 병렬 실행을 지원하지 않기 때문이다 — 단일 스레드 언어라 '병렬' 이라 부르는 것은 실제로는 번갈아 도는 것이고, 전환 비용만 더해져 느려진다",
    "배열은 병렬화할 수 없고 스트림으로 바꿔야 한다 — 배열은 인덱스로 묶여 있어 조각으로 나눌 수 없고, 지연 평가되는 스트림이어야 조각마다 독립적으로 계산된다" ] },
  { k: "렌즈로 바꾸면", o: [
    "원본은 그대로이고, 손대지 않은 가지는 원본과 같은 객체다 — 바뀐 길목만 새로 만들어진다",
    "원본도 함께 바뀐다 — 렌즈는 경로를 따라 내려가 그 자리의 값을 제자리에서 수정하므로 원본을 들고 있던 쪽도 새 값을 본다",
    "원본은 그대로지만 전체가 깊이 복사되어 원본과 공유되는 객체가 없게 된다 — 안전을 위해 모든 가지를 새로 만들므로 메모리는 두 배가 된다",
    "원본이 얼어붙어 더 이상 못 쓰게 된다 — 렌즈가 한 번 지나간 객체는 freeze 되어 이후의 수정 시도가 모두 예외로 끝난다" ] },
] },
{ track: "math", fixes: [
  { k: "빈 목록에서 참", o: [
    "버그다. 빈 배열에서는 두 결과가 모두 false 여야 일관된다 — 검사할 원소가 없는데 '모두 양수' 라고 답하는 것은 근거 없는 긍정이고, every 와 some 은 같은 입력에 같은 답을 내야 한다",
    "버그가 아니다. '모든 x 가 P' 는 P 가 아닌 x 가 하나도 없다는 뜻이라, 원소가 없으면 반례도 없으니 참이다 — 반대로 '어떤 x 가 P' 는 사례가 하나도 없으니 거짓이다",
    "버그다. 판단할 대상이 없으므로 참도 거짓도 아닌 undefined 를 돌려주는 것이 옳다 — 빈 입력은 질문 자체가 성립하지 않는 경우라 불리언으로 답하면 호출자가 잘못된 분기를 타게 된다",
    "구현마다 다르며 표준으로 정해져 있지 않다 — 빈 배열의 every 는 언어와 라이브러리가 각자 정하는 경계 조건이라, 이식성이 필요하면 길이를 먼저 확인하고 따로 처리해야 한다" ] },
  { k: "모듈러 역원", o: [
    "a 가 홀수일 때만 존재한다. 6 이 짝수라서 없다 — 짝수는 2 의 배수라 어떤 수를 곱해도 짝수이고, 짝수에서 1 을 뺀 값은 9 의 배수가 될 수 없으므로 역원이 없다",
    "gcd(a, m) = 1 일 때만 존재한다. gcd(6, 9) = 3 이므로 6 의 9 에 대한 역원은 없다 — 위처럼 배수가 0·3·6 만 맴돈다",
    "a < m 이기만 하면 역원은 존재하므로 6 의 9 에 대한 역원도 있다 — 곱셈표를 끝까지 적으면 6 의 줄 어딘가에 1 이 나오게 되어 있고, 그 열의 수가 역원이다",
    "m 이 소수일 때만 존재한다. 9 는 합성수라서 없다 — 소수 모듈러에서만 0 이 아닌 모든 수가 역원을 가지므로, 9 처럼 합성수이면 어떤 a 에 대해서도 역원을 말할 수 없다" ] },
  { k: "입력이 흔들리면", o: [
    "두 문제의 차이는 계산 속도에만 영향을 준다 — 입력이 흔들리는 문제는 수렴이 느려 반복을 더 돌려야 할 뿐이고, 충분히 기다리면 같은 정확도에 이른다",
    "B 는 계산이 불가능한 문제이므로 처음부터 풀려고 시도하지 않는 것이 옳다 — 답이 입력에 따라 널뛰면 어떤 값을 내도 의미가 없으므로 문제를 다시 정의해야 한다",
    "B 는 아무리 좋은 알고리즘을 써도 정확한 답을 얻을 수 없다 — 입력에 이미 들어 있는 미세한 오차가 답을 크게 흔들기 때문이다",
    "B 는 알고리즘을 개선하면 A 만큼 정확해질 수 있다 — 흔들림은 계산 도중 쌓이는 반올림 때문이므로 더 높은 정밀도와 안정한 공식을 쓰면 입력이 같은 한 답도 같아진다" ] },
  { k: "같은 식 다른 답", o: [
    "반복문으로 쓰면 부정확해지므로 되도록 반복문을 피하고 닫힌 식으로 바꿔 써야 한다 — 반복은 오차를 쌓지만 닫힌 식은 연산이 한 번뿐이라 언제나 정확하다",
    "0.1 이라는 값이 특별해서 생기는 예외적인 경우다 — 이진수로 딱 떨어지지 않는 몇몇 소수만 문제이고, 0.5 나 0.25 같은 값으로 계산하면 어떤 식이든 같은 답이 나온다",
    "곱셈이 덧셈보다 정확하게 구현되어 있기 때문이다 — 곱셈은 유효 숫자를 그대로 곱하지만 덧셈은 자릿수를 맞추느라 잘라 내므로, 같은 식이라도 덧셈이 많은 쪽이 틀린다",
    "연산을 거칠 때마다 반올림이 일어나므로, 같은 값에 이르는 경로가 다르면 쌓이는 오차도 다르다 — 식의 형태가 정확도를 정한다" ] },
] },
{ track: "ai", fixes: [
  { k: "ai-eval-judge", o: [
    "판정 모델도 편향(위치 편향, 장문 선호, 자기 모델 선호)을 가지므로 사람 라벨과의 일치도를 먼저 검증해야 한다",
    "판정 모델은 생성 모델과 달라야 정확해진다 — 같은 모델이면 자기 답을 알아보고 후하게 주므로, 다른 모델을 쓰기만 하면 편향은 사라진다",
    "판정 결과는 결정적이라 같은 입력에 같은 점수가 나온다 — 채점은 생성이 아니라 분류이므로 temperature 와 무관하게 재현되고, 그래서 한 번만 돌리면 된다",
    "판정 모델은 사람보다 정확하므로 사람 검토가 필요 없다 — 피로도 편견도 없이 수천 건을 같은 기준으로 보므로, 사람 라벨은 오히려 잡음이 되어 빼는 편이 낫다" ] },
  { k: "ai-rag-structure", o: [
    "정확히 N 자마다 잘라 내는 것이 최선 — 청크 길이가 같아야 임베딩의 분포가 고르고, 구조를 따지느라 길이가 들쭉날쭉해지면 검색 점수가 비교되지 않는다",
    "문서 전체를 하나의 청크로 두는 것이 문맥 보존에 가장 좋다 — 잘라 내면 앞뒤 관계가 끊기므로, 컨텍스트 창이 허락하는 한 문서 단위로 넣는 것이 정확도가 높다",
    "표는 셀 단위로 쪼개 각각 따로 임베딩한다 — 셀 하나가 사실 하나이므로 가장 작은 단위로 나눠야 질문과 정확히 맞는 조각을 찾을 수 있다",
    "헤딩, 문단, 코드블록 같은 구조 경계를 존중하며 자르고 섹션 제목 등 메타데이터를 함께 저장한다" ] },
  { k: "ai-rag-ann", o: [
    "정확한 완전탐색과 결과가 100% 똑같다 — 그래프를 따라 내려가는 방식이 다를 뿐 도달하는 이웃은 같으므로, 정확도를 잃지 않고 속도만 얻는다",
    "벡터의 차원 수를 자동으로 줄여 준다 — 인덱스를 만들 때 주성분만 남기므로 저장 공간이 줄고, 검색도 낮은 차원에서 이루어져 빨라진다",
    "데이터가 늘어도 메모리 사용량이 변하지 않는다 — 그래프의 층 수가 고정되어 있어 벡터를 아무리 넣어도 인덱스 크기는 상한 안에 머문다",
    "검색 속도를 위해 약간의 재현율을 희생하며, 파라미터로 속도와 정확도를 조절한다" ] },
] },
{ track: "cs", fixes: [
  { k: "emoji-js-length", o: [
    "이모지 뒤에는 눈에 보이지 않는 종료 문자가 하나 붙기 때문이다 — 문자열은 이모지의 끝을 표시하는 제어 문자를 함께 세므로 항상 하나가 더 나온다",
    "문자열이 UTF-16 코드 유닛 단위로 길이를 세는데 이모지가 서로게이트 페어이기 때문",
    "이모지는 색상 정보 때문에 두 글자로 저장되기 때문 — 모양을 담는 코드와 색을 담는 코드가 짝을 이루므로 어떤 이모지든 길이가 2 다",
    "브라우저가 이모지를 폰트와 문자로 나눠 저장하기 때문 — 글꼴 참조가 문자열 안에 한 칸을 차지해 실제 글자 수보다 하나 많게 센다" ] },
  { k: "utf8-bom-bytes", o: [
    "BOM 은 EF BB BF 3바이트이며 UTF-8 에서는 선택 사항이다",
    "BOM 은 파일 끝에 붙어 파일 종료를 표시한다 — 마지막 바이트 열을 보고 파일이 잘리지 않았는지 확인하는 용도라 편집기가 저장할 때 붙인다",
    "UTF-8 파일은 BOM 으로 시작해야 유효하다 — 앞 3바이트가 없으면 파서가 인코딩을 판별하지 못해 ASCII 로 읽어 한글이 깨진다",
    "BOM 은 문서의 문자 개수를 기록한 헤더다 — 처음 몇 바이트에 길이를 적어 두어 읽는 쪽이 버퍼를 미리 잡을 수 있게 한다" ] },
  { k: "precision-absorption", o: [
    "지수 필드가 초기화되면서 값 자체가 통째로 사라져 버린 것이다 — 크기가 너무 다른 두 수를 더하면 작은 쪽의 지수가 0 으로 재설정되어 0 을 더한 셈이 된다",
    "정수 오버플로가 발생했다 — 큰 수가 표현 범위의 끝에 있어 작은 수를 더하는 순간 자릿수가 넘치고, 넘친 부분이 잘려 원래 값처럼 보인다",
    "덧셈이 곱셈으로 최적화되었기 때문이다 — 컴파일러가 x + tiny 를 x * (1 + tiny/x) 로 바꾸는데 괄호 안이 1 로 반올림되어 x 가 그대로 나온다",
    "작은 값이 유효 비트 범위 밖으로 밀려나 반올림에 흡수된 정밀도 손실이다" ] },
] },
{ track: "os", fixes: [
  { k: "osobs-ns-cg", o: [
    "namespace 는 커널 자체를 분리하고 cgroup 은 네트워크 스택을 분리한다 — 컨테이너마다 커널 사본이 뜨는 것이 namespace 이고, 포트와 라우팅을 나누는 것이 cgroup 이다",
    "namespace 가 자원 제한을, cgroup 이 가시성 격리를 담당한다 — 이름 공간마다 CPU 와 메모리 상한이 붙고, 제어 그룹은 프로세스가 볼 수 있는 PID 와 마운트를 정한다",
    "namespace 는 프로세스가 '무엇을 볼 수 있는지' 를 격리하고, cgroup 은 '자원을 얼마나 쓸 수 있는지' 를 제한한다",
    "둘 다 자원을 제한하는 기능이며 v1 과 v2 버전으로만 갈린다 — namespace 는 옛 이름이고 cgroup 은 계층 구조를 다시 짠 새 이름이라 하는 일은 같다" ] },
  { k: "osipc-pipe-boundary", o: [
    "한 번의 write 호출이 한 번의 read 호출로 짝지어 대응되어 읽힌다 — 커널이 쓰기 단위를 기억해 두므로 읽는 쪽은 버퍼 크기와 무관하게 한 덩어리씩 받는다",
    "보내는 쪽이 나눈 메시지 단위로 경계가 언제나 보존된다 — 파이프는 메시지 큐처럼 동작해 write 마다 경계 표시가 들어가고 read 는 그 표시까지만 읽는다",
    "바이트 스트림이라 메시지 경계가 보존되지 않으므로 앱이 길이 접두사나 구분자로 프레이밍해야 한다",
    "미리 정해 둔 고정 크기 레코드만 주고받을 수 있다 — 파이프를 열 때 레코드 길이를 정하고, 그 배수가 아닌 쓰기는 EINVAL 로 거부된다" ] },
  { k: "ospg-swappiness", o: [
    "스왑 영역이 완전히 비활성화되어 쓰이지 않게 된다 — 10 이하의 값은 커널이 '스왑 끔' 으로 해석하므로 메모리가 모자라면 곧바로 OOM 이 난다",
    "OOM Killer 가 비활성화되어 프로세스가 죽지 않는다 — 스왑을 아끼는 대신 커널이 메모리 부족을 프로세스 종료가 아니라 대기로 처리한다",
    "스왑 파티션의 실제 크기가 줄어든다 — 값은 전체 스왑 가운데 커널이 쓸 수 있는 비율이라 10 이면 파티션의 10% 만 쓴다",
    "익명 페이지를 스왑아웃하기보다 페이지 캐시를 먼저 회수하려는 성향이 강해진다" ] },
] },
{ track: "ml", fixes: [
  { k: "랜덤 탐색", o: [
    "격자에 모든 조합을 빠짐없이 확인하기 때문 — 무작위로 뽑아도 시행이 충분하면 결국 격자의 모든 칸을 한 번씩 지나므로 그리드와 같은 보장을 얻는다",
    "계산량이 항상 더 크기 때문 — 같은 예산이라도 랜덤은 중복된 조합을 다시 시험하므로 더 많은 학습을 돌리고, 그만큼 좋은 값을 만날 기회가 는다",
    "전역 최적해를 항상 보장하기 때문 — 무작위 표본은 편향이 없어 충분히 뽑으면 최적점 근처를 반드시 포함하고, 격자는 칸 사이의 값을 영영 못 본다",
    "중요한 하이퍼파라미터의 값을 더 다양하게 시험해 볼 수 있기 때문" ] },
  { k: "트리 깊이", o: [
    "개별 트리의 과적합(높은 분산)이 여러 트리의 평균으로 상쇄되기 때문",
    "트리 개수가 자동으로 줄기 때문 — 트리가 깊어지면 학습기가 필요한 트리 수를 다시 계산해 줄이므로 전체 복잡도는 일정하게 유지된다",
    "깊이가 깊어지면 편향이 커지기 때문 — 깊은 트리는 데이터를 지나치게 단순화해 편향이 늘지만, 편향은 평균으로 줄일 수 있어 성능이 유지된다",
    "깊게 자란 트리는 원래부터 과적합되지 않기 때문 — 리프마다 소수의 표본만 남아도 배깅으로 뽑은 부분집합이라 훈련 데이터를 외우지 않는다" ] },
] },
{ track: "test", fixes: [
  { k: "제약 조건", o: [
    "애플리케이션 검증만으로는 동시 요청을 막지 못하기 때문",
    "데이터베이스 버전이 올라가면 제약이 사라지기 때문 — 마이그레이션 도구가 스키마를 다시 만들 때 제약을 빠뜨리는 일이 잦다",
    "제약이 있으면 실행 계획이 달라지기 때문 — 옵티마이저가 유니크 제약을 보고 조인 방식을 바꾸므로 성능 회귀를 테스트로 잡아야 한다",
    "제약은 개발 DB 에만 있고 운영에 반영되지 않기 때문 — 운영에서는 성능 때문에 제약을 끄는 것이 관례라 테스트로 대신 지켜야 한다" ] },
  { k: "느린 흐름", o: [
    "짧은 테스트 여러 개로 쪼갠다 — 흐름을 단계마다 끊어 두면 각 테스트는 빨라지고, 실패한 단계도 바로 드러난다",
    "일주일에 한 번만 돌린다 — 느린 E2E 는 매 커밋마다 돌릴 것이 아니라 릴리스 전 점검용으로 남겨 두는 편이 파이프라인을 지킨다",
    "대기 시간을 줄이고 실패하면 재시도한다 — 느린 원인은 대개 넉넉히 잡은 sleep 이므로 짧게 줄이고, 흔들리면 두 번까지 다시 돌린다",
    "UI 를 거치지 않고 준비 단계를 API 로 처리한다" ] },
] },
{ track: "arduino", fixes: [
  { k: "모터를 핀에 직접", o: [
    "모터가 요구하는 전류가 핀 한도의 수십 배라 핀 회로가 손상된다. 핀은 트랜지스터에 '켜라' 는 신호만 주고, 모터 전류는 별도 전원에서 트랜지스터를 거쳐 흐르게 해야 한다",
    "핀이 자동으로 전류를 40mA 로 제한하므로 안전하다 — 출력 회로에 전류 제한이 내장되어 있어 모터가 더 끌어당겨도 그 이상은 흐르지 않고, 모터만 힘없이 돌 뿐이다",
    "전압이 5V 밖에 되지 않아 모터가 돌지 않을 뿐이고 보드에는 아무 영향이 없다 — 모터는 12V 부품이라 5V 로는 코일이 자화되지 않으므로 전류도 거의 흐르지 않는다",
    "모터가 천천히 돌 뿐 아무 문제도 없다 — 핀이 낼 수 있는 만큼만 전류가 흐르므로 토크가 약할 뿐이고, 원하는 속도가 필요하면 PWM 듀티를 올리면 된다" ] },
] },
{ track: "net", fixes: [
  { k: "netch-412", o: [
    "충돌이 실제로 있었는지와 관계없이 서버가 409 Conflict 를 반환하도록 만들어 둔다 — 편집자가 둘 이상 열어 둔 문서는 어떤 PUT 이든 거절되므로 덮어쓰기가 원천적으로 사라진다",
    "PUT 에 If-None-Match 를 붙여 304 를 받게 한다 — 서버의 ETag 가 내가 읽은 것과 같으면 304 로 '바뀐 것 없음' 을 알려 주므로 그때만 저장하면 된다",
    "PUT 에 If-Match: <읽을 때 받은 ETag> 를 붙이고 서버가 불일치 시 412 Precondition Failed 를 반환하게 한다",
    "모든 요청에 no-store 를 붙여 캐시를 막는다 — 남이 바꾼 뒤 내가 옛 사본을 보는 것은 캐시 때문이므로 캐시를 끄면 항상 최신을 읽고 충돌도 사라진다" ] },
] },
{ track: "numpy", fixes: [
  { k: "메모리 보관", o: [
    "뷰가 원본 버퍼를 참조하므로 원본 전체가 메모리에서 해제되지 않는다",
    "뷰는 원본 값이 바뀌어도 내용이 갱신되지 않는다 — 슬라이싱 시점의 값이 굳어지므로 오래 들고 있으면 원본과 어긋난 옛 값을 보게 된다",
    "뷰는 원본이 바뀌어도 값이 갱신되지 않고, 반대로 뷰를 고쳐도 원본에 반영되지 않는다 — 양쪽이 독립된 사본이라 동기화를 직접 해야 한다",
    "뷰는 읽기 전용이라 값을 수정할 수 없다 — 슬라이스는 writeable 플래그가 꺼진 채 만들어지므로 대입하면 예외가 난다" ] },
] },
{ track: "php", fixes: [
  { k: "의존 판단", o: [
    "GitHub 스타 수 — 별이 많다는 것은 그만큼 많은 프로젝트가 검증했다는 뜻이므로 품질과 유지보수를 한 번에 가늠할 수 있는 가장 빠른 지표다",
    "그 패키지의 유지보수 상태·의존성 트리 크기·보안 이력, 그리고 직접 구현했을 때의 유지 비용을 견주는 것",
    "다운로드 수가 가장 많은 패키지를 고르는 것 — 설치 수가 곧 실전 검증 횟수이므로, 같은 기능이라면 가장 많이 쓰이는 것을 고르는 편이 문제가 적다",
    "가능한 한 모든 것을 직접 구현한다 — 외부 코드는 언제 깨질지 모르므로 작은 기능은 물론 큰 기능도 직접 짜야 의존성 사고에서 자유롭다" ] },
] },
{ track: "stat", fixes: [
  { k: "사전 분포를 고르기", o: [
    "강한 사전 분포를 써야 표본이 적을 때에도 결론이 빨리 나므로 두는 것이 좋다 — 사전이 뾰족할수록 사후도 빨리 좁혀져 실험을 일찍 끝낼 수 있다",
    "과거 데이터가 있으면 그것을 반영하되, 표본이 적을 때 결론을 좌우할 만큼 강하게 두지는 않는다",
    "사전 분포는 결과에 영향을 주지 않으므로 아무거나 좋다 — 데이터가 쌓이면 사후 분포는 사전과 무관하게 같은 곳으로 수렴하므로 고민할 가치가 없다",
    "균등 분포가 옳다 — 편견을 넣으면 안 된다. 어떤 값이든 같은 확률로 두어야 데이터만이 결론을 정하고, 과거 실험을 반영하는 것은 결과를 오염시킨다" ] },
] },
{ track: "web", fixes: [
  { k: "web-pf-jsanim", o: [
    "합성만 필요한 속성이면 메인 스레드가 바빠도 합성 스레드에서 계속 부드럽게 실행될 수 있다",
    "CSS 애니메이션은 60fps 가 보장되기 때문 — 브라우저가 CSS 전환에는 프레임 예산을 우선 배정하므로 스크립트가 무거워도 끊기지 않는다",
    "자바스크립트는 애니메이션 API 를 제공하지 않기 때문 — 스크립트로는 setTimeout 으로 값을 바꾸는 흉내만 낼 수 있어 프레임에 맞출 수 없다",
    "CSS 애니메이션은 어떤 경우에도 레이아웃 계산을 유발하지 않기 때문 — 스타일 시트로 선언된 변화는 렌더 트리를 건드리지 않으므로 어떤 속성을 움직여도 reflow 가 없다" ] },
] },
];
