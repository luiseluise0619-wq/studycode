/* go — 40자 미만 해설 45개. node ex_short.cjs --apply ex_go.cjs */
module.exports = { track: "go", fixes: [
  { k: "개념", ex0: "func main() 에서 실행이 시작", ex: "Go 프로그램은 main 패키지의 func main() 에서 시작한다. init() 은 main 보다 먼저 자동으로 불리는 초기화 함수라 시작점은 아니고, start 와 run 은 특별한 뜻이 없는 이름이다. main 이 끝나면 다른 고루틴이 남아 있어도 프로그램이 종료된다." },
  { k: "빈칸", ex0: "fmt 패키지가 입출력을 담당", ex: "Println 은 fmt 패키지의 함수라 import \"fmt\" 가 필요하다. fmt 는 format 의 줄임말로 화면 출력과 문자열 서식을 맡는다. 임포트하고 쓰지 않으면 컴파일 오류가 나고, 쓰면서 임포트하지 않아도 오류가 난다." },
  { k: "출력", ex0: "fmt.Println() 으로 출력", ex: "한 줄 출력은 fmt.Println() 이다. 인자를 공백으로 이어 찍고 끝에 줄바꿈을 붙인다. print() 는 디버깅용 내장 함수라 표준 에러로 나가고 서식이 없으며, echo 는 셸, console.log 는 자바스크립트의 것이다. 서식이 필요하면 fmt.Printf 를 쓴다." },
  { k: "함수 정의", ex0: "func 로 함수를 정의", ex: "Go 의 함수 정의 키워드는 func 다. func add(a, b int) int { ... } 처럼 이름·매개변수·반환 타입 순으로 적는다. def 는 파이썬, fn 은 러스트, function 은 자바스크립트의 키워드라 Go 에서는 문법 오류다." },
  { k: "명시 선언", ex0: "var x int = 10 형태도 가능", ex: "변수 선언 키워드는 var 다. var x int = 10 처럼 타입을 적거나 var x = 10 처럼 추론하게 둘 수 있다. let 은 자바스크립트·러스트, val 은 코틀린·스칼라, dim 은 베이직의 키워드다. 함수 안에서는 x := 10 이라는 짧은 선언도 있다." },
  { k: "반복", ex0: "Go 는 for 하나로 모든 반복을 표현", ex: "Go 의 반복문은 for 뿐이다. for i := 0; i < n; i++ 는 카운터 반복, for cond { } 는 while 처럼, for { } 는 무한 반복, for k, v := range m 은 순회다. while, loop, repeat 키워드는 없다." },
  { k: "짧은 선언", ex0: "x := 10 처럼 := 로 짧게 선언", ex: ":= 는 타입을 적지 않고 새 변수를 만들며 값을 넣는 짧은 선언이다. = 는 이미 있는 변수에 대입만 하므로 선언 없이 쓰면 오류다. <- 는 채널 송수신 기호이고 :: 는 Go 에 없다. := 는 함수 안에서만 쓸 수 있고 패키지 수준에서는 var 를 써야 한다." },
  { k: "특징", ex0: "goroutine 으로 쉬운 동시성 처리가 강점", ex: "Go 의 대표 특징은 go 키워드 하나로 가벼운 스레드(고루틴)를 띄우는 동시성이다. 클래스 상속은 없고 임베딩과 인터페이스로 조립하며, 포인터는 있고, 인터프리터가 아니라 네이티브 코드로 컴파일한다." },
  { k: "배열 선언", ex0: "Go 배열은 var 이름 [크기]타입 형태", ex: "Go 배열은 var a [3]int 처럼 이름 뒤에 [크기]타입 순으로 적는다. C 처럼 타입 뒤에 대괄호를 두는 int a[3] 이나 var a int[3] 은 문법 오류이고, array 키워드는 없다. 크기가 타입의 일부라 [3]int 와 [4]int 는 다른 타입이다." },
  { k: "길이 함수", ex0: "len 내장 함수로 길이를 구한다", ex: "배열·슬라이스·맵·문자열의 길이는 모두 내장 함수 len() 으로 잰다. .length 나 .size() 같은 메서드는 없다. 슬라이스에는 len 외에 밑 배열의 여유를 재는 cap() 도 있다." },
  { k: "인덱싱 출력", ex0: "인덱스는 0부터 시작하므로 a[0]은 첫 요소 1", ex: "인덱스는 0 부터 시작하므로 a[0] 은 첫 번째 원소 1 이다. a[1] 이 두 번째다. 1 부터 세는 언어에 익숙하면 하나씩 밀려 읽기 쉽다. 범위를 벗어난 인덱스는 컴파일 시점(상수)이나 실행 시점(변수)에 오류가 난다." },
  { k: "제로값", ex0: "Go는 배열을 제로값으로 초기화한다. int의 제로값은 0", ex: "Go 는 선언만 한 변수도 반드시 제로값으로 채운다. int 의 제로값은 0 이므로 a[2] 는 0 이다. C 처럼 쓰레기 값이 들어 있지 않고, nil 은 포인터·슬라이스·맵 같은 참조 타입의 제로값이라 int 에는 해당하지 않는다." },
  { k: "추가 함수", ex0: "append 내장 함수로 슬라이스에 요소를 추가", ex: "슬라이스 끝에 원소를 붙이는 함수는 append 다. s = append(s, 4) 처럼 결과를 다시 받아야 한다. 용량이 모자라면 새 배열을 만들어 옮기므로 반환값을 버리면 추가가 사라진다. push 나 add 같은 메서드는 없다." },
  { k: "append 출력", ex0: "요소 하나를 추가해 길이가 2에서 3", ex: "길이 2 인 슬라이스에 append 로 원소 하나를 붙였으므로 len 은 3 이다. cap 은 여유에 따라 그대로일 수도 두 배가 될 수도 있지만, 묻는 것은 len 이다. append 의 반환값을 s 에 다시 대입했기 때문에 늘어난 길이가 반영된다." },
  { k: "make 길이", ex0: "make의 두 번째 인자가 슬라이스의 초기 길이", ex: "make([]int, 5) 는 길이 5, 용량 5 인 슬라이스를 만들고 다섯 칸을 0 으로 채운다. 세 번째 인자를 주면 용량만 따로 정할 수 있다(make([]int, 0, 5)). 길이가 5 이므로 s[0]~s[4] 를 바로 쓸 수 있고 append 는 여섯 번째부터 붙는다." },
  { k: "인덱스 출력", ex0: "인덱스 1은 두 번째 요소이므로 20", ex: "슬라이스 {10, 20, 30} 에서 s[1] 은 두 번째 원소 20 이다. 인덱스가 0 부터 시작하기 때문이다. s[0] 이 10, s[2] 가 30 이다." },
  { k: "첫 인덱스", ex0: "Go의 인덱스는 0부터 시작", ex: "Go 의 슬라이스와 배열은 0 부터 센다. 첫 원소는 s[0], 마지막은 s[len(s)-1] 이다. 음수 인덱스는 파이썬과 달리 허용되지 않아 s[-1] 은 컴파일 오류다." },
  { k: "길이 표현식", ex0: "len(s)가 슬라이스의 요소 개수를 반환", ex: "슬라이스 s 의 원소 개수는 len(s) 다. s.len() 이나 s.length 는 문법에 없다. cap(s) 는 밑 배열의 용량이라 개수와 다를 수 있다. 빈 슬라이스와 nil 슬라이스 모두 len 은 0 이다." },
  { k: "범위 초과", ex0: "범위를 벗어난 인덱스 접근은 런타임 패닉", ex: "길이 3 인 슬라이스에서 s[5] 를 읽으면 실행 중에 index out of range 패닉이 나고 프로그램이 멈춘다. 0 이나 nil 을 돌려주지 않고, 자동으로 늘어나지도 않는다. 크기를 늘리려면 append 를 써야 한다." },
  { k: "초기화 함수", ex0: "make로 맵을 초기화", ex: "맵은 make(map[string]int) 로 만들어야 쓸 수 있다. var m map[string]int 처럼 선언만 하면 nil 맵이라 읽기는 되지만 쓰면 패닉이 난다. 리터럴 map[string]int{} 도 같은 역할이다. new 는 포인터를 돌려주므로 맵에는 쓰지 않는다." },
  { k: "맵 조회", ex0: "키 \"a\"에 저장된 값 1을 출력", ex: "m[\"a\"] 는 키 \"a\" 에 저장된 값 1 을 돌려준다. 맵 조회는 m[키] 꼴이고 없는 키면 값 타입의 제로값이 나온다. 있는지 확인하려면 v, ok := m[\"a\"] 로 두 값을 받는다." },
  { k: "키:값 기호", ex0: "맵 리터럴은 키: 값 형태로 콜론", ex: "맵 리터럴은 map[string]int{\"a\": 1, \"b\": 2} 처럼 키와 값을 콜론으로 잇는다. 등호는 대입, 세미콜론은 문장 구분, 화살표 <- 는 채널에 쓰는 기호라 맵 리터럴에는 맞지 않는다. 항목 사이는 쉼표이고 여러 줄로 쓰면 마지막에도 쉼표를 붙여야 한다." },
  { k: "맵 생성", ex0: "make(map[키타입]값타입) 형태로 맵을", ex: "맵 타입은 map[키타입]값타입 이고 make(map[string]int) 로 만든다. 소괄호 map(string) 은 문법 오류, .new() 메서드나 new map 구문은 Go 에 없다. 리터럴 map[string]int{} 로 만들어도 같다." },
  { k: "존재 확인", ex0: "v, ok := m[key] 형태에서 ok가 존재 여부", ex: "맵 조회는 두 값을 돌려줄 수 있다. v, ok := m[key] 에서 ok 가 true 면 키가 있는 것이다. 두 번째 값은 error 가 아니라 bool 이고, exists 나 has 같은 메서드는 없다. 값이 0 인 키와 없는 키를 구분하려면 이 ok 를 봐야 한다." },
  { k: "없는 키", ex0: "없는 키를 조회하면 값은 제로값 0, ok는 false", ex: "없는 키를 v, ok := m[key] 로 조회하면 v 는 값 타입의 제로값(int 면 0), ok 는 false 다. 그래서 0 false 가 찍힌다. 패닉이 나지 않으므로 '없음' 을 알려면 ok 를 확인해야 한다." },
  { k: "ok 타입", ex0: "존재 여부를 나타내는 ok는 bool", ex: "v, ok := m[key] 의 ok 는 bool 이다. 키가 있으면 true, 없으면 false 다. error 타입이 아니라 if !ok { } 처럼 바로 조건에 쓴다. 이름은 관례일 뿐이라 found 처럼 다르게 지어도 된다." },
  { k: "있는 키", ex0: "키 \"b\"는 존재하므로 값 2와 ok true", ex: "키 \"b\" 가 맵에 있으므로 v 는 저장된 값 2, ok 는 true 가 되어 2 true 가 찍힌다. 없는 키였다면 0 false 였을 것이다. Println 은 두 값을 공백으로 나눠 한 줄에 찍는다." },
  { k: "없는 키 값", ex0: "없는 키는 값 타입의 제로값을 반환한다. int라면 0", ex: "맵에 없는 키를 읽으면 패닉이 아니라 값 타입의 제로값이 나온다. int 면 0, string 이면 빈 문자열, 포인터면 nil 이다. -1 같은 특별한 값은 없다. 그래서 값이 0 인지 키가 없는지 구분하려면 v, ok 꼴을 써야 한다." },
  { k: "delete", ex0: "delete(맵, 키) 형태의 내장 함수로 키를 삭제", ex: "맵에서 키를 지우는 것은 내장 함수 delete(m, key) 다. 맵에는 메서드가 없으므로 m.delete(key) 나 m.remove(key) 는 컴파일 오류다. 없는 키를 지워도 오류가 나지 않고 조용히 넘어간다." },
  { k: "삭제 코드", ex0: "delete(m, \"a\") 로 키를 삭제", ex: "delete(m, \"a\") 가 맵 m 에서 키 \"a\" 와 그 값을 지운다. 첫 인자가 맵, 둘째가 키다. 반환값은 없고, 키가 없어도 아무 일 없이 끝난다. 순회 중에 delete 를 불러도 안전하다." },
  { k: "range 순회", ex0: "맵을 range로 순회하면 첫 값이 키, 둘째 값이 값", ex: "for k, v := range m 에서 k 는 키, v 는 값이다. 슬라이스를 range 하면 첫 값이 인덱스이지만 맵은 인덱스가 없으므로 키가 온다. 키만 필요하면 for k := range m, 값만 필요하면 for _, v := range m 으로 쓴다." },
  { k: "순회 순서", ex0: "맵 순회 순서는 보장되지 않으며 실행마다", ex: "Go 의 맵은 순서를 지키지 않는다. range 로 돌면 실행할 때마다 순서가 달라질 수 있도록 일부러 섞여 있다. 삽입 순서나 정렬 순서에 기대는 코드는 버그다. 순서가 필요하면 키를 슬라이스로 뽑아 sort 한 뒤 그 순서로 접근한다." },
  { k: "필드 접근", ex0: "p.X 처럼 점(.)으로 필드에 접근", ex: "구조체 필드는 p.X 처럼 점으로 접근한다. 포인터 변수라도 (*pp).X 대신 pp.X 로 쓸 수 있어 화살표 -> 는 없다. 대괄호는 배열·슬라이스·맵의 인덱싱이고 콜론은 리터럴에서 필드명과 값을 잇는 기호다." },
  { k: "키워드", ex0: "struct 키워드로 구조체를 정의", ex: "구조체는 type 이름 struct { 필드들 } 로 정의한다. struct 가 필드 묶음을 뜻하는 키워드다. class 키워드는 Go 에 없고, 메서드는 구조체 밖에서 리시버를 붙여 따로 정의한다." },
  { k: "필드 출력", ex0: "필드 X에 7을 저장했으므로 p.X는 7", ex: "p.X = 7 로 필드에 7 을 넣었으므로 p.X 를 찍으면 7 이다. 구조체는 값 타입이라 p 의 사본을 만들면 사본의 X 를 바꿔도 원본 p.X 는 그대로 7 이다." },
  { k: "필드 제로값", ex0: "지정하지 않은 필드는 제로값을 가진다. int는 0", ex: "P{} 처럼 값을 주지 않고 만들면 모든 필드가 제로값이 된다. int 필드는 0, string 은 빈 문자열, 포인터는 nil 이다. '정의되지 않음' 상태는 Go 에 없다. 그래서 초기화를 잊어도 쓰레기 값이 아니라 예측 가능한 값이 들어 있다." },
  { k: "구조체 정의", ex0: "type 이름 struct { ... } 형태로 구조체를", ex: "구조체 타입은 type Point struct { X, Y int } 처럼 type 키워드로 시작한다. C 식의 struct Point { } 나 class Point { } 는 Go 문법이 아니다. type 은 구조체뿐 아니라 어떤 타입에든 새 이름을 붙이는 키워드다." },
  { k: "필드명 지정", ex0: "P{X: 1, Y: 2} 처럼 필드명: 값 형태", ex: "필드 이름을 밝혀 만들 때는 P{X: 1, Y: 2} 처럼 중괄호 안에 필드명: 값 을 적는다. 대괄호나 소괄호는 문법 오류이고 new 메서드는 없다. 이름을 밝히면 순서를 바꾸거나 일부만 채워도 되고, 나머지는 제로값이 된다." },
  { k: "위치 초기화", ex0: "위치 순서대로 X=1, Y=2가 되므로 p.Y는 2", ex: "P{1, 2} 처럼 이름 없이 값만 적으면 필드 선언 순서대로 X=1, Y=2 가 들어간다. 그래서 p.Y 는 2 다. 이 방식은 모든 필드를 다 적어야 하고 필드 순서가 바뀌면 값이 어긋나므로, 이름을 밝히는 방식이 더 안전하다." },
  { k: "제로값 리터럴", ex0: "P{} 는 모든 필드를 제로값으로 채운", ex: "P{} 는 필드를 모두 제로값으로 채운 P 값을 만든다. var p P 와 같다. 포인터가 필요하면 &P{} 로 주소를 받는다. 필드를 하나도 적지 않아도 오류가 아니다." },
  { k: "값 타입", ex0: "Go 구조체는 값 타입이라 할당하면 필드 값이 복사", ex: "b := a 는 구조체의 모든 필드를 복사해 b 에 넣는다. 그 뒤 b.X 를 바꿔도 a.X 는 그대로다. 같은 메모리를 가리키게 하려면 포인터 &a 를 넘겨야 한다. 오류나 nil 이 되는 일은 없다." },
  { k: "리시버", ex0: "메서드가 붙는 대상을 나타내는 (p P)를 리시버", ex: "func (p P) Area() int 에서 func 바로 뒤의 (p P) 가 리시버다. 이 메서드가 P 타입에 속한다는 뜻이고 p 는 메서드 안에서 그 값을 부르는 이름이다. 매개변수는 메서드 이름 뒤 괄호에, 반환값은 그 뒤에 온다. 인터페이스와는 별개다." },
  { k: "메서드 호출", ex0: "p.m() 처럼 점으로 메서드를 호출", ex: "메서드는 값 뒤에 점을 찍고 이름과 괄호를 붙여 p.m() 으로 부른다. 인자가 없어도 괄호는 필요하다. p.m 처럼 괄호를 빼면 호출이 아니라 메서드 값을 가리키는 식이 된다." },
  { k: "메서드 출력", ex0: "Area는 W와 H의 곱을 반환하므로 3 곱하기 4는 12", ex: "Area() 는 r.W * r.H 를 돌려준다. W 가 3, H 가 4 이므로 12 다. 리시버 r 이 메서드 안에서 구조체의 필드를 읽는 통로다. 값 리시버라 필드를 바꿔도 원본에는 영향이 없지만, 여기서는 읽기만 하므로 상관없다." },
  { k: "메서드 정의", ex0: "func (리시버) 메서드명() 반환타입 형태로", ex: "메서드는 func (p P) Area() int { } 처럼 func 와 이름 사이에 리시버를 둔다. method 키워드나 func P.Area() 꼴은 없고, 리시버를 매개변수 자리에 두면 그냥 함수가 된다. 구조체를 고치는 메서드는 (p *P) 처럼 포인터 리시버로 만든다." },
] };
