/* 5차 이론 — sql 에서 점수가 가장 높던(가장 어렵던) 두 유닛.

   '실행 계획 읽기 실무' 와 '성능 튜닝 심화' 는 세 레슨이 똑같은 글을 나눠 쓰고 있었다.
   게다가 한 문장이 90자를 넘고, '왜 그런지' 가 없고, '해시' 같은 말을 안 풀고 썼다.
   레슨마다 자기 주제를 갖도록 따로 썼다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

sql: [
{
  unit: "실행 계획 읽기 실무", lesson: "EXPLAIN 읽기",
  th: {
    sum: "`EXPLAIN` 은 데이터베이스가 **어떻게 찾을 작정인지** 미리 보여 준다. 짐작 대신 이걸 본다.",
    body: [
      { h: "느린 이유는 대개 눈에 안 보인다", t: "같은 결과를 내는 쿼리도 찾는 방법에 따라 시간이 백 배씩 차이 난다. 쿼리 글자만 봐서는 어느 쪽인지 알 수 없다. 짐작으로 고치면 엉뚱한 데를 고치고, 느린 자리는 그대로 남는다." },
      { h: "무엇을 보나", t: "계획에는 '어느 표를 먼저 보는지', '인덱스를 쓰는지', '몇 줄쯤 볼 것 같은지' 가 적혀 있다. 이 셋만 읽어도 대부분의 문제는 찾아진다. 특히 '몇 줄쯤' 이 실제와 크게 다르면 통계가 낡은 것이다." },
      { h: "예상이지 결과가 아니다", t: "`EXPLAIN` 은 실행하기 전의 **계획**이다. 실제로 몇 줄을 봤는지는 다를 수 있다. 그래서 계획이 멀쩡한데도 느리면, 실제로 돌려 보고 재는 도구(`EXPLAIN ANALYZE` 등)를 함께 쓴다." },
      { h: "고친 뒤에는 다시 본다", t: "인덱스를 만들었다고 반드시 쓰이는 것은 아니다. 조건 모양이 맞지 않으면 그냥 무시된다. 고친 다음 계획을 다시 보지 않으면, 쓰이지도 않는 인덱스를 믿고 넘어가게 된다." },
    ],
    code: { c: "EXPLAIN QUERY PLAN\nSELECT * FROM 직원 WHERE 부서 = '개발';\n-- SEARCH 직원 USING INDEX ix_부서 (부서=?)", cap: "찾을 방법을 미리 보여 준다" },
    key: ["짐작 대신 계획을 본다", "표 순서·인덱스·예상 줄 수", "고친 뒤 다시 본다"],
  },
},
{
  unit: "실행 계획 읽기 실무", lesson: "스캔 방식",
  th: {
    sum: "찾는 방법은 크게 둘이다. **처음부터 다 훑기**와 **인덱스로 바로 가기**.",
    body: [
      { h: "다 훑기(스캔)", t: "표의 모든 줄을 하나씩 본다. 줄이 열 배 늘면 시간도 열 배 는다. 그런데 표가 작거나 어차피 대부분을 읽어야 한다면, 이 방법이 오히려 빠르다 — 이리저리 오갈 필요가 없기 때문이다." },
      { h: "인덱스로 바로 가기(탐색)", t: "인덱스는 책 뒤의 찾아보기와 같다. 값이 어느 줄에 있는지 미리 정렬해 둔 것이라, 조건에 맞는 자리로 바로 갈 수 있다. 찾는 줄이 전체의 일부일 때 크게 이긴다." },
      { h: "인덱스가 있어도 안 쓰일 때", t: "조건에서 열을 가공하면 인덱스를 못 쓴다. `WHERE substr(이름,1,1)='김'` 처럼 쓰면, 미리 정렬해 둔 것이 소용없어져 결국 다 훑는다. 열은 그대로 두고 오른쪽을 바꾸는 것이 규칙이다." },
      { h: "다 훑기가 늘 나쁜 것은 아니다", t: "표에 줄이 백 개뿐이면 인덱스를 타는 것보다 그냥 훑는 편이 빠르다. 그래서 데이터베이스가 일부러 인덱스를 무시하기도 한다. '스캔이 보이면 무조건 잘못' 이라고 읽으면 엉뚱한 인덱스만 늘어난다." },
    ],
    code: { c: "-- 인덱스를 못 쓴다\nWHERE substr(이름, 1, 1) = '김'\n\n-- 인덱스를 쓴다\nWHERE 이름 >= '김' AND 이름 < '깋'", cap: "열은 그대로, 오른쪽을 바꾼다" },
    key: ["다 훑기와 인덱스 탐색", "열을 가공하면 인덱스가 죽는다", "작은 표는 훑는 게 빠르다"],
  },
},
{
  unit: "실행 계획 읽기 실무", lesson: "조인 방식과 진단",
  th: {
    sum: "표를 이어 붙일 때도 방법이 여럿이다. 어느 표를 **먼저 잡느냐**가 시간을 가른다.",
    body: [
      { h: "한 줄씩 짝 찾기", t: "왼쪽 표를 한 줄씩 훑으면서, 그때마다 오른쪽에서 짝을 찾는다. 오른쪽에 인덱스가 있으면 빠르지만, 없으면 왼쪽 줄마다 오른쪽 전체를 훑게 된다. 줄 수가 곱해지므로 금방 느려진다." },
      { h: "미리 표를 만들어 두고 맞추기", t: "오른쪽 값을 먼저 한 번 훑어 '값 → 줄' 대응표를 만들어 두는 방법도 있다(해시 조인). 그 표는 값을 넣으면 자리가 바로 나오는 구조라, 그다음부터는 짝을 한 번에 찾는다. 대신 만들어 두는 동안 메모리를 쓴다." },
      { h: "작은 쪽을 먼저 잡는다", t: "먼저 잡는 표가 작을수록 뒤에서 할 일이 줄어든다. 데이터베이스가 스스로 정하지만, 통계가 낡으면 큰 표를 작다고 착각해 정반대로 고른다. 갑자기 느려졌다면 통계 갱신부터 확인한다." },
      { h: "조건을 빠뜨리면 곱해진다", t: "`ON` 조건을 안 적으면 왼쪽 모든 줄이 오른쪽 모든 줄과 짝지어진다. 1000줄과 1000줄이면 100만 줄이다. 결과가 갑자기 엄청나게 많아졌다면 이것부터 의심한다." },
    ],
    code: { c: "SELECT d.이름, s.금액\nFROM 직원 d JOIN 실적 s ON s.직원 = d.id;\n-- ON 을 빼면 줄 수가 곱해진다", cap: "작은 쪽을 먼저, 조건은 반드시" },
    key: ["한 줄씩 짝 찾기와 대응표 만들기", "작은 표를 먼저 잡는다", "`ON` 을 빼면 줄이 곱해진다"],
  },
},
{
  unit: "성능 튜닝 심화", lesson: "조인 알고리즘",
  th: {
    sum: "조인은 결국 **짝을 어떻게 찾느냐**의 문제다. 자료 크기에 따라 유리한 방법이 다르다.",
    body: [
      { h: "중첩 반복", t: "왼쪽 한 줄마다 오른쪽을 뒤진다. 오른쪽이 작거나 인덱스가 있으면 아주 빠르다. 반대로 양쪽이 다 크고 인덱스가 없으면 최악이다 — 줄 수가 곱해지기 때문이다." },
      { h: "대응표 만들기", t: "한쪽을 먼저 훑어 '값 → 줄' 대응표를 만든다. 값을 넣으면 자리가 바로 나오는 구조라 짝 찾기가 한 번에 끝난다. 양쪽이 다 클 때 가장 잘 맞지만, 표를 담을 메모리가 필요하다." },
      { h: "양쪽을 정렬해 훑기", t: "두 표를 같은 기준으로 정렬한 뒤 나란히 훑으며 맞춘다. 이미 정렬돼 있거나 인덱스 순서가 맞으면 아주 싸다. 정렬부터 해야 한다면 그 비용이 먼저 든다." },
      { h: "고르는 것은 대개 맡긴다", t: "어느 방법을 쓸지는 데이터베이스가 통계를 보고 정한다. 우리가 할 일은 인덱스를 맞게 만들고 통계를 최신으로 두는 것이다. 방법을 강제로 지정하면 자료가 바뀐 뒤에 오히려 느려진다." },
    ],
    code: { c: "-- 오른쪽에 인덱스가 있으면 중첩 반복이 유리하다\nCREATE INDEX ix_실적_직원 ON 실적(직원);", cap: "인덱스를 맞추고 통계를 최신으로" },
    key: ["중첩 반복 · 대응표 · 정렬 훑기", "크기와 인덱스가 유불리를 정한다", "방법은 대개 맡긴다"],
  },
},
{
  unit: "성능 튜닝 심화", lesson: "실행계획과 인덱스",
  th: {
    sum: "인덱스는 **찾아보기**다. 만들면 읽기가 빨라지지만, 쓸 때마다 같이 고쳐야 한다.",
    body: [
      { h: "공짜가 아니다", t: "줄을 넣거나 고칠 때마다 인덱스도 같이 고쳐야 한다. 그래서 인덱스를 많이 만들수록 쓰기가 느려지고 저장 공간도 는다. '일단 다 만들어 두자' 는 대개 손해다." },
      { h: "순서가 있는 인덱스", t: "여러 열로 만든 인덱스는 **적은 순서대로만** 앞에서부터 쓸 수 있다. `(부서, 급여)` 인덱스는 부서 조건에는 쓰이지만, 급여만 조건으로 걸면 쓰이지 않는다. 자주 쓰는 조건을 앞에 둔다." },
      { h: "필요한 열이 인덱스에 다 있으면", t: "조회하는 열까지 인덱스 안에 들어 있으면 원본 표를 볼 필요가 없다. 찾아보기만 보고 답하는 셈이라 훨씬 빠르다. 자주 쓰는 조회에 한해 이렇게 맞춰 두면 효과가 크다." },
      { h: "만들었으면 확인한다", t: "인덱스를 만들었다고 반드시 쓰이는 것은 아니다. 조건 모양이 맞지 않거나 표가 너무 작으면 무시된다. 계획을 다시 보지 않으면 쓰이지도 않는 인덱스를 믿고 넘어가게 된다." },
    ],
    code: { c: "CREATE INDEX ix_부서_급여 ON 직원(부서, 급여);\n-- 부서 조건: 쓰인다 / 급여만 조건: 안 쓰인다", cap: "앞에서부터만 쓸 수 있다" },
    key: ["인덱스는 쓰기를 느리게 한다", "여러 열은 앞에서부터", "만든 뒤 계획을 다시 본다"],
  },
},
{
  unit: "성능 튜닝 심화", lesson: "쿼리 최적화 패턴",
  th: {
    sum: "쿼리를 빠르게 하는 방법은 대개 셋이다. **덜 읽기 · 덜 만들기 · 인덱스가 살아 있게 쓰기.**",
    body: [
      { h: "필요한 열만 고른다", t: "`SELECT *` 는 쓰지 않는 열까지 다 읽어 온다. 열이 많거나 긴 글이 섞여 있으면 그만큼 느려진다. 필요한 열만 적으면 인덱스만 보고 답할 수 있는 경우도 생긴다." },
      { h: "먼저 걸러 내고 붙인다", t: "조인한 뒤에 거르면, 거를 줄까지 전부 짝을 지어 놓고 버리는 셈이다. 먼저 조건으로 줄인 다음 붙이면 짝지을 양 자체가 줄어든다. 결과는 같은데 하는 일이 다르다." },
      { h: "열을 가공하지 않는다", t: "`WHERE date(만든날) = '2026-01-01'` 처럼 열을 감싸면 인덱스를 못 쓴다. `만든날 >= '2026-01-01' AND 만든날 < '2026-01-02'` 로 바꾸면 인덱스가 그대로 산다. 결과는 같고 속도만 달라진다." },
      { h: "먼저 재고 나서 고친다", t: "느릴 것 같다는 느낌으로 고치면 엉뚱한 데를 고친다. 계획을 보고 어디서 줄을 많이 읽는지 확인한 뒤 거기만 손댄다. 대부분은 인덱스 하나나 조건 모양 하나로 끝난다." },
    ],
    code: { c: "-- 인덱스가 죽는다\nWHERE date(만든날) = '2026-01-01'\n\n-- 인덱스가 산다\nWHERE 만든날 >= '2026-01-01' AND 만든날 < '2026-01-02'", cap: "열은 그대로 두고 범위로 바꾼다" },
    key: ["필요한 열만", "먼저 걸러 내고 붙인다", "열을 감싸면 인덱스가 죽는다"],
  },
},
],

};
