module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 이분 탐색·정렬·힙",
  source: "./algo_sortsearch.cjs",
  lessons: [
    { t: "이분 탐색 — 구간의 의미를 먼저 정한다", n: 5,
      th: { sum: "이분 탐색이 어려운 이유는 아이디어가 아니라 **경계**다. `lo` 와 `hi` 가 무엇을 뜻하는지 한 문장으로 못 쓰면 그 코드는 반드시 한 칸 어긋난다.",
        body: [
          { h: "닫힌 구간과 반열린 구간을 섞지 않는다", t: "`hi = xs.length` 로 시작하면 구간은 `[lo, hi)` 이고 조건은 `while (lo < hi)`, 줄일 때는 `hi = mid` 다. `hi = xs.length - 1` 로 시작하면 `[lo, hi]` 라서 `while (lo <= hi)`, `hi = mid - 1` 이어야 한다. **한쪽 규약을 정해 끝까지 지키는 것**이 전부이고, 두 규약을 섞는 순간 무한 루프나 한 칸 오차가 난다. `lowerBound` 와 `upperBound` 는 비교 연산자 하나(`<` 냐 `<=` 냐)만 다르다 — 그 하나가 '처음 나오는 자리' 와 '마지막 다음 자리' 를 가른다." },
          { h: "값이 아니라 답을 이분 탐색한다", t: "배열을 반으로 접는 것만 이분 탐색이 아니다. **'이 답이 가능한가?' 가 단조로우면** 답 자체를 이분 탐색할 수 있다. 짐을 나르는 최소 용량, 최소 완료 시간, 최대 최소 거리가 모두 이렇다 — 용량이 크면 반드시 가능하고 작으면 반드시 불가능하므로 가능/불가능의 경계가 답이다. 판정 함수 `ok(x)` 를 O(n) 으로 짜면 전체가 O(n log 범위) 가 된다. 탐색 범위의 하한을 **가장 큰 원소**로 잡아야 하는 것 같은 사소한 실수가 오답의 대부분이다." }],
        code: { c: "while (lo < hi) {          // 구간은 [lo, hi)\n  const mid = (lo + hi) >> 1;\n  if (xs[mid] < target) lo = mid + 1;\n  else hi = mid;            // mid 도 답 후보다\n}", cap: "규약을 정하고 끝까지 지킨다" },
        key: ["[lo, hi) 와 [lo, hi] 를 섞지 않는다", "lower/upper 는 < 와 <= 하나 차이", "단조로우면 답을 이분 탐색", "판정 함수와 범위 하한을 먼저 정한다"] } },
    { t: "정렬 — 만들어 보면 비교자가 보인다", n: 4,
      th: { sum: "실무에서 정렬을 직접 짤 일은 드물다. 그런데 **비교자를 잘못 써서 나는 버그**는 흔하다. 한 번 만들어 보면 왜 그런지가 보인다.",
        body: [
          { h: "안정성은 '같을 때 원래 순서' 다", t: "병합 정렬에서 왼쪽·오른쪽이 같을 때 **왼쪽을 먼저 꺼내면** 안정 정렬이 된다. `<=` 를 `<` 로 바꾸는 순간 같은 값의 순서가 뒤집힌다. 자바스크립트 `sort` 는 명세상 안정이므로, '점수 내림차순, 같으면 이름 오름차순' 을 두 번 정렬로 풀 수도 있지만 비교자 하나에 `||` 로 잇는 편이 의도가 분명하다. 문자열을 `a - b` 로 빼거나 숫자를 비교자 없이 정렬해 `10` 이 `9` 앞에 오는 것이 가장 흔한 사고다." },
          { h: "전부 정렬하지 않아도 되는 경우가 많다", t: "k번째 값 하나만 필요하면 퀵 셀렉트가 평균 O(n) 이다 — 분할한 뒤 **한쪽만** 더 파고들기 때문이다. 병합 정렬의 병합 단계에 카운터 하나만 얹으면 역순 쌍의 개수가 공짜로 나온다. 오른쪽 값을 꺼낼 때 왼쪽에 남은 개수만큼 한꺼번에 더하면 되는데, 이것이 O(n²) 이중 루프를 O(n log n) 으로 바꾼다. **정렬 과정에서 부수적으로 얻는 정보**를 보는 눈이 정렬을 직접 짜 보는 진짜 이득이다." }],
        code: { c: "xs.sort((a, b) => b.score - a.score\n  || a.name.localeCompare(b.name));", cap: "|| 로 잇는 2차 기준" },
        key: ["같을 때 왼쪽 먼저 = 안정 정렬", "숫자는 비교자 없이 정렬하지 않는다", "하나만 필요하면 퀵 셀렉트", "병합하며 역순 쌍을 센다"] } },
    { t: "힙과 빈도 — 상위 k 개를 싸게 얻는다", n: 3,
      th: { sum: "'가장 큰 k 개' 를 위해 전부 정렬하는 것은 낭비다. **크기 k 인 힙**이면 O(n log k), 값의 범위가 좁으면 **버킷**으로 O(n) 이다.",
        body: [
          { h: "최소 힙에 k 개만 남긴다", t: "가장 큰 k 개를 구할 때는 역설적으로 **최소 힙**을 쓴다. 힙의 꼭대기가 '현재 k등' 이므로, 새 값이 그보다 크면 꼭대기를 버리고 넣는다. 메모리가 k 에 비례하므로 다 담을 수 없는 스트림에서도 동작한다 — 로그 상위 URL, 실시간 랭킹이 이 구조다. 반대로 가장 작은 k 개는 최대 힙이다. **부호를 뒤집는 것**만으로 한쪽 구현을 재활용할 수 있다." },
          { h: "빈도는 값이 아니라 개수로 줄 세운다", t: "빈도 상위 k 개는 개수가 **0 이상 n 이하의 정수**라는 점을 이용한다. `count → 단어 목록` 버킷 배열을 만들어 뒤에서부터 훑으면 정렬 없이 O(n) 이다. k 개 정렬 배열을 합칠 때도 매번 전체를 훑는 대신 각 배열의 머리만 힙에 넣으면 O(N log k) 가 된다 — 여러 정렬된 로그 파일을 시간순으로 합치는 작업이 정확히 이것이다. **동점 처리 규칙**을 먼저 정하지 않으면 실행할 때마다 결과가 흔들린다." }],
        code: { c: "if (heap.size < k) heap.push(x);\nelse if (x > heap.top()) { heap.pop(); heap.push(x); }", cap: "큰 k 개를 위해 최소 힙을 쓴다" },
        key: ["상위 k 개 → 크기 k 최소 힙", "메모리가 k 에 비례해 스트림도 된다", "빈도는 버킷으로 O(n)", "동점 규칙을 먼저 정한다"] } },
  ],
};
