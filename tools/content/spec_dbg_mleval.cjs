module.exports = {
  track: "mleval", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 점수는 나오는데 비교를 망치는 코드 고치기",
  source: "./dbg_mleval.cjs",
  lessons: [
    { t: "분모와 경계를 잘못 잡은 지표", n: 6,
      th: { sum: "평가 코드의 버그는 **모델이 아니라 결론을 바꾼다** — 잘못된 지표로 고른 모델은 되돌리기도 어렵다.",
        body: [
          { h: "무엇으로 나누는가", t: "TPR 의 분모는 실제 양성, FPR 의 분모는 **실제 음성**이다. 맥니마 검정의 분모는 전체 표본이 아니라 **엇갈린 쌍**이고, 짝지은 t 검정의 분모는 표준편차가 아니라 **표준오차**다. 분모를 틀리면 값이 그럴듯하게 나와서 ROC 모양이나 검정력으로만 드러난다." },
          { h: "구간의 끝과 정렬", t: "`floor(1 × bins)` 는 마지막 구간을 넘어가고, 백분위 자리는 `n` 그 자체가 되어 배열 밖을 가리킨다. 사다리꼴 적분은 **x 오름차순**이 전제라 문턱을 거꾸로 훑으면 AUC 가 음수가 된다. 셋 다 **자르기와 정렬**을 함수 안에서 보장하는 편이 안전하다." }],
        code: { c: "return Math.min(bins - 1, Math.floor(q * bins));\nconst ps = points.slice().sort((p, q) => p[0] - q[0]);", cap: "위쪽 끝을 자르고, 정렬을 보장한다" },
        key: ["혼동행렬 지표는 실제 라벨로 나눈다", "엇갈린 쌍만 세는 검정", "마지막 구간은 닫혀 있다", "적분 전에 정렬한다"] } },
    { t: "기준선과 평균 방식이 어긋난 지표", n: 6,
      th: { sum: "'좋다' 는 언제나 **무엇과 견주어**다 — 기준선을 잘못 잡거나 평균 방식을 바꾸면 같은 모델이 전혀 다른 점수를 받는다.",
        body: [
          { h: "기준선이 무엇인가", t: "R² 의 기준선은 **실제값의 평균으로 예측하는 모델**이다. 0 으로 예측하는 모델을 기준으로 삼으면 값이 100 근처인 자료에서 무엇이든 1 에 가까워진다. 비용을 목적함수로 쓸 때는 **최소화**이고, 순위는 1 부터 세므로 top-k 는 `rank <= k` 다." },
          { h: "어느 평균인가", t: "매크로 F1 은 **클래스마다 같은 무게**로 단순 평균해야 소수 클래스 실패가 보인다 — 표본 수로 가중하면 그것은 가중 평균이다. MAPE 는 실제값 0 에서 폭발하므로 그 항목을 빼고 세고, **몇 개를 뺐는지** 함께 보고해야 한다." }],
        code: { c: "ssTot = Σ(y - mean(y))²        // 기준선은 평균\nreturn Σf1 / rows.length;      // 매크로는 단순 평균", cap: "기준선과 평균 방식을 이름에 적는다" },
        key: ["R² 의 기준선은 평균", "비용은 최소화", "매크로는 단순 평균", "0 을 뺀 개수를 함께 보고"] } },
  ],
};
