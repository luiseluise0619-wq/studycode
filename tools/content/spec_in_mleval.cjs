module.exports = {
  track: "mleval", guide: "", xp: 60,
  unit: "단답으로 확인하기 — 지표를 직접 셈해 보기",
  source: "./in_mleval.cjs",
  lessons: [
    { t: "숫자로 재는 법", n: 8,
      th: { sum: "지표는 **분모가 무엇인가**로 구분된다. 그 한 가지만 정확히 알면 나머지는 저절로 따라온다.",
        body: [
          { h: "분류 지표의 분모", t: "정확도의 분모는 전체라 <b>클래스 비율이 비슷할 때만</b> 뜻이 있고, 재현율의 분모는 실제 양성, 정밀도의 분모는 양성이라 한 것, <b>특이도</b>의 분모는 실제 음성이다. F1 은 정밀도와 재현율의 <b>조화평균</b>이라 낮은 쪽에 끌려간다 — 0.6 과 0.4 면 0.5 가 아니라 0.48 이다. AUC 0.5 는 무작위와 같고, 그 값은 '양성 하나가 음성 하나보다 높은 점수를 받을 확률' 이다." },
          { h: "회귀 지표와 확률 손실", t: "<b>MAE</b> 는 단위가 원 데이터와 같아 해석이 쉽고 이상치에 덜 흔들리며, <b>RMSE</b> 는 제곱 때문에 큰 오차를 특히 미워한다 — 하나의 튀는 값이 평가를 좌우하면 둘을 함께 본다. <b>R²</b> 는 완벽하면 1 이고 평균만도 못하면 <b>음수</b>가 된다. 로그 손실은 완벽하면 0 이고 '<b>확신에 찬 오답</b>' 을 무한대로 벌하므로, 구현은 <code>log(0)</code> 을 피해 확률을 자른다." }],
        code: { c: "F1 = 2PR / (P + R)          // 조화평균\n특이도 = TN / (TN + FP) = 1 - FPR\nRMSE 는 이상치에, MAE 는 전체에 정직하다", cap: "분모가 무엇인지만 정확히 안다" },
        key: ["정확도는 균형 데이터에서만", "F1 은 낮은 쪽에 끌려간다", "AUC 0.5 는 찍기와 같다", "R² 는 음수가 될 수 있다"] } },
    { t: "무엇을 보고 무엇을 고를까", n: 7,
      th: { sum: "지표를 고르는 기준은 성능이 아니라 **틀렸을 때의 비용**이고, 임계값은 학습 없이 즉시 바꿀 수 있는 손잡이다.",
        body: [
          { h: "곡선과 평균 방식", t: "ROC 의 가로축은 <b>위양성률</b>(1-특이도)인데, 음성이 압도적으로 많으면 오탐 수천 건이어도 작게 보인다 — 그래서 불균형에서는 <b>정밀도-재현율 곡선</b>을 본다(기준선이 대각선이 아니라 양성 비율이다). 여러 클래스를 합칠 때 <b>매크로</b>는 클래스마다 같은 무게, 마이크로는 큰 클래스에 끌려가므로 둘이 크게 다르면 그 자체가 불균형의 신호다." },
          { h: "확률과 임계값", t: "대부분의 모델은 라벨이 아니라 <b>확률·점수</b>를 내놓고, 라벨은 임계값을 정해야 나온다 — 기본값 0.5 에는 아무 근거가 없다. 오탐과 미탐의 비용을 숫자로 적고 검증 데이터에서 <b>총비용이 가장 작은</b> 임계값을 고르면, 모델을 다시 학습하지 않고도 운영을 맞출 수 있다. 확률 자체를 쓴다면(기댓값 계산·가격 책정) 순위보다 <b>보정</b>이 중요하다. 데이터가 적으면 교차검증으로 재고 <b>표준편차까지</b> 보고한다." }],
        code: { c: "불균형 → PR 곡선 · 기준선 = 양성 비율\n임계값 = argmin(오탐비용×FP + 미탐비용×FN)\n신뢰도 곡선으로 보정 확인", cap: "비용을 적어야 임계값이 정해진다" },
        key: ["불균형에는 PR 곡선", "매크로와 마이크로를 구분한다", "임계값은 학습 없이 바꾼다", "확률을 쓰려면 보정을 본다"] } },
  ],
};
