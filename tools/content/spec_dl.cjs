module.exports={
  track:"dl", guide:"🧠", xp:80,
  unit:"실행형 실전 — 학습 구성 요소를 직접 만든다",
  source:"./exec_aidl.cjs", slice:[6,12],
  lessons:[
    { t:"정규화·옵티마이저·스케줄", n:3,
      th:{ sum:"깊은 신경망이 실제로 학습되게 만드는 셋 — **분포를 안정시키고, 걸음 크기를 적응시키고, 학습률을 시간에 따라 바꾼다**.",
        body:[
          {h:"분모를 정확히 만든다", t:"LayerNorm 은 표준편차로 나눠야 하고 `eps` 는 **제곱근 안**에 들어간다 — 분산으로 나누면 차원이 어긋나 스케일이 완전히 달라진다. 그런데 상수 입력으로만 테스트하면 양쪽 다 0 을 내서 통과하므로, **값이 다른 입력**이 테스트에 있어야 한다. BatchNorm 과 달리 한 샘플 안에서만 계산하므로 배치 크기 1 에서도 같게 동작하고 학습·추론 코드가 같다."},
          {h:"첫 걸음과 마지막 걸음", t:"Adam 의 `m`·`v` 는 0 에서 시작하므로 초기에 과소평가되고, **바이어스 보정**이 있어야 첫 걸음이 대략 `lr` 크기가 된다(기울기 크기와 거의 무관해지는 성질). `eps` 는 0으로 나누기 방지만이 아니라 **아주 작은 기울기에서 걸음을 억제하는 문턱**이다. 학습률 스케줄은 초반 워밍업으로 아직 통계가 없는 옵티마이저를 보호하고, 후반 감쇠로 최소점 근처의 진동을 줄인다 — 워밍업 누락이 트랜스포머 발산의 가장 흔한 원인이다."}],
        code:{ c:"mh = m / (1 - b1 ** t)     # 바이어스 보정\nvh = v / (1 - b2 ** t)\n# t 를 체크포인트에 저장하지 않으면 재개 때 손실이 튄다", cap:"옵티마이저는 스텝 수를 상태로 들고 있다" },
        key:["eps 는 제곱근 안에 들어간다","Adam 의 첫 걸음 ≈ lr","eps 는 작은 기울기의 문턱이기도 하다","워밍업 없이 시작하면 발산한다"] } },
    { t:"드롭아웃·어텐션·합성곱", n:3,
      th:{ sum:"층을 이루는 셋 — 전부 '**학습과 추론의 값 크기를 맞추는 것**' 이 숨은 주제다.",
        body:[
          {h:"기댓값을 보존한다", t:"인버티드 드롭아웃은 남긴 값에 `1/(1−p)` 를 곱해 **학습과 추론의 출력 기댓값을 같게** 만든다. 스케일링을 빼먹으면 학습 시 활성값이 추론의 절반이 되고, 가중치가 그 절반에 맞춰지므로 **손실은 잘 떨어지는데 추론만 나쁜** 버그가 된다. 학습 시에 나누는 쪽으로 뒤집은 덕분에 추론 코드에는 드롭아웃 처리가 아예 없어진다 — 그래서 `model.eval()` 한 줄이 전부다."},
          {h:"스케일과 크기", t:"어텐션에서 내적을 `√d` 로 나누는 이유는 차원이 커질수록 내적의 분산이 `d` 만큼 커지기 때문이다. 나누지 않으면 softmax 가 거의 원-핫이 되고 **기울기가 0 에 가까워져 학습이 멈춘다** — 손실 곡선이 평평한 형태로 나타난다. 합성곱에서는 패딩을 빼먹으면 층마다 크기가 줄고 **경계 정보가 소실**되므로 `padding=(k−1)//2` 로 크기를 유지하는 것이 기본이다."}],
        code:{ c:"raw = [dot(q, k) / math.sqrt(len(q)) for k in K]\n# 나누지 않으면 뾰족해지고, 뾰족하면 기울기가 죽는다", cap:"성능이 안 나오는데 손실 곡선만 평평하다면" },
        key:["드롭아웃은 남긴 값을 1/(1−p) 로 키운다","추론에서 반드시 끈다(model.eval)","어텐션 내적은 √d 로 나눈다","패딩 없으면 경계 정보가 소실된다"] } },
  ]
};
