module.exports = {
  track: "arch", guide: "", xp: 80,
  unit: "실행형 실전 — 비트와 성능 계산이 어긋난 코드 고치기",
  source: "./dbg_arch.cjs",
  lessons: [
    { t: "비트와 주소를 잘못 다룬 곳", n: 6,
      th: { sum: "구조 코드의 버그는 **양수 구간에서만 맞는다** — 부호 비트, 블록 단위, 파이프 채우는 구간이 어긋나 있다.",
        body: [
          { h: "부호는 위쪽으로 복사된다", t: "좁은 비트 폭의 값을 넓은 자리로 옮길 때는 **맨 위 비트를 위쪽 전체에 복사**해야 한다. 자바스크립트의 비트 연산은 결과를 **부호 있는 32비트**로 만들므로, 최상위 비트가 켜지면 음수가 되고 `>>` 는 부호를 채우며 내려간다. 두 함정 모두 0x80 을 넘는 값에서만 드러난다." },
          { h: "단위가 바이트가 아니다", t: "캐시는 **블록 단위**로 움직이므로 세트 번호는 주소가 아니라 블록 번호로 구한다. 파이프라인은 첫 명령이 파이프를 채우고 그 뒤로 하나씩 나오므로 `stages + instrs - 1` 이다. 둘 다 **경계값 하나**로 검산된다 — 인접 주소가 같은 세트인가, 명령 1 개면 stages 인가." }],
          code: { c: "return (value & (top - 1)) - (value & top);   // 부호 확장\nreturn Math.floor(addr / lineSize) % sets;    // 블록 번호로", cap: "부호 비트와 단위를 먼저 본다" },
        key: ["부호 확장은 가장 큰 음수로 테스트", "비트 결과는 >>> 0 으로 되돌린다", "캐시 색인은 블록 번호", "파이프라인은 채우는 구간 + 1개씩"] } },
    { t: "성능 식을 잘못 세운 곳", n: 6,
      th: { sum: "성능 계산은 **극단값으로 검산**하면 거의 모든 실수가 잡힌다 — 0 을 넣고, 1 을 넣고, 무한대를 넣어 본다.",
        body: [
          { h: "확률 × 비용, 그리고 괄호", t: "페널티는 **미스가 났을 때만** 낸다. 곱하기를 빼면 값이 수십 배 어긋난다. 캐시 계층은 **중첩된 조건부 확률**이라 `l1 + r1 × (l2 + r2 × mem)` 처럼 안쪽부터 묶어야 한다. 암달의 법칙에서 직렬 구간을 빼면 '코어를 늘리면 무한히 빨라진다' 는 정반대 결론이 나온다." },
          { h: "평균에도 종류가 있다", t: "**더해서 뜻이 있는 값**은 산술, **곱해서 뜻이 있는 비율**은 기하평균이다. 속도 향상 2배와 8배의 대표값은 5 가 아니라 4 다. 그리고 병렬 처리 예측은 평균이 아니라 **가장 오래 걸리는 사람**이 정하므로 올림해야 하고, 여러 상한 중에서는 **작은 쪽**이 답이다." }],
        code: { c: "l1 + r1 * (l2 + r2 * mem)          // 안쪽부터 묶는다\nMath.min(peakFlops, bw * intensity) // 상한은 min", cap: "0 과 1 을 넣어 검산한다" },
        key: ["확률 × 비용을 더하지 말 것", "계층은 중첩 확률", "비율의 대표값은 기하평균", "상한은 min, 하한은 max"] } },
  ],
};
