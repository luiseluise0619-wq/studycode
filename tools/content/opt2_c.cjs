/* c 트랙 — 정답이 5자 이상 단독 최장인 71문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "c", fixes: [
  { k: "c2-strtol-vs-atoi", o: [
    "atoi 는 C11 에서 폐기 예정으로 표시되어 새 컴파일러가 호출마다 경고를 내고, 다음 표준에서는 아예 사라지기 때문",
    "strtol 은 endptr 와 ERANGE 로 멈춘 위치와 범위 초과를 알려 주지만 atoi 는 실패와 0 을 구분 못 하기 때문",
    "atoi 는 앞의 공백과 부호를 처리하지 못해 \" -12\" 같은 입력에서 0 을 돌려주고 음수는 아예 다루지 못하는 함수이기 때문",
    "strtol 은 진법을 인자로 받아 변환 표를 미리 만들어 두므로 자릿수가 많은 입력에서 atoi 보다 몇 배나 빠르게 변환하기 때문" ] },
  { k: "c3-pf-3", o: [
    "형식 지정자를 해석하느라 출력이 눈에 띄게 느려지고, 긴 입력이면 응답이 멈춘 것처럼 보이기 때문",
    "%n 같은 지정자로 메모리 쓰기가 일어나거나 스택에 있던 값이 그대로 출력되어 유출될 수 있기 때문",
    "인자 개수가 지정자 수보다 적으면 printf 가 문자열을 잘라 버려 뒤쪽 출력이 조용히 사라지기 때문",
    "형식 문자열이 변수이면 컴파일러가 타입을 검사할 수 없어 대부분의 경우 컴파일 자체가 막히기 때문" ] },
  { k: "c3-bss-2", o: [
    "초기값이 있는 배열은 자동 저장 기간으로 바뀌어 BSS 대신 스택에 놓이고, 크면 스택이 넘친다",
    "초기화된 데이터가 필요해 data 영역에 실제 바이트가 저장되므로 실행 파일이 커질 수 있다",
    "첫 원소만 1 이고 나머지는 0 이라 BSS 에 그대로 남으며, 파일 크기와 적재 방식은 달라지지 않는다",
    "정적 배열은 상수식으로만 초기화할 수 있는데 {1} 은 부분 초기화라 컴파일 단계에서 오류가 난다" ] },
  { k: "c2-strtol-errno", o: [
    "반환값이 -1 이면 실패이므로 errno 는 보지 않고 반환값이 -1 인지만 확인한 뒤, 아니면 그 값을 그대로 결과로 쓴다",
    "반환값이 0 이면 변환 실패이고 0 이 아니면 성공이므로, 반환값 하나만 확인하면 endptr 없이도 판별이 끝난다",
    "errno 는 실패할 때만 바뀌므로 초기화 없이 호출한 뒤 errno != 0 인지만 보면 endptr 없이도 정확히 판별된다",
    "호출 직전에 errno = 0 으로 초기화한 뒤, 변환이 멈춘 위치(endptr)와 errno == ERANGE 를 함께 확인한다" ] },
  { k: "c2-assert-fail", o: [
    "표준 오류에 파일 이름, 줄 번호, 조건식을 출력하고 abort 를 호출해 프로그램을 비정상 종료한다",
    "SIGABRT 대신 assert 전용 예외를 던져 호출 스택을 거슬러 올라가며 상위 함수가 잡을 기회를 준다",
    "조건식을 다시 평가하며 참이 될 때까지 기다리다가 시간이 지나면 경고만 남기고 다음 문장으로 넘어간다",
    "표준 오류에 조건식을 한 줄 남기고 0 을 반환한 뒤 그대로 계속 실행되므로 호출자가 반환값을 봐야 한다" ] },
  { k: "c3-enum-5", o: [
    "C에서는 열거형이 정수와 자유롭게 오가므로 컴파일이 통과되며, 값의 의미는 프로그래머가 지켜야 한다",
    "열거형 변수는 실행 중에 범위 검사를 받으므로 7 을 대입하는 순간 런타임 오류가 나며 프로그램이 중단된다",
    "열거형은 나열된 상수만 값으로 가질 수 있는 별도 타입이라 정수 7 을 넣는 대입문은 컴파일 오류가 된다",
    "컴파일러가 7 에 가장 가까운 열거 상수를 골라 대신 저장하므로 의미는 어긋나도 값은 목록 안에 머문다" ] },
  { k: "c3-const-2", o: [
    "s 가 NULL 이면 const 포인터 규칙에 따라 역참조 없이 0 을 돌려주므로 호출자가 NULL 검사를 생략할 수 있다",
    "읽기 전용임을 알면 컴파일러가 문자열을 레지스터에 올려 두므로 같은 함수가 언제나 눈에 띄게 빨라진다",
    "호출자에게 '이 함수는 s를 수정하지 않는다'는 계약을 알리고, 실수로 수정하면 컴파일 단계에서 잡힌다",
    "인자로 넘어온 문자열이 읽기 전용 영역으로 복사되어 함수가 도는 동안 다른 스레드가 바꿀 수 없게 된다" ] },
  { k: "c2-proto-missing", o: [
    "선언과 실제 정의의 타입이 어긋난 채 호출되어, 인자가 잘못 전달되고 디버깅하기 어려운 오류가 생긴다",
    "링커가 두 목적 파일의 심볼 타입을 대조해 어긋난 호출을 오류로 잡아 주므로 실행 파일이 만들어지지 않는다",
    "호출 시점에 인자가 정의 쪽 타입으로 자동 변환되므로 값이 다소 달라질 수는 있어도 오류로 이어지지는 않는다",
    "컴파일러가 원형 없는 호출을 경고와 함께 건너뛰어 그 함수는 실행되지 않고 반환값 자리에는 0 이 들어간다" ] },
  { k: "c2-malloc-check", o: [
    "malloc 이 준 메모리는 0 으로 초기화되어 있다고 믿게 되어, 쓰레기 값을 0 으로 착각한 계산이 조용히 틀린다",
    "할당 실패 시 NULL 을 역참조하게 되어 프로그램이 비정상 종료하거나 예측할 수 없이 동작한다",
    "검사를 생략하면 할당기가 안전을 위해 요청 크기의 두 배를 잡으므로 메모리가 눈에 띄게 낭비된다",
    "반환값을 쓰지 않는 호출은 경고가 아니라 오류로 취급되어 -Wall 을 켠 빌드에서 컴파일이 실패한다" ] },
  { k: "c3-const-4", o: [
    "const 는 선언한 번역 단위 안에서만 효력이 있어 다른 파일에서 extern 으로 참조하면 자유롭게 값을 바꿀 수 있다",
    "원래 const가 아닌 객체를 const 포인터로 받아 읽는 동안, 원래 이름이나 다른 비-const 포인터로 수정하는 경우",
    "const 변수는 컴파일러가 volatile 로 승격시키므로 하드웨어 레지스터처럼 읽을 때마다 다른 값이 들어올 수 있다",
    "const 는 컴파일러 힌트일 뿐 실행 시점에는 흔적이 남지 않아 어떤 경로로든 쓰기가 일어나면 값이 그대로 바뀐다" ] },
  { k: "c3-vol-1", o: [
    "해당 변수를 CPU 레지스터에 고정해 메모리를 거치지 않고 읽게 하므로 인터럽트 핸들러와 값을 빠르게 주고받을 수 있다",
    "해당 객체에 대한 접근을 최적화로 생략하거나 캐시하지 않고, 코드에 적힌 대로 매번 읽고 쓰게 한다",
    "변수 갱신을 원자적 명령으로 바꿔 주므로 count++ 같은 연산도 스레드 사이에서 잃어버리는 일 없이 반영된다",
    "컴파일러가 접근마다 메모리 장벽을 넣어 여러 스레드가 동시에 읽고 써도 순서가 지켜지도록 보장해 준다" ] },
  { k: "c3-va-5", o: [
    "va_arg 는 인자 목록 끝에 이르면 NULL 을 돌려주도록 정의되어 있어 반환값이 NULL 인지로 끝을 알 수 있다",
    "형식 문자열, 개수 매개변수, NULL 같은 센티널 등 프로그래머가 직접 약속해 전달해야 한다",
    "호출 규약이 인자 개수를 첫 번째 숨은 인자로 함께 넘겨 주므로 va_start 가 그 값을 읽어 개수를 알려 준다",
    "va_list 는 배열처럼 연속 메모리에 놓이므로 sizeof(ap) / sizeof(첫 인자) 로 개수를 계산할 수 있다" ] },
  { k: "c3-port-1", o: [
    "int 는 어떤 플랫폼에서든 4바이트이고 그 위에서 long 과 포인터의 크기가 정해진다",
    "sizeof(char)는 1이고, 각 타입은 최소 표현 범위와 크기 순서만 보장된다",
    "long 은 언제나 포인터와 같은 크기라 포인터 값을 long 에 담아 옮겨도 잘리지 않는다",
    "short 는 모든 플랫폼에서 2바이트로 고정되어 있어 이식성 있는 코드의 기준 타입이 된다" ] },
  { k: "c2-strcmp-ret", o: [
    "정확히 -1 이며, 표준이 부호와 크기를 모두 정해 두었으므로 == -1 로 비교해도 된다",
    "첫 번째가 사전순으로 앞서므로 음수이며, 구체적인 값은 구현에 따라 다르다",
    "두 문자열이 같은 글자로 시작하지 않으므로 0 이며, 다른 위치는 반환값에 반영되지 않는다",
    "두 문자열의 길이 차이 -1 을 돌려주며, 내용 비교는 길이가 같을 때만 이루어진다" ] },
  { k: "c3-align-3", o: [
    "패딩 크기·엔디언·타입 폭이 플랫폼마다 달라 다른 환경에서 읽으면 어긋날 수 있다",
    "fwrite 는 바이트 배열만 받으므로 구조체를 넘기면 컴파일 경고와 함께 첫 멤버만 기록된다",
    "패딩 바이트까지 함께 기록되므로 파일이 실제 데이터의 두 배 가까이 커져 저장 공간을 낭비한다",
    "구조체 배치는 표준이 정해 두었으므로 어떤 환경에서 읽어도 같아 가장 이식성 높은 방법이다" ] },
  { k: "c2-pragma-once", o: [
    "인클루드 가드와 함께 쓰면 헤더가 두 번 처리된 것으로 간주되어 반드시 중복 정의 오류가 난다",
    "표준은 아니지만 주요 컴파일러가 지원하는 확장으로, 인클루드 가드와 같은 목적을 한 줄로 처리한다",
    "C99 부터 표준에 들어간 지시문이라 어떤 컴파일러에서든 같은 방식으로 동작한다고 믿어도 된다",
    "헤더를 한 번만 포함시키는 대신 그 헤더 안에서 정의한 매크로를 파일 끝에서 모두 해제한다" ] },
  { k: "꼬리 패딩 이유", o: [
    "가비지 컬렉터가 구조체 경계를 찾을 수 있도록 끝에 표식 바이트를 남겨 두기 위해서",
    "디버거가 멤버 이름과 오프셋을 저장할 공간을 구조체 끝에 확보해 두기 위해서",
    "구조체마다 크기가 겹치지 않게 만들어 sizeof 만으로 타입을 구별할 수 있게 하기 위해서",
    "구조체를 배열로 나열했을 때 각 원소의 멤버가 모두 정렬을 만족하도록 하기 위해" ] },
  { k: "c2-signal-loop", o: [
    "SIG_IGN 을 등록해 시그널을 무시하고, 대신 종료 요청은 별도 관리용 소켓으로만 받아 처리하도록 설계한다",
    "핸들러 안에서 열린 연결을 모두 닫고 파일을 저장한 뒤 exit 를 호출해 그 자리에서 정리와 종료를 끝낸다",
    "핸들러를 등록하지 않고 기본 동작에 맡기되, 정리 작업은 atexit 로 등록해 두어 종료 시점에 실행되게 한다",
    "핸들러에서 플래그만 세우고, 메인 루프가 매 반복마다 그 플래그를 확인해 정리 작업을 수행한 뒤 정상 종료한다" ] },
  { k: "c2-qsort-sub-overflow", o: [
    "두 포인터의 뺄셈이라 결과가 원소 사이의 거리(인덱스 차)가 되어 값의 크기와 무관한 순서가 나오기 때문",
    "두 값의 차이가 int 범위를 넘으면 정수 오버플로가 나서 부호가 뒤집힐 수 있기 때문",
    "qsort 의 비교 함수는 size_t 를 돌려줘야 하는데 뺄셈 결과는 int 라 부호가 잘려 순서가 틀리기 때문",
    "qsort 는 비교 함수가 음수를 돌려주면 정렬을 중단하도록 되어 있어 내림차순 구간이 처리되지 않기 때문" ] },
  { k: "unsigned 오버플로", o: [
    "2^N 으로 나눈 나머지가 되는 랩어라운드로 명확히 정의되어 있다",
    "최댓값에 이르면 포화되어 그 값에 머물며 더 커지지 않도록 정의되어 있다",
    "부호 있는 정수와 마찬가지로 정의되지 않은 동작이라 결과를 믿을 수 없다",
    "구현이 정하는 동작이라 컴파일러 문서를 확인해야 결과를 알 수 있다" ] },
  { k: "c2-assert-vs-error", o: [
    "사용자가 넘긴 파일 이름이 실제로 존재하는지 열기 전에 검증할 때",
    "네트워크 연결이 끊겼을 때 재시도 대신 즉시 중단시키고 싶을 때",
    "malloc 이 NULL 을 돌려주면 더 진행할 수 없으므로 그 자리에서 멈출 때",
    "함수 진입 시 내부적으로 절대 NULL 일 수 없는 포인터가 NULL 인지 확인할 때" ] },
  { k: "c2-static-assert", o: [
    "프로그램이 시작될 때 한 번만 검사하고 그 뒤로는 비용이 들지 않도록 최적화된다",
    "조건이 거짓이면 컴파일을 멈추지 않고 경고만 남기므로 빌드 로그를 봐야 알 수 있다",
    "컴파일 시점에 상수식을 검사해 조건이 거짓이면 컴파일 자체를 실패시킨다",
    "NDEBUG 를 정의하면 assert 와 마찬가지로 전처리 단계에서 지워져 검사가 사라진다" ] },
  { k: "c3-loop-2", o: [
    "최적화를 끈 -O0 에서도 꼬리 재귀는 언제나 루프로 바뀌므로 깊이를 걱정할 필요가 없다",
    "꼬리 위치의 재귀 호출은 C 문법이 금지하므로 컴파일러가 오류로 잡아 준다",
    "C 표준은 꼬리 재귀를 반드시 루프로 바꾸도록 요구하므로 어떤 컴파일러든 스택을 쓰지 않는다",
    "C 표준은 꼬리 호출 최적화를 보장하지 않으므로 깊은 재귀를 최적화에 기대면 안 된다" ] },
  { k: "c3-loop-4", o: [
    "goto 로 함수 시작 지점으로 되돌아가면 호출 없이 같은 코드를 다시 돌 수 있어 스택이 필요 없다",
    "두 갈래 재귀는 호출 스택 없이는 표현할 수 없어 반복문으로는 원리적으로 바꿀 수 없다",
    "직접 만든 스택(배열이나 연결 리스트)에 아직 처리하지 않은 노드를 담아야 한다",
    "현재 노드를 담는 전역 변수 하나만 두고 왼쪽 자식이 끝나면 오른쪽으로 옮겨 주면 충분하다" ] },
  { k: "c3-nm-2", o: [
    "나중에 선언된 typedef 이름이 먼저 선언된 태그를 덮어써 하나의 이름만 남기 때문",
    "태그 이름은 별도의 이름 공간에 있어 변수·타입 이름과 충돌하지 않기 때문",
    "컴파일러가 둘 중 나중 것을 무시하고 struct 키워드가 붙은 쪽만 타입으로 인정하기 때문",
    "실제로는 충돌이라 오류가 나야 하지만 대부분의 컴파일러가 확장으로 허용해 주기 때문" ] },
  { k: "c3-port-2", o: [
    "long이 LP64에서는 8바이트지만 LLP64에서는 4바이트다",
    "int 가 LP64 에서는 8바이트지만 LLP64 에서는 4바이트다",
    "포인터가 LP64 에서는 8바이트지만 LLP64 에서는 4바이트다",
    "char 가 LP64 에서는 1바이트지만 LLP64 에서는 2바이트다" ] },
  { k: "c3-size-2", o: [
    "sizeof 는 컴파일 시점 연산이라 실행 중 크기가 정해지는 함수 매개변수에는 쓸 수 없기 때문",
    "매개변수의 배열 타입은 포인터로 조정되므로 sizeof가 포인터 크기를 주기 때문",
    "arr 을 초기화하지 않은 채 넘겨서 sizeof 가 실제 원소 수를 셀 수 없기 때문",
    "%zu 는 size_t 전용 지정자가 아니라 출력 단계에서 값이 잘못 해석되기 때문" ] },
  { k: "c3-mem-3", o: [
    "프로그램 시작 시 0으로 초기화되고 함수 호출 사이에도 값이 유지된다",
    "함수에 들어갈 때마다 새로 만들어져 0 이 되고 함수가 끝나면 사라진다",
    "저장 위치가 고정되어 컴파일러가 자동으로 volatile 로 취급해 매번 메모리에서 읽는다",
    "스택 프레임 안에 놓이지만 static 덕분에 함수가 끝나도 그 자리가 해제되지 않는다" ] },
  { k: "sizeof 실패", o: [
    "`const` 가 붙은 매개변수는 크기 정보가 지워져 `sizeof` 가 0 을 돌려준다",
    "매개변수 배열은 포인터로 조정되어 64가 아니라 포인터 크기가 나온다",
    "`sizeof` 는 배열 전체에는 쓸 수 없고 원소 하나에만 쓸 수 있어 1 이 나온다",
    "매개변수의 크기는 호출될 때 정해지므로 컴파일 시점의 `sizeof` 는 오류가 된다" ] },
  { k: "c3-frame-5", o: [
    "프레임이 걷히기 전에 힙으로 복사되므로 반환된 뒤에도 그 주소로 안전하게 읽을 수 있다",
    "다음 재귀 호출의 프레임으로 값이 복사되어 넘어가므로 호출 사슬이 끝날 때까지 남는다",
    "프레임이 걷히면서 사라지므로 그 주소를 반환해 사용하면 안 된다",
    "재귀 호출의 지역 변수는 정적 저장 기간을 가져 프로그램이 끝날 때까지 값이 유지된다" ] },
  { k: "c3-bss-1", o: [
    "전역 배열은 실행 시점에 힙에서 할당되므로 실행 파일에는 크기 정보만 남기 때문",
    "쓰이지 않는 전역 배열은 컴파일러가 통째로 제거해 실행 파일에서 사라지기 때문",
    "실행 파일은 압축되어 저장되므로 같은 값이 반복되는 큰 배열은 몇 바이트로 줄기 때문",
    "값이 모두 0이라 BSS에 크기만 기록하고 실행 시 0으로 채우기 때문" ] },
  { k: "c3-vol-2", o: [
    "컴파일러가 volatile 접근마다 뮤텍스를 잠그고 푸는 코드를 넣어 갱신이 겹치지 않게 막아 준다",
    "volatile 변수는 스레드마다 별도 복사본을 가지므로 서로 다른 값을 쓰더라도 충돌하지 않는다",
    "volatile은 원자성도 메모리 순서도 보장하지 않으므로 데이터 경쟁이 그대로 남는다",
    "매번 메모리에서 읽고 쓰므로 스레드끼리 값이 즉시 보여 경쟁 조건이 사라지고 결과가 정확해진다" ] },
  { k: "fflush", o: [
    "표준 입력 버퍼에 남은 글자를 버려 다음 입력이 깨끗하게 시작되게 한다",
    "스트림을 닫고 파일 기술자를 운영체제에 돌려주어 더 쓸 수 없게 한다",
    "버퍼에 쌓인 출력을 내보내지 않고 버려서 버퍼를 비우고 다음 출력을 받는다",
    "버퍼에 쌓인 출력을 지금 즉시 스트림으로 내보낸다" ] },
  { k: "c2-guard-why", o: [
    "링커가 같은 정의를 발견하면 하나만 남기고 나머지를 지우므로 문제는 없지만 링크가 느려진다",
    "전처리 결과가 두 배로 길어져 컴파일 시간이 늘어날 뿐 생성되는 코드는 달라지지 않는다",
    "같은 매크로가 두 번 정의되면 값이 두 배가 되어 그 매크로를 쓴 계산이 모두 어긋난다",
    "구조체나 typedef 정의가 중복되어 컴파일 오류가 난다" ] },
  { k: "자동 수명", o: [
    "힙에 할당되므로 함수가 끝나기 전에 `free` 로 해제하지 않으면 누수가 된다",
    "정적 저장 기간을 가져 프로그램이 끝날 때까지 값과 저장 공간이 유지된다",
    "선언과 동시에 0 으로 초기화되므로 초기값을 적지 않아도 읽기에 안전하다",
    "함수가 반환되면 수명이 끝나 그 저장 공간을 더는 쓸 수 없다" ] },
  { k: "c2-ll-freeall", o: [
    "for (Node *p = head; p; ) { Node *nxt = p-&gt;next; free(p); p = nxt; }",
    "free(head); — 노드가 next 로 이어져 있으므로 첫 노드를 해제하면 뒤따르는 노드도 함께 해제된다",
    "for (Node *p = head; p; p = p-&gt;next) p = NULL; — 포인터를 끊으면 노드가 회수된다",
    "for (Node *p = head; p; p = p-&gt;next) free(p); — 순서대로 해제하면 된다" ] },
  { k: "c2-free-null", o: [
    "errno 가 EINVAL 로 설정되고 함수는 그냥 돌아오므로 호출 뒤 errno 를 확인해야 한다",
    "아무 일도 일어나지 않는다 — 표준이 안전하다고 규정한다",
    "정의되지 않은 동작이라 어떤 구현에서는 멀쩡하고 어떤 구현에서는 힙이 손상된다",
    "할당기가 유효하지 않은 주소를 감지해 abort 를 호출하므로 프로그램이 즉시 중단된다" ] },
  { k: "c2-fork-copy", o: [
    "자식은 부모의 주소 공간 사본을 갖게 되어, 이후 한쪽에서 변수를 바꿔도 다른 쪽에는 보이지 않는다",
    "자식이 먼저 끝까지 실행된 뒤에야 부모가 fork 다음 줄로 넘어가므로 실행 순서가 언제나 정해져 있다",
    "부모와 같은 물리 메모리를 실제로 공유하므로 한쪽이 변수 값을 바꾸면 다른 쪽에서도 곧바로 보인다",
    "자식은 스택과 힙만 복사받고 전역 변수는 물려받지 않으므로 전역 값은 모두 0 으로 시작한다" ] },
  { k: "c3-res-4", o: [
    "표준 라이브러리 함수의 원형에만 붙일 수 있고 사용자 함수에는 문법상 붙일 수 없다",
    "모든 포인터 매개변수에 기본으로 붙여 두고 문제가 생기는 곳만 나중에 떼어 낸다",
    "const 가 붙은 포인터는 겹칠 일이 없으므로 언제나 restrict 를 함께 붙인다",
    "성능 병목이 측정으로 확인되고 겹치지 않음이 확실한 함수에만 붙인다" ] },
  { k: "차이", q: "널 문자", o: [
    "'0' 의 값이 0 이고 '\\0' 은 문자열 끝 표시로만 쓰는 특수 기호다",
    "둘은 같은 문자를 다르게 적은 것이라 어디에 써도 결과가 같다",
    "둘 다 문자열의 끝을 뜻하므로 \"10\" 은 길이가 1 인 문자열이다",
    "둘은 다르며 '\\0' 의 값은 0이다" ] },
  { k: "c3-loop-3", o: [
    "호출이 끝나면 돌아갈 위치를 담아 두던 반환 주소",
    "각 호출마다 스택 프레임의 크기를 기록해 두던 값",
    "호출이 되돌아오며 곱해지던 중간 결과(누산기)",
    "재귀를 멈추게 하던 기저 사례의 조건식" ] },
  { k: "c3-td-6", o: [
    "선언된 스코프(블록·파일)를 따르며, 블록 안에서 선언하면 그 블록 안에서만 유효하다",
    "같은 프로젝트의 모든 파일에서 자동으로 보이므로 헤더에 두지 않아도 어디서든 쓸 수 있다",
    "어디에 선언하든 전역이라 함수 안에서 만든 별명도 그 함수 밖에서 그대로 쓸 수 있다",
    "typedef 는 파일 범위에서만 허용되므로 함수 안에서 선언하면 컴파일 오류가 난다" ] },
  { k: "sizeof 관용구", o: [
    "`p`를 역참조해 실제 값을 읽어 그 값만큼의 바이트를 할당하기 위해",
    "`sizeof(int *)`와 같은 값을 얻어 포인터 배열을 만들기 위해",
    "타입 이름을 반복하지 않아 `p`의 타입이 바뀌어도 크기가 자동으로 맞기 때문에",
    "`malloc` 의 인자는 문법상 역참조 식이어야만 하므로 다른 형태는 컴파일되지 않기 때문에" ] },
  { k: "이중 해제", o: [
    "두 번째 호출은 이미 해제된 블록임을 할당기가 알아채고 무시한다",
    "컴파일러가 흐름을 추적해 같은 포인터의 두 번째 `free` 를 오류로 잡는다",
    "정의되지 않은 동작이며 힙 구조가 손상될 수 있다",
    "블록이 두 번 회수되어 할당 가능한 메모리가 실제보다 크게 잡힌다" ] },
  { k: "c2-bst-degenerate", o: [
    "오른쪽으로만 뻗은 사슬 모양이 되어 탐색이 O(n)으로 퇴화한다",
    "정렬된 입력은 BST 의 전제를 어기므로 두 번째 삽입부터 실패해 노드가 하나만 남는다",
    "삽입할 때마다 중앙값이 루트가 되도록 재배치되어 3 을 루트로 한 트리가 만들어진다",
    "삽입 규칙이 높이를 최소로 유지하므로 높이가 3 인 균형 잡힌 트리가 저절로 만들어진다" ] },
  { k: "c3-size-5", o: [
    "부호 있는 타입이라 배열 끝에서 앞으로 도는 루프에서 음수 인덱스를 자연스럽게 쓸 수 있기 때문",
    "포인터와 같은 크기와 표현을 가지므로 주소 값을 그대로 담아 두었다가 다시 포인터로 쓸 수 있기 때문",
    "부호 없는 타입이며 어떤 객체의 크기도 담을 수 있게 정의되어 있어 큰 값에서도 넘치지 않기 때문",
    "CPU 의 기본 워드 크기와 같아 int 보다 산술이 언제나 빠르고 루프 카운터로 쓰면 성능이 좋아지기 때문" ] },
  { k: "개념", q: "2차원 배열의 모든 원소", o: [
    "행과 열을 위해 반복문을 두 번 중첩한다",
    "행·열·원소 값을 위해 반복문을 세 번 중첩한다",
    "포인터 하나로 끝까지 훑으면 되어 반복문이 필요 없다",
    "행 번호만 돌리는 반복문 하나면 열까지 방문된다" ] },
  { k: "c2-bit-vs-logical", o: [
    "둘은 완전히 같은 연산이며 &amp; 는 옛 표기, &amp;&amp; 는 새 표기라 어느 쪽을 써도 결과와 평가 순서가 같다",
    "&amp;&amp; 는 비트 단위로 두 값을 모두 계산하고, &amp; 는 왼쪽이 0 이면 오른쪽을 평가하지 않는 단축 평가를 한다",
    "&amp; 는 비트 단위로 두 피연산자를 모두 계산하고, &amp;&amp; 는 논리값을 만들며 왼쪽이 거짓이면 오른쪽을 평가하지 않는다",
    "&amp; 는 실수를 포함한 모든 산술 타입에 쓸 수 있지만 &amp;&amp; 는 정수와 포인터에만 쓸 수 있어 적용 대상이 다르다" ] },
  { k: "c3-mem-2", o: [
    "malloc 을 호출한 함수가 반환되는 순간 그 함수의 프레임과 함께 자동으로 회수된다",
    "다음 malloc 호출이 같은 자리를 다시 쓰므로 그 시점까지만 값이 유지된다",
    "free를 호출하거나 프로그램이 끝날 때까지 유지된다",
    "포인터 변수가 선언된 블록을 벗어나면 블록과 함께 수명이 끝난다" ] },
  { k: "종단 미보장", o: [
    "n 바이트에 다 들어가지 않으면 `d` 의 크기를 늘려 원본 전체를 복사한다",
    "n바이트만 복사하고 널 종단 문자를 넣지 않아 `d`는 문자열이 아니게 된다",
    "n 바이트를 복사한 뒤 널 문자를 하나 더 붙여 모두 n+1 바이트를 쓴다",
    "원본이 버퍼보다 길다고 판단해 복사를 거부하고 `NULL` 을 돌려준다" ] },
  { k: "c3-va-3", o: [
    "가변 인자는 한 번만 읽을 수 있으므로 두 번 필요하면 처음부터 배열로 받도록 API 를 바꿔야 한다",
    "va_start 를 다시 부르지 않아도 va_arg 는 끝에 이르면 처음으로 되돌아가므로 그냥 다시 읽으면 된다",
    "va_end 로 닫은 뒤 같은 va_list 변수를 그대로 다시 쓰면 위치가 처음으로 돌아가 있다",
    "va_copy로 복사본을 만들어 두 번째 순회에 쓰고, 복사본도 va_end로 닫는다" ] },
  { k: "c3-align-2", o: [
    "멤버가 같은 엔디언으로 저장되어 다른 플랫폼과 파일을 주고받을 때 변환이 필요 없어진다",
    "패딩이 줄어 구조체 전체 크기가 작아질 수 있다",
    "멤버 오프셋이 모두 정렬 배수가 되어 포인터 연산 없이 상수 오프셋만으로 접근하게 된다",
    "큰 멤버가 캐시 줄 앞쪽에 놓여 모든 멤버의 접근 속도가 두 배 가까이 빨라진다" ] },
  { k: "원리", q: "strlen", o: [
    "컴파일러가 리터럴 길이를 미리 계산해 두었다가 그 값을 돌려준다",
    "처음부터 널 문자 '\\0' 를 만날 때까지 세어 나간다",
    "배열을 선언할 때 적은 크기를 그대로 돌려준다",
    "마지막 칸부터 거꾸로 훑어 첫 글자까지의 거리를 돌려준다" ] },
  { k: "바이너리 모드", o: [
    "읽기 버퍼를 두 배로 잡아 큰 파일을 읽는 속도를 눈에 띄게 올려 준다",
    "표준 입출력의 버퍼링을 끄고 read 시스템 호출로 곧바로 읽게 한다",
    "열기 전에 파일의 백업본을 만들어 두어 쓰기 도중 오류가 나도 원본이 남게 한다",
    "바이너리 모드로 열어 Windows 등에서 줄바꿈 변환이 일어나지 않게 한다" ] },
  { k: "c2-extern-decl", o: [
    "헤더에 static int g_count; 를 두어 그 헤더를 포함하는 모든 .c 파일이 같은 변수 하나를 함께 보게 한다",
    "모든 .c 파일에 int g_count = 0; 을 각각 적어 두면 링커가 같은 이름의 정의를 하나로 합쳐 공유해 준다",
    "헤더에 int g_count = 0; 을 두고 필요한 .c 파일이 모두 그 헤더를 포함하면 정의가 하나로 공유된다",
    "헤더에 extern int g_count; 를 두고, 정확히 하나의 .c 파일에서 int g_count = 0; 으로 정의한다" ] },
  { k: "c3-rec-6", o: [
    "두 호출이 같은 스택 프레임을 나눠 쓰게 되어 fib(n-1) 과 fib(n-2) 의 지역 값이 서로 섞인다",
    "기저 사례가 n == 0 과 n == 1 두 개라 컴파일러가 어느 쪽에서 멈출지 정하지 못해 컴파일되지 않는다",
    "덧셈 결과가 반환되기 전에 프레임이 걷히므로 어떤 n 을 넣어도 반환값이 항상 0 이 된다",
    "같은 부분 문제를 지수적으로 여러 번 다시 계산해 매우 느려진다" ] },
  { k: "c3-td-4", o: [
    "포인터 타입은 typedef 의 대상이 될 수 없어 그런 선언은 표준에서 허용하지 않기 때문",
    "NodeP 를 쓰는 곳마다 struct Node 의 완전한 정의가 필요해 헤더 의존이 늘어나기 때문",
    "포인터라는 사실이 선언에서 보이지 않아 const 위치나 해제 책임을 헷갈리게 만들 수 있기 때문",
    "별명을 거칠 때마다 포인터가 한 단계 더 감싸여 역참조 비용과 메모리가 두 배로 들기 때문" ] },
  { k: "c3-nm-6", o: [
    "enum 태그 없이 LogLevel이라는 이름만으로 변수를 선언할 수 있게 된다",
    "LogLevel 변수에 목록 밖의 정수를 넣으면 실행 중에 검사되어 오류가 난다",
    "열거 상수가 이름 문자열로 저장되어 printf 로 찍으면 이름이 그대로 나온다",
    "typedef 가 붙으면 열거 상수의 값이 0 이 아니라 1 부터 시작하도록 바뀐다" ] },
  { k: "소유권", o: [
    "반환된 포인터를 **호출자가** `free`할 책임이 있다는 소유권 규칙을 문서화해야 한다",
    "함수가 돌아가기 전에 `free(p)` 를 호출해야 힙 블록이 남지 않아 누수를 막을 수 있다",
    "`malloc` 은 실패해도 빈 블록을 돌려주므로 반환값이 `NULL` 인지 검사할 필요가 없다",
    "`strlen(s)` 만큼만 잡으면 되는데 `+ 1` 을 더해 한 바이트를 낭비하므로 크기 계산이 틀렸다" ] },
  { k: "구분", q: "두 연산자의 역할", o: [
    "둘 다 값에 1 을 더하는 증감 연산자다",
    "둘은 같은 연산을 다르게 적은 것이다",
    "&amp;는 주소를 얻고, *는 주소의 값을 얻는다",
    "&amp;는 값을 얻고, *는 주소를 얻는다" ] },
  { k: "c2-localtime-static", o: [
    "반환된 객체는 호출마다 새로 만들어지므로 여러 스레드에서 동시에 호출해도 언제나 안전하다",
    "구현이 관리하는 정적 객체를 가리킬 수 있어, 다시 호출하면 이전 결과가 덮어써질 수 있다",
    "반환된 struct tm 은 힙에 할당된 것이라 다 쓴 뒤 free 로 돌려주지 않으면 누수가 된다",
    "시간 값은 항상 변환할 수 있으므로 반환값이 NULL 인지는 따로 검사하지 않아도 된다" ] },
  { k: "c3-const-1", o: [
    "p는 가리키는 문자를 바꿀 수 없고, q는 가리키는 대상(주소)을 바꿀 수 없다",
    "const 는 포인터 자체에는 붙일 수 없으므로 char * const q 는 문법 오류다",
    "p 는 가리키는 대상(주소)을 바꿀 수 없고, q 는 가리키는 문자를 바꿀 수 없다",
    "const 의 위치는 읽기 편의일 뿐이라 둘 다 문자와 주소 모두 바꿀 수 없다" ] },
  { k: "매크로 vs 함수", o: [
    "매크로는 컴파일 단계에서 인자 타입을 검사하므로 잘못된 타입을 넣으면 오류가 난다",
    "매크로는 전처리 단계의 텍스트 치환이라 타입 검사가 없고 디버깅이 어렵다",
    "매크로는 자기 자신을 다시 부를 수 있어 재귀 알고리즘을 함수 없이 적을 수 있다",
    "함수는 호출할 때 인자를 여러 번 평가하므로 부작용이 있는 식을 넘기면 안 된다" ] },
  { k: "c3-rec-5", o: [
    "재귀 호출은 매번 힙에서 프레임을 할당하므로 깊어질수록 free 하지 않은 메모리가 쌓인다",
    "호출마다 반환 주소 저장과 프레임 설정 비용이 들고 스택 공간을 소비한다",
    "컴파일러가 모든 재귀를 반복문으로 바꿔 주므로 호출 비용과 스택 소비가 전혀 없다",
    "재귀 호출은 레지스터만 쓰고 메모리를 쓰지 않으므로 깊이에 제한이 없다" ] },
  { k: "c3-enum-2", o: [
    "enum 상수는 이름 문자열로 저장되어 실행 중에도 이름을 찍어 볼 수 있으므로 로그가 읽기 쉬워지기 때문",
    "enum 은 컴파일 시점에 사라져 메모리를 전혀 쓰지 않으므로, #define 보다 실행 파일이 작아지기 때문",
    "이름으로 의도가 드러나고, switch 처리 누락을 컴파일러 경고로 잡을 여지가 생기며, 디버거에도 이름이 보이기 때문",
    "enum 값은 읽기 전용 영역에 놓여 실행 중에 바뀌지 않으므로, 실수로 덮어써도 원래 값이 지켜지기 때문" ] },
  { k: "c3-bss-3", o: [
    "불확정한 값이므로 읽기 전에 반드시 초기화해야 한다",
    "같은 자리를 다시 쓰므로 이전 호출에서 남긴 값이 그대로 들어온다",
    "자동 변수도 정적 변수처럼 0 으로 초기화되므로 항상 0 이다",
    "디버그 빌드에서는 컴파일러가 -1 로 채워 초기화 누락을 드러낸다" ] },
  { k: "realloc 실패", o: [
    "실패한 요청을 정리하는 과정에서 원래 블록까지 자동으로 해제된다",
    "새 크기로 늘리지 못했으므로 원래 블록의 크기가 0 으로 줄어든다",
    "원래 블록도 함께 해제되므로 접근하면 크래시가 나며 `p` 는 더 쓸 수 없다",
    "그대로 유효하게 남아 있으므로 필요하면 계속 쓰거나 직접 `free` 해야 한다" ] },
  { k: "패딩 동작", o: [
    "복사한 뒤의 자리는 손대지 않으므로 이전에 있던 불확정 값이 남는다",
    "널 문자를 하나만 붙이고 나머지 자리는 건드리지 않고 그대로 둔다",
    "널 문자(`'\\0'`)로 전부 채운다",
    "텍스트 정렬을 위해 남는 자리를 공백 문자로 채운다" ] },
  { k: "pack 위험", o: [
    "표준에 없는 지시문이라 -pedantic 을 켜지 않아도 컴파일이 항상 실패한다",
    "정렬되지 않은 접근이 생겨 성능이 떨어지거나 일부 아키텍처에서 오류가 난다",
    "원소 사이의 정렬이 깨져 그 구조체로는 배열을 만들 수 없게 된다",
    "컴파일러가 패딩 대신 멤버 순서를 바꿔 채우므로 선언 순서를 믿을 수 없게 된다" ] },
  { k: "c2-2d-init", o: [
    "첫 행만 {1,2,3} 으로 채워지고 둘째 행은 초기화되지 않아 쓰레기 값이 남는다",
    "안쪽 배열마다 중괄호가 있어야 하므로 중첩 없이 나열하면 컴파일 오류가 난다",
    "첫 값 1 만 유효한 초기화로 인정되어 여섯 원소가 모두 1 로 채워진다",
    "a[0] 이 {1,2,3}, a[1] 이 {4,5,6} 으로 순서대로 채워진다" ] },
  { k: "c2-ll-double-ptr", o: [
    "노드가 함수 안과 밖에서 한 번씩 만들어져 메모리가 두 배로 할당되고, 밖에서 만든 쪽은 누수로 남는다",
    "매개변수는 값 복사라 함수 안에서 head 를 바꿔도 호출자의 head 는 그대로여서 새 노드가 반영되지 않는다",
    "새 노드가 head 앞이 아니라 뒤에 붙어 리스트가 뒤집힌 순서로 쌓이고, 순회하면 거꾸로 나온다",
    "Node* 매개변수에 새 노드 주소를 대입하는 순간 타입이 맞지 않아 컴파일 오류가 난다" ] }
] };
