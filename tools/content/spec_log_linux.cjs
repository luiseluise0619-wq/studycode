module.exports = {
  track: "linux", guide: "", xp: 70,
  unit: "로그 분석 — 서버가 남긴 것",
  source: "./log_ops.cjs", slice: [0, 10],
  lessons: [
    { t: "숫자가 어긋날 때", n: 5,
      th: { sum: "서버 사고의 절반은 **두 숫자가 서로 다른 말을 하는 자리**에서 드러난다.",
        body: [
          { h: "무엇을 세는 숫자인가", t: "<code>df</code> 는 블록을 세고 <code>df -i</code> 는 아이노드를 셉니다. <code>du</code> 는 이름이 있는 파일을 따라 세고 <code>df</code> 는 실제로 잡혀 있는 블록을 셉니다. 부하 평균은 CPU 를 기다리는 것만이 아니라 <b>디스크를 기다리는 것까지</b> 셉니다. 그래서 '용량은 남는데 못 쓴다', '지웠는데 안 준다', '부하는 높은데 CPU 는 논다' 가 전부 같은 모양의 이야기가 됩니다 — 숫자가 틀린 것이 아니라 <b>다른 것을 세고 있는</b> 것입니다." },
          { h: "어긋남이 곧 단서다", t: "두 숫자가 다를 때 그 차이가 원인을 가리킵니다. <code>du</code> 는 줄었는데 <code>df</code> 는 그대로면 누군가 지워진 파일을 아직 쥐고 있는 것이고, 사용률은 낮은데 요청이 거절되면 <b>평균이 쏠림을 감추고</b> 있는 것입니다. 그래서 사고를 볼 때는 숫자 하나가 아니라 <b>짝이 되는 숫자</b>를 함께 봅니다. 호스트 지표만 보는 감시는 컨테이너 한도에 걸려 죽는 사고를 통째로 놓칩니다." }],
        code: { c: "df -h && df -i        # 블록과 아이노드는 따로 찬다\ndu -sh /var/log       # df 와 다르면 열린 채 지워진 파일\nlsof +L1              # 이름 없는 파일을 쥔 프로세스", cap: "짝이 되는 숫자를 함께 본다" },
        key: ["블록과 아이노드는 따로 찬다", "rm 은 이름만 지운다", "부하 평균은 D 상태도 센다", "평균은 쏠림을 감춘다"] } },
    { t: "환경과 한도", n: 5,
      th: { sum: "'손으로는 되는데 자동으로는 안 된다' 의 답은 대개 **환경**이거나 **한도**다.",
        body: [
          { h: "같은 명령, 다른 환경", t: "크론은 로그인 셸이 아니라서 <code>.bashrc</code> 를 읽지 않고 아주 짧은 <code>PATH</code> 만 들고 돕니다. systemd 도 마찬가지라 절대 경로가 필요합니다. 그룹도 같은 종류의 함정입니다 — 그룹 목록은 <b>세션이 시작될 때 정해져</b> 그 뒤로 바뀌지 않으므로, 사용자를 그룹에 넣어도 이미 열려 있는 세션에는 반영되지 않습니다. 이름 풀이도 그렇습니다: 응답 없는 DNS 는 거절이 아니라 <b>침묵</b>으로 답하므로 시간 초과가 날 때까지 그대로 멈춥니다." },
          { h: "한도는 여러 층에 있다", t: "프로세스가 죽거나 막힐 때 볼 곳은 하나가 아닙니다. cgroup 메모리 한도는 호스트 메모리와 무관하게 프로세스를 죽이고(종료 코드 137 이 그 표식입니다), <code>LimitNOFILE</code> 은 시스템 전체 한도가 텅 비어 있어도 그 서비스 하나만 막습니다. 소켓도 파일 기술자이므로 접속이 늘면 여기에 먼저 걸립니다. <code>KillMode=process</code> 처럼 <b>정지 방식</b>을 정하는 설정도 같은 자리에 있습니다 — 자식이 살아남아 포트를 쥐고 있으면 다음 기동이 실패합니다." }],
        code: { c: "systemctl show api -p LimitNOFILE -p KillMode\nsystemd-cgtop            # cgroup 단위 사용량\nid -nG user; getent group sudo   # 둘이 다르면 세션이 옛것", cap: "환경과 한도는 층마다 다르다" },
        key: ["크론은 PATH 가 짧다", "그룹은 세션 시작 때 정해진다", "137 은 밖에서 죽인 것", "기술자 한도는 소켓도 먹는다"] } },
  ],
};
