/* fp — 정답률 15% 아래 끝맺음의 오답 22개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "fp", fixes: [
  { k: "fp-pure-benefit1", from: "예외를 던지지 않기 때문이다", to: "순수함수는 예외를 던지지 않도록 정의되어서다" },
  { k: "꼬리 위치란", from: "꼬리 위치로 치지 않기 때문이다", to: "둘 다 꼬리 위치가 아니다 — 삼항 연산자 안에 들어 있는 호출은 꼬리 위치로 치지 않는 규칙이다" },
  { k: "꼬리 위치란", from: "마지막 가지에 있기 때문이다", to: "둘 다 꼬리 위치다 — 재귀 호출이 조건식의 마지막 가지에 있어서다" },
  { k: "꼬리 위치란", from: "오른쪽 끝에 적혀 있기 때문이다", to: "A 다 — 호출이 식의 오른쪽 끝에 적혀 있어서다" },
  { k: "부르지 말고 돌려주기", from: "스택 프레임을 만들지 않기 때문이다", to: "화살표 함수는 스택 프레임을 만들지 않아서다" },
  { k: "부르지 말고 돌려주기", from: "한 번씩 비워 주기 때문", to: "while 문이 안쪽에서 쌓인 스택을 주기적으로 한 번씩 비워 주어서" },
  { k: "깊이가 무엇에 비례하나", from: "얼마나 중첩됐는지 알 수 없기 때문이다", to: "입력이 배열이면 안전하고 객체면 위험하다 — 배열은 길이가 정해져 있어 깊이를 미리 알 수 있지만 객체는 얼마나 중첩됐는지 알 길이 없어서다" },
  { k: "끝없는 목록", from: "왼쪽부터 접기를 지원하지 않기 때문이다", to: "게으른 언어에서는 왼쪽부터 접기가 정의되어 있지 않아서다" },
  { k: "재귀냐 반복이냐", from: "반복문은 쓰지 않는다", to: "재귀를 고른다 — 함수형 코드에서 반복문은 금기다" },
  { k: "첫 실패에서 멈출까 모을까", from: "정보를 잃으므로 쓰지 않는다", to: "모두 모아야 하며 멈추는 방식은 정보를 잃으므로 폐기한다" },
  { k: "함수형이 손해인 자리", from: "피할 방법이 없다", to: "불변성 때문에 값을 통째로 복사하느라 느린 것이며 함수형을 쓰는 한 감수해야 한다" },
  { k: "몰래 깨지는 참조 투명성", from: "바꿔 넣을 일 자체가 없기 때문이다", to: "C 만 문제이며 B 는 값을 돌려주지 않으므로 참조 투명성과는 무관하다. 반환값이 없는 함수는 식으로 쓰이지 않으니 다른 값으로 바꿔 넣을 일 자체가 없어서다" },
  { k: "경쟁이 성립하는 조건", from: "겹쳐 들어가는 일 자체가 생기지 않는다", to: "동시성을 없앤다 — 불변 값은 한 번에 하나씩만 접근되도록 런타임이 보장하므로, 여러 스레드가 한꺼번에 겹쳐 들어가는 일 자체가 막힌다" },
  { k: "어디서 돌려도 같다", from: "메모리를 쓰지 않아 경쟁이 생기지 않는다", to: "순수함수는 메모리를 쓰지 않아 경쟁이 생길 자리가 없다" },
  { k: "map 이 쉬운 이유", from: "표준에 정해져 있기 때문이다", to: "reduce 는 순차 실행하도록 표준에 정해져 있어서다" },
  { k: "map 이 쉬운 이유", from: "모르면 나눌 방법이 없다", to: "map 은 결과 크기를 미리 알 수 있고 reduce 는 알 수 없기 때문이다. 크기를 알면 미리 자리를 잡아 두고 나눠 쓸 수 있지만, 모르면 나눌 길이 막힌다" },
  { k: "평균은 왜 안 되나", from: "같게 나누는 것 말고는 방법이 없다", to: "조각 크기를 모두 같게 나누는 것이 유일한 길이다" },
  { k: "결과 순서가 뒤섞이나", from: "입력 순서가 유지되므로 신경 쓸 필요가 없다", to: "입력 순서가 그대로 유지되므로 정렬은 군더더기다" },
  { k: "공유하지 말고 건네기", from: "아예 생길 수 없게 된다", to: "메시지를 쓰면 순서가 자동으로 보장되어 모든 동시성 문제가 사라지기 때문이다. 보낸 순서대로 도착하고 그 순서대로 처리되므로 경쟁도 교착도 생길 자리가 사라진다" },
  { k: "남는 문제", from: "다룰 방법이 없다", to: "단일 스레드로 바꾸는 것이 남은 문제를 다루는 유일한 길이다" },
  { k: "같은 변환, 다른 목적지", from: "미리 들어 있기 때문이다", to: "xf 안에 세 가지 목적지가 미리 들어 있어서다" },
  { k: "언제 쓰지 않는가", from: "배열을 만들 수 없기 때문이다", to: "결과를 배열로 받고 싶을 때다 — 트랜스듀서는 배열을 만들지 못해서다" },
  { k: "렌즈를 잇는다", from: "읽기 전용 뷰가 되어 바꿀 수 없게 된다", to: "읽기 전용 뷰가 되어 set 은 막힌다" },
  { k: "무엇을 먼저 재는가", from: "그것 말고는 방법이 없다", to: "원소 수를 줄인다 — 그것이 유일한 길이다" }
] };
