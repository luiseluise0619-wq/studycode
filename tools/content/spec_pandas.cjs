module.exports={
  track:"pandas", guide:"🐼", xp:80,
  unit:"실행형 실전 — pandas 의 규칙을 직접 만든다",
  source:"./exec_npd.cjs", slice:[6,12],
  lessons:[
    { t:"조인 — 행 수가 변한다", n:3,
      th:{ sum:"조인의 가장 큰 함정은 결과가 틀리는 것이 아니라 **행 수가 조용히 변하는 것**이다.",
        body:[
          {h:"복제되거나 사라진다", t:"키가 양쪽에서 유일하면 결과는 최대 왼쪽 행 수지만, 오른쪽에 같은 키가 2개 있으면 왼쪽 행이 2개로 복제된다(양쪽 2개씩이면 4행). 그래서 '**조인 뒤에 SUM**' 은 항상 의심할 신호다 — 배송이 두 번 있던 주문의 금액이 두 번 더해지고, 결과는 그럴듯한 숫자라 재무 대조에서 뒤늦게 드러난다. 반대로 내부 조인은 짝 없는 행을 통째로 버려 '결제하지 않은 사용자' 가 사라지고 평균이 부풀려진다."},
          {h:"선언하고 확인한다", t:"방어는 조인 전후 행 수를 단정하거나 `merge(..., validate='many_to_one')` 로 **관계를 선언**하는 것이다 — 선언이 깨졌다는 것은 데이터에 대한 이해가 틀렸다는 신호다. 왼쪽 조인의 채움 값도 결정이다: `None` 은 이후 집계에서 제외되고 `0` 은 계산에 참여하므로, '모른다' 와 '0 이다' 중 무엇인지 도메인이 정해야 한다."}],
        code:{ c:"before = len(df)\ndf = df.merge(other, on='id', validate='many_to_one')\nassert len(df) == before", cap:"조인 뒤 행 수를 확인하지 않으면 합계가 틀린다" },
        key:["조인은 행을 복제한다","조인 뒤 SUM 은 의심 신호","왼쪽 조인도 행 수를 보장하지 않는다","관계를 선언하고 행 수를 단정한다"] } },
    { t:"집계·결측·변형", n:3,
      th:{ sum:"집계와 결측 처리는 **질문 자체를 바꾼다** — 무엇을 세고 무엇을 채우는지가 곧 결론이다.",
        body:[
          {h:"모른다와 0 은 다르다", t:"결측을 0 으로 바꾸면 평균이 체계적으로 낮아지고 `count` 가 전체 행 수가 된다 — pandas 의 `count()` 는 **non-null 개수**이고 전체는 `size()` 다. 이 둘을 혼동하면 응답률 같은 지표가 조용히 100% 로 계산된다. sum 과 mean 의 비대칭도 알아 둘 값이 있다: 전부 결측인 그룹의 sum 은 0(빈 합의 항등원)이지만 mean 은 정의할 수 없어 NaN 이다. 그리고 `if not x` 로 결측을 판정하면 **정상적인 0 을 덮어쓴다**."},
          {h:"유일해야 하는 것과 볼 수 있었던 것", t:"피벗은 `(행, 열)` 조합이 **유일해야** 성립한다 — 중복을 조용히 덮어쓰면 결과가 완벽히 정상적인 표인데 합계가 작게 나온다. pandas 가 `ValueError` 를 던지고 `pivot_table(aggfunc=...)` 로 안내하는 것은 의도를 명시하게 만드는 설계다. 롤링 계산은 창을 미래 방향으로 잡으면 **데이터 누수**가 되는데, 오류가 나지 않고 그래프도 매끄러워 학습 성능만 좋아진다."}],
        code:{ c:"window = xs[max(0, i - w + 1):i + 1]   # 과거 w 개\n# xs[i:i+w] 는 미래를 본다 — 학습은 잘 되고 배포는 무너진다", cap:"그 시점에 실제로 알 수 있었는가" },
        key:["count 는 non-null, size 는 전체","0 을 결측으로 판정하지 않는다","피벗은 조합이 유일해야 한다","롤링 창은 과거 방향이어야 한다"] } },
  ]
};
