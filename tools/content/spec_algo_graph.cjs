module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 그래프 탐색과 최단거리",
  source: "./algo_graph.cjs",
  lessons: [
    { t: "탐색 — 격자도 그래프다", n: 4,
      th: { sum: "격자 문제는 인접 리스트를 만들지 않은 그래프 문제다. **칸이 정점, 상하좌우가 간선**이라고 읽는 순간 DFS·BFS 가 그대로 들어간다.",
        body: [
          { h: "방문 표시를 언제 찍느냐가 정답을 정한다", t: "DFS 는 **함수에 들어오자마자** 찍는다 — 재귀 호출 직전에 찍으면 사이클에서 무한히 돈다. BFS 는 **큐에 넣을 때** 찍는다 — 꺼낼 때 찍으면 같은 칸이 큐에 여러 번 들어가 시간이 폭발하고, 더 긴 경로가 짧은 값을 덮어쓸 수 있다. 이 한 줄의 위치가 맞고 틀림을 가른다." },
          { h: "간선 비용이 같으면 BFS 가 곧 최단거리", t: "큐에서 꺼내는 순서가 거리 순이므로, 어떤 칸에 **처음 닿은 순간**이 최단이다. 출발점이 여럿이면(썩은 귤, 여러 소방서) 전부 큐에 먼저 넣고 시작하면 된다 — **다중 소스 BFS** 다. 한 칸씩이 아니라 **한 층씩** 처리하면 층 번호가 곧 시간이 된다. 비용이 서로 다르면 이 논리가 깨져 다익스트라로 가야 한다." }],
        code: { c: "dist[nr][nc] = dist[r][c] + 1;\nqueue.push([nr, nc]);   // 넣을 때 표시한다", cap: "꺼낼 때 표시하면 중복이 쌓인다" },
        key: ["칸=정점, 상하좌우=간선", "DFS 는 들어올 때·BFS 는 넣을 때 표시", "같은 비용이면 BFS 가 최단", "출발점이 여럿이면 전부 큐에 먼저"] } },
    { t: "순서와 연결 — 위상 정렬·유니온 파인드", n: 4,
      th: { sum: "'무엇을 먼저 해야 하나' 와 '같은 무리인가' 는 서로 다른 자료구조를 부른다. 전자는 **진입 차수**, 후자는 **대표 정점**이다.",
        body: [
          { h: "차수가 0인 것부터 꺼내면 사이클 판정이 공짜다", t: "위상 정렬은 들어오는 간선이 없는 정점을 꺼내고, 그것이 막고 있던 작업의 차수를 줄인다. 사이클 안의 정점들은 서로를 막아 차수가 영영 0이 되지 않으므로, **꺼낸 개수가 n 보다 적으면 순환**이다. 빌드 의존성·선수 과목·스프레드시트 재계산이 모두 이 문제이고, '순환 참조' 오류 메시지는 대개 이 알고리즘이 만든다." },
          { h: "합칠 때만 세면 중복 간선이 문제되지 않는다", t: "유니온 파인드는 무리마다 대표를 하나 두고, 합칠 때 대표끼리 잇는다. 덩어리 수는 n 에서 시작해 **실제로 합쳐질 때만** 1을 뺀다 — 같은 간선이 두 번 들어와도 두 번 줄지 않는다. `parent[x] = parent[parent[x]]` 한 줄의 **경로 압축**만 붙여도 사실상 O(1) 에 가까워진다. 크루스칼 MST 는 여기에 '싼 간선부터' 라는 그리디를 얹은 것이다." }],
        code: { c: "if (deg[u] === 0) ready.push(u);\n// ...\nreturn order.length === n ? order : [];", cap: "덜 꺼냈으면 사이클이다" },
        key: ["진입 차수 0 부터 꺼낸다", "꺼낸 수 < n 이면 순환", "덩어리는 합쳐질 때만 하나씩 줄인다", "경로 압축 한 줄을 잊지 않는다"] } },
    { t: "최단거리 — 셋을 언제 쓰는가", n: 4,
      th: { sum: "다익스트라·벨만-포드·플로이드는 경쟁 관계가 아니라 **조건이 다른 도구**다. 음수 간선이 있는가, 모든 쌍이 필요한가로 갈린다.",
        body: [
          { h: "다익스트라는 음수 간선에서 무너진다", t: "'아직 확정 안 된 것 중 가장 가까운 것은 더 줄어들 수 없다' 는 그리디가 근거인데, 음수 간선이 있으면 멀리 돌아가서 더 짧아지는 일이 생겨 확정이 뒤집힌다. 그때는 벨만-포드다 — 모든 간선을 n−1 번 훑고, **한 번 더 훑어 여전히 줄어들면 음수 사이클**이다. 그 판정이 곧 환전 차익·잘못된 할인 규칙을 찾는 도구가 된다." },
          { h: "플로이드는 반복 순서가 전부다", t: "거쳐 가는 정점 `k` 가 **반드시 가장 바깥**이어야 한다. 표의 의미가 '0..k 만 거쳐 갈 때의 최단' 이기 때문인데, k 를 안쪽에 두면 아직 완성되지 않은 값을 참조해 조용히 틀린다. 코드는 거의 같아 보이고 결과만 달라지므로 가장 잡기 어려운 실수다. O(n³) 이라 정점 400 정도가 한계지만, **모든 쌍**을 열 줄로 준다." }],
        code: { c: "for (let k = 0; k < n; k++)      // 거쳐 가는 정점이 바깥\n  for (let i = 0; i < n; i++)\n    for (let j = 0; j < n; j++)", cap: "k 를 안쪽에 두면 조용히 틀린다" },
        key: ["음수 없음 → 다익스트라", "음수 있음 → 벨만-포드(+사이클 판정)", "모든 쌍 → 플로이드, k 는 바깥", "무한대에 더하기 전에 검사한다"] } },
  ],
};
