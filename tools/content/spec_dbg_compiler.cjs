module.exports = {
  track: "compiler", guide: "", xp: 80,
  unit: "실행형 실전 — 작은 예제만 통과하는 컴파일러 코드 고치기",
  source: "./dbg_compiler.cjs",
  lessons: [
    { t: "앞단 — 읽고 나누고 세는 곳", n: 7,
      th: { sum: "렉서·파서 버그는 **문법의 규칙을 코드가 반만 옮겼을 때** 난다 — 최장 일치, 상태, 결합 방향, 마무리 단계.",
        body: [
          { h: "가장 긴 것부터, 낱말이 끝난 뒤에", t: "렉서의 원칙은 <b>최장 일치</b>다. 기호를 한 글자씩 집으면 <code>==</code> 가 둘로 쪼개지므로 연산자 표는 <b>긴 것부터</b> 맞춰 본다. 반대로 키워드는 <b>식별자를 끝까지 읽은 다음</b> 목록에 있는지 본다 — 먼저 맞춰 보면 <code>iffy</code> 가 <code>if</code>+<code>fy</code> 로 쪼개지고 <code>int</code> 가 <code>in</code> 으로 읽힌다. 그리고 문자열·주석 안인지는 <b>지금까지 읽은 것</b>에 달린 상태라, 정규식 하나로는 절대 셀 수 없다." },
          { h: "결합 방향과 마무리 단계", t: "<code>-</code> 와 <code>+</code> 는 <b>좌결합</b>이다 — 재귀 하강에서 오른쪽 항을 다시 <code>expr()</code> 로 읽으면 <code>1-(2-3)</code> 이 된다. 좌결합은 <b>반복</b>으로, 우결합은 재귀로 적는다. 션팅 야드는 본문 반복이 끝난 뒤 <b>스택을 비우는 드레인</b> 단계가 명세의 마지막 줄이다. 진단의 줄 번호는 <code>split('\\n').length</code> 가 곧 1 기반 줄 수이고, 매크로 치환은 <b>낱말 단위</b>로 한 번만 훑어야 순서 의존성이 사라진다." }],
        code: { c: "if (TWO.includes(s.slice(i, i + 2))) { … }   // 긴 것부터\nwhile (t[i] === '-' || t[i] === '+') { … }   // 좌결합은 반복으로\nwhile (ops.length) out.push(ops.pop());      // 마무리 드레인", cap: "문법의 규칙을 빠짐없이 옮긴다" },
        key: ["연산자 표는 긴 것부터 시도한다", "키워드는 낱말을 다 읽고 판정한다", "상태가 필요하면 렉서를 거친다", "좌결합은 반복, 우결합은 재귀"] } },
    { t: "뒷단 — 이름·순서·부수효과", n: 6,
      th: { sum: "뒷단의 버그는 **방문 순서와 수명**에서 온다 — 어디서 이름을 찾는가, 언제 판정하는가, 무엇을 지워도 되는가.",
        body: [
          { h: "이름의 수명과 바인딩의 개수", t: "렉시컬 스코프는 <b>맵의 스택</b>이다 — 블록마다 얹고 벗기며, 조회는 안쪽부터 바깥으로 훑는다. 맵 하나로 두면 안쪽 선언이 바깥 이름을 영구히 덮는다. 같은 문제가 클로저에도 나타난다 — <code>var</code> 는 함수 하나를 스코프로 삼아 <b>모든 클로저가 변수 하나</b>를 잡고, <code>let</code> 은 반복마다 <b>새 바인딩</b>을 만든다. 요는 '바인딩을 언제 새로 만드는가' 다." },
          { h: "방문 순서와 지워도 되는 것", t: "트리 패스는 방문 순서가 곧 알고리즘이다 — 상수 접기·타입 추론은 <b>아래에서 위로</b>(후위), 스코프 주입은 위에서 아래로(전위). 링커의 심볼 해석처럼 <b>전방 참조</b>가 있는 판정은 '모으고 나서 판정' 두 패스로 해야 파일 순서에 안 흔들린다. 최적화는 '<b>관찰 가능한 동작을 바꾸지 않는다</b>' 는 약속 위에서만 정당하므로, 부수효과 여부를 모르면 <b>보수적으로 있음</b>으로 둔다. 그리고 스택은 <b>나중 것이 먼저 나온다</b> — 첫 pop 이 오른쪽 피연산자다." }],
        code: { c: "for (let i = stack.length - 1; i >= 0; i -= 1) …   // 안쪽부터 바깥으로\nconst l = fold(n.l), r = fold(n.r);                // 자식을 먼저\nit.eff || used.has(it.dst)                         // 부수효과도 살아 있음", cap: "순서가 정당성의 근거다" },
        key: ["스코프는 맵의 스택으로", "클로저는 바인딩 개수를 세어 본다", "전방 참조가 있으면 두 패스로", "교환법칙 없는 연산으로 테스트한다"] } },
  ],
};
