/* 71차 마무리 — 정답이 5자 이상 단독 최장으로 남은 30문항(14트랙)의 오답을 내용 있게 다시 쓴다 */
module.exports = [
{ track: "arduino", fixes: [
  { k: "동기를 잃었을 때", o: [
    "길이 필드는 잡음에 강하게 부호화되어 있어 이런 일은 애초에 생기지 않으며 5바이트 프레임이 그대로 처리된다",
    "200바이트가 정해진 시간 안에 다 안 오면 수신기가 타임아웃으로 알아서 버리고 자동으로 복구된다",
    "체크섬이 틀리게 나오므로 그 프레임 하나만 버리고 넘어가면 다음 프레임부터는 정상으로 돌아온다",
    "다음 프레임들을 페이로드로 잘못 삼켜 이후 전체가 어긋난다 — 동기를 다시 맞출 방법이 필요하다" ] }
] },
{ track: "backend", fixes: [
  { k: "cache-aside", o: [
    "캐시는 건드리지 않고 TTL 이 만료될 때까지 그대로 둔다",
    "수정할 때 캐시를 새 값으로 곧바로 덮어써 항상 최신으로 유지한다",
    "수정 시 캐시를 무효화(삭제)하고 다음 읽기에서 다시 채운다",
    "캐시 TTL 을 무한으로 두어 한 번 채운 값을 계속 쓴다" ] }
] },
{ track: "c", fixes: [
  { k: "c3-enum-2", o: [
    "enum 상수는 이름 문자열로 그대로 저장되어 실행 중에도 이름을 찍어 볼 수 있으므로 로그가 훨씬 읽기 쉬워지기 때문",
    "enum 은 컴파일 시점에 완전히 사라져 메모리를 전혀 쓰지 않으므로, #define 매크로보다 실행 파일이 훨씬 작아지기 때문",
    "이름으로 의도가 드러나고, switch 처리 누락을 컴파일러 경고로 잡을 여지가 생기며, 디버거에도 이름이 보이기 때문",
    "enum 값은 읽기 전용 메모리 영역에 놓여 실행 중에 바뀌지 않으므로, 실수로 덮어써도 원래 값이 안전하게 그대로 지켜지기 때문" ] },
  { k: "c2-ll-double-ptr", o: [
    "노드가 함수 안과 밖에서 한 번씩 만들어져 메모리가 두 배로 할당되고, 밖에서 만든 쪽은 그대로 누수로 남는다",
    "매개변수는 값 복사라 함수 안에서 head 를 바꿔도 호출자의 head 는 그대로여서 새 노드가 반영되지 않는다",
    "새 노드가 head 앞이 아니라 맨 뒤에 붙어 리스트가 뒤집힌 순서로 쌓이고, 순회하면 값이 처음부터 거꾸로 나온다",
    "Node* 매개변수에 새 노드의 주소를 대입하는 순간 포인터 타입이 맞지 않는다며 컴파일러가 오류를 내고 멈춘다" ] }
] },
{ track: "cpp", fixes: [
  { k: "cpp-move-noexcept", o: [
    "std::vector가 재할당할 때 강한 예외 보장을 위해 noexcept가 아닌 이동 대신 복사를 선택하기 때문",
    "noexcept 가 붙으면 컴파일러가 예외 검사 코드를 통째로 생략해 이동 연산 자체가 눈에 띄게 빨라지기 때문",
    "noexcept 가 붙은 이동 생성자는 원본 객체의 소멸자 호출을 아예 생략해 주므로 정리 비용이 통째로 사라지기 때문",
    "noexcept 없는 이동 생성자를 가진 타입은 표준 컨테이너 원소로 쓸 수 없다는 규칙이 있어 컴파일되지 않기 때문" ] }
] },
{ track: "devops", fixes: [
  { k: "cloud2-tf-state-lock", o: [
    "state 파일을 git 저장소에 커밋해 두 사람이 항상 같은 파일을 보게 한다",
    "원격 백엔드(S3 등)+상태 잠금(DynamoDB 락)으로 동시 실행을 직렬화",
    "apply 를 실행하기 전에 서로 채팅으로 연락해 시간이 겹치지 않게 조율한다",
    "로컬 state 를 각자 따로 보관하고 나중에 두 파일을 수동으로 합친다" ] }
] },
{ track: "git", fixes: [
  { k: "pull 전략", o: [
    "같은 양의 커밋을 압축된 형태로 받아 오므로 네트워크 전송이 더 빨리 끝난다",
    "원격 커밋 위에 내 커밋을 다시 얹어 불필요한 병합 커밋을 만들지 않는다",
    "내 커밋을 새로 만들어 얹기 때문에 합칠 때 충돌이 아예 나지 않게 된다",
    "받아서 합친 뒤 원격 브랜치로 푸시까지 자동으로 대신 해 준다" ] }
] },
{ track: "java", fixes: [
  { k: "ForkJoin", o: [
    "유휴 스레드는 다른 스레드의 deque 반대쪽(FIFO 끝)에서 작업을 훔쳐 부하를 고르게 분산한다",
    "각 워커 스레드는 자신의 작업을 double-ended queue(deque)의 LIFO 끝에서 꺼내 처리한다",
    "블로킹 I/O를 자주 수행하는 작업에 ForkJoinPool의 common pool을 그대로 쓰는 것이 이상적이다",
    "RecursiveTask 는 계산 결과를 반환하고 RecursiveAction 은 반환값 없이 부작용만 남긴다" ] },
  { k: "java3-ver-enum", o: [
    "모르는 값은 어느 라이브러리에서나 자동으로 ordinal 0 인 첫 번째 상수로 해석되므로 첫 상수를 기본값으로 두기만 하면 된다",
    "구버전 소비자는 모르는 값을 만나 파싱에 실패할 수 있으므로 UNKNOWN 같은 기본 처리 경로를 미리 마련해야 한다",
    "배포된 enum 에 값을 새로 추가하는 것은 어떤 경우에도 불가능하며, 값 추가는 형식 자체를 깨는 파괴적 변경이다",
    "구버전 소비자도 새 값을 자동으로 이해한다 — enum 은 이름 문자열로 그대로 전달되므로 값이 늘어도 아무 문제가 없다" ] }
] },
{ track: "javascript", fixes: [
  { k: "js2-n5-03", o: [
    "SyntaxError — 안전 정수 범위를 넘는 숫자 리터럴은 파서가 거부하기 때문",
    "false — 아주 큰 정수 리터럴은 자동으로 BigInt 타입으로 승격되어 정확히 구분되기 때문",
    "true — 안전 정수 범위(2^53-1)를 넘어 두 값이 같은 부동소수로 표현된다",
    "false — 서로 다른 정수 리터럴이므로 값이 달라 === 로는 같을 수 없기 때문" ] },
  { k: "js-fetch-preflight", o: [
    "GET 요청에서만 프리플라이트가 발생한다 — 읽기 요청은 캐시될 수 있어 브라우저가 먼저 서버의 허용 여부를 확인해야 하기 때문이다",
    "HTTPS 요청에서만 프리플라이트가 발생한다 — 암호화된 연결에서는 인증서와 함께 CORS 정책도 사전 협상 대상이 되기 때문이다",
    "모든 교차 오리진 요청은 프리플라이트를 먼저 보낸다 — 오리진이 다르면 브라우저가 예외 없이 OPTIONS 로 허용 여부를 묻기 때문이다",
    "PUT·DELETE 같은 메서드이거나 Content-Type: application/json, 커스텀 헤더처럼 단순 요청 조건을 벗어날 때" ] },
  { k: "js2-g2-01", o: [
    "length 프로퍼티와 0 부터 시작하는 숫자 인덱스 — for...of 는 그것으로 원소를 하나씩 센다",
    "Array.prototype 을 상속받고 있어야 한다는 것 — 배열이 아닌 값은 아예 순회할 수 없기 때문",
    "Symbol.iterator 메서드가 { value, done }을 반환하는 next()를 가진 객체를 돌려주는 것",
    "forEach 메서드 — for...of 는 내부적으로 forEach 를 호출해 각 원소를 차례로 넘겨받는다" ] }
] },
{ track: "linux", fixes: [
  { k: "근본 대책", o: [
    "로그인 시도 횟수 제한만 걸어 무차별 대입 공격을 늦춘다",
    "VPN 이나 배스천 호스트 뒤에 두고 공개 인터넷에 열지 않는다",
    "포트 번호를 자주 바꿔 가며 운영해 스캔에 잡히지 않게 한다",
    "접속이 필요할 때마다 방화벽을 잠깐 열었다가 다시 닫는다" ] },
  { k: "가축과 애완동물", o: [
    "값싼 하드웨어를 여러 대 사서 고장 나면 그냥 버린다는 뜻",
    "언제든 지우고 같은 것을 다시 만들 수 있는 상태로 둔다는 뜻",
    "서버 한 대 한 대에 따로 이름을 붙여 부르지 않는다는 뜻",
    "여러 대를 하나의 관리 도구로 한꺼번에 묶어 다룬다는 뜻" ] }
] },
{ track: "ml", fixes: [
  { k: "L2 효과", o: [
    "계수를 매 반복마다 무작위 값으로 흔들어 특정 특성에 치우치지 않게 한다",
    "값이 작은 계수를 정확히 0 으로 만들어 쓸모없는 특성을 걸러 내 준다",
    "계수를 전체적으로 0에 가깝게 축소하지만 정확히 0으로 만들지는 않는다",
    "계수의 부호를 반대로 뒤집어 과적합된 방향을 반대로 상쇄해 준다" ] }
] },
{ track: "pandas", fixes: [
  { k: "apply 성능", o: [
    "반환 shape 이 항상 그룹 수와 같아 결과 형태를 미리 예측하기 쉽다",
    "그룹마다 파이썬 함수를 호출하므로 agg/transform보다 일반적으로 느리다",
    "transform 과 완전히 동일한 결과를 더 짧게 쓸 수 있는 문법일 뿐이다",
    "내부적으로 C 로 최적화되어 있어 agg 보다 항상 훨씬 빠르게 동작한다" ] }
] },
{ track: "python", fixes: [
  { k: "예외 계층", q: "베이스 예외", o: [
    "공통 부모를 두면 예외 객체가 슬롯을 공유해 생성 비용과 메모리 사용량이 줄어든다",
    "타입 검사기가 예외 발생 여부를 강제로 검사해 주므로 처리 누락을 미리 막을 수 있다",
    "호출자가 except AppError 하나로 우리 코드가 낸 오류만 골라 처리할 수 있다",
    "베이스 예외에 핸들러가 붙어 있어 하위 예외가 발생하면 모두 자동으로 로깅된다" ] },
  { k: "gunicorn-uvicorn-worker", o: [
    "--workers 4 (기본 sync 워커) — 코어마다 프로세스가 하나씩 돌며 각자 요청을 처리한다",
    "--workers 4 --worker-class uvicorn.workers.UvicornWorker",
    "--threads 8 --workers 1 (gthread) — 스레드 8개가 이벤트 루프 하나를 나눠 쓴다",
    "--worker-class gthread --workers 1 --threads 4 로 스레드 워커 하나만 띄운다" ] },
  { k: "to_thread", o: [
    "함수 실행을 취소 가능한 태스크로 이벤트 루프에 등록만 해 두고 실제 실행은 나중으로 미룬다",
    "새 프로세스를 띄워 CPU 병렬 처리를 하고 결과를 피클로 직렬화해 돌려받아 await 로 받는다",
    "함수를 코루틴으로 자동 변환해 단일 스레드에서 실행하므로 새 스레드를 만들지 않고도 대기한다",
    "블로킹 함수를 별도 스레드에서 실행하고 그 결과를 await로 받아 이벤트 루프를 막지 않게 한다" ] }
] },
{ track: "sql", fixes: [
  { k: "원인 분석", q: "NOT IN 목록에 NULL", o: [
    "목록 안의 NULL 이 자동으로 0 으로 변환되어 x &lt;&gt; 0 비교가 대부분 거짓이 되기 때문",
    "x &lt;&gt; NULL 비교가 UNKNOWN을 반환해 AND 전체가 결코 TRUE가 되지 못하기 때문",
    "IN 이 목록의 NULL 을 조용히 무시하고 나머지만 비교하므로 NOT IN 도 같은 규칙을 그대로 따르기 때문",
    "NOT IN 이 목록 안의 NULL 을 만나면 목록 전체를 제거해 버려 비교할 대상이 하나도 남지 않기 때문" ] },
  { k: "기본 격리 수준", o: [
    "둘 다 READ UNCOMMITTED — 커밋 전 데이터도 읽으므로 잠금 대기가 없어 성능이 좋다",
    "PostgreSQL은 REPEATABLE READ, MySQL InnoDB는 READ UNCOMMITTED",
    "둘 다 SERIALIZABLE — SQL 표준이 정한 기본값이라 두 엔진 모두 그대로 따르고 있다",
    "PostgreSQL은 READ COMMITTED, MySQL InnoDB는 REPEATABLE READ" ] }
] },
{ track: "web", fixes: [
  { k: "web-rs-vh", o: [
    "vh 는 세로 모드에서만 계산되고 가로 모드로 돌리면 값이 갱신되지 않는다",
    "vh 단위 자체가 모바일 브라우저에서는 지원되지 않아 높이가 0 으로 계산되기 때문",
    "주소창이 접히고 펼쳐지며 뷰포트 높이가 변해 잘리거나 스크롤이 생긴다",
    "vh 는 뷰포트가 아니라 부모 요소 높이를 기준으로 계산되므로 부모가 작으면 잘린다" ] },
  { k: "web-rs-viewport", o: [
    "모바일 브라우저에서는 미디어쿼리가 전혀 동작하지 않고 CSS 자체가 무시되어 스타일이 모두 사라져 버리게 된다",
    "rem 과 em 단위가 모두 px 로 강제 변환되어 사용자의 글꼴 크기 설정이 무시되고 화면이 고정 크기로 그려진다",
    "브라우저가 980px 같은 가상 뷰포트로 렌더링해 축소해 보여주고 min-width 미디어쿼리가 의도와 다르게 걸린다",
    "페이지가 세로 방향으로만 고정되어 기기를 가로로 돌려도 화면이 회전하지 않고 세로 레이아웃이 그대로 유지된다" ] },
  { k: "안내", o: [ "placeholder", "autocomplete", "title", "value" ] },
  { k: "web-pf-critical", o: [
    "CSS 링크를 전부 body 끝으로 옮겨 HTML 이 먼저 그려지게 한 뒤 스타일을 입힌다",
    "첫 화면에 필요한 최소 규칙만 head에 인라인하고 나머지 CSS는 비동기로 불러온다",
    "CSS 를 자바스크립트로 런타임에 생성해 파일 요청 자체를 없애 첫 화면을 빠르게 한다",
    "모든 CSS 를 하나의 파일로 합쳐 요청 수를 줄이고, 두 번째 방문부터는 브라우저 캐시에 기댄다" ] },
  { k: "web-sel-where", o: [
    ":where()가 인자 중 가장 높은 특이도를 따르고, :is()는 항상 0이다",
    ":is()는 인자 중 가장 높은 특이도를 따르고, :where()는 항상 0이다",
    "둘 다 인자로 받은 셀렉터들의 특이도를 전부 더해 하나의 값으로 계산한다",
    "둘 다 인자와 무관하게 특이도가 항상 0 이라 어떤 규칙에도 덮어써진다" ] },
  { k: "web-gd-autofit", o: [
    "auto-fill 만 minmax() 와 함께 쓸 수 있고, auto-fit 은 고정 너비 트랙에서만 제대로 동작한다",
    "auto-fit 은 열 방향으로만 트랙을 만들고 auto-fill 은 행 방향으로만 트랙을 만든다는 방향의 차이가 있다",
    "둘은 동작이 완전히 같고 이름만 다른 별칭이라, 오래된 브라우저 호환을 위해 둘 다 남겨 둔 것이다",
    "아이템이 트랙 수보다 적을 때 auto-fill은 빈 트랙을 남기고, auto-fit은 빈 트랙을 접어 아이템이 늘어난다" ] },
  { k: "web-rs-emunit", o: [
    "사용자가 브라우저 기본 글꼴 크기를 키우면 중단점도 함께 움직여 글자 기준 일관성이 유지된다",
    "em/rem 은 px 보다 계산이 빨라 미디어쿼리 평가와 전체 렌더링 성능이 눈에 띄게 좋아진다",
    "px 라는 단위는 최신 브라우저에서 미디어쿼리 조건으로 더 이상 지원되지 않아 무시되기 때문",
    "em 을 쓰면 미디어쿼리가 뷰포트가 아니라 가장 가까운 컨테이너 크기를 기준으로 바뀌기 때문" ] },
  { k: "web-pf-repaint", o: [
    "display 를 block 에서 none 으로 바꿔 숨기기",
    "padding 값을 8px 에서 16px 로 늘리는 변경",
    "visibility: visible 에서 hidden 으로 변경",
    "font-size 를 14px 에서 18px 로 키우는 변경" ] },
  { k: "web-sel-varfallback", o: [
    "@media (min-width: var(--bp)) 처럼 미디어쿼리 조건식 안에서도 어디서나 직접 사용할 수 있다",
    "상속되므로 부모에 선언하면 자손 전체에서 참조할 수 있고 var(--x, 기본값) 로 대체값을 줄 수 있다",
    "선언된 값이 없으면 항상 자동으로 0 으로 계산되므로 대체값을 따로 적지 않아도 레이아웃이 깨지지 않는다",
    "사용자 정의 속성은 상속되지 않으므로 사용할 모든 요소마다 빠짐없이 개별적으로 다시 선언해 두어야만 쓸 수 있다" ] }
] }
];
