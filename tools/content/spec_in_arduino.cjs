module.exports = {
  track: "arduino", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 보드의 숫자와 신호 규격",
  source: "./in_arduino.cjs",
  lessons: [
    { t: "보드가 가진 만큼만 쓴다", n: 8,
      th: { sum: "임베디드는 **숫자를 외우는 것이 곧 설계**다 — 램 2KB, 플래시 32KB, ADC 10비트, PWM 8비트.",
        body: [
          { h: "메모리와 분해능", t: "우노의 SRAM 은 <b>2,048 바이트</b>뿐이라 변수·스택·버퍼가 전부 그 안에 들어가야 하고, 원인 모를 재시작은 대개 램이 모자라 스택과 힙이 부딪힌 것이다 — 문자열은 <code>F(\"...\")</code> 로 감싸 플래시(32KB)에 남긴다. 아날로그 입력은 <b>10비트</b>라 1,024 단계이고 <b>최댓값은 1023</b> 이라, 전압으로 바꿀 때 1023 으로 나눠야 양 끝이 맞는다. PWM 은 8비트라 최댓값이 255 다." },
          { h: "전류와 시간", t: "LED 앞의 저항은 옴의 법칙 그대로 <code>(전원 - 순방향) / 전류</code> 다 — 5V·2V·20mA 면 150Ω 이고, 실제로는 여유를 두어 220Ω 을 즐겨 쓴다. <code>millis()</code> 는 <b>약 49.7 일</b>마다 0 으로 되돌아오므로 시각 비교는 언제나 <b>차이로</b> 적는다. <code>setup()</code> 은 전원이 켜진 뒤 한 번이지 평생 한 번이 아니다 — 리셋과 시리얼 연결이 다시 부른다." }],
        code: { c: "R = (5 - 2) / 0.02        // 150Ω\nmv = raw * 5000 / 1023    // 최댓값이 1023\nnow - last >= interval    // 되돌아와도 맞다", cap: "숫자를 알면 설계가 정해진다" },
        key: ["램 2KB · 플래시 32KB", "ADC 10비트, 최댓값 1023", "PWM 은 0~255", "millis 는 49.7일마다 되돌아온다"] } },
    { t: "선으로 주고받는 규격", n: 7,
      th: { sum: "통신 규격은 **선의 개수와 고르는 방법**으로 나뉜다 — 속도를 약속하거나(UART), 주소로 고르거나(I²C), 선으로 고르거나(SPI).",
        body: [
          { h: "세 가지 방식", t: "<b>UART</b> 는 TX·RX 두 선(GND 별도)에 클럭이 없어 양쪽이 <b>보율을 미리 약속</b>해야 하고, 관례적인 기본값이 9600 이다 — 깨진 글자만 나오면 보율부터 의심한다. <b>I²C</b> 는 두 선을 공유하고 <b>7비트 주소</b>로 상대를 고르는데, 데이터시트는 흔히 8비트로 적어 두어 한 칸 밀어야 한다. <b>SPI</b> 는 장치마다 <b>CS</b> 선을 따로 두어 고르므로 장치가 늘면 핀이 는다." },
          { h: "입력과 저장의 함정", t: "입력 핀은 <b>언제나</b> 풀업이나 풀다운이 있어야 한다 — 없으면 부유 입력이라 잡음에 따라 값이 뒤집힌다. <code>INPUT_PULLUP</code> 을 쓰면 <b>눌렸을 때 0</b> 이라 논리가 반대이므로 극성을 이름으로 적어 둔다. ISR 안에서는 <code>delay</code> 를 쓰면 안 된다 — 인터럽트가 꺼져 있어 영영 돌아오지 않는다. <b>EEPROM</b> 은 셀당 약 10 만 회라 <b>값이 바뀔 때만</b> 써야 한다." }],
        code: { c: "pinMode(pin, INPUT_PULLUP);   // 눌리면 LOW\nEEPROM.update(addr, v);       // 같으면 안 쓴다\nISR: 깃발만 세우고 loop() 에서 처리", cap: "규격이 곧 배선이고 배선이 곧 코드다" },
        key: ["UART 는 속도 약속, I²C 는 주소, SPI 는 선", "I²C 주소는 7비트", "입력 핀에는 풀업이나 풀다운", "EEPROM 은 바뀔 때만 쓴다"] } },
  ],
};
