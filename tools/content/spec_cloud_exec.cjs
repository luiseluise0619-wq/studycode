module.exports = {
  track: "cloud", guide: "", xp: 80,
  unit: "직접 구현 — 클라우드가 속으로 하는 계산",
  source: "./cloud_exec.cjs",
  lessons: [
    { t: "주소와 경로 — 비트로 하는 판단", n: 5,
      th: { sum: "보안 그룹·라우팅 테이블·VPC 설계가 전부 **접두 비트 비교** 하나로 돌아간다. 만들어 보면 콘솔의 칸들이 규칙으로 바뀐다.",
        body: [
          { h: "마스크를 씌워 비교한다", t: "CIDR 판정은 주소를 32비트 정수로 바꾸고 **접두 길이만큼의 상위 비트가 같은가**를 보는 것이 전부다. 자바스크립트에서 두 가지를 조심해야 한다 — `-1 << 32` 는 0이 아니라 **-1** 이라 `/0`(전체 허용)이 뒤집히고, 비트 연산 결과는 부호 있는 32비트라 `>>> 0` 으로 되돌리지 않으면 비교가 어긋난다. 보안 규칙을 다루는 코드에서 이런 실수는 그대로 사고가 된다." },
          { h: "가장 구체적인 것이 이긴다", t: "라우팅은 여러 경로가 맞아도 **접두 길이가 가장 긴 것**을 고른다. 이 규칙 덕분에 '기본은 인터넷으로, 안쪽은 내부로' 가 두 줄로 표현되고, `0.0.0.0/0` 은 언제나 마지막 후보가 된다. 서브넷 크기 계산에는 함정이 있다 — 클라우드는 서브넷마다 **5개를 예약**하므로 `/28` 은 16이 아니라 **11대**다. 대역이 겹치면 두 네트워크를 연결할 수 없고, 이 실수는 나중에 재구축 말고는 고칠 방법이 없다." }],
        code: { c: "const mask = bits === 0 ? 0 : (-1 << (32 - bits)) >>> 0;\nreturn ((toNum(net) & mask) >>> 0) === ((toNum(ip) & mask) >>> 0);", cap: "/0 과 부호를 함께 조심한다" },
        key: ["접두 비트가 같은지만 본다", "`-1 << 32` 는 0이 아니다", "가장 긴 접두가 이긴다", "겹친 대역은 연결할 수 없다"] } },

    { t: "권한 — 거부가 언제나 이긴다", n: 5,
      th: { sum: "클라우드에서 가장 값비싼 사고는 대개 **권한을 넓게 준 것**에서 시작한다. 평가 규칙 자체는 단순해서 코드로 옮기면 여섯 줄이다.",
        body: [
          { h: "기본 거부 · 명시적 허용 · 명시적 거부", t: "아무 정책도 없으면 아무것도 못 하고(**암묵적 거부**), 허용이 있어야 통과하며, **거부는 어떤 허용보다 우선한다.** 순서나 구체성은 결과에 영향을 주지 않아 예측하기 쉽다. 이 성질 덕분에 조직 차원의 금지를 거부로 걸어 두면 하위에서 아무리 넓게 허용해도 뚫리지 않는다. 역할을 넘겨받는 구조는 이중 잠금이다 — **신뢰 정책**이 '누가 쓸 수 있는가' 를, **권한 정책**이 '무엇을 할 수 있는가' 를 정한다." },
          { h: "넓게 준 것은 저절로 좁아지지 않는다", t: "권한은 **늘어나기만 하고 줄어들지 않는** 성질이 있다. 급할 때 넓게 준 것을 좁힐 계기가 생기지 않기 때문이다. 그래서 실제 사용 기록으로 안 쓰인 권한을 정기적으로 걷어 낸다. 태그도 같은 이야기다 — 만든 뒤에 붙이려 하면 이미 수천 개가 쌓여 있으므로 **만들 때 강제**한다. 비밀은 로그·덤프·오류 보고로 새므로 마스킹하되, 유출을 발견하면 지우는 것이 아니라 **폐기하고 재발급**한다." }],
        code: { c: "if (deny.some(hit)) return \"Deny\";\nif (allow.some(hit)) return \"Allow\";\nreturn \"ImplicitDeny\";   // 아무도 허용하지 않았다", cap: "순서도 구체성도 보지 않는다" },
        key: ["기본은 거부, 거부가 최우선", "역할은 신뢰와 권한의 이중 잠금", "안 쓰인 권한은 걷어 낸다", "유출된 키는 지우지 말고 폐기"] } },

    { t: "비용 — 켜 둔 시간과 나가는 데이터", n: 5,
      th: { sum: "요금이 예상을 넘는 이유는 대개 비싼 것을 써서가 아니라 **쓰지 않는 것에 계속 내고 있어서**다.",
        body: [
          { h: "끄지 않은 것이 가장 비싸다", t: "요금은 대부분 **켜져 있던 시간**으로 매겨진다. 개발·테스트 환경을 밤과 주말에 끄면 일주일 168시간 중 50시간 남짓만 켜져, 어떤 최적화보다 효과가 크다. 같은 기계에 값이 세 가지인 이유는 파는 것이 성능이 아니라 **약속**이기 때문이다 — 오래 쓰겠다고 하면 깎아 주고(약정), 회수해도 좋다고 하면 더 깎아 준다(스팟). 조금만 쓸 거라면 약정이 오히려 가장 비싸다." },
          { h: "나가는 쪽에만 붙는다", t: "들어오는 데이터는 대개 무료인데 **나가는 데이터**에는 요금이 붙고, AZ 사이·리전 사이·NAT 통과에도 각각 붙는다. 이미지를 그대로 서비스하면 전송비가 저장비를 훌쩍 넘어, 앞에 CDN 을 두는 것이 가장 큰 절감이 되기도 한다. 보관 등급을 낮추는 규칙에는 함정이 있다 — **값싼 등급에는 꺼내는 값이 붙어서**, 자주 조회한다면 옮겨서 아낀 것보다 꺼내느라 내는 것이 크다." }],
        code: { c: "const billable = Math.max(gb - freeGb, 0);   // 음수 요금을 막는다\nreturn Math.round(billable * rate * 100) / 100;", cap: "무료 제공량 안에서도 부호를 조심" },
        key: ["끄지 않은 것이 가장 비싸다", "약정은 계속 쓸 것에만", "나가는 데이터에만 붙는다", "싼 등급에는 꺼내는 값이 붙는다"] } },

    { t: "규모와 가용성 — 늘리고 빼고 넘긴다", n: 5,
      th: { sum: "오토스케일과 헬스 체크는 **작은 판정 함수**다. 어려움은 계산이 아니라 **반응의 속도와 완충**에 있다.",
        body: [
          { h: "비례식 하나와 상한", t: "목표 추적 스케일링은 '지금 지표가 목표의 두 배면 대수도 두 배로' 라는 비례식이다. **올림**하지 않으면 언제나 조금씩 모자라고, **최대 대수**가 없으면 잘못된 지표 하나로 요금이 폭발한다. 상한은 성능 제한이 아니라 사고 방지 장치다. 다만 계산이 맞아도 **새 인스턴스가 준비되는 시간**이 반응의 한계를 정하므로, 예상되는 부하는 일정 기반으로 미리 늘려 둔다." },
          { h: "한 번 실패했다고 빼지 않는다", t: "헬스 체크는 **연속 실패 횟수**로 상태를 바꾼다. 이 완충이 없으면 순간적인 지연 하나로 서버가 들락날락하고 그때마다 연결이 끊긴다. 횟수를 크게 잡으면 안정적이지만 죽은 서버를 늦게 빼므로, 주기와 함께 정해야 한다. 재시도도 같은 이야기다 — 같은 간격으로 계속 두드리면 회복하려는 서버를 다시 넘어뜨리므로 **두 배씩 늘리고 상한과 지터**를 둔다. 붙이기 전에 **여러 번 실행돼도 안전한가**를 먼저 확인한다." }],
        code: { c: "const want = Math.ceil(current * metric / target);\nreturn Math.min(Math.max(want, min), max);   // 상한이 사고를 막는다", cap: "올림하고 범위로 자른다" },
        key: ["올림하고 상·하한으로 자른다", "준비 시간이 반응 한계", "연속 실패로 판정한다", "재시도에는 상한과 지터"] } },

    { t: "약속과 복구 — 숫자로 말한다", n: 4,
      th: { sum: "가용성·오류 예산·RPO 는 전부 **숫자를 정하는 일**이고, 그 숫자가 곧 비용과 설계를 정한다.",
        body: [
          { h: "9 하나가 10분의 1", t: "99.9% 는 30일에 **43분**이다. 9 하나가 늘 때마다 허용 시간이 10분의 1이 되어 99.999% 는 26초인데, 사람이 알아채고 대응할 수 없는 시간이라 **자동 전환 없이는 달성할 수 없다.** 그래서 목표를 정하는 일이 곧 비용을 정하는 일이고, 모든 서비스에 최고 수준을 적용하면 감당할 수 없다. 남은 여유분인 **오류 예산**은 '안정성과 속도 중 무엇이 먼저인가' 를 매번 논쟁하지 않게 해 준다 — 다만 목표를 100%로 잡으면 예산이 0이라 아무것도 배포할 수 없게 된다." },
          { h: "적용 전에 보고, 복원해 보고", t: "RPO 검산에서 **장애 이후에 만들어진 백업은 쓸 수 없다** — 이미 망가진 데이터를 담고 있기 때문이다. 백업 주기가 60분인데 RPO 가 15분이면 구조적으로 지킬 수 없으니, 숫자가 방식을 정한다. 인프라 코드의 계획 단계도 같은 성격이다 — **적용 전에 무엇이 바뀌는지 보여 주는 것**이 실수로 운영 자원을 지우는 사고를 막는다. 삭제가 목록에 있으면 멈추고, 변경으로 표시된 것이 사실은 재생성인지 확인한다." }],
        code: { c: "const before = backups.filter((b) => b <= failAt);\n// 장애 이후 백업은 후보가 아니다\nreturn failAt - Math.max.apply(null, before) <= rpoMin;", cap: "지난 것 중에서만 고른다" },
        key: ["99.9% 는 한 달에 43분", "100% 목표는 목표가 없는 것", "장애 이후 백업은 쓸 수 없다", "계획에 삭제가 있으면 멈춘다"] } },
  ],
};
