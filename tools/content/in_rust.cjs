/* 러스트 단답 15 — 소유권·빌림·타입 규칙을 낱말로 적어 본다.
   검증:  node ver_input.cjs ./in_rust.cjs */
module.exports = [

{ track:"rust", t:"input", cat:"internals", k:"대입하면 원래 변수는 못 쓴다",
  q:"소유권이 있는 값을 다른 변수에 대입하면 원래 변수는 더 쓸 수 없게 됩니다. 이 일을 무엇이라 부를까요? (영문 한 단어)",
  a:["move", "이동", "무브"],
  ex:"소유권이 <b>옮겨 갑니다</b>. 그래서 원래 변수는 컴파일러가 '값이 옮겨 갔다' 며 막습니다.\n개념: 값을 하나만 책임지게 만들어 <b>이중 해제</b>와 <b>해제 후 사용</b>을 컴파일 시점에 없앱니다.\n실무: 옮기고 싶지 않으면 참조를 넘기거나 <code>clone()</code> 을 합니다 — 다만 <code>clone()</code> 을 남발하면 성능 이점이 사라집니다." },

{ track:"rust", t:"input", cat:"internals", k:"고칠 수 있는 참조의 개수",
  q:"한 시점에 같은 값에 대해 <b>고칠 수 있는</b> 참조는 최대 몇 개까지 존재할 수 있을까요? (숫자)",
  a:["1", "1개", "하나"],
  ex:"가변 참조는 <b>딱 하나</b>만 허용됩니다. 이 규칙 하나가 데이터 경쟁을 컴파일 시점에 없앱니다.\n개념: 반대로 <b>읽기만 하는</b> 참조는 몇 개든 동시에 있어도 됩니다.\n실무: 이 규칙에 걸리면 대개 자료구조를 잘못 나눈 것입니다 — 인덱스로 나눠 잡거나(<code>split_at_mut</code>), 소유 구조를 다시 그리세요." },

{ track:"rust", t:"input", cat:"internals", k:"읽기와 쓰기를 같이 빌릴 수 있는가",
  q:"같은 값에 대해 읽기만 하는 참조와 고칠 수 있는 참조를 <b>동시에</b> 가질 수 있을까요? (예 또는 아니오)",
  a:["아니오", "아니요", "no", "불가", "불가능"],
  ex:"안 됩니다. 읽는 쪽이 보고 있는 값을 다른 쪽이 바꾸면 <b>일관성이 깨지기</b> 때문입니다.\n개념: 규칙은 두 줄로 요약됩니다 — '읽기 여럿' 이거나 '쓰기 하나' 이거나, 둘 중 하나만.\n실무: 컬렉션을 훑으면서 고치려 할 때 이 규칙에 걸립니다. 인덱스만 모아 두었다가 나중에 고치거나, 새 컬렉션을 만들어 돌려주세요." },

{ track:"rust", t:"input", cat:"internals", k:"참조의 수명을 검사하는 장치",
  q:"참조가 가리키는 값보다 오래 살지 않는지 컴파일 시점에 검사하는 컴파일러의 장치를 무엇이라 부를까요? (영문 두 낱말)",
  a:["borrow checker", "빌림검사기", "대여검사기"],
  ex:"'참조가 원본보다 오래 살지 않는다' 를 검사합니다. 통과하면 <b>허상 포인터가 없다</b>는 것이 증명된 셈입니다.\n개념: 생명주기 표기(<code>'a</code>)는 새 규칙이 아니라 이 검사에 <b>필요한 정보를 적어 주는</b> 문법입니다.\n실무: 대부분은 컴파일러가 알아서 채우고(생명주기 생략 규칙), 반환 참조가 어느 인자에서 왔는지 모호할 때만 직접 적습니다." },

{ track:"rust", t:"input", cat:"internals", k:"값이 없을 수도 있는 타입의 빈 쪽",
  q:"값이 있을 수도 없을 수도 있는 표준 타입에서, <b>값이 없는 쪽</b>의 이름은 무엇일까요? (영문 한 낱말)",
  a:["None", "논"],
  ex:"<code>Option&lt;T&gt;</code> 는 <code>Some(T)</code> 와 <code>None</code> 둘 중 하나입니다. 널 포인터 대신 <b>타입으로</b> 없음을 표현합니다.\n개념: 그래서 값을 꺼내려면 반드시 두 경우를 다뤄야 하고, 컴파일러가 빠뜨림을 잡아 줍니다.\n실무: <code>unwrap()</code> 은 없을 때 프로그램을 멈춥니다 — 확실한 자리에만 쓰고, 아니면 <code>?</code>·<code>unwrap_or</code>·<code>match</code> 로 다루세요." },

{ track:"rust", t:"input", cat:"internals", k:"실패를 담는 쪽의 이름",
  q:"성공과 실패를 함께 담는 표준 타입에서, <b>실패한 쪽</b>의 이름은 무엇일까요? (영문 한 낱말)",
  a:["Err", "에러"],
  ex:"<code>Result&lt;T, E&gt;</code> 는 <code>Ok(T)</code> 와 <code>Err(E)</code> 둘 중 하나입니다. 예외를 던지는 대신 <b>반환값으로</b> 실패를 알립니다.\n개념: 실패가 <b>타입에 적혀 있으므로</b> 어떤 함수가 실패할 수 있는지 시그니처만 봐도 압니다.\n실무: <code>?</code> 연산자는 실패면 즉시 반환하고 성공이면 값을 꺼내 줘서, 오류 전파 코드가 한 글자로 줄어듭니다." },

{ track:"rust", t:"input", cat:"internals", k:"힙에 글자를 소유하는 타입",
  q:"문자열을 다루는 두 타입 가운데 <b>힙에 담긴 내용을 소유하고 길이를 늘릴 수 있는</b> 쪽의 이름은? (영문 한 낱말)",
  a:["String", "스트링"],
  ex:"<code>String</code> 은 소유하고 자랄 수 있으며, <code>&amp;str</code> 은 <b>남의 글자를 빌려 보는 창</b>입니다.\n개념: 함수 인자는 <code>&amp;str</code> 로 받는 편이 넓게 쓰입니다 — 소유 문자열도 빌려 줄 수 있기 때문입니다.\n실무: 반환할 때는 소유가 필요하니 <code>String</code>, 받을 때는 <code>&amp;str</code> — 이 조합이 가장 흔한 규약입니다." },

{ track:"rust", t:"input", cat:"internals", k:"여러 스레드가 함께 세는 포인터",
  q:"참조 개수를 세어 마지막에 해제하는 포인터 가운데 <b>여러 스레드에서 함께</b> 쓸 수 있는 쪽의 이름은? (영문 세 글자)",
  a:["Arc", "std::sync::Arc"],
  ex:"<code>Rc</code> 는 단일 스레드용이라 세는 일이 빠르고, <code>Arc</code> 는 <b>원자적으로</b> 세어 스레드 사이에서 안전합니다.\n개념: 이름이 다른 이유가 곧 비용의 차이입니다 — 필요 없는 곳에서 원자 연산을 하면 손해입니다.\n실무: 공유 상태는 보통 <code>Arc&lt;Mutex&lt;T&gt;&gt;</code> 로 감쌉니다 — 소유는 <code>Arc</code> 가, 배타 접근은 <code>Mutex</code> 가 맡습니다." },

{ track:"rust", t:"input", cat:"internals", k:"실행 중에 빌림을 세는 상자",
  q:"겉으로는 읽기 전용이지만 <b>실행 중에</b> 빌림 규칙을 검사해 안쪽 값을 고칠 수 있게 해 주는 표준 타입의 이름은? (영문 한 낱말)",
  a:["RefCell", "std::cell::RefCell"],
  ex:"컴파일 시점 검사를 <b>실행 시점으로 미룬</b> 것입니다. 규칙을 어기면 컴파일 오류 대신 그 자리에서 프로그램이 멈춥니다.\n개념: 이런 방식을 <b>내부 가변성</b>이라 하고, <code>Cell</code>·<code>RefCell</code>·<code>Mutex</code> 가 모두 그 갈래입니다.\n실무: 트리에서 부모를 가리키는 참조처럼 소유 구조로 표현하기 어려운 그래프에 쓰지만, 남용하면 컴파일러의 보증을 스스로 버리는 셈입니다." },

{ track:"rust", t:"input", cat:"internals", k:"실행 중에 구현을 고르는 키워드",
  q:"트레이트 객체를 만들어 <b>실행 시점에</b> 구현을 고르게 할 때 타입 앞에 붙이는 키워드는? (영문 세 글자)",
  a:["dyn"],
  ex:"<code>Box&lt;dyn Trait&gt;</code> 처럼 씁니다. 함수 호출이 <b>표를 거쳐</b> 이뤄져 약간 느리지만, 서로 다른 타입을 한 목록에 담을 수 있습니다.\n개념: 반대편은 제네릭(<code>impl Trait</code>)입니다 — 컴파일 시점에 타입마다 코드를 따로 만들어 빠르지만 바이너리가 커집니다.\n실무: 플러그인·핸들러 목록처럼 <b>가짓수를 미리 모르는</b> 곳에만 트레이트 객체를 쓰세요." },

{ track:"rust", t:"input", cat:"internals", k:"안전하지 않은 블록에서만 되는 일",
  q:"<code>unsafe</code> 블록 안에서만 할 수 있는 대표적인 일은 '원시 포인터 ___' 입니다. 빈칸에 들어갈 말은? (세 글자)",
  a:["역참조", "디레퍼런스", "dereference"],
  ex:"컴파일러가 유효성을 보증할 수 없으므로, 그 책임을 <b>사람이 지겠다</b>고 표시하는 것이 <code>unsafe</code> 입니다.\n개념: <code>unsafe</code> 는 빌림 검사를 <b>끄지 않습니다</b> — 몇 가지 연산을 추가로 허용할 뿐입니다.\n실무: 안전하지 않은 코드는 작게 감싸 <b>안전한 API</b> 로 내보내고, 지켜야 할 전제를 주석에 적어 두는 것이 관례입니다." },

{ track:"rust", t:"input", cat:"internals", k:"뒤에 붙이는 비용",
  q:"동적 배열의 맨 뒤에 값을 붙이는 연산의 <b>균등 상환</b> 시간 복잡도를 빅오 표기로 적으세요. (예: O(n log n))",
  a:["O(1)", "O(1) 상환", "상수시간"],
  ex:"자리가 모자라면 용량을 <b>두 배로</b> 늘리며 옮기는데, 그 비용을 전체 삽입 횟수로 나누면 상수가 됩니다.\n개념: 개별 삽입 하나는 <code>O(n)</code> 일 수 있습니다 — '균등 상환' 은 <b>평균</b>이 아니라 '연속된 연산 전체의 합' 을 나눈 값입니다.\n실무: 넣을 개수를 미리 안다면 용량을 먼저 잡아 두세요 — 옮기는 일이 아예 없어집니다." },

{ track:"rust", t:"input", cat:"internals", k:"크기를 컴파일 때 모르는 타입",
  q:"슬라이스나 트레이트 객체처럼 컴파일 시점에 <b>크기를 알 수 없는</b> 타입을 무엇이라 부를까요? (영문 약어 세 글자)",
  a:["DST", "dynamically sized type", "동적크기타입"],
  ex:"Dynamically Sized Type 입니다. 크기를 모르니 <b>그대로는 변수에 담을 수 없고</b>, 반드시 참조나 <code>Box</code> 뒤에 두어야 합니다.\n개념: 그래서 이런 값을 가리키는 포인터는 <b>주소 + 길이</b>(또는 주소 + 표) 두 칸짜리 '뚱뚱한 포인터' 입니다.\n실무: '크기를 알 수 없다' 는 컴파일 오류를 만나면 대개 <code>&amp;</code> 나 <code>Box&lt;&gt;</code> 를 씌우면 풀립니다." },

{ track:"rust", t:"input", cat:"internals", k:"디버그 빌드에서 정수가 넘치면",
  q:"디버그 빌드에서 정수 연산이 표현 범위를 넘으면 프로그램은 어떻게 될까요? (두 글자)",
  a:["패닉", "panic", "중단", "멈춤"],
  ex:"조용히 감싸 도는 대신 <b>그 자리에서 멈춥니다</b>. 릴리스 빌드에서는 기본적으로 감싸 돌아, 두 빌드의 동작이 다릅니다.\n개념: 넘침을 값으로 다루려면 <code>checked_add</code>(Option)·<code>saturating_add</code>(끝값으로 고정)·<code>wrapping_add</code>(감싸 돌기)를 골라 씁니다.\n실무: 릴리스에서만 틀리는 계산이 있다면 이 차이를 먼저 의심하세요 — 원하는 동작을 <b>이름으로</b> 적어 두면 빌드에 안 딸려 갑니다." },

{ track:"rust", t:"input", cat:"internals", k:"없음을 공짜로 담는 최적화",
  q:"64비트 환경에서 <code>Option&lt;Box&lt;i32&gt;&gt;</code> 한 개의 크기는 몇 바이트일까요? (숫자)",
  a:["8", "8바이트"],
  ex:"<code>Box</code> 는 <b>널이 될 수 없는</b> 포인터라, 컴파일러가 0 이라는 쓰이지 않는 비트 패턴을 <code>None</code> 으로 씁니다 — 태그를 위한 추가 공간이 필요 없습니다.\n개념: 이를 <b>널 포인터 최적화</b>(니치 최적화)라 하며, 안전한 <code>Option</code> 을 공짜로 쓰게 해 줍니다.\n실무: 반대로 <code>Option&lt;u64&gt;</code> 는 모든 비트 패턴이 정당한 값이라 태그가 따로 필요해 16 바이트입니다 — 구조체를 촘촘히 담고 싶다면 이 차이를 기억하세요." },

];
