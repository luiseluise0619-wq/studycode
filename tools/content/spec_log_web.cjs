module.exports = {
  track: "web", guide: "", xp: 70,
  unit: "로그 분석 — 네트워크 탭이 말해 주는 것",
  source: "./log_web.cjs",
  lessons: [
    { t: "요청이 닿기 전에 막힐 때", n: 6,
      th: { sum: "서버 로그가 비어 있는데 화면이 실패한다면, **막은 것은 브라우저**다.",
        body: [
          { h: "브라우저가 먼저 거절한다", t: "CORS 사전 요청이 통과해도 <b>보낼 헤더 목록</b>에 없으면 본 요청은 아예 나가지 않는다. https 문서가 http 자원을 부르면 혼합 콘텐츠로 차단되고, <code>SameSite</code>·<code>Path</code>·<code>Domain</code> 이 어긋난 쿠키는 붙지 않는다. 셋 다 서버에는 흔적이 없으므로 <b>브라우저 콘솔과 네트워크 탭</b>이 유일한 증거다." },
          { h: "401 과 302 의 고리", t: "쿠키가 안 붙으면 서버는 401 을 내고 앱은 로그인으로 되돌린다. 로그인은 성공하니 다시 들어가고 또 401 — 무한 고리는 대개 <b>인증이 아니라 전송 조건</b>의 문제다. 로그에서 <code>Set-Cookie</code> 한 줄의 속성을 그대로 읽는 것이 가장 빠르다." }],
        code: { c: "OPTIONS 204 + Allow-Origin ✓  → 그래도 Allow-Headers 없으면 차단\nSet-Cookie: sid=…; Path=/login   → /dashboard 에는 안 붙는다\nhttps 문서 + http 이미지          → Mixed Content 차단", cap: "서버 로그가 비면 브라우저를 본다" },
        key: ["Allow-Origin 만으로는 부족", "쿠키는 Path·Domain·SameSite 로 걸러진다", "혼합 콘텐츠는 조용히 막힌다", "무한 고리는 전송 조건을 의심"] } },
    { t: "느린 첫 화면의 증거", n: 6,
      th: { sum: "TTFB·전송·렌더 중 **어디에 시간이 쌓였는지**를 먼저 가른다. 서버 처리 30ms 에 화면 3.4초라면 서버는 무죄다.",
        body: [
          { h: "오기까지 걸리는 시간", t: "리다이렉트 세 번은 왕복과 TLS 를 세 번 치르고, 압축을 안 켜면 4MB 를 그대로 보낸다. HTTP/1.1 은 호스트당 동시 연결이 여섯 개라 요청이 많으면 계단이 생긴다. 이 셋은 모두 <b>응답 내용과 무관한</b> 지연이라 코드로는 못 고친다." },
          { h: "온 뒤에 걸리는 시간", t: "<code>&lt;head&gt;</code> 의 일반 스크립트는 파서를 멈추고, 크기를 안 알려 준 이미지는 도착한 뒤 화면을 밀어낸다(CLS). 폰트는 기본값이 <b>글자를 안 그리고 기다리는 것</b>이라 느린 CDN 하나가 본문을 3초간 숨긴다. 캐시는 반대로 <b>너무 오래</b> 잡아 옛 화면을 지킨다." }],
        code: { c: "TTFB 940ms, 서버 40ms   → 리다이렉트·연결 비용\nContent-Encoding 없음    → 압축 미적용\n<img width height>       → 자리를 미리 잡는다\nindex.html: no-cache     → 진입 문서는 짧게", cap: "구간을 갈라야 처방이 갈린다" },
        key: ["서버 시간과 총 시간을 나눠 본다", "리다이렉트는 왕복 비용", "head 의 스크립트는 파서를 멈춘다", "해시 없는 파일은 짧게 캐시"] } },
  ],
};
