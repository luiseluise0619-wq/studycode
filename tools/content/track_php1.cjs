/* PHP 트랙 전반부 — 유닛 1~6.
   문항은 쉬운 것부터 어려운 것 순으로 배치한다(계단식).
   _p 필드가 붙은 문항은 주장을 실제 php 로 실행해 확인한다(주입 전에 제거된다). */

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex)=>({t:"input",k,q,a,ex});

/* _p: {code:"php 표현식", want:"var_export 결과", pre:"선언부"} — 보기가 주장하는 값을 실제로 확인한다 */
const P=(code,want,pre)=>({code,want,pre});

module.exports=[

/* ═══════════════ 유닛 1 ═══════════════ */
{ t:"PHP 첫걸음 — 무엇이고, 어디서 도는가", l:[

  { t:"실행 모델 — 요청마다 새로 태어난다", xp:15,
    th:{ sum:"PHP 는 **웹 요청 하나마다 프로그램이 새로 시작하고 끝나는** 언어다. 이 '아무것도 공유하지 않는' 모델이 PHP 의 장점과 한계를 거의 다 설명한다.",
      body:[
        {h:"HTML 속에서 태어났다", t:"PHP 파일은 기본이 **출력**이고 `<?php … ?>` 안쪽만 코드다. 그래서 템플릿 엔진 없이도 HTML 에 값을 끼워 넣을 수 있었고, 이것이 90년대 웹에서 PHP 가 퍼진 결정적 이유였다. 지금은 반대로 **로직과 출력을 분리**하는 것이 관례지만, 언어의 뿌리는 여전히 여기에 있다."},
        {h:"공유하지 않는다(shared-nothing)", t:"요청이 끝나면 변수·객체·연결이 **전부 사라진다**. 그래서 메모리 누수가 서비스를 죽이는 일이 드물고 배포가 단순하다. 대신 요청 사이에 무엇을 남기려면 세션·캐시·DB 같은 **바깥 저장소**가 반드시 필요하고, '앱 시작 시 한 번만' 같은 초기화도 매 요청 반복된다 — 그 비용을 줄이는 것이 OPcache 다."}],
      code:{ c:"<h1>안녕, <?= htmlspecialchars($name) ?></h1>\n<?php foreach ($items as $it): ?>\n  <li><?= htmlspecialchars($it) ?></li>\n<?php endforeach; ?>", cap:"<?= 는 echo 의 축약형이다" },
      key:["PHP 파일의 기본은 출력, <?php 안쪽만 코드","요청마다 프로그램이 새로 시작하고 끝난다","요청 사이의 상태는 바깥 저장소에 둔다","<?= 는 echo 축약"] },
    q:[
      C("실행 위치","PHP 코드는 기본적으로 어디에서 실행되는가?",
        ["사용자의 브라우저","웹 서버","데이터베이스 엔진","사용자의 그래픽 카드"],1,
        "PHP 는 **서버에서 실행되어 결과(주로 HTML)를 브라우저로 보냅니다**. 브라우저는 PHP 코드를 본 적도 없습니다.\n💡 그래서 PHP 소스에 API 키를 두는 것은 (JavaScript 와 달리) 사용자에게 노출되지 않습니다 — 다만 서버 설정이 잘못되면 소스가 그대로 내려갈 수 있으니 '노출되지 않는다' 를 전제로 삼지는 않습니다."),
      C("태그","PHP 파일에서 코드로 해석되는 구간은?",
        ["파일 전체","<?php 와 ?> 사이","// 로 시작하는 줄","<script> 태그 안"],1,
        "`<?php … ?>` **바깥은 그대로 출력**되고 안쪽만 코드입니다.\n💡 순수 PHP 파일에서는 닫는 `?>` 를 **쓰지 않는 것이 관례**입니다 — 뒤에 공백이나 줄바꿈이 남으면 그게 그대로 응답에 섞여 `headers already sent` 오류를 냅니다."),
      C("출력","<code>echo</code> 와 <code>&lt;?= ?&gt;</code> 의 관계로 옳은 것은?",
        ["둘은 무관하다","<?= $x ?> 는 <?php echo $x; ?> 의 축약형이다","<?= 는 변수 선언 전용이다","<?= 는 PHP 8 에서 삭제되었다"],1,
        "`<?= $x ?>` 는 `<?php echo $x; ?>` 와 **같습니다**(short echo tag, 항상 켜져 있습니다).\n💡 템플릿에서는 거의 항상 `<?= htmlspecialchars($x) ?>` 형태로 써야 합니다 — 그냥 `<?= $x ?>` 는 XSS 통로입니다."),
      C("생명주기","한 HTTP 요청이 끝났을 때 PHP 스크립트의 전역 변수는?",
        ["다음 요청에서도 그대로 남는다","전부 사라진다","세션에 자동 저장된다","디스크에 직렬화된다"],1,
        "PHP 는 **shared-nothing** 모델이라 요청이 끝나면 그 프로세스의 메모리가 전부 정리됩니다.\n💡 그래서 Node.js·Java 처럼 '앱이 계속 살아 있는' 언어의 습관(전역 캐시, 모듈 수준 싱글턴)이 PHP 에서는 통하지 않습니다. 요청 사이에 남길 것은 세션·Redis·DB 로 보내야 합니다.\n🎯 반대로 이 성질 덕분에 **메모리 누수가 서비스를 죽이지 않고**, 배포가 '파일 교체' 만으로 끝납니다."),
      I("확장자","PHP 로 처리되도록 웹 서버에 넘겨지는 파일의 일반적인 확장자는? (점 없이)",
        ["php",".php","PHP"],
        "정답은 `php` 입니다.\n💡 예전에 쓰이던 `.php3`·`.phtml` 은 지금은 쓰지 않습니다. 오히려 **업로드 디렉터리에서 .php 실행을 막는 것**이 보안의 기본입니다 — 업로드된 이미지가 PHP 로 실행되면 그것이 곧 서버 장악입니다."),
      C("현대적 구조","요즘 PHP 프로젝트가 <b>모든 요청을 <code>public/index.php</code> 하나로 받는</b> 이유로 가장 적절한 것은?",
        ["PHP 가 파일 하나만 실행할 수 있어서",
         "라우팅·인증·에러 처리를 한 곳에서 일관되게 적용하고, 소스 파일을 웹 루트 밖에 둘 수 있어서",
         "OPcache 가 파일 하나만 캐시할 수 있어서",
         "Composer 가 파일 하나만 요구해서"],1,
        "**프론트 컨트롤러** 패턴입니다. 진입점이 하나면 라우팅·인증·예외 처리·로깅을 **빠짐없이** 통과시킬 수 있고, `public/` 만 웹에 노출하고 소스·설정·`vendor/` 는 **웹 루트 바깥**에 둘 수 있습니다.\n⚠️ 파일마다 URL 이 대응하던 옛 구조에서는 '인증 검사를 빠뜨린 파일 하나' 가 곧 구멍이었습니다. 디스트랙터들은 전부 기술적 제약처럼 들리지만 실제 제약이 아닙니다."),
    ] },

  { t:"변수·타입·출력", xp:15,
    th:{ sum:"PHP 의 변수는 **`$` 로 시작하고 타입을 선언하지 않는다**. 타입은 값에 붙어 있고, 필요할 때 자동으로 바뀐다 — 이 편리함이 곧 함정의 출발점이다.",
      body:[
        {h:"타입은 값에 붙는다", t:"변수 자체에는 타입이 없고 담긴 값이 타입을 가진다: `int`·`float`·`string`·`bool`·`array`·`object`·`null`. `gettype()` 보다 `var_dump()` 가 실무에서 더 유용하다 — **타입과 값을 함께** 보여 주기 때문이다. `\"5\"` 와 `5` 를 눈으로 구분할 수 있는 유일한 방법이기도 하다."},
        {h:"문자열 안의 변수", t:"큰따옴표와 heredoc 은 `$변수` 를 **해석**하고, 작은따옴표와 nowdoc(`<<<'EOT'`)은 **그대로** 둔다. 이어 붙이기는 `+` 가 아니라 **`.`** 이며(`+` 는 언제나 산술이다), 복잡한 표현은 `{$obj->prop}` 처럼 중괄호로 감싸야 의도대로 해석된다."}],
      code:{ c:"$n = 5;\necho \"값은 $n\";     // 값은 5\necho '값은 $n';      // 값은 $n\necho '합: ' . ($n + 1);  // 이어 붙이기는 점(.)", cap:"작은따옴표는 해석하지 않는다" },
      key:["변수는 $ 로 시작하고 타입 선언이 없다","var_dump 는 타입과 값을 함께 보여 준다","큰따옴표는 해석, 작은따옴표는 그대로","문자열 이어 붙이기는 . (+ 아님)"] },
    q:[
      C("변수","PHP 변수 이름 앞에 반드시 붙는 기호는?",
        ["#","$","@","%"],1,
        "PHP 의 변수는 언제나 `$` 로 시작합니다(`$name`).\n💡 `@` 는 오류 억제 연산자입니다 — **쓰지 마세요**. 오류를 숨겨 원인 파악을 불가능하게 만듭니다."),
      C("따옴표","<code>$n = 3;</code> 일 때 <code>echo '값 $n';</code> 의 출력은?",
        ["값 3","값 $n","값","오류"],1,
        "작은따옴표는 변수를 **해석하지 않습니다**. 큰따옴표(`\"값 $n\"`)라야 `값 3` 이 됩니다.\n💡 그래서 성능을 이유로 작은따옴표를 강요하는 규칙이 돌아다니는데, 차이는 측정 불가 수준입니다. 기준은 **해석이 필요한가**여야 합니다.",
        P("(function(){$n=3; return '값 $n';})()","'값 $n'")),
      C("연결","PHP 에서 문자열을 이어 붙이는 연산자는?",
        ["+","&",".","++"],2,
        "PHP 는 **점(`.`)** 으로 잇습니다. `+` 는 언제나 산술이라 `\"a\" + \"b\"` 는 PHP 8 에서 `TypeError` 입니다.\n💡 JavaScript·Java 에서 온 사람이 가장 자주 틀리는 부분입니다. 반대로 PHP 의 `.` 은 **명확해서**, 숫자 덧셈인지 문자열 연결인지 헷갈릴 일이 없다는 장점도 있습니다."),
      C("디버깅","값의 <b>타입까지</b> 확인하며 디버깅할 때 가장 알맞은 함수는?",
        ["echo","print_r","var_dump","printf"],2,
        "`var_dump()` 만 **타입과 길이까지** 보여 줍니다 — `string(1) \"5\"` 와 `int(5)` 를 구분할 수 있는 것은 이것뿐입니다.\n💡 `print_r` 은 구조는 보여 주지만 `5` 와 `\"5\"` 를 똑같이 찍습니다. 느슨한 비교 버그를 쫓을 때 `print_r` 을 쓰면 원인을 영원히 못 찾습니다.\n🔧 `var_export($v, true)` 는 **다시 PHP 코드로 붙여 넣을 수 있는 형태**라 테스트 기대값을 만들 때 좋습니다."),
      I("널","값이 '없음' 을 나타내는 PHP 의 특수 값은? (소문자 영문)",
        ["null","NULL"],
        "정답은 `null` 입니다.\n💡 PHP 에서 '없음' 은 세 가지로 갈립니다: 변수 자체가 **없음**(undefined, 경고 발생), 값이 **null**, 값이 **빈 문자열**. `isset()` 은 앞의 둘을 함께 false 로 만들고, `array_key_exists()` 는 키의 존재만 봅니다 — 이 차이가 설정 병합에서 버그를 만듭니다."),
      C("타입 선언","<code>declare(strict_types=1);</code> 을 파일 맨 위에 두면 달라지는 것은?",
        ["모든 변수에 타입 선언이 강제된다",
         "함수의 인자·반환 타입이 자동 변환 없이 정확히 일치해야 한다",
         "런타임 속도가 빨라진다",
         "배열이 불변이 된다"],1,
        "엄격 모드에서는 `int` 를 요구하는 자리에 `\"5\"` 를 넘기면 **조용히 5 로 바뀌지 않고 `TypeError`** 가 납니다.\n🎯 핵심은 '더 엄격해서 좋다' 가 아니라, **잘못된 입력이 시스템 깊숙이 들어가기 전에 멈춘다**는 것입니다. 느슨한 모드에서는 `\"3 apples\"` 가 3 이 되어 계산까지 성공해 버립니다.\n⚠️ 선언은 **파일 단위**이고 첫 문장이어야 합니다. 호출하는 쪽 파일의 설정이 적용되므로, 라이브러리를 엄격 모드로 쓰려면 내 파일에 선언해야 합니다."),
    ] },
]},

/* ═══════════════ 유닛 2 ═══════════════ */
{ t:"타입과 비교 — PHP 를 가장 많이 오해하는 지점", l:[

  { t:"느슨한 타입과 형변환", xp:20,
    th:{ sum:"PHP 는 필요하면 타입을 **자동으로 바꾼다**. 어떤 규칙으로 바꾸는지 모르면 '분명히 맞는데 틀린' 코드가 나온다.",
      body:[
        {h:"숫자처럼 생긴 문자열", t:"`\"5\" + 1` 은 6 이다 — `+` 가 산술 전용이라 문자열을 숫자로 바꾼다. `\"5abc\"` 는 PHP 8 에서 **경고와 함께 5**, `\"abc\"` 는 **TypeError** 다(PHP 7 에서는 조용히 0 이었다). 이 변화가 PHP 8 의 가장 큰 안전 개선 중 하나다."},
        {h:"참과 거짓", t:"거짓으로 취급되는 값은 정해져 있다: `false`·`0`·`0.0`·`\"\"`·**`\"0\"`**·`[]`·`null`. 특히 **`\"0\"` 이 거짓**이고 `\"0.0\"`·`\"false\"`·`\" \"` 는 **참**이다. 그래서 `if ($_GET['page'])` 는 `page=0` 을 놓치고, 이때 필요한 것은 `isset()` 이나 `!== \"\"` 같은 **명시적 검사**다."}],
      code:{ c:"var_dump((bool)\"0\");    // false ← 함정\nvar_dump((bool)\"0.0\");  // true\nvar_dump((bool)\" \");    // true\nvar_dump((bool)[]);     // false", cap:"\"0\" 만 거짓인 문자열이다" },
      key:["+ 는 언제나 산술, 이어 붙이기는 .","PHP 8 은 숫자가 아닌 문자열 산술을 TypeError 로","거짓: false 0 0.0 \"\" \"0\" [] null","\"0\" 은 거짓, \"0.0\" 은 참"] },
    q:[
      C("형변환","<code>\"5\" + 1</code> 의 결과는?",
        ["\"51\"","6","오류","null"],1,
        "`+` 는 산술 전용이라 `\"5\"` 를 숫자로 바꿔 **6** 이 됩니다.\n💡 문자열을 잇고 싶었다면 `.` 를 써야 합니다: `\"5\" . 1` → `\"51\"`.",
        P("\"5\" + 1","6")),
      C("진리값","<code>if (\"0\")</code> 는 참인가 거짓인가?",
        ["참","거짓","오류","경고 후 참"],1,
        "**`\"0\"` 은 거짓입니다** — 빈 문자열이 아닌데도 거짓인 유일한 문자열입니다.\n⚠️ 이 규칙 때문에 `if ($row['count'])` 는 count 가 `\"0\"` 일 때 조용히 건너뜁니다. DB 에서 온 값은 대개 문자열이라 실제로 자주 터집니다.\n🔧 존재 확인이면 `isset()`, 빈 값 확인이면 `=== \"\"` 또는 `=== 0` 처럼 **의도를 명시**하세요.",
        P("(bool)\"0\"","false")),
      C("empty","<code>empty(\"0\")</code> 의 결과는?",
        ["true","false","오류","null"],0,
        "`empty()` 는 '거짓 같은 값' 을 전부 비었다고 보므로 `\"0\"` 도 **true** 입니다.\n⚠️ `empty($qty)` 로 수량을 검사하면 **0개 주문과 미입력을 구분하지 못합니다**. `empty` 의 유일한 장점은 없는 변수에도 경고를 내지 않는다는 것뿐이라, 대부분 `!isset()` 이나 명시적 비교로 대체하는 편이 낫습니다.",
        P("empty(\"0\")","true")),
      C("PHP 8 변화","PHP 8 에서 <code>\"abc\" + 1</code> 은?",
        ["1 (문자열은 0으로 변환)","TypeError","\"abc1\"","null"],1,
        "PHP 8 부터 **숫자가 아닌 문자열의 산술은 `TypeError`** 입니다. PHP 7 에서는 경고 후 조용히 0 으로 바뀌어 `1` 이 나왔습니다.\n🎯 이 변경 덕분에 '오타 난 필드명이 0 으로 계산되어 통계가 틀리는' 부류의 버그가 실행 시점에 드러납니다.\n💡 `\"5abc\" + 1` 은 **경고와 함께 6** 입니다 — 앞부분이 숫자면 그만큼만 씁니다."),
      I("함수","값이 <b>숫자로 해석 가능한지</b> 검사하는 PHP 내장 함수 이름은? (괄호 없이)",
        ["is_numeric"],
        "정답은 `is_numeric()` 입니다 — `\"12\"`·`\"1.5\"`·`\"1e3\"`·`\" 12\"`(앞 공백) 에 true 입니다.\n⚠️ PHP 8.0 부터 **뒤쪽 공백**(`\"12 \"`)도 true 가 되었습니다. 그리고 `is_numeric` 은 범위를 보지 않으므로, 정수 ID 검증에는 `filter_var($v, FILTER_VALIDATE_INT)` 가 더 정확합니다."),
      C("타입 저글링","<code>var_dump(\"abc\" == 0);</code> 의 결과가 <b>PHP 7 과 PHP 8 에서 다른</b> 이유는?",
        ["PHP 8 이 == 를 === 로 바꿨다",
         "PHP 8 은 숫자와 비숫자 문자열을 비교할 때 숫자를 문자열로 바꿔서 비교하기 때문",
         "PHP 8 이 문자열을 항상 참으로 취급하기 때문",
         "PHP 8 에서 == 가 삭제되었기 때문"],1,
        "PHP 7 은 **문자열을 숫자로** 바꿔 `\"abc\"→0` 이므로 `true` 였고, PHP 8 은 반대로 **숫자를 문자열로** 바꿔 `\"abc\" == \"0\"` 이 되어 `false` 입니다.\n🎯 이것이 PHP 8 의 '**Saner string to number comparisons**' 변경이며, `if ($password == 0)` 류의 인증 우회를 막았습니다.\n💡 여전히 `\"1\" == \"01\"` 은 **true** 입니다 — **양쪽 다 숫자 문자열**이면 숫자로 비교하기 때문입니다.",
        P("\"abc\" == 0","false")),
    ] },

  { t:"비교·switch·match", xp:20,
    th:{ sum:"`==` 는 타입을 맞춰 보고, `===` 는 **타입까지** 본다. 이 한 글자 차이가 인증·분기·검색에서 결과를 바꾼다.",
      body:[
        {h:"기본값은 ===", t:"실무의 기본값은 `===` 다. `==` 를 쓸 곳은 '타입이 달라도 같다고 봐야 하는' 이유를 설명할 수 있을 때뿐이다. 특히 `in_array($needle, $hay)` 는 **기본이 느슨한 비교**라 세 번째 인자에 `true` 를 주지 않으면 `0` 이 문자열 원소와 매칭될 수 있다 — `array_search`·`array_keys` 도 마찬가지다."},
        {h:"switch 와 match", t:"`switch` 는 `==` 로 비교하고 **fall-through** 가 있어 `break` 를 빠뜨리면 아래로 흘러간다. PHP 8 의 `match` 는 `===` 로 비교하고, 값을 **돌려주는 표현식**이며, 어디에도 걸리지 않으면 조용히 넘어가지 않고 `UnhandledMatchError` 를 던진다 — 즉 새 상태를 추가했을 때 **빠뜨린 곳이 드러난다**."}],
      code:{ c:"$label = match($status) {\n    'new', 'open' => '진행 중',\n    'done'        => '완료',\n};  // 없는 값 → UnhandledMatchError", cap:"침묵하지 않는 것이 match 의 핵심" },
      key:["기본값은 ===","in_array 는 세 번째 인자 true","switch 는 ==, match 는 ===","match 는 미처리 값을 예외로 알린다"] },
    q:[
      C("연산자","값과 <b>타입</b>을 함께 비교하는 연산자는?",
        ["==","===","=","<=>"],1,
        "`===` 는 **타입까지 같아야** 참입니다. `5 === \"5\"` 는 false 입니다.\n💡 `<=>` (spaceship) 는 비교가 아니라 **정렬용**입니다 — 작으면 −1, 같으면 0, 크면 1 을 돌려줍니다."),
      C("switch","<code>switch</code> 의 <code>case</code> 에서 <code>break</code> 를 빠뜨리면?",
        ["문법 오류가 난다","다음 case 의 코드까지 이어서 실행된다","switch 가 즉시 끝난다","default 로 점프한다"],1,
        "**fall-through** 로 아래 case 들이 이어서 실행됩니다.\n💡 일부러 쓰는 경우도 있어 PHP 는 경고하지 않습니다 — 그래서 조용히 틀립니다. `match` 는 이 문제 자체가 없습니다."),
      C("in_array","<code>in_array(0, ['a','b'])</code> 가 <b>PHP 8 에서</b> 돌려주는 값은?",
        ["true","false","null","오류"],1,
        "PHP 8 의 문자열↔숫자 비교 규칙 변경으로 **false** 입니다. PHP 7 에서는 `'a' == 0` 이 참이라 **true** 였습니다.\n⚠️ 그래도 `in_array(\"1e2\", [\"100\"])` 처럼 **둘 다 숫자 문자열**인 경우는 여전히 느슨하게 매칭됩니다.\n🔧 결론은 같습니다 — `in_array($x, $arr, true)` 로 **엄격 모드를 켜는 것이 기본값**이어야 합니다.",
        P("in_array(0, ['a','b'])","false")),
      C("match","<code>match</code> 가 <code>switch</code> 와 다른 점으로 <b>틀린</b> 것은?",
        ["=== 로 비교한다","값을 돌려주는 표현식이다","일치하는 것이 없으면 예외를 던진다","break 를 써서 빠져나와야 한다"],3,
        "`match` 는 **fall-through 자체가 없어** `break` 가 필요 없습니다.\n🎯 나머지 셋은 모두 맞고, 그중 가장 실무적인 이득은 세 번째입니다 — 상태를 새로 추가했을 때 `switch` 는 조용히 `default` 로 흘러가지만 `match` 는 **어디를 고쳐야 하는지 알려 줍니다**."),
      I("연산자 이름","<code>&lt;=&gt;</code> 연산자의 이름은? (영문, 한 단어)",
        ["spaceship","우주선","spaceship operator"],
        "정답은 `spaceship`(우주선 연산자)입니다.\n💡 `usort($a, fn($x,$y) => $x['age'] <=> $y['age'])` 처럼 정렬 비교 함수를 한 줄로 만듭니다. 배열끼리도 쓸 수 있어 `[$a1,$b1] <=> [$a2,$b2]` 로 **다중 키 정렬**을 표현할 수 있습니다."),
      C("널 병합","<code>$v = $arr['k'] ?? 'def';</code> 와 <code>$v = $arr['k'] ?: 'def';</code> 의 차이는?",
        ["차이가 없다",
         "?? 는 키가 없거나 null 일 때만 기본값을 쓰고, ?: 는 0·\"\"·\"0\" 같은 거짓 값에도 기본값을 쓴다",
         "?? 는 배열에만, ?: 는 객체에만 쓸 수 있다",
         "?: 가 더 엄격하다"],1,
        "`??` 는 **isset 기준**(없음·null)이고, `?:` 는 **truthy 기준**입니다.\n⚠️ 그래서 `$page = $_GET['page'] ?: 1;` 은 `page=0` 을 1 로 바꿔 버립니다. 사용자가 명시한 `0`·`\"\"` 를 지워야 하는 경우는 거의 없습니다.\n💡 `??` 는 없는 키에 **경고도 내지 않으므로** 배열 접근의 기본형으로 쓰기 좋습니다. 다만 `??` 로 감싸면 오타 난 키도 조용히 기본값이 되니, 필수 설정에는 오히려 `array_key_exists` + 예외가 낫습니다."),
    ] },
]},

/* ═══════════════ 유닛 3 ═══════════════ */
{ t:"문자열과 배열 — 매일 쓰는 두 가지", l:[

  { t:"문자열 다루기와 인코딩", xp:20,
    th:{ sum:"PHP 의 문자열은 **문자의 나열이 아니라 바이트의 나열**이다. 한글을 다루는 순간 이 사실이 전면에 나온다.",
      body:[
        {h:"바이트냐 문자냐", t:"`strlen(\"한\")` 은 UTF-8 에서 **3** 이다 — 한글 한 글자가 3바이트이기 때문이다. 글자 수가 필요하면 `mb_strlen`, 자르기는 `mb_substr` 을 써야 하고, `substr` 로 자르면 글자가 **반토막 나 깨진다**. 대소문자 변환도 `mb_strtolower` 여야 다국어에서 안전하다."},
        {h:"찾기와 바꾸기", t:"`strpos` 는 위치를 돌려주는데 **첫 글자면 0** 이라 `if (strpos(...))` 가 거짓이 된다 — PHP 8 의 `str_contains`·`str_starts_with`·`str_ends_with` 가 이 고전적 함정을 없앴다. 바꾸기는 `str_replace` 가 기본이고 정규식이 필요할 때만 `preg_replace` 를 쓴다 — 정규식은 느리고 읽기 어렵다."}],
      code:{ c:"if (str_contains($s, '@')) { … }   // PHP 8\n// 예전 방식은 이렇게 써야 했다\nif (strpos($s, '@') !== false) { … }", cap:"!== false 를 빼먹으면 위치 0 을 놓친다" },
      key:["strlen 은 바이트, mb_strlen 은 글자","자르기는 mb_substr","strpos 는 !== false 로 검사","PHP 8 은 str_contains 로 대체"] },
    q:[
      I("길이","문자열의 <b>바이트</b> 길이를 돌려주는 PHP 함수 이름은? (괄호 없이)",
        ["strlen"],
        "정답은 `strlen()` 입니다.\n💡 글자 수는 `mb_strlen($s, 'UTF-8')` 입니다. 비밀번호 최소 길이를 `strlen` 으로 재면 한글 4글자가 12자로 통과하는 반대 방향 버그도 생깁니다."),
      C("한글","UTF-8 에서 <code>strlen(\"한글\")</code> 의 값은?",
        ["2","4","6","오류"],2,
        "한글 한 글자가 UTF-8 에서 3바이트이므로 **6** 입니다.\n💡 `mb_strlen(\"한글\")` 이 2 입니다. DB 컬럼 길이(`VARCHAR(20)`)도 MySQL 은 **글자 수** 기준이라 여기서 또 한 번 어긋납니다.",
        P("strlen(\"한글\")","6")),
      C("포함 검사","PHP 8 에서 <b>부분 문자열 포함</b>을 검사하는 가장 알맞은 함수는?",
        ["strpos","str_contains","in_array","preg_match"],1,
        "`str_contains($haystack, $needle)` 이 **bool 을 직접 돌려줍니다**.\n⚠️ `strpos` 는 첫 글자에서 찾으면 `0` 을 돌려주는데 `0` 은 거짓이라, `if (strpos($s,'a'))` 가 **찾았는데도 거짓**이 됩니다. 이 함정을 없애려고 도입된 함수입니다.\n💡 `preg_match` 로도 되지만 정규식 엔진을 부르는 비용이 붙고, 사용자 입력을 그대로 넣으면 특수문자가 패턴으로 해석됩니다."),
      C("따옴표","<code>&lt;&lt;&lt;'EOT'</code> (nowdoc) 이 <code>&lt;&lt;&lt;EOT</code> (heredoc) 과 다른 점은?",
        ["여러 줄을 담지 못한다","변수를 해석하지 않는다","자동으로 이스케이프한다","PHP 8 에서 제거되었다"],1,
        "nowdoc 은 **작은따옴표처럼** 변수를 해석하지 않습니다.\n💡 SQL·정규식·JS 코드 조각처럼 `$` 가 그대로 필요한 문자열을 여러 줄로 담을 때 씁니다."),
      C("이스케이프","HTML 로 출력하기 직전 사용자 입력에 적용해야 하는 함수는?",
        ["strip_tags","htmlspecialchars","addslashes","urlencode"],1,
        "`htmlspecialchars($s, ENT_QUOTES, 'UTF-8')` 이 `< > & \" '` 를 엔티티로 바꿔 **XSS 를 막습니다**.\n⚠️ 디스트랙터 분석: `strip_tags` 는 태그를 지우지만 `onerror=` 같은 속성 맥락을 막지 못하고 데이터도 손실됩니다. `addslashes` 는 SQL 용(그것도 부적절)이고, `urlencode` 는 URL 맥락 전용입니다.\n🎯 원칙은 '**출력하는 맥락에 맞는 이스케이프를, 출력 직전에**' 입니다 — 입력 시점에 미리 변환해 저장하면 원본이 망가지고 맥락이 바뀔 때 대응할 수 없습니다."),
      C("정규식","<code>preg_match('/^\\d+$/', $s)</code> 로 정수를 검증할 때 남는 문제는?",
        ["숫자를 못 잡는다",
         "값의 범위(오버플로)와 앞의 0 을 걸러 주지 않는다",
         "정규식이 PHP 8 에서 동작하지 않는다",
         "항상 false 를 돌려준다"],1,
        "패턴은 **모양만** 봅니다 — `\"000123\"`·`\"99999999999999999999\"` 도 통과합니다.\n🔧 그래서 정수 검증에는 `filter_var($s, FILTER_VALIDATE_INT)` 가 낫습니다. 범위 옵션(`['options'=>['min_range'=>1]]`)까지 함께 걸 수 있고, 통과하면 **int 로 변환된 값**을 돌려줍니다.\n💡 정규식은 '형식' 을, filter_var 는 '값' 을 검증한다고 기억하면 선택이 쉬워집니다."),
    ] },

  { t:"배열은 순서 있는 맵이다", xp:20,
    th:{ sum:"PHP 의 배열은 리스트가 아니라 **순서를 기억하는 해시맵** 하나뿐이다. 리스트도, 딕셔너리도, 스택도 전부 이 하나로 표현한다.",
      body:[
        {h:"키는 int 아니면 string", t:"키는 정수와 문자열만 가능하고, `\"1\"` 같은 **정수처럼 생긴 문자열 키는 정수로 정규화**된다. `true`·`false`·`null` 을 키로 쓰면 각각 `1`·`0`·`\"\"` 가 된다. 그리고 값을 지워도 **다른 키의 번호는 다시 매겨지지 않는다** — `unset` 후 `[0,1,2]` 가 아니라 구멍이 남는다."},
        {h:"합치기의 두 방식", t:"`array_merge` 는 **정수 키를 다시 매기고** 문자열 키는 뒤가 앞을 덮는다. `+` 연산자는 반대로 **왼쪽 키를 지키고** 왼쪽에 없는 키만 채운다 — 그래서 기본값 채우기에는 `$opts + $defaults` 가 잘 맞는다. 이 둘을 바꿔 쓰면 인덱스가 어긋나거나 기본값이 사용자 값을 덮는다."}],
      code:{ c:"$a = ['x' => 1, 5 => 'v'];\n$b = ['x' => 2, 9 => 'w'];\nvar_dump($a + $b);        // x=>1, 5=>'v', 9=>'w'\nvar_dump(array_merge($a,$b)); // x=>2, 0=>'v', 1=>'w'", cap:"+ 는 왼쪽을, merge 는 오른쪽을 살린다" },
      key:["배열은 순서 있는 해시맵 하나뿐","\"1\" 키는 정수 1 로 정규화","unset 은 번호를 다시 매기지 않는다","array_merge 는 정수 키 재배치, + 는 왼쪽 우선"] },
    q:[
      C("생성","PHP 7 이상에서 배열을 만드는 짧은 문법은?",
        ["array(1,2,3) 만 가능","[1, 2, 3]","{1, 2, 3}","(1, 2, 3)"],1,
        "대괄호 `[1,2,3]` 이 표준입니다(PHP 5.4+).\n💡 `array()` 도 여전히 동작하지만 새 코드에서는 대괄호가 관례입니다.\n🎯 중요한 것은 문법이 아니라 **PHP 에는 배열이 하나뿐**이라는 사실입니다 — `[1,2,3]` 은 사실 `[0=>1, 1=>2, 2=>3]` 이고, 리스트와 딕셔너리를 구분하지 않습니다. 그래서 `json_encode` 가 같은 값을 어떤 때는 `[...]`, 어떤 때는 `{...}` 로 내보내는 일이 생깁니다."),
      C("키","PHP 배열의 키로 쓸 수 있는 타입은?",
        ["정수와 문자열","모든 타입","문자열만","객체만"],0,
        "**정수와 문자열**뿐입니다. `float` 키는 소수점이 **잘려** 정수가 되고(`1.7` → `1`), 배열·객체는 `TypeError` 입니다.\n💡 객체를 키로 쓰고 싶다면 `SplObjectStorage` 나 `spl_object_id()` 를 씁니다."),
      C("추가","배열 끝에 값을 붙이는 문법은?",
        ["$a->push(1)","$a[] = 1","$a + 1","array_push_end($a,1)"],1,
        "`$a[] = 1` 이 가장 짧고 빠릅니다.\n💡 `array_push($a, 1, 2)` 는 여러 개를 한 번에 넣을 때만 이득입니다 — 하나만 넣을 때는 함수 호출 비용만 더 듭니다."),
      C("unset","<code>$a = [10,20,30]; unset($a[1]);</code> 이후 <code>array_keys($a)</code> 는?",
        ["[0,1]","[0,2]","[0,1,2]","[]"],1,
        "**키는 다시 매겨지지 않습니다** — `[0, 2]` 가 남습니다.\n⚠️ 이 상태로 `json_encode` 하면 배열이 아니라 **객체 `{\"0\":10,\"2\":30}`** 로 나갑니다. 프런트엔드가 `.map()` 을 부르다 깨지는 전형적인 사고입니다.\n🔧 번호를 다시 매기려면 `array_values($a)` 를 거칩니다.",
        P("(function(){$a=[10,20,30]; unset($a[1]); return array_keys($a);})()","array (\n  0 => 0,\n  1 => 2,\n)")),
      I("함수","연관 배열에서 <b>값들만</b> 뽑아 0부터 다시 번호를 매긴 배열을 돌려주는 함수는? (괄호 없이)",
        ["array_values"],
        "정답은 `array_values()` 입니다.\n💡 `array_filter` 로 걸러 낸 뒤에는 키에 구멍이 남으므로, JSON 으로 내보내기 전에 거의 항상 `array_values` 가 필요합니다."),
      C("합치기","기본 설정 <code>$def</code> 를 사용자 설정 <code>$opt</code> 로 <b>덮되, 사용자가 지정하지 않은 키만 채우고</b> 싶다. 알맞은 것은?",
        ["array_merge($opt, $def)","$opt + $def","array_merge($def, $opt) 만 가능","$def + $opt"],1,
        "`+` 는 **왼쪽 키를 지키므로** `$opt + $def` 가 정확히 그 의미입니다.\n⚠️ 디스트랙터 분석: `array_merge($opt, $def)` 는 **기본값이 사용자 값을 덮어** 반대로 동작합니다. `array_merge($def, $opt)` 는 결과는 비슷해 보이지만 **정수 키를 전부 다시 매기므로** 목록형 설정이 망가집니다.\n💡 다만 `+` 는 얕게만 동작합니다 — 중첩 설정은 재귀 병합 함수를 직접 써야 하고, 그때 `null` 을 '설정하지 않음' 으로 볼지 정해야 합니다.",
        P("(function(){$opt=['a'=>1]; $def=['a'=>9,'b'=>2]; return $opt+$def;})()","array (\n  'a' => 1,\n  'b' => 2,\n)")),
    ] },

  { t:"배열 함수와 정렬", xp:20,
    th:{ sum:"배열 작업은 **직접 반복문을 돌리는 대신 이름 붙은 함수**로 표현할수록 읽기 쉬워진다 — 다만 각 함수가 키를 어떻게 다루는지 알아야 한다.",
      body:[
        {h:"map · filter · reduce", t:"`array_map` 은 키를 **보존**하지만 배열을 여러 개 넘기면 키를 버린다. `array_filter` 는 키를 **보존**해 구멍이 남고, 콜백을 생략하면 **truthy 검사**라 `0` 과 `\"0\"` 까지 지운다 — 그래서 `fn($v) => $v !== null` 처럼 명시하는 편이 안전하다. `array_reduce` 는 초기값을 반드시 주는 것이 좋다."},
        {h:"정렬 함수의 이름 규칙", t:"`sort`(값, 키 재배치) · `asort`(값, 키 유지) · `ksort`(키) 로 나뉘고, 앞에 `r` 이 붙으면 역순, `u` 가 붙으면 사용자 비교 함수다. **정렬 함수는 원본을 참조로 바꾸고 bool 을 돌려준다** — `$sorted = sort($a)` 라고 쓰면 `$sorted` 에 `true` 가 들어간다. PHP 8.0 부터 모든 정렬이 **안정 정렬**이라 동점의 원래 순서가 유지된다."}],
      code:{ c:"usort($rows, fn($a, $b) => $b['score'] <=> $a['score']);\n// 원본이 바뀐다. 반환값은 true/false 일 뿐이다.", cap:"$x = usort(...) 는 거의 항상 버그" },
      key:["array_filter 는 키를 보존한다","콜백 없는 filter 는 0 도 지운다","정렬 함수는 참조로 원본을 바꾼다","PHP 8 의 정렬은 안정 정렬"] },
    q:[
      C("map","<code>array_map</code> 이 하는 일은?",
        ["조건에 맞는 원소만 남긴다","각 원소를 변환해 새 배열을 만든다","배열을 하나의 값으로 접는다","배열을 정렬한다"],1,
        "각 원소에 콜백을 적용해 **새 배열**을 만듭니다. 원본은 그대로입니다.\n💡 배열을 두 개 넘기면 짝지어 처리하는데, 이때는 **키가 버려지고 0부터 다시** 매겨집니다."),
      C("filter","<code>array_filter([0, 1, '', 'a'])</code> 의 결과 원소는?",
        ["[0,1,'','a']","[1,'a']","['a']","[]"],1,
        "콜백을 생략하면 **truthy 검사**라 `0` 과 `''` 가 사라져 `[1, 'a']` 만 남습니다.\n⚠️ 그리고 **키는 보존**되므로 결과의 키는 `1` 과 `3` 입니다 — `json_encode` 하면 객체로 나갑니다.\n🔧 `array_values(array_filter($a, fn($v) => $v !== null))` 처럼 **조건과 재색인을 둘 다 명시**하는 것이 안전합니다.",
        P("array_values(array_filter([0,1,'','a']))","array (\n  0 => 1,\n  1 => 'a',\n)")),
      C("정렬 반환","<code>$sorted = sort($arr);</code> 에서 <code>$sorted</code> 에 담기는 것은?",
        ["정렬된 배열","true 또는 false","원본 배열의 복사본","null"],1,
        "PHP 의 정렬 함수는 **원본을 참조로 정렬하고 성공 여부(bool)** 를 돌려줍니다.\n⚠️ `$sorted` 를 배열처럼 쓰면 `true[0]` 이 되어 조용히 null 이 나옵니다. `sort($arr);` 를 부른 뒤 `$arr` 를 쓰는 것이 맞습니다."),
      C("키 유지","<b>값 기준으로 정렬하되 키를 유지</b>하려면?",
        ["sort","asort","ksort","rsort"],1,
        "`asort` 가 값으로 정렬하면서 **키를 유지**합니다.\n💡 이름 규칙: `a`=associative(키 유지) · `k`=key(키 기준) · `r`=reverse · `u`=user(비교 함수). 조합해서 `uasort`(사용자 비교 + 키 유지), `krsort`(키 역순)처럼 씁니다."),
      I("함수","배열을 하나의 값으로 접어(누적해) 계산하는 함수 이름은? (괄호 없이)",
        ["array_reduce"],
        "정답은 `array_reduce()` 입니다.\n💡 `array_reduce($a, fn($c,$v)=>$c+$v, 0)` 처럼 **초기값을 반드시 주세요** — 생략하면 빈 배열에서 `null` 이 나와 뒤에서 터집니다."),
      C("안정 정렬","PHP 8.0 부터 모든 정렬 함수가 <b>안정 정렬</b>이 되었다는 것의 실무적 의미는?",
        ["정렬이 더 빨라진다",
         "비교 결과가 같은 원소들의 원래 순서가 유지되어, 여러 번 나눠 정렬하는 방식이 성립한다",
         "정렬 중 예외가 나지 않는다",
         "메모리를 덜 쓴다"],1,
        "동점 원소의 **원래 순서가 보존**되므로, '먼저 이름으로 정렬하고 그다음 점수로 정렬' 하면 **점수 같으면 이름 순** 이라는 결과가 자연스럽게 나옵니다.\n⚠️ PHP 7 에서는 이 방식이 실행할 때마다 다른 결과를 낼 수 있어, 페이지네이션에서 같은 행이 두 페이지에 나오거나 사라졌습니다.\n💡 그래도 **정렬 키에 유일한 tie-break(예: id)를 넣는 습관**은 유지하는 것이 좋습니다 — DB 정렬은 여전히 안정성을 보장하지 않습니다."),
    ] },
]},

/* ═══════════════ 유닛 4 ═══════════════ */
{ t:"함수와 스코프", l:[

  { t:"함수 · 인자 · 반환", xp:20,
    th:{ sum:"PHP 의 함수는 **기본적으로 값을 복사해 받는다**. 그 위에 기본값·가변 인자·이름 있는 인자·타입 선언이 얹혀 있다.",
      body:[
        {h:"넘기는 방식", t:"스칼라와 배열은 **값**으로 넘어간다(내부적으로는 copy-on-write 라 쓰기 전까지 복사하지 않는다). 객체는 **핸들**이 복사되므로 함수 안에서 프로퍼티를 바꾸면 바깥에도 반영된다 — '객체는 참조로 넘어간다' 는 흔한 오해이고, 정확히는 '**참조를 값으로**' 넘긴다. `&$x` 로 진짜 참조를 만들 수 있지만, 흐름을 감추므로 꼭 필요할 때만 쓴다."},
        {h:"인자를 읽기 쉽게", t:"`...$rest` 로 가변 인자를 받고, 호출부에서 `f(...$arr)` 로 펼친다. PHP 8 의 **이름 있는 인자**(`f(limit: 10)`)는 `f($x, null, null, true)` 같은 불투명한 호출을 없애 준다 — 다만 **인자 이름이 공개 API 가 된다**는 뜻이라, 라이브러리에서는 이름 변경이 곧 파괴적 변경이다."}],
      code:{ c:"function make(string $name, int $qty = 1, bool $urgent = false): array {\n    return compact('name', 'qty', 'urgent');\n}\nmake('볼트', urgent: true);   // qty 는 기본값 유지", cap:"이름 있는 인자로 순서를 건너뛴다" },
      key:["스칼라·배열은 값, 객체는 핸들 복사","기본값 인자는 뒤쪽에 둔다","...$rest 로 받고 ...$arr 로 펼친다","이름 있는 인자는 공개 API 가 된다"] },
    q:[
      C("정의","PHP 함수를 정의하는 키워드는?",
        ["func","function","def","fn"],1,
        "`function` 입니다.\n💡 `fn` 은 PHP 7.4 의 **화살표 함수**로, 한 줄 표현식만 담고 바깥 변수를 자동으로 캡처합니다: `fn($x) => $x * 2`."),
      C("기본값","기본값이 있는 인자는 어디에 두어야 하는가?",
        ["맨 앞","가운데","맨 뒤","아무 데나"],2,
        "기본값 인자는 **뒤쪽**에 둡니다 — 앞에 두면 뒤 인자를 넘기기 위해 앞에도 값을 줘야 해서 기본값이 의미를 잃습니다.\n💡 PHP 8 의 이름 있는 인자를 쓰면 순서를 건너뛸 수 있지만, 8.1 부터 **필수 인자 앞의 선택 인자는 deprecated** 입니다."),
      C("가변 인자","임의 개수의 인자를 배열로 받는 문법은?",
        ["func_get_args() 만 가능","function f(...$args)","function f(array &$args)","function f($args[])"],1,
        "`...$args` (**variadic**) 가 표준입니다 — 타입도 붙일 수 있습니다: `function f(int ...$n)`.\n💡 `func_get_args()` 도 동작하지만 시그니처에 드러나지 않아 IDE·정적 분석이 도와주지 못합니다."),
      C("객체 전달","함수에 <b>객체</b>를 넘기고 함수 안에서 프로퍼티를 바꾸면?",
        ["바깥 객체는 그대로다","바깥 객체도 바뀐다","오류가 난다","복사본이 반환된다"],1,
        "객체는 **핸들이 복사**되므로 가리키는 대상이 같아 **바깥에도 반영**됩니다.\n💡 다만 함수 안에서 `$obj = new Other();` 라고 **재할당**하면 바깥에는 영향이 없습니다 — 핸들 자체는 값으로 넘어갔기 때문입니다. 이것이 '참조로 넘어간다' 는 표현이 부정확한 이유입니다.\n🔧 바깥이 바뀌지 않게 하려면 `clone` 하거나, 애초에 **불변 객체(readonly)** 로 설계합니다."),
      I("연산자","배열을 함수 인자로 <b>펼쳐서</b> 넘길 때 쓰는 연산자는? (기호 3글자)",
        ["...","spread"],
        "정답은 `...` (spread) 입니다: `f(...$args)`.\n💡 PHP 8.1 부터 **문자열 키 배열도 펼칠 수 있어** 이름 있는 인자로 매핑됩니다: `f(...['limit' => 10])`."),
      C("반환 타입","<code>function f(): never</code> 의 의미는?",
        ["아무것도 반환하지 않는다(void 와 같다)",
         "정상적으로 반환되지 않는다 — 반드시 예외를 던지거나 종료한다",
         "null 만 반환한다",
         "재귀 함수 전용이다"],1,
        "PHP 8.1 의 `never` 는 **함수가 절대 정상 반환하지 않는다**는 뜻입니다(`throw` 또는 `exit`).\n🎯 `void` 와의 차이가 핵심입니다 — `void` 는 반환은 하되 값이 없는 것이고, `never` 는 **그 뒤 코드가 실행되지 않음**을 타입으로 선언합니다. 정적 분석기가 `redirect()` 뒤의 코드를 도달 불가로 판정해 주므로, `return redirect();` 를 빠뜨린 버그를 잡아 줍니다."),
    ] },

  { t:"스코프와 클로저", xp:20,
    th:{ sum:"PHP 의 함수는 **바깥 변수를 자동으로 보지 못한다**. 이 '닫힌' 기본값이 다른 언어에서 온 사람을 가장 많이 놀라게 한다.",
      body:[
        {h:"전역은 보이지 않는다", t:"함수 안에서 전역 변수를 그냥 쓸 수 없다 — `global` 키워드나 `$GLOBALS` 가 필요한데, 둘 다 **의존이 시그니처에 드러나지 않아** 테스트와 추적을 어렵게 한다. 필요한 값은 **인자로 받는 것**이 정답이다. `static` 지역 변수는 호출 사이에 값을 유지하지만, 요청이 끝나면 함께 사라진다."},
        {h:"클로저가 잡는 것", t:"`function() use ($x)` 는 그 시점의 **값을 복사**하고, `use (&$x)` 는 참조를 잡는다. 화살표 함수 `fn() => $x` 는 필요한 변수를 **자동으로 값 캡처**한다(참조 캡처는 불가). 클로저 안의 `$this` 는 정의된 클래스의 인스턴스에 자동으로 묶이며, `Closure::bind` 로 다른 객체에 다시 묶을 수도 있다."}],
      code:{ c:"$n = 1;\n$f = function() use ($n) { return $n; };\n$g = fn() => $n;\n$n = 99;\necho $f(), $g();   // 1, 1 ← 둘 다 정의 시점의 값", cap:"값 캡처는 '그때 그 값' 을 얼려 둔다" },
      key:["함수는 전역 변수를 자동으로 보지 않는다","global 대신 인자로 넘긴다","use 는 값 복사, use (&$x) 는 참조","fn 은 자동 값 캡처, 참조 캡처 불가"] },
    q:[
      C("스코프","함수 안에서 함수 <b>바깥</b>의 일반 변수를 그냥 쓰면?",
        ["자동으로 보인다","보이지 않는다(정의되지 않은 변수)","오류로 실행이 중단된다","null 이 아니라 0 이 된다"],1,
        "PHP 함수는 **자기만의 스코프**를 가져 바깥 변수를 보지 못합니다 — 접근하면 '정의되지 않은 변수' 경고와 함께 `null` 입니다.\n💡 JavaScript·Python 과 반대라 자주 걸립니다. 대신 이 성질 덕분에 **함수의 입력이 인자로 전부 드러납니다**."),
      C("클로저","<code>function() use ($x) {}</code> 에서 <code>$x</code> 는 어떻게 잡히는가?",
        ["참조로","정의 시점의 값이 복사되어","호출 시점에 다시 읽혀","전역에서 찾아"],1,
        "`use ($x)` 는 **클로저를 정의하는 순간의 값**을 복사합니다. 이후 `$x` 가 바뀌어도 클로저 안의 값은 그대로입니다.\n💡 최신 값을 따라가야 한다면 `use (&$x)` 로 참조를 잡습니다 — 다만 반복문 안에서 참조를 잡으면 **모든 클로저가 마지막 값을 보는** 고전적 버그가 생깁니다.",
        P("(function(){$x=1; $f=function() use ($x){ return $x; }; $x=99; return $f();})()","1")),
      C("화살표 함수","<code>fn($v) => $v * $rate</code> 에서 <code>$rate</code> 는?",
        ["자동으로 값 캡처된다","use 로 선언해야 한다","항상 null 이다","참조로 캡처된다"],0,
        "화살표 함수는 쓰인 바깥 변수를 **자동으로 값 캡처**합니다 — `use` 가 필요 없습니다.\n⚠️ 참조 캡처는 **불가능**하고, 본문은 **표현식 하나**만 담을 수 있습니다. 여러 문장이 필요하면 일반 클로저를 써야 합니다."),
      C("static","함수 안의 <code>static $n = 0;</code> 가 하는 일은?",
        ["전역 변수를 만든다","같은 요청 안에서 호출 사이에 값이 유지된다","상수를 만든다","다음 요청까지 값이 남는다"],1,
        "`static` 지역 변수는 **호출 사이에 값을 유지**합니다 — 초기화는 첫 호출 때 한 번뿐입니다.\n⚠️ 다만 PHP 는 요청마다 새로 시작하므로 **다음 요청에는 남지 않습니다**. 요청 간 캐시로 착각하면 안 됩니다.\n💡 테스트에서 상태가 새는 원인이 되기도 합니다 — 같은 프로세스에서 여러 테스트를 돌리면 값이 이어집니다."),
      I("키워드","함수 안에서 전역 변수를 쓰겠다고 선언하는 키워드는? (소문자)",
        ["global"],
        "정답은 `global` 입니다.\n⚠️ 다만 실무에서는 **피해야 할 기능**입니다 — 함수가 무엇에 의존하는지 시그니처에 드러나지 않아 테스트에서 격리할 수 없고, 어디서 값이 바뀌는지 추적할 수 없습니다. 필요한 값은 인자로 받으세요."),
      C("first-class callable","PHP 8.1 의 <code>$f = strlen(...);</code> 문법이 <code>$f = 'strlen';</code> 보다 나은 이유는?",
        ["더 빠르다",
         "이름이 아니라 실제 호출 가능 객체를 만들어, 오타·이름 변경을 즉시 잡고 스코프·정적 분석이 작동한다",
         "문자열보다 메모리를 덜 쓴다",
         "private 메서드도 호출할 수 있다"],1,
        "`strlen(...)` 은 **`Closure` 를 만듭니다** — 존재하지 않는 함수면 그 자리에서 오류가 나고, IDE·정적 분석기가 시그니처를 추적할 수 있습니다.\n⚠️ 디스트랙터 분석: 문자열 콜러블은 오타가 나도 **호출하는 순간까지 조용하고**, `$this->method(...)` 처럼 바인딩이 필요한 경우 `[$obj,'m']` 배열 문법으로 부풀어야 했습니다. private 접근 규칙은 그대로입니다."),
    ] },
]},

/* ═══════════════ 유닛 5 ═══════════════ */
{ t:"객체지향 — 구조를 만드는 도구", l:[

  { t:"클래스 · 가시성 · 생성자", xp:25,
    th:{ sum:"클래스는 **데이터와 그 데이터를 다루는 규칙을 한 덩어리로 묶는다**. 가시성은 '바깥에서 무엇을 믿고 써도 되는가' 를 선언하는 계약이다.",
      body:[
        {h:"세 가지 가시성", t:"`public` 은 누구나, `protected` 는 자신과 자식, `private` 은 자신만 접근한다. 기본값은 **가장 좁게(private)** 두고 필요할 때 여는 것이 원칙이다 — 한 번 public 으로 열면 바깥이 의존하기 시작해 되돌리기 어렵다. PHP 는 `private` 을 **선언한 클래스 기준**으로 판단하므로, 같은 클래스의 다른 인스턴스의 private 에는 접근할 수 있다."},
        {h:"생성자를 짧게", t:"PHP 8 의 **생성자 프로퍼티 승격**은 `public function __construct(private string $name) {}` 한 줄로 선언·할당을 끝낸다. 8.1 의 `readonly` 를 붙이면 **생성 후 변경 불가**가 되어, 값 객체를 '만들 때만 조립하고 이후엔 믿을 수 있는' 것으로 만든다. 변경이 필요하면 `with…()` 로 **새 객체를 만들어** 돌려준다."}],
      code:{ c:"final class Money {\n    public function __construct(\n        public readonly int $cents,\n        public readonly string $cur = 'KRW',\n    ) {}\n    public function plus(Money $o): static {\n        return new static($this->cents + $o->cents, $this->cur);\n    }\n}", cap:"불변 값 객체 — 바뀌지 않으니 공유해도 안전하다" },
      key:["가시성은 좁게 시작해서 필요할 때 연다","생성자 프로퍼티 승격으로 보일러플레이트 제거","readonly 는 생성 후 변경 불가","변경 대신 새 객체를 돌려준다"] },
    q:[
      C("인스턴스","클래스로부터 객체를 만드는 키워드는?",
        ["create","new","make","instance"],1,
        "`new ClassName()` 입니다.\n💡 PHP 8.4 부터는 `new Foo()->bar()` 처럼 **괄호로 감싸지 않고** 바로 메서드를 부를 수 있습니다."),
      C("접근","클래스 안에서 자기 프로퍼티에 접근하는 문법은?",
        ["self.prop","$this->prop","this.prop","$self::prop"],1,
        "`$this->prop` 입니다(화살표, `$` 는 앞에만).\n💡 `$this->$name` 처럼 쓰면 **변수 이름으로 프로퍼티를 동적 접근**합니다 — 헷갈리기 쉬우니 필요할 때만."),
      C("가시성","<code>private</code> 프로퍼티에 접근할 수 있는 곳은?",
        ["어디서나","같은 클래스 안에서만","자식 클래스까지","같은 네임스페이스 안"],1,
        "**선언한 클래스 안에서만** 접근할 수 있습니다. 자식까지 열려면 `protected` 입니다.\n💡 재미있는 예외: `private` 은 **클래스 기준**이라 같은 클래스의 *다른 인스턴스* 의 private 에는 접근할 수 있습니다 — `equals(self $other)` 안에서 `$other->secret` 이 됩니다."),
      C("상속","<code>final</code> 을 클래스 앞에 붙이면?",
        ["인스턴스를 만들 수 없다","상속할 수 없다","프로퍼티를 바꿀 수 없다","네임스페이스를 벗어날 수 없다"],1,
        "`final class` 는 **상속을 금지**합니다(메서드에 붙이면 그 메서드의 오버라이드 금지).\n🎯 '확장 금지' 가 아니라 '**상속으로 확장하지 말고 조합으로 하라**' 는 신호입니다. 상속을 열어 두면 자식이 내부 구현에 의존하게 되어 리팩터링이 파괴적 변경이 됩니다."),
      I("키워드","생성자 메서드의 이름은? (앞의 밑줄 두 개 포함)",
        ["__construct"],
        "정답은 `__construct` 입니다.\n💡 소멸자는 `__destruct` 이지만, PHP 는 요청이 끝나면 어차피 전부 정리하므로 **파일 핸들·락 해제** 정도에만 씁니다. 소멸자 안에서 예외를 던지면 처리하기 곤란하니 피합니다."),
      C("readonly","<code>readonly</code> 프로퍼티에 대해 옳은 것은?",
        ["선언과 동시에만 값을 줄 수 있다",
         "선언된 클래스의 스코프 안에서 한 번만 초기화할 수 있고 이후 변경하면 Error 가 난다",
         "정적 프로퍼티에도 붙일 수 있다",
         "값이 배열이면 내부 원소도 바꿀 수 없다"],1,
        "`readonly` 는 **선언 클래스 스코프에서 한 번 초기화**를 허용하고, 이후 쓰기는 `Error` 입니다.\n⚠️ 디스트랙터 분석: 선언 시점 기본값은 오히려 **불가능**하고(초기화되지 않은 상태여야 함), 정적 프로퍼티에는 **붙일 수 없으며**, 배열이 담겼을 때 그 **내부는 얼지 않습니다**(재할당만 막힙니다).\n💡 PHP 8.2 의 `readonly class` 는 모든 프로퍼티에 한 번에 적용합니다."),
    ] },

  { t:"인터페이스 · 추상 · 트레이트", xp:25,
    th:{ sum:"세 가지는 목적이 다르다 — **인터페이스는 계약**, **추상 클래스는 뼈대**, **트레이트는 코드 붙여넣기**다.",
      body:[
        {h:"계약과 뼈대", t:"인터페이스는 '무엇을 할 수 있는가' 만 정하고 구현을 담지 않는다(상수와 시그니처만). 클래스는 인터페이스를 **여러 개** 구현할 수 있지만 클래스 상속은 **하나**뿐이다. 추상 클래스는 공통 구현 + 자식이 채울 구멍(`abstract` 메서드)을 함께 가진다. 타입 힌트는 **구현체가 아니라 인터페이스**에 걸어야 나중에 교체할 수 있다."},
        {h:"트레이트는 상속이 아니다", t:"트레이트는 컴파일 시점에 클래스 본문에 **그대로 복사**된다 — 타입이 아니므로 `instanceof` 로 잡을 수 없다. 같은 이름의 메서드가 충돌하면 `insteadof`·`as` 로 직접 골라야 한다. 우선순위는 **클래스 본문 > 트레이트 > 부모 클래스** 순이다. 상태(프로퍼티)를 가진 트레이트는 재사용이 아니라 **숨은 결합**이 되기 쉽다."}],
      code:{ c:"interface Clock { public function now(): int; }\n\nfinal class FrozenClock implements Clock {\n    public function __construct(private int $t) {}\n    public function now(): int { return $this->t; }\n}\n// 테스트에서 시간을 고정할 수 있게 된다", cap:"인터페이스에 의존하면 교체가 가능해진다" },
      key:["인터페이스는 여러 개, 클래스 상속은 하나","타입 힌트는 인터페이스에 건다","트레이트는 타입이 아니다","충돌은 insteadof·as 로 명시 해결"] },
    q:[
      C("구현","클래스가 인터페이스를 구현할 때 쓰는 키워드는?",
        ["extends","implements","uses","inherits"],1,
        "`class A implements B` 입니다. 클래스 상속은 `extends`, 트레이트 사용은 `use` 입니다.\n💡 인터페이스끼리는 `extends` 로 상속하고, 이때는 **여러 개를 동시에** 상속할 수 있습니다(`interface C extends A, B`). 클래스 상속만 하나로 제한됩니다."),
      C("개수","한 클래스가 <b>구현할 수 있는 인터페이스</b>와 <b>상속할 수 있는 클래스</b>의 개수는?",
        ["둘 다 하나","인터페이스 여러 개, 클래스 하나","둘 다 여러 개","인터페이스 하나, 클래스 여러 개"],1,
        "인터페이스는 **여러 개**, 클래스 상속은 **하나**입니다.\n💡 이 비대칭이 '상속보다 조합' 원칙의 실무적 근거입니다 — 상속 슬롯은 하나뿐이라 아껴 써야 합니다."),
      C("추상","<code>abstract</code> 클래스에 대해 옳은 것은?",
        ["인스턴스를 만들 수 없고, 구현이 있는 메서드도 가질 수 있다",
         "메서드 구현을 전혀 담을 수 없다",
         "인스턴스를 만들 수 있다",
         "프로퍼티를 가질 수 없다"],0,
        "추상 클래스는 **직접 인스턴스화할 수 없고**, 구현된 메서드와 프로퍼티를 함께 가질 수 있습니다.\n💡 인터페이스와의 선택 기준: 공유할 **구현**이 있으면 추상 클래스, **계약만** 정하면 인터페이스입니다. 다만 추상 클래스를 쓰면 상속 슬롯 하나를 소모합니다."),
      C("트레이트","트레이트에 대해 <b>틀린</b> 설명은?",
        ["메서드 구현을 담을 수 있다","프로퍼티를 담을 수 있다","instanceof 로 검사할 수 있다","여러 개를 한 클래스에 붙일 수 있다"],2,
        "트레이트는 **타입이 아니라 코드 조각**이라 `instanceof` 로 검사할 수 없습니다.\n💡 그래서 트레이트로 기능을 붙였다면, 타입으로 다뤄야 하는 부분은 **인터페이스를 함께 구현**하는 것이 정석입니다 — `class A implements Jsonable { use JsonableTrait; }`."),
      I("키워드","인터페이스에서 <b>구현 없이 시그니처만</b> 적는 것처럼, 추상 클래스에서 자식이 반드시 구현해야 하는 메서드에 붙이는 키워드는? (소문자)",
        ["abstract"],
        "정답은 `abstract` 입니다.\n💡 `abstract` 메서드가 하나라도 있으면 클래스도 `abstract` 여야 합니다. 자식이 구현할 때는 **시그니처가 호환**되어야 하며, 인자 타입은 넓히고 반환 타입은 좁히는 것만 허용됩니다(반공변·공변).\n⚠️ `abstract` 와 `final` 을 함께 붙이면 '상속해야 하는데 상속할 수 없는' 모순이라 문법 오류입니다."),
      C("충돌","두 트레이트가 같은 이름의 메서드를 가질 때 PHP 는?",
        ["나중에 use 한 쪽이 이긴다","먼저 use 한 쪽이 이긴다","치명적 오류를 내고, insteadof 로 직접 골라야 한다","자동으로 둘 다 실행한다"],2,
        "PHP 는 **암묵적으로 고르지 않고 오류**를 냅니다 — `A::m insteadof B;` 로 명시하고, 필요하면 `B::m as bM;` 로 다른 이름을 붙여 둘 다 살릴 수 있습니다.\n🎯 다중 상속의 '죽음의 다이아몬드' 를 **컴파일 시점 에러**로 밀어낸 설계입니다. 조용히 하나를 고르는 언어보다 낫습니다.\n💡 우선순위는 **클래스 본문 > 트레이트 > 부모 클래스** 이므로, 클래스에 같은 이름 메서드를 직접 정의하면 트레이트 것이 조용히 무시됩니다."),
    ] },

  { t:"정적 · 매직 메서드 · 복사", xp:25,
    th:{ sum:"편리해 보이는 기능일수록 **추적 가능성**을 대가로 요구한다 — static, 매직 메서드, 객체 복사가 대표적이다.",
      body:[
        {h:"self 와 static", t:"`self::` 는 **쓰인 곳의 클래스**로 고정되고, `static::` 는 **실제 호출된 클래스**를 따라간다(늦은 정적 바인딩). 부모에 `new self()` 를 두면 자식에서 불러도 부모가 만들어지므로, 팩토리에는 `new static()` 이 맞다. 그리고 정적 메서드는 **전역 함수와 다를 바 없어** 테스트에서 교체할 수 없다 — 의존성 주입을 쓰지 못하게 만드는 주범이다."},
        {h:"매직과 복사", t:"`__get`·`__call` 은 없는 프로퍼티·메서드 접근을 가로채는데, **오타가 런타임까지 숨고** IDE 자동완성이 죽는다. `__toString` 처럼 계약이 분명한 것만 쓰는 편이 낫다. 객체 복사(`clone`)는 **얕은 복사**라 안에 든 객체는 공유된다 — 깊은 복사가 필요하면 `__clone()` 에서 내부 객체를 다시 복제해야 한다."}],
      code:{ c:"public function __clone(): void {\n    $this->createdAt = clone $this->createdAt;   // 얕은 복사 보정\n}", cap:"clone 은 프로퍼티 값을 복사할 뿐이다" },
      key:["self 는 고정, static 은 호출 클래스를 따라간다","팩토리에는 new static()","매직 메서드는 오타를 런타임까지 숨긴다","clone 은 얕은 복사"] },
    q:[
      C("정적 호출","정적 메서드를 부르는 문법은?",
        ["Cls->m()","Cls::m()","Cls.m()","$Cls->m()"],1,
        "`ClassName::method()` 입니다(`::` 는 scope resolution operator).\n💡 상수도 `ClassName::CONST` 로 접근합니다."),
      C("상수","클래스 상수를 선언하는 키워드는?",
        ["define","const","static final","readonly"],1,
        "`const MAX = 10;` 입니다. PHP 8.3 부터는 **타입을 붙일 수 있습니다**(`const int MAX = 10;`).\n💡 `define()` 은 전역 상수용이라 클래스 안에서는 쓰지 않습니다."),
      C("clone","<code>clone $obj</code> 가 하는 복사는?",
        ["깊은 복사","얕은 복사","복사하지 않고 같은 객체를 돌려준다","직렬화 후 역직렬화"],1,
        "**얕은 복사**입니다 — 프로퍼티 값이 복사되지만, 값이 객체면 **같은 객체를 함께 가리킵니다**.\n⚠️ 그래서 복제한 주문의 날짜를 바꿨더니 원본 날짜도 바뀌는 사고가 납니다. `__clone()` 에서 내부 객체를 `clone` 해 주어야 합니다.\n💡 `unserialize(serialize($obj))` 로 깊은 복사를 흉내 내는 방법이 있지만, 리소스·클로저가 들어 있으면 실패합니다."),
      C("늦은 정적 바인딩","부모 클래스의 <code>public static function make() { return new self(); }</code> 를 자식 <code>Child::make()</code> 로 부르면?",
        ["Child 인스턴스가 생긴다","부모 인스턴스가 생긴다","오류가 난다","null 이 반환된다"],1,
        "`self` 는 **쓰인 곳의 클래스**로 고정되므로 **부모** 인스턴스가 생깁니다.\n🔧 `new static()` 으로 바꾸면 실제 호출된 클래스(`Child`)를 따라갑니다 — 이것이 **늦은 정적 바인딩**입니다.\n💡 반환 타입도 `self` 대신 `static` 으로 적어야 자식에서 타입이 맞습니다.",
        P("(function(){ return [get_class(P5::make()), get_class(C5::make())]; })()",
          "array (\n  0 => 'P5',\n  1 => 'P5',\n)",
          "class P5 { public static function make() { return new self(); } }\nclass C5 extends P5 {}")),
      I("메서드","객체를 문자열 맥락에서 쓸 때 호출되는 매직 메서드 이름은? (밑줄 두 개 포함)",
        ["__toString"],
        "정답은 `__toString` 입니다.\n💡 PHP 8 부터 `__toString` 이 있으면 자동으로 `Stringable` 인터페이스를 구현한 것으로 취급됩니다 — 타입 힌트에 `string|Stringable` 을 쓸 수 있습니다."),
      C("매직 메서드 판단","<code>__get</code>·<code>__call</code> 을 남용했을 때 생기는 <b>가장 실질적인 비용</b>은?",
        ["실행 속도가 조금 느려진다",
         "프로퍼티·메서드가 코드에 존재하지 않아 오타·리팩터링 오류가 런타임까지 숨고, IDE·정적 분석이 무력해진다",
         "상속을 쓸 수 없게 된다",
         "직렬화가 불가능해진다"],1,
        "속도도 느려지지만(1번) 그건 부차적입니다. 진짜 비용은 **컴파일·분석 시점에 잡히던 오류가 런타임으로 밀리는 것**입니다.\n⚠️ `$user->emial` 오타가 `__get` 때문에 조용히 `null` 이 되어, 메일이 안 가는 원인을 며칠 뒤에 찾게 됩니다.\n🔧 꼭 필요하면 `@property` PHPDoc 을 함께 적어 정적 분석기에 알려 주고, 없는 이름은 `__get` 안에서 **예외를 던지도록** 만드세요."),
    ] },
]},

/* ═══════════════ 유닛 6 ═══════════════ */
{ t:"오류와 예외 — 실패를 다루는 법", l:[

  { t:"Throwable 계층", xp:25,
    th:{ sum:"PHP 7 부터 실패는 **`Throwable` 아래 두 갈래**로 나뉜다 — 프로그래머의 실수인 `Error`, 예상 가능한 실패인 `Exception`.",
      body:[
        {h:"Error 는 Exception 이 아니다", t:"`Error` 와 `Exception` 은 **둘 다 `Throwable` 을 구현할 뿐 상속 관계가 아니다**. 그래서 `catch (Exception $e)` 로는 `TypeError`·`DivisionByZeroError` 가 잡히지 않는다. 전부 잡으려면 `catch (Throwable $e)` 여야 한다 — 다만 `Error` 는 대개 **고쳐야 할 버그**라 잡아서 삼키면 문제를 숨긴다."},
        {h:"잡을 것과 넘길 것", t:"잡아서 처리할 수 있을 때만 잡는다. 로그만 남기고 다시 던지는 것은 정상이지만, **빈 catch 는 사고의 원인을 지운다**. `finally` 는 return·예외와 무관하게 실행되어 자원 해제에 쓰지만, `finally` 안의 `return` 은 **바깥 return 과 예외를 모두 덮어쓴다**. 예외를 다시 감쌀 때는 `previous` 인자로 **원인을 이어 붙여야** 스택 트레이스가 끊기지 않는다."}],
      code:{ c:"try {\n    $r = $repo->find($id);\n} catch (PDOException $e) {\n    throw new RepositoryError('조회 실패', previous: $e);  // 원인 보존\n} finally {\n    $lock->release();\n}", cap:"previous 를 빼면 진짜 원인이 사라진다" },
      key:["Error 와 Exception 은 형제, 공통 부모는 Throwable","catch (Exception) 은 TypeError 를 못 잡는다","빈 catch 는 원인을 지운다","finally 안의 return 은 모든 것을 덮는다"] },
    q:[
      C("문법","예외를 발생시키는 키워드는?",
        ["raise","throw","error","panic"],1,
        "`throw new Exception('메시지');` 입니다.\n💡 PHP 8 부터 `throw` 는 **표현식**이라 `$v = $x ?? throw new E();` 처럼 쓸 수 있습니다."),
      C("블록","예외를 잡는 구문은?",
        ["try / catch","try / rescue","begin / rescue","check / handle"],0,
        "`try { … } catch (SomeException $e) { … }` 입니다.\n💡 PHP 8 부터 변수를 안 쓸 거면 `catch (SomeException) {}` 처럼 **이름을 생략**할 수 있습니다."),
      C("계층","<code>Error</code> 와 <code>Exception</code> 의 관계는?",
        ["Error 가 Exception 을 상속한다",
         "Exception 이 Error 를 상속한다",
         "둘 다 Throwable 을 구현하는 별개의 계층이다",
         "같은 클래스의 다른 이름이다"],2,
        "**형제 관계**입니다 — 공통 부모는 인터페이스 `Throwable` 뿐입니다.\n⚠️ 그래서 `catch (Exception $e)` 로 감싼 코드에서 `TypeError` 가 나면 **잡히지 않고 그대로 치명적 오류**가 됩니다. PHP 5 시절 관용구를 그대로 쓰다 겪는 흔한 사고입니다.",
        P("[is_subclass_of('Error','Exception'), (new Error('x')) instanceof Throwable]",
          "array (\n  0 => false,\n  1 => true,\n)")),
      C("finally","<code>try</code> 안에서 <code>return</code> 했을 때 <code>finally</code> 블록은?",
        ["실행되지 않는다","return 값이 정해진 뒤 실행된다","예외가 있을 때만 실행된다","return 을 취소한다"],1,
        "`finally` 는 **반환값이 정해진 뒤에도 반드시 실행**됩니다.\n⚠️ 그런데 `finally` 안에서 `return` 하면 **try 의 반환값도, 던져지던 예외도 덮어씁니다** — 예외가 조용히 사라지는 가장 찾기 어려운 버그 중 하나라, `finally` 에는 return 을 두지 않는 것이 규칙입니다."),
      I("클래스","0 으로 나눌 때 PHP 8 이 던지는 예외 클래스 이름은? (정확한 이름)",
        ["DivisionByZeroError"],
        "정답은 `DivisionByZeroError` 입니다(`ArithmeticError` → `Error` 계열).\n💡 `intdiv(1, 0)` 과 `1 % 0` 도 같은 예외를 던집니다. 반면 `1 / 0` 은 PHP 7 에서는 **경고 후 INF** 였습니다 — PHP 8 이 값 대신 예외로 바꿨습니다."),
      C("예외 설계","커스텀 예외 계층을 만들 때 <b>가장 실용적인 기준</b>은?",
        ["예외마다 새 클래스를 만들어 최대한 세분화한다",
         "호출자가 서로 다르게 처리할 것들만 다른 클래스로 나누고, 나머지는 메시지·코드로 구분한다",
         "모든 예외를 Exception 하나로 쓴다",
         "예외 대신 false 를 반환한다"],1,
        "클래스를 나누는 유일한 이유는 **`catch` 를 다르게 쓰기 위해서**입니다. 처리 방식이 같다면 클래스를 나눠도 catch 절만 늘어납니다.\n⚠️ 디스트랙터 분석: 1번은 catch 절 폭발을 부르고, 3번은 '재시도할 것' 과 '즉시 실패할 것' 을 구분하지 못하며, 4번은 **실패 정보를 통째로 잃습니다**(`false` 하나로는 원인을 알 수 없고, `if` 를 빠뜨리면 조용히 진행됩니다).\n💡 실무 관례는 **패키지마다 표시 인터페이스 하나**(`interface PaymentError extends Throwable`)를 두고, 구체 예외들이 그것을 구현하게 하는 것입니다."),
    ] },

  { t:"오류 보고 · 로깅 · 종료", xp:25,
    th:{ sum:"오류는 '**나에게는 전부 보이고, 사용자에게는 아무것도 보이지 않아야**' 한다.",
      body:[
        {h:"보여줄 곳과 남길 곳", t:"운영 환경에서는 `display_errors=Off`, `log_errors=On` 이 기본이다 — 화면에 뜬 스택 트레이스는 경로·쿼리·자격증명을 그대로 노출한다. 개발 환경에서만 `display_errors=On`, `error_reporting=E_ALL` 로 둔다. **`@` 오류 억제 연산자는 쓰지 않는다** — 원인 추적을 불가능하게 만드는 대가로 아무것도 얻지 못한다."},
        {h:"마지막 방어선", t:"`set_exception_handler` 로 잡히지 않은 예외를 받고, `set_error_handler` 로 경고를 예외(`ErrorException`)로 승격하면 실패가 조용히 지나가지 않는다. 다만 **파싱 오류나 메모리 초과 같은 치명적 오류는 핸들러로 잡히지 않으므로**, `register_shutdown_function` + `error_get_last()` 로 마지막 순간에 기록한다. 로그는 PSR-3 인터페이스에 맞춰 **구조화된 컨텍스트**와 함께 남긴다."}],
      code:{ c:"register_shutdown_function(function () {\n    $e = error_get_last();\n    if ($e && $e['type'] & (E_ERROR | E_PARSE)) {\n        error_log(json_encode($e));\n    }\n});", cap:"치명적 오류를 남기는 마지막 기회" },
      key:["운영은 display_errors=Off + log_errors=On","@ 억제 연산자는 쓰지 않는다","경고를 ErrorException 으로 승격","치명적 오류는 shutdown 함수로 기록"] },
    q:[
      C("운영 설정","운영 서버의 <code>display_errors</code> 값으로 알맞은 것은?",
        ["On","Off","E_ALL","1"],1,
        "**Off** 여야 합니다 — 오류 화면에는 파일 경로·SQL·때로는 자격증명까지 노출됩니다.\n💡 대신 `log_errors=On` 으로 **로그에는 전부** 남깁니다. '숨긴다' 가 아니라 '보는 사람을 바꾼다' 입니다."),
      C("억제","<code>@file_get_contents($url)</code> 처럼 <code>@</code> 를 쓰면?",
        ["오류가 발생하지 않는다","오류 메시지가 억제될 뿐 실패는 그대로다","자동으로 재시도한다","예외로 변환된다"],1,
        "`@` 는 **메시지만 지웁니다** — 함수는 여전히 실패하고 `false` 를 돌려줍니다.\n⚠️ 그리고 그 `false` 를 검사하지 않으면 실패가 문자열 맥락에서 `\"\"` 가 되어 조용히 흘러갑니다. 원인 추적만 불가능해질 뿐 얻는 것이 없습니다."),
      I("함수","로그 파일에 메시지를 기록하는 PHP 내장 함수는? (괄호 없이)",
        ["error_log"],
        "정답은 `error_log()` 입니다.\n💡 실무에서는 PSR-3 로거(Monolog 등)를 쓰고 `error_log` 는 부트스트랩 이전 단계처럼 로거를 아직 못 쓰는 구간에만 씁니다."),
      C("승격","<code>set_error_handler</code> 로 경고를 <code>ErrorException</code> 으로 바꾸는 목적은?",
        ["경고를 없애기 위해",
         "경고가 조용히 지나가지 않고 try/catch 로 다룰 수 있게 하기 위해",
         "실행 속도를 높이기 위해",
         "로그 파일 크기를 줄이기 위해"],1,
        "경고는 기본적으로 **실행을 멈추지 않아** 잘못된 값이 그대로 흘러갑니다. 예외로 승격하면 **그 자리에서 멈추고** 정상 경로로 다룰 수 있습니다.\n⚠️ 단, `set_error_handler` 는 `E_ERROR`·`E_PARSE` 같은 **치명적 오류를 잡지 못합니다** — 그건 `register_shutdown_function` 의 몫입니다."),
      C("치명적 오류","메모리 한도 초과처럼 <b>치명적</b>인 오류를 기록하려면?",
        ["try/catch 로 감싼다","set_error_handler 를 등록한다","register_shutdown_function 안에서 error_get_last() 를 확인한다","@ 연산자를 쓴다"],2,
        "치명적 오류는 핸들러를 거치지 않고 실행을 끝내므로, **종료 직전에 도는 shutdown 함수**에서 `error_get_last()` 로 마지막 오류를 확인해 기록합니다.\n💡 여기서 `memory_limit` 을 살짝 남겨 두는 요령도 있습니다 — 한도에 정확히 부딪히면 로그를 남길 메모리조차 없기 때문입니다."),
      C("로그 설계","실패 로그에 <b>반드시 포함해야 할 것</b>과 <b>절대 넣지 말아야 할 것</b>의 조합으로 옳은 것은?",
        ["포함: 요청 ID·사용자 ID·예외 클래스와 스택 / 제외: 비밀번호·토큰·주민번호",
         "포함: 전체 요청 본문 그대로 / 제외: 스택 트레이스",
         "포함: 비밀번호(디버깅용) / 제외: 요청 ID",
         "포함: 예외 메시지만 / 제외: 사용자 ID 를 포함한 모든 식별자"],0,
        "**추적에 필요한 식별자는 넣고, 비밀은 뺍니다**.\n🎯 요청 ID 가 있어야 여러 서비스의 로그를 한 줄로 꿸 수 있고, 예외 클래스와 스택이 있어야 원인을 좁힙니다.\n⚠️ 디스트랙터 분석: 2번은 요청 본문에 비밀번호가 들어 있고 스택이 없어 최악입니다. 3번은 설명이 필요 없습니다. 4번은 안전해 보이지만 **어떤 사용자에게 일어난 일인지 몰라 재현이 불가능**합니다 — 개인정보 규정이 걱정이면 원본 대신 **가명 ID·해시**를 남기는 것이 정답입니다."),
    ] },
]},

];
