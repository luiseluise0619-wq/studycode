module.exports = {
  track: "dbt", guide: "", xp: 70,
  unit: "로그 분석 — 파이프라인이 남긴 것",
  source: "./log_ops2.cjs", slice: [8, 16],
  lessons: [
    { t: "성공했는데 틀린 것", n: 4,
      th: { sum: "데이터 파이프라인에서 가장 위험한 상태는 실패가 아니라 **성공한 채 틀린 것**이다.",
        body: [
          { h: "성공과 최신은 다른 말", t: "변환이 완벽하게 성공해도 상류가 멈춰 있으면 **어제 데이터를 다시 변환**했을 뿐이다. 도구는 그 사실을 알 방법이 없어 정직하게 성공을 알리고, 행 수가 어제와 같은 것만이 유일한 흔적으로 남는다. 원천에 신선도 기준을 걸어야 이 상황에서 멈춘다. 같은 자리에 있는 것이 **중복**이다 — 상류가 고쳐서 다시 보내는 일은 늘 있는데, 유일 키가 없으면 갱신 대신 덧붙이기가 되어 매출이 두 배로 보고된다." },
          { h: "그래프가 끊기면 순서가 없다", t: "참조 함수 대신 스키마를 손으로 적으면 도구는 그것을 그냥 문자열로 본다. 그러면 그 모델의 **부모가 0개**가 되어 아무 때나 돌고, 상류보다 먼저 돌면 빈 표를 읽어 0행을 만든다. 그런데 도구에게 그것은 실패가 아니다. 검사도 같은 함정을 갖는다 — 심각도를 경고로 낮추면 종료 코드가 0이라 파이프라인이 초록불이 되고, 잡아 놓은 문제가 그대로 하류로 흘러간다. **막지 않는 검사는 없는 검사와 같다.**" }],
        code: { c: "sources: freshness: {error_after: {count: 12, period: hour}}\nconfig(unique_key='event_id')\nfrom {{ ref('stg_orders') }}   -- 손으로 적지 않는다", cap: "초록불과 맞는 값은 다르다" },
        key: ["성공했다고 최신은 아니다", "유일 키가 없으면 덧붙는다", "손으로 적으면 그래프가 끊긴다", "경고로 낮춘 검사는 없는 검사"] } },
    { t: "비용과 사고", n: 4,
      th: { sum: "파이프라인의 사고는 대개 **한 번에 크게** 온다 — 하룻밤 비용, 덮어써진 운영 표, 열두 날치 빈 이력.",
        body: [
          { h: "한 줄이 전체를 다시 만든다", t: "상류가 열을 하나 더한 것만으로 증분 모델이 통째로 다시 만들어질 수 있다. 스키마 변화를 자동으로 따라가는 설정은 편하지만, 큰 표에서는 그 대가가 **세 시간과 스무 배의 요금**으로 온다. 실행 시간과 스캔량에 상한과 경보를 걸어 둔다. 반대 방향의 낭비도 있다 — 끝단까지 뷰로 두면 변환은 늘 빠른데 조회가 겹겹이 펼쳐져, 실행 시간과 조회 시간이 **반대로 움직인다.**" },
          { h: "구조로 막는다", t: "노트북에서 돌린 실행의 기본 대상이 운영이면, 작업 중인 브랜치가 운영 표를 그대로 갈아 치울 수 있다. 각 줄이 전부 정상 동작이고 오류도 없으므로 사람의 주의로는 막지 못한다 — 기본 대상을 개발로 두고 운영 자격은 CI 에만 준다. 이력을 쌓는 스냅샷도 전제를 확인해야 한다: 시각을 기준으로 삼는 전략은 **그 열이 실제로 바뀐다**는 전제 위에 있어서, 적재가 원천 값을 그대로 복사하면 아무 기록도 남지 않는다." }],
        code: { c: "on_schema_change: fail      # 사람이 승인하게\n# 개인 계정에는 운영 스키마 쓰기 권한을 주지 않는다\nsnapshot strategy: check   # updated_at 을 믿을 수 없으면", cap: "주의가 아니라 구조로 막는다" },
        key: ["스키마 변화가 전체 재생성을 부른다", "끝단까지 뷰면 조회가 느려진다", "운영 대상은 CI 에만", "시각 전략은 그 열을 믿는다"] } },
  ],
};
