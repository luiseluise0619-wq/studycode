module.exports = {
  track: "cloud", guide: "", xp: 80,
  unit: "직접 구현 — 저장 · 큐 · 배포 · 관측",
  source: "./cloud_exec2.cjs",
  lessons: [
    { t: "객체 저장소 — 폴더가 없는 세계", n: 5,
      th: { sum: "객체 저장소는 파일 시스템처럼 생겼지만 아니다. 폴더도 이름 바꾸기도 없고, **긴 문자열 열쇠와 그것을 접어 보여 주는 착시**가 전부다.",
        body: [
          { h: "열쇠는 이름이지 경로가 아니다", t: "콘솔에서 폴더처럼 보이는 것은 접두사를 잘라 만든 **그때그때의 계산 결과**다. 그래서 '폴더 이름 바꾸기' 가 없고, 안의 객체를 전부 새 열쇠로 복사한 뒤 옛것을 지우는 수밖에 없어 객체가 많으면 아주 비싸다. 빈 폴더도 없다 — 마지막 객체를 지우면 폴더가 통째로 사라진다. 파일 시스템처럼 동작할 것이라 믿고 만든 코드가 정확히 이 자리에서 어긋난다. 열쇠 이름은 성능에도 닿아 있어, 앞부분이 전부 같으면 요청이 한 곳에 몰릴 수 있다." },
          { h: "빌려주는 권한과 남는 조각", t: "서명된 URL 은 **권한을 시간으로 빌려주는** 방법이다. 링크가 로그·채팅·브라우저 기록에 남으므로 **새어도 곧 쓸모없어지는 것**이 유일한 방어이고, 이미 나간 링크는 취소할 수 없어 유효 시간이 사실상 유일한 조절 장치다. 큰 파일은 조각으로 나눠 올려야 실패한 것만 다시 보낼 수 있는데, **마무리 요청을 보내야 객체가 생긴다** — 중간에 그만두면 올라간 조각이 목록에 안 보이는 채로 요금을 낸다. '저장 용량이 계산과 안 맞는' 흔한 답이 이 남은 조각이다." }],
        code: { c: "const rest = String(k).slice(prefix.length);\nconst cut = rest.indexOf(\"/\");\nout.add(cut < 0 ? rest : rest.slice(0, cut + 1));  // 폴더는 만들어 낸 것", cap: "한 층만 접어 보여 준다" },
        key: ["열쇠는 문자열, 폴더는 착시", "이름 바꾸기는 복사 후 삭제", "서명은 취소할 수 없다", "미완성 조각도 요금을 낸다"] } },

    { t: "큐 — 적어도 한 번이라는 약속", n: 5,
      th: { sum: "큐가 약속하는 것은 '정확히 한 번' 이 아니라 **적어도 한 번**이다. 같은 메시지가 두 번 오는 것은 장애가 아니라 정상 동작이다.",
        body: [
          { h: "지우지 않고 가려 둔다", t: "메시지를 받으면 사라지는 것이 아니라 **잠깐 안 보이게** 된다. 처리에 성공해 지우기 전에 시간이 다하면 다시 나타나므로, 워커가 죽어도 일이 사라지지 않는다. 이 시간을 처리 시간보다 짧게 잡으면 최악이 된다 — 아직 처리 중인데 다른 워커가 같은 일을 시작하고, 그것이 반복되며 늘어난다. 길게 잡는 것도 답이 아니라, 오래 걸리는 일에는 고정 값 대신 **연장**을 쓴다. 계속 실패하는 메시지는 **죽은 편지 큐**로 보내되, 아무도 안 보면 조용히 잃어버리는 것과 같으므로 경보를 건다." },
          { h: "여러 번 실행돼도 안전하게", t: "중복이 정상이므로 처리하는 쪽이 안전해야 한다. 아이디로 걸러 내거나, 아예 결과가 같아지도록(같은 값으로 덮어쓰기) 만드는 두 길이 있다. 걸러 내는 방식은 본 아이디를 기억해야 하는데 무한히 쌓을 수 없어 **기간을 정해** 그 안에서만 막는다. 결제처럼 두 번 하면 안 되는 일에는 메시지 아이디가 아니라 **주문 번호 같은 업무 열쇠**로 걸러야 다른 경로의 중복까지 막힌다. 큐의 건강은 길이가 아니라 **비우는 데 걸리는 시간**으로 본다." }],
        code: { c: "if (!(perSec > 0)) return -1;   // 오래 걸리는 게 아니라 워커가 죽은 것\nreturn Math.ceil(pending / perSec);", cap: "밀림은 시간으로 잰다" },
        key: ["가려 둘 뿐 지우지 않는다", "가림 시간 < 처리 시간이면 무한 증식", "중복은 정상, 처리는 멱등하게", "큐는 길이가 아니라 시간으로 본다"] } },

    { t: "배포 — 되돌릴 수 있게 나눈다", n: 5,
      th: { sum: "배포 전략의 차이는 결국 하나다 — **잘못됐을 때 얼마나 빨리, 얼마나 싸게 되돌릴 수 있는가.**",
        body: [
          { h: "조금씩 · 지켜보며 · 통째로", t: "순차 배포는 조금씩 바꿔 피해를 그 몫으로 제한하지만, **두 버전이 함께 도는 시간**이 생긴다. 회차 크기에 최소 1을 보장하지 않으면 3대에 10% 가 0이 되어 영원히 끝나지 않는 배포가 되는데, 이런 경계는 작은 환경에서만 드러나 운영에서 늦게 발견된다. 카나리는 여기에 판단을 더한다 — 나쁘면 되돌리고 **충분히 좋으면 늘린다**. 비율이 작을 때는 표본이 모자라 오류율 0% 가 아무 뜻이 없으므로 최소 관측 수를 함께 조건으로 둔다. 청·녹은 한 벌을 더 띄워 두는 대신 전환과 되돌리기가 즉시다." },
          { h: "데이터는 되돌릴 수 없다", t: "'되돌리면 된다' 는 코드에만 해당한다. 지운 칸을 다시 만들어도 **안에 들었던 값은 돌아오지 않는다.** 그래서 칸을 더하는 것과 빼는 것의 위험이 완전히 다르다 — 더하는 것은 옛 코드가 무시하면 그만이지만, 빼는 것은 옛 코드가 그 칸을 찾다가 터진다. 실무의 해법은 **두 단계로 나누는 것**이다: 먼저 새 칸을 더해 양쪽을 다 쓰게 배포하고, 충분히 지난 뒤 옛 칸을 지운다. 각 단계는 되돌릴 수 있고 두 버전이 함께 돌아도 괜찮다." }],
        code: { c: "if (errPct > limitPct) return [\"rollback\", 0];\nif (errPct <= limitPct / 2) return [\"up\", Math.min(pct * 2, 100)];\nreturn [\"hold\", pct];   // 확실히 좋을 때만 앞으로", cap: "늘릴 조건이 있어야 자동이 된다" },
        key: ["회차 크기는 속도와 위험의 맞바꿈", "카나리는 늘릴 조건까지 있어야 한다", "스키마는 더하기와 빼기를 나눈다", "데이터는 되돌아오지 않는다"] } },

    { t: "관측과 비용 — 무엇을 남기고 무엇을 볼까", n: 5,
      th: { sum: "전부 남기면 저장비가 서비스 비용을 넘고, 요약만 남기면 문제가 숨는다. 관측 설계는 **무엇을 버릴지 정하는 일**이다.",
        body: [
          { h: "흥미로운 것을 남긴다", t: "추적을 무작위로 표본 추출하면 정작 보고 싶은 것이 빠진다. **오류와 느린 요청은 전부** 남기고 평범한 것은 일부만 남기는 편이 낫다 — 조사할 때 필요한 것은 대개 앞의 둘이다. 다만 이 방식은 통계를 왜곡하므로, 지표는 표본이 아니라 **전수 집계**에서 얻어야 한다. 판단은 추적이 끝난 뒤에 하는 것이 좋다. 시작할 때는 느릴지 알 수 없어서, 앞에서 정하면 결국 무작위와 다를 바 없어진다. 경보도 같은 절제가 필요하다 — 자주 울리고 대개 별일 아니면 사람은 곧 무시하게 되고, 그때 진짜 경보까지 묻힌다." },
          { h: "요금은 구간으로, 낭비는 목록 밖에", t: "클라우드 요금 대부분은 계단식이다. 흔한 오해가 '구간을 넘으면 전부 싼 단가' 인데, 실제로는 **넘은 분량만** 싼 단가라 소득세 구간과 같다. 무료 제공량도 단가가 0인 첫 구간일 뿐이다. 진짜 새는 곳은 계산이 아니라 **아무도 안 쓰는데 살아 있는 것들**이다 — 인스턴스를 지웠는데 남은 디스크, 붙지 않은 동안 오히려 요금이 붙는 고정 IP. 이런 것은 인스턴스 목록에 없고 따로 찾지 않으면 몇 달씩 조용히 요금을 낸다. 만들 때 태그를 강제하는 것이 사실상 유일한 해법이다." }],
        code: { c: "// 구간을 넘은 분량만 다음 단가 — 소득세와 같다\nconst span = Math.min(Math.max(tr[0] - prev, 0), left);\nsum += span * tr[1];", cap: "전부 싼 단가가 되는 게 아니다" },
        key: ["오류와 느린 것을 남긴다", "지표는 전수 집계에서", "요금은 넘은 분량만 싼 단가", "주인 없는 자원은 목록 밖에 산다"] } },
  ],
};
