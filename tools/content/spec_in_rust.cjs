module.exports = {
  track: "rust", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 소유권과 타입 규칙",
  source: "./in_rust.cjs",
  lessons: [
    { t: "누가 값을 책임지는가", n: 8,
      th: { sum: "러스트의 규칙은 몇 줄 안 된다 — **주인은 하나**, **읽기 여럿이거나 쓰기 하나**, 그리고 **참조는 원본보다 오래 못 산다**.",
        body: [
          { h: "옮기고, 빌리고, 검사한다", t: "소유권이 있는 값을 대입하면 소유권이 <b>옮겨 가고</b> 원래 변수는 못 쓴다 — 이 규칙 하나가 이중 해제와 해제 후 사용을 컴파일 시점에 없앤다. 빌릴 때는 '<b>읽기 여럿</b>' 이거나 '<b>쓰기 하나</b>' 둘 중 하나만 되고, 이것이 데이터 경쟁을 막는다. <b>빌림 검사기</b>는 참조가 원본보다 오래 살지 않는지 보며, 생명주기 표기는 새 규칙이 아니라 그 검사에 필요한 정보를 적어 주는 문법이다." },
          { h: "없음과 실패를 타입에 적는다", t: "<code>Option&lt;T&gt;</code> 는 <code>Some</code>/<code>None</code>, <code>Result&lt;T, E&gt;</code> 는 <code>Ok</code>/<code>Err</code> 다 — 널 포인터와 예외 대신 <b>타입으로</b> 표현하니, 어떤 함수가 실패할 수 있는지 시그니처만 봐도 안다. 문자열은 <code>String</code> 이 소유하고 <code>&amp;str</code> 이 빌려 보는 창이라, <b>받을 때 <code>&amp;str</code>, 돌려줄 때 <code>String</code></b> 이 가장 흔한 규약이다. 참조 세기 포인터는 단일 스레드면 <code>Rc</code>, 여럿이면 원자적으로 세는 <code>Arc</code> 다." }],
        code: { c: "fn greet(name: &str) -> String   // 받을 때 빌리고 돌려줄 때 소유\nArc<Mutex<T>>                    // 소유는 Arc, 배타는 Mutex\nvalue?                           // Err 면 그대로 위로 올린다", cap: "규칙을 지키면 컴파일이 증명이 된다" },
        key: ["대입은 소유권 이동", "읽기 여럿이거나 쓰기 하나", "없음과 실패는 타입으로", "받을 때 &str, 돌려줄 때 String"] } },
    { t: "규칙을 미루거나 비켜 갈 때", n: 7,
      th: { sum: "컴파일 시점 규칙으로 표현하기 어려운 것이 있다. 그때 쓰는 도구들은 하나같이 **보증을 어디로 미뤘는지**가 이름에 적혀 있다.",
        body: [
          { h: "실행 시점으로 미루기", t: "<code>RefCell</code> 은 빌림 검사를 <b>실행 시점</b>으로 미룬다 — 규칙을 어기면 컴파일 오류 대신 그 자리에서 멈춘다. <code>dyn Trait</code> 은 구현 선택을 실행 시점으로 미뤄 서로 다른 타입을 한 목록에 담게 해 주지만, 호출이 표를 거쳐 약간 느리다. <code>unsafe</code> 는 빌림 검사를 <b>끄지 않고</b> 몇 가지 연산(대표적으로 원시 포인터 역참조)을 추가로 허용할 뿐이며, 작게 감싸 안전한 API 로 내보내는 것이 관례다." },
          { h: "크기와 넘침이라는 물리적 사실", t: "슬라이스·트레이트 객체처럼 크기를 컴파일 시점에 모르는 <b>DST</b> 는 그대로 담을 수 없어 참조나 <code>Box</code> 뒤에 두어야 하고, 그 포인터는 <b>주소 + 길이</b> 두 칸짜리다. 정수 넘침은 디버그에서 <b>패닉</b>, 릴리스에서 감싸 돌기라 빌드에 따라 동작이 갈리므로 <code>checked_</code>·<code>saturating_</code>·<code>wrapping_</code> 으로 <b>원하는 동작을 이름으로</b> 적는다. <code>Vec::push</code> 가 균등 상환 <code>O(1)</code> 인 것은 용량을 두 배씩 늘리기 때문이다." }],
        code: { c: "Box<dyn Trait>            // 실행 시점에 구현을 고른다\nx.checked_add(y)          // 넘치면 None\nOption<Box<i32>>          // 8바이트 — 널 포인터 최적화", cap: "미룬 검사는 이름에 적혀 있다" },
        key: ["RefCell 은 검사를 실행 시점으로", "unsafe 는 빌림 검사를 끄지 않는다", "DST 는 참조나 Box 뒤에", "넘침 동작을 이름으로 고른다"] } },
  ],
};
