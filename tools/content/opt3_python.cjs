/* python — 정답률 15% 아래 끝맺음의 오답 18개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "python", fixes: [
  { k: "__name__ 로거", from: "핸들러가 자동으로 연결되어 설정이 필요 없다", to: "모듈 이름과 같은 파일에 핸들러가 자동으로 연결되어 설정 없이 파일 로그가 남는다" },
  { k: "eq와 hash", from: "그대로 유지되므로 아무 문제가 없다", to: "기본 id 기반 __hash__가 그대로 유지되어 dict 키로 쓸 수 있다" },
  { k: "정적 검사 도구", from: "검사할 방법이 없다", to: "힌트는 주석이라 검사 대상이 아니다" },
  { k: "형변환 필요", from: "float 타입만 더할 수 있기 때문이다", to: "덧셈은 float 끼리만 허용되어서다" },
  { k: "parametrize 이점", from: "픽스처 함수를 따로 만들 필요가 없다", to: "케이스마다 필요한 픽스처를 인자 값에서 자동으로 생성해 주므로 픽스처 함수는 생략된다" },
  { k: "GIL해제", from: "순수 파이썬 코드로 작성되어 있기 때문이다", to: "NumPy 연산이 순수 파이썬 코드로 작성되어 있어서다" },
  { k: "response-model-leak", from: "출력 스키마를 따로 만들 필요가 없다", to: "응답 미들웨어에서 문자열 치환으로 민감 필드를 지워 버린다. 출력 스키마는 그대로 둔다" },
  { k: "jwt-stateless", from: "상태 저장이 필요 없다", to: "JWT는 암호화되어 있어 내용을 아무도 볼 수 없기 때문에 안전하다. 내용이 감춰져 있으니 상태 저장은 생략된다" },
  { k: "pyspy프로덕션", from: "재배포하지 않고는 볼 방법이 없다", to: "코드에 cProfile 을 삽입한 뒤 다시 배포해서 본다. 재배포 없이는 내부를 들여다볼 길이 막혀 있다" },
  { k: "문자열결합", from: "문자열은 원래 느리다, 방법이 없다", to: "문자열은 원래 느리니 감수해야 한다" },
  { k: "event-schema-evolution", from: "이벤트를 쓰지 않는다", to: "이벤트 대신 동기 API 호출로 바꾼다" },
  { k: "GIL 오개념", from: "파이썬에서는 우회 방법이 없다", to: "맞는 말이며 파이썬에서는 우회할 길이 막혀 있다" },
  { k: "상속 매칭", from: "런타임 오류가 난다", to: "except 절 타입 불일치로 실행이 중단된다" },
  { k: "io-bound threading ok", from: "전면 재작성하는 것 외엔 방법이 없다", to: "맞는 말이므로 asyncio로 전면 재작성하는 것이 유일하다" },
  { k: "lru_cache memoize", from: "지연이 생기지 않는다", to: "heavy_lookup 을 별도 스레드로 돌리면 재계산 비용이 사라진다 → 계산이 백그라운드로 옮겨져 호출자는 기다리지 않게 되므로 초당 수천 번 호출에도 지연 없이 응답한다" },
  { k: "무효화 함정", from: "별도 장치는 필요 없다", to: "모든 요청이 각자 DB 를 친다 — 원인: 어느 요청이든 공평하게 처리되어야 함 / 해결: 요청마다 스스로 값을 갱신하게 두면 자연히 분산된다. 부하가 자연스럽게 흩어져 DB 가 안정된다. 별도 장치는 오히려 병목이 된다" },
  { k: "데이터 vs 비데이터", from: "우선순위를 따로 기억할 필요가 없다", to: "둘은 완전히 같다 — 원인: 데이터·비데이터는 문서상 이름일 뿐 인터프리터가 구분하는 속성이 아님 / 해결: 어느 쪽이든 인스턴스 __dict__ 가 먼저 조회되므로 우선순위는 하나뿐이다" },
  { k: "경계 검증", from: "런타임 검증을 하면 힌트는 필요 없다", to: "런타임 검증을 하면 힌트는 중복이라 뺀다" }
] };
