module.exports = {
  track: "ai", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 파이프라인은 도는데 답만 어긋나는 자리",
  source: "./dbg_ai.cjs",
  lessons: [
    { t: "모델에 넣기 전에 어긋나는 것", n: 7,
      th: { sum: "RAG 사고의 대부분은 모델이 아니라 **넣기 전 단계**에서 난다 — 조각이 빠졌거나, 자리를 잘못 나눴거나, 캐시가 옛 것을 준다.",
        body: [
          { h: "빠뜨리지 않고 넣었는가", t: "청킹은 '<b>모든 글자가 적어도 한 조각에 들어간다</b>' 는 성질로 검사해야 한다 — 온전한 크기만 담는 반복문은 꼬리를 통째로 버리고, 문서가 조각보다 짧으면 아무것도 안 만든다. 컨텍스트 예산은 <b>시스템 프롬프트 + 검색 결과 + 대화 기록 + 응답 여유</b> 네 몫으로 나누고, 기록은 <b>최근 것부터</b> 채운다. 오래된 쪽부터 채우면 예산이 찰수록 방금 한 말이 사라진다." },
          { h: "같은 것을 같다고 보는가", t: "캐시 열쇠에는 <b>결과를 달라지게 하는 모든 것</b>이 들어가야 한다 — 임베딩 캐시에 모델 이름이 빠지면 모델을 바꾼 뒤 옛 좌표계의 벡터가 그대로 나온다. 파라미터로 열쇠를 만들 때는 이름을 <b>정렬</b>해야 순서만 다른 같은 설정이 같은 열쇠가 된다. 코사인 유사도는 <b>각도만</b> 보는 값이므로 길이로 나눠야 하고, 검색은 <b>거르고 → 줄 세우고 → 자르는</b> 순서를 지켜야 한다." }],
        code: { c: "if (i + size >= text.length) break;                 // 꼬리도 담는다\nfor (let i = msgs.length - 1; i >= 0; i -= 1) …     // 최근 것부터\nreturn d / (Math.sqrt(na) * Math.sqrt(nb));         // 각도만 남긴다", cap: "넣기 전 단계에 계약을 적어 둔다" },
        key: ["조각이 원문을 덮는지 성질로 검사한다", "예산은 네 몫으로 나눠 적는다", "캐시 열쇠에 모델과 버전을 넣는다", "거르고 → 줄 세우고 → 자른다"] } },
    { t: "모델이 준 것을 다루다 어긋나는 것", n: 6,
      th: { sum: "모델의 출력은 **문자열이지 자료구조가 아니고**, 로짓은 **실제 스케일에서만 무너진다**. 채점기도 코드라서 먼저 테스트가 필요하다.",
        body: [
          { h: "경계와 표현 범위", t: "top-p 는 '<b>p 를 처음 넘기는 토큰까지 포함</b>' 이 정의의 전부다 — 담고·더하고·검사하는 순서가 뒤집히면 확률이 큰 토큰 하나가 통째로 빠진다. <code>Math.exp(800)</code> 은 <code>Infinity</code> 라 소프트맥스가 <code>NaN</code> 이 된다. 최댓값을 빼는 것은 근사가 아니라 <b>항등식</b>이다. 개수를 받아 자를 때 <code>-n</code> 은 <code>n=0</code> 에서 <code>-0</code> 이 되어 학습·평가를 통째로 뒤집는다." },
          { h: "관대하게 읽고 엄격하게 채점한다", t: "'오직 JSON 만 답하라' 고 적어도 코드 펜스와 인사말이 붙는다 — <b>파싱은 관대하게, 검증은 엄격하게</b> 한다. 조각에 순번이 붙어 있다는 것 자체가 '<b>도착 순서를 믿지 말라</b>' 는 신호다. 채점기는 정규화 규칙을 한 곳에 모아 <b>양쪽에 똑같이</b> 적용하고, '정답으로 쳐야 할 응답' 목록으로 회귀 검사를 한다 — 점수가 낮을 때 고쳐야 할 것이 모델이 아닐 수 있다." }],
        code: { c: "const m = Math.max(...xs);\nconst e = xs.map(v => Math.exp(v - m));   // 항등식이라 정확도 손실이 없다\nevents.slice().sort((a, b) => a.i - b.i)  // 순번이 있으면 그것이 기준", cap: "출력은 문자열이고, 로짓은 크다" },
        key: ["경계 원소를 포함하는지 정의를 확인한다", "지수는 작게 유지하며 계산한다", "구조화 출력이 가능하면 파싱을 없앤다", "채점기에도 회귀 테스트를 둔다"] } },
  ],
};
