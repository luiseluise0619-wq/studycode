/* Cloud 진단 유닛 — 클라우드에서 '안 되는데 왜 안 되는지 모르겠는' 순간들.
   막힌 곳은 대개 네 겹(경로·보안 그룹·역할·애플리케이션) 중 하나이고,
   새는 곳은 대개 눈에 안 보이는 자원이다.

   보기 규칙: 네 보기의 길이를 흩는다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const D=(k,q,o,a,ex)=>({t:"choice",cat:"debug",k,q,o,a,ex});
const I=(k,q,a,ex)=>({t:"input",cat:"debug",k,q,a,ex});

module.exports=shuffleUnits([

{ t:"진단 — 안 되는 것과 새는 것", l:[

  { t:"연결이 막혔다", xp:40,
    th:{ sum:"클라우드에서 '안 된다' 는 대개 **네 겹 중 하나**다 — 경로(라우팅), 보안 그룹, 권한(역할), 애플리케이션. 바깥에서 안으로 좁혀 가면 대개 몇 분 안에 끝난다.",
      body:[
        {h:"경로가 없으면 나머지는 소용없다", t:"공인 IP 가 붙어 있어도 **라우팅 테이블에 인터넷 게이트웨이로 가는 경로가 없으면** 통신되지 않는다. 프라이빗 서브넷의 인스턴스가 밖으로 못 나가는 것도 같은 이유이고, 그때 필요한 것이 NAT 다. 보안 그룹은 **허용 목록**이라 규칙에 없으면 막히고, 네트워크 ACL 은 **상태를 기억하지 않아** 응답 방향까지 열어야 한다."},
        {h:"권한 실패는 다르게 생겼다", t:"인스턴스에서 클라우드 API 를 부를 때 실패하면 네트워크가 아니라 **역할이 안 붙었거나 정책이 좁은 것**이다. 메시지에 `AccessDenied` 가 있으면 경로 문제가 아니다. 반대로 아무 응답 없이 멈춰 있다면 경로나 보안 그룹 쪽이다 — **거부는 빠르게 오고 경로 없음은 조용히 기다린다**는 차이가 진단의 단서가 된다."}],
      code:{ c:"# 바깥에서 안으로\n1) 라우팅 테이블에 경로가 있나\n2) 보안 그룹이 그 포트를 열었나\n3) 인스턴스에 역할이 붙었나\n4) 애플리케이션이 그 포트를 듣고 있나", cap:"순서를 지키면 대개 몇 분" },
      key:["경로가 없으면 나머지는 소용없다","보안 그룹은 허용 목록","AccessDenied 는 권한 문제","조용히 멈추면 경로 쪽"] },
    q:[
      D("접속이 안 된다","공인 IP 를 붙였는데 인스턴스에 접속되지 않습니다. 먼저 확인할 것으로 알맞은 것은?",
        ["라우팅 테이블에 인터넷 게이트웨이 경로가 있는지",
         "인스턴스의 사양이 들어오는 요청을 감당할 만큼 충분한지",
         "인스턴스에 붙인 태그가 올바른지",
         "같은 이름을 쓰는 인스턴스가 이미 있지는 않은지"],0,
        "**경로가 없으면 공인 IP 가 있어도 통신되지 않습니다.** '왜 접속이 안 되지' 의 가장 흔한 원인입니다.\n그다음이 보안 그룹, 그다음이 인스턴스 안의 방화벽 순서입니다.\n사양·태그·이름은 접속 가능 여부와 아무 관계가 없습니다."),
      D("밖으로 못 나간다","프라이빗 서브넷의 서버가 패키지를 내려받지 못합니다. 원인으로 알맞은 것은?",
        ["나가는 경로를 만들어 주는 NAT 가 없다",
         "그 인스턴스에 공인 IP 가 붙어 있지 않다",
         "그 서브넷 보안 그룹의 인바운드 규칙이 좁다",
         "그 서브넷에 남은 주소 범위가 너무 작다"],0,
        "**NAT** 가 나가는 방향을 맡습니다. 없으면 프라이빗에서는 밖으로 나갈 수 없습니다.\n공인 IP 를 붙이는 것은 해결이 아니라 그 서브넷을 퍼블릭으로 만드는 다른 선택입니다.\n인바운드 규칙은 들어오는 방향이라 이 증상과 관계가 없습니다."),
      D("API 호출이 거부된다","앱이 객체 스토리지에 접근할 때 <code>AccessDenied</code> 가 납니다. 원인으로 알맞은 것은?",
        ["인스턴스에 역할이 붙지 않았거나 정책이 좁다",
         "서브넷에서 그 서비스로 이어지는 경로가 없다",
         "보안 그룹이 아웃바운드를 막고 있다",
         "버킷이 다른 리전에 있다"],0,
        "**`AccessDenied` 는 권한 문제**입니다. 요청이 닿았고 거부된 것이므로 경로는 열려 있습니다.\n역할이 붙었는지, 정책이 그 버킷과 그 행동을 허용하는지, 버킷 정책이 막고 있지는 않은지 순서로 봅니다.\n경로나 보안 그룹 문제라면 거부가 아니라 **응답 없이 멈추는** 모양으로 나타납니다."),
      D("전부 unhealthy","로드 밸런서가 모든 대상을 비정상으로 판정합니다. 확인할 것으로 알맞은 것은?",
        ["헬스 체크 경로와 포트, 그리고 그 포트의 보안 그룹",
         "대상 인스턴스마다 공인 IP 가 붙어 있는지 여부",
         "로드 밸런서가 걸쳐 있는 AZ 의 개수와 그 배치 방식",
         "대상 인스턴스에 붙인 이름의 규칙"],0,
        "**헬스 체크가 실제로 닿는가**를 봅니다. 경로가 틀렸거나, 포트가 다르거나, 그 포트가 보안 그룹에서 막힌 경우가 대부분입니다.\n로드 밸런서에서 인스턴스로 가는 방향을 열어야 하는데, 출발지를 로드 밸런서의 보안 그룹으로 지정하면 깔끔합니다.\n애플리케이션이 아직 뜨는 중이라면 유예 시간을 주는 설정도 함께 봐야 합니다."),
      D("연결이 안 맺어진다","두 VPC 를 피어링했는데 통신이 되지 않습니다. 확인할 것으로 알맞은 것은?",
        ["주소 범위가 겹치지 않는지, 그리고 양쪽 라우팅과 보안 그룹을 열었는지",
         "두 VPC 의 서브넷이 서로 같은 AZ 에 놓여 있는지 여부와 그 배치",
         "두 VPC 의 이름이 서로 겹치지 않는지",
         "피어링 연결의 이름이 정해진 규칙을 따르는지"],0,
        "**겹치면 애초에 맺을 수 없고**, 맺었어도 라우팅과 보안 그룹을 양쪽에서 열어야 통신됩니다.\n피어링은 길을 낼 수 있게 해 줄 뿐 자동으로 통하게 하지 않습니다.\nAZ 나 이름은 관계가 없습니다 — 겹치는 대역은 나중에 고치기가 거의 불가능하므로 설계 단계에서 막아야 합니다."),
      D("배포 때마다 오류","배포할 때마다 일부 요청이 오류로 끝납니다. 원인으로 알맞은 것은?",
        ["진행 중인 요청이 끝나기 전에 인스턴스를 빼고 있다",
         "새로 올린 버전의 코드에 처리하지 못한 버그가 남아 있다",
         "배포 중에는 로드 밸런서가 멈춘다",
         "배포를 진행하는 시각에 마침 트래픽이 크게 몰린다"],0,
        "**연결 드레이닝**이 없거나 짧은 것입니다. 새 요청은 보내지 않되 이미 받은 요청은 끝까지 처리하도록 유예를 주어야 합니다.\n이 설정 하나가 '배포할 때마다 가끔 나는 오류' 를 통째로 없앱니다.\n애플리케이션 쪽에서도 종료 신호를 받고 정리하는 처리를 넣어야 완성됩니다."),
      D("오토스케일이 안 늘어난다","부하가 몰리는데 인스턴스가 늘어나지 않습니다. 확인할 것으로 알맞은 것은?",
        ["지표 선택과 최대 대수, 그리고 서브넷에 남은 주소",
         "인스턴스 타입이 최신 세대인지",
         "로드 밸런서가 요청을 나누어 보내는 알고리즘 설정",
         "자원에 붙여 둔 태그의 키와 값"],0,
        "세 가지가 흔합니다 — **지표가 실제 포화를 나타내지 않거나**(기다리는 서비스에 CPU 를 쓴 경우), **최대 대수에 이미 닿았거나**, **서브넷에 주소가 모자란** 경우입니다.\n특히 마지막은 눈에 잘 안 띕니다. `/28` 서브넷은 예약 5개를 빼면 11대뿐입니다.\n타입이나 태그는 확장 동작과 관계가 없습니다."),
      I("인터넷 없이 접근","프라이빗 서브넷에서 인터넷을 거치지 않고 클라우드 서비스에 닿게 해 주는 장치는? (한국어)",
        ["VPC 엔드포인트","엔드포인트","VPC endpoint","프라이빗 엔드포인트"],
        "정답은 **VPC 엔드포인트** 입니다. 클라우드 내부 경로로 바로 연결해 인터넷을 타지 않습니다.\n더 안전할 뿐 아니라 **NAT 전송 비용도 아낍니다** — 로그를 대량으로 내보내거나 백업을 올리면 차이가 큽니다.\n객체 스토리지처럼 자주 쓰는 대상은 엔드포인트로 빼 두는 것이 기본 구성에 가깝습니다.")
    ] },

  { t:"돈과 데이터가 샌다", xp:40,
    th:{ sum:"요금 명세와 유출 사고는 대개 **눈에 안 보이는 곳**에서 시작한다. 콘솔 첫 화면에 나오지 않는 것들을 찾는 눈이 필요하다.",
      body:[
        {h:"보이지 않는 자원", t:"지운 인스턴스에 붙어 있던 **디스크**, 회수하지 않은 **고정 IP**, 끝나지 않은 **멀티파트 업로드**, 아무도 안 보는 **스냅숏과 로그**. 비용을 줄이러 들어가면 절반 이상이 이런 버려진 자원에서 나온다. 요금이 갑자기 튀었다면 **나가는 데이터 전송**과 **NAT 통과량**을 가장 먼저 본다 — 둘 다 명세 상단에 잘 나오지 않는다."},
        {h:"복제는 실수도 복제한다", t:"다른 리전으로 실시간 복제를 걸어 두어도 **실수로 지운 것은 저쪽에서도 지워진다.** 복제는 리전 장애를 대비하고, 실수와 손상은 **시점 백업**이 대비한다 — 둘은 막는 위험이 다르다. 그리고 백업은 **다른 계정·다른 리전에 두고 삭제 권한을 분리**해야 계정이 통째로 침해되어도 남는다."}],
      code:{ c:"# 요금이 튀면 이 순서로 본다\n1) 나가는 데이터 전송\n2) NAT 게이트웨이 처리량\n3) 주인 없는 디스크 · 스냅숏\n4) 로그 보관량", cap:"명세 상단에 안 나오는 것들" },
      key:["버려진 자원이 절반 이상","전송과 NAT 를 먼저 본다","복제는 실수까지 복제한다","백업은 분리해 둔다"] },
    q:[
      D("요금이 갑자기 튀었다","한 달 요금이 두 배가 되었습니다. 가장 먼저 볼 항목으로 알맞은 것은?",
        ["나가는 데이터 전송과 NAT 처리량",
         "인스턴스 종류별 시간당 단가표",
         "계정에 등록되어 있는 사용자의 수",
         "계정 안에 만들어 둔 버킷과 폴더의 총 개수"],0,
        "**전송과 NAT** 가 갑자기 튀는 대표적인 항목입니다. 명세 상단에 잘 나오지 않아 놓치기 쉽습니다.\n객체 스토리지 트래픽이 NAT 를 통과하고 있었다면 VPC 엔드포인트 하나로 사라집니다.\n인스턴스 단가는 갑자기 바뀌지 않고, 사용자 수나 버킷 개수에는 요금이 붙지 않습니다."),
      D("지웠는데 요금이 나온다","인스턴스를 지웠는데 요금이 계속 나옵니다. 원인으로 알맞은 것은?",
        ["붙어 있던 디스크가 남아 있다",
         "지운 인스턴스의 이름이 예약되어 있다",
         "인스턴스 요금은 한 달 단위로 묶여 청구된다",
         "스냅숏이 자동으로 인스턴스를 되살린다"],0,
        "**주인 없는 볼륨**이 가장 흔합니다. 인스턴스를 지울 때 디스크를 함께 지우도록 설정하지 않으면 남습니다.\n회수하지 않은 고정 IP 도 같은 종류입니다 — 붙어 있지 않을 때 오히려 요금이 붙는 경우가 많습니다.\n정기적으로 미연결 볼륨과 오래된 스냅숏을 찾아 정리하는 작업을 자동화해 두는 곳이 많습니다."),
      D("버킷이 공개되었다","비공개여야 할 버킷이 인터넷에 열려 있었습니다. 사후 조치 순서로 알맞은 것은?",
        ["즉시 잠그고 접근 기록을 확인한 뒤 노출된 데이터를 판단한다",
         "버킷 이름을 바꿔 밖에서 추측하기 어렵게 만들어 둔다",
         "객체마다 접근 권한을 하나씩 다시 설정해 전부 잠가 나간다",
         "버킷을 지우고 새로 만들어 객체를 전부 옮긴다"],0,
        "**잠그는 것이 먼저이고 그다음이 조사**입니다. 얼마나 노출되었는지는 접근 로그로만 알 수 있습니다.\n이름을 바꾸는 것은 보안이 아닙니다 — 이미 가져간 것은 되돌아오지 않습니다.\n계정 수준의 공개 차단을 켜 두면 개별 설정 실수가 있어도 막히므로, 재발 방지는 그쪽으로 합니다."),
      D("복원이 안 된다","백업은 매일 돌고 있었는데 막상 복원이 되지 않습니다. 빠진 것으로 알맞은 것은?",
        ["복원 연습",
         "백업을 보관하는 기간의 연장",
         "백업 압축 설정",
         "백업 알림 설정"],0,
        "**한 번도 복원해 본 적 없는 백업은 백업이 아닙니다.** 빠진 테이블, 없는 권한, 예상보다 긴 소요 시간이 그때 드러납니다.\n정기적으로 복원해 보고 **걸린 시간을 재 두면** 그 숫자가 곧 실제 RTO 가 됩니다.\n보관 기간이나 압축은 이 문제와 관계가 없습니다."),
      D("복제했는데 함께 사라졌다","다른 리전으로 복제해 두었는데 실수로 지운 데이터가 양쪽에서 사라졌습니다. 필요한 것은?",
        ["시점 백업",
         "복제가 반영되는 지연 시간 늘리기",
         "복제 리전 추가",
         "복제 방향 반대로 설정"],0,
        "**복제는 실수까지 복제합니다.** 지운 것이 저쪽에서도 지워집니다.\n복제는 리전 장애를, **시점 백업은 실수와 손상**을 대비합니다 — 막는 위험이 다르므로 둘 다 필요합니다.\n객체 스토리지라면 버전 관리를 켜 두는 것만으로도 실수 삭제가 되살아납니다."),
      D("로그 비용이 폭증","로그 수집 비용이 몇 배로 늘었습니다. 원인으로 알맞은 것은?",
        ["로그 수준을 디버그로 올려 둔 채 잊었다",
         "로그를 담아 두는 저장소의 단가가 올랐다",
         "로그 형식을 JSON 으로 바꿨다",
         "로그를 여러 파일로 나눴다"],0,
        "**디버그 수준을 켜 둔 채 잊는 것**이 가장 흔합니다. 양이 몇 배로 늘어 수집·저장·조회 비용이 모두 오릅니다.\n조사할 때 잠깐 올렸다가 되돌리는 절차를 정해 두거나, 일정 시간 뒤 자동으로 되돌아가게 만듭니다.\nJSON 형식은 용량이 조금 늘지만 몇 배가 되지는 않습니다."),
      D("아무 경보도 안 울렸다","배치가 사흘째 돌지 않았는데 경보가 없었습니다. 필요한 경보로 알맞은 것은?",
        ["정해진 시간 안에 성공 기록이 없으면 울리는 경보",
         "오류율이 정해진 기준을 넘어설 때 곧바로 울리는 경보",
         "CPU 사용률이 정해진 기준보다 높을 때 울리는 경보",
         "응답 지연이 기준보다 길어질 때 울리는 경보"],0,
        "**있어야 할 일이 없었던 경우**라 실패도 오류도 남지 않습니다. 그래서 '지난 25시간 안에 성공 기록이 있는가' 로 겁니다.\n조용한 실패는 오류 기반 경보로는 절대 잡히지 않습니다 — 아무 일도 일어나지 않았기 때문입니다.\n정기 작업에는 이 종류의 경보를 짝으로 두는 것이 원칙입니다."),
      I("백업 보관 원칙","백업을 원본과 다른 곳에 두고 지울 권한도 나누는 원칙을 한 낱말로 뭐라 하나? (한국어 두 글자)",
        ["분리","격리"],
        "정답은 **분리** 입니다. 다른 계정·다른 리전에 두고 삭제 권한도 나눕니다.\n계정이 통째로 침해되거나 실수로 지워져도 백업은 남아야 하기 때문입니다.\n랜섬웨어 사고에서 백업까지 함께 암호화된 사례가 많아, 삭제 권한을 나누는 것이 특히 중요합니다.")
    ] }
]}

]);
