module.exports = {
  track: "linux", guide: "", xp: 80,
  unit: "직접 구현 — 텍스트 도구와 관측 값",
  source: "./linux_exec2.cjs",
  lessons: [
    { t: "텍스트 도구 — 한 줄짜리 집계의 정체", n: 5,
      th: { sum: "cut·sort·uniq·awk·sed 는 서로 다른 다섯 도구처럼 보이지만, **쪼개고 · 세고 · 견주고 · 바꾸는** 네 가지 계산의 조합이다.",
        body: [
          { h: "쪼개는 규칙이 답을 정한다", t: "`cut -d: -f1` 은 칸을 **1부터** 세고, 구분자가 연속으로 나오면 **빈 칸**이 생긴다. 공백으로 정렬된 표에서 `cut` 이 엉뚱한 값을 주는 이유가 이것이라, 그럴 때는 연속 공백을 하나로 보는 `awk` 를 쓴다. 정렬에서도 규칙이 답을 바꾼다 — `-n` 없이 정렬하면 글자 순서라 `100` 이 `30` 보다 앞에 온다. '크기순으로 봤는데 이상하다' 의 대부분이 이 한 글자다." },
          { h: "붙어 있는 것만 접는다", t: "`uniq -c` 는 **인접한** 같은 줄만 센다. 앞에 `sort` 를 붙이지 않으면 흩어져 있는 같은 줄이 각각 1로 세어져 결과가 조용히 틀린다. `sort | uniq -c | sort -rn` 이 관용구가 된 이유다. `sed` 에도 같은 성격의 함정이 있다 — `g` 가 없으면 **줄마다 첫 번째 하나만** 바뀌므로, 치환이 절반만 된 채 성공한 것처럼 보인다. 이런 도구에서는 '무엇을 했는가' 보다 **몇 건을 했는가**를 확인하는 습관이 안전장치가 된다." }],
        code: { c: "// awk 의 연관 배열은 선언 없이 0에서 시작한다\n// awk '{s[$1]+=$2} END{for(k in s) print k, s[k]}'\nrows.forEach((r) => m.set(r[0], (m.get(r[0]) || 0) + Number(r[1])));", cap: "집계는 결국 열쇠별 더하기다" },
        key: ["칸은 1부터 센다", "`sort -n` 없이는 글자 순서", "`uniq` 는 붙어 있는 것만 접는다", "`sed` 는 `g` 가 없으면 첫 하나만"] } },

    { t: "프로세스 — 상태 · 부모 · 시그널", n: 5,
      th: { sum: "프로세스를 다루는 명령들은 전부 **상태 한 글자와 부모 하나**를 중심으로 돌아간다. 그 둘만 읽을 줄 알면 대부분의 상황이 설명된다.",
        body: [
          { h: "D 와 Z 는 다른 이야기다", t: "대부분의 프로세스는 `S` — 무언가를 기다리는 중이고 정상이다. `D`(중단 불가)는 **시그널로도 깨울 수 없어** `kill -9` 조차 듣지 않으며, 대개 디스크나 네트워크 파일 시스템이 응답하지 않는다는 뜻이다. 프로세스를 죽이려 애쓸 자리가 아니라 저장 장치를 볼 자리다. `Z`(좀비)는 이미 끝난 프로세스가 **부모가 종료 코드를 거둬 가기를** 기다리는 상태라, 쌓인다면 부모가 제 일을 안 하고 있다는 신호다." },
          { h: "부모가 죽으면 1번이 거둔다", t: "부모가 먼저 죽어도 자식은 사라지지 않고 **init(pid 1)** 이 거둔다. 터미널을 닫아도 `nohup` 으로 띄운 것이 계속 도는 이유이며, 동시에 좀비를 거둘 사람이 언제나 있게 하는 장치다. 컨테이너의 전형적인 실수가 여기서 나온다 — 셸 스크립트를 pid 1 로 띄우면 시그널도 전달하지 않고 자식도 거두지 않아, `docker stop` 이 유예 시간을 다 기다렸다 강제 종료한다. `KILL`(9)만 잡을 수 없다는 성질이 이 모든 이야기의 바닥에 있다." }],
        code: { c: "if (policy === \"always\") return true;\nif (policy === \"on-failure\") return code !== 0;\nreturn false;   // 137 = 128+9 — KILL 로 죽었다", cap: "재시작 정책은 한 줄짜리 판단이다" },
        key: ["`D` 는 kill -9 도 안 듣는다", "좀비는 부모의 게으름", "고아는 pid 1 이 거둔다", "`KILL` 만 잡을 수 없다"] } },

    { t: "파일 시스템 — 크기 · 링크 · 아카이브", n: 5,
      th: { sum: "디스크가 찼을 때 하는 일은 정해져 있다 — **큰 것부터 찾고, 왜 안 줄어드는지 본다.** 두 번째 질문의 답은 대개 inode 에 있다.",
        body: [
          { h: "사람이 읽는 값은 정확하지 않다", t: "`ls -lh` 의 `1.5K` 는 1536 일 수도 1540 일 수도 있다. 그래서 크기를 견주거나 더할 때는 **줄이기 전의 바이트**로 해야 한다. 1024 냐 1000 이냐도 계속되는 다툼이라, `-h` 는 1024 를 `--si` 는 1000 을 쓰고 디스크 제조사는 1000 을 쓴다. 1TB 를 샀는데 931GB 로 보이는 차이가 여기서 나온다. 크기순 정렬에도 같은 함정이 있어, `-h` 로 줄인 값을 그냥 정렬하면 `9K` 가 `1G` 보다 크게 잡힌다." },
          { h: "지웠는데 안 줄어드는 두 가지", t: "파일의 진짜 이름은 **inode 번호**이고 경로는 거기 붙인 이름표다. 하드 링크가 남아 있으면 링크 수가 0이 아니라 파일이 살아 있고, 이것이 첫 번째 이유다. 두 번째는 **지운 파일을 아직 열고 있는 프로세스** — `lsof +L1` 로 찾아 재시작해야 공간이 돌아온다. 아카이브를 다룰 때는 반대 방향의 조심이 필요하다 — 남이 만든 것을 그대로 풀면 절대 경로나 `..` 로 시스템 파일을 덮어쓸 수 있으므로, 언제나 `tar -tf` 로 **목록을 먼저 본다.**" }],
        code: { c: "// 남의 배열을 그 자리에서 정렬하지 않는다\nreturn files.slice()\n  .sort((a, b) => b[1] - a[1])\n  .slice(0, n).map((f) => f[0]);", cap: "복사한 뒤에 정렬한다" },
        key: ["사람이 읽는 값으로 계산하지 않는다", "진짜 이름은 inode 번호", "링크가 남으면 공간도 남는다", "풀기 전에 목록을 먼저 본다"] } },

    { t: "네트워크와 서비스 — 누가 · 언제 · 얼마나", n: 5,
      th: { sum: "'주소가 이미 사용 중', '가끔 느리다', '접속이 몰린다' — 세 가지 흔한 질문은 각각 **상태 · 구간 차이 · 겹침**을 세는 계산으로 답한다.",
        body: [
          { h: "포트는 상태와 함께 본다", t: "`ss -ltnp` 에서 같은 포트 번호가 잔뜩 보여도 대부분은 `ESTAB` — **이미 맺어진 연결**이지 듣고 있는 소켓이 아니다. 상태를 안 보면 엉뚱한 프로세스를 죽인다. 포트가 비어 보이는데도 못 여는 경우는 `TIME_WAIT` 로 남아 있거나 1024 미만 포트를 일반 사용자가 여는 것이고, 후자는 **권한** 문제라 프로세스를 찾아도 소용없다. 프로세스 이름 칸이 비었다면 관리자 권한부터 의심한다." },
          { h: "누적을 차이로 바꿔 읽는다", t: "`traceroute` 의 숫자는 **누적**이라 그 자체로는 뜻이 적고, 앞 홉과의 **차이**가 그 구간의 시간이다. 중간에서 튀었다가 다음 홉에서 낮아지는 것은 무시해도 된다 — 라우터는 지나가는 패킷을 먼저 처리하고 자기에게 온 응답은 뒤로 미룬다. 진짜 신호는 **튄 값이 끝까지 이어지는 것**이다. 동시 접속도 같은 성격의 변환이다 — 구간을 **시작 +1 · 끝 -1 인 사건**으로 바꾸면 정렬 한 번으로 최대 겹침이 나와, 로그 수백만 줄에서도 쓸 수 있다." }],
        code: { c: "// 구간을 사건으로 바꾼다 — 같은 시각이면 끝을 먼저\nev.sort((a, b) => (a[0] - b[0]) || (a[1] - b[1]));\nev.forEach((e) => { cur += e[1]; if (cur > top) top = cur; });", cap: "겹침은 훑기 한 번이면 된다" },
        key: ["포트는 상태와 함께 본다", "누적을 차이로 바꿔 읽는다", "중간 홉의 튐은 대개 무시", "구간 문제는 사건으로 바꾼다"] } },
  ],
};
