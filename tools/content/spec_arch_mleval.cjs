module.exports = {
  track: "mleval", guide: "", xp: 80,
  unit: "설계 · 직접 그리는 평가 파이프라인",
  source: "./arch_mleval.cjs",
  lessons: [
    { t: "재는 장치를 설계한다", n: 5,
      th: { sum: "평가가 **보고서**이면 사람이 읽고 넘어가고, **관문**이면 통과하지 못한 모델이 배포되지 않는다.",
        body: [
          { h: "무엇으로 막는가", t: "전체 평균은 소수 집단이 무너져도 거의 움직이지 않으므로 <b>슬라이스별 하한</b>을 함께 건다. 비교 대상은 직전 승격 모델의 점수이고, 평가 데이터는 <b>버전으로 고정</b>해야 어제와 오늘을 견줄 수 있다. 실행 로그에 실제 추론 건수를 남겨 캐시가 옛 예측을 돌려주는 일을 잡는다." },
          { h: "실제 트래픽으로 재는 순서", t: "오프라인 다음은 <b>섀도</b>(응답은 버리고 지표만), 그다음 <b>카나리</b>(작은 트래픽), 마지막이 전면이다. A/B 는 배정을 결정적으로 하고 <b>의도 기준</b>으로 집계하며, 표본과 기간을 미리 정해 중간에 멈추지 않는다. 라벨이 늦게 오면 성숙한 코호트만 지표를 확정한다." }],
        code: { c: "gate: 전체 ≥ base − 0.002  AND  모든 슬라이스 ≥ base − 0.01\nSRM p < 0.001 → 결과를 읽기 전에 파이프라인부터\n라벨 도착률 < 90% 코호트는 미확정", cap: "통과 기준을 먼저 정한다" },
        key: ["슬라이스 하한을 관문에 넣는다", "평가 데이터는 버전 고정", "섀도 → 카나리 → 전면", "덜 모인 라벨은 확정하지 않는다"] } },
  ],
};
