module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 트리와 힙",
  source: "./algo_tree.cjs",
  lessons: [
    { t: "트리 재귀 — 멈추는 자리와 합치는 자리", n: 4,
      th: { sum: "트리 재귀는 늘 같은 세 줄이다. **어디서 멈추나**, **자식에게 무엇을 묻나**, **그 답을 어떻게 합치나**. 이 셋을 말로 못 하면 코드도 안 나온다.",
        body: [
          { h: "잎이 아니라 null 에서 멈춘다", t: "'잎에 도착하면 멈춘다' 로 짜면 자식이 하나뿐인 노드에서 없는 쪽을 파고들다 터진다. **`null` 에서 0을 돌려주면** 그런 분기가 통째로 사라진다. 예외는 '뿌리에서 잎까지' 처럼 **경로의 끝이 잎이어야만** 하는 문제인데, 그때는 `null` 에서 멈추면 없는 자식도 경로로 세어 조용히 틀린다. 무엇을 세는지에 따라 멈추는 자리가 달라진다." },
          { h: "돌려주는 값과 구하는 값이 다를 수 있다", t: "지름 문제에서 재귀가 돌려주는 것은 '내 아래로의 깊이' 인데 답은 '나를 꼭짓점으로 하는 경로 길이' 다. 그래서 **답은 바깥 변수에 적어 두고** 부모에게는 깊이만 돌려준다. 반대로 조상 최댓값처럼 **위에서 아래로 흐르는 정보**는 인자로 물려줘야 한다 — 바깥 변수에 두면 형제 가지끼리 오염된다. 방향을 먼저 정하는 것이 트리 재귀의 절반이다." }],
        code: { c: "if (!node) return 0;\nconst a = go(node.l), b = go(node.r);\nif (a + b > best) best = a + b;   // 답은 밖에\nreturn 1 + Math.max(a, b);        // 부모에겐 깊이만", cap: "두 값을 헷갈리면 트리가 아닌 것이 나온다" },
        key: ["null 에서 멈춘다", "경로 문제만 잎에서 멈춘다", "아래→위는 반환값", "위→아래는 인자"] } },
    { t: "순회와 BST — 순서가 곧 성질이다", n: 4,
      th: { sum: "순회 순서는 취향이 아니다. **중위는 정렬**, **층별은 거리**다. 어떤 순서로 보느냐가 어떤 문제를 풀 수 있는지를 정한다.",
        body: [
          { h: "재귀를 반복으로 바꾸면 스택이 보인다", t: "중위 순회를 스택으로 짜면 '값을 아직 안 꺼낸, 되돌아와야 할 노드' 가 스택에 쌓이는 것이 눈에 보인다. 바깥 조건이 `node || stack.length` 인 이유도 그때 이해된다 — **손에 든 노드**와 **되돌아갈 곳** 중 하나만 있어도 아직 끝이 아니다. 층별 순회에서는 반대로 현재 층과 다음 층을 따로 두어 **층의 경계**를 잃지 않는 것이 요점이다." },
          { h: "부모-자식만 보면 BST 판정이 틀린다", t: "각 노드에서 자기 자식 둘만 비교하면 왼쪽 부분 트리 깊숙이 숨은 큰 값을 못 잡는다. 올바른 판정은 내려가면서 **허용 범위**를 좁혀 물려주는 것이다. 범위 밖의 값을 만나면 거짓. 같은 문제를 '중위 순회가 오름차순인가' 로도 풀 수 있다. BST 에서는 값 비교만으로 방향을 알 수 있어 **최소 공통 조상**도 재귀 없이 한 줄기 반복으로 끝난다." }],
        code: { c: "return go(node.l, lo, node.v)\n    && go(node.r, node.v, hi);", cap: "내려갈수록 범위는 좁아지기만 한다" },
        key: ["중위 = 오름차순", "층별 = 가까운 것부터", "BST 판정은 범위를 물려준다", "없음을 큰 수로 흉내내지 않는다"] } },
    { t: "힙 — 배열 하나로 만드는 우선순위", n: 4,
      th: { sum: "힙은 자료 구조가 아니라 **인덱스 규칙**이다. 자식은 `2i+1`·`2i+2`, 부모는 `(i-1)>>1`. 포인터 없이 배열 하나로 트리를 다룬다.",
        body: [
          { h: "올려보내기와 내려보내기 두 개면 끝", t: "넣을 때는 맨 뒤에 붙이고 부모보다 작으면 **올려보낸다**. 뺄 때는 꼭대기를 답으로 쓰고 **맨 끝을 끌어올려 내려보낸다**. 힙을 처음 만들 때 중간부터 거꾸로 내려보내면 O(n) 이고, 하나씩 넣으며 올려보내면 O(n log n) 이다. 실수가 몰리는 곳은 뺄 때의 마지막 원소 처리와, **비교 방향**이다 — 최대 힙과 최소 힙은 부등호 하나 차이라 조용히 반대 답을 준다." },
          { h: "정렬보다 우선순위 큐로 더 많이 쓴다", t: "힙 정렬은 추가 메모리가 상수이고 최악에도 O(n log n) 이지만 안정 정렬이 아니고 실제 속도는 퀵 정렬보다 느려, 현실에서는 **우선순위 큐**로 쓰인다. 상위 k 개를 지킬 때 최소 힙을 쓰는 것이 대표적이다 — 꼭대기가 곧 'k등' 이라 새 값과 한 번만 비교하면 되고, 메모리가 k 에만 비례해 **다 담을 수 없는 스트림**에서도 동작한다. 다익스트라·작업 스케줄러·이벤트 루프가 전부 이 위에 있다." }],
        code: { c: "heap[0] = heap.pop();   // 끝을 끌어올려\ndown();                 // 제자리로 내려보낸다", cap: "뺄 때 실수가 몰리는 두 줄" },
        key: ["부모 (i-1)>>1 · 자식 2i+1", "만들 때는 중간부터 거꾸로 O(n)", "상위 k 개 → 크기 k 최소 힙", "최대/최소는 부등호 하나 차이"] } },
  ],
};
