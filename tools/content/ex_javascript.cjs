/* javascript 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "javascript", fixes: [
  { k: "빈칸", ex0: "console.log(\"hi\") 다", ex: "콘솔에 값을 찍는 것은 console 객체의 log 메서드라 console.log(\"hi\") 다. print 는 브라우저에서 인쇄 대화상자를 여는 window.print 이고, echo 나 write 라는 메서드는 console 에 없다. 점(.) 뒤에 오는 메서드 이름을 정확히 써야 한다." },
  { k: "문장 끝", ex0: "세미콜론(;) 을 붙이는 것이 관례", ex: "자바스크립트는 문장 끝에 세미콜론(;)을 붙이는 것이 관례다. 생략해도 자동 삽입 규칙(ASI) 덕에 대개 돌아가지만 괄호나 대괄호로 시작하는 다음 줄과 붙어 버리는 함정이 있다. 마침표는 속성 접근, 콜론은 객체 키와 값 사이, # 은 private 필드 표시에 쓴다." },
  { k: "개념", ex0: "console.log() 로 콘솔에 출력", ex: "콘솔 출력은 console.log() 다. print() 는 Python 의 것이고 브라우저에서는 인쇄창을 띄우며, echo() 는 PHP 나 셸의 명령이다. printf() 는 C 의 함수라 자바스크립트에는 없다. 디버깅 때 값을 확인하는 가장 기본 도구다." },
  { k: "출력 결과", ex0: "숫자끼리 더하면 3", ex: "1 + 2 는 둘 다 숫자라 산술 덧셈이 되어 3 이다. \"1\" + 2 처럼 한쪽이 문자열이면 이어붙여 \"12\" 가 되니 타입을 봐야 한다. 오류는 나지 않고, 1+2 라는 글자가 그대로 찍히려면 따옴표로 감싸 문자열로 줬어야 한다." },
  { k: "문자열 합치기", ex0: "문자열 + 는 이어붙이기", ex: "문자열에 + 를 쓰면 이어붙이기라 \"a\" + \"b\" 는 \"ab\" 다. 공백은 저절로 들어가지 않아 \"a b\" 가 아니고, 글자를 숫자로 바꾸려 시도하지도 않아 2 가 되거나 오류가 나지 않는다. 숫자와 문자열이 섞여도 문자열 쪽으로 붙는다." },
  { k: "빈칸", ex0: "let age = 20; 형태", ex: "값이 바뀔 수 있는 변수는 let age = 20; 으로 만든다. const 는 재할당이 막히고, var 는 옛 방식이라 함수 범위와 호이스팅 문제가 있어 새 코드에서는 피한다. 키워드 없이 age = 20 만 쓰면 전역에 새는 암묵적 선언이 되어 strict 모드에서는 오류다." },
  { k: "타입 확인", ex0: "typeof x 로 자료형을 확인", ex: "값의 자료형은 typeof 연산자로 알아낸다. typeof 42 는 \"number\", typeof \"a\" 는 \"string\" 을 돌려준다. type() 은 Python 함수이고, kind 와 class 는 자료형 조회에 쓰이지 않는다(class 는 클래스 선언 키워드다). typeof null 이 \"object\" 인 점은 유명한 예외다." },
  { k: "개념", ex0: "let 은 재할당 가능한 변수", ex: "값을 바꿀 변수는 let 으로 선언한다. const 는 한 번 넣으면 재할당이 안 되고, fix 와 val 은 자바스크립트 키워드가 아니다(val 은 Kotlin 의 것이다). 바뀌지 않을 값은 const, 바뀔 값만 let 으로 두는 것이 요즘 관례다." },
  { k: "상수", ex0: "const 는 재할당 불가한 상수", ex: "한 번 정하고 바꾸지 않을 값은 const 로 선언한다. 다시 대입하면 TypeError 가 난다. let 과 var 는 재할당이 되고, static 은 클래스 안에서 정적 멤버를 표시하는 키워드라 변수 선언에 쓰지 않는다. 단, const 객체의 속성은 바꿀 수 있다는 점은 기억해 두자." },
  { k: "자료형", ex0: "boolean 은 참/거짓", ex: "true 와 false 두 값만 갖는 자료형이 boolean 이다. number 는 숫자, string 은 글자 묶음이고, null 은 '값이 없음' 을 뜻하는 별도의 값이다. 조건문의 판정 결과가 boolean 이고, if (x) 처럼 다른 타입을 넣으면 truthy/falsy 규칙으로 변환된다." },
  { k: "개념", ex0: "if (조건) { ... } 형태", ex: "조건 분기는 if (조건) { ... } 이고 else 와 else if 로 다른 경우를 잇는다. case 는 switch 문 안에서만 쓰는 가지 표시이며, when 과 cond 는 자바스크립트에 없는 키워드다(Kotlin 과 Lisp 의 것이다)." },
  { k: "논리 연산", ex0: "&& 는 AND, || 는 OR", ex: "'둘 다 참' 은 && (AND) 다. || 는 둘 중 하나만 참이어도 참인 OR 이고, ! 는 NOT 이다. and 라는 단어 연산자는 Python 의 것이라 자바스크립트에서는 오류다. && 는 왼쪽이 거짓이면 오른쪽을 평가하지 않는 단축 평가를 한다." },
  { k: "엄격한 비교", ex0: "=== 는 값+타입을 엄격히 비교", ex: "=== 는 값과 타입을 모두 비교한다. 1 === \"1\" 은 false 다. == 는 타입을 억지로 맞춘 뒤 비교해서 1 == \"1\" 이 true 가 되므로 예상 밖 결과를 낳는다. = 는 대입이지 비교가 아니고, Object.is() 는 NaN 과 -0 처리만 다른 특수 비교라 일반 권장 연산자는 아니다." },
  { k: "화살표 함수", ex0: "const f = () => { } 형태", ex: "ES6 화살표 함수는 => 를 쓴다. const f = (a, b) => a + b 처럼 짧게 적을 수 있고 this 를 바깥에서 물려받는다. -> 는 C 의 포인터 접근, :: 는 다른 언어의 범위 연산자, >> 는 비트 이동 연산자라 함수 정의와 무관하다." },
  { k: "빈칸", ex0: "return x; 다", ex: "함수가 결과를 돌려주려면 return x; 를 쓴다. return 을 만나면 함수가 그 자리에서 끝나고 값이 호출한 쪽으로 간다. return 이 없거나 값 없이 return; 만 쓰면 undefined 가 돌아온다. 화살표 함수의 한 줄 표현식은 return 이 생략된 형태다." },
  { k: "개념", ex0: "function 이름() { } 로 정의", ex: "함수는 function 이름() { } 으로 정의한다. def 는 Python, fn 은 Rust, func 는 Go 와 Swift 의 키워드라 자바스크립트에서는 오류다. 화살표 함수 (() => {}) 는 function 없이 쓰는 또 다른 방법이다." },
  { k: "반환", ex0: "return 값; 으로 결과를 돌려준다", ex: "값을 돌려주는 키워드는 return 이다. yield 는 제너레이터 함수에서 값을 하나씩 내보낼 때 쓰는 것이라 일반 함수에서는 오류이고, give 와 out 은 키워드가 아니다. return 뒤에 값을 안 쓰면 undefined 가 돌아간다." },
  { k: "추가", ex0: "arr.push(값) 으로 끝에 추가", ex: "배열 끝에 값을 넣는 것은 push() 다. arr.push(4) 는 원본을 바꾸고 새 길이를 돌려준다. append() 는 Python 리스트, add() 는 Set 의 메서드이고, insert() 는 자바스크립트 배열에 없다(중간에 넣으려면 splice 를 쓴다)." },
  { k: "길이", ex0: "arr.length 로 개수를 얻는다", ex: "배열의 요소 개수는 arr.length 속성이다. 괄호 없이 쓴다. len(arr) 은 Python, size 는 Java 컬렉션이나 Map/Set 의 것이고, count() 라는 메서드는 배열에 없다. length 는 마지막 인덱스 + 1 이라 arr[arr.length - 1] 이 마지막 요소다." },
  { k: "개념", ex0: "배열은 [1,2,3] 처럼 대괄호로", ex: "여러 값을 순서대로 담는 자료형은 배열이고 [1, 2, 3] 처럼 대괄호로 만든다. 객체 {} 는 키와 값의 쌍을 담아 순서보다 이름으로 찾는 구조다. 문자열은 글자의 나열이지만 요소를 바꿀 수 없고, 숫자는 값 하나다." },
  { k: "인덱스", ex0: "인덱스는 0부터라 arr[1] 은 20", ex: "배열 인덱스는 0 부터 시작하므로 arr[0] 이 10, arr[1] 이 20, arr[2] 가 30 이다. 1 은 인덱스 자체이지 값이 아니고, 30 은 arr[2] 다. 범위를 벗어난 arr[3] 은 오류 대신 undefined 를 돌려준다." },
  { k: "length", ex0: "hello는 5글자", ex: "문자열의 length 는 글자 수를 돌려주므로 \"hello\" 는 5 다. 마지막 인덱스는 4 이고 length 는 그보다 1 크다. 6 은 널 종료 문자를 세는 C 식 생각이고, hello 라는 값 자체는 length 가 아니라 문자열 그대로다. 괄호 없이 속성으로 읽는다." },
  { k: "출력 결과", ex0: "toLowerCase()는 모든 문자를 소문자로", ex: "toLowerCase() 는 모든 글자를 소문자로 바꾼 새 문자열을 돌려주므로 \"HELLO\" 는 hello 가 된다. 원본 문자열은 바뀌지 않는다(문자열은 불변이다). 반대로 toUpperCase() 는 대문자로 바꾼다. 대소문자 구분 없이 비교할 때 자주 쓴다." },
  { k: "메서드", ex0: "toUpperCase()는 문자열을 모두 대문자로", ex: "toUpperCase() 는 모든 글자를 대문자로 바꾸므로 \"abc\" 는 \"ABC\" 가 된다. 첫 글자만 바꾸는 메서드는 따로 없어 \"Abc\" 가 나오지 않고, 원본이 그대로 나오는 \"abc\" 는 반환값을 안 쓴 경우다. 문자열에 정의된 메서드라 에러도 아니다." },
  { k: "메서드", ex0: "includes()는 문자열에 특정 값이 포함되면", ex: "includes() 는 인자가 문자열 안에 들어 있으면 true, 없으면 false 를 돌려준다. \"hello world\" 에 \"world\" 가 있으므로 true 다. 위치를 숫자로 알려 주는 것은 indexOf 라 0 이 아니고, 찾은 조각 \"world\" 를 돌려주지도 않는다." },
  { k: "개념", ex0: "찾는 값이 없으면 indexOf는 -1", ex: "indexOf 는 찾은 위치를 돌려주고 못 찾으면 -1 을 돌려준다. 0 은 '첫 번째 자리에서 찾았다' 는 뜻이라 없음과 구별하기 위해 -1 을 쓴다. null 이나 undefined 를 돌려주지 않으므로 if (s.indexOf(x) !== -1) 처럼 검사한다." },
  { k: "출력 결과", ex0: "${ } 안에는 표현식을 넣을 수 있어 2+3인 5", ex: "백틱 문자열의 ${ } 안에는 변수뿐 아니라 어떤 표현식이든 넣을 수 있어 ${2 + 3} 은 계산 결과 5 로 바뀐다. 따옴표 문자열에 ${ } 를 쓰면 그대로 글자로 나오므로 백틱인지 확인해야 한다. \"2+3\" 이라는 글자가 아니라 값이 들어간다." },
  { k: "개념", ex0: "백틱 문자열 안에서 ${ } 안에 표현식을", ex: "템플릿 리터럴은 백틱(`)으로 감싸고 ${변수} 로 값을 끼워 넣는다. #{} 는 Ruby, %변수% 는 Windows 배치, {{변수}} 는 Handlebars 나 Vue 템플릿의 문법이라 자바스크립트 문자열 안에서는 글자 그대로 남는다." },
  { k: "개념", ex0: "콜백은 나중에 호출되도록 다른 함수에 전달", ex: "다른 함수의 인자로 넘겨져 나중에 호출되는 함수가 콜백이다. setTimeout(fn, 1000) 의 fn 이 그렇다. 즉시 실행 함수는 정의하자마자 스스로 실행되는 것이고, 생성자 함수는 new 로 객체를 만드는 함수이며, 익명 클래스는 자바스크립트에서 쓰는 말이 아니다." },
  { k: "메서드", ex0: "catch는 rejected 상태의 에러를 처리", ex: "Promise 가 거부되면 .catch() 에 넘긴 함수가 에러를 받아 처리한다. .then() 은 성공(fulfilled)했을 때 값을 받고, .resolve() 는 Promise 를 성공 상태로 만드는 정적 메서드이며, .all() 은 여러 Promise 를 묶어 기다리는 것이다. async/await 에서는 try/catch 가 같은 역할이다." },
  { k: "출력 결과", ex0: "6은 인덱스 1에 있다", ex: "배열의 indexOf 도 문자열처럼 값이 있는 위치를 돌려준다. 6 이 두 번째 자리에 있으므로 인덱스는 1 이다. 인덱스가 0 부터 시작하는 점을 놓치면 2 라고 답하기 쉽다. 값이 없으면 -1 이 나온다." },
  { k: "메서드", ex0: "includes는 배열에 값이 있으면 true", ex: "배열의 includes() 는 값이 들어 있는지를 true/false 로 알려 준다. [1, 2, 3] 에 2 가 있으므로 true 다. 위치를 알려 주는 것은 indexOf(2) 이고 그 값이 1 이다. 찾은 값 2 자체를 돌려주지 않고, 있는데 false 가 나올 수도 없다." },
  { k: "개념", ex0: "sort는 원본 배열 자체를 정렬(변경)하고", ex: "sort 는 원본 배열을 제자리에서 정렬하고 그 배열을 돌려준다. 새 배열을 돌려주고 원본을 보존하는 것은 toSorted 나 [...arr].sort() 다. 문자열도 숫자도 정렬할 수 있지만, 비교 함수 없이 숫자를 정렬하면 문자열로 바꿔 비교해 [10, 9, 1] 같은 엉뚱한 순서가 나온다." },
  { k: "개념", ex0: "reduce(콜백, 초기값)에서 두 번째 인자는", ex: "reduce(콜백, 초기값) 에서 두 번째 인자는 누적값(accumulator)의 시작값이다. 생략하면 배열의 첫 요소가 초기값이 되고 빈 배열에서는 오류가 난다. 콜백은 첫 번째 인자이고, 인덱스와 배열은 콜백 함수가 받는 매개변수이지 reduce 의 인자가 아니다." },
  { k: "개념", ex0: "spread 연산자는 배열 요소를 펼쳐 얕은 복사본", ex: "[...arr] 은 전개(spread) 연산자로 arr 의 요소를 하나씩 펼쳐 새 배열에 담는다. 그래서 원본과 다른 배열이 생기는 얕은 복사가 된다. 정렬하거나 길이를 돌려주지 않고, 문자열로 합치려면 join() 을 써야 한다. 함수 호출 f(...arr) 에도 같은 기호를 쓴다." },
  { k: "라이브", ex0: "반복 횟수만큼 줄이 출력돼요", ex: "반복문이 도는 횟수만큼 console.log 가 실행되어 줄이 찍힌다. 지금은 5 번 돌아 5 줄이 나오고, 5 를 10 으로 바꾸면 10 줄이 된다. 조건이 끝나면 멈추므로 무한이 아니고, 한 번만 도는 코드도 아니다. 반복 조건의 숫자가 출력 줄 수를 결정한다." },
  { k: "라이브", ex0: "따옴표 안 글자를 바꾸면 결과가 바로", ex: "console.log 는 괄호 안의 값을 콘솔 화면에 출력한다. 따옴표 안 글자를 바꾸면 찍히는 내용이 바로 바뀐다. 파일에 저장하거나 화면을 지우는 일은 하지 않고, 계산만 하고 숨기는 것도 아니다. 값이 어떻게 흘러가는지 눈으로 확인하는 가장 기본 도구다." },
  { k: "라이브", ex0: "변수 name 과 글자가 이어붙어 출력", ex: "name 에 든 값과 뒤의 글자가 + 로 이어붙어 '코딩 최고' 처럼 출력된다. 변수 이름 name 이 아니라 그 안의 값이 들어가므로 'name 최고' 가 아니고, 본인 이름으로 바꾸면 그 이름 뒤에 '최고' 가 붙는다. 문법이 맞으니 오류도 나지 않는다." }
] };
