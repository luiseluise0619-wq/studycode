module.exports = {
  track: "backend", guide: "", xp: 70,
  unit: "로그 분석 II — API·DB·큐의 경계",
  source: "./log_backend.cjs",
  lessons: [
    { t: "데이터베이스가 느려지는 이유", n: 5,
      th: { sum: "느린 쿼리가 없는데 DB 가 아프다면, **횟수**·**행 수**·**붙잡은 시간**을 본다.",
        body: [
          { h: "횟수와 행 수", t: "1ms 쿼리 101번은 슬로우 쿼리 로그에 안 잡히지만 요청을 420ms 로 만듭니다(N+1). 인덱스 없는 조건은 행이 40배 늘면 시간도 그만큼 늘고, 큰 스캔은 <b>버퍼 캐시까지 밀어냅니다</b>. 요청당 쿼리 수와 실행 계획을 지표로 두면 둘 다 미리 보입니다." },
          { h: "붙잡은 시간", t: "트랜잭션 안에서 외부 API 를 부르면 커넥션이 그 시간만큼 묶여, 상류 지연 하나가 서비스 전체를 멈춥니다. 큰 표를 다시 쓰는 마이그레이션도 같은 종류입니다 — <b>강한 잠금을 오래</b> 잡습니다." }],
        code: { c: "WHERE id IN (...)            -- 한 번에\nCREATE INDEX ... (status, created_at)\n외부 호출은 커밋 뒤에", cap: "짧게 잡고 빨리 놓는다" },
        key: ["빠른 쿼리도 백 번이면 느리다", "인덱스는 행 수가 늘 때 드러난다", "트랜잭션에 외부 호출 금지", "마이그레이션은 단계로"] } },
    { t: "한 번만 일어나게 만들기", n: 5,
      th: { sum: "분산된 곳에서는 **두 번 오는 것**과 **한 번도 안 오는 것**을 모두 설계해야 한다.",
        body: [
          { h: "두 번 와도 한 번", t: "응답을 못 받은 클라이언트는 반드시 다시 보냅니다. 멱등 키가 없으면 그대로 중복 주문이 되고, 읽고 계산해 쓰는 갱신은 동시에 오면 하나가 사라집니다(잃어버린 갱신). <b>DB 가 계산하게</b> 하거나 잠금·버전으로 지킵니다." },
          { h: "빠지지 않게", t: "커밋과 메시지 발행은 원자적이지 않아 이벤트만 사라질 수 있고(아웃박스로 해결), 데드레터 큐가 없으면 고칠 수 없는 메시지 하나가 줄 전체를 막습니다. 페이지네이션의 <code>OFFSET</code> 은 데이터가 움직이면 <b>중복과 누락</b>을 만듭니다." }],
        code: { c: "INSERT ... ON CONFLICT (idem_key) DO NOTHING\nUPDATE stock = stock - 1 WHERE stock > 0\nWHERE (created_at, id) < (?, ?)  -- 커서", cap: "재시도와 유실을 전제로 짠다" },
        key: ["멱등 키로 중복을 막는다", "갱신은 DB 가 계산하게", "이벤트는 아웃박스로", "커서 페이지네이션"] } },
  ],
};
