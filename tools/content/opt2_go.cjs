/* go 트랙 — 정답이 5자 이상 단독 최장인 64문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "go", fixes: [
  { k: "go2-md-04", o: [
    "각 모듈에 대해 요구된 버전 중 가장 낮은 버전을 골라, 어떤 의존성이든 최소한의 기능만 쓰도록 강제한다",
    "각 모듈에 대해 요구된 여러 버전 중 가장 높은 버전을 선택하되, 아무도 요구하지 않은 더 새로운 버전으로는 올리지 않는다",
    "각 의존성의 최신 릴리스를 프록시에서 조회해 자동으로 선택하므로 빌드할 때마다 가장 새 코드가 들어온다",
    "go.sum 에 기록된 순서를 따라 처음 나오는 버전을 선택하고, 나머지 요구는 무시하므로 go.sum 순서가 결과를 정한다" ] },
  { k: "go3-scanner-limit", o: [
    "필요한 만큼 내부 버퍼를 자동으로 늘려 정상적으로 읽으며, 메모리만 그만큼 더 쓸 뿐 동작은 달라지지 않는다",
    "기본 한계를 넘는 줄은 앞부분만 잘라 반환하고 나머지는 버린 뒤 다음 줄로 계속 진행하므로 데이터가 조용히 사라진다",
    "기본 토큰 한계(약 64KB)를 넘어 Scan이 false를 반환하며 멈추고 Err()에 에러가 담긴다",
    "버퍼 크기를 넘는 순간 bufio 패키지가 런타임 패닉을 일으켜 프로그램이 스택 추적과 함께 종료된다" ] },
  { k: "go2-cp-02", o: [
    "close(jobs)가 없어 워커들의 range가 끝나지 않고, wg.Wait()이 영원히 기다리기 때문",
    "버퍼 없는 채널은 송신자와 수신자가 만나야만 진행되므로 워커 풀에서는 반드시 버퍼를 줘야 하기 때문",
    "고루틴 수보다 작업 수가 많아 남는 작업을 받을 워커가 없으므로 송신이 채널에서 영원히 막히기 때문",
    "wg.Add 를 고루틴 안에서 호출해 Wait 가 카운터 0 을 보고 먼저 지나가 버린 뒤 Done 이 음수를 만들기 때문" ] },
  { k: "go2-ei-03", o: [
    "임베딩된 인터페이스의 메서드가 자동으로 빈 구현으로 채워져, 값을 넣지 않아도 호출이 아무 일 없이 돌아온다",
    "인터페이스는 구조체에 임베딩할 수 없고 메서드를 승격시킬 대상이 없다는 이유로 컴파일 에러가 난다",
    "구조체가 해당 인터페이스를 만족하게 되지만, 임베딩된 값이 nil인 채로 메서드를 호출하면 런타임 panic이 난다",
    "임베딩된 인터페이스가 nil 이면 호출이 조용히 무시되고 반환값 자리에는 제로값이 들어가므로 panic 은 나지 않는다" ] },
  { k: "go-cc-closed-recv", o: [
    "1이 출력된 뒤 0 false가 출력된다 — 버퍼를 비운 후에는 제로값과 false를 준다",
    "1 true 가 두 번 연달아 출력된다 — 닫힌 뒤에도 마지막 값이 버퍼에 남아 계속 읽힌다",
    "첫 수신에서 즉시 패닉이 발생한다 — 닫힌 채널은 송신뿐 아니라 수신도 허용하지 않는다",
    "1이 출력된 뒤 두 번째 수신에서 영원히 블록된다 — 보낼 고루틴이 없으므로 기다린다" ] },
  { k: "go2-hd-04", o: [
    "WriteHeader 이후의 헤더 변경은 무시되므로 설정되지 않고, Go가 본문을 보고 추측한 타입이 나간다",
    "헤더 맵은 응답이 끝날 때 한꺼번에 직렬화되므로 application/json 이 정상적으로 설정되어 나간다",
    "WriteHeader 전에 자동 추측된 타입과 뒤에 설정한 타입이 모두 남아 Content-Type 헤더가 두 번 전송된다",
    "이미 전송된 헤더를 바꾸려는 시도라 net/http 가 런타임 panic 을 일으키고 연결이 끊긴다" ] },
  { k: "go-panic-unwind", o: [
    "역순으로 모두 실행되며 콜 스택을 거슬러 올라가면서 각 함수의 defer도 실행된다",
    "같은 함수 안에 recover 가 있을 때에만 실행되고, 없으면 되감기 없이 프로세스가 곧바로 종료된다",
    "등록한 순서대로 실행되며, 패닉을 일으킨 함수의 defer 만 돌고 상위 함수의 defer 는 건너뛴다",
    "패닉은 정상 반환이 아니므로 어떤 defer 도 실행되지 않고 스택 추적만 출력한 뒤 종료된다" ] },
  { k: "go2-ec-01", o: [
    "A 와 B 모두 {\"meta\":{\"id\":1},\"name\":\"x\"} 로 똑같이 나온다 — 임베딩은 직렬화 결과에 영향을 주지 않는다",
    "A는 {\"id\":1,\"name\":\"x\"} 로 평탄화되고, B는 {\"meta\":{\"id\":1},\"name\":\"x\"} 로 중첩된다",
    "A 는 임베딩 필드에 이름이 없어 json 이 무시하므로 {\"name\":\"x\"} 만 나오고, B 는 중첩으로 나온다",
    "둘 다 {\"id\":1,\"name\":\"x\"} 로 평탄화된다 — json 은 구조체 필드를 모두 상위로 끌어올린다" ] },
  { k: "go2-in-04", o: [
    "main 패키지에서만 정의할 수 있고, 라이브러리 패키지에 두면 진입점이 없다는 이유로 컴파일 에러가 난다",
    "인자나 반환값을 가질 수 있고 다른 코드에서 직접 호출할 수도 있으며, 테스트에서 개별적으로 부를 수도 있다",
    "패키지 전체에 딱 하나만 정의할 수 있으므로, 파일이 여러 개면 한 파일에 모아 두고 나머지는 그것을 부른다",
    "한 패키지에 여러 개(여러 파일에 걸쳐서도) 정의할 수 있고, 인자와 반환값이 없으며 직접 호출할 수 없다" ] },
  { k: "go2-gn-06", o: [
    "타입과 무관하게 항상 nil 이며, 값 타입이면 대입할 때 변환된다",
    "타입 파라미터는 var 로 선언할 수 없어 컴파일 에러가 난다",
    "T의 제로값(int면 0, string이면 \"\", 포인터면 nil)",
    "인스턴스화 전까지는 정해지지 않은 미정의 값이라 읽으면 안 된다" ] },
  { k: "go-defer-named", o: [
    "0 — defer 가 실행될 때 명명된 반환값이 초기화되어 대입이 사라진다",
    "10 — 명명된 반환값에 5를 대입한 뒤 defer가 실행되어 두 배가 된다",
    "5 — defer 는 반환값이 확정된 뒤에 돌므로 결과를 바꿀 수 없다",
    "컴파일 에러 — defer 안에서 반환값 변수에 대입할 수 없다" ] },
  { k: "go-if-nilcase", o: [
    "인터페이스 값 자체가 nil일 때(타입과 값이 모두 비었을 때)",
    "동적 타입은 있어도 동적 값이 nil 포인터일 때",
    "어떤 값에도 선택되지 않으며 default 와 같은 자리 표시다",
    "다른 case 가 모두 맞지 않으면 마지막에 선택된다" ] },
  { k: "go2-cf-04", o: [
    "소비자가 range 를 빠져나가면 채널이 자동으로 닫히고 업스트림 송신이 실패해 모든 스테이지가 정상 종료된다",
    "받는 쪽이 사라진 채널에 송신하는 순간 업스트림에서 즉시 panic 이 발생해 파이프라인 전체가 멈춘다",
    "아무도 참조하지 않는 채널에 블록된 고루틴은 GC 가 회수하므로 Go 런타임이 알아서 종료시켜 준다",
    "업스트림 고루틴들이 아무도 받지 않는 채널에 송신하려다 영원히 블록되어 고루틴이 누수된다" ] },
  { k: "go-cc-select-default", o: [
    "준비된 케이스가 없을 때 즉시 default로 빠져 논블로킹이 된다",
    "다른 케이스보다 default 가 항상 먼저 실행되어 채널을 볼 기회가 없다",
    "다른 케이스가 모두 한 번씩 실행된 뒤에야 default 가 실행된다",
    "default 는 문서용 자리라 블로킹 동작에는 아무 영향이 없다" ] },
  { k: "go-test-helper", o: [
    "그 보조 함수 안의 검증이 다른 테스트와 병렬로 실행된다",
    "해당 함수가 테스트 함수로 등록되어 go test 목록에 나타난다",
    "실패 보고 시 이 함수가 아니라 호출한 쪽의 줄 번호가 표시된다",
    "그 함수 안에서 실패해도 테스트 전체는 통과한 것으로 기록된다" ] },
  { k: "go2-al-04", o: [
    "Pool 은 항목 수가 64개로 고정되어 있어 그보다 많이 넣으면 오래된 값부터 버려지기 때문",
    "GC가 실행될 때 Pool의 내용이 비워질 수 있어 값이 언제든 사라질 수 있기 때문",
    "Pool 에 넣은 값은 Get 할 때마다 깊은 복사가 일어나 캐시로 쓰면 오히려 느려지기 때문",
    "Pool 은 만든 고루틴에서만 접근할 수 있어 다른 고루틴의 조회는 항상 nil 을 받기 때문" ] },
  { k: "go3-fail-fast", o: [
    "기동 시점에 즉시 에러를 남기고 종료해 잘못된 상태로 트래픽을 받지 않게 한다",
    "경고만 남기고 하드코딩된 개발용 기본값으로 대체해 서비스가 멈추지 않게 한다",
    "일단 기동하고 첫 요청이 들어올 때 에러를 반환해 헬스체크는 통과시킨다",
    "빈 값을 그대로 드라이버에 넘겨 드라이버가 기본 접속 정보를 쓰도록 맡긴다" ] },
  { k: "go3-sqlopen-lazy", o: [
    "DB 서버가 살아 있고 스키마도 존재하며 접속 계정에 권한이 있다는 것까지 확인된 것이다",
    "드라이버 이름과 인자 형식이 유효하다는 것뿐이며 실제 연결은 아직 맺어지지 않았다",
    "DB 에 정상적으로 접속되었고 인증도 통과했으므로 첫 쿼리는 인증 실패 없이 실행된다",
    "커넥션 하나를 미리 만들어 풀에 넣어 두었으므로 첫 쿼리는 연결 지연 없이 실행된다" ] },
  { k: "go3-flag-parse-order", o: [
    "새 플래그가 등록되는 순간 Parse 가 다시 실행되어 명령행 값이 정상적으로 채워진다",
    "Parse 이후의 정의는 중복 정의로 간주되어 flag 패키지가 패닉을 일으킨다",
    "이미 파싱이 끝났으므로 그 플래그는 기본값만 갖고 명령행 값이 반영되지 않는다",
    "플래그 정의는 Parse 앞에만 올 수 있도록 검사되므로 컴파일 에러가 발생한다" ] },
  { k: "go-sl-retain", o: [
    "잘라낸 슬라이스가 기반 배열의 일부만 가리키므로 컴파일 시 경고가 발생한다",
    "잘라낸 슬라이스가 기반 배열 전체를 계속 참조해 큰 메모리가 회수되지 않는다",
    "원본이 회수되면서 잘라낸 슬라이스의 원소가 무작위 값으로 바뀐다",
    "원본이 먼저 해제되어 잘라낸 슬라이스가 해제된 메모리를 가리키게 된다" ] },
  { k: "go-defer-eval", o: [
    "둘 다 실행 시점의 x를 출력한다 — defer 는 호출을 통째로 미룬다",
    "둘 다 defer 문 시점의 x 를 출력한다 — 클로저도 변수를 그때 복사한다",
    "뒤쪽은 즉시 호출되어 defer 로 등록되지 않으므로 실행되지 않는다",
    "앞은 defer 문 시점의 x를, 뒤는 실행 시점의 x를 출력한다" ] },
  { k: "go2-gc-02", o: [
    "일반 변수 타입으로도 자유롭게 쓸 수 있어 var n Number 에 int 값과 float64 값을 번갈아 담을 수 있다",
    "메서드를 갖지 않으므로 any 와 동일하며, 타입 파라미터의 제약으로 쓰면 어떤 타입이든 받아들인다",
    "타입 집합을 표현하는 제약 전용 인터페이스로, 타입 파라미터의 제약 자리에서만 쓸 수 있다",
    "int 와 float64 사이의 변환을 자동으로 해 주므로 두 타입의 값을 섞어 산술 연산을 할 수 있다" ] },
  { k: "go2-ec-02", o: [
    "안쪽 타입의 모든 공개 메서드를 그대로 노출해 바깥 타입이 그 인터페이스를 자동으로 만족하게 하고 싶을 때",
    "인터페이스를 코드 없이 자동으로 만족시키고 싶을 때 — 이름 붙은 필드도 메서드를 승격시킨다",
    "안쪽 타입을 내부 구현 세부사항으로 감추고 필요한 메서드만 골라 위임하고 싶을 때",
    "필드 이름을 짧게 쓰고 싶을 때 — 임베딩은 타입 이름 전체를 필드 이름으로 써야 하기 때문" ] },
  { k: "go-sl-shared-append", o: [
    "append 가 x 의 길이를 5 로 늘려 a 와 같은 원소를 모두 담게 된다",
    "cap(x)가 5라 재할당 없이 a[2]가 99로 덮어써진다",
    "x 의 cap 을 넘어선 쓰기라 런타임 패닉이 발생한다",
    "x 는 새 배열로 복사되므로 a 는 어떤 경우에도 영향을 받지 않는다" ] },
  { k: "go2-ec-04", o: [
    "같은 이름의 메서드가 둘이라 모호하다는 이유로 즉시 컴파일 에러가 나서 빌드가 깨진다",
    "임베딩된 새 메서드가 우선해 기존 동작이 조용히 바뀌므로 상류 버전 업이 곧 버그가 된다",
    "두 메서드가 임베딩 깊이 순서대로 모두 호출되어 바깥 것과 안쪽 것이 차례로 실행된다",
    "바깥 타입의 메서드가 계속 우선하며(더 얕은 깊이) 기존 동작이 유지된다" ] },
  { k: "go2-in-06", o: [
    "패키지 이름이 main이고 그 안에 인자·반환값 없는 func main()이 있어야 한다",
    "파일 이름이 main.go 이면 패키지 이름과 무관하게 go build 가 실행 파일을 만든다",
    "패키지 이름이 cmd 이고 그 안에 대문자로 시작하는 func Main() 이 있어야 한다",
    "go.mod 에 executable = true 를 적어 두어야 빌드 결과가 실행 파일이 된다" ] },
  { k: "go-sl-nil", o: [
    "len과 cap이 0이고 s == nil이 참이지만, append와 len·range는 정상 동작한다",
    "[]int{} 와 완전히 동일한 값이라 s == nil 비교는 항상 거짓이 되고 구별할 방법이 없다",
    "기반 배열이 없으므로 append 하면 nil 포인터 역참조로 런타임 패닉이 발생한다",
    "헤더가 비어 있어 len(s) 를 호출하는 순간 패닉이 나므로 먼저 nil 검사를 해야 한다" ] },
  { k: "go2-cs-01", o: [
    "make(chan int, 5) 에 미리 5 를 보내 두고 읽지 않는다 — 버퍼가 가득 찬 상태가 동시 실행 상한이 된다",
    "make(chan struct{}) 버퍼 없는 채널을 만들고 작업이 끝날 때마다 한 번씩 닫아 다섯 번 닫히면 멈춘다",
    "고루틴 5개를 먼저 만들고 나머지 작업은 time.Sleep 으로 잠시 재웠다가 앞의 것이 끝나면 깨운다",
    "make(chan struct{}, 5) 를 만들고 작업 시작 전에 값을 보내(획득), 끝나면 받아(반납) 낸다" ] },
  { k: "go2-cs-07", o: [
    "전역 bool 변수를 락 없이 주기적으로 읽어 종료를 판단하고, 취소하는 쪽은 그 변수를 true 로 바꿔 종료 신호를 보낸다",
    "고루틴 안에서 runtime.Goexit() 을 주기적으로 호출해 컨텍스트 없이도 스스로 종료 여부를 결정하고 알아서 빠져나가게 한다",
    "상위에서 받은 context 를 select 의 &lt;-ctx.Done() 으로 함께 감시하고, 신호가 오면 정리한 뒤 return 한다",
    "고루틴 ID 를 저장해 두었다가 외부에서 runtime 의 강제 종료 함수를 호출해 그 고루틴만 골라 즉시 멈추게 만들어 준다" ] },
  { k: "go2-al-02", o: [
    "fmt.Sprintf 는 패키지 전역 버퍼를 공유하므로 스레드 안전하지 않아 여러 고루틴에서 부르면 결과가 서로 섞이기 때문",
    "strconv 는 스택만 쓰도록 언어가 보장하지만 fmt 는 반드시 힙을 쓰도록 되어 있어 GC 압력이 근본적으로 다르기 때문",
    "fmt.Sprintf 는 만든 문자열을 내부 캐시에 보관해 두므로 호출 횟수가 늘수록 메모리를 계속 점유해 누수처럼 보이기 때문",
    "fmt는 인자를 interface{}로 받아 박싱(할당)이 생기고 포맷 문자열을 런타임에 해석하므로 더 느리고 할당이 많기 때문" ] },
  { k: "go3-rows-close", o: [
    "같은 db 에서 다음 Query 를 부르면 이전 rows 가 자동으로 닫히므로 순서만 지키면 문제가 없다",
    "결과 버퍼가 GC 될 때까지 남아 메모리 사용량만 조금 늘어날 뿐 기능적으로는 문제가 없다",
    "커넥션이 반납되지 않고 붙잡혀 있어 풀이 점점 고갈되고 결국 쿼리가 멈춘다",
    "rows 는 파이널라이저가 붙어 있어 참조가 끊기면 GC 가 즉시 닫아 주므로 아무 문제 없다" ] },
  { k: "go3-null-pointer-scan", o: [
    "가능하다 — NULL이면 포인터가 nil이 되고 값이 있으면 새 문자열을 가리킨다",
    "불가능하다 — 포인터로는 NULL 을 받을 수 없으므로 반드시 sql.NullString 을 써야 한다",
    "불가능하다 — Scan 은 포인터의 포인터를 지원하지 않아 런타임 패닉을 일으킨다",
    "가능하지만 NULL 일 때 nil 이 아니라 빈 문자열을 가리키는 포인터가 되어 구별할 수 없다" ] },
  { k: "go-if-methodset", o: [
    "컴파일은 되지만 메서드를 호출하는 순간 런타임 패닉이 발생한다",
    "컴파일 에러 — 포인터 리시버 메서드는 *T의 메서드 집합에만 속한다",
    "정상 동작한다 — 인터페이스에 담을 때도 Go 가 자동으로 주소를 취해 준다",
    "경고만 나고 메서드 호출이 무시되어 인터페이스 값이 nil 처럼 동작한다" ] },
  { k: "go2-mx-04", o: [
    "time.Time 을 만족하는 인터페이스를 내 패키지에 선언하면 그 인터페이스의 메서드가 time.Time 에 자동으로 붙는다",
    "다른 패키지 타입에는 메서드를 못 붙이므로 정의 타입(type MyTime time.Time)이나 임베딩 구조체를 만들어 거기에 메서드를 정의한다",
    "time 패키지를 임포트한 파일 안이라면 func (t time.Time) MyFmt() 로 바로 추가할 수 있으므로 같은 파일에 두면 허용된다",
    "//go:linkname 지시어로 time 패키지 내부 심볼에 연결해야만 가능하며, 그 밖의 방법은 컴파일러가 모두 거부한다" ] },
  { k: "go3-builder", o: [
    "매번 fmt.Sprintf(\"%s%s\", s, part) 로 새 문자열을 만들어 s 에 다시 담는다",
    "strings.Builder에 WriteString으로 쌓고 마지막에 String()을 호출한다",
    "[]string 에 넣어 두고 매 반복마다 strings.Join 을 호출해 결과를 갱신한다",
    "`s += part` 를 그대로 반복한다 — 컴파일러가 버퍼 재사용으로 바꿔 준다" ] },
  { k: "go3-search-monotonic", o: [
    "f 가 인덱스와 무관하게 무작위여도 상관없다 — 이진 탐색이 모든 칸을 확인한다",
    "f 가 정확히 하나의 인덱스에서만 true 여야 한다 — 그 인덱스가 반환값이 된다",
    "f 가 항상 true 를 반환해야 한다 — false 가 섞이면 탐색이 중단된다",
    "f의 결과가 false들 뒤에 true들이 오는 단조 형태여야 한다" ] },
  { k: "go-defer-lifo", o: [
    "3 3 3 — defer 는 루프가 끝난 뒤 실행되므로 그때의 i 값이 세 번 찍힌다",
    "0 1 2 — defer 는 등록한 순서대로 실행되고 인자는 등록 시점에 평가된다",
    "아무것도 출력되지 않는다 — 루프 안의 defer 는 등록만 되고 실행되지 않는다",
    "2 1 0 — defer는 LIFO 순서로 실행되고 인자는 등록 시점에 평가된다" ] },
  { k: "go2-cf-02", o: [
    "각 복사 고루틴이 끝날 때마다 close(out) 을 호출하는 처리 — 마지막 닫힘이 소비자에게 끝을 알린다",
    "입력 채널마다 고루틴을 띄워 out에 복사하고, WaitGroup으로 전부 끝난 뒤 out을 닫는 처리",
    "out 을 입력 수만큼의 버퍼 채널로 만들어 close 를 생략하는 처리 — 버퍼가 비면 range 가 끝난다",
    "입력 채널을 순서대로 하나씩 range 하며 out 에 복사하는 단일 루프 — 고루틴 없이도 합쳐진다" ] },
  { k: "go3-nullstring-fields", o: [
    "Value 필드와 IsNull bool 필드를 가지며, IsNull 이 true 면 값이 NULL 이었다는 뜻이다",
    "String 필드만 있고 NULL 이면 자동으로 빈 문자열이 들어가므로 빈 문자열 검사로 NULL 을 판별한다",
    "String 필드와 Valid bool 필드를 가지며, Valid가 false면 값이 NULL이었다는 뜻이다",
    "내부적으로 *string 하나만 가지며 NULL 이면 그 포인터가 nil 이 되므로 nil 검사로 판별한다" ] },
  { k: "go-if-embed", o: [
    "Reader 의 구현을 상속받아 Read 메서드를 따로 쓰지 않아도 된다",
    "둘 중 하나만 만족하면 되는 인터페이스가 되어 Reader 만 구현해도 담긴다",
    "런타임에 동적으로 두 메서드 집합이 합쳐져 어느 쪽이든 호출된다",
    "두 인터페이스의 메서드를 모두 요구하는 새 인터페이스가 된다" ] },
  { k: "go2-js-06", o: [
    "reflect 로 구조체 필드 목록을 뽑아 map[string]any 로 먼저 받은 키와 하나씩 직접 비교하는 수밖에 없다",
    "구조체에 `json:\"strict\"` 태그를 추가하면 그 구조체로 디코딩할 때 모르는 키가 자동으로 에러가 된다",
    "json.NewDecoder(r) 로 만든 디코더에 DisallowUnknownFields() 를 호출한 뒤 Decode한다",
    "json.Unmarshal 은 기본적으로 모르는 키를 에러로 처리하므로 별도 설정 없이 그대로 쓰면 된다" ] },
  { k: "go2-md-02", o: [
    "직접 의존성과 전이 의존성을 구분해 기록하므로 어느 모듈이 무엇을 끌어왔는지 추적한다",
    "내려받은 모듈의 암호학적 해시를 기록해 내용이 변조되지 않았음을 검증한다",
    "빌드에 사용할 의존성 버전을 잠가 두는 락 파일이라 go.mod 보다 우선해 버전을 정한다",
    "사용하지 않는 의존성 목록을 보관해 두었다가 go mod tidy 가 지울 대상을 고르게 한다" ] },
  { k: "go2-pk-02", o: [
    "빌드 태그를 붙여 두 파일을 다른 빌드에 넣으면 허용되며 순환은 태그 단위로 끊긴다",
    "컴파일 에러가 난다. Go는 순환 임포트를 금지한다",
    "컴파일은 되지만 런타임에 초기화 순서를 정할 수 없다는 경고만 출력된다",
    "허용되지만 두 패키지의 init 순서가 무작위가 되어 실행마다 결과가 달라질 수 있다" ] },
  { k: "go3-alias-identical", o: [
    "Byte와 uint8은 완전히 같은 타입이라 서로 자유롭게 대입되고 메서드 집합도 동일하다",
    "Byte 는 uint8 을 기반으로 한 새 정의 타입이라 대입할 때 명시적 변환이 필요하다",
    "Byte 는 uint8 의 메서드만 물려받고 uint8 이 만족하던 인터페이스 구현은 물려받지 못한다",
    "별칭은 선언한 패키지 안에서만 쓸 수 있고 다른 패키지에서는 uint8 로만 보인다" ] },
  { k: "go3-env-parse", o: [
    "time.Duration(\"30s\") 처럼 문자열을 그대로 형 변환한다",
    "time.ParseDuration으로 time.Duration으로 변환한다",
    "strconv.Atoi 로 정수로 바꾼 뒤 time.Second 를 곱한다",
    "time.Parse 에 \"30s\" 를 넘겨 시각으로 해석한 뒤 뺀다" ] },
  { k: "go-err-swallow", o: [
    "가장 안전한 처리 방식이다 — 로그에 남았으므로 나중에 추적할 수 있다",
    "컴파일러가 반환값 nil 과 로그 호출을 보고 경고를 낸다",
    "호출자가 nil 을 받으면 런타임이 자동으로 재시도해 준다",
    "호출자가 실패를 알 수 없어 잘못된 상태로 계속 진행하게 된다" ] },
  { k: "go-test-name", o: [
    "이름 앞 줄에 // Test 주석이 붙은 함수라면 어느 파일에 있어도 테스트로 인식된다",
    "파일 이름이 test_ 로 시작하는 파일 안의 모든 함수를 이름과 무관하게 테스트로 인식한다",
    "main 패키지 안에서 *testing.T 를 받는 모든 함수를 파일 이름과 무관하게 인식한다",
    "_test.go 파일 안에 있고 이름이 Test로 시작하며 *testing.T를 인자로 받는 함수" ] },
  { k: "go-if-assert-iface", o: [
    "불가능하다 — 단언 대상은 구체 타입만 쓸 수 있고 인터페이스는 컴파일 에러다",
    "포인터 타입만 대상으로 쓸 수 있고 인터페이스나 값 타입은 허용되지 않는다",
    "가능하지만 동적 타입은 항상 구체 타입이라 인터페이스 단언은 항상 실패한다",
    "가능하다 — 동적 타입이 그 인터페이스를 만족하는지 검사한다" ] },
  { k: "go2-in-01", o: [
    "임포트된 패키지 초기화 -&gt; 패키지 변수 초기화(의존 순) -&gt; init 함수 -&gt; main",
    "패키지 변수 초기화 -&gt; 임포트된 패키지 초기화 -&gt; main -&gt; init 함수",
    "init 함수 -&gt; 임포트된 패키지 초기화 -&gt; 패키지 변수 초기화 -&gt; main",
    "main 함수 실행 -&gt; 임포트된 패키지 초기화 -&gt; 패키지 변수 초기화 -&gt; init 함수" ] },
  { k: "go3-typeswitch-multi", o: [
    "구체 타입으로 좁혀지지 않고 원래 인터페이스 타입(any 등) 그대로 남는다",
    "나열한 타입들의 공통 상위 타입으로 승격되어 int64 로 다룰 수 있게 된다",
    "나열한 타입 중 실제로 담긴 구체 타입으로 자동 결정되어 바로 산술을 할 수 있다",
    "여러 타입을 나열하면 v 를 하나의 타입으로 선언할 수 없어 컴파일 에러가 난다" ] },
  { k: "go3-multikey", o: [
    "if ps[i].Age != ps[j].Age { return ps[i].Age &lt; ps[j].Age }; return ps[i].Name &lt; ps[j].Name",
    "return ps[i].Age &lt; ps[j].Age &amp;&amp; ps[i].Name &lt; ps[j].Name — 두 조건을 한 줄에 적으면 둘 다 만족된다",
    "return ps[i].Age &lt; ps[j].Age || ps[i].Name &lt; ps[j].Name — 나이든 이름이든 한쪽이 작으면 앞에 온다",
    "return ps[i].Age &lt;= ps[j].Age || ps[i].Name &lt;= ps[j].Name — 같은 나이는 안정 정렬이 원래 순서를 지켜 준다" ] },
  { k: "go-err-sentinel", o: [
    "에러 메시지를 한 곳에 두어 짧고 일관되게 만들기 위해서",
    "매번 errors.New 를 부르지 않아 할당을 줄이고 성능을 높이기 위해서",
    "호출자가 특정 실패 상황을 값 비교로 식별하고 분기할 수 있게 하기 위해",
    "에러 대신 패닉이 나는 것을 막고 항상 값으로 돌려주기 위해서" ] },
  { k: "go2-gs-06", o: [
    "os.Exit(0) 을 타이머로 주기적으로 호출해 종료 여부를 확인하고, 시그널이 와 있으면 그 자리에서 프로세스를 바로 끝내 버린다",
    "defer 로 등록한 정리 함수가 시그널 수신 시 자동으로 실행되므로 별도의 context 나 채널 없이도 종료 절차가 알아서 돌아간다",
    "runtime.SetFinalizer 로 main 함수의 종료 훅을 등록해 두면 시그널이 올 때 런타임이 그 훅을 먼저 실행한 뒤에 종료해 준다",
    "signal.NotifyContext(ctx, os.Interrupt, syscall.SIGTERM) 이 돌려준 context 의 Done 을 기다린다" ] },
  { k: "go3-select-star", o: [
    "SELECT * 는 database/sql 이 허용하지 않는 문법이라 드라이버가 거부한다",
    "나중에 컬럼이 추가되면 Scan 인자 개수가 어긋나 실행 중에 깨지기 때문이다",
    "SELECT * 는 트랜잭션 안에서 쓰면 잠금 범위가 테이블 전체로 넓어지기 때문이다",
    "SELECT * 는 옵티마이저가 인덱스를 고르지 못하게 만들어 항상 전체 스캔이 되기 때문이다" ] },
  { k: "go2-ms-02", o: [
    "0이 출력된다 — 값 리시버로 복사되어 필드가 제로값이 된다",
    "런타임에 panic 이 발생한다 — nil 인터페이스에서 메서드를 부른다",
    "6이 출력된다 — 컴파일러가 자동으로 &amp;Rect 를 넘겨 준다",
    "Rect가 Shape를 구현하지 않아 컴파일 에러가 난다" ] },
  { k: "go3-byteloop-hangul", o: [
    "s[i] 가 byte 라 %c 와 맞지 않아 한글 부분에서 컴파일 에러가 난다",
    "한글은 건너뛰고 ASCII 문자만 골라서 출력되어 글자 수가 줄어든다",
    "한글 한 글자가 3개의 바이트로 쪼개져 깨진 문자로 출력된다",
    "한글도 정상적으로 한 글자씩 출력된다 — %c 가 UTF-8 을 해석한다" ] },
  { k: "go3-substr-slice", o: [
    "앞에서 3글자를 잘라 낸다 — 문자열 인덱스는 글자 단위다",
    "어떤 경우에도 문자 경계에 맞춰 런타임이 자동으로 보정해 준다",
    "문자열은 불변이라 슬라이싱을 적용할 수 없고 []rune 으로 바꿔야 한다",
    "앞에서 3바이트를 잘라 내므로 문자 경계와 어긋나면 깨진 문자가 생긴다" ] },
  { k: "go3-invalid-utf8", o: [
    "0(제로값)이 들어오고 그 자리에서 순회가 즉시 중단된다",
    "해당 바이트를 건너뛰고 다음 유효한 룬으로 넘어가므로 r 에는 들어오지 않는다",
    "유니코드 대체 문자 U+FFFD가 들어오고 1바이트씩 전진한다",
    "잘못된 인코딩이라 range 가 런타임 패닉을 일으킨다" ] },
  { k: "go-sl-header", o: [
    "길이만 담은 정수 하나 — 원소는 런타임이 따로 관리한다",
    "기반 배열 포인터, len, cap 세 필드",
    "원소 전체를 그대로 담은 배열 자체 — 대입하면 원소가 복사된다",
    "연결 리스트의 헤드 노드 포인터 — 원소는 노드로 이어진다" ] },
  { k: "go-sl-append-assign", o: [
    "append 가 상황에 따라 nil 을 반환할 수 있으므로 nil 처리를 위해 결과를 받아 두어야 하기 때문",
    "컴파일러가 반환값을 버리는 호출을 문법적으로 막으므로 대입하지 않으면 컴파일이 되지 않기 때문",
    "재할당이 일어나면 새 배열을 가리키는 새 헤더가 반환되므로, 대입하지 않으면 추가 결과를 잃는다",
    "append 가 원본 슬라이스의 헤더를 파괴하므로 새 헤더를 받아 두지 않으면 원본을 더 쓸 수 없기 때문" ] },
  { k: "go-cc-empty-select", o: [
    "선택할 케이스가 없으므로 즉시 반환되어 다음 문장으로 넘어간다",
    "케이스가 없는 select 는 문법 오류로 보아 컴파일 단계에서 에러가 난다",
    "영원히 블록되며, main에서 실행하면 교착으로 패닉이 난다",
    "스케줄러가 임의의 시점에 깨우므로 무작위 시간이 지난 뒤 종료된다" ] },
  { k: "go-ctx-done", o: [
    "ctx.IsCancelled() 를 루프마다 호출해 true 가 되면 빠져나온다",
    "select에서 case &lt;-ctx.Done() 을 감시한다",
    "ctx.Wait() 를 호출해 취소될 때까지 블록했다가 돌아온다",
    "취소 시 런타임이 패닉을 올려 주므로 recover 로 잡는다" ] },
  { k: "go2-cf-06", o: [
    "close 뒤에 range 가 닫힌 채널을 읽으려 하므로 첫 반복에서 panic 이 난다",
    "채널은 LIFO 로 동작하므로 3 2 1 처럼 값이 역순으로 출력된다",
    "버퍼 없는 채널에 수신자가 없는 상태로 송신해 첫 송신에서 데드락이 난다",
    "close 이후에는 range 를 돌 수 없다는 규칙 때문에 컴파일 에러가 난다" ] },
  { k: "go3-readall-risk", o: [
    "끝에 이르면 io.EOF 를 에러로 반환하므로 호출자가 EOF 를 따로 걸러내야 한다",
    "내용 전체를 메모리에 올리므로 크기가 큰 입력에서는 메모리를 과도하게 쓴다",
    "읽은 데이터를 string 으로만 반환하므로 바이너리 입력은 손상될 수 있다",
    "io.Reader 가 아니라 *os.File 만 인자로 받으므로 네트워크 응답에는 쓸 수 없다" ] }
] };
