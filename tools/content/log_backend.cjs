/* backend 트랙 로그 분석 2 — API·DB·큐 로그에서 원인을 짚는다. */
module.exports = [
{
  t: "log", k: "목록 하나에 쿼리 이백 번", cat: "logs", d: 2, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "목록 API 만 느리고 DB CPU 가 높습니다. 개별 쿼리는 전부 1ms 입니다. 원인 줄을 고르세요.",
  items: [
    { txt: "09:00:00 INFO  api: GET /orders 처리 시작 (페이지 크기 100), 인증 검사 0.3ms" },
    { txt: "09:00:00 INFO  db: SELECT * FROM orders LIMIT 100 → 1.2ms" },
    { txt: "09:00:00 INFO  api: 각 주문의 고객 이름을 반복문에서 하나씩 조회", bad: true },
    { txt: "09:00:00 INFO  db: SELECT * FROM users WHERE id=? → 0.9ms (100회)" },
    { txt: "09:00:01 WARN  api: 요청 하나당 쿼리 101개, 응답 420ms" },
    { txt: "09:10:00 WARN  db: CPU 78%, 초당 쿼리 12,000 (대부분 users 단건 조회)" },
  ],
  ex: "빠른 쿼리라도 <b>101번</b>이면 왕복 비용이 그대로 쌓입니다. 개별 지연이 정상이라 슬로우 쿼리 로그에도 안 잡히고, DB 는 가벼운 조회에 파묻힙니다. 처방은 필요한 사용자를 <b>한 번에</b> 가져오는 것(<code>WHERE id IN (...)</code> 또는 조인)이고, ORM 을 쓴다면 <b>즉시 로딩</b>을 명시하는 것입니다. 예방은 요청당 쿼리 수를 로그에 남겨 <b>임계치를 넘으면 경보</b>하는 것입니다.",
},
{
  t: "log", k: "데이터가 자라자 드러난 전체 훑기", cat: "logs", d: 2, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "코드 변경이 없었는데 어느 날부터 특정 조회만 느려졌습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "10:00:00 INFO  api: 최근 배포 없음, 트래픽 평시" },
    { txt: "10:00:00 INFO  db: 서버 사양·설정 변경 없음" },
    { txt: "10:00:01 INFO  db: orders 행 수 40만 → 1,800만 (3개월)" },
    { txt: "10:00:02 INFO  db: WHERE status=? AND created_at>? 조건에 맞는 인덱스 없음", bad: true },
    { txt: "10:00:03 INFO  db: 실행 계획 Seq Scan on orders (cost 0.00..421,900)" },
    { txt: "10:00:04 WARN  api: 해당 API p95 30ms → 4,100ms" },
    { txt: "10:20:00 WARN  db: 버퍼 캐시 적중률 99% → 62%" },
  ],
  ex: "인덱스가 없으면 <b>행 수에 비례해</b> 느려집니다. 40만 행일 때는 전체 훑기도 30ms 라 보이지 않았고, 1,800만 행이 되자 4초가 됐습니다. 캐시 적중률 하락은 결과입니다 — 큰 스캔이 캐시를 밀어냅니다. 처방은 조건에 맞는 <b>복합 인덱스</b>를 만드는 것(선택도가 높은 열을 앞에)이고, 예방은 실행 계획을 CI 에서 확인하고 <b>표 크기와 함께</b> 쿼리 시간을 추적하는 것입니다.",
},
{
  t: "log", k: "트랜잭션 안에서 외부를 부른다", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "결제사가 느려지자 DB 커넥션이 고갈됐습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "11:00:00 INFO  api: 커넥션 풀 최대 20, 평시 사용 4" },
    { txt: "11:00:00 INFO  api: 요청량 평시 수준, 오류율 0%" },
    { txt: "11:00:01 INFO  api: 주문 생성은 트랜잭션으로 감싸 처리" },
    { txt: "11:00:02 INFO  api: 트랜잭션 안에서 결제사 HTTP 호출을 수행", bad: true },
    { txt: "11:10:00 WARN  psp: 결제사 응답 200ms → 12초" },
    { txt: "11:10:20 WARN  db: 활성 트랜잭션 20, 대기 요청 340" },
    { txt: "11:10:30 ERROR api: 커넥션 획득 타임아웃, 주문 외 기능까지 실패" },
  ],
  ex: "커넥션을 쥔 채 <b>남의 서비스를 기다렸습니다</b>. 외부 호출이 느려지면 그 시간만큼 커넥션이 묶이고, 스무 개가 차는 순간 관계없는 기능까지 멈춥니다. 결제사 지연은 방아쇠일 뿐입니다. 처방은 <b>외부 호출을 트랜잭션 밖으로</b> 빼는 것 — 먼저 커밋해 상태를 <code>대기</code>로 남기고, 결제는 그 뒤에 호출한 다음 결과로 상태를 갱신합니다.",
},
{
  t: "log", k: "재시도가 주문을 두 배로", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "네트워크가 잠깐 흔들린 뒤 같은 주문이 두 건씩 생겼습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "12:00:00 INFO  api: POST /orders 처리 정상, 중복 없음" },
    { txt: "12:04:00 WARN  net: 클라이언트-서버 구간 패킷 손실 3%" },
    { txt: "12:04:01 INFO  api: 서버는 주문을 저장했으나 응답이 클라이언트에 닿지 않음" },
    { txt: "12:04:02 INFO  api: 클라이언트가 같은 요청을 재전송" },
    { txt: "12:04:02 INFO  api: 생성 요청에 멱등 키가 없어 새 주문으로 처리", bad: true },
    { txt: "12:04:03 WARN  db: 같은 사용자·같은 금액 주문 2건" },
    { txt: "12:30:00 ERROR ops: 중복 결제 118건 환불" },
  ],
  ex: "재전송은 <b>정상적인 동작</b>입니다. 응답을 못 받은 클라이언트는 성공했는지 알 수 없으니 다시 보낼 수밖에 없고, 서버가 그것을 구별하지 못하면 두 건이 생깁니다. 패킷 손실은 방아쇠일 뿐입니다. 처방은 <b>멱등 키</b> — 클라이언트가 만든 키를 저장해 두고 같은 키가 오면 <b>처음 결과를 그대로</b> 돌려주는 것입니다. 유니크 제약을 함께 두면 경합에서도 안전합니다.",
},
{
  t: "log", k: "먼저 읽은 값을 그대로 덮어쓴다", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "재고가 동시에 팔릴 때만 숫자가 맞지 않습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "13:00:00 INFO  api: 재고 차감 요청 처리 시작 (단건 요청에서는 항상 정확, 격리 수준 READ COMMITTED)" },
    { txt: "13:00:01 INFO  api: SELECT stock → 애플리케이션에서 -1 계산 → UPDATE stock=?", bad: true },
    { txt: "13:00:01 INFO  api: 두 요청이 같은 값 10 을 읽음" },
    { txt: "13:00:02 WARN  db: 둘 다 stock=9 로 갱신 (실제 판매 2건)" },
    { txt: "13:30:00 ERROR ops: 재고 0 인데 주문 24건 추가 접수" },
    { txt: "13:50:00 ERROR ops: 품절 상품 배송 불가 안내 발송" },
  ],
  ex: "읽고, 계산하고, 쓰는 사이에 <b>남이 끼어들었습니다</b>. 둘 다 10 을 읽었으니 둘 다 9 를 씁니다 — 갱신 하나가 사라지는 <b>잃어버린 갱신</b>입니다. 처방은 데이터베이스가 계산하게 하는 것(<code>UPDATE ... SET stock = stock - 1 WHERE stock &gt; 0</code>)이고, 여러 값을 함께 봐야 한다면 <code>SELECT ... FOR UPDATE</code> 나 버전 컬럼으로 <b>낙관적 잠금</b>을 걸어야 합니다.",
},
{
  t: "log", k: "영원히 다시 오는 메시지", cat: "logs", d: 2, track: "backend",
  file: "/var/log/order-api/consumer.log",
  q: "큐 소비가 어느 순간부터 한 발짝도 나아가지 않습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "14:00:00 INFO  consumer: 오프셋 정상 진행, 초당 900건" },
    { txt: "14:04:00 ERROR consumer: 메시지 파싱 실패 (필수 필드 누락)", bad: true },
    { txt: "14:04:00 INFO  consumer: 예외 발생 시 커밋하지 않고 재시도 — 데드레터 큐 없음" },
    { txt: "14:04:01 ERROR consumer: 같은 메시지 파싱 실패 (2회)" },
    { txt: "14:10:00 WARN  consumer: 같은 오프셋에서 4만 회 재시도, 진행 0" },
    { txt: "14:40:00 ERROR ops: 큐 적체 210만 건, 후속 주문 처리 지연" },
  ],
  ex: "고칠 수 없는 메시지 하나가 <b>줄 전체를 막았습니다</b>. 실패하면 커밋하지 않고 다시 받으니 같은 실패가 무한히 반복되고, 뒤에 있는 정상 메시지는 영원히 차례가 오지 않습니다. 처방은 <b>재시도 횟수를 정하고 넘으면 데드레터 큐로 보내</b> 줄을 비우는 것이며, 그렇게 모인 메시지를 사람이 따로 살펴봅니다. 예방은 소비자에서 <b>진행 정체</b>를 지표로 감시하는 것입니다.",
},
{
  t: "log", k: "커밋은 됐는데 알림이 없다", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "주문은 저장됐는데 후속 처리가 일부 건에서만 일어나지 않았습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "15:00:00 INFO  api: 주문 생성 요청 수신, 검증 통과" },
    { txt: "15:00:00 INFO  api: 결제 승인 완료" },
    { txt: "15:00:00 INFO  api: 재고 차감 정상" },
    { txt: "15:00:00 INFO  api: 주문 저장 트랜잭션 커밋 성공 (id=90211)" },
    { txt: "15:00:00 INFO  api: 커밋 직후 메시지 브로커로 이벤트 발행 시도", bad: true },
    { txt: "15:00:01 WARN  broker: 연결 재조정 중 발행 실패 — 로그만 남기고 요청은 200 으로 응답" },
    { txt: "15:00:02 WARN  ops: 주문은 있는데 배송·정산 이벤트 없음" },
    { txt: "15:40:00 ERROR ops: 누락 주문 61건 수기 처리" },
  ],
  ex: "데이터베이스 커밋과 메시지 발행은 <b>하나의 원자적 동작이 아닙니다</b>. 커밋 뒤 발행이 실패하면 상태는 남고 이벤트만 사라져, 어디에도 오류로 보이지 않는 누락이 됩니다. 처방은 <b>아웃박스</b> — 이벤트를 같은 트랜잭션에서 테이블에 함께 저장하고, 별도 워커가 그 표를 읽어 발행한 뒤 표시합니다. 발행이 실패해도 다시 시도할 수 있고, <b>둘 다 커밋되거나 둘 다 안 되거나</b>가 보장됩니다.",
},
{
  t: "log", k: "표를 바꾸는 동안 멈춘 서비스", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/deploy.log",
  q: "배포 중 몇 분간 쓰기가 전부 실패했습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "16:00:00 INFO  deploy: 배포 시작, 애플리케이션 이미지 교체 완료" },
    { txt: "16:00:00 INFO  deploy: 마이그레이션 시작" },
    { txt: "16:00:00 INFO  db: orders 행 수 1,800만" },
    { txt: "16:00:01 INFO  db: ALTER TABLE orders ADD COLUMN ... NOT NULL DEFAULT ... 실행", bad: true },
    { txt: "16:00:02 INFO  db: 전체 재작성이 필요해 ACCESS EXCLUSIVE 잠금 유지" },
    { txt: "16:00:10 WARN  api: INSERT/UPDATE 대기 큐 증가" },
    { txt: "16:03:40 ERROR api: 쓰기 요청 타임아웃 12,400건" },
    { txt: "16:04:10 INFO  db: 마이그레이션 완료, 잠금 해제" },
  ],
  ex: "표를 통째로 다시 쓰는 변경은 <b>가장 강한 잠금</b>을 오래 붙잡습니다. 1,800만 행이면 몇 분이고, 그동안 모든 쓰기가 줄을 섭니다. 처방은 <b>단계로 쪼개는 것</b> — 널 허용 컬럼을 먼저 추가하고, 값을 배치로 채우고, 마지막에 제약을 검증하며 붙입니다. 예방은 마이그레이션에 <b>잠금 타임아웃</b>을 걸어 오래 잡으면 스스로 물러나게 하는 것입니다.",
},
{
  t: "log", k: "모두가 같은 주소로 보인다", cat: "logs", d: 2, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "특정 회사 사용자 전체가 한꺼번에 429 를 받습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "17:00:00 INFO  api: 서비스 정상, 오류율 0%" },
    { txt: "17:00:00 INFO  api: 인증 실패율 평시 수준" },
    { txt: "17:00:00 INFO  api: 속도 제한 IP 당 분당 60회" },
    { txt: "17:00:00 INFO  api: 프록시 뒤에 배치됨 (ALB → nginx → app)" },
    { txt: "17:00:01 INFO  api: 클라이언트 IP 를 소켓 주소에서 읽음 — X-Forwarded-For 미사용", bad: true },
    { txt: "17:00:02 INFO  api: 모든 요청의 출발지가 10.0.1.7 (nginx) 로 집계" },
    { txt: "17:00:10 WARN  api: 429 응답 급증, 정상 사용자 포함" },
    { txt: "17:30:00 ERROR ops: 대형 고객사 전체 접속 불가 신고" },
  ],
  ex: "프록시 뒤에서는 소켓 주소가 <b>언제나 프록시</b>입니다. 그러면 모든 사용자가 한 사람으로 세어져 제한이 즉시 소진되고, 잘못 없는 사용자까지 막힙니다. 처방은 신뢰하는 프록시 목록을 정하고 <b><code>X-Forwarded-For</code> 에서 올바른 항목</b>을 골라 쓰는 것입니다. 이 헤더는 위조될 수 있으므로 <b>신뢰 경계</b>를 명시해야 하며, 가능하면 IP 대신 <b>계정·API 키</b>로 제한하는 편이 안전합니다.",
},
{
  t: "log", k: "페이지를 넘길수록 느려지고 빠뜨린다", cat: "logs", d: 3, track: "backend",
  file: "/var/log/order-api/app.log",
  q: "목록을 끝까지 내려받으면 일부 항목이 빠지고 뒤 페이지일수록 느립니다. 원인 줄을 고르세요.",
  items: [
    { txt: "18:00:00 INFO  api: 목록 조회 페이지네이션 제공" },
    { txt: "18:00:00 INFO  api: 첫 페이지 응답 40ms 로 정상" },
    { txt: "18:00:00 INFO  db: 정렬 기준 created_at DESC, 동시에 새 행이 계속 삽입됨" },
    { txt: "18:00:01 INFO  api: 페이지 이동을 OFFSET n LIMIT 100 으로 구현", bad: true },
    { txt: "18:00:02 INFO  db: OFFSET 200000 쿼리 실행 시간 1,900ms" },
    { txt: "18:00:03 WARN  api: 앞 페이지에서 본 항목이 다음 페이지에 다시 등장" },
    { txt: "18:00:04 WARN  batch: 전체 내려받기 결과에서 412건 누락" },
  ],
  ex: "<code>OFFSET</code> 은 <b>건너뛸 행을 실제로 읽고 버립니다</b>. 뒤 페이지일수록 느려지고, 그 사이 앞쪽에 행이 삽입되면 경계가 밀려 <b>중복과 누락</b>이 함께 생깁니다. 처방은 <b>커서 방식</b> — 마지막으로 본 <code>(created_at, id)</code> 를 기억했다가 <code>WHERE (created_at, id) &lt; (?, ?)</code> 로 이어 읽습니다. 정렬 키에 고유 값을 포함해야 경계가 흔들리지 않습니다.",
},
];
