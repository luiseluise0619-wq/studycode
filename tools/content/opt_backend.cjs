/* backend·sql·sysd — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다.
   node fix_opts.cjs --apply opt_backend.cjs  (SPEC 배열을 돌리려면 아래 all 을 쓴다) */
const all = [
{ track: "backend", fixes: [
  { k: "세션 선택 근거", o: [
    "세션은 서버가 상태를 들고 있어 인스턴스를 늘릴 수 없으므로, 트래픽이 커지면 어차피 JWT 로 갈아타야 하고 처음부터 JWT 로 시작하는 편이 이중 작업을 줄인다",
    "JWT 는 모바일 전용, 세션은 웹 전용이다 — 앱은 쿠키를 다루기 어려워 토큰을 헤더에 싣고, 브라우저는 쿠키가 자동으로 붙으므로 세션이 자연스럽다",
    "즉시 로그아웃과 권한 변경 반영이 중요하면 서버 세션(+중앙 세션 저장소)을, 여러 서비스나 서드파티가 토큰을 검증해야 하고 짧은 만료를 감수할 수 있으면 JWT 를 택한다",
    "신규 서비스는 JWT 가 정답이다 — 서명만 검증하면 되니 세션 저장소가 필요 없고, 마이크로서비스로 쪼갤 때도 그대로 쓸 수 있어 처음부터 JWT 로 가는 것이 업계 표준이다" ] },
  { k: "무효화 시점", o: [
    "문제 없다 — 롤백되면 캐시가 비어 있을 뿐이고 다음 요청이 DB 에서 옛 값을 읽어 다시 채우므로, 미스 한 번을 빼면 어떤 부작용도 없고 순서도 무관하다",
    "캐시 삭제는 트랜잭션 안에서 해야 원자성이 보장된다 — 커밋과 삭제가 한 단위로 묶여야 둘 중 하나만 성공하는 상태가 생기지 않는다",
    "롤백됐는데 캐시만 지워져 미스가 생기고, 커밋 전에 다른 요청이 미스로 **옛 값**을 다시 캐싱해 stale 이 고착될 수 있다. 삭제는 커밋 뒤에 한다",
    "롤백되면 캐시도 자동으로 복구된다 — 캐시 클라이언트가 트랜잭션에 참여하므로 DB 가 되돌아가면 삭제한 키도 함께 되살아난다" ] },
  { k: "에러 응답 형식", o: [
    "적절한 상태 코드 + 기계가 분기할 수 있는 고정 에러 코드 + 사람이 읽을 메시지 + 추적용 ID 를 함께 내려 주고, 형식을 전 API 에서 통일한다",
    "에러 메시지는 보안상 'error' 라는 문자열만 준다 — 원인을 알려 주면 공격자에게 내부 구조를 드러내므로 상태 코드 외의 정보는 모두 숨긴다",
    "스택 트레이스를 그대로 내려 클라이언트가 원인을 파악하게 한다 — 서버에서 해석하는 것보다 원본 정보를 넘기는 편이 디버깅이 빠르다",
    "200 OK 를 주고 본문의 success 필드로 성공 여부를 표현한다 — 상태 코드는 전송 계층의 것이고 업무 실패는 본문에서 다루는 편이 일관된다" ] },
  { k: "idempotency-key", o: [
    "재시도 자체를 금지한다 — 타임아웃이 나면 실패로 확정하고 사용자에게 다시 결제하도록 안내해 서버가 중복을 판단할 필요를 없앤다",
    "응답을 항상 200 으로 반환하도록 만든다 — 클라이언트는 오류가 아니면 재시도하지 않으므로 상태 코드만 바꿔도 이중 청구가 사라진다",
    "타임아웃을 무한대로 늘린다 — 응답이 늦어도 기다리기만 하면 재시도가 일어나지 않으므로 청구도 한 번만 된다",
    "클라이언트가 생성한 멱등성 키(Idempotency-Key)를 받아 서버가 첫 처리 결과를 저장·재사용" ] },
  { k: "be2-deploy-backward-compat-migration", o: [
    "각 마이그레이션은 직전 버전 코드와도 호환되도록(하위호환) 만들어, 코드 롤백 시 DB 가 신버전 스키마여도 동작하게 한다",
    "배포에서 스키마와 코드는 원자적으로 동시에 바뀌어야 한다 — 둘이 어긋나는 순간이 없도록 점검 시간을 두고 한 번에 교체한다",
    "마이그레이션은 코드보다 먼저 파괴적으로 적용한다 — 옛 컬럼을 지워 두어야 새 코드가 잘못된 필드를 읽는 실수를 막을 수 있다",
    "롤백은 고려하지 않는다 — 마이그레이션은 전진만 하는 것이 원칙이고, 문제가 생기면 고친 마이그레이션을 다시 앞으로 배포한다" ] },
  { k: "TTL만의 한계", o: [
    "TTL 은 정확한 무효화를 보장하므로 트레이드오프가 없다 — 60초가 지나면 반드시 새 값을 읽으므로 그보다 오래된 값은 존재하지 않는다",
    "TTL 이 짧을수록 DB 부하가 줄어든다 — 캐시가 자주 비워져 메모리가 여유로워지고, 그만큼 DB 커넥션도 덜 잡는다",
    "TTL 은 읽기 성능에만 영향을 주고 정합성과 무관하다 — 정합성은 쓰기 경로가 정하는 것이라 만료 시간은 속도 조절 손잡이일 뿐이다",
    "TTL 을 줄이면 stale 은 줄지만 미스가 늘어 DB 부하가 커지고, 늘리면 반대로 오래된 값이 최대 TTL 만큼 노출된다" ] },
  { k: "error-budget", o: [
    "의미 없다 — SLO 는 목표일 뿐 예산이 아니고, 실패를 미리 허용한다는 발상 자체가 품질을 낮추므로 항상 100% 를 지향해야 한다",
    "항상 배포를 멈춘다 — 버짓이 조금이라도 줄었다면 새 변경은 위험하므로 다음 달 예산이 채워질 때까지 아무것도 내보내지 않는다",
    "남은 버짓과 무관하게 배포를 강행한다 — 배포를 미루면 변경이 쌓여 더 위험해지므로 예산은 사후 보고에만 쓴다",
    "남은 에러 버짓이 부족하면 위험한 배포·실험을 늦추고 안정화에, 여유가 있으면 빠른 출시에 투자" ] },
  { k: "be2-resil-health-check", o: [
    "핵심 의존성(DB 등)까지 점검하는 readiness 체크를 두어, 요청 처리 불가 시 트래픽에서 빼낸다",
    "프로세스만 살아 있으면 건강한 것으로 봐도 된다 — DB 연결은 요청 처리 중에 다시 맺어지므로 로드밸런서가 신경 쓸 일이 아니다",
    "헬스 체크를 제거한다 — 잘못된 체크로 멀쩡한 인스턴스가 빠지는 것이 더 위험하므로 라우팅은 오류율로만 판단한다",
    "헬스 체크 주기를 1시간으로 늘린다 — 체크가 잦으면 DB 에 부하를 주므로 드물게 확인하고 그 사이는 마지막 결과를 믿는다" ] },
  { k: "be2-cache-local-vs-distributed", o: [
    "인스턴스 간 정합성이 중요하면 분산 캐시를 쓰거나, 로컬 캐시엔 짧은 TTL·무효화 브로드캐스트를 적용한다",
    "인스턴스를 하나로 줄이면 된다 — 캐시가 한 곳뿐이면 불일치가 생길 수 없고, 수평 확장은 정합성 문제가 없는 서비스에서만 한다",
    "로컬 캐시가 더 빠르고 우월하므로 그대로 두면 된다 — 옛 값이 잠깐 보이는 것은 캐시의 본질이고 사용자는 새로고침으로 해결한다",
    "분산 캐시는 네트워크를 타므로 느리니 쓰면 안 된다 — 홉이 하나 늘면 지연이 배가 되어 캐시를 두는 의미가 사라진다" ] },
  { k: "pagination-cursor", o: [
    "OFFSET 이 클수록 느려지고 삽입 시 항목 중복/누락이 생김 → 커서(키셋) 기반으로 전환",
    "OFFSET 은 인덱스를 타므로 빠르게 동작해 아무 문제가 없다 — 페이지 번호만 바꾸면 되므로 구현이 단순하고 이동도 자유롭다",
    "LIMIT 을 늘리면 해결된다 — 한 페이지에 더 많이 담으면 페이지 수가 줄어 OFFSET 이 커질 일이 없어진다",
    "정렬을 제거하면 해결된다 — ORDER BY 가 없으면 DB 가 정렬 비용을 치르지 않아 OFFSET 이 커도 빠르다" ] },
  { k: "be2-oauth-oidc-vs-oauth", o: [
    "문제 없다 — access token 에는 사용자 정보가 들어 있으므로 그것을 읽어 신원을 확인하면 되고, 별도 토큰은 중복이다",
    "OAuth2 는 인가 프로토콜이라 access token 은 신원 증명 용도가 아니다. 신원은 OIDC 의 id_token 으로 확인해야 한다",
    "OIDC 는 OAuth2 와 무관한 별개 표준이라 함께 쓸 수 없다 — 둘 중 하나를 골라야 하고 로그인에는 OAuth2 를 쓰는 것이 관례다",
    "access token 을 그냥 디코딩해 이메일을 신뢰하면 된다 — 서명된 토큰이므로 안의 클레임은 위조될 수 없고 신원으로 써도 안전하다" ] },
] },
{ track: "sql", fixes: [
  { k: "복합 인덱스 순서", o: [
    "복합 인덱스는 선두 컬럼(a)부터 정렬되므로 a 조건이 없으면 b 만으로 탐색 이점을 못 쓴다 → (b, a) 인덱스를 만들거나 a 조건을 함께 준다",
    "복합 인덱스는 첫 컬럼만 쓸 수 있다 — 두 번째 컬럼은 정렬 순서를 정할 뿐 검색에는 참여하지 않으므로 b 조건은 어떤 경우에도 인덱스를 타지 못한다",
    "b 를 별도 테이블로 분리한다 — 한 테이블에 두 조회 패턴을 담으면 인덱스가 충돌하므로 컬럼을 나누고 조인으로 합치는 편이 빠르다",
    "인덱스를 UNIQUE 로 바꾼다 — 유일 인덱스는 옵티마이저가 한 건만 찾으면 되는 것을 알고 어느 컬럼으로 시작하든 탐색을 멈출 수 있다" ] },
  { k: "secondary-index-lookup", o: [
    "보조 인덱스 리프는 기본키 값을 저장하고, 실제 행을 얻으려면 클러스터드 인덱스를 다시 타는 2차 탐색이 필요하다",
    "기본키를 저장하지 않으며 조회 시 전체 테이블을 스캔한다 — 보조 인덱스는 어느 행이 조건에 맞는지만 알려 주고 위치는 알지 못한다",
    "보조 인덱스는 커버링 인덱스라서 클러스터드 인덱스를 다시 볼 필요가 없다 — 리프에 행 전체가 복사되어 있어 한 번의 탐색으로 끝난다",
    "보조 인덱스는 행의 물리 주소를 저장하므로 추가 탐색이 전혀 없다 — 힙 테이블처럼 rowid 로 곧바로 페이지를 읽는다" ] },
  { k: "lsm-vs-btree", o: [
    "LSM-tree 는 읽기와 쓰기 모두 B-tree 보다 빠르다 — 정렬된 파일을 여러 층으로 두므로 어떤 접근이든 한 층만 보면 된다",
    "LSM-tree 는 인덱스 구조가 없어서 쓰기를 할 때 정렬 비용이 들지 않는다 — 들어온 순서대로 붙이기만 하고 정렬은 읽을 때 한다",
    "LSM-tree 는 디스크에 데이터를 쓰지 않기 때문에 빠르다 — 모든 데이터를 메모리에 두고 스냅숏만 가끔 내려 쓴다",
    "쓰기를 메모리(memtable)에 모아 순차적으로 SSTable 로 플러시하므로 랜덤 쓰기를 순차 쓰기로 바꾼다" ] },
  { k: "os-page-cache", o: [
    "shared_buffers 를 키우면 WAL 이 비활성화된다 — 버퍼가 충분하면 로그 없이도 복구할 수 있다고 보고 기록을 생략한다",
    "OS 페이지 캐시는 PostgreSQL 이 사용할 수 없다 — 데이터 파일을 O_DIRECT 로 열기 때문에 커널 캐시를 아예 거치지 않는다",
    "같은 데이터가 shared_buffers 와 OS 페이지 캐시에 이중 캐싱되어 유효 메모리가 낭비될 수 있다",
    "shared_buffers 는 크게 잡을수록 빨라지므로 비효율이 생길 여지가 없다 — 메모리는 남기는 것보다 버퍼로 쓰는 편이 언제나 낫다" ] },
  { k: "LIMIT정렬", o: [
    "created_at 에 정렬 방향에 맞는 인덱스가 있으면 상위 10건만 읽고 멈출 수 있다",
    "DESC 는 인덱스를 쓸 수 없다 — B-tree 는 오름차순으로만 저장되므로 내림차순 정렬은 언제나 전체를 읽어 뒤집어야 한다",
    "LIMIT 은 전체 정렬 후 잘라내므로 인덱스가 아무 의미가 없다 — 몇 건을 원하든 정렬 비용은 같으므로 인덱스는 저장 공간만 쓴다",
    "인덱스가 없을수록 빠르다 — 정렬 컬럼에 인덱스가 있으면 옵티마이저가 그것을 타느라 랜덤 I/O 가 늘어 오히려 느려진다" ] },
  { k: "단점", o: [
    "저장 공간을 절반 수준으로 줄여 준다 — 압축되어 저장되므로 디스크는 아끼지만 그만큼 읽을 때 풀어야 해 CPU 를 쓴다",
    "조인이 항상 빨라진다 — 관련 데이터가 한 문서 안에 있어 조인이 사라지고, 그래서 정규화된 설계보다 조회가 단순해진다",
    "트랜잭션이 불가능해진다 — JSON 컬럼은 원자적으로 갱신할 수 없어 여러 키를 바꾸는 도중 다른 세션이 절반만 본다",
    "타입 안정성·제약이 약해지고 내부 값 조회가 느리거나 인덱스 설계가 번거롭다" ] },
  { k: "NestedLoop", o: [
    "대량-대량 조인에 항상 최적이다 — 양쪽이 클수록 한 행씩 맞춰 보는 방식이 메모리를 덜 쓰므로 해시 조인보다 안정적이다",
    "항상 두 테이블을 정렬한 뒤 병합한다 — 정렬된 두 입력을 나란히 훑어 같은 키를 짝짓는 방식이라 인덱스가 없어도 빠르다",
    "바깥 테이블의 각 행마다 안쪽 테이블을 조회하며, 안쪽에 인덱스가 있고 결과가 작을 때 유리",
    "어떤 상황에서든 해시 테이블을 메모리에 만들어 두고 그것으로 매칭한다 — 작은 쪽을 해시로 올린 뒤 큰 쪽을 한 번 훑는다" ] },
  { k: "COUNT과 NULL", o: [
    "둘 다 NULL 포함 전체 행 수를 센다 — COUNT 는 인자를 보지 않고 그룹의 행 개수만 세는 함수라 무엇을 넣든 결과가 같다",
    "둘은 항상 동일한 값을 낸다 — 컬럼 이름은 옵티마이저 힌트일 뿐이고 NULL 은 '값이 없음' 이 아니라 값의 하나로 세어진다",
    "COUNT(col) 은 NULL 을 제외하고 세며, COUNT(*) 는 모든 행을 센다",
    "COUNT(col) 이 COUNT(*) 보다 더 큰 값을 돌려준다 — 컬럼의 중복 값을 따로 세므로 같은 값이 여러 행에 있으면 개수가 늘어난다" ] },
  { k: "UNION 차이", o: [
    "반복마다 중복 행을 제거하므로 순환 데이터의 무한루프를 막는 데 도움될 수 있다",
    "중복 제거 단계가 추가되어도 성능이 더 빨라진다 — 결과 집합이 작아져 다음 반복이 다룰 행이 줄고 정렬 비용은 그보다 적다",
    "재귀 깊이가 2배가 된다 — UNION 은 각 단계를 두 번 평가해 중복을 걸러내므로 같은 결과에 도달하는 데 반복이 곱절로 든다",
    "문법 오류가 난다 — 재귀 CTE 의 재귀 부분은 UNION ALL 로만 이을 수 있고, UNION 을 쓰면 파서가 거부한다" ] },
  { k: "최소권한", o: [
    "모든 계정에 GRANT ALL 을 준다 — 권한 부족으로 장애가 나는 것이 더 흔하므로 넉넉히 주고 감사 로그로 오남용을 잡는다",
    "앱 계정에 슈퍼유저 권한을 부여해 두고 필요할 때 골라 쓰게 한다 — 기능이 늘 때마다 권한을 추가하는 운영 비용이 사라진다",
    "root 로 애플리케이션을 접속시킨다 — 계정을 하나로 두면 비밀번호 관리가 단순해지고 권한 문제로 배포가 막히지 않는다",
    "앱 계정에 필요한 테이블의 SELECT/INSERT 만 부여하고 DROP 은 주지 않는다" ] },
  { k: "Index Only Scan", o: [
    "테이블에 인덱스가 하나만 있어야 한다 — 인덱스가 여럿이면 옵티마이저가 어느 것을 쓸지 정하지 못해 테이블을 읽는다",
    "조건이 기본키에 대한 등호 비교여야 하고 다른 컬럼은 쓸 수 없다 — 기본키 인덱스만 행 전체를 담고 있기 때문이다",
    "테이블 행 수가 1000건 미만이어야 한다 — 작은 테이블만 인덱스가 메모리에 통째로 올라와 테이블 접근을 생략할 수 있다",
    "SELECT 와 WHERE 에 필요한 모든 컬럼이 그 인덱스 안에 포함되어 있어야 한다" ] },
] },
{ track: "sysd", fixes: [
  { k: "Raft·로그커밋", o: [
    "로그 엔트리는 커밋 없이 즉시 상태 머신에 적용되기 때문 — 커밋 표시는 나중에 로그를 정리할 때 쓰는 관리용 기록일 뿐 안전성과는 관계가 없다",
    "과반수에 복제되면 안전하므로 사실 표시해도 된다 — 과반수가 가진 엔트리는 어떤 새 리더도 반드시 갖고 있으므로 덮어써질 수 없기 때문",
    "Raft 는 term 개념을 사용하지 않기 때문 — 로그 인덱스만으로 순서를 정하므로 '이전 term' 이라는 구분 자체가 성립하지 않는다",
    "이전 term 의 엔트리는 다른 리더가 덮어쓸 수 있어, 리더가 현재 term 의 엔트리를 커밋할 때 함께(간접적으로) 커밋되어야 안전하기 때문" ] },
  { k: "저장엔진", o: [
    "LSM-tree 는 WAL 이 필요 없기 때문 — memtable 자체가 로그 역할을 하므로 쓰기마다 로그를 한 번 더 남기는 비용이 사라진다",
    "LSM-tree 는 읽기 증폭이 전혀 없기 때문 — 최신 값이 항상 맨 위 층에 있어 한 번의 조회로 끝나고, 그만큼 쓰기에 자원을 돌릴 수 있다",
    "무작위 in-place 갱신 대신 메모리 버퍼에 모아 순차 append 하고 배치 compaction 으로 정리해 쓰기를 순차 I/O 로 바꾸기 때문",
    "LSM-tree 는 더 적은 디스크 공간을 사용하기 때문 — 같은 데이터가 한 번만 저장되므로 쓰기 양 자체가 B-tree 의 절반 이하로 준다" ] },
  { k: "재시도·백오프", o: [
    "즉시, 무한히 재시도한다 — 서버가 회복되는 첫 순간을 잡으려면 빈틈없이 두드려야 하고, 실패한 요청은 어차피 가벼워 부담이 없다",
    "정확히 같은 고정 간격으로 재시도한다 — 간격이 일정해야 서버가 부하를 예측할 수 있고, 클라이언트도 구현이 단순해 오류가 적다",
    "재시도를 완전히 비활성화한다 — 실패는 사용자에게 곧바로 알리고 다시 시도할지는 사람이 정하게 하면 폭풍은 생기지 않는다",
    "재시도 간격을 지수적으로 늘리되 무작위 jitter 를 섞는다(exponential backoff with jitter)" ] },
  { k: "sd2-health-check", o: [
    "'/' 가 아니라 정적 파일이 있는지만 확인한다 — 애플리케이션 코드를 거치지 않으므로 체크가 가볍고 오탐도 줄어든다",
    "핵심 의존성(DB 등) 상태까지 확인하는 심층(readiness) 헬스체크를 두어 준비 안 된 노드를 풀에서 제외",
    "헬스체크를 아예 없앤다 — 체크 자체가 부하를 만들고 오판으로 멀쩡한 노드를 뺄 수 있으므로 라우팅은 응답 오류율로만 판단한다",
    "헬스체크 주기를 1시간으로 늘린다 — DB 연결 상태는 자주 바뀌지 않으므로 드물게 확인하고 그 사이는 마지막 결과를 신뢰한다" ] },
  { k: "sd2-failover", o: [
    "failover 이후에는 롤백이 불가능하므로 하지 않는 편이 낫다 — 마스터가 되살아나도 되돌릴 수 없으니 사람이 확인한 뒤 수동으로 전환한다",
    "스탠바이는 승격 즉시 최신 데이터를 갖는다 — 복제가 항상 동기식이므로 마지막 트랜잭션까지 반영된 상태에서 마스터가 된다",
    "페일오버는 위험이 없어 별도 대비가 불필요하다 — 장애 감지와 승격이 자동화되어 있으면 사람이 개입할 여지가 없다",
    "네트워크 분단으로 양쪽이 서로 마스터가 되는 split-brain 위험이 있어, 쿼럼/펜싱으로 단일 마스터를 강제" ] },
  { k: "sysd-idem-2pc", o: [
    "코디네이터 장애 시 참여자들이 잠금을 쥔 채 블로킹되어 가용성과 지연이 크게 나빠지기 때문",
    "데이터 일관성을 보장하지 못하기 때문 — 준비 단계와 커밋 단계 사이에 참여자가 바뀌면 일부만 커밋되어 원자성이 깨진다",
    "구현할 수 있는 언어가 제한되기 때문 — XA 인터페이스는 자바와 C 에만 있어 다른 언어로 짠 서비스는 참여할 수 없다",
    "네트워크 대역폭을 지나치게 많이 쓰기 때문 — 준비·커밋 두 단계의 메시지가 참여자 수의 제곱으로 늘어 트래픽이 폭증한다" ] },
  { k: "sysd-dr-sync-repl", o: [
    "모든 쓰기가 복제 대상까지의 왕복 지연을 기다려야 하므로 쓰기 지연이 늘어난다",
    "백업을 받을 수 없게 된다 — 복제본이 항상 원본과 같으므로 스냅숏을 뜰 시점이 없고, 별도 백업은 복제와 충돌한다",
    "저장 용량이 절반으로 줄어든다 — 두 노드가 같은 데이터를 나눠 갖는 방식이라 각 노드는 원본의 반만 담을 수 있다",
    "읽기 일관성이 약해진다 — 복제본이 쓰기를 기다리는 동안 읽기는 옛 값을 돌려주므로 동기 복제가 오히려 stale 을 만든다" ] },
  { k: "일관해싱", o: [
    "노드가 링에 불균등하게 배치되어 부하가 치우친다; 해결책은 노드당 다수의 가상 노드(virtual node) 도입",
    "키 조회가 O(N) 이라 느리다 — 링을 처음부터 훑어 다음 노드를 찾아야 하므로 노드 수를 줄여 탐색을 짧게 하는 것이 해결책이다",
    "재해싱 때 모든 키가 옮겨 간다 — 링 위의 위치가 노드 수에 따라 전부 바뀌므로 해결책은 모듈로 해싱으로 바꾸는 것이다",
    "해시 충돌이 잦다 — 여러 노드가 링의 같은 지점에 떨어져 키가 사라지므로 해결책은 더 짧은 해시로 링을 촘촘하게 만드는 것이다" ] },
  { k: "사가vs2PC", o: [
    "saga 는 조정자(coordinator)가 잠금을 오래 유지해야 한다 — 모든 단계가 끝날 때까지 각 서비스의 행을 잠가 두어야 보상이 가능하다",
    "saga 는 격리성을 포기하며(중간 상태가 외부에 보임), 실패 시 보상 트랜잭션으로 되돌리는 최종 일관성 모델이다",
    "saga 는 원자성과 격리성을 모두 강하게 보장한다 — 단계마다 로컬 커밋을 하므로 2PC 와 같은 보장을 잠금 없이 얻는다",
    "saga 는 2PC 보다 더 강한 격리를 제공한다 — 각 단계가 독립된 트랜잭션이라 다른 사가가 중간 상태를 볼 수 없다" ] },
  { k: "완충 vs 처리량", o: [
    "큐는 처리량과 무관하며 오직 순서 보장을 위한 장치다 — 도착 순서가 그대로 유지되기 때문에 쓰는 것이고, 처리 속도와는 아무 관계가 없다",
    "네, 큐가 병렬 처리를 자동으로 늘려 준다 — 메시지가 쌓이면 브로커가 소비자를 더 띄워 주므로 처리량은 큐 길이에 맞춰 커진다",
    "아니요. 큐는 순간 폭주를 완충하고 생산과 소비를 분리할 뿐, 최종 처리량은 소비자 능력이 결정한다. 소비가 계속 밀리면 지연만 무한히 커진다",
    "네, 큐 크기를 늘리면 처리량도 비례해 늘어난다 — 버퍼가 클수록 한 번에 더 많은 메시지를 받아들이므로 초당 처리 건수가 오른다" ] },
] },
];
module.exports = all;
