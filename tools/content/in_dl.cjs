/* 딥러닝 단답 15 — 층의 성질과 크기 계산을 적어 본다.
   검증:  node ver_input.cjs ./in_dl.cjs
   출력값 대조:  node chk_predict.cjs ./in_dl.cjs */
module.exports = [

{ track:"dl", t:"input", cat:"internals", k:"음수를 만나면 꺾인다",
  q:"ReLU 활성화 함수에 음수를 넣으면 무엇이 나올까요? (숫자)",
  a:["0"],
  ex:"양수는 그대로 통과시키고 음수는 0 으로 만듭니다. 계산이 아주 싸고 기울기가 1 이라 <b>깊게 쌓아도 기울기가 잘 흐릅니다</b>.\n개념: 대신 음수 쪽 기울기가 0 이라, 한번 죽은 뉴런은 되살아나지 못할 수 있습니다(dying ReLU).\n실무: 그 문제가 보이면 음수 쪽에 작은 기울기를 남기는 변형(LeakyReLU·GELU)을 쓰거나 학습률을 낮추세요." },

{ track:"dl", t:"input", cat:"internals", k:"에스자 곡선의 위쪽 끝",
  q:"시그모이드 함수의 출력이 다가가는 가장 큰 값은 얼마일까요? (숫자)",
  a:["1", "1.0"],
  ex:"출력 범위가 0 과 1 사이라 <b>이진 분류의 확률</b>로 쓰기 좋습니다.\n개념: 다만 양 끝에서 기울기가 거의 0 이라, 깊게 쌓으면 <b>기울기가 사라집니다</b> — 은닉층 활성화로는 요즘 거의 쓰지 않습니다.\n실무: 마지막 층에 이 함수를 두었다면 손실은 <code>BCEWithLogits</code> 처럼 <b>로짓을 직접 받는</b> 것을 쓰세요. 따로 계산하면 수치가 불안정해집니다." },

{ track:"dl", t:"input", cat:"internals", k:"확률로 만들면 합은",
  q:"소프트맥스를 통과한 출력 벡터의 원소를 모두 더하면 얼마가 될까요? (숫자)",
  a:["1", "1.0"],
  ex:"지수를 취한 뒤 전체 합으로 나누므로 합이 언제나 1 입니다 — 그래서 <b>다중 분류의 확률</b>로 읽습니다.\n개념: 입력에 같은 상수를 더하거나 빼도 결과가 <b>정확히 같습니다</b>. 그래서 최댓값을 빼는 것이 근사가 아니라 항등식입니다.\n실무: 라벨이 여러 개 붙을 수 있는 문제(멀티라벨)에는 쓰면 안 됩니다 — 합이 1 이라 서로 경쟁하기 때문입니다. 그때는 클래스마다 시그모이드를 씁니다." },

{ track:"dl", t:"input", cat:"internals", k:"추론할 때는 끈다",
  q:"학습 중에 뉴런 일부를 무작위로 꺼서 과적합을 막는 기법은 <b>추론</b>할 때 어떻게 해야 할까요? (두 글자, 한글)",
  a:["끈다", "비활성", "off", "해제"],
  ex:"끄지 않으면 예측할 때마다 결과가 달라집니다. 프레임워크의 <code>eval()</code>·<code>model.eval</code> 이 이 전환을 해 줍니다.\n개념: 학습 때 확률 p 로 껐으므로, 추론 때는 출력 크기를 맞추기 위한 보정이 필요합니다 — 요즘 구현은 학습 때 미리 나눠 둡니다(inverted dropout).\n실무: '학습은 잘되는데 추론만 이상하다' 는 증상의 단골 원인입니다. 배치 정규화도 같은 전환이 필요하니 두 가지를 함께 확인하세요." },

{ track:"dl", t:"input", cat:"internals", k:"무엇을 기준으로 정규화하나",
  q:"배치 정규화는 각 특성 채널의 통계를 무엇의 단위로 계산할까요? (세 글자, 한글)",
  a:["배치", "미니배치", "batch", "미니배치단위"],
  ex:"미니배치 안에서 평균과 분산을 구해 정규화하므로, <b>배치 크기가 작으면 통계가 흔들립니다</b>.\n개념: 추론 때는 배치가 없으니 학습 중 모아 둔 <b>이동 평균</b>을 씁니다 — 학습과 추론의 동작이 다른 대표적인 층입니다.\n실무: 배치가 아주 작거나 시퀀스 길이가 제각각이면 층 정규화(LayerNorm)를 쓰는 편이 안정적입니다 — 트랜스포머가 그쪽을 택한 이유입니다." },

{ track:"dl", t:"input", cat:"internals", k:"깊어질수록 기울기가 사라진다",
  q:"층을 깊게 쌓았을 때 앞쪽 층의 기울기가 0 에 가까워져 학습이 안 되는 현상을 무엇이라 부를까요? (다섯 글자, 한글)",
  a:["기울기소실", "gradient vanishing", "그래디언트소실", "경사소실"],
  ex:"1 보다 작은 기울기가 층마다 <b>곱해지면서</b> 지수적으로 작아집니다.\n개념: 반대로 1 보다 큰 값이 곱해지면 폭발합니다 — 그때는 기울기 자르기(clipping)로 막습니다.\n실무: 대책은 셋입니다 — 기울기가 1 인 활성화(ReLU 계열), 정규화 층, 그리고 <b>지름길 연결</b>. 셋 다 넣는 것이 현대 구조의 기본입니다." },

{ track:"dl", t:"input", cat:"internals", k:"층을 건너뛰는 지름길",
  q:"입력을 몇 층 건너뛰어 출력에 그대로 더해 주는 연결을 무엇이라 부를까요? (세 글자, 한글 또는 영문)",
  a:["잔차연결", "skip connection", "residual", "스킵연결", "잔차"],
  ex:"덧셈 경로가 있으므로 기울기가 <b>1 의 배수로 곧장 흘러</b> 앞쪽 층까지 닿습니다.\n개념: 또 하나의 효과는 '<b>아무것도 안 하기</b>' 가 쉬워지는 것입니다 — 층이 0 을 학습하면 항등 함수가 되어, 층을 늘려도 성능이 나빠지지 않습니다.\n실무: 이 아이디어 하나로 100 층이 넘는 망을 학습할 수 있게 됐고, 트랜스포머의 모든 블록에도 들어 있습니다." },

{ track:"dl", t:"input", cat:"internals", k:"두 아이디어를 합친 최적화기",
  q:"아담(Adam)은 관성으로 방향을 유지하는 아이디어와, 파라미터마다 보폭을 조절하는 아이디어를 합친 것입니다. 앞쪽 아이디어의 이름은? (세 글자, 한글)",
  a:["모멘텀", "momentum", "관성"],
  ex:"기울기의 지수 이동 평균으로 <b>방향의 관성</b>을 유지하고, 제곱의 이동 평균으로 <b>파라미터마다 보폭</b>을 나눕니다.\n개념: 초기에는 이동 평균이 0 에 치우쳐 있어 <b>편향 보정</b>을 함께 합니다 — 그 항이 없으면 처음 몇 스텝이 지나치게 작습니다.\n실무: 기본값(lr 1e-3, β 0.9/0.999)이 웬만하면 잘 듣습니다. 다만 가중치 감쇠를 제대로 걸려면 <code>AdamW</code> 를 쓰세요 — 원래 구현은 감쇠가 보폭에 섞입니다." },

{ track:"dl", t:"input", cat:"internals", k:"처음에는 살살 올린다",
  q:"학습 초반에 학습률을 0 에서 목표치까지 서서히 올리는 구간을 무엇이라 부를까요? (네 글자, 한글)",
  a:["워밍업", "warmup", "웜업", "예열"],
  ex:"초반에는 파라미터가 무작위라 큰 보폭이 <b>엉뚱한 방향으로 크게</b> 움직이게 만듭니다.\n개념: 적응형 최적화기는 초반 통계가 불안정해 특히 그렇습니다 — 그래서 큰 배치·큰 모델일수록 이 구간이 중요합니다.\n실무: 올린 뒤에는 코사인이나 계단 모양으로 줄여 내립니다. 학습 초반에 손실이 <code>NaN</code> 이 된다면 이 구간을 늘리는 것이 가장 먼저 해 볼 조치입니다." },

{ track:"dl", t:"input", cat:"internals", k:"합성곱을 지나면 몇 칸",
  q:"길이 7 인 입력에 커널 3, 스트라이드 2, 패딩 0 인 합성곱을 걸면 출력 길이는 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"const n = 7, k = 3, s = 2, p = 0;\nconsole.log(Math.floor((n + 2 * p - k) / s) + 1);",
  a:["3"],
  ex:"<code>(7 - 3) / 2 + 1 = 3</code> 입니다. 커널이 놓일 수 있는 <b>시작 자리의 개수</b>를 세는 셈입니다.\n개념: 크기를 유지하려면 스트라이드 1 에 패딩을 <code>(k-1)/2</code> 로 둡니다 — 커널 3 이면 패딩 1 입니다.\n실무: 층을 쌓다 크기가 0 이하가 되어 나는 오류가 흔합니다. 설계할 때 층마다 크기를 손으로 적어 내려가 보세요." },

{ track:"dl", t:"input", cat:"internals", k:"완전 연결층의 파라미터 수",
  q:"입력 4, 출력 3 인 완전 연결층의 파라미터는 편향까지 합쳐 모두 몇 개일까요? 출력되는 값을 적으세요. (숫자)",
  code:"const i = 4, o = 3;\nconsole.log(i * o + o);",
  a:["15"],
  ex:"가중치 <code>4 × 3 = 12</code> 개에 출력마다 하나씩 붙는 편향 3 개를 더해 15 입니다.\n개념: 완전 연결층의 파라미터는 <b>입력 크기에 비례해 폭발</b>합니다 — 이미지에 그대로 쓰지 않고 합성곱을 쓰는 이유입니다.\n실무: 모델이 큰데 대부분의 파라미터가 마지막 분류층에 몰려 있는 경우가 많습니다. 파라미터 수를 층별로 찍어 보면 줄일 곳이 바로 보입니다." },

{ track:"dl", t:"input", cat:"internals", k:"어텐션의 세 가지",
  q:"어텐션은 쿼리와 키의 유사도로 가중치를 만들고, 그 가중치로 무엇을 섞을까요? (한 글자, 한글)",
  a:["값", "value", "밸류"],
  ex:"쿼리·키·값 셋으로 이뤄집니다 — '<b>무엇을 찾는가</b>(쿼리)', '<b>무엇으로 찾을 수 있는가</b>(키)', '<b>실제로 가져올 내용</b>(값)' 입니다.\n개념: 쿼리와 키의 내적을 <code>√d</code> 로 나누는 이유는, 차원이 커지면 내적이 커져 소프트맥스가 <b>한쪽으로 쏠리기</b> 때문입니다.\n실무: 시퀀스 길이의 제곱에 비례해 계산과 메모리가 늘어납니다 — 긴 문서를 다룰 때 가장 먼저 부딪히는 벽입니다." },

{ track:"dl", t:"input", cat:"internals", k:"순서를 알려 주는 신호",
  q:"어텐션은 순서를 모르기 때문에 입력에 자리 정보를 더해 줍니다. 이 신호를 무엇이라 부를까요? (여섯 글자, 한글)",
  a:["위치인코딩", "positional encoding", "포지셔널인코딩", "위치임베딩"],
  ex:"어텐션은 집합처럼 다루므로, 이 신호가 없으면 <b>단어 순서를 바꿔도 결과가 같습니다</b>.\n개념: 사인·코사인으로 고정해 주는 방식과 학습으로 배우는 방식이 있고, 요즘은 상대 위치를 회전으로 넣는 방식(RoPE)이 널리 쓰입니다.\n실무: 학습 때 본 길이보다 긴 입력에서 성능이 무너진다면 이 부분을 먼저 의심하세요 — 길이 일반화는 방식마다 크게 다릅니다." },

{ track:"dl", t:"input", cat:"internals", k:"희소한 번호를 촘촘한 벡터로",
  q:"단어 번호처럼 큰 정수 하나를 촘촘한 실수 벡터로 바꿔 주는, 사실상 조회 표인 층의 이름은? (네 글자, 한글)",
  a:["임베딩", "embedding", "임베딩층", "매입"],
  ex:"원-핫 벡터에 행렬을 곱한 것과 같지만, 구현은 <b>행 하나를 꺼내는 조회</b>라 훨씬 빠릅니다.\n개념: 그 벡터들이 학습을 거치며 <b>뜻이 가까운 것끼리 모입니다</b> — 그래서 코사인 유사도로 관련어를 찾을 수 있습니다.\n실무: 어휘가 크면 이 표가 모델 파라미터의 절반을 넘기도 합니다. 입력과 출력에서 같은 표를 나눠 쓰는(weight tying) 방법으로 크게 줄일 수 있습니다." },

{ track:"dl", t:"input", cat:"internals", k:"더 나빠지기 전에 멈추기",
  q:"검증 손실이 더 이상 줄지 않고 오르기 시작할 때 학습을 멈추는 기법을 무엇이라 부를까요? (네 글자, 한글)",
  a:["조기종료", "early stopping", "조기중단", "얼리스탑핑"],
  ex:"그 지점부터는 <b>잡음을 외우기 시작</b>하므로, 검증 성능이 가장 좋았던 가중치를 되살려 씁니다.\n개념: 규제의 한 형태로 볼 수 있습니다 — 파라미터가 커질 시간을 주지 않기 때문입니다.\n실무: 손실은 오르내리므로 <b>몇 번 참을지</b>(patience)를 정해 둬야 합니다. 그리고 멈춘 시점이 아니라 <b>가장 좋았던 시점</b>의 체크포인트를 저장하는지 반드시 확인하세요." },

];
