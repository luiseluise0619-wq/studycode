module.exports = {
  track: "javascript", guide: "📜", xp: 70,
  unit: "로그 분석 II — 이벤트 루프와 프로세스",
  source: "./log_javascript.cjs",
  lessons: [
    { t: "루프를 막는 것들", n: 5,
      th: { sum: "Node 는 한 줄기로 돈다. **한 요청이 오래 붙잡으면 전부가 멈춘다.**",
        body: [
          { h: "동기 작업과 역압", t: "<code>JSON.parse</code> 같은 동기 호출은 그 시간만큼 이벤트 루프를 멈추므로, 큰 본문 하나가 헬스체크까지 죽입니다. 스트림에서 <code>write()</code> 의 반환값을 무시하면 느린 클라이언트에게 보낼 데이터가 메모리에 쌓입니다. <b>이벤트 루프 지연</b>과 <b>RSS</b> 를 지표로 두면 둘 다 먼저 보입니다." },
          { h: "기다릴 것과 아닌 것", t: "루프 안의 <code>await</code> 는 독립적인 호출을 줄 세우고, 반대로 <code>await</code> 를 빼먹은 프라미스는 잡히지 않은 거절로 <b>프로세스를 끕니다</b>. '기다려야 하는가' 를 매번 정하는 것이 규칙입니다." }],
        code: { c: "await Promise.all(ids.map(fetchOne))\npipeline(src, res)          // 역압 자동\nsendMail(...).catch(log)    // 떠 있는 프라미스 금지", cap: "루프를 막지도, 놓치지도 않는다" },
        key: ["동기 파싱이 전체를 멈춘다", "write 반환값이 역압 신호", "루프 안 await 는 직렬화", "잡지 않은 거절은 종료"] } },
    { t: "프로세스마다 다른 세계", n: 5,
      th: { sum: "워커·모듈·환경변수는 **경계가 어디인지** 모르면 반드시 사고가 난다.",
        body: [
          { h: "한 프로세스의 상태는 그 프로세스만의 것", t: "클러스터 워커마다 카운터를 두면 한도가 워커 수만큼 곱해지고, 모듈 스코프의 커넥션 풀은 테스트 스위트 사이로 상태를 나릅니다. <b>공유해야 하는 상태</b>는 프로세스 밖(레디스·DB)에 두고, <b>공유하면 안 되는 것</b>은 함수가 만들어 돌려주게 합니다." },
          { h: "문자열은 문자열이다", t: "환경변수 <code>\"false\"</code> 는 참이고, 날짜만 있는 ISO 문자열은 UTC 자정으로 해석됩니다. 해석 규칙을 모르면 끄려던 스위치가 켜지고 마감일이 하루 당겨집니다. 기동 로그에 <b>해석된 값</b>을 찍는 습관이 이런 것을 잡아 줍니다." }],
        code: { c: "const on = process.env.FLAG === \"true\"\nnew Date(\"2026-08-24\")      // UTC 자정\nrate limit → 공유 저장소", cap: "경계를 알고 상태를 둔다" },
        key: ["워커별 한도는 곱해진다", "모듈은 캐시된다", "환경변수는 문자열", "날짜 문자열은 UTC 해석"] } },
  ],
};
