module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 투 포인터·슬라이딩 윈도우·누적합",
  source: "./algo_window.cjs",
  lessons: [
    { t: "투 포인터 — 버려도 되는 후보를 논증한다", n: 5,
      th: { sum: "투 포인터가 O(n²) 를 O(n) 으로 접는 근거는 '**지금 버리는 후보가 정말 답이 될 수 없다**' 는 논증이다. 그 문장을 못 만들면 쓰면 안 된다.",
        body: [
          { h: "전제가 있어야 방향을 정할 수 있다", t: "정렬된 배열에서 합이 작으면 왼쪽을 올린다 — 지금의 왼쪽은 **가장 큰 오른쪽과도 모자랐으니** 어떤 짝과도 답이 될 수 없다. 물통 문제에서 낮은 쪽을 옮기는 것도 같다 — 높은 쪽을 옮기면 거리는 줄고 높이는 그대로라 반드시 손해다. 정렬이 없거나 이런 단조성이 없으면 방향 판단 자체가 불가능하고, 그때는 해시나 누적합으로 간다." },
          { h: "포인터 세 개로 구간을 나눈다", t: "0·1·2 정렬(네덜란드 국기)은 배열을 '**0 구역 / 미확인 / 2 구역**' 으로 나눠 가며 미확인을 줄인다. 2를 뒤로 보낸 뒤 `mid` 를 **올리지 않는** 것이 핵심 — 뒤에서 가져온 값을 아직 안 봤기 때문이다. 같은 3분할이 중복 값이 많은 배열의 퀵 정렬을 눈에 띄게 빠르게 만든다." }],
        code: { c: "if (sum < target) lo++;   // 이 lo 는 어떤 짝과도 안 된다\nelse hi--;", cap: "버리는 근거가 곧 정당성" },
        key: ["정렬·단조성이 전제", "버리는 후보의 근거를 말할 수 있어야", "lo < hi 로 같은 자리 두 번 막기", "3분할은 mid 를 언제 올리는지가 전부"] } },
    { t: "슬라이딩 윈도우 — 겹치는 계산을 버리지 않는다", n: 5,
      th: { sum: "창을 한 칸 밀 때 **전부 다시 계산하지 않는다.** 들어온 하나를 더하고 나간 하나를 빼면 O(1) 이라 전체가 O(n) 이 된다.",
        body: [
          { h: "고정 창과 가변 창", t: "크기가 정해진 창은 `sum += xs[i] - xs[i-k]` 한 줄이면 된다. 크기가 정해지지 않은 창은 **오른쪽을 늘리고, 조건을 어기면 왼쪽을 당긴다.** 안쪽에 `while` 이 있어도 두 포인터가 각각 n 번만 앞으로 가므로 전체는 여전히 O(n) 이다 — `lo` 가 되돌아가지 않는다는 것이 근거다." },
          { h: "음수가 섞이면 무너진다", t: "'조건을 만족하는 동안 창을 줄인다' 는 논리는 **줄이면 합이 준다**는 전제 위에 있다. 음수가 있으면 창을 줄였는데 합이 늘 수 있어 이 전제가 깨진다. 그때는 누적합 + 해시로 간다. 최댓값 초기화를 0으로 두는 것도 흔한 함정이다 — 전부 음수인 입력에서 있지도 않은 0이 답이 된다. **첫 창의 합**으로 시작해야 한다." }],
        code: { c: "for (hi...) {\n  sum += xs[hi];\n  while (sum >= target) { best = min(best, hi-lo+1); sum -= xs[lo]; lo++; }\n}", cap: "lo 는 되돌아가지 않는다 — 그래서 O(n)" },
        key: ["더하고 빼기만 한다", "가변 창은 '어기면 왼쪽을 당긴다'", "양수 전제를 확인한다", "초기값을 0으로 두지 않는다"] } },
    { t: "누적합 — 미리 계산해 두고 빼기 한 번", n: 2,
      th: { sum: "질의가 많으면 **한 번 만들어 두고 빼기 한 번**으로 답한다. 구간 합 문제의 기본기다.",
        body: [
          { h: "0으로 시작해 한 칸 늘린다", t: "누적합 배열을 `[0]` 으로 시작해 n + 1 칸으로 두면 `l` 이 0일 때도 `acc[l]` 이 존재해 경계 분기가 사라진다. **배열을 한 칸 늘려 `if` 를 없애는** 기법은 곳곳에서 쓰인다. 포함·미포함 규약은 주석이 아니라 테스트로 못 박아 둔다 — `acc[r+1] - acc[l]` 에서 `+1` 하나가 답을 바꾼다." },
          { h: "차이가 k 인 쌍을 세면 구간을 센 것이다", t: "구간 `(i, j]` 의 합이 `acc[j] - acc[i]` 이므로, 합이 k 인 구간의 개수는 **차이가 k 인 누적합 쌍**의 개수다. 해시에 지금까지 본 누적합의 빈도를 담아 두면 한 번 훑는 것으로 끝난다. `{0: 1}` 로 미리 채워 두어야 처음부터 시작하는 구간을 셀 수 있다. 이 방법은 **음수가 있어도 그대로 성립**한다는 점에서 슬라이딩 윈도우와 갈린다." }],
        code: { c: "found += seen.get(acc - k) || 0;\nseen.set(acc, (seen.get(acc) || 0) + 1);", cap: "차이가 k 인 쌍 = 합이 k 인 구간" },
        key: ["질의가 많으면 미리 계산", "0으로 시작해 한 칸 늘린다", "합이 k → 차이가 k 인 쌍", "음수가 있으면 누적합·해시로"] } },
  ],
};
