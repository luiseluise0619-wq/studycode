/* cpp·linux·devops — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다.
   opt_backend.cjs 와 같은 꼴 — 트랙별 SPEC 배열. */
module.exports = [
{ track: "cpp", fixes: [
  { k: "cpp-vf-final", o: [
    "소멸자를 자동 생성한다 — final 이 붙은 클래스는 더 파생되지 않으므로 컴파일러가 가상 소멸자를 대신 만들어 준다",
    "가상 함수에 붙이면 더 이상 재정의할 수 없고, 클래스에 붙이면 더 이상 상속할 수 없다",
    "해당 함수가 반드시 재정의되어야 함을 뜻한다 — 순수 가상 함수의 다른 표기로, 파생 클래스가 구현하지 않으면 컴파일 오류다",
    "객체를 불변으로 만든다 — final 이 붙은 클래스의 인스턴스는 생성 뒤 멤버를 바꿀 수 없어 const 와 같은 효과를 낸다" ] },
  { k: "cpp-raii-newdelete", o: [
    "delete 가 성능이 매우 느리기 때문 — 힙 관리자를 매번 부르므로 스마트 포인터의 풀 할당보다 몇 배 느리다",
    "예외나 이른 return 경로에서 delete 가 누락되기 쉬우며, 스마트 포인터가 같은 일을 안전하게 대신하기 때문",
    "new 가 표준에서 제거되었기 때문 — C++17 부터 new 식은 비권장으로 표시되고 C++20 에서 삭제되어 컴파일 경고가 난다",
    "new 가 실패하면 nullptr 을 반환하기 때문 — 예외를 던지지 않으므로 호출마다 검사하지 않으면 널 역참조가 난다" ] },
  { k: "cpp-vf-dtor-design", o: [
    "비가상으로 두는 것이 성능상 낫다 — 소멸자가 가상이면 모든 객체에 vptr 이 붙어 크기가 늘고 삭제마다 간접 호출이 생긴다",
    "private virtual 로 둔다 — 파생 클래스가 소멸자를 부를 수 없게 막아 두면 기반 포인터로만 삭제하도록 강제할 수 있다",
    "소멸자를 선언하지 않는 것이 최선이다 — 컴파일러가 알아서 만들고, 다형적으로 쓰이면 가상으로 만들어 주기 때문이다",
    "다형적으로 쓰면 public virtual, 그럴 의도가 없으면 protected non-virtual 로 둔다" ] },
  { k: "cpp-move-named", o: [
    "store 가 rvalue 만 받기 때문 — 매개변수 타입이 T&& 라 lvalue 를 넘기면 오버로드가 없어 std::move 로 바꿔 주어야 한다",
    "이름이 붙은 rvalue 참조 매개변수 s 자체는 lvalue 이므로, 그냥 넘기면 복사가 선택되기 때문",
    "std::move 없이는 컴파일되지 않기 때문 — s 의 타입이 std::string&& 라 std::string 을 받는 함수에 그대로 넘길 수 없다",
    "s 가 이미 파괴되었기 때문 — rvalue 참조로 받은 객체는 호출 직후 소멸하므로 move 로 내용을 먼저 옮겨 두어야 한다" ] },
  { k: "cpp-stl-deque", o: [
    "임의 접근이 불가능하다 — 양 끝에서만 넣고 뺄 수 있는 큐라 d[i] 같은 인덱스 접근은 제공하지 않는다",
    "앞쪽 삽입·삭제도 O(1) 이지만 메모리가 하나의 연속 블록이 아니다",
    "어떤 경우에도 vector 보다 빠르다 — 청크를 나눠 관리하므로 재할당이 없고 그만큼 push_back 도 항상 더 싸다",
    "정렬 상태를 유지한다 — 원소를 넣을 때마다 제자리를 찾아 끼우므로 언제 읽어도 오름차순이 보장된다" ] },
  { k: "cpp-const-mutable", o: [
    "논리적 상태와 무관한 캐시나 뮤텍스를 const 멤버 함수 안에서도 수정할 수 있게 한다",
    "멤버를 정적으로 만든다 — 모든 인스턴스가 공유하는 하나의 저장소가 되어 const 객체에서도 값을 바꿀 수 있다",
    "멤버를 스레드 안전하게 만든다 — mutable 이 붙은 멤버는 접근할 때 컴파일러가 락을 끼워 넣어 동시 수정을 막는다",
    "모든 const 검사를 무력화해 준다 — 클래스에 mutable 멤버가 하나라도 있으면 그 클래스의 const 멤버 함수 전체가 예외가 된다" ] },
  { k: "cpp2-val-shallow", o: [
    "포인터 멤버가 있으면 복사 생성자가 생성되지 않아 복사할 때마다 컴파일 에러가 난다 — 그래서 직접 정의해야만 복사가 된다",
    "포인터를 nullptr 로 초기화해 원본 데이터를 잃는다 — 암시적 복사는 포인터 멤버를 안전하게 비워 두므로 사본에서 접근하면 크래시가 난다",
    "포인터 값만 그대로 복사(얕은 복사)하므로 두 객체가 같은 메모리를 가리키다가 소멸 시 이중 해제가 발생한다",
    "복사할 때마다 새 메모리를 할당해 메모리 누수가 생긴다 — 암시적 복사는 깊은 복사를 하지만 원본 버퍼를 해제하지 않는다" ] },
  { k: "cpp3-uninit-read", o: [
    "정의되지 않은 동작이며, 테스트에서는 우연히 맞는 값이 들어와 통과할 수도 있다",
    "0 이 읽히므로 논리 오류만 생긴다 — 스택 프레임은 함수 진입 때 0 으로 채워지므로 값은 예측 가능하고 크래시는 없다",
    "컴파일러가 컴파일 에러로 막아 주므로 실행까지 가지 않는다 — 초기화되지 않은 변수의 읽기는 정적으로 잡히는 오류다",
    "항상 즉시 크래시하므로 쉽게 발견된다 — 초기화되지 않은 메모리는 접근 불가로 표시되어 읽는 순간 세그폴트가 난다" ] },
  { k: "cpp-wptr-role", o: [
    "단독으로 역참조해 값을 읽을 수 있다 — shared_ptr 과 같은 제어 블록을 보므로 * 와 -> 를 그대로 쓸 수 있다",
    "항상 유효한 객체를 보장한다 — 참조 카운트를 올리지 않아도 제어 블록이 객체를 붙들어 두므로 대상이 사라지지 않는다",
    "소유권 카운트를 올리지 않고 대상을 관찰하며, lock() 으로 유효할 때만 shared_ptr 을 얻는다",
    "shared_ptr 보다 빠른 소유 포인터로 쓰인다 — 카운트를 세지 않으므로 복사가 싸고, 소유권이 필요한 자리에 대신 쓸 수 있다" ] },
] },
{ track: "linux", fixes: [
  { k: "로그 회전", o: [
    "회전 방식이 삭제보다 더 빠르게 처리되기 때문 — 이름만 바꾸므로 큰 파일을 지우느라 디스크가 바쁜 시간이 없다",
    "삭제에는 루트 권한이 필요하기 때문 — 서비스 계정은 자기 로그를 지울 수 없어 이름을 바꾸는 방법밖에 없다",
    "백업 정책에 어긋나기 때문 — 지운 파일은 백업에 잡히지 않으므로 옮겨 두었다가 백업이 돈 뒤에 지워야 한다",
    "프로세스가 열고 있는 파일을 지우면 공간이 반환되지 않고 기록도 끊기기 때문" ] },
  { k: "서비스가 안 뜬다", o: [
    "systemctl status 와 journalctl 로 실패 이유를 읽는다",
    "설정 파일을 통째로 지우고 처음부터 다시 작성해 본다 — 어디가 틀렸는지 찾는 것보다 깨끗한 설정으로 다시 시작하는 편이 빠르다",
    "서버를 재부팅한 뒤 다시 시도한다 — 서비스가 안 뜨는 원인의 대부분은 꼬인 프로세스 상태라 재부팅이 한 번에 정리한다",
    "포트 번호를 바꿔 본다 — 시작 실패의 가장 흔한 원인이 포트 충돌이므로 다른 번호로 뜨면 원인이 확인된 셈이다" ] },
  { k: "보관 기간", o: [
    "일주일이면 어떤 경우에도 충분하다 — 장애는 발생 직후에 조사하므로 그보다 오래된 로그를 다시 볼 일은 없다",
    "보관하지 않고 즉시 삭제한다 — 로그는 유출 위험만 키우므로 모니터링 지표로 뽑은 뒤 원본은 남기지 않는다",
    "장애 조사에 필요한 기간과 법적 요구, 개인정보 보관 원칙을 함께 본다",
    "디스크 용량이 허락하는 한 최대한 오래 보관해 둔다 — 언제 필요할지 모르므로 지우는 것보다 남기는 쪽이 안전하다" ] },
  { k: "첫 확인", o: [
    "애플리케이션 소스 코드를 열어 처음부터 훑어본다 — 느린 원인은 결국 코드에 있으므로 서버 지표보다 로직을 먼저 본다",
    "서버를 먼저 재부팅해 본다 — 30초 안에 해결될 가능성이 가장 높은 조치이고, 그래도 느리면 그때 원인을 찾는다",
    "uptime·free·df 로 부하·메모리·디스크를 한 번에 훑는다",
    "네트워크 케이블과 스위치 포트 상태를 확인한다 — '느리다' 는 신고의 대부분은 물리 계층의 패킷 손실에서 온다" ] },
  { k: "연결 상태", o: [
    "서버 자체에 장애가 발생하고 있다는 확실한 신호다 — 응답하지 못한 연결이 닫히지 못하고 쌓여 있는 상태다",
    "방화벽이 연결을 끊고 있다 — 중간에서 RST 를 보내면 양쪽이 TIME-WAIT 으로 남으므로 방화벽 로그를 먼저 본다",
    "연결을 먼저 닫은 쪽에 정상적으로 남는 상태이며 대개 문제가 아니다",
    "메모리가 부족한 상태다 — 커널이 소켓 버퍼를 확보하지 못하면 연결을 완전히 닫지 못하고 TIME-WAIT 에 세워 둔다" ] },
  { k: "구조화", o: [
    "파일 크기가 줄어든다는 점 — 필드 이름이 짧은 키로 바뀌어 같은 내용을 텍스트 로그보다 작게 담을 수 있다",
    "사람이 읽기 더 편하다는 점 — 필드가 이름과 함께 찍히므로 tail 로 볼 때도 어떤 값인지 바로 알 수 있다",
    "기계가 필드를 정확히 뽑을 수 있어 필터링과 집계가 쉬워진다는 점",
    "저장하는 시점에 암호화가 자동으로 적용된다는 점 — JSON 로거는 민감 필드를 감지해 마스킹한 뒤 기록한다" ] },
  { k: "키 관리", o: [
    "서버를 재부팅한다 — 열려 있던 SSH 세션이 모두 끊기고, 재부팅 뒤에는 캐시된 키가 지워져 다시 들어올 수 없다",
    "모든 서버의 authorized_keys 에서 해당 공개키를 제거한다",
    "SSH 포트를 변경한다 — 퇴사자는 새 포트를 모르므로 접속 시도 자체가 실패하고, 나머지 팀원에게만 새 포트를 알린다",
    "그 사람이 쓰던 계정의 비밀번호를 새로 변경해 둔다 — 비밀번호를 모르면 키가 있어도 로그인 단계에서 막힌다" ] },
  { k: "스크립트의 한계", o: [
    "실행 속도가 느려졌을 때 — Ansible 은 병렬로 돌기 때문에 스크립트가 몇 분 이상 걸리기 시작하면 바꿀 때다",
    "다른 팀이 요청했을 때 — 도구 선택은 조직 표준을 따르는 문제라 요청이 오기 전까지는 스크립트로 충분하다",
    "직접 작성한 스크립트가 100줄을 넘어섰을 때 — 그 길이부터는 셸 문법의 한계로 읽기 어려워지므로 도구로 옮긴다",
    "여러 서버에 적용해야 하고 이미 적용된 상태를 건너뛰어야 할 때" ] },
] },
{ track: "devops", fixes: [
  { k: "layer-cache", o: [
    "package.json/package-lock.json 만 먼저 COPY 해 npm ci 를 실행하고, 그 다음 COPY . 을 한다",
    "COPY . 을 두 번 반복한다 — 첫 COPY 의 레이어가 캐시되어 있으면 두 번째는 바뀐 파일만 다시 올라가므로 install 이 재실행되지 않는다",
    "RUN npm ci 를 맨 위로 옮긴다 — 소스보다 먼저 실행되면 그 레이어는 소스 변경과 무관해져 캐시가 계속 유지된다",
    "--no-cache 옵션으로 새로 빌드한다 — 잘못 남은 캐시가 원인이므로 한 번 깨끗하게 빌드하면 이후부터는 캐시가 제대로 잡힌다" ] },
  { k: "migration-order", o: [
    "마이그레이션을 롤백한 뒤 재시도한다 — 컬럼을 되살리면 구버전이 다시 동작하므로, 파드 교체가 끝난 직후에 같은 마이그레이션을 다시 돌린다",
    "배포와 파괴적 마이그레이션을 동시에 실행한다 — 두 작업을 같은 순간에 시작하면 구버전이 새 스키마를 볼 시간이 거의 없어 오류가 사라진다",
    "expand-and-contract: 먼저 하위호환 변경(추가) 배포 후, 구버전 제거가 끝난 다음 파괴적 변경(삭제)",
    "구버전 파드를 먼저 전부 죽인다 — 롤링 대신 재생성 전략으로 바꾸면 두 버전이 공존하는 구간이 없어 마이그레이션 순서를 신경 쓸 필요가 없다" ] },
  { k: "레이어캐시", o: [
    "소스가 조금만 바뀌어도 COPY 레이어 해시가 변해 그 이후의 install 레이어 캐시가 전부 무효화된다",
    "레이어 순서는 캐시에 영향을 주지 않는다 — 각 명령은 자기 입력만으로 해시를 계산하므로 install 은 package.json 이 같으면 언제나 캐시를 탄다",
    "COPY 는 캐시를 지원하지 않는 명령이기 때문이다 — 파일 복사는 매번 새로 실행되므로 어디에 두든 그 뒤는 다시 빌드된다",
    "install 이 COPY 보다 먼저 실행되기 때문이다 — 빌드는 RUN 을 먼저 모아 돌리므로 COPY 위치와 무관하게 install 은 캐시를 못 쓴다" ] },
  { k: "RED-USE", o: [
    "RED 는 인프라, USE 는 애플리케이션용이다 — 요청 지표는 로드밸런서가 내고 자원 지표는 앱이 스스로 재기 때문이다",
    "RED 는 각각 로그를, USE 는 트레이스를 다루기 위한 표준이다 — 세 글자는 로그 필드와 스팬 속성의 이름을 뜻한다",
    "RED 는 요청 기반 서비스(핸들러)의 사용자 관점 지표, USE 는 리소스(CPU/디스크/큐 등) 관점 지표에 적합하다",
    "둘 다 동일하며 이름만 다르다 — Errors 가 겹치는 데서 보듯 같은 세 지표를 부르는 회사별 이름일 뿐이다" ] },
  { k: "오개념", q: "카나리 배포를 하면 롤백", o: [
    "일부 사용자에게는 이미 새 버전이 노출되므로, 이상 감지 시 되돌리는 절차가 여전히 필요하기 때문",
    "카나리는 배포가 아니라 테스트이기 때문 — 실제 트래픽으로 검증만 하고 결과가 좋으면 그때 진짜 배포를 따로 한다",
    "카나리는 스테이징에서만 동작하기 때문 — 운영 환경으로 나가기 전 단계라 되돌릴 대상이 애초에 없다",
    "카나리 방식은 롤백 기능을 포함하지 않기 때문 — 비중을 줄이는 기능만 있고 이전 버전으로 되돌리는 절차는 다른 도구가 맡는다" ] },
  { k: "capacity", o: [
    "성장 예측과 부하 특성을 근거로 여유(headroom)를 확보하고 포화 지표를 모니터링한다",
    "한 번 정한 용량을 바꾸지 않는다 — 용량을 자주 바꾸면 비용 예측이 어려워지므로 연초에 정한 값을 연말까지 유지한다",
    "사용량의 100% 에 정확히 맞춰 프로비저닝한다 — 남는 자원은 낭비이므로 평균 사용량에 딱 맞추고 급증은 큐로 흡수한다",
    "비용 절감을 위해 최소 인스턴스만 둔다 — 오토스케일링이 있으면 급증 때 알아서 늘어나므로 평소에는 최소로 두는 것이 옳다" ] },
  { k: "cloud2-cicd-artifact-immutable", o: [
    "배포마다 다시 빌드한다 — 같은 커밋에서 같은 Dockerfile 로 빌드하면 결과도 같으므로 재빌드는 위험이 아니다",
    "한 번 빌드한 불변 아티팩트(이미지 다이제스트)를 전 단계에서 승격(promote)해 배포",
    "latest 태그를 붙여 최신 이미지를 받아 쓴다 — 테스트가 끝난 이미지가 곧 최신이므로 latest 를 당기면 같은 것이 나간다",
    "테스트를 건너뛴다 — 테스트 단계가 없으면 '테스트한 것과 다른 산출물' 이라는 불일치 자체가 생기지 않는다" ] },
] },
];
