module.exports = {
  track: "security", guide: "📜", xp: 70,
  unit: "로그 분석 II — 오설정이 만든 문",
  source: "./log_security.cjs",
  lessons: [
    { t: "확인하지 않은 신원과 권한", n: 5,
      th: { sum: "침해의 절반은 공격 기법이 아니라 **검사하지 않은 한 줄**에서 시작한다.",
        body: [
          { h: "인증과 인가는 다르다", t: "토큰을 <code>decode</code> 만 하면 서명을 보지 않아 누구나 클레임을 지어낼 수 있고, 인증만 하고 소유자를 비교하지 않으면 번호만 바꿔 남의 자원을 봅니다. '통과율 100%' 는 안심이 아니라 <b>아무도 걸러지지 않았다</b>는 신호일 수 있습니다." },
          { h: "세션과 이동", t: "로그인 뒤 세션 식별자를 새로 발급하지 않으면 공격자가 미리 심어 둔 번호가 인증됩니다(세션 고정). 돌아갈 주소를 검증 없이 받으면 우리 도메인이 피싱에 쓰입니다. 둘 다 <b>우리 신뢰를 빌려 주는</b> 형태입니다." }],
        code: { c: "verify(token, key, {algorithms:[\"RS256\"]})\nWHERE id = ? AND user_id = ?\n로그인 성공 → 세션 재발급", cap: "주장 말고 사실로 판단한다" },
        key: ["decode 는 검증이 아니다", "소유자 비교가 인가다", "로그인 뒤 세션 재발급", "리다이렉트는 같은 출처만"] } },
    { t: "서버가 대신 해 주는 일", n: 5,
      th: { sum: "서버는 방화벽 안에 있고 권한이 크다. **시키는 대로 하면** 그 권한이 그대로 공격에 쓰인다.",
        body: [
          { h: "대신 읽고 대신 부른다", t: "사용자가 준 URL 로 요청하면 내부 전용 주소에 닿고(SSRF), 사용자가 준 파일명을 이어 붙이면 지정 디렉터리 밖을 읽으며(경로 순회), XML 파서의 외부 실체는 서버 파일을 문서 안으로 끌어옵니다. 공통점은 <b>입력이 목적지를 정한다</b>는 것입니다." },
          { h: "새어 나가는 경로", t: "비밀은 코드가 아니라 <b>로그·빌드</b>로 새기도 합니다. 헤더를 통째로 찍으면 토큰이 90일치 로그에 남고, 락파일 없는 설치는 어제와 다른 코드를 빌드 권한으로 실행합니다. 비교조차 위험합니다 — 짧게 끝나는 문자열 비교는 시간으로 답을 흘립니다." }],
        code: { c: "허용 목록 + 해석된 IP 가 사설 대역이면 거절\n로깅에서 Authorization·쿠키 마스킹\nnpm ci --ignore-scripts", cap: "입력이 목적지를 정하게 두지 않는다" },
        key: ["입력이 목적지를 정하면 SSRF", "경로는 정규화 후 검사", "비밀은 로그로도 샌다", "비교는 상수 시간으로"] } },
  ],
};
