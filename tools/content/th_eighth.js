/* 8차 이론 — cpp 빌드, go 표준 라이브러리, react 이벤트.

   셋 다 세 레슨이 같은 글을 나눠 쓰고 있었고, 한 문장이 90자를 넘었다.
   cpp '빌드와 헤더 관리' 는 점수 24 로 그 트랙 최상위였다.
   방금 C++ 실습을 만들다 링크 오류로 7건이 막힌 것도 정확히 이 유닛의 내용이라,
   그때 배운 것을 그대로 적었다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

cpp: [
{
  unit: "빌드와 헤더 관리 (중급)", lesson: "컴파일과 링크",
  th: {
    sum: "C++ 빌드는 두 걸음이다. 파일마다 **따로 번역하고**, 그다음 **하나로 잇는다**.",
    body: [
      { h: "파일마다 따로 번역한다", t: "`.cpp` 파일 하나가 번역의 단위다. 각각 독립적으로 기계어 조각으로 바뀐다. 그래서 한 파일만 고치면 그 파일만 다시 번역하면 된다 — 큰 프로젝트에서 빌드가 빨라지는 이유다." },
      { h: "이름만 알려 주면 번역은 된다", t: "다른 파일에 있는 함수는 **선언**만 보이면 번역이 통과한다. 몸통이 어디 있는지는 나중 문제다. 헤더에 선언을 모아 두고 여러 파일이 가져다 쓰는 구조가 여기서 나온다." },
      { h: "몸통은 이을 때 찾는다", t: "마지막에 조각들을 이으면서 '이 이름의 몸통이 어디 있나' 를 찾는다. 없으면 `undefined reference` 다. 컴파일은 됐는데 마지막에 터지는 오류는 대개 이것이고, 몸통을 안 만들었거나 그 파일을 빌드에 안 넣은 것이다." },
      { h: "둘이면 그것도 오류다", t: "같은 이름의 몸통이 두 곳에 있으면 어느 것을 쓸지 정할 수 없어 거부한다(`multiple definition`). 헤더에 함수 몸통을 그냥 적어 두고 여러 파일이 가져가면 이렇게 된다." },
    ],
    code: { c: "// util.h — 선언만\nint add(int a, int b);\n\n// util.cpp — 몸통\nint add(int a, int b) { return a + b; }", cap: "선언은 헤더, 몸통은 한 곳" },
    key: ["파일마다 따로 번역", "선언만 있어도 번역은 된다", "몸통이 없거나 둘이면 링크가 거부"],
  },
},
{
  unit: "빌드와 헤더 관리 (중급)", lesson: "ODR과 inline",
  th: {
    sum: "**하나의 정의 규칙(ODR)**: 몸통은 프로그램 전체에서 딱 하나여야 한다.",
    body: [
      { h: "헤더는 복사되어 들어간다", t: "`#include` 는 그 파일의 내용을 그대로 붙여 넣는 것이다. 헤더에 함수 몸통을 적어 두면, 그 헤더를 가져간 파일마다 몸통이 하나씩 생긴다. 링크할 때 여러 개가 되어 거부당한다." },
      { h: "inline 은 '여럿이어도 같은 것' 이라는 표시", t: "`inline` 을 붙이면 '여러 파일에 같은 몸통이 있어도 하나로 봐 달라' 는 뜻이 된다. 그래서 헤더에 몸통을 두려면 `inline` 이 필요하다. 이름과 달리 '끼워 넣어 달라' 는 요청은 요즘 거의 뜻이 없다." },
      { h: "클래스 안에 쓴 멤버는 자동으로 그렇다", t: "클래스 정의 안에 몸통까지 적은 멤버 함수는 자동으로 그 성질을 갖는다. 그래서 헤더에 클래스를 통째로 두어도 문제가 없다. 반대로 클래스 밖에서 정의하면 몸통이 하나여야 한다." },
      { h: "변수도 마찬가지다", t: "헤더에 일반 변수를 정의하면 파일마다 하나씩 생겨 링크가 거부한다. C++17 부터는 변수에도 `inline` 을 붙일 수 있어, 헤더 하나로 끝낼 수 있다. 정적 멤버를 클래스 안에서 바로 초기화할 때도 이것을 쓴다." },
    ],
    code: { c: "// 헤더에 몸통을 두려면\ninline int twice(int x) { return x * 2; }\n\nstruct T { static inline int count = 0; };", cap: "헤더의 몸통에는 inline" },
    key: ["`#include` 는 복사다", "헤더의 몸통에는 `inline`", "클래스 안 멤버는 자동"],
  },
},
{
  unit: "빌드와 헤더 관리 (중급)", lesson: "전방 선언과 컴파일 시간",
  th: {
    sum: "헤더를 많이 가져올수록 번역할 양이 늘어난다. **꼭 필요할 때만** 가져온다.",
    body: [
      { h: "포함은 번지수처럼 퍼진다", t: "A 가 B 를 가져오고 B 가 C 를 가져오면, A 는 C 까지 다 번역하게 된다. 헤더 하나를 고쳤을 때 프로젝트 절반이 다시 빌드되는 이유가 이것이다." },
      { h: "이름만 알려 주면 되는 경우", t: "포인터나 참조로만 쓰는 타입은 **이름만 알려 주면** 된다. `class Foo;` 한 줄이면 충분하고 헤더를 가져오지 않아도 된다. 크기를 알아야 하는 경우, 즉 값으로 담거나 멤버를 부를 때만 진짜 헤더가 필요하다." },
      { h: "구현 파일에서 가져온다", t: "헤더에는 전방 선언만 두고, 실제 `#include` 는 `.cpp` 안에서 한다. 그러면 내 헤더를 쓰는 다른 파일들이 남의 헤더까지 끌고 오지 않는다." },
      { h: "포함 가드는 반드시", t: "같은 헤더가 두 번 들어오면 정의가 두 번 생겨 오류가 난다. `#pragma once` 나 매크로 가드를 두면 한 번만 들어온다. 헤더를 만들면 습관적으로 먼저 적는 줄이다." },
    ],
    code: { c: "// 헤더\n#pragma once\nclass Foo;              // 이름만\nvoid use(const Foo& f); // 참조라 이걸로 충분하다", cap: "이름만 알려 주고 넘어간다" },
    key: ["포함은 퍼져 나간다", "포인터·참조면 전방 선언", "헤더에는 `#pragma once`"],
  },
},
],

go: [
{
  unit: "표준 라이브러리 실무 중급: json·time·strings·io", lesson: "encoding/json 실무",
  th: {
    sum: "Go 는 구조체와 JSON 을 서로 바꿔 준다. 규칙은 **대문자로 시작한 필드만** 나간다는 것이다.",
    body: [
      { h: "소문자 필드는 나가지 않는다", t: "Go 에서 소문자로 시작하는 필드는 패키지 밖에서 볼 수 없다. JSON 으로 바꾸는 코드도 밖에 있으므로 그 필드를 못 본다. 그래서 값을 넣어 뒀는데 결과 JSON 이 비어 있는 일이 생긴다." },
      { h: "이름은 태그로 정한다", t: "구조체 필드 이름이 그대로 JSON 키가 된다. 다른 이름을 쓰려면 `json:\"user_name\"` 태그를 붙인다. 태그를 안 붙이면 `UserName` 처럼 대문자로 나가서 받는 쪽 규칙과 어긋난다." },
      { h: "빈 값을 빼려면 omitempty", t: "값이 0이나 빈 문자열이어도 기본적으로 키가 나간다. `json:\"age,omitempty\"` 를 붙이면 빈 값일 때 아예 빠진다. 다만 '0을 보낸 것' 과 '안 보낸 것' 을 구분해야 한다면 이걸 쓰면 안 된다." },
      { h: "읽을 때는 포인터로 넘긴다", t: "`json.Unmarshal(data, &v)` 처럼 주소를 넘겨야 값이 채워진다. 값으로 넘기면 복사본만 채워지고 원래 변수는 그대로다 — 오류도 안 나서 한참 헤맨다." },
    ],
    code: { c: "type User struct {\n\tName string `json:\"name\"`\n\tAge  int    `json:\"age,omitempty\"`\n}\n\njson.Unmarshal(data, &u)   // 주소를 넘긴다", cap: "대문자 필드 + 태그" },
    key: ["소문자 필드는 안 나간다", "이름은 태그로", "읽을 때는 주소를 넘긴다"],
  },
},
{
  unit: "표준 라이브러리 실무 중급: json·time·strings·io", lesson: "time 다루기",
  th: {
    sum: "Go 의 시간 형식은 숫자가 아니라 **정해진 예시 시각**으로 적는다. 이것부터 낯설다.",
    body: [
      { h: "형식은 예시로 쓴다", t: "`2006-01-02 15:04:05` 라는 특정 시각을 본보기로 삼아 원하는 모양을 적는다. `YYYY` 같은 기호를 쓰지 않는다. 숫자를 하나라도 바꿔 쓰면 형식으로 인식되지 않아 엉뚱한 결과가 나온다." },
      { h: "기간은 숫자가 아니다", t: "`time.Duration` 은 나노초 단위의 값이다. `3` 이 아니라 `3 * time.Second` 처럼 단위를 곱해 적어야 뜻이 분명하다. 그냥 숫자를 넘기면 나노초로 읽혀서 사실상 즉시가 된다." },
      { h: "시각 비교는 전용 메서드로", t: "`t1.Before(t2)`·`After`·`Equal` 을 쓴다. `==` 는 시간대나 내부 정보까지 같아야 참이라, 같은 순간인데도 거짓이 될 수 있다. 같은 순간인지 물을 때는 `Equal` 이다." },
      { h: "시간대를 정해 둔다", t: "`time.Now()` 는 그 기계의 시간대를 따른다. 서버와 개발 기계의 시간대가 다르면 결과가 달라진다. 저장하고 주고받을 때는 UTC 로 통일하고, 보여 줄 때만 지역 시간으로 바꾼다." },
    ],
    code: { c: "t.Format(\"2006-01-02\")          // 예시 시각이 곧 형식\ntime.Sleep(3 * time.Second)     // 단위를 곱한다\nt1.Equal(t2)                    // == 말고", cap: "예시로 적고, 단위를 곱한다" },
    key: ["형식은 정해진 예시 시각", "기간은 단위를 곱한다", "비교는 `Equal`·`Before`"],
  },
},
{
  unit: "표준 라이브러리 실무 중급: json·time·strings·io", lesson: "strings와 io 인터페이스",
  th: {
    sum: "Go 에서 읽고 쓰는 일은 **`io.Reader` 와 `io.Writer` 두 약속**으로 통한다.",
    body: [
      { h: "약속 하나로 다 통한다", t: "파일이든 네트워크든 메모리 위의 글자든 전부 같은 약속을 지킨다. 그래서 함수는 `io.Reader` 만 받으면 되고, 부르는 쪽이 무엇을 넘기든 상관없다. 시험할 때는 `strings.NewReader` 로 가짜 입력을 만들면 된다." },
      { h: "구체 타입을 받으면 시험할 수 없다", t: "매개변수를 `*os.File` 로 받으면 진짜 파일을 만들어야만 시험할 수 있다. `io.Reader` 로 받으면 글자 몇 줄로 대신할 수 있다 — 시험하기 쉬운 함수는 대개 이 모양이다." },
      { h: "문자열은 이어 붙이지 않는다", t: "반복문에서 `s += x` 를 하면 매번 새 문자열이 만들어진다. 길어질수록 느려진다. `strings.Builder` 에 담았다가 마지막에 한 번 꺼내면 이 낭비가 없다." },
      { h: "읽기는 끝을 확인해야 한다", t: "`Read` 는 읽은 만큼과 함께 끝났는지를 알려 준다. 이 신호를 확인하지 않으면 빈 값을 계속 읽으며 돌게 된다. 대개는 `io.ReadAll` 이나 `bufio.Scanner` 를 써서 이 처리를 맡긴다." },
    ],
    code: { c: "func count(r io.Reader) (int, error) { ... }\n\ncount(strings.NewReader(\"가나다\"))   // 시험은 이렇게\n\nvar b strings.Builder\nb.WriteString(x)", cap: "약속으로 받으면 갈아 끼울 수 있다" },
    key: ["읽기·쓰기는 두 약속으로 통한다", "약속으로 받으면 시험이 쉽다", "이어 붙이기는 `Builder`"],
  },
},
],

react: [
{
  unit: "React 중급 — 이벤트 처리 실무", lesson: "합성 이벤트 이해하기",
  th: {
    sum: "React 는 요소마다 처리기를 붙이지 않는다. **한 곳에서 받아 나눠 준다.**",
    body: [
      { h: "왜 한 곳에서 받나", t: "목록 항목 1000개에 처리기를 각각 붙이면 그만큼 등록 비용이 든다. React 는 뿌리에서 한 번만 듣고, 어디서 일어난 일인지 보고 알맞은 처리기를 부른다. 그래서 항목이 많아도 등록 비용이 늘지 않는다." },
      { h: "우리가 받는 것은 감싼 객체", t: "처리기가 받는 `e` 는 브라우저의 원래 이벤트를 감싼 것이다. 브라우저마다 다른 부분을 맞춰 줘서, 어디서든 같은 방식으로 쓸 수 있다. 원래 것이 필요하면 `e.nativeEvent` 로 꺼낸다." },
      { h: "섞어 쓰면 순서가 헷갈린다", t: "`addEventListener` 로 직접 붙인 처리기와 React 처리기를 같이 쓰면, 어느 것이 먼저 도는지 헷갈린다. 한 화면에서는 한 방식으로 통일하는 편이 낫다 — 안 그러면 '분명 막았는데 왜 돌지' 가 생긴다." },
      { h: "비동기 안에서 e 를 쓸 때", t: "`setTimeout` 안에서 `e.target` 을 읽는 코드는 조심해야 한다. 필요한 값은 먼저 변수에 꺼내 두는 편이 안전하다 — 그러면 나중에 읽어도 값이 확실하다." },
    ],
    code: { c: "function onClick(e) {\n  const id = e.currentTarget.dataset.id;   // 먼저 꺼내 둔다\n  setTimeout(() => remove(id), 300);\n}", cap: "한 곳에서 받아 나눠 준다" },
    key: ["뿌리에서 한 번만 듣는다", "`e` 는 감싼 객체", "필요한 값은 먼저 꺼내 둔다"],
  },
},
{
  unit: "React 중급 — 이벤트 처리 실무", lesson: "전파와 기본 동작",
  th: {
    sum: "이벤트는 누른 곳에서 **위로 타고 올라간다.** 그리고 어떤 요소에는 **기본 동작**이 있다.",
    body: [
      { h: "안쪽을 눌러도 바깥이 돈다", t: "항목 안의 '삭제' 를 누르면 삭제 처리기가 돌고, 이어서 항목 전체의 '열기' 처리기까지 돈다. 지우려고 눌렀는데 상세 화면이 열리는 사고가 이것이다. `e.stopPropagation()` 으로 거기서 멈춘다." },
      { h: "폼과 링크에는 기본 동작이 있다", t: "폼을 제출하면 페이지가 새로 열리고, 링크를 누르면 주소가 바뀐다. 화면 안에서 처리하고 싶다면 `e.preventDefault()` 로 막아야 한다. 안 막으면 입력하던 값이 통째로 사라진다." },
      { h: "둘은 다른 이야기다", t: "`stopPropagation` 은 위로 안 올라가게 하는 것이고, `preventDefault` 는 브라우저의 기본 동작을 막는 것이다. 둘을 헷갈려 반대로 쓰면 아무것도 해결되지 않는다." },
      { h: "위임을 쓸 때는 표시를 남긴다", t: "바깥 하나에서 여러 항목의 클릭을 받으려면, 어느 항목이었는지 알아야 한다. `data-id` 같은 속성에 적어 두고 `e.target` 에서 읽으면 처리기를 하나만 두고도 구분할 수 있다." },
    ],
    code: { c: "<button onClick={(e) => { e.stopPropagation(); del(); }}>삭제</button>\n<form onSubmit={(e) => { e.preventDefault(); save(); }}>", cap: "멈추는 것과 막는 것은 다르다" },
    key: ["이벤트는 위로 올라간다", "폼·링크는 기본 동작을 막는다", "둘은 서로 다른 일"],
  },
},
{
  unit: "React 중급 — 이벤트 처리 실무", lesson: "디바운스·스로틀",
  th: {
    sum: "너무 자주 일어나는 이벤트는 **줄여서 처리한다.** 방법은 둘이고 쓰임이 다르다.",
    body: [
      { h: "디바운스는 '멈추면 한 번'", t: "타이핑이 멈추고 300밀리초가 지나면 그때 한 번 실행한다. 검색창처럼 '다 치고 나서 한 번' 이면 이쪽이다. 안 쓰면 글자마다 요청이 나가서 서버도 화면도 시달린다." },
      { h: "스로틀은 '정해진 간격으로'", t: "아무리 자주 일어나도 200밀리초에 한 번만 실행한다. 스크롤 위치 표시처럼 '계속 반응은 하되 너무 자주는 말고' 일 때 쓴다. 디바운스로 하면 스크롤이 멈출 때까지 아무 반응이 없다." },
      { h: "마지막 값이 쓰이게 한다", t: "요청을 줄여도, 늦게 온 옛 응답이 최신 결과를 덮어쓸 수 있다. 요청마다 번호를 붙이고 '내가 마지막인가' 를 확인하면 막을 수 있다." },
      { h: "치운 뒤에 남지 않게", t: "걸어 둔 타이머는 화면을 떠날 때 없앤다. 안 없애면 이미 사라진 화면을 고치려다 오류가 나거나, 쓸데없는 요청이 한 번 더 나간다." },
    ],
    code: { c: "React.useEffect(() => {\n  const id = setTimeout(() => search(q), 300);\n  return () => clearTimeout(id);   // 다음 입력에 취소된다\n}, [q]);", cap: "멈추면 한 번 / 간격마다 한 번" },
    key: ["디바운스는 멈추면 한 번", "스로틀은 간격마다 한 번", "타이머는 반드시 치운다"],
  },
},
],

};
