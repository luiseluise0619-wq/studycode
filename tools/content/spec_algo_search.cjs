module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 완전탐색과 백트래킹",
  source: "./algo_search.cjs",
  lessons: [
    { t: "완전탐색 — 모든 경우를 빠짐없이", n: 6,
      th: { sum: "완전탐색은 **가장 느리지만 반드시 맞는** 풀이다. 빠른 풀이를 짜기 전에 이것부터 짜 두면, 무엇을 세는 문제인지가 코드로 확정된다.",
        body: [
          { h: "갈래를 어떻게 나눌지부터 정한다", t: "부분집합은 원소마다 **고른다/안 고른다** 두 갈래(2ⁿ), 순열은 **맨 앞에 무엇이 올까** n 갈래(n!), 조합은 **다음 시작점을 되돌리지 않는** n개 갈래(nCk)다. 갈래를 정하면 재귀는 저절로 써진다. 반대로 갈래를 흐릿하게 두면 같은 답을 여러 번 세거나 빠뜨린다. 순서가 의미 있으면 순열, 없으면 조합 — 이 한 줄이 시작점을 올릴지 말지를 정한다." },
          { h: "n 이 얼마까지 되는지 먼저 센다", t: "2ⁿ 은 n=20 에서 백만, n=30 이면 10억이다. n! 은 n=10 에서 360만, n=13 이면 60억이다. O(n³) 는 n=500 에서 1억이다. **문제의 입력 한계를 먼저 읽고** 완전탐색이 통과할지 계산한 뒤 짠다. 통과하지 못하면 가지치기·메모이제이션·그리디로 갈아탈 준비를 한다 — 완전탐색은 그 모든 것의 출발점이다." }],
        code: { c: "const go = (i, sum) => {\n  if (sum === target) return true;\n  if (i === xs.length || sum > target) return false;\n  return go(i + 1, sum + xs[i]) || go(i + 1, sum);\n};", cap: "고른다 / 안 고른다 — 두 갈래가 전부다" },
        key: ["갈래를 먼저 정하고 재귀는 그대로 옮긴다", "2ⁿ·n! 을 입력 한계에 대 본다", "완전탐색은 빠른 풀이의 정답 기준선", "가지치기는 '답이 될 수 없는 갈래' 만 접는다"] } },
    { t: "백트래킹 — 가 보고, 아니면 되돌린다", n: 6,
      th: { sum: "백트래킹은 완전탐색에 **되돌리기**를 붙인 것이다. 상태를 고치고 내려갔다가, 올라오면서 **정확히 그만큼 되돌려** 형제 갈래를 깨끗하게 만든다.",
        body: [
          { h: "고친 것은 반드시 짝지어 되돌린다", t: "`push` 뒤에는 `pop`, `used[i] = true` 뒤에는 `used[i] = false`, 격자를 덮어썼으면 원래 값을 복원한다. 이 짝이 하나라도 어긋나면 다음 갈래가 이전 갈래의 흔적을 물려받아 답이 늘어나거나 사라진다. 결과를 담을 때 `cur.slice()` 로 **복사**하는 것도 같은 이유다 — 되돌리는 순간 이미 담아 둔 것까지 비워진다." },
          { h: "중복은 '표시' 가 아니라 '규칙' 으로 막는다", t: "같은 답이 여러 경로로 나오는 것을 막을 때, 만들어 놓고 `Set` 으로 지우면 시간은 그대로 든다. 대신 **경로 자체를 하나만 남기는 규칙**을 둔다 — 조합은 시작점을 되돌리지 않고, 중복 값 순열은 정렬한 뒤 '앞의 같은 값이 아직 안 쓰였으면 지금 것도 쓰지 않는다'. 규칙은 만들기 전에 걸러 내므로 탐색량 자체가 준다." }],
        code: { c: "cur.push(v);\ngo(i + 1);\ncur.pop();      // 내려간 만큼 되돌린다", cap: "고치기·내려가기·되돌리기 세 줄이 한 묶음" },
        key: ["고친 것은 짝지어 되돌린다", "담을 때는 복사한다", "중복은 경로 규칙으로 막는다", "안 되는 갈래는 들어가기 전에 접는다"] } },
  ],
};
