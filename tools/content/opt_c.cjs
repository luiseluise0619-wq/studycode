/* c 트랙 — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다 (1차, 19문항).
   node fix_opts.cjs --apply opt_c.cjs */
module.exports = { track: "c", fixes: [
  { k: "c3-enum-6", o: [
    "enum 은 정수라 switch 에 쓸 수는 있지만 case 에 열거 이름을 직접 적을 수 없어 값으로 바꿔 적어야 하고, default 도 그 값들 사이에 둘 수 없다",
    "모든 열거 값을 나열하고 default 를 생략하면 case 누락 경고(-Wswitch)를 받을 수 있어, 미지 값은 switch 뒤에서 처리하기도 한다",
    "default 는 문법상 반드시 있어야 하며, 없으면 컴파일러가 나열되지 않은 값을 만났을 때 어디로 갈지 정할 수 없어 컴파일을 거부한다",
    "default 를 넣으면 컴파일러가 점프 테이블을 만들지 못해 모든 case 를 차례로 비교하게 되므로, 열거 값이 많을수록 실행이 눈에 띄게 느려진다" ] },
  { k: "c3-port-5", o: [
    "int16_t 는 정확히 16비트라 그런 타입이 없는 구현에는 없고, int_least16_t 는 16비트 이상인 가장 작은 타입이라 항상 있다",
    "둘은 같은 타입의 별칭이며 이름만 다르다. stdint.h 가 typedef 로 서로를 가리키므로 어느 쪽을 써도 크기와 결과가 같다",
    "int_least16_t 가 언제나 더 큰 타입이다. '적어도' 라는 이름대로 여유를 두고 잡기 때문에 int16_t 보다 폭이 넓고 느리다",
    "int16_t 는 부호가 없는 타입이고 int_least16_t 는 부호가 있는 타입이라, 음수를 담으려면 뒤쪽을 써야 한다" ] },
  { k: "c2-signal-vs-sigaction", o: [
    "sigaction 은 동작 세부(재설정, 블록할 시그널, 재시작 여부)를 명확히 지정할 수 있어 플랫폼마다 달랐던 signal 의 모호함이 없기 때문",
    "sigaction 은 SIGKILL 과 SIGSTOP 까지 잡을 수 있어 강제 종료 앞에서도 정리 코드를 돌릴 기회를 주기 때문",
    "signal 은 C 표준에 없는 유닉스 전용 확장이라 표준 C 로만 짜야 하는 코드에서는 아예 쓸 수 없고, sigaction 만 표준이기 때문",
    "signal 은 프로세스 전체에 핸들러를 하나만 등록할 수 있어 시그널 종류마다 다른 함수를 붙이려면 sigaction 이 필요하기 때문" ] },
  { k: "c2-fd-vs-file", o: [
    "FILE* 는 디스크립터 위에 버퍼링과 서식 입출력을 얹은 상위 계층이며, fileno 로 내부 디스크립터를 얻을 수 있다",
    "파일 디스크립터로는 파일을 열고 닫을 수만 있고, 실제로 내용을 읽고 쓰려면 반드시 FILE* 로 감싸야 한다",
    "FILE* 가 더 하위 계층이라 커널과 직접 통신하고, 디스크립터는 그 위에서 번호만 붙여 관리하는 편의 장치다",
    "둘은 같은 것을 가리키는 두 표기이며, int 로 쓰면 디스크립터이고 포인터로 쓰면 FILE* 가 될 뿐 계층 차이는 없다" ] },
  { k: "c2-errno-not-reset", o: [
    "errno 는 매크로로 정의된 상수라 값을 대입할 수 없고, 읽기만 가능하므로 초기화하려면 별도 함수를 불러야 한다",
    "errno 는 read/write 같은 시스템 호출에서만 설정되고, strtol 이나 fopen 같은 라이브러리 함수는 건드리지 않는다",
    "표준 함수는 성공했을 때 errno 를 0 으로 초기화하지 않으므로, 반환값으로 실패를 먼저 확인한 뒤에 errno 를 읽어야 한다",
    "함수가 성공하면 표준 라이브러리가 errno 를 0 으로 되돌리므로, 호출 뒤 errno 가 0 이 아니면 그 호출이 실패한 것이다" ] },
  { k: "c2-goto-cleanup", o: [
    "여러 자원을 순서대로 잡는 함수에서, 어느 지점에서 실패하든 해제 코드를 한곳에 모아 중복과 누락을 없애기 위해",
    "예외가 없는 C 에서 오류를 상위 함수로 던지기 위해 — cleanup 라벨로 점프하면 호출자의 정리 코드가 이어서 실행된다",
    "함수를 여러 개로 나누면 생기는 호출 오버헤드를 없애기 위해 — 라벨로 묶으면 한 함수 안에서 모든 일을 끝낼 수 있다",
    "중첩된 반복문을 한 번에 빠져나오기 위해 — break 는 가장 안쪽 루프만 끝내므로 바깥까지 나가려면 goto 가 필요하다" ] },
  { k: "c2-gets-removed", o: [
    "표준 입력만 읽을 수 있고 파일이나 소켓에서는 쓸 수 없어, 입력 원천이 다양해진 뒤로는 쓸모가 없어졌기 때문",
    "한 글자씩 읽어 들이는 구현이라 큰 입력에서 너무 느리고, 줄 단위로 버퍼링하는 fgets 가 이를 대체했기 때문",
    "읽은 줄 끝의 개행 문자를 버퍼에 그대로 남겨 두어, 다음 입력 함수가 빈 줄을 읽는 문제가 끊이지 않았기 때문",
    "읽을 최대 길이를 지정할 수 없어 입력이 길면 버퍼를 넘겨 쓰는 것을 막을 방법이 없기 때문" ] },
  { k: "디스패치 테이블", o: [
    "여러 함수가 코드를 공유하게 만들어 실행 파일 크기와 메모리 사용량을 줄이는 것",
    "재귀 호출을 반복문으로 바꿔 스택 깊이를 제한하는 것 — 호출할 함수를 배열에서 꺼내면 재귀가 필요 없어진다",
    "함수 주소를 미리 배열에 담아 두어 호출할 때마다 심볼을 찾는 비용을 없애고 실행 속도를 높이는 것",
    "명령 코드나 상태를 인덱스로 삼아 긴 `switch` 대신 해당 처리 함수를 바로 호출하는 디스패치 테이블" ] },
  { k: "c3-bss-6", o: [
    "BSS 규칙은 정적 저장 기간(전역·static)에만 적용되고, 자동 저장 기간(지역)은 초기화하지 않으면 값이 불확정이라서",
    "컴파일러 옵션에 따라 지역 변수도 0 이 되기 때문에, 옵션이 다른 환경으로 옮기면 값이 달라져 이식성이 깨지기 때문",
    "지역 변수는 프로그램이 시작되고 한참 뒤에 만들어져 BSS 를 지우는 시점을 놓치기 때문에 0 이 보장되지 않는다",
    "지역 변수는 메모리가 아니라 레지스터에만 존재해 0 으로 지울 대상 자체가 없기 때문에 BSS 규칙이 닿지 않는다" ] },
  { k: "c2-handler-safe", o: [
    "핸들러는 프로그램의 임의 지점에 끼어들 수 있어, 그 함수들이 내부 상태를 갱신하던 중이었다면 재진입으로 상태가 깨질 수 있기 때문",
    "핸들러 안에서는 어떤 함수도 부를 수 없도록 커널이 막아 두었기 때문에, 호출하는 순간 프로세스가 강제로 종료된다",
    "printf 와 malloc 은 커널 모드로 들어가는 함수라 시그널 처리 중에는 실행이 느려져 다른 시그널을 놓칠 수 있기 때문",
    "핸들러는 반환값을 가질 수 없어 그 함수들이 돌려주는 값을 받을 곳이 없고, 버려진 반환값이 메모리 누수로 이어지기 때문" ] },
  { k: "c3-va-6", o: [
    "va_start 가 잡아 둔 메모리가 해제되지 않아 가변 인자 함수를 부를 때마다 조금씩 누수가 쌓인다",
    "인자 목록을 훑던 포인터가 정리되지 않은 채 남아 함수가 반환되는 순간 스택이 깨져 크래시가 난다",
    "표준이 va_end 호출을 요구하므로 일부 구현에서 정리가 누락되어 이식성 없는 코드가 된다",
    "va_start 와 짝이 맞지 않으면 컴파일러가 매크로 확장 단계에서 오류를 내어 컴파일 자체가 되지 않는다" ] },
  { k: "c3-life-3", o: [
    "결과를 담을 메모리를 함수 안에서 새로 잡지 않으니 malloc 을 부르지 않아 항상 더 빠르다",
    "버퍼 크기를 호출자가 정하므로 함수 쪽에서는 잘림을 신경 쓸 필요가 없고 n 은 참고용 값에 그친다",
    "할당과 해제의 책임이 호출자에게 있어 수명이 명확해지고, 지역 버퍼 주소를 반환하는 실수를 피할 수 있다",
    "결과가 buf 에 담기므로 반환값이 필요 없어져 함수를 void 로 선언할 수 있고 오류 검사도 생략할 수 있다" ] },
  { k: "c2-2d-not-ptrptr", o: [
    "2차원 배열은 함수 밖으로 넘길 수 없어서, 함수 안에서만 쓸 수 있는 a 를 바깥 포인터에 담는 것이 막혀 있기 때문",
    "a 는 int (*)[4] 로 감쇠하는데 int** 는 'int 포인터를 담은 메모리'를 가리키는 다른 타입이라 호환되지 않기 때문",
    "int** 는 3차원 이상의 배열에만 쓸 수 있는 타입이라, 2차원 배열을 담으려면 int* 로 한 단계 낮춰야 하기 때문",
    "배열 이름 a 는 상수라서 어떤 포인터 변수에도 대입할 수 없고, 주소를 넘기려면 &a[0][0] 처럼 원소 주소를 써야 하기 때문" ] },
  { k: "c2-memcmp-padding", o: [
    "정렬을 위해 삽입된 패딩 바이트의 내용이 불확정일 수 있어, 논리적으로 같은 값인데도 다르다고 나올 수 있기 때문",
    "memcmp 는 문자열 전용이라 구조체 안에서 0 바이트를 만나면 거기서 비교를 멈추고 뒤쪽 멤버를 보지 않기 때문",
    "memcmp 는 첫 바이트가 다르면 바로 멈추므로 첫 멤버만 같으면 나머지 멤버가 달라도 같다고 판정하기 때문",
    "구조체는 포인터로만 비교할 수 있게 정해져 있어, 내용을 비교하려면 멤버를 하나씩 꺼내 == 로 견줘야 하기 때문" ] },
  { k: "c2-2d-omit-dim", o: [
    "초기화 값의 개수로 행 수를 컴파일러가 계산할 수 있고, 원소의 주소를 구하는 데는 두 번째 이후 차원만 필요하기 때문",
    "첫 번째 차원은 실행 중에 필요한 만큼 자동으로 늘어나므로 미리 정할 필요가 없고, 나머지 차원만 고정하면 되기 때문",
    "C 는 모든 차원의 크기를 생략할 수 있게 허용하지만, 관례상 첫 차원만 비워 두고 나머지는 적는 것이 읽기 쉽기 때문",
    "생략한 차원은 자동으로 1 이 되고 초기화 값이 넘치면 컴파일러가 조용히 늘려 주므로 어느 차원이든 비워 둘 수 있기 때문" ] },
  { k: "c2-scanf-leftover", o: [
    "%d 가 숫자만 소비하고 그 뒤의 개행 문자를 입력 스트림에 남겨 두어, fgets 가 그 개행을 곧바로 읽어 버리기 때문",
    "fgets 는 호출될 때마다 첫 줄을 건너뛰고 둘째 줄부터 읽도록 되어 있어, 첫 호출에서는 항상 빈 줄이 돌아오기 때문",
    "scanf 가 읽기를 마치며 입력 버퍼를 통째로 비워 버려, 뒤이어 부른 fgets 가 읽을 것이 없어 빈 줄을 돌려주기 때문",
    "n 이 초기화되지 않은 채 scanf 에 넘어가면 변환이 실패하고, 실패한 scanf 가 스트림을 빈 줄 상태로 되돌리기 때문" ] },
  { k: "괄호 차이", o: [
    "완전히 같은 선언이며 괄호는 읽기 좋으라고 붙인 것일 뿐이라, 어느 쪽으로 적든 같은 함수 f 를 선언한다",
    "앞은 int 를 가리키는 포인터 변수 f 의 선언이고, 뒤는 int 를 받아 int 를 돌려주는 함수 f 의 선언이다",
    "앞은 `int *`를 반환하는 함수 선언, 뒤는 함수를 가리키는 포인터 변수 선언이다",
    "뒤쪽은 문법 오류다 — 식별자를 괄호로 감싼 선언은 C 문법에 없으며 typedef 를 거쳐야만 함수 포인터를 만들 수 있다" ] },
  { k: "c3-pf-1", o: [
    "가변 인자에는 타입 정보가 함께 전달되지 않아 형식 문자열의 지정자만 믿고 va_arg를 호출하기 때문",
    "printf 가 표준 라이브러리 함수라서 컴파일러가 본문을 볼 수 없고, 본문을 모르면 인자 타입도 검사할 수 없기 때문",
    "printf 가 어셈블리로 작성되어 있어 C 의 타입 체계 바깥에서 돌아가고, 레지스터에 든 값에는 타입이 없기 때문",
    "형식 문자열이 런타임에 만들어질 수 있어 컴파일 시점에는 어떤 지정자가 올지 알 수 없고, 그래서 검사를 포기했기 때문" ] },
  { k: "c2-qsort-cmp-ret", o: [
    "정확히 -1, 0, 1 중 하나여야 하며, 다른 값을 돌려주면 qsort 가 정렬을 멈추고 배열을 원래 순서로 되돌린다",
    "첫 인자가 더 작으면 1, 크면 0 을 돌려주고, 같은 경우는 어느 쪽이든 상관없어 관례상 0 으로 둔다",
    "첫 인자가 더 작으면 음수, 같으면 0, 크면 양수 (값의 크기는 상관없다)",
    "두 원소가 정렬된 뒤 차지할 인덱스의 차이를 돌려주어야 하며, qsort 는 그 값으로 원소를 옮길 자리를 계산한다" ] },
  /* 2차 — 20~44 */
  { k: "진단 도구", o: [
    "들여쓰기·이름 규칙 같은 코드 스타일을 검사해 팀 규약에서 벗어난 줄을 알려 준다",
    "실행 중 메모리 누수, 해제 후 사용, 범위 밖 접근 같은 메모리 오류를 탐지",
    "소스 코드를 읽어 정적으로 분석하고, 실행하지 않고도 초기화되지 않은 변수를 찾아 준다",
    "오브젝트 파일을 캐시해 다시 컴파일할 파일만 골라내어 빌드 시간을 줄여 준다" ] },
  { k: "선언 읽기", q: "int *arr[5];", o: [
    "둘 다 같은 선언이며, 괄호는 사람이 읽기 좋으라고 붙이는 것일 뿐 타입에는 영향이 없다",
    "앞은 int 5개짜리 배열을 가리키는 포인터이고, 뒤는 int 포인터 5개를 담은 배열이다",
    "앞은 int 포인터 5개짜리 배열, 뒤는 int 5개짜리 배열을 가리키는 포인터다",
    "뒤쪽 선언은 문법 오류다 — 식별자를 괄호로 감싸는 것은 함수 포인터에만 허용되고 배열에는 쓸 수 없다" ] },
  { k: "gets 제거", o: [
    "한 글자씩 읽는 구현이라 큰 입력에서 너무 느렸고, 줄 단위로 버퍼링하는 fgets 로 대체됐기 때문",
    "바이트 단위로만 읽어 멀티바이트 문자가 중간에서 잘릴 수 있어 유니코드 입력을 안전하게 다루지 못하기 때문",
    "실패했을 때 알려 줄 반환값이 없어 입력 끝(EOF)과 오류를 구분할 수 없고, 호출자가 실패를 처리할 방법이 없기 때문",
    "버퍼 크기를 전달할 방법이 없어 오버플로를 원천적으로 막을 수 없기 때문" ] },
  { k: "c2-vla", o: [
    "열 크기를 실행 시점 값으로 받을 수 있어 임의 크기 2차원 배열을 자연스럽게 다룰 수 있다",
    "크기를 실행 시점에 알게 되므로 컴파일러가 배열을 항상 힙에 할당하고, 함수가 끝날 때 자동으로 해제한다",
    "rows 와 cols 를 a 뒤에 적어도 되며, 컴파일러가 매개변수 목록 전체를 본 뒤에 크기를 맞추기 때문에 순서는 상관없다",
    "C89 부터 표준에 있던 문법이라 모든 컴파일러가 필수로 지원하며, 이식성을 걱정할 필요가 없다" ] },
  { k: "c2-bst-insert-code", o: [
    "같은 값이 이미 있으면 반환된 기존 노드로 덮어써서 중복 노드가 트리에 두 번 들어가지 않게 하기 위해",
    "반환된 서브트리를 다시 붙이면서 높이를 다시 재어 한쪽으로 기운 트리의 균형을 맞추기 위해",
    "빈 자리(NULL)에 새로 만든 노드를 부모의 링크에 연결하기 위해",
    "반환값을 받으면 호출이 끝난 프레임이 바로 정리되어 재귀 깊이가 절반으로 줄어들기 때문" ] },
  { k: "c3-res-1", o: [
    "이 포인터가 가리키는 객체는 이 포인터를 통해서만 접근된다(다른 포인터와 겹치지 않는다)",
    "이 포인터는 malloc 으로 받은 힙 메모리를 가리키므로 함수가 끝나기 전에 free 로 돌려주어야 한다",
    "이 포인터는 NULL 이 될 수 없으므로 함수 안에서 NULL 검사를 생략해도 되고 컴파일러가 그 검사를 제거한다",
    "이 포인터가 가리키는 값은 함수 안에서 바뀌지 않으므로 const 처럼 읽기 전용으로 다뤄야 한다" ] },
  { k: "c3-pr-2", o: [
    "char 는 va_arg 로 읽을 수 없도록 표준이 따로 정해 둔 예약 타입이라, 문자는 항상 문자열 포인터로 받아야 하기 때문",
    "기본 인자 승격으로 char는 int로 전달되므로 va_arg(ap, int)로 읽어야 하기 때문",
    "char 는 1바이트라 스택에서 4바이트 경계에 맞지 않고, 정렬이 어긋난 주소를 읽으면 버스 오류가 나기 때문",
    "char 의 부호가 구현마다 달라 같은 바이트를 양수로 읽을지 음수로 읽을지 va_arg 가 결정할 수 없기 때문" ] },
  { k: "c2-sscanf-check", o: [
    "&a 대신 a 를 넘겨야 한다 — sscanf 는 값을 돌려주는 함수라 주소가 아니라 변수 자체를 받아 채운다",
    "%d 대신 %i 를 써야 한다 — %d 는 10진수만 받아들이므로 0x 로 시작하는 입력에서 변환이 실패한다",
    "sscanf 를 부르기 전에 line 을 printf 로 한 번 출력해 두어야 버퍼가 정리되어 변환이 제대로 된다",
    "반환값을 검사하지 않아, 파싱이 실패하면 초기화되지 않은 a, b 를 그대로 사용하게 된다" ] },
  { k: "strncat의 n", o: [
    "`s` 에서 복사할 바이트 수의 최솟값 — 그보다 짧으면 나머지를 널 문자로 채워 n 바이트를 맞춘다",
    "`d` 버퍼의 전체 크기 — 함수가 이 값을 보고 널 문자까지 들어갈 자리를 계산해 잘라 준다",
    "`s`에서 이어붙일 최대 문자 수이며 여기에 널 문자 1개가 추가로 더 쓰인다",
    "`d` 에 이미 들어 있는 문자열의 길이 — strlen 을 다시 계산하지 않도록 호출자가 알려 주는 값이다" ] },
  { k: "c2-2d-layout", o: [
    "행마다 포인터 3개가 먼저 놓이고, 각 포인터가 가리키는 자리에 int 4개씩 모두 12개가 저장된다",
    "int 12개가 연속된 한 덩어리이며 a[0] 행 4개, a[1] 행 4개, a[2] 행 4개 순으로 놓인다",
    "행마다 별도의 메모리 블록이 잡혀 서로 떨어져 있고, a[1] 의 주소는 a[0] 바로 뒤라고 보장되지 않는다",
    "열 우선으로 a[0][0], a[1][0], a[2][0] 순서로 놓여 같은 열의 값들이 메모리에서 이웃한다" ] },
  { k: "사후 검사", o: [
    "덧셈을 두 번 하고 비교까지 하므로 런타임 비용이 커서, 자주 도는 루프에서는 쓸 수 없기 때문",
    "`<` 연산자는 signed 피연산자를 unsigned 로 바꿔 비교하기 때문에 음수가 섞이면 결과가 뒤집히기 때문",
    "검사 식 자체가 이미 오버플로(UB)를 일으켜, 컴파일러가 항상 거짓으로 보고 검사를 제거할 수 있어서",
    "`a + b` 가 넘치면 항상 0 이 되므로 `< a` 대신 `== 0` 으로 검사해야 하고, 이 식으로는 잡히지 않기 때문" ] },
  { k: "c2-ll-vs-array", o: [
    "임의 인덱스 접근이 O(1) 로 빨라지는 대신, 중간에 원소를 끼워 넣을 때 뒤쪽 노드를 모두 한 칸씩 옮겨야 해 삽입이 O(n) 으로 느려진다",
    "노드를 끼워 넣을 때마다 정렬된 자리에 들어가므로 정렬이 자동으로 유지되지만, 그만큼 삽입마다 비교 비용이 든다",
    "위치를 아는 지점의 삽입·삭제는 O(1) 로 싸지지만, n 번째 원소 접근은 O(n) 이고 노드마다 포인터 오버헤드와 캐시 미스가 생긴다",
    "필요한 만큼만 노드를 잡으므로 메모리를 더 적게 쓰고, 노드가 작아 캐시에 잘 들어가 순회도 배열보다 빠르다" ] },
  { k: "c2-bit-precedence", o: [
    "== 가 & 보다 우선순위가 낮아 (x & 1) == 0 으로 묶이는데, & 의 결과가 0 또는 1 이라 비교가 항상 참이 되기 때문",
    "& 는 부호 없는 정수에만 쓸 수 있어 signed int 인 x 에 적용하면 결과가 정의되지 않기 때문",
    "1 대신 1u 를 써야 하며, 부호 있는 1 과 AND 하면 부호 비트까지 검사해 음수 짝수가 홀수로 판정되기 때문",
    "== 가 & 보다 우선순위가 높아 x & (1 == 0) 즉 x & 0 으로 묶이기 때문" ] },
  { k: "c2-shift-ub-width", o: [
    "시프트할 비트가 모두 밀려 나가고 원래 값이 그대로 남아, x << 32 는 x 와 같은 값이 된다",
    "모든 비트가 왼쪽으로 밀려 나가므로 항상 0 이 되며, 이는 표준이 보장하는 결과라 이식성도 있다",
    "시프트 폭이 자료형의 비트 수 이상이므로 정의되지 않은 동작이다",
    "시프트 폭이 상수라 컴파일러가 범위를 검사해 오류를 내며, 변수로 32 를 넘길 때만 컴파일이 된다" ] },
  { k: "c2-strtok-modify", o: [
    "원본을 읽기만 하고 바꾸지 않으며, 토큰의 시작과 길이를 내부 상태에 기억해 두었다가 돌려준다",
    "첫 호출 때 원본을 내부 버퍼에 복사해 두고 복사본을 잘라 나가므로, 원본은 파싱이 끝난 뒤에도 그대로다",
    "구분자를 만난 자리에 널 문자 '\\0' 을 써 넣어 원본을 변형한다",
    "구분자를 공백 한 칸으로 바꿔 토큰 사이를 고르게 만들고, 반환한 포인터는 그 공백 다음을 가리킨다" ] },
  { k: "c2-tu-def", o: [
    "하나의 .c 파일에 #include 를 펼치고 매크로를 처리한 뒤의 결과물로, 컴파일러가 한 번에 처리하는 단위",
    "프로젝트의 모든 .c 파일을 하나로 합친 것으로, 링커가 심볼을 맞추기 위해 한 번에 들여다보는 전체 코드 덩어리",
    "함수 하나 — 컴파일러는 함수 단위로 코드를 만들고 최적화하므로 함수가 곧 번역의 기본 단위다",
    "실행 파일 하나 — 소스가 몇 개든 결국 하나의 실행 파일로 번역되므로 그 결과물을 번역 단위라 부른다" ] },
  { k: "c2-shift-neg-right", o: [
    "구현 정의(implementation-defined)이며, 대부분은 부호를 유지하는 산술 시프트를 쓴다",
    "어떤 경우에도 왼쪽을 0 으로 채우는 논리 시프트로 정해져 있어, 음수를 밀면 부호가 사라지고 큰 양수가 된다",
    "부호 비트가 밀려 나가는 순간 값이 무의미해지므로 표준은 결과를 항상 0 으로 규정한다",
    "부호 있는 음수 상수에 >> 를 적용하면 컴파일러가 오류를 내고, 변수일 때만 컴파일이 된다" ] },
  { k: "c2-static-func", o: [
    "한 번 계산한 결과를 내부에 저장해 두고 호출할 때마다 같은 값을 반환하므로 순수 함수에만 붙인다",
    "프로그램이 시작될 때 main 보다 먼저 자동으로 한 번 호출되어 초기화 코드를 두는 자리로 쓰인다",
    "재귀 호출을 할 수 없으며, 함수 안에서 자기 이름을 부르면 컴파일러가 내부 링키지 위반으로 거부한다",
    "같은 파일 안에서만 호출할 수 있고, 다른 .c 에서 이름으로 참조하면 링크 오류가 난다" ] },
  { k: "c3-enum-2", o: [
    "enum 상수는 이름 문자열로 저장되어 실행 중에도 이름을 찍어 볼 수 있으므로 로그가 읽기 쉬워지기 때문",
    "enum 은 컴파일 시점에 사라져 메모리를 전혀 쓰지 않으므로, #define 보다 실행 파일이 작아지기 때문",
    "이름으로 의도가 드러나고, switch 처리 누락을 컴파일러 경고로 잡을 여지가 생기며, 디버거에도 이름이 보이기 때문",
    "enum 값은 읽기 전용 영역에 놓여 실행 중에 바뀌지 않으므로, 실수로 덮어써도 원래 값이 지켜지기 때문" ] },
  { k: "문자열 리터럴", o: [
    "힙에 놓이므로 다 쓰고 나면 `free(s)` 로 돌려주어야 하고, 잊으면 프로그램이 끝날 때까지 누수가 남는다",
    "스택에 놓이므로 이 함수가 반환되는 순간 사라지고, 포인터를 밖으로 넘기면 매달린 포인터가 된다",
    "정적 저장 수명을 가지며 수정하려 하면 정의되지 않은 동작이다",
    "레지스터에만 존재하므로 주소를 얻을 수 없고, `&s[0]` 처럼 원소의 주소를 구하는 코드는 컴파일되지 않는다" ] },
  { k: "c2-flag-power2", o: [
    "각 상수가 서로 다른 비트 한 개만 차지해, | 로 합쳐도 어떤 옵션이 켜졌는지 구분할 수 있기 때문",
    "컴파일러가 2의 거듭제곱만 매크로 상수로 허용하므로 3 이나 5 같은 값은 #define 으로 만들 수 없기 때문",
    "2의 거듭제곱은 CPU 가 곱셈 대신 시프트로 처리하므로 플래그 비교가 다른 값보다 빠르기 때문",
    "가장 높은 비트를 비워 두어 음수가 나오지 않게 하고, 그래야 플래그를 signed int 에 담아도 안전하기 때문" ] },
  { k: "c2-strtok-literal", o: [
    "strtok 의 첫 인자는 NULL 이어야 하며, 자를 문자열은 두 번째 인자로 넘기고 구분자는 세 번째로 넘겨야 한다",
    "s 는 문자열 리터럴을 가리키므로 strtok 가 그 내용을 수정하려 하는 순간 문제가 된다",
    "구분자를 문자열 \",\" 이 아니라 문자 ',' 로 넘겨야 하며, 문자열로 넘기면 첫 글자만 구분자로 쓰인다",
    "반환값을 char* 로 받으면 안 되고 size_t 로 받아야 하며, strtok 는 토큰의 시작 위치를 정수로 돌려준다" ] },
  { k: "c2-static-file", o: [
    "내부 링키지를 가져 다른 번역 단위에서는 이 이름으로 접근할 수 없다",
    "프로그램이 끝나기 전에 자동으로 해제되므로, 종료 직전에 실행되는 코드에서는 이 변수를 읽을 수 없다",
    "값이 변하지 않는 상수가 되어 counter++ 같은 대입이 컴파일 오류가 나고, 초기값을 반드시 적어야 한다",
    "스택이 아니라 힙에 저장되므로 크기가 큰 배열도 담을 수 있지만, 접근할 때마다 포인터를 한 번 더 거친다" ] },
  { k: "c3-loop-1", o: [
    "재귀 호출의 결과를 그대로 반환하며, 호출 이후에 남은 계산이 없는 형태",
    "한 함수 안에서 재귀 호출을 두 번 이상 하는 형태로, 피보나치처럼 두 갈래로 뻗는 재귀가 대표적이다",
    "재귀 호출의 결과를 받아 다시 곱하거나 더하는 형태로, 되돌아오면서 계산이 이어지는 재귀를 말한다",
    "함수의 첫 줄에서 곧바로 재귀 호출을 하는 형태로, 종료 조건보다 호출이 먼저 나오는 재귀를 말한다" ] },
  { k: "c2-exit-code", o: [
    "main 은 void 를 반환해야 한다. 종료 코드는 exit 함수로만 전달할 수 있고, return 으로 돌려준 값은 셸에 닿지 않는다",
    "성공이 1, 실패가 0 이다. C 의 참·거짓 규약과 같아서 셸의 if 문과 && 도 1 이 돌아와야 다음 명령으로 넘어간다",
    "반환값은 운영체제가 읽지 않고 버리므로 아무 의미가 없고, 성공 여부를 알리려면 표준 출력이나 파일에 결과를 따로 써야 한다",
    "성공은 EXIT_SUCCESS(보통 0), 실패는 EXIT_FAILURE(0 이 아닌 값)로 알리고, 셸이 그 종료 코드를 읽는다" ] },
  /* 3차 — 45~74 */
  { k: "c2-include-quote", o: [
    "따옴표 형식은 보통 소스 파일이 있는 디렉터리 등 사용자 경로를 먼저 찾고, 꺾쇠 형식은 구현이 정한 표준 헤더 경로에서 찾는다",
    "따옴표 형식만 헤더 안의 매크로를 펼치고, 꺾쇠 형식은 미리 컴파일된 표준 헤더를 그대로 붙여 넣어 매크로가 처리되지 않는다",
    "둘은 완전히 같은 동작을 하며 어느 쪽을 쓰든 같은 경로를 같은 순서로 찾으므로, 표기는 팀 취향의 문제다",
    "꺾쇠 형식은 시스템 헤더 캐시를 쓰므로 컴파일이 더 빠르고, 탐색 경로와 순서는 따옴표 형식과 같다" ] },
  { k: "c3-td-2", o: [
    "전역 변수를 다른 파일에서 보이지 않게 감출 때 — typedef 로 감싼 이름은 파일 밖으로 새어 나가지 않는다",
    "int 하나에 짧은 별명을 붙여 줄 때 — 타입 이름이 짧아질수록 코드가 읽기 쉬워지고 의도도 잘 드러난다",
    "매크로 대신 상수를 정의할 때 — typedef 로 정의한 이름은 디버거에 보이고 타입 검사도 받는다",
    "함수 포인터나 배열 포인터처럼 선언 문법이 난해한 타입을 다룰 때" ] },
  { k: "누수 판정", o: [
    "더 이상 접근할 수단이 없는 힙 블록이 해제되지 않은 채 남는 것",
    "이미 free 로 돌려준 메모리를 포인터가 계속 가리키고 있어 그 자리를 다시 읽거나 쓰는 것",
    "재귀가 너무 깊거나 지역 배열이 커서 스택 공간을 전부 써 버려 프로그램이 죽는 것",
    "포인터 연산이 배열의 끝을 넘어가 이웃한 다른 변수의 메모리를 읽거나 덮어쓰는 것" ] },
  { k: "구현 정의 요소", o: [
    "필드에 값을 대입하는 방법 — 어떤 구현은 = 을, 어떤 구현은 전용 매크로를 요구한다",
    "비트 수를 지정하는 문법 — 콜론 뒤에 오는 수를 상수로만 받는지 식으로도 받는지가 구현마다 다르다",
    "저장 단위 안에서 필드가 배치되는 순서와 경계를 넘는지 여부",
    "구조체 안에서만 쓸 수 있다는 점 — 공용체에서 허용하는지는 구현이 정한다" ] },
  { k: "c2-ll-double-ptr", o: [
    "노드가 함수 안과 밖에서 한 번씩 만들어져 메모리가 두 배로 할당되고, 밖에서 만든 쪽은 누수로 남는다",
    "매개변수는 값 복사라 함수 안에서 head 를 바꿔도 호출자의 head 는 그대로여서 새 노드가 반영되지 않는다",
    "새 노드가 head 앞이 아니라 뒤에 붙어 리스트가 뒤집힌 순서로 쌓이고, 순회하면 거꾸로 나온다",
    "Node* 매개변수에 새 노드 주소를 대입하는 순간 타입이 맞지 않아 컴파일 오류가 난다" ] },
  { k: "c2-error-style", o: [
    "성공/실패를 반환값(예: 0 또는 음수 코드)으로 알리고, 실제 결과는 출력 매개변수로 돌려준다",
    "실패해도 아무것도 하지 않고 조용히 넘어간다 — 호출자가 처리하지 않을 오류로 흐름을 끊지 않는 편이 낫다",
    "실패하면 메시지를 찍고 exit 를 호출해 프로그램을 끝낸다 — 잘못된 상태로 계속 도는 것보다 안전하다",
    "실패 원인을 전역 변수 하나에 기록하고 반환값은 언제나 void 로 둔다 — errno 와 같은 방식이라 호출자에게 익숙하다" ] },
  { k: "c2-defensive-param", o: [
    "검사를 하지 않는 것이 옳다 — 매 호출마다 인자를 확인하면 성능이 떨어지고, 잘못된 인자를 넘긴 것은 전적으로 호출자의 책임이다",
    "잘못된 인자가 오면 조용히 무시하고 성공을 반환한다 — 호출자를 신뢰해야 API 가 단순해지고 호출 코드도 짧아진다",
    "NULL 포인터나 잘못된 크기처럼 호출자가 흔히 하는 실수는 확인해 오류 코드로 돌려주고, 내부 불변식은 assert 로 검사한다",
    "모든 인자를 assert 로만 검사한다 — 릴리스 빌드에서는 사라지므로 성능 손해가 없고 개발 중에는 바로 드러난다" ] },
  { k: "c3-res-2", o: [
    "둘은 완전히 같은 함수이며 이름만 다르다 — 옛 코드와의 호환을 위해 두 이름을 모두 남겨 두었을 뿐이다",
    "memcpy는 두 영역이 겹치지 않는다고 가정(restrict)하고, memmove는 겹쳐도 올바르게 동작하도록 정의되어 있다",
    "memmove 가 memcpy 보다 어떤 경우에도 빠르다 — 겹침을 검사해 최적 경로를 고르므로 항상 memmove 를 쓰면 된다",
    "memcpy 는 문자열 전용이라 널 문자를 만나면 멈추고, memmove 는 바이트 수만큼 끝까지 복사한다" ] },
  { k: "c3-td-1", o: [
    "ulong 은 플랫폼과 무관하게 항상 8바이트로 고정되므로, unsigned long 이 4바이트인 환경에서는 서로 다른 타입이다",
    "ulong은 unsigned long과 완전히 같은 타입이며 서로 자유롭게 대입된다",
    "ulong 은 새로운 타입이라 unsigned long 값을 캐스팅 없이 대입하면 컴파일러가 타입 불일치 오류를 낸다",
    "ulong 은 unsigned long 하나를 멤버로 가진 구조체가 되어, 값을 쓰려면 멤버 이름으로 접근해야 한다" ] },
  { k: "snprintf 반환값", o: [
    "실제로 버퍼에 쓰인 문자 수 — 잘렸다면 n-1 이 돌아오므로 이 값으로 잘림 여부를 알 수는 없다",
    "잘리지 않았다면 쓰였을 전체 문자 수(널 종단 제외)",
    "성공하면 0, 실패하면 -1 — 다른 표준 입출력 함수와 같은 규약이라 반환값을 곧바로 오류 검사에 쓴다",
    "남은 버퍼 공간의 크기 — 이어서 쓸 때 이 값을 다음 호출의 n 으로 넘기면 된다" ] },
  { k: "c2-endian-scope", o: [
    "x >> 8 의 계산 결과가 엔디언에 따라 달라진다 — 리틀 엔디언에서는 낮은 바이트가 앞에 있어 반대 방향으로 밀린다",
    "엔디언은 값을 메모리에 바이트 단위로 늘어놓는 순서이며, 값 자체에 대한 시프트·마스킹 결과는 엔디언과 무관하다",
    "엔디언은 한 바이트 안에서 비트가 놓이는 순서를 뜻하며, 그래서 & 로 마스킹한 결과가 기계마다 달라진다",
    "리틀 엔디언 기계에서는 비트 연산자의 결과를 믿을 수 없어, 이식성이 필요한 코드는 산술 연산으로 바꿔 써야 한다" ] },
  { k: "c2-fgets-newline", o: [
    "줄 끝의 개행 문자가 항상 두 개 붙어 저장되므로, 출력하기 전에 마지막 두 글자를 잘라 내야 한다",
    "개행 문자가 제거된 상태로 저장된다 — fgets 는 줄 단위 함수라 줄 끝 표시를 버퍼에 남기지 않는다",
    "개행 문자가 공백으로 바뀌어 저장되므로, 여러 줄을 이어 읽으면 한 줄로 합쳐진다",
    "줄이 버퍼 안에 다 들어갔다면 개행 문자('\\n')까지 포함해 저장되고 그 뒤에 널 문자가 붙는다" ] },
  { k: "c2-header-selfcontained", o: [
    "헤더에는 다른 헤더를 포함하지 않고, 필요한 헤더를 어떤 순서로 먼저 포함할지는 사용자가 맞추게 한다",
    "헤더는 자기 자신만 포함해도 컴파일되도록 필요한 타입의 헤더를 스스로 포함하고, 공개할 선언만 노출한다",
    "구현에 쓰는 구조체는 헤더에 전부 공개해 사용자가 멤버에 직접 접근할 수 있게 하고 접근 함수는 만들지 않는다",
    "모든 내부 헬퍼 함수도 헤더에 선언해 두어 다른 파일에서 재사용할 수 있게 하고, static 은 쓰지 않는다" ] },
  { k: "c3-frame-6", o: [
    "전역 변수와 static 변수의 현재 값을 표로 보여 주어 어느 변수가 언제 바뀌었는지 추적하게 해 준다",
    "컴파일러가 어떤 함수를 인라인하고 어떤 루프를 펼쳤는지 적어 둔 최적화 로그를 보여 준다",
    "현재 지점까지 쌓여 있는 호출들의 목록과 각 호출의 위치",
    "힙에 남아 있는 할당 블록의 목록과 크기를 보여 주어 누수가 어디서 생겼는지 찾게 해 준다" ] },
  { k: "c3-frame-3", o: [
    "재귀 함수 안에서는 배열을 선언할 수 없고, 컴파일러가 프레임 크기를 정하지 못해 오류를 낸다",
    "호출 깊이마다 그 크기만큼 스택을 더 소비해 비교적 얕은 깊이에서도 스택이 고갈될 수 있다",
    "지역 배열은 호출마다 0 으로 초기화되므로 8KB 를 매번 지우느라 재귀가 눈에 띄게 느려진다",
    "그 크기의 지역 배열은 컴파일러가 힙에 잡아 주므로, 재귀가 끝날 때마다 free 를 빼먹어 누수가 쌓인다" ] },
  { k: "지역 배열 반환", o: [
    "배열이 값으로 복사되어 반환되므로 크기가 클수록 느리고, 큰 배열은 반환 자체가 실패할 수 있기 때문",
    "지역 배열은 힙에 잡히므로 호출자가 `free` 해야 하는데, 반환된 포인터로는 그 책임이 드러나지 않기 때문",
    "함수가 끝나면 그 배열의 수명이 끝나 반환된 포인터가 dangling 포인터가 되기 때문",
    "배열은 반환 타입으로 쓸 수 없어 컴파일 오류가 나고, 반환하려면 구조체로 감싸야 하기 때문" ] },
  { k: "실행 결과", o: [
    "'a','b','c','d' 4바이트가 채워지고 널 종단 문자가 없다",
    "\"abcdef\" 전체가 저장되어 버퍼가 넘치고, 뒤에 놓인 다른 변수를 덮어쓴다",
    "\"abc\" 가 저장되고 널 종단된다 — strncpy 는 마지막 한 칸을 종단 문자 자리로 남긴다",
    "아무것도 복사되지 않는다 — 원본이 버퍼보다 길면 strncpy 는 복사를 거부하고 NULL 을 돌려준다" ] },
  { k: "c2-ll-pp-idiom", o: [
    "노드를 지운 뒤에도 남은 노드들의 정렬 상태를 유지해 주므로, 정렬된 리스트에서 안심하고 쓸 수 있다",
    "pp 가 '이전 노드'가 아니라 '링크 자체'를 들고 있어, 머리 노드 삭제와 중간 노드 삭제를 특수 처리 없이 한 코드로 처리한다",
    "삭제 대상을 찾는 탐색이 O(n) 에서 O(log n) 으로 빨라진다 — 링크의 주소를 들고 있으면 노드를 절반씩 건너뛰며 찾을 수 있다",
    "prev 변수 하나를 덜 쓰므로 스택 메모리를 아끼고, 포인터를 한 단계 덜 거쳐 삭제 자체도 빨라진다" ] },
  { k: "c2-clock-cpu", o: [
    "시스템이 부팅된 뒤 흐른 시간(초)으로, 프로세스가 언제 시작했든 같은 기준으로 잰다",
    "프로그램이 사용한 프로세서 시간(초)으로, 대기 시간은 포함되지 않는다",
    "프로세스에 허용된 남은 실행 시간(초)으로, 0 에 가까워지면 스케줄러가 강제로 멈춘다",
    "프로그램이 시작한 뒤 흐른 실제 벽시계 시간(초)으로, sleep 으로 기다린 시간도 포함된다" ] },
  { k: "feof 루프", o: [
    "`fgets` 는 `feof` 와 함께 쓸 수 없다 — 두 함수가 내부 버퍼 위치를 서로 다르게 관리해 조합하면 줄이 어긋난다",
    "`feof` 가 호출될 때마다 파일 포인터를 처음으로 되감기 때문에 같은 줄을 반복해서 읽게 된다",
    "EOF 플래그는 읽기가 실패한 **뒤에** 서므로 마지막 줄을 한 번 더 처리하게 된다",
    "파일을 연 직후에는 `feof` 가 참이라 루프가 한 번도 돌지 않고, 첫 읽기를 먼저 해 주어야 한다" ] },
  { k: "c2-flag-unsigned", o: [
    "unsigned 타입만 비트 연산자를 지원하므로, 부호 있는 int 에 시프트를 쓰면 컴파일러가 거부한다",
    "부호 있는 int 에서 1 << 31 은 부호 비트를 침범하는 오버플로라 정의되지 않은 동작이기 때문",
    "unsigned 연산은 부호 확장을 하지 않아 CPU 가 더 빨리 처리하므로, 마스크 연산이 많은 코드에서 성능이 좋아진다",
    "1u 를 쓰면 상수가 자동으로 64비트 unsigned long 으로 확장되어, 31 을 밀어도 폭이 남아 안전해진다" ] },
  { k: "c3-mem-5", o: [
    "스택이 힙보다 훨씬 크다 — 지역 변수와 호출 프레임이 모두 스택에 놓이므로 운영체제가 넉넉히 잡아 둔다",
    "스택은 보통 수 MB 수준으로 제한되고, 힙은 그보다 훨씬 크게 늘어날 수 있다",
    "힙에는 크기 개념이 없다 — malloc 은 필요한 만큼 가상 주소를 만들어 내므로 실패하는 일이 없다",
    "둘 다 프로세스 시작 때 같은 크기로 고정되며, 어느 쪽이든 넘치면 같은 오류로 프로그램이 끝난다" ] },
  { k: "strlcpy", o: [
    "n이 0보다 크면 항상 널 종단하고 원본 문자열의 길이를 반환한다",
    "ISO C 표준 함수라 모든 컴파일러에 들어 있으며, 이식성을 걱정하지 않고 어디서나 쓸 수 있다",
    "복사한 뒤 남는 자리를 널 문자로 가득 채우므로, strncpy 처럼 버퍼 끝까지 쓰는 비용이 든다",
    "`strncpy` 와 완전히 같은 동작을 하며 이름만 다르다 — BSD 가 호환을 위해 별칭을 붙였을 뿐이다" ] },
  { k: "c2-time-epoch", o: [
    "프로그램이 시작한 뒤 CPU 가 소비한 클럭 수로, CLOCKS_PER_SEC 로 나누면 초가 된다",
    "현재 시각을 사람이 읽을 수 있게 담은 문자열로, 그대로 printf 의 %s 로 출력할 수 있다",
    "1970년 1월 1일 UTC(에폭)부터 흐른 초를 담은 time_t 값",
    "프로그램이 시작한 뒤 흐른 시간을 밀리초 단위로 담은 값으로, 두 번 불러 빼면 경과 시간이 된다" ] },
  { k: "큰 지역 배열", o: [
    "배열 크기만큼 코드가 커져 컴파일 시간이 길어질 뿐, 실행할 때는 아무 문제가 없다",
    "스택 공간을 초과해 스택 오버플로로 프로그램이 죽을 수 있다",
    "지역 배열은 호출마다 0 으로 초기화되므로 4MB 를 매번 지우느라 함수가 느려질 뿐이다",
    "그 크기는 컴파일러가 자동으로 힙으로 옮기므로, 함수가 끝나도 해제되지 않아 누수가 생긴다" ] },
  { k: "힙 수명", o: [
    "`free` 로 해제하거나 프로그램이 끝날 때까지 유지된다",
    "할당한 함수가 반환될 때 끝난다 — 지역 변수와 같은 프레임에 묶여 있어 함께 정리된다",
    "다음 `malloc` 호출 때 회수된다 — 할당기가 새 요청을 받으면 이전 블록을 재사용한다",
    "블록을 가리키던 포인터가 스코프를 벗어나면 자동으로 끝난다 — 가리키는 곳이 없으면 해제된다" ] },
  { k: "c2-bst-rule", o: [
    "부모가 두 자식보다 항상 작다 — 그래서 뿌리에 가장 작은 값이 오고, 최솟값 조회가 O(1) 이 된다",
    "왼쪽 자식이 오른쪽 자식보다 항상 깊게 놓인다 — 깊이로 정렬되어 있어 왼쪽으로 내려갈수록 값이 작아진다",
    "모든 리프의 깊이가 항상 같다 — 그래야 어느 값을 찾든 같은 횟수의 비교로 도달한다",
    "어떤 노드 기준으로 왼쪽 서브트리에는 더 작은 값만, 오른쪽 서브트리에는 더 큰 값만 있다" ] },
  { k: "c3-const-6", o: [
    "파일 범위에서는 초기화가 아직 실행되지 않아 N 이 0 으로 보이고, 크기가 0 인 배열은 선언할 수 없기 때문",
    "const 변수는 배열 선언과 함께 쓸 수 없다 — 배열 크기 자리에 변수를 적는 것 자체가 문법 위반이다",
    "C의 const 한정 변수는 상수 표현식이 아니어서 파일 범위 배열 크기로 쓸 수 없다(#define이나 enum을 쓴다)",
    "C 에서는 배열 크기를 상수로 줄 때 상한이 있어 10 은 넘을 수 없고, 더 큰 배열은 malloc 으로만 만들 수 있기 때문" ] },
  { k: "c3-pr-6", o: [
    "printf 에서 double 은 %lf 로만 써야 한다 — %f 는 float 전용이라 double 을 넘기면 상위 4바이트가 잘려 나가 값이 엉뚱하게 찍힌다",
    "%f 는 float 전용이라 double 에 쓰면 컴파일 에러가 난다 — 두 지정자가 서로 다른 타입 전용이라 승격은 일어나지 않는다",
    "printf 에서는 float 도 double 로 승격되어 오므로 %f 로 찍고, scanf 에서는 float 포인터에 %f, double 포인터에 %lf 를 쓴다",
    "scanf 에서는 %f 와 %lf 가 같게 동작한다 — 읽은 값을 포인터가 가리키는 타입에 맞춰 알아서 변환해 넣기 때문이다" ] },
  { k: "c3-pf-4", o: [
    "printf(fmt, ap) 를 호출한다 — va_list 를 넘기면 printf 가 그 안의 인자를 차례로 꺼내 쓴다",
    "va_arg 로 하나씩 꺼내 직접 문자열을 조립해야만 한다 — 서식 문자열을 해석하는 일은 호출자의 몫이다",
    "vprintf(fmt, ap)처럼 va_list를 받는 v 계열 함수를 쓴다",
    "printf 계열은 감쌀 수 없다 — 가변 인자를 다른 가변 인자 함수로 넘기는 방법이 C 에는 없다" ] },
] };
