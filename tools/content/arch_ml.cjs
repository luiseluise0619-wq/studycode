/* ml 트랙 설계 문항 — 학습 파이프라인을 직접 그린다.
   ref 는 검증용 참조 설계다(주입하지 않는다). */
module.exports = [
{ t: "arch", kind: "cloud", k: "학습 파이프라인의 재현성", lv: 2, cat: "design", track: "ml",
  q: "지난달 모델을 <b>똑같이 다시 만들 수 없습니다</b>. 코드는 커밋으로 남았지만 어떤 데이터로 어떤 설정에서 돌렸는지가 없습니다. 학습을 <b>다시 재현할 수 있는</b> 파이프라인을 그리세요.",
  src: '{\n  "components": [\n    { "name": "train-job", "icon": "🏋️" }\n  ],\n  "connections": []\n}',
  ref: { components: [
      { name: "raw-store", icon: "🗄️", note: "원천 데이터는 덮어쓰지 않고 스냅숏 버전으로 쌓는다" },
      { name: "dataset-v", icon: "📦", note: "학습에 쓴 데이터 버전과 해시를 남긴다" },
      { name: "train-job", icon: "🏋️", note: "코드 커밋·데이터 버전·하이퍼파라미터·seed 를 입력으로 받는다" },
      { name: "experiment-tracker", icon: "📒", note: "실행마다 입력과 결과 지표를 기록한다" },
      { name: "model-registry", icon: "🏷️", note: "산출된 모델에 데이터 버전과 커밋을 함께 붙여 저장한다" }],
    connections: [{ from: "raw-store", to: "dataset-v" }, { from: "dataset-v", to: "train-job" },
      { from: "train-job", to: "experiment-tracker" }, { from: "train-job", to: "model-registry" }],
    reproduce: "seed 고정 · 데이터 버전 고정 · 커밋 해시 기록 — 세 가지가 같으면 같은 모델이 나온다" },
  tests: [
    { d: "데이터를 버전으로 고정하는 자리가 있다", js: "(D.components||[]).some(c=>/dataset|snapshot|version|버전/i.test(c.name+' '+(c.note||'')))" },
    { d: "실험 기록을 남기는 구성 요소가 있다", js: "(D.components||[]).some(c=>/track|experiment|registry|기록/i.test(c.name+' '+(c.note||'')))" },
    { d: "모델 저장소가 있다", js: "(D.components||[]).some(c=>/model|registry|모델/i.test(c.name))" },
    { d: "seed 를 고정한다는 점을 적었다", js: "/seed|시드|난수 고정/i.test(JSON.stringify(D))" },
    { d: "학습에 쓴 코드 버전을 남긴다", js: "/commit|커밋|코드 버전|git/i.test(JSON.stringify(D))" },
    { d: "데이터에서 학습으로 흐름이 이어져 있다", js: "(D.connections||[]).some(e=>/train|학습/i.test(e.to))" }],
  ex: "🎯 재현성은 <b>같은 입력에서 같은 산출물</b>이 나오는 성질입니다. 코드만 고정해서는 부족합니다 — 데이터·설정·난수 셋 중 하나만 달라도 결과가 달라집니다.\n🔧 그래서 학습 작업의 입력을 <b>전부 값으로</b> 만듭니다. 데이터는 <code>latest</code> 가 아니라 버전으로 가리키고, 하이퍼파라미터와 seed 는 파일로 남기고, 산출된 모델에는 그 셋을 태그로 붙입니다.\n💡 실험 추적기는 사치가 아니라 <b>비교의 전제</b>입니다. 무엇을 바꿔서 얼마나 좋아졌는지 말할 수 없으면 그 실험은 결론을 낼 수 없습니다.\n⚠️ GPU 연산은 완전한 비트 단위 재현이 어려운 경우가 있습니다. '완전히 같은 가중치' 대신 <b>같은 결론이 나오는 범위</b>를 재현 목표로 잡는 편이 현실적입니다." },

{ t: "arch", kind: "cloud", k: "학습과 서빙이 같은 특성을 보게", lv: 3, cat: "design", track: "ml",
  q: "오프라인 점수는 좋은데 <b>운영에서만 떨어집니다</b>. 학습은 배치 SQL 로 특성을 만들고 서빙은 애플리케이션 코드가 따로 계산합니다. <b>두 경로가 갈리지 않도록</b> 설계하세요.",
  src: '{\n  "components": [\n    { "name": "batch-etl", "icon": "🧮" },\n    { "name": "api-server", "icon": "🖥️" }\n  ],\n  "connections": []\n}',
  ref: { components: [
      { name: "feature-def", icon: "📐", note: "특성 정의를 한 곳에만 둔다 — 학습과 서빙이 같은 정의를 읽는다" },
      { name: "feature-store-offline", icon: "🗃️", note: "학습용. 시점 기준 조회로 미래 값이 새어 들지 않게 한다" },
      { name: "feature-store-online", icon: "⚡", note: "서빙용. 같은 정의로 계산된 값을 낮은 지연으로 돌려준다" },
      { name: "train-job", icon: "🏋️" },
      { name: "api-server", icon: "🖥️" },
      { name: "parity-check", icon: "🔍", note: "같은 입력에 두 경로의 특성값이 같은지 주기적으로 비교한다" }],
    connections: [{ from: "feature-def", to: "feature-store-offline" }, { from: "feature-def", to: "feature-store-online" },
      { from: "feature-store-offline", to: "train-job" }, { from: "feature-store-online", to: "api-server" },
      { from: "feature-store-online", to: "parity-check" }],
    skew: "학습·서빙 왜곡(training-serving skew)을 정의 공유와 대조 검사로 막는다" },
  tests: [
    { d: "특성 정의를 한 곳에 두는 구성 요소가 있다", js: "(D.components||[]).some(c=>/feature.?def|정의|feature.?store/i.test(c.name+' '+(c.note||'')))" },
    { d: "학습용과 서빙용 경로가 모두 있다", js: "(D.connections||[]).some(e=>/train|학습/i.test(e.to)) && (D.connections||[]).some(e=>/api|serv|서빙/i.test(e.to))" },
    { d: "두 경로의 값을 대조하는 검사가 있다", js: "(D.components||[]).some(c=>/parity|대조|비교|check/i.test(c.name+' '+(c.note||'')))" },
    { d: "시점 기준 조회(누수 방지)를 적었다", js: "/시점|point.?in.?time|미래 값|누수/i.test(JSON.stringify(D))" },
    { d: "학습·서빙 왜곡이라는 문제를 명시했다", js: "/왜곡|skew/i.test(JSON.stringify(D))" }],
  ex: "🎯 같은 이름의 특성이 <b>두 벌의 코드</b>로 계산되면 언젠가 반드시 갈라집니다. 반올림 한 자리, 결측 처리 한 줄이 다르면 모델이 보는 세상이 달라집니다.\n🔧 해법은 <b>정의를 하나만 두는 것</b>입니다. 학습은 오프라인 저장소에서, 서빙은 온라인 저장소에서 읽되 둘 다 같은 정의에서 파생되게 합니다. 그리고 같은 입력에 두 값이 같은지 <b>대조 검사</b>를 주기적으로 돌립니다.\n💡 오프라인 조회는 반드시 <b>시점 기준</b>이어야 합니다. '지금의 값' 으로 과거를 학습하면 미래 정보가 새어 들어 오프라인 점수만 좋아집니다.\n⚠️ 특성 저장소는 공짜가 아닙니다. 운영할 시스템이 하나 늘어나므로, 특성이 몇 개뿐이라면 <b>공유 라이브러리</b> 하나로 정의를 묶는 것으로 충분할 때가 많습니다." },

{ t: "arch", kind: "cloud", k: "중단되는 장비 위에서 학습하기", lv: 3, cat: "design", track: "ml",
  q: "비용을 아끼려고 <b>언제든 회수될 수 있는 스팟 장비</b>로 장시간 학습을 돌립니다. 지금은 회수될 때마다 처음부터 다시 시작합니다. <b>진행을 잃지 않는</b> 구조를 그리세요.",
  src: '{\n  "components": [\n    { "name": "spot-worker", "icon": "💸", "scale": 8 }\n  ],\n  "connections": []\n}',
  ref: { components: [
      { name: "spot-worker", icon: "💸", scale: 8, note: "회수 신호를 받으면 즉시 체크포인트를 저장하고 빠진다" },
      { name: "checkpoint-store", icon: "💾", note: "정해진 스텝마다 가중치·옵티마이저 상태·데이터 위치를 함께 저장한다" },
      { name: "coordinator", icon: "🎛️", note: "살아남은 워커로 다시 묶고 마지막 체크포인트에서 이어 시작한다" },
      { name: "on-demand-worker", icon: "🛡️", scale: 1, note: "최소 한 대는 회수되지 않는 장비로 둔다" }],
    connections: [{ from: "spot-worker", to: "checkpoint-store" }, { from: "checkpoint-store", to: "coordinator" },
      { from: "coordinator", to: "spot-worker" }, { from: "coordinator", to: "on-demand-worker" }],
    resume: "이어 시작(resume) — 마지막 체크포인트의 스텝과 데이터 오프셋에서 재개한다" },
  tests: [
    { d: "체크포인트를 저장하는 자리가 있다", js: "(D.components||[]).some(c=>/checkpoint|체크포인트/i.test(c.name+' '+(c.note||'')))" },
    { d: "옵티마이저 상태까지 저장한다고 적었다", js: "/옵티마이저|optimizer/i.test(JSON.stringify(D))" },
    { d: "이어 시작하는 흐름이 있다", js: "/resume|이어 시작|재개/i.test(JSON.stringify(D))" },
    { d: "회수 신호를 다룬다고 적었다", js: "/회수|preempt|interrupt|중단 신호/i.test(JSON.stringify(D))" },
    { d: "데이터의 어디까지 읽었는지도 남긴다", js: "/오프셋|offset|데이터 위치|스텝/i.test(JSON.stringify(D))" }],
  ex: "🎯 스팟 장비의 전제는 '<b>언제든 사라진다</b>' 입니다. 사라지는 것을 막을 수는 없으므로, 사라져도 잃는 것이 적게 만드는 것이 설계의 목표입니다.\n🔧 정해진 간격으로 <b>가중치·옵티마이저 상태·데이터 오프셋</b>을 함께 저장합니다. 셋 중 하나라도 빠지면 재개한 학습이 이전과 다른 궤적을 그립니다. 회수 신호를 받으면 마지막으로 한 번 더 저장하고 빠집니다.\n💡 간격은 <b>저장 비용과 잃을 시간</b>의 균형입니다. 저장이 1분 걸리는데 5분마다 저장하면 20%를 저장에 쓰는 셈이고, 한 시간마다면 최대 한 시간을 잃습니다.\n⚠️ 모든 워커가 동시에 회수될 수도 있습니다. 조정자와 체크포인트 저장소는 <b>회수되지 않는 자리</b>에 두어야 다시 묶을 수 있습니다." },

{ t: "arch", kind: "cloud", k: "라벨을 만들며 배우는 고리", lv: 2, cat: "design", track: "ml",
  q: "라벨이 모자라 모델이 늘지 않습니다. 예산은 한정돼 있으니 <b>아무 데이터나 라벨링할 수는 없습니다</b>. 라벨링과 학습이 서로를 돕는 고리를 설계하세요.",
  src: '{\n  "components": [\n    { "name": "labeling-tool", "icon": "🏷️" },\n    { "name": "train-job", "icon": "🏋️" }\n  ],\n  "connections": []\n}',
  ref: { components: [
      { name: "unlabeled-pool", icon: "🌊", note: "운영 트래픽에서 모은 라벨 없는 데이터" },
      { name: "sampler", icon: "🎯", note: "모델이 가장 불확실해하는 표본을 골라 보낸다(능동 학습)" },
      { name: "labeling-tool", icon: "🏷️", note: "두 명 이상이 라벨링하고 불일치는 조정자가 정한다" },
      { name: "golden-set", icon: "🥇", note: "라벨 품질을 재는 고정 표본 — 라벨러 일치도를 기록한다" },
      { name: "train-job", icon: "🏋️" },
      { name: "model-registry", icon: "🏷️" }],
    connections: [{ from: "unlabeled-pool", to: "sampler" }, { from: "sampler", to: "labeling-tool" },
      { from: "labeling-tool", to: "train-job" }, { from: "train-job", to: "model-registry" },
      { from: "model-registry", to: "sampler", via: "다음 회차에 쓸 불확실도 계산" },
      { from: "labeling-tool", to: "golden-set" }],
    loop: "모델이 고른 표본을 사람이 라벨링하고, 그 라벨로 다시 학습하는 되먹임 고리" },
  tests: [
    { d: "라벨링할 표본을 고르는 구성 요소가 있다", js: "(D.components||[]).some(c=>/sampl|select|고르|추출/i.test(c.name+' '+(c.note||'')))" },
    { d: "모델의 불확실도를 기준으로 고른다고 적었다", js: "/불확실|uncertain|능동 학습|active learning/i.test(JSON.stringify(D))" },
    { d: "라벨 품질을 재는 자리가 있다", js: "(D.components||[]).some(c=>/golden|품질|일치도|검수/i.test(c.name+' '+(c.note||'')))" },
    { d: "라벨러 사이의 불일치를 다룬다고 적었다", js: "/불일치|일치도|카파|두 명|이중/i.test(JSON.stringify(D))" },
    { d: "모델에서 표본 선택으로 되돌아오는 연결이 있다", js: "(D.connections||[]).some(e=>/model|registry|모델/i.test(e.from) && /sampl|고르|select/i.test(e.to))" }],
  ex: "🎯 라벨 예산이 한정되면 <b>무엇을 라벨링할지</b>가 모델 구조보다 중요해집니다. 이미 잘 맞히는 표본에 라벨을 붙이는 것은 돈만 쓰는 일입니다.\n🔧 <b>능동 학습</b>은 모델이 가장 헷갈려하는 표본을 골라 사람에게 보냅니다. 그 라벨로 다시 학습하면 같은 예산으로 훨씬 빨리 늡니다 — 고리가 도는 구조가 핵심입니다.\n💡 라벨 자체가 틀릴 수 있습니다. 이중 라벨링과 불일치 조정, 그리고 <b>고정된 품질 표본</b>으로 라벨러 일치도를 재지 않으면, 모델을 재는 자가 흔들립니다.\n⚠️ 불확실한 표본만 모으면 데이터 분포가 한쪽으로 치우칩니다. 무작위 표본을 일정 비율 섞어 <b>전체 분포도 함께</b> 유지해야 합니다." },

{ t: "arch", kind: "cloud", k: "모델을 올릴지 정하는 관문", lv: 3, cat: "design", track: "ml",
  q: "새 모델을 <b>누가 어떤 근거로</b> 배포할지가 사람마다 다릅니다. 오프라인 점수만 보고 올렸다가 두 번 롤백했습니다. 승격 과정을 관문으로 설계하세요.",
  src: '{\n  "components": [\n    { "name": "model-registry", "icon": "🏷️" },\n    { "name": "prod", "icon": "🚀" }\n  ],\n  "connections": []\n}',
  ref: { components: [
      { name: "model-registry", icon: "🏷️", note: "후보 모델은 candidate 단계로 등록된다" },
      { name: "offline-eval", icon: "📏", note: "고정된 평가 세트 + 슬라이스별 하한. 하나라도 깨지면 여기서 멈춘다" },
      { name: "shadow", icon: "👥", note: "운영 트래픽을 복제해 응답은 버리고 지표만 비교한다" },
      { name: "canary", icon: "🐤", scale: 1, note: "트래픽 5% 로 시작해 지표가 유지되면 늘린다" },
      { name: "prod", icon: "🚀" },
      { name: "rollback", icon: "↩️", note: "이전 버전을 즉시 되돌릴 수 있게 남겨 둔다" }],
    connections: [{ from: "model-registry", to: "offline-eval" }, { from: "offline-eval", to: "shadow" },
      { from: "shadow", to: "canary" }, { from: "canary", to: "prod" }, { from: "canary", to: "rollback" }],
    gate: "각 단계는 통과 기준이 미리 정해져 있고, 자동으로 판정한다" },
  tests: [
    { d: "오프라인 평가 단계가 있다", js: "(D.components||[]).some(c=>/offline|평가|eval/i.test(c.name+' '+(c.note||'')))" },
    { d: "실제 트래픽으로 먼저 재 보는 단계가 있다", js: "(D.components||[]).some(c=>/shadow|canary|카나리|섀도/i.test(c.name))" },
    { d: "되돌리는 경로가 있다", js: "(D.components||[]).some(c=>/rollback|되돌|롤백/i.test(c.name+' '+(c.note||'')))" },
    { d: "슬라이스별 기준을 본다고 적었다", js: "/슬라이스|slice|집단별|세그먼트/i.test(JSON.stringify(D))" },
    { d: "통과 기준을 미리 정해 둔다고 적었다", js: "/기준|threshold|하한|게이트/i.test(JSON.stringify(D))" },
    { d: "단계가 순서대로 이어져 있다", js: "(D.connections||[]).length>=4" }],
  ex: "🎯 배포 결정을 사람의 판단에 맡기면 <b>매번 기준이 달라집니다</b>. 관문은 '무엇을 통과해야 다음으로 가는가' 를 미리 못박아 두는 장치입니다.\n🔧 오프라인 평가는 <b>슬라이스별 하한</b>까지 봐야 합니다 — 전체 평균은 소수 집단이 무너져도 거의 움직이지 않습니다. 그다음 섀도로 실제 입력을 먹여 보고, 카나리로 작은 트래픽에 노출한 뒤 늘립니다.\n💡 각 단계에서 <b>무엇을 보고 얼마나 기다릴지</b>도 함께 정해야 합니다. '지표가 괜찮으면' 은 기준이 아니고, '5% 트래픽에서 30분간 오류율 +0.1%p 이내' 가 기준입니다.\n⚠️ 롤백 경로는 <b>평소에 연습</b>해 두어야 합니다. 되돌릴 수 있다고 적어 두기만 하고 한 번도 해 보지 않았다면, 그것은 계획이지 능력이 아닙니다." },
];
