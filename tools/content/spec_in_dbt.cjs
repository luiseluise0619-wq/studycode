module.exports = {
  track: "dbt", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 트랜잭션과 인덱스의 약속",
  source: "./in_dbt.cjs",
  lessons: [
    { t: "트랜잭션이 지켜 주는 것", n: 8,
      th: { sum: "ACID 는 네 개의 약속이고, 격리성만 **수준을 골라 성능과 맞바꾼다**. 어느 수준을 쓰는지 모르고 짠 코드가 가장 위험하다.",
        body: [
          { h: "네 가지 약속", t: "<b>원자성</b>은 중간 상태를 남기지 않겠다는 약속이라 부분 성공을 다루는 코드를 안 써도 되게 하고, <b>격리성</b>은 동시에 돌아도 하나씩 한 것과 같게 해 주며, <b>지속성</b>은 커밋했다고 답했으면 전원이 나가도 남는다는 약속이다 — 이것을 실현하는 것이 <b>WAL</b>(변경을 데이터 파일보다 먼저 기록에 남기기)이다. 여러 서비스에 걸친 작업에는 원자성이 공짜로 오지 않아 사가나 아웃박스로 직접 만들어야 한다." },
          { h: "수준을 고른다는 것", t: "수준을 낮출수록 더티 리드·반복 불가능 읽기·팬텀 리드가 하나씩 허용된다. InnoDB 기본은 <b>REPEATABLE READ</b>, PostgreSQL 기본은 <b>READ COMMITTED</b> 라 DB 를 옮기면 조용한 버그가 생긴다. 가장 엄격한 <b>SERIALIZABLE</b> 은 안전한 대신 <b>트랜잭션이 실패할 수 있다</b>는 전제로 짜야 한다. 교착 상태는 최소 <b>두</b> 트랜잭션이 서로를 기다릴 때 생기며, 자원을 늘 같은 순서로 잠그면 예방된다." }],
        code: { c: "SELECT … FOR UPDATE          -- 읽고 판단한 뒤 쓴다면\nUNIQUE (email)                -- DB 가 마지막으로 막게 한다\n자원은 언제나 같은 순서로 잠근다", cap: "어느 수준 위에서 도는지 적어 둔다" },
        key: ["원자성은 중간 상태를 없앤다", "커밋은 WAL 이 디스크에 닿은 시점", "기본 격리 수준은 DB 마다 다르다", "데드락에는 재시도가 필요하다"] } },
    { t: "인덱스가 쓰이는 조건", n: 7,
      th: { sum: "인덱스는 **정렬된 사본**이다. 그 사실 하나에서 쓰이는 경우와 안 쓰이는 경우가 전부 따라 나온다.",
        body: [
          { h: "정렬돼 있으니까", t: "<b>B-tree</b> 는 값이 정렬된 채 유지되므로 같음 검색뿐 아니라 <b>범위 조회와 정렬</b>까지 인덱스만으로 처리한다 — 해시 인덱스가 순서를 못 주는 것과의 차이다. 복합 인덱스 <code>(a, b)</code> 는 a 로 먼저 정렬돼 있어 <b>b 만으로는 못 쓴다</b>(왼쪽 접두사 규칙). 필요한 컬럼이 인덱스 안에 다 있으면 본체를 안 읽는 <b>커버링 인덱스</b>가 되어 랜덤 I/O 가 사라진다." },
          { h: "안 쓰이거나 안 써야 할 때", t: "인덱스가 있는데 <b>풀 스캔</b>이 나온다면 컬럼에 함수를 씌웠거나, 타입이 안 맞아 암묵적 형변환이 일어났거나, 조건이 너무 넓은 것이다 — 큰 부분을 어차피 읽어야 하면 풀 스캔이 더 빠르다. <b>N+1</b> 은 목록 1 번 + 행마다 1 번이라 개발용 열 건에서는 안 보인다. 정규화는 <b>중복</b>을 없애 갱신 이상을 막고, 읽기가 압도적이면 되돌리되 원본과 재동기 시점을 정해 둔다." }],
        code: { c: "WHERE DATE(created_at) = ?   -- 인덱스를 못 쓴다\nWHERE created_at >= ? AND < ? -- 이렇게 적는다\n(a, b) 인덱스는 a 없이 못 쓴다", cap: "정렬된 사본이라는 사실에서 다 나온다" },
        key: ["B-tree 는 범위와 정렬까지 준다", "복합 인덱스는 왼쪽부터", "컬럼에 함수를 씌우면 못 쓴다", "N+1 은 쿼리 개수를 세어 잡는다"] } },
  ],
};
