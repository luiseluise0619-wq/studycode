/* Git/GitHub 트랙 전반부 — 유닛 1~6.
   문항은 쉬운 것부터 어려운 것 순으로 배치한다(계단식).
   _p 필드가 붙은 문항은 주장을 실제 git 으로 실행해 확인한다(주입 전에 제거된다). */

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex,p)=>{ const o={t:"input",k,q,a,ex}; if(p) o._p=p; return o; };

/* _p: {sh:"임시 저장소에서 돌릴 셸 스크립트", want:"마지막 줄 기대 출력"} */
const P=(sh,want)=>({sh,want});

const {shuffleUnits}=require("./shuffle_choice.cjs");

const UNITS=[

/* ═══════════════ 유닛 1 ═══════════════ */
{ t:"Git 첫걸음 — 무엇을 저장하는가", l:[

  { t:"버전 관리가 없으면 무슨 일이 벌어지나", xp:15,
    th:{ sum:"Git 은 **되돌릴 수 있게 만드는 도구**다. 실수를 없애 주는 것이 아니라, 실수해도 **안전하게 되돌아갈 자리**를 만들어 준다.",
      body:[
        {h:"최종_최종_진짜최종.zip 의 문제", t:"파일을 복사해 두는 방식은 세 가지를 못 한다. **누가 왜 바꿨는지** 알 수 없고, 두 사람이 같은 파일을 고쳤을 때 **합칠 방법**이 없고, '어제는 됐는데' 를 **언제부터 깨졌는지**로 좁힐 수 없다. Git 은 이 셋을 각각 커밋 메시지·병합·이분 탐색으로 푼다."},
        {h:"바꾼 것이 아니라 전체를 저장한다", t:"많은 사람이 Git 이 **변경분(diff)** 을 쌓는다고 생각하지만, Git 은 커밋할 때마다 **그 순간 프로젝트 전체의 모습(스냅숏)** 을 저장한다. 바뀌지 않은 파일은 이전 것을 **가리키기만** 하므로 용량이 폭발하지 않는다. 이 모델 덕분에 아무 커밋으로나 즉시 되돌아갈 수 있고, 브랜치를 만드는 데 비용이 거의 들지 않는다."}],
      code:{ c:"git init          # 이 폴더를 Git 저장소로 만든다\ngit add .         # 저장할 것을 고른다\ngit commit -m \"첫 커밋\"   # 지금 모습을 통째로 남긴다", cap:"세 줄이면 되돌아갈 자리가 생긴다" },
      key:["Git 은 되돌아갈 자리를 만드는 도구","커밋은 diff 가 아니라 그 순간의 스냅숏","안 바뀐 파일은 이전 것을 가리키기만 한다","브랜치가 싸다는 성질이 여기서 나온다"] },
    q:[
      I("첫 커밋 남기기","스테이지에 올린 것을 메시지와 함께 커밋하는 명령은? (git 과 -m 까지, 메시지는 제외)",
        ["git commit -m","git commit"],
        "정답은 `git commit -m \"메시지\"` 입니다. `-m` 없이 치면 편집기가 열려 거기서 메시지를 씁니다.\n편집기에서 아무것도 쓰지 않고 저장하면 커밋이 취소됩니다 — 실수로 빈 메시지가 남지 않도록 Git 이 막아 주는 것입니다."),
      C("목적","Git 을 쓰는 가장 근본적인 이유는?",
        ["저장소가 쓰는 파일 용량을 줄이려고","되돌아갈 수 있게","코드의 잘못된 부분을 자동으로 찾아 고쳐 주려고","인터넷에 백업하려고"],1,
        "Git 의 핵심은 **되돌아갈 수 있음**입니다. 그래서 과감하게 고쳐 볼 수 있습니다.\n백업은 결과이지 목적이 아닙니다 — 원격 저장소 없이 내 컴퓨터에서만 써도 Git 은 충분히 제 역할을 합니다.\n용량은 오히려 늘어납니다. 모든 과거를 들고 다니기 때문입니다."),
      C("저장 방식","Git 이 커밋할 때 실제로 저장하는 것은?",
        ["직전 커밋에서 달라진 줄만 골라 낸 차이(diff)","그 순간 프로젝트 전체의 모습(스냅숏)","바뀐 줄의 번호 목록","파일 이름의 목록"],1,
        "Git 은 커밋마다 **그 순간의 전체 모습**을 저장합니다. 화면에 보이는 diff 는 **두 스냅숏을 비교해 그때그때 계산한 것**입니다.\n그렇다고 파일이 복사되지는 않습니다. 내용이 같은 파일은 **같은 객체를 가리키므로** 실제로는 한 벌만 저장됩니다.\n이 모델이 '아무 커밋으로나 즉시 이동' 과 '브랜치가 거의 공짜' 를 동시에 가능하게 합니다."),
      I("시작","현재 폴더를 Git 저장소로 만드는 명령은? (git 포함)",
        ["git init","git init ."],
        "정답은 `git init` 입니다. `.git` 이라는 숨김 폴더가 생기고 그 안에 모든 역사가 들어갑니다.\n실수로 홈 디렉터리에서 `git init` 을 하면 컴퓨터 전체가 저장소가 되어 곤란해집니다. 그때는 그 `.git` 폴더를 지우면 원상복구됩니다."),
      C("역사의 위치","<code>.git</code> 폴더를 통째로 지우면 어떻게 되는가?",
        ["파일이 전부 사라진다","작업 파일은 남고 역사만 사라진다","시스템 오류가 난다","원격 저장소에서 알아서 자동으로 복구된다"],1,
        "`.git` 안에 **역사만** 들어 있습니다. 지금 보이는 파일은 그대로 남고 그냥 평범한 폴더가 됩니다.\n반대로 말하면 `.git` 하나만 있으면 모든 과거를 복원할 수 있다는 뜻이라, 백업 대상이 정확히 이 폴더입니다.\n원격에 푸시해 두었다면 다시 클론하면 되지만, 푸시하지 않은 커밋은 함께 사라집니다."),
      C("분산", "Git 이 '분산' 버전 관리라고 불리는 이유는?",
        ["여러 서버로 자동 복제되기 때문","각자의 컴퓨터가 전체 역사를 가진 완전한 저장소이기 때문",
         "여러 사람이 같은 파일을 동시에 편집하도록 잠금을 걸어 주기 때문","파일을 여러 조각으로 나눠 서버마다 분산 저장하기 때문"],1,
        "클론하면 **전체 역사가 통째로** 내 컴퓨터에 복사됩니다. 그래서 인터넷이 끊겨도 커밋·브랜치·로그 조회가 전부 됩니다.\n중앙 집중식(SVN 등)에서는 커밋조차 서버에 연결되어야 했습니다. 이 차이가 Git 이 빠르게 느껴지는 진짜 이유입니다.\n'GitHub 가 없으면 Git 을 못 쓴다' 는 오해도 여기서 풀립니다 — GitHub 는 **여러 저장소가 만나는 장소**일 뿐입니다."),
      C("커밋의 단위","좋은 커밋 하나의 크기로 가장 적절한 것은?",
        ["그날 고친 파일 전부를 하루가 끝날 때 한꺼번에 담아 둔 분량","통째로 되돌려도 말이 되는 한 덩어리",
         "고친 파일 하나 전부","고친 줄 하나하나를 따로"],1,
        "커밋의 기준은 크기가 아니라 **되돌릴 수 있는 단위**입니다. '로그인 버그 수정' 은 파일 다섯 개를 건드려도 하나의 커밋입니다.\n'로그인 버그 수정 + 오타 정정 + 라이브러리 업그레이드' 를 한 커밋에 담으면, 나중에 라이브러리만 되돌리고 싶을 때 방법이 없습니다.\n이 기준이 있으면 커밋 메시지도 저절로 한 문장으로 써집니다."),
      C("적용 범위","Git 으로 관리하기에 <b>적절하지 않은</b> 것은?",
        ["팀이 함께 고치는 소스 코드 파일","값을 비워 둔 설정 파일 템플릿","수백 MB 짜리 동영상 원본","프로젝트 설명을 적은 Markdown 문서"],2,
        "Git 은 **텍스트의 변화**를 다루도록 설계되었습니다. 큰 바이너리는 조금만 바뀌어도 통째로 새로 저장되어 저장소가 급격히 무거워집니다.\n한 번 커밋하면 역사에 영원히 남으므로, 지운다고 용량이 돌아오지 않는 것이 더 큰 문제입니다.\n큰 파일은 Git LFS 나 오브젝트 스토리지에 두고, 저장소에는 **주소만** 남기는 것이 정석입니다.")
    ] },

  { t:"세 개의 공간 — 작업 폴더 · 스테이지 · 저장소", xp:15,
    th:{ sum:"Git 이 헷갈리는 이유의 절반은 **파일이 있을 수 있는 자리가 세 곳**이라는 것을 모르기 때문이다. 이 그림 하나면 대부분의 명령이 제자리를 찾는다.",
      body:[
        {h:"작업 폴더 → 스테이지 → 저장소", t:"**작업 폴더**는 지금 편집기에 보이는 파일이다. `git add` 하면 **스테이지(index)** 라는 대기 장소로 올라가고, `git commit` 하면 **저장소**에 영구히 남는다. 스테이지가 따로 있는 이유는 '고친 것 중 **일부만** 이번 커밋에 담고 싶다' 는 요구 때문이다 — `git add -p` 로 한 파일 안에서 줄 단위로 고를 수도 있다."},
        {h:"status 가 알려 주는 것", t:"`git status` 의 출력은 이 세 공간의 **차이 보고서**다. '**Changes to be committed**' 은 스테이지에 있는 것, '**Changes not staged**' 는 작업 폴더에만 있는 것, '**Untracked**' 는 Git 이 아직 한 번도 본 적 없는 파일이다. 막혔을 때 `git status` 를 읽으면 대부분 다음에 칠 명령까지 친절히 적혀 있다."}],
      code:{ c:"작업 폴더  --git add-->  스테이지  --git commit-->  저장소\n    ^                                          |\n    +------------- git checkout / restore -----+", cap:"명령은 이 화살표에 하나씩 대응한다" },
      key:["파일이 있을 수 있는 자리는 세 곳","add 는 '이번 커밋에 담을 것' 을 고르는 일","status 는 세 공간의 차이 보고서","일부만 담고 싶어서 스테이지가 있다"] },
    q:[
      I("전부 담기","현재 폴더의 변경을 모두 스테이지에 올리는 짧은 명령은? (git add 로 시작)",
        ["git add .","git add -A","git add --all"],
        "정답은 `git add .` 입니다. 현재 폴더 아래 전부를 올립니다.\n편할수록 위험한 명령이기도 합니다 — 의도하지 않은 파일이 섞여 들어가므로, 친 뒤에는 반드시 `git status` 로 무엇이 담겼는지 확인하세요."),
      C("중간 단계","<code>git add</code> 가 하는 일은?",
        ["저장소에 영구히 기록한다","이번 커밋에 담을 내용을 스테이지에 올린다","파일을 새로 만든다","지정해 둔 원격 저장소로 파일을 그대로 올려 보낸다"],1,
        "`add` 는 **고르는 일**이지 저장하는 일이 아닙니다. 영구 저장은 `commit` 이 합니다.\n이름이 'add' 라 '추가' 로 읽히지만, 이미 추적 중인 파일에도 씁니다 — 정확한 뜻은 '**이 상태를 다음 커밋 후보로 올린다**' 입니다.\n그래서 `add` 한 뒤 파일을 또 고치면, 커밋에는 `add` 한 시점의 내용이 들어갑니다."),
      C("add 후 수정","파일을 <code>git add</code> 한 뒤 다시 고치고 바로 커밋하면 무엇이 커밋되는가?",
        ["나중에 고친 최신 내용","git add 했던 시점의 내용","두 버전이 각각 커밋된다","내용이 어긋났다는 오류가 나서 커밋이 중단된다"],1,
        "스테이지에는 `add` 한 **그 순간의 스냅숏**이 들어갑니다. 이후 수정은 작업 폴더에만 있습니다.\n그래서 `git status` 를 보면 같은 파일이 'staged' 와 'not staged' 양쪽에 동시에 나타납니다 — 처음 보면 당황하지만 정확한 표시입니다.\n최신 내용을 담으려면 `git add` 를 다시 하면 됩니다.",
        P("echo one > a.txt && git add a.txt && echo two > a.txt && git commit -qm c && git show HEAD:a.txt","one")),
      I("상태 확인","지금 무엇이 스테이지에 있고 무엇이 아닌지 보는 명령은? (git 포함)",
        ["git status 로 지금 상태를 확인한다","git status -s","git status --short"],
        "정답은 `git status` 입니다. Git 에서 가장 많이 치게 되는 명령이고, **막혔을 때 가장 먼저 칠 명령**이기도 합니다.\n출력에 다음에 쓸 명령이 그대로 적혀 있는 경우가 많아, 읽는 습관을 들이면 외울 명령이 크게 줄어듭니다."),
      C("Untracked","<code>Untracked files</code> 로 표시되는 파일은?",
        ["고치기는 했지만 아직 add 하지 않아 스테이지에 없는 파일","추적된 적 없는 새 파일","삭제된 파일","충돌 표식이 남은 파일"],1,
        "**한 번도 커밋에 들어간 적이 없는** 파일입니다. Git 은 이 파일의 변화를 감시하지 않습니다.\n그래서 `git commit -a`(추적 중인 파일을 자동 add) 로도 잡히지 않습니다 — 새 파일은 반드시 명시적으로 `add` 해야 합니다.\n영영 추적하고 싶지 않다면 `.gitignore` 에 적어 목록에서 치웁니다."),
      C("일부만 담기","한 파일 안에서 <b>고친 줄 중 일부만</b> 이번 커밋에 담고 싶다면?",
        ["불가능하다","git add -p 로 고른다","파일을 두 개로 나눈다","git commit --partial 옵션을 붙여 커밋한다"],1,
        "`git add -p`(patch 모드)는 변경을 조각(hunk)으로 나눠 하나씩 **담을지 물어봅니다**.\n버그 수정과 리팩터링이 한 파일에 섞였을 때, 커밋을 목적별로 갈라 놓는 가장 실용적인 도구입니다.\n조각을 더 잘게 쪼개려면 `s`, 직접 편집하려면 `e` 를 누릅니다."),
      C("commit -a","<code>git commit -am \"메시지\"</code> 가 하지 <b>못하는</b> 일은?",
        ["수정된 파일을 자동으로 스테이지에 올리기","삭제된 파일을 반영하기","새로 만든 파일을 포함하기","메시지 함께 지정하기"],2,
        "`-a` 는 **이미 추적 중인** 파일의 수정·삭제만 자동으로 담습니다. 새 파일(untracked)은 포함하지 않습니다.\n이 때문에 '분명 커밋했는데 파일이 없다' 는 사고가 자주 납니다. 커밋 전에 `git status` 로 Untracked 가 남았는지 확인하는 습관이 예방책입니다."),
      C("스테이지의 실체","스테이지(index)는 실제로 무엇인가?",
        ["프로그램이 도는 동안 메모리에만 있는 임시 목록","<code>.git/index</code> 라는 파일","작업 폴더의 숨김 복사본","원격 서버의 대기열"],1,
        "스테이지는 `.git/index` 라는 **실제 파일**입니다. 파일 경로와 그 내용의 해시를 담은 목록입니다.\n그래서 컴퓨터를 껐다 켜도 `add` 한 상태가 유지됩니다.\n이 파일이 손상되면 `git status` 가 이상해지는데, `.git/index` 를 지우고 `git reset` 하면 다시 만들어집니다 — 커밋된 역사는 안전합니다.")
    ] }
]},

/* ═══════════════ 유닛 2 ═══════════════ */
{ t:"기본 흐름 — 기록하고 읽는다", l:[

  { t:"커밋 메시지 — 미래의 나에게 쓰는 편지", xp:20,
    th:{ sum:"커밋 메시지는 형식이 아니라 **정보**다. 6개월 뒤 이 줄만 보고 '왜 이렇게 바꿨는지' 를 알 수 있어야 한다.",
      body:[
        {h:"무엇이 아니라 왜", t:"코드를 보면 **무엇이** 바뀌었는지는 알 수 있다. 알 수 없는 것은 **왜** 바꿨는지다. '`timeout` 을 3초에서 10초로' 는 diff 를 보면 안다. 정작 필요한 것은 '결제 게이트웨이가 피크에 5초까지 걸려 타임아웃이 났다' 는 이유다. 첫 줄에 요약, 빈 줄, 그 다음에 이유 — 이 구조가 널리 쓰이는 이유가 여기 있다."},
        {h:"첫 줄은 50자, 명령형으로", t:"첫 줄은 목록·알림·이메일 제목에 그대로 쓰이므로 **짧아야** 한다. 관례상 명령형(`Add`, `Fix`, `추가한다`)을 쓰는데, Git 자신이 만드는 메시지(`Merge branch …`, `Revert …`)와 어투를 맞추기 위해서다. 마침표를 찍지 않는 것도 제목이기 때문이다."}],
      code:{ c:"결제 타임아웃을 10초로 늘린다\n\n피크 시간대에 게이트웨이 응답이 5초를 넘겨\n3초 타임아웃에 걸려 결제가 실패했다.\n\nCloses #142", cap:"제목 · 빈 줄 · 이유" },
      key:["무엇이 아니라 왜를 적는다","첫 줄은 짧게, 마침표 없이","제목과 본문 사이에 빈 줄","이슈 번호로 맥락을 잇는다"] },
    q:[
      I("변경 통계","각 커밋이 몇 줄을 더하고 지웠는지 요약해 보는 옵션은? (git log 뒤에 오는 옵션만)",
        ["--stat","-stat"],
        "정답은 `--stat` 입니다. `git log --stat` 으로 커밋마다 파일별 증감이 나옵니다.\n'이 커밋이 얼마나 큰가' 를 빠르게 가늠할 때 씁니다. 400줄이 넘는 커밋이 자주 보인다면 커밋 단위를 다시 생각해 볼 신호입니다."),
      C("좋은 메시지","다음 중 커밋 메시지로 가장 좋은 것은?",
        ["수정 (필요한 부분을 손봄)","버그 픽스 (동작하지 않던 부분을 찾아내 원래대로 고쳐 둠)","asdf (나중에 고쳐 쓰려고 임시로 남긴 메모)","결제 타임아웃을 10초로 늘린다 (피크에 게이트웨이가 5초 초과)"],3,
        "**무엇을 왜 바꿨는지**가 들어 있어야 합니다. '수정'·'버그 픽스' 는 커밋이 있다는 사실 외에 아무것도 알려 주지 않습니다.\n6개월 뒤 `git log --oneline` 을 훑을 때, 제목만으로 원하는 커밋을 찾을 수 있어야 좋은 메시지입니다.\n그것이 곧 `git bisect`·`git blame` 을 쓸모 있게 만드는 조건이기도 합니다."),
      C("첫 줄 규칙","커밋 메시지 첫 줄의 관례로 옳지 <b>않은</b> 것은?",
        ["50자 내외로 짧게","마침표를 찍지 않는다","본문과는 빈 줄 하나로 구분한다","반드시 영어로 쓴다"],3,
        "언어는 팀이 정하는 것이지 Git 의 규칙이 아닙니다. 한국어 팀이라면 한국어가 더 정확합니다.\n나머지 셋은 도구가 제목처럼 취급하기 때문에 생긴 실용적 관례입니다 — 빈 줄이 없으면 `git log --oneline` 이 본문까지 제목으로 붙여 버립니다."),
      I("한 줄 로그","커밋 역사를 한 줄씩 간단히 보는 명령은? (git log 로 시작)",
        ["git log --oneline","git log --oneline --graph","git log --pretty=oneline"],
        "정답은 `git log --oneline` 입니다. 짧은 해시와 제목만 보여 줍니다.\n`--graph --all` 을 붙이면 브랜치가 갈라지고 합쳐진 모양까지 그림으로 보여 주어, 지금 저장소가 어떤 상태인지 한눈에 파악됩니다."),
      C("직전 커밋 고치기","방금 한 커밋의 메시지에 오타가 있다. 아직 푸시하지 않았다면?",
        ["git commit --amend","git revert HEAD","git reset --hard 로 작업 폴더까지 통째로 되돌리기","되돌릴 수 없다"],0,
        "`git commit --amend` 는 **직전 커밋을 새로 만들어 갈아 끼웁니다**. 메시지 수정과 파일 추가 모두 됩니다.\n주의할 점은 커밋 해시가 **바뀐다**는 것입니다. 이미 푸시한 커밋에 쓰면 남의 역사와 어긋나므로, **푸시 전에만** 쓰는 것이 원칙입니다.\n메시지만 고칠 때는 `git commit --amend -m \"새 메시지\"` 로 편집기를 건너뛸 수 있습니다."),
      C("변경 내용 보기","<b>아직 add 하지 않은</b> 수정 내용만 보는 명령은?",
        ["git diff 로 변경 비교","git diff --staged","git show","git log -p 로 커밋별 변경 내용 보기"],0,
        "`git diff` 는 **작업 폴더와 스테이지의 차이**, 즉 아직 `add` 하지 않은 변경을 보여 줍니다.\n`git diff --staged`(= `--cached`)는 반대로 **스테이지와 직전 커밋의 차이**, 즉 이번에 커밋될 내용을 보여 줍니다.\n커밋 직전에 이 둘을 각각 확인하는 것이 실수를 막는 가장 값싼 습관입니다."),
      C("누가 이 줄을","특정 줄을 누가 언제 왜 바꿨는지 찾는 명령은?",
        ["git blame 으로 작성자 추적","git log --author 로 작성자별 커밋 찾기","git shortlog","git whoami"],0,
        "`git blame 파일` 은 각 줄 옆에 **마지막으로 그 줄을 바꾼 커밋과 사람**을 보여 줍니다.\n이름이 험악하지만 실제 용도는 책임 추궁이 아니라 **맥락 찾기**입니다 — 해시를 얻어 `git show` 로 그 커밋의 메시지를 읽는 것이 진짜 목적입니다.\n그래서 커밋 메시지가 부실하면 `blame` 도 쓸모가 없어집니다."),
      C("빈 커밋","<code>git commit</code> 을 쳤는데 <code>nothing to commit</code> 이 나온다면?",
        ["Git 설치가 고장 나 명령이 잘못 도는 것이다","스테이지에 올린 것이 없다","커밋 메시지가 비었다","원격 연결이 끊겼다"],1,
        "커밋할 것은 **스테이지에 있는 것**뿐입니다. 파일을 고쳤어도 `add` 하지 않았다면 커밋할 것이 없습니다.\n메시지에 이어지는 안내에 `git add` 를 쓰라는 문장이 함께 나옵니다 — Git 의 오류 메시지는 대부분 다음 명령을 알려 줍니다.\n의도적으로 빈 커밋이 필요하면(CI 재실행 등) `--allow-empty` 를 붙입니다.")
    ] },

  { t:"무시하기와 추적 해제 — .gitignore", xp:20,
    th:{ sum:"저장소에 **들어가면 안 되는 것**이 있다. 비밀번호, 빌드 결과물, 남의 컴퓨터에만 맞는 설정. 한 번 들어가면 지워도 역사에 남는다.",
      body:[
        {h:"이미 추적 중이면 무시되지 않는다", t:"`.gitignore` 는 **아직 추적하지 않는 파일**에만 적용된다. 이미 커밋된 파일은 나중에 `.gitignore` 에 적어도 계속 추적된다. 이때는 `git rm --cached 파일` 로 **추적만 해제**하고 커밋해야 한다 — `--cached` 를 빼면 실제 파일까지 지워지므로 주의한다."},
        {h:"비밀은 역사에서 지워지지 않는다", t:"API 키를 커밋한 뒤 다음 커밋에서 지워도, **과거 커밋에는 그대로 남아 있다**. 저장소를 클론한 사람은 누구나 볼 수 있다. 유일하게 옳은 대응은 **그 키를 즉시 폐기하고 새로 발급**하는 것이다. 역사를 다시 쓰는 도구도 있지만, 이미 유출된 값은 되돌아오지 않는다."}],
      code:{ c:"# .gitignore\nnode_modules/\n.env\n*.log\ndist/\n.DS_Store\n\n!.env.example   # 예외: 이건 추적한다", cap:"! 로 예외를 만든다" },
      key:["이미 추적 중이면 gitignore 가 안 통한다","git rm --cached 로 추적만 해제","커밋된 비밀은 즉시 폐기·재발급","예시 설정 파일은 커밋해 둔다"] },
    q:[
      I("규칙 찾기","어떤 무시 규칙이 이 파일을 걸렀는지 알려 주는 명령은? (git 과 옵션까지, 파일명은 제외)",
        ["git check-ignore -v","git check-ignore"],
        "정답은 `git check-ignore -v 파일` 입니다. 어느 파일 몇 번째 줄의 규칙이 걸렸는지까지 알려 줍니다.\n`.gitignore` 가 여러 폴더에 흩어져 있거나 전역 설정이 섞였을 때, 추측 대신 확인하는 유일한 방법입니다."),
      C("무시가 안 될 때","<code>.gitignore</code> 에 적었는데도 파일이 계속 추적된다면 이유는?",
        [".gitignore 파일을 둔 위치가 잘못됐다","그 파일이 이미 커밋된 적이 있다","패턴 문법이 틀렸다","Git 버전이 낮다"],1,
        "`.gitignore` 는 **추적 시작을 막는** 장치입니다. 이미 추적 중인 파일에는 아무 영향이 없습니다.\n해결은 `git rm --cached 파일` 로 추적을 끊고 커밋하는 것입니다. 이때 `--cached` 를 빠뜨리면 디스크의 파일까지 지워집니다.\n물론 위치나 문법이 틀렸을 수도 있으니, `git check-ignore -v 파일` 로 어떤 규칙이 걸렸는지 확인할 수 있습니다."),
      C("추적만 해제","파일은 그대로 두고 Git 추적에서만 빼는 명령은?",
        ["git rm 파일","git rm --cached 파일","git clean 파일 로 추적 안 된 것 지우기","git reset 파일 로 인덱스를 되돌리기"],1,
        "`--cached` 는 **스테이지(index)에서만** 지웁니다. 디스크의 파일은 남습니다.\n`--cached` 없이 `git rm` 하면 파일이 실제로 삭제되고 그 삭제가 커밋됩니다.\n실수로 `node_modules` 를 커밋했을 때 쓰는 표준 처방입니다.",
        P("echo x > keep.txt && git add keep.txt && git commit -qm c && git rm -q --cached keep.txt && ls keep.txt","keep.txt")),
      C("유출 대응","API 키를 실수로 커밋해 GitHub 에 푸시했다. 가장 먼저 할 일은?",
        ["다음 커밋에서 지운다","그 키를 폐기하고 새로 발급한다","저장소를 비공개로 바꾼다","README 에 쓰지 말라는 경고 문구를 적어 둔다"],1,
        "**폐기가 최우선**입니다. 푸시된 순간 이미 봇이 훑어 갔다고 가정해야 합니다.\n다음 커밋에서 지워도 **과거 커밋에는 그대로** 남아 클론한 사람 누구나 볼 수 있습니다. 비공개로 바꿔도 이미 받아 간 사본은 되돌릴 수 없습니다.\n키를 갈아 끼운 뒤에 역사 정리(`filter-repo` 등)를 하는 것이 순서입니다."),
      C("예시 파일","<code>.env</code> 를 무시하면서도 팀원이 필요한 설정 키를 알게 하려면?",
        ["README 문서에 필요한 값을 전부 적어 두고 공유한다","빈 .env.example 커밋","무시하지 않는다","각자 알아서 채우게 둔다"],1,
        "값 없이 **키 이름만** 담은 `.env.example` 을 커밋하는 것이 표준입니다.\n새로 합류한 사람이 `cp .env.example .env` 한 뒤 값만 채우면 되므로, 설정 항목이 늘어도 문서가 저절로 최신이 됩니다.\n`.gitignore` 에서 `!.env.example` 로 예외를 명시하면 실수로 무시되는 것도 막을 수 있습니다."),
      I("무시 파일 이름","Git 이 무시할 파일 패턴을 적는 파일의 이름은? (점 포함)",
        [".gitignore"],
        "정답은 `.gitignore` 입니다. 폴더마다 둘 수 있고, 하위 폴더의 것이 우선합니다.\n내 컴퓨터에만 해당하는 무시 규칙(편집기 설정 등)은 저장소가 아니라 `~/.gitignore_global` 에 두는 것이 예의입니다 — 팀의 `.gitignore` 를 내 개인 사정으로 어지럽히지 않습니다."),
      C("패턴","<code>*.log</code> 는 어떤 파일을 무시하는가?",
        ["이름이 log 로 시작하는 모든 파일","확장자가 log 인 모든 파일","log 라는 이름을 가진 폴더 전체","로그를 남기도록 따로 만들어진 프로그램 파일"],1,
        "`*` 는 아무 문자열이나 대응하므로 `error.log`·`access.log` 가 모두 걸립니다.\n폴더를 통째로 무시하려면 `logs/` 처럼 슬래시를 붙입니다. 슬래시가 없으면 같은 이름의 **파일도** 함께 걸립니다.\n맨 앞 슬래시(`/dist`)는 '저장소 루트의 그것만' 이라는 뜻입니다."),
      C("추적된 파일 목록","현재 Git 이 추적 중인 파일 목록을 보려면?",
        ["ls -la","git ls-files","git status 로 지금 상태를 확인한다","git log --name-only"],1,
        "`git ls-files` 는 인덱스에 등록된 파일, 즉 **추적 중인 파일 전부**를 나열합니다.\n'이게 왜 저장소에 들어가 있지?' 를 확인할 때 가장 빠른 방법이고, `git ls-files | xargs wc -l` 처럼 이어 붙여 규모를 재기도 합니다.\n`git status` 는 **변화**를 보여 줄 뿐 전체 목록은 보여 주지 않습니다.")
    ] }
]},

/* ═══════════════ 유닛 3 ═══════════════ */
{ t:"되돌리기 3형제 — restore · reset · revert", l:[

  { t:"무엇을 되돌리려는지부터 정한다", xp:25,
    th:{ sum:"Git 에서 가장 많이 사고가 나는 곳이다. 명령을 외우기 전에 **어느 공간의 무엇을 되돌리려는지**를 먼저 정해야 한다.",
      body:[
        {h:"세 가지 상황, 세 가지 명령", t:"① **아직 커밋 안 한 수정을 버린다** → `git restore 파일`. ② **add 한 것을 스테이지에서 내린다** → `git restore --staged 파일`. ③ **이미 한 커밋을 없던 일로 한다** → `git reset`(역사를 지움) 또는 `git revert`(취소하는 새 커밋을 쌓음). 상황을 먼저 말로 정하면 명령은 하나로 좁혀진다."},
        {h:"reset 과 revert 의 결정적 차이", t:"`reset` 은 **역사를 뒤로 옮긴다** — 없던 일처럼 만든다. `revert` 는 **반대 내용의 새 커밋을 앞에 쌓는다** — 되돌렸다는 사실까지 기록에 남는다. 이미 남과 공유한(푸시한) 커밋에는 반드시 **revert** 를 쓴다. 공유된 역사를 reset 으로 지우면, 그 커밋 위에서 작업하던 동료의 저장소가 어긋난다."}],
      code:{ c:"git restore 파일            # 수정 버리기 (되돌릴 수 없다)\ngit restore --staged 파일   # add 취소\ngit reset --soft HEAD~1     # 커밋만 취소, 내용은 스테이지에 남김\ngit revert HEAD             # 취소 커밋을 새로 쌓음 (공유된 역사에 안전)", cap:"위에서 아래로 갈수록 영향 범위가 넓다" },
      key:["먼저 '어느 공간의 무엇' 인지 정한다","restore 는 파일, reset/revert 는 커밋","공유된 커밋은 revert","reset --hard 는 되돌릴 수 없다"] },
    q:[
      I("추적 안 된 파일 지우기","추적된 적 없는 파일까지 지우기 전에 <b>무엇이 지워질지 먼저 보는</b> 명령은? (git clean 과 옵션)",
        ["git clean -nd","git clean -n","git clean --dry-run"],
        "정답은 `git clean -nd` 입니다. `-n` 이 예행연습(dry run), `-d` 가 폴더까지 포함입니다.\n실제로 지우는 `-fd` 는 되돌릴 수 없으므로, 먼저 `-nd` 로 목록을 보는 것이 정석입니다."),
      C("수정 버리기","파일을 고쳤는데 마음에 안 들어 <b>마지막 커밋 상태로</b> 되돌리려면? (아직 add 안 함)",
        ["git restore 파일","git reset 파일 로 인덱스를 되돌리기","git revert 파일 로 취소 커밋 만들기","git clean 파일 로 추적 안 된 것 지우기"],0,
        "`git restore 파일` 은 작업 폴더의 그 파일을 **마지막 커밋 내용으로 덮어씁니다**.\n덮어쓴 내용은 어디에도 남지 않아 **되돌릴 수 없습니다** — Git 이 한 번도 본 적 없는 변경이기 때문입니다. 아깝다면 먼저 `git stash` 로 치워 두세요.\n예전 문법은 `git checkout -- 파일` 이었고, 지금도 동작하지만 `restore` 가 의도가 분명합니다."),
      C("add 취소","실수로 <code>git add</code> 한 파일을 스테이지에서 내리려면?",
        ["git restore --staged 파일","git restore 파일","git rm 파일","git reset --hard 로 작업 폴더까지 통째로 되돌리기"],0,
        "`git restore --staged 파일` 은 스테이지에서만 내리고 **작업 폴더의 수정은 그대로 둡니다**.\n`git restore 파일`(--staged 없이)은 반대로 수정 자체를 버리므로, 이 둘을 헷갈리면 작업이 날아갑니다.\n예전 문법 `git reset HEAD 파일` 도 같은 일을 합니다 — `git status` 가 안내하는 문구가 Git 버전에 따라 달라 보이는 이유입니다."),
      C("공유된 커밋","이미 <b>푸시한</b> 커밋을 취소해야 한다. 옳은 방법은?",
        ["git reset --hard 로 커밋을 지운 뒤 원격에 강제 푸시","git revert 를 쓴다","메시지만 고친다","저장소를 새로 만든다"],1,
        "`revert` 는 **반대 내용의 새 커밋**을 쌓으므로 역사가 어긋나지 않습니다. 남이 이미 받아 간 커밋은 그대로 남습니다.\n`reset` + 강제 푸시는 동료의 저장소와 역사가 갈라져, 다음 `pull` 에서 충돌과 혼란을 만듭니다.\n'되돌렸다는 사실' 자체가 기록으로 남는 것도 장점입니다 — 나중에 왜 되돌렸는지 추적할 수 있습니다."),
      C("soft 와 hard","<code>git reset --soft HEAD~1</code> 을 실행하면?",
        ["커밋도 사라지고 고쳐 둔 파일 내용까지 함께 사라진다","커밋만 취소, 내용은 남는다",
         "브랜치가 삭제된다","원격까지 되돌아간다"],1,
        "`--soft` 는 **HEAD 만 뒤로 옮깁니다**. 파일도 스테이지도 건드리지 않아, 방금 커밋한 내용이 그대로 `add` 된 상태로 남습니다.\n커밋을 쪼개거나 메시지를 다시 쓰고 싶을 때 가장 안전한 선택입니다.\n`--mixed`(기본값)는 스테이지까지 비우고, `--hard` 는 작업 폴더까지 되돌려 **수정 내용을 삭제**합니다.",
        P("echo a>f && git add f && git commit -qm one && echo b>f && git add f && git commit -qm two && git reset -q --soft HEAD~1 && git diff --cached --name-only","f")),
      C("가장 위험한 것","다음 중 되돌릴 수 없어 가장 위험한 명령은?",
        ["git revert HEAD","git reset --soft HEAD~1","git reset --hard HEAD","git restore --staged 파일"],2,
        "`git reset --hard` 는 **커밋되지 않은 모든 수정을 삭제**합니다. 스테이지에도 작업 폴더에도 남지 않습니다.\n커밋된 것이라면 `git reflog` 로 되찾을 수 있지만, **한 번도 커밋한 적 없는 수정은 복구 수단이 전혀 없습니다**.\n습관적으로 치기 전에 `git stash` 로 치워 두면 언제든 되돌릴 수 있습니다."),
      I("취소 커밋","이미 공유된 커밋을 안전하게 되돌리는 명령은? (git 과 대상 없이 명령어만)",
        ["git revert","revert"],
        "정답은 `git revert` 입니다. 지정한 커밋의 **반대 변경**을 담은 새 커밋을 만듭니다.\n병합 커밋을 되돌릴 때는 어느 쪽 줄기를 남길지 `-m 1` 로 지정해야 하는데, 이 한 가지가 revert 에서 유일하게 까다로운 부분입니다."),
      C("추적 안 된 파일 정리","<code>git reset --hard</code> 로도 지워지지 <b>않는</b> 것은?",
        ["이미 추적 중이면서 고쳐 놓은 파일","add 로 스테이지에 올려 둔 변경","추적된 적 없는 새 파일","커밋"],2,
        "`reset` 은 **Git 이 아는 것**만 되돌립니다. 추적된 적 없는 파일(untracked)은 손대지 않습니다.\n그것까지 지우려면 `git clean -fd` 를 씁니다. 위험하므로 먼저 `git clean -nd`(n = 예행연습)로 **무엇이 지워질지 확인**하는 것이 정석입니다.\n빌드 결과물이 남아 '분명 되돌렸는데 여전히 이상하다' 고 느낄 때 대부분 이 문제입니다.")
    ] },

  { t:"stash — 지금 하던 것을 잠깐 치워 둔다", xp:25,
    th:{ sum:"'급한 버그 수정 요청이 왔는데 지금 하던 게 어중간하다.' 이때 커밋하기도 버리기도 애매한 것을 **잠시 보관**하는 도구가 stash 다.",
      body:[
        {h:"임시 보관함이지 저장소가 아니다", t:"`git stash` 는 작업 폴더와 스테이지의 변경을 치우고 **마지막 커밋 상태로 되돌린다**. 치운 것은 스택에 쌓여 `git stash pop` 으로 되돌아온다. 다만 stash 는 **브랜치에 속하지 않고 이름도 없다** — 며칠씩 쌓아 두면 무엇이 무엇인지 알 수 없게 되므로, 하루를 넘길 것 같으면 임시 브랜치에 커밋하는 편이 낫다."},
        {h:"pop 과 apply, 그리고 -u", t:"`pop` 은 꺼내면서 **목록에서 지우고**, `apply` 는 꺼내되 **남겨 둔다**. 여러 브랜치에 같은 변경을 시험해 볼 때는 `apply` 가 안전하다. 기본 `stash` 는 **추적 안 된 새 파일을 담지 않으므로**, 새로 만든 파일이 있으면 `-u` 를 붙여야 한다 — 이것을 몰라 '분명 stash 했는데 파일이 그대로' 라고 당황하는 일이 흔하다."}],
      code:{ c:"git stash -u            # 새 파일까지 함께 치운다\ngit stash list          # 무엇이 쌓여 있나\ngit stash pop           # 마지막 것을 꺼내고 목록에서 제거\ngit stash apply stash@{1}   # 특정 항목을 꺼내되 남겨 둔다", cap:"pop 은 꺼내며 지우고, apply 는 남긴다" },
      key:["커밋하기 애매한 것을 잠시 치운다","pop 은 지우고, apply 는 남긴다","새 파일은 -u 를 붙여야 담긴다","오래 둘 것은 임시 브랜치에 커밋"] },
    q:[
      I("전부 비우기","쌓아 둔 stash 를 한 번에 모두 지우는 명령은? (git stash 뒤 낱말까지)",
        ["git stash clear","clear"],
        "정답은 `git stash clear` 입니다. 확인을 묻지 않고 통째로 지웁니다.\n지운 stash 도 한동안은 도달 불가 커밋으로 남아 `git fsck --unreachable` 로 찾을 수 있지만, 기대할 것은 못 됩니다."),
      C("용도","<code>git stash</code> 를 쓰기에 가장 적절한 상황은?",
        ["작업한 것을 커밋으로 영구히 남겨 두고 싶을 때가 되었을 때","작업이 어중간한데 다른 브랜치로 급히 옮겨야 할 때",
         "원격에 올려 두고 싶을 때","직전 커밋의 메시지만 고쳐 쓰고 싶을 때"],1,
        "stash 는 **잠깐 치워 두는** 용도입니다. 브랜치를 옮기려는데 수정이 남아 있어 Git 이 막을 때 가장 많이 씁니다.\n영구 저장은 커밋이 합니다. stash 는 브랜치에 속하지 않아 나중에 찾기 어렵고, 실수로 `git stash clear` 하면 통째로 사라집니다.\n'오늘 안에 다시 꺼낼 것' 정도가 적당한 기준입니다."),
      C("새 파일","새로 만든 파일이 <code>git stash</code> 후에도 그대로 남아 있는 이유는?",
        ["오래된 Git 버전에서만 생기는 것으로 알려져 있는 버그다","기본 stash 는 새 파일을 안 담는다","파일 크기가 너무 커서 담기지 못해서","이미 커밋된 파일이라서"],1,
        "기본 `git stash` 는 **추적 중인 파일**만 치웁니다. 한 번도 `add` 한 적 없는 새 파일은 그대로 남습니다.\n`git stash -u`(--include-untracked)를 쓰면 함께 치워집니다. `.gitignore` 로 무시된 것까지 담으려면 `-a` 입니다.\n이걸 모르고 브랜치를 옮기면, 새 파일이 따라와서 두 브랜치에 뒤섞이는 혼란이 생깁니다."),
      C("pop vs apply","꺼낸 뒤에도 stash 목록에 <b>남겨 두는</b> 명령은?",
        ["git stash pop","git stash apply","git stash drop","git stash clear 로 목록 통째로 비우기"],1,
        "`apply` 는 **적용만** 하고 목록에 남깁니다. `pop` 은 적용 후 목록에서 지웁니다.\n같은 변경을 여러 브랜치에서 시험해 볼 때는 `apply` 가 안전합니다 — `pop` 후 충돌이 나면 목록에서는 이미 사라진 뒤라 당황하게 됩니다(실제로는 충돌 시 pop 이 삭제를 보류합니다).\n다 쓴 항목은 `git stash drop` 으로 정리합니다."),
      I("목록 보기","쌓아 둔 stash 목록을 보는 명령은? (git 포함)",
        ["git stash list"],
        "정답은 `git stash list` 입니다. `stash@{0}` 이 가장 최근 것입니다.\n항목마다 어느 브랜치에서 만들었는지가 함께 표시되므로, 여러 개가 쌓였을 때 단서가 됩니다. 내용을 미리 보려면 `git stash show -p stash@{0}` 을 씁니다."),
      C("메시지","stash 에 설명을 붙여 두는 방법은?",
        ["git stash -m \"설명\"","git stash --comment","불가능하다","git stash --name 으로 이름 붙이기"],0,
        "`git stash push -m \"설명\"`(짧게 `git stash -m`)으로 메시지를 붙일 수 있습니다.\n메시지가 없으면 `WIP on main: 1a2b3c 직전 커밋 제목` 처럼 자동 생성되는데, 여러 개가 쌓이면 구분이 되지 않습니다.\n스스로에게 남기는 메모라도 있으면 며칠 뒤의 자신이 고마워합니다."),
      C("stash 의 실체","stash 는 내부적으로 무엇으로 저장되는가?",
        ["텍스트 파일","커밋 객체","임시 폴더","메모리"],1,
        "stash 는 실제로 **커밋**입니다. 어떤 브랜치도 가리키지 않는 커밋을 만들어 `refs/stash` 에 매달아 둡니다.\n그래서 실수로 `git stash drop` 했더라도 `git fsck --unreachable` 로 그 커밋을 찾아 되살릴 수 있습니다.\nGit 의 거의 모든 기능이 결국 '커밋과 그것을 가리키는 이름' 으로 환원된다는 점을 보여 주는 좋은 예입니다."),
      C("한계","stash 를 오래 쌓아 두면 안 되는 이유로 가장 적절한 것은?",
        ["쌓아 둔 stash 가 저장소 용량을 점점 많이 차지하게 되어서","브랜치에 속하지 않아 맥락이 사라지고 찾기 어려워서",
         "일정 기간 뒤 삭제되어서","푸시할 때 원격으로 함께 올라가서"],1,
        "stash 는 **어느 브랜치에도 속하지 않고 이름도 없습니다**. 일주일 뒤에는 어떤 작업이었는지 알 수 없게 됩니다.\n또 `git stash` 는 원격에 푸시되지 않으므로, 컴퓨터가 망가지면 통째로 사라집니다.\n하루를 넘길 작업이라면 `git switch -c wip/기능이름` 으로 임시 브랜치를 만들어 커밋해 두는 편이 모든 면에서 낫습니다.")
    ] }
]},

/* ═══════════════ 유닛 4 ═══════════════ */
{ t:"브랜치 — 사실은 포인터 하나다", l:[

  { t:"브랜치가 싸다는 말의 진짜 뜻", xp:25,
    th:{ sum:"다른 버전 관리 도구에서 브랜치는 '폴더 통째로 복사' 였다. Git 에서 브랜치는 **커밋 하나를 가리키는 41바이트짜리 파일**이다.",
      body:[
        {h:"이름표를 옮길 뿐이다", t:"브랜치는 `.git/refs/heads/main` 같은 파일 안에 든 **커밋 해시 한 줄**이다. 커밋하면 그 줄이 새 해시로 바뀐다. `HEAD` 는 '지금 어느 브랜치에 있는가' 를 가리키는 또 하나의 포인터다. 그래서 브랜치를 만드는 데 드는 비용이 사실상 0이고, 하루에 열 개를 만들어도 아무 부담이 없다."},
        {h:"브랜치를 나누는 이유", t:"기술적 이유가 아니라 **사회적 이유**다. 완성되지 않은 코드를 남에게 보이지 않고, 여러 갈래 작업이 서로를 깨뜨리지 않게 하고, 리뷰의 단위를 만든다. 그래서 브랜치 하나는 **하나의 목적**을 가져야 한다 — '이 브랜치는 무엇을 하는가' 를 한 문장으로 못 쓰면 너무 커진 것이다."}],
      code:{ c:"git switch -c feature/login   # 만들고 그리로 이동\ngit switch main               # 돌아오기\ngit branch -d feature/login   # 병합된 브랜치 삭제\ngit branch -v                 # 브랜치와 각자의 최신 커밋", cap:"switch 는 이동, branch 는 관리" },
      key:["브랜치는 커밋을 가리키는 포인터","HEAD 는 지금 있는 브랜치를 가리킨다","만드는 비용이 사실상 0","브랜치 하나에 목적 하나"] },
    q:[
      I("브랜치 목록","원격 추적 브랜치까지 포함해 모든 브랜치를 보는 명령은? (git branch 와 옵션)",
        ["git branch -a","git branch --all"],
        "정답은 `git branch -a` 입니다. `-r` 은 원격 것만, 옵션 없이는 로컬만 보여 줍니다.\n`-v` 를 더하면 각 브랜치의 최신 커밋 제목까지 보여 주어, 오래 방치된 브랜치를 찾기 좋습니다."),
      C("실체","Git 브랜치의 실체는?",
        ["프로젝트 폴더 전체를 복사해 둔 사본","커밋 하나를 가리키는 포인터","커밋마다의 변경 내역을 쌓아 둔 목록","원격 서버에 따로 만들어지는 디렉터리"],1,
        "브랜치는 **커밋 해시 한 줄이 든 작은 파일**입니다. `.git/refs/heads/` 안을 열어 보면 확인할 수 있습니다.\n그래서 브랜치를 만들어도 파일이 복사되지 않고, 100개를 만들어도 저장소 크기가 거의 그대로입니다.\n이 성질이 'Git 에서는 일단 브랜치를 따고 시작하라' 는 문화를 만들었습니다."),
      C("HEAD","<code>HEAD</code> 가 가리키는 것은?",
        ["저장소에서 가장 최근에 만들어진 브랜치","현재 작업 중인 위치(대개 브랜치)","원격의 기본 브랜치","첫 번째 커밋"],1,
        "`HEAD` 는 '**지금 내가 어디에 있는가**' 입니다. 보통은 브랜치 이름을 가리키고, 그 브랜치가 다시 커밋을 가리킵니다.\n특정 커밋으로 직접 이동하면 브랜치를 거치지 않게 되는데, 이 상태를 **detached HEAD** 라고 부릅니다.\n`HEAD~1` 은 한 칸 앞, `HEAD~3` 은 세 칸 앞 커밋을 뜻해 명령에서 자주 쓰입니다."),
      I("만들며 이동","<code>feature/login</code> 브랜치를 만들고 동시에 그리로 이동하는 최신 명령은?",
        ["git switch -c feature/login","git checkout -b feature/login"],
        "정답은 `git switch -c feature/login` 입니다. 예전의 `git checkout -b` 와 같은 일을 합니다.\nGit 2.23 에서 `checkout` 이 하던 일이 너무 많아(브랜치 이동 + 파일 복원) `switch`(이동)와 `restore`(복원)로 갈라졌습니다. 새로 배운다면 갈라진 쪽을 쓰는 편이 헷갈리지 않습니다."),
      C("이동 실패","브랜치를 옮기려는데 <code>your local changes would be overwritten</code> 이 나온다면?",
        ["브랜치 참조가 손상됐다","옮기려는 곳과 충돌하는 수정이 남아 있다","그 브랜치를 읽을 권한이 없다","작업을 이어서 하려면 원격 연결이 반드시 필요하다"],1,
        "지금 고친 파일이 **옮겨 갈 브랜치에서도 다르게 생겼을 때** Git 이 덮어쓰기를 막는 것입니다. 데이터를 지키려는 안전장치입니다.\n방법은 셋입니다 — 커밋하거나, `git stash` 로 치우거나, 버릴 것이면 `git restore` 합니다.\n두 브랜치에서 그 파일이 같다면 수정을 들고 그대로 넘어갈 수 있어, 항상 막히는 것은 아닙니다."),
      C("detached HEAD","<code>git switch --detach 해시</code> 로 이동한 상태에서 커밋하면?",
        ["커밋할 수 없다는 에러가 난다","가리키는 이름이 없어 잃기 쉽다",
         "만들어진 커밋이 자동으로 main 브랜치의 가장 뒤에 이어 붙는다","새 브랜치가 만들어진다"],1,
        "커밋은 정상적으로 만들어지지만 **가리키는 이름이 없습니다**. 다른 곳으로 이동하면 찾을 길이 막막해집니다.\nGit 이 이동할 때 경고와 함께 해시를 알려 주고, `git reflog` 로도 되찾을 수 있습니다. 하지만 정리(gc) 후에는 사라집니다.\n계속 작업할 생각이면 즉시 `git switch -c 새이름` 으로 **이름을 붙여** 두는 것이 정답입니다."),
      C("삭제","<code>git branch -d</code> 가 거부되는 경우는?",
        ["이름을 줄여야 할 때","아직 어디에도 병합되지 않은 커밋이 있을 때","지우려는 브랜치가 원격 저장소에도 함께 올라가 있을 때","브랜치를 만든 지 얼마 안 되었을 때"],1,
        "`-d` 는 **안전 삭제**입니다. 병합되지 않은 작업이 사라질 상황이면 거부합니다.\n정말 버릴 것이면 `-D`(대문자)로 강제 삭제합니다. 실수로 지웠더라도 `git reflog` 에서 해시를 찾아 되살릴 수 있습니다.\n원격 브랜치까지 지우려면 `git push origin --delete 브랜치` 를 따로 해야 합니다."),
      C("이름 규칙","브랜치 이름으로 가장 좋은 것은?",
        ["test (시험 삼아 잠깐 만들어 본 브랜치)","new (새 작업이라는 뜻)","feature/login-oauth","브랜치1 (순서대로 번호를 붙인 이름)"],2,
        "`영역/무엇` 형태가 널리 쓰입니다. 목록에서 정렬되어 보이고, 도구들이 접두사로 필터링하기도 좋습니다.\n`test`·`new` 같은 이름은 일주일 뒤 자신도 무엇인지 모릅니다. 여럿이 쓰는 저장소에서는 더 심각합니다.\n이슈 번호를 넣는 관례(`fix/142-payment-timeout`)도 맥락을 잇는 좋은 방법입니다.")
    ] },

  { t:"브랜치 전략 — 팀이 합의하는 것", xp:25,
    th:{ sum:"어떤 전략이 옳은지는 **배포 주기**가 정한다. 하루에 열 번 배포하는 팀과 분기마다 배포하는 팀은 같은 전략을 쓸 수 없다.",
      body:[
        {h:"트렁크 기반과 Git Flow", t:"**트렁크 기반**은 `main` 하나를 두고 짧은 브랜치를 며칠 안에 합친다. 병합 충돌이 작고 배포가 잦은 팀에 맞는다. **Git Flow** 는 `develop`·`release`·`hotfix` 를 두어 버전을 관리한다. 설치형 소프트웨어처럼 **여러 버전을 동시에 지원**해야 할 때 값어치가 있다. 웹 서비스에 Git Flow 를 그대로 들여왔다가 브랜치 관리에 지치는 경우가 많다."},
        {h:"오래 사는 브랜치가 비용이다", t:"브랜치가 오래 살수록 `main` 과 멀어져 병합이 어려워진다. 이것을 **머지 지옥**이라 부른다. 해법은 브랜치를 짧게 유지하고, 미완성 기능은 **기능 플래그**로 감춰 일단 합치는 것이다. '완성될 때까지 안 합친다' 가 아니라 '**꺼 둔 채로 합친다**' 가 현대적인 답이다."}],
      code:{ c:"main ──●──●──●──●──●   (항상 배포 가능)\n        \\    /  \\    /\n         ●──●    ●──●    (짧게 살다 합쳐지는 브랜치)", cap:"짧게 갈라지고 자주 합친다" },
      key:["전략은 배포 주기가 정한다","오래 사는 브랜치가 비용","미완성은 기능 플래그로 감추고 합친다","main 은 언제나 배포 가능해야"] },
    q:[
      I("이전 브랜치로","바로 직전에 있던 브랜치로 돌아가는 짧은 표현은? (git switch 뒤 기호 하나)",
        ["git switch -","-"],
        "정답은 `git switch -` 입니다. 셸의 `cd -` 와 같은 발상입니다.\n기능 브랜치와 `main` 을 오갈 때 이름을 다시 치지 않아도 되어, 하루에도 수십 번 쓰게 됩니다."),
      C("트렁크 기반","트렁크 기반 개발의 핵심은?",
        ["main 에서만 일한다","짧게 사는 브랜치를 자주 main 에 합친다",
         "main 을 건드리지 않고 따로 관리한다","릴리스할 때마다 브랜치를 새로 만들어 그대로 남겨 둔다"],1,
        "브랜치를 만들지 않는 것이 아니라 **짧게 유지**하는 것이 핵심입니다. 보통 하루~며칠입니다.\n자주 합칠수록 각 병합의 크기가 작아 충돌이 줄고, 문제가 생겨도 원인 범위가 좁습니다.\n대신 `main` 이 항상 배포 가능해야 하므로 **자동 테스트**가 전제 조건입니다."),
      C("Git Flow 가 맞는 곳","Git Flow 가 상대적으로 잘 맞는 상황은?",
        ["하루에도 여러 번씩 배포를 반복하는 웹 서비스 운영 팀","1.x 와 2.x 를 동시에 지원해야 하는 설치형 제품",
         "혼자 만들고 혼자 배포하는 작은 개인 프로젝트","며칠 만에 버릴 프로토타입"],1,
        "Git Flow 의 `release`·`hotfix` 브랜치는 **여러 버전을 동시에 유지보수**할 때 값어치가 있습니다.\n웹 서비스는 대개 최신 버전 하나만 살아 있으므로 그 구조가 순수한 비용이 됩니다.\n'유명하니까' 가 아니라 '**우리가 몇 개의 버전을 동시에 지원하는가**' 로 판단해야 합니다."),
      C("머지 지옥","브랜치를 오래 유지할 때 생기는 가장 큰 문제는?",
        ["브랜치가 남아 저장소 용량이 늘어난다","멀어져 충돌이 커진다",
         "시간이 지날수록 브랜치 이름이 점점 길어진다","오래 두면 브랜치끼리 커밋 해시가 겹쳐 중복되기 시작한다"],1,
        "떨어져 있는 시간에 비례해 **양쪽이 각자 변합니다**. 나중에 합칠 때 충돌 범위가 넓어지고, 해결하다 새 버그가 생깁니다.\n같은 파일을 서로 다르게 리팩터링해 놓았다면 자동 병합으로는 손쓸 수 없습니다.\n주기적으로 `main` 을 브랜치로 끌어와 **거리를 좁혀 두는 것**이 최소한의 방어입니다."),
      C("기능 플래그","완성되지 않은 기능을 <code>main</code> 에 합치면서도 사용자에게 보이지 않게 하려면?",
        ["완성될 때까지 해당 코드를 주석 처리해 둔다","기능 플래그로 꺼 둔 채 배포한다","별도 브랜치에 둔다","배포를 미룬다"],1,
        "설정값이나 조건문으로 **기능을 꺼 둔 채 코드만 합치는** 방식입니다. 브랜치를 짧게 유지하면서도 미완성 기능을 감출 수 있습니다.\n특정 사용자에게만 켜서 시험하거나(카나리), 문제가 생기면 배포 없이 즉시 끌 수 있다는 큰 장점이 따라옵니다.\n대신 플래그가 쌓이면 그 자체가 복잡도이므로, **다 켜진 플래그는 지우는** 규율이 필요합니다."),
      C("main 보호","<code>main</code> 브랜치에 직접 푸시를 막는 이유는?",
        ["Git 공식 문서가 반드시 그렇게 하라고 강하게 권장하고 있어서","리뷰·테스트를 강제하려고",
         "저장소 용량을 아끼기 위해서","저장소에 남아 쌓이는 브랜치 수를 줄이려고"],1,
        "**리뷰와 자동 테스트를 강제하는 장치**입니다. 사람의 규율이 아니라 도구로 막는 것이 안정적입니다.\nGitHub 의 브랜치 보호 규칙으로 '승인 1명 이상', 'CI 통과' 를 필수로 걸 수 있습니다.\n급할 때 예외를 만들고 싶어지는데, 그 예외가 사고를 부르는 것이 대부분의 장애 후기의 결론입니다."),
      I("기본 브랜치","GitHub 가 2020년부터 새 저장소의 기본 브랜치로 쓰는 이름은?",
        ["main"],
        "정답은 `main` 입니다. 그 전에는 `master` 였습니다.\n`git config --global init.defaultBranch main` 으로 내 컴퓨터의 기본값도 맞춰 둘 수 있습니다. 이름만 다를 뿐 기능적으로는 완전히 같은 평범한 브랜치입니다."),
      C("릴리스 표시","특정 커밋을 'v1.2.0 릴리스' 로 <b>영구히 표시</b>하려면?",
        ["브랜치를 만든다","태그를 붙인다","커밋 메시지에 적는다","파일 이름을 바꾼다"],1,
        "**태그**는 움직이지 않는 이름표입니다. 브랜치는 커밋할 때마다 앞으로 나아가지만 태그는 그 자리에 고정됩니다.\n`git tag -a v1.2.0 -m \"설명\"` 으로 만들고, `git push origin v1.2.0` 으로 따로 올려야 합니다 — 일반 푸시로는 태그가 가지 않습니다.\nGitHub 릴리스와 배포 파이프라인이 대부분 이 태그를 신호로 씁니다.")
    ] }
]},

/* ═══════════════ 유닛 5 ═══════════════ */
{ t:"병합 — 갈라진 것을 합친다", l:[

  { t:"fast-forward 와 진짜 병합", xp:30,
    th:{ sum:"'합쳤는데 병합 커밋이 안 생긴다' 는 질문의 답은 **fast-forward** 다. 두 가지 병합 모양을 구분하면 로그가 읽힌다.",
      body:[
        {h:"갈라진 적이 없으면 그냥 이름표를 옮긴다", t:"`main` 이 그대로인 동안 브랜치만 앞으로 나갔다면, 합칠 것이 없다. Git 은 그냥 `main` 이라는 **이름표를 앞으로 옮긴다** — 이것이 fast-forward 다. 역사가 한 줄로 깔끔해지지만, '이 커밋들이 하나의 기능이었다' 는 정보는 사라진다. 그래서 팀에 따라 `--no-ff` 로 **일부러 병합 커밋을 남긴다**."},
        {h:"양쪽이 다 나아갔으면 3-way merge", t:"둘 다 커밋이 쌓였다면 Git 은 **공통 조상**을 찾아 세 지점을 비교한다. '조상 대비 이쪽이 바꾼 것' 과 '조상 대비 저쪽이 바꾼 것' 을 각각 적용하는데, **같은 줄을 양쪽이 다르게 바꿨을 때만** 충돌이 난다. 즉 충돌은 오류가 아니라 '사람이 정해야 할 지점' 이라는 표시다."}],
      code:{ c:"# fast-forward\nmain ──●──●        →   main ──●──●──●──●\n            \\──●──●\n\n# 3-way merge\nmain ──●──●──●──M   ← 병합 커밋(부모가 둘)\n        \\──●──●─/", cap:"병합 커밋은 부모가 둘이다" },
      key:["갈라진 적 없으면 fast-forward","양쪽 다 나아갔으면 병합 커밋","충돌은 같은 줄을 서로 다르게 고쳤을 때","--no-ff 로 기능 단위를 남길 수 있다"] },
    q:[
      I("병합 커밋 강제","fast-forward 가 가능해도 <b>일부러 병합 커밋을 만드는</b> 옵션은? (git merge 뒤 옵션만)",
        ["--no-ff","-no-ff"],
        "정답은 `--no-ff` 입니다. 'PR 하나가 이 커밋들의 덩어리였다' 는 정보를 역사에 남깁니다.\n반대로 `--ff-only` 는 fast-forward 가 아니면 아예 실패시켜, 의도치 않은 병합 커밋을 막습니다."),
      C("fast-forward","<code>main</code> 이 그대로인 동안 브랜치만 커밋이 쌓였다. <code>git merge</code> 하면?",
        ["병합 커밋이 생긴다","main 포인터가 앞으로 이동할 뿐이다","같은 줄이 겹쳐 충돌이 난다","이미 같은 내용이라 아무 변화도 일어나지 않는다"],1,
        "합칠 것이 없으므로 Git 은 **이름표만 앞으로 옮깁니다**. 이것이 fast-forward 입니다.\n역사가 한 줄이 되어 깔끔하지만, 어디까지가 하나의 기능이었는지는 알 수 없게 됩니다.\n그 정보를 남기고 싶으면 `git merge --no-ff` 로 병합 커밋을 강제로 만듭니다.",
        P("echo a>f && git add f && git commit -qm base && git switch -qc side && echo b>f && git add f && git commit -qm side && git switch -q master 2>/dev/null || git switch -q main; git merge -q side && git rev-list --count --merges HEAD","0")),
      C("병합 커밋","병합 커밋이 일반 커밋과 다른 점은?",
        ["커밋 메시지를 따로 넣지 못한다","부모 커밋이 둘이다","내용이 비어 있다","되돌릴 수 없다"],1,
        "병합 커밋은 **부모를 둘 가집니다**. 이것이 역사가 갈라졌다 합쳐졌음을 기록하는 방식입니다.\n그래서 `git revert` 로 병합을 되돌릴 때 `-m 1` 처럼 **어느 부모 쪽을 주 줄기로 볼지** 알려 줘야 합니다.\n`git log --graph` 가 그리는 갈래 모양도 이 부모 관계로 그려집니다."),
      C("충돌 조건","병합 충돌이 나는 경우는?",
        ["두 브랜치가 같은 이름을 가진 파일을 각자 건드렸을 때","같은 줄을 다르게 바꿨을 때",
         "두 브랜치에 쌓인 커밋 개수가 서로 다를 때","두 브랜치의 이름이 서로 같을 때"],1,
        "같은 **파일**을 건드려도 서로 다른 부분이면 Git 이 알아서 합칩니다. 충돌은 **같은 줄**이 서로 다르게 바뀌었을 때만 납니다.\n한쪽이 지운 파일을 다른 쪽이 고친 경우처럼 파일 수준 충돌도 있습니다.\n충돌은 실패가 아니라 '**여기는 사람이 정해야 한다**' 는 표시입니다."),
      C("충돌 표식","충돌 파일에서 <code>=======</code> 위쪽에 있는 내용은?",
        ["지금 병합해 들어오고 있는 브랜치의 내용","현재 브랜치(HEAD)의 내용","공통 조상의 내용","자동 생성된 내용"],1,
        "`<<<<<<< HEAD` 부터 `=======` 까지가 **현재 있는 브랜치**의 내용, 그 아래 `>>>>>>> 브랜치명` 까지가 들어오는 쪽입니다.\n해결이란 이 표식 세 줄을 **모두 지우고** 최종 코드만 남기는 일입니다. 표식을 남긴 채 커밋하면 코드가 깨집니다.\n`git config merge.conflictStyle zdiff3` 을 켜면 공통 조상까지 함께 보여 줘 판단이 훨씬 쉬워집니다."),
      C("해결 후","충돌을 손으로 고친 다음 해야 할 일은?",
        ["git merge --continue 명령만 이어서 친다","git add 후 커밋",
         "브랜치를 지운다","아무것도 안 해도 된다"],1,
        "`git add` 가 '이 파일은 해결했다' 는 **신호**입니다. 그 뒤 `git commit`(또는 `git merge --continue`)으로 병합을 마칩니다.\nGit 은 파일 내용을 검사하지 않으므로, 충돌 표식을 남긴 채 `add` 해도 그대로 커밋됩니다 — 커밋 전에 한 번 훑어보는 것이 안전합니다.\n중간에 그만두려면 `git merge --abort` 로 병합 전 상태로 되돌아갑니다."),
      I("중단","진행 중인 병합을 취소하고 병합 전으로 돌아가는 명령은? (git 포함)",
        ["git merge --abort"],
        "정답은 `git merge --abort` 입니다. 충돌이 너무 복잡해 처음부터 다시 하고 싶을 때 씁니다.\n같은 방식으로 `git rebase --abort`·`git cherry-pick --abort` 도 있습니다 — Git 의 중간 상태는 대부분 되돌릴 길이 마련되어 있습니다."),
      C("squash","<code>git merge --squash</code> 의 효과는?",
        ["양쪽 브랜치를 각각 가리키는 병합 커밋을 두 개나 만든다","브랜치의 모든 변경을 한 커밋으로 합쳐 스테이지에 올린다",
         "충돌이 나면 자동으로 한쪽을 골라 해결해 준다","합친 뒤 원본 브랜치를 삭제한다"],1,
        "브랜치의 여러 커밋을 **하나로 눌러** 스테이지에 올려 줍니다. 커밋은 직접 해야 합니다.\n'작업 중', '오타 수정' 같은 지저분한 커밋이 `main` 에 남지 않아 로그가 깔끔해집니다.\n대신 **개별 커밋의 맥락이 사라지고** 부모 연결도 남지 않아, 나중에 그 브랜치를 다시 합치려 하면 Git 이 이미 합쳤음을 알지 못합니다.")
    ] },

  { t:"rebase — 역사를 다시 쓴다", xp:30,
    th:{ sum:"merge 는 '갈라졌다 합쳤다' 는 사실을 남기고, rebase 는 '**처음부터 한 줄로 작업한 것처럼**' 다시 쓴다. 결과물은 같아도 역사는 완전히 다르다.",
      body:[
        {h:"커밋을 새로 만들어 옮겨 붙인다", t:"rebase 는 내 커밋들을 **떼어 내 최신 `main` 위에 하나씩 다시 적용**한다. 중요한 것은 커밋이 **복사되는 것이 아니라 새로 만들어진다**는 점이다 — 내용이 같아도 부모가 달라져 **해시가 바뀐다**. 그래서 rebase 후에는 강제 푸시가 필요하고, 그 때문에 위험해진다."},
        {h:"황금률 — 남과 공유한 것은 rebase 하지 않는다", t:"내 브랜치가 나만의 것이면 rebase 는 아주 유용하다. 하지만 **이미 남이 받아 간 브랜치**를 rebase 하면 같은 작업의 커밋이 두 벌 생겨 동료의 저장소가 엉킨다. 규칙은 하나다 — '내 로컬 브랜치는 마음껏, 공유된 브랜치는 절대'. 강제 푸시가 꼭 필요하면 `--force-with-lease` 로 남의 커밋을 덮지 않게 보호한다."}],
      code:{ c:"git switch feature\ngit rebase main            # 내 커밋을 main 위에 다시 얹는다\ngit push --force-with-lease  # 해시가 바뀌었으므로 강제 푸시", cap:"--force-with-lease 는 남의 커밋을 덮지 않는다" },
      key:["rebase 는 커밋을 새로 만든다 (해시 변경)","역사가 한 줄로 정리된다","공유된 브랜치는 rebase 금지","강제 푸시는 --force-with-lease 로"] },
    q:[
      I("대화형 정리","최근 세 커밋을 정리(합치기·메시지 수정)하려고 여는 명령은? (git rebase 부터 대상까지)",
        ["git rebase -i HEAD~3","git rebase --interactive HEAD~3"],
        "정답은 `git rebase -i HEAD~3` 입니다. 편집기가 열리고 커밋마다 무엇을 할지 고릅니다.\n푸시 전에만 쓰는 것이 원칙입니다 — 해시가 바뀌므로 이미 공유된 커밋에 쓰면 강제 푸시가 필요해집니다."),
      C("본질","<code>git rebase main</code> 이 하는 일은?",
        ["main 의 커밋을 내 브랜치 맨 뒤에 그대로 이어 붙여 합친다","내 커밋을 main 위에 다시 얹는다",
         "합치고 나서 main 브랜치를 삭제한다","두 브랜치가 각각 가리키던 커밋을 서로 맞바꾼다"],1,
        "내 커밋을 떼어 **최신 `main` 위에 하나씩 다시 적용**합니다. 결과적으로 갈라진 적이 없던 것처럼 보입니다.\n이때 커밋은 새로 만들어지므로 **해시가 바뀝니다** — 내용이 같아도 다른 커밋입니다.\n로그가 한 줄이 되어 읽기 좋아지는 것이 rebase 를 쓰는 이유입니다."),
      C("황금률","rebase 를 쓰면 <b>안 되는</b> 경우는?",
        ["나만 쓰는 로컬 브랜치","이미 푸시해서 남이 받아 간 브랜치","커밋이 아주 많이 쌓여 있는 브랜치","만든 지 오래된 브랜치"],1,
        "**공유된 역사는 rebase 하지 않는다** — Git 의 황금률입니다.\n해시가 바뀌므로 동료의 저장소에는 옛 커밋이, 원격에는 새 커밋이 남아 같은 작업이 두 벌로 보입니다. 그 상태로 병합하면 중복 커밋과 이상한 충돌이 생깁니다.\n예외는 '이 브랜치는 내 것이고 강제 푸시할 수 있다' 고 팀이 합의한 PR 브랜치입니다."),
      C("해시","rebase 후 커밋 해시가 바뀌는 이유는?",
        ["해시를 계산하는 부분에 남아 있는 Git 버그","부모 커밋이 달라져 커밋 내용 자체가 달라지기 때문","커밋한 시각이 지나서","커밋 메시지가 새로 쓰여서"],1,
        "커밋 해시는 **내용 + 부모 + 작성자 + 시각**을 전부 넣어 계산합니다. 부모가 바뀌면 해시가 바뀝니다.\n그래서 rebase 는 '옮긴다' 기보다 '**같은 변경으로 새 커밋을 만든다**' 가 정확한 표현입니다.\n이 성질 때문에 rebase 후에는 원격과 역사가 어긋나 강제 푸시가 필요해집니다."),
      C("안전한 강제 푸시","<code>--force-with-lease</code> 가 <code>--force</code> 보다 안전한 이유는?",
        ["태그를 다는 같은 일을 더 빠르게 끝낸다","남이 푸시했으면 거부한다","원격을 덮어쓰기 전에 자동으로 백업을 만들어 둔다","실행하기 전에 정말 덮어써도 되는지 한 번 더 물어본다"],1,
        "`--force` 는 원격에 무엇이 있든 **무조건 덮어씁니다**. 그 사이 동료가 푸시한 커밋이 있으면 그대로 사라집니다.\n`--force-with-lease` 는 원격이 내가 아는 상태 그대로일 때만 덮어씁니다. 달라졌으면 거부하고 알려 줍니다.\n강제 푸시가 필요한 상황이라면 **항상** 이쪽을 쓰는 것이 좋습니다."),
      C("대화형 rebase","<code>git rebase -i HEAD~3</code> 으로 할 수 <b>없는</b> 것은?",
        ["커밋 메시지를 다시 고쳐 쓰기","커밋의 순서를 바꾸기","여러 개로 나뉜 커밋을 하나로 합치기","이미 푸시된 남의 커밋 삭제"],3,
        "대화형 rebase 는 **내 로컬 역사**를 정리하는 도구입니다. 원격이나 남의 저장소를 바꾸지는 못합니다.\n`reword`(메시지), `squash`/`fixup`(합치기), `drop`(삭제), 순서 바꾸기가 가능해 PR 을 올리기 전 정리에 아주 유용합니다.\n푸시 후에 쓰면 결국 강제 푸시가 필요해지므로 황금률이 그대로 적용됩니다."),
      I("합치기","대화형 rebase 에서 <b>이전 커밋에 합치되 메시지는 버리는</b> 명령어는?",
        ["fixup","f"],
        "정답은 `fixup`(단축 `f`)입니다. `squash` 는 메시지를 합쳐 편집기를 띄우지만, `fixup` 은 이전 커밋의 메시지를 그대로 씁니다.\n'오타 수정' 같은 커밋을 원래 커밋에 흡수시킬 때 딱 맞습니다. `git commit --fixup=해시` 로 표시해 두면 `git rebase -i --autosquash` 가 자동으로 자리를 잡아 줍니다."),
      C("pull 전략","<code>git pull --rebase</code> 를 쓰면 무엇이 달라지나?",
        ["같은 양을 더 빨리 받아 온다","원격 커밋 위에 내 커밋을 다시 얹어 불필요한 병합 커밋을 만들지 않는다",
         "합칠 때 충돌이 아예 안 난다","받아서 합친 뒤 원격으로 자동 푸시까지 대신 해 주기 때문에"],1,
        "기본 `git pull` 은 받아온 것과 내 것을 **병합**해, 의미 없는 'Merge branch main' 커밋이 로그를 어지럽힙니다.\n`--rebase` 는 내 커밋을 원격 위에 다시 얹어 한 줄로 유지합니다. `git config --global pull.rebase true` 로 기본값을 바꿔 둘 수 있습니다.\n충돌은 여전히 날 수 있고, 이때는 rebase 방식으로 해결합니다 — 커밋 하나씩 물어봅니다.")
    ] }
]},

/* ═══════════════ 유닛 6 ═══════════════ */
{ t:"사고 복구 — 잃어버린 것을 찾는다", l:[

  { t:"reflog — Git 의 블랙박스", xp:30,
    th:{ sum:"'커밋을 날렸다' 는 대부분 사실이 아니다. Git 은 **HEAD 가 움직인 모든 기록**을 따로 남긴다. 그것이 reflog 다.",
      body:[
        {h:"브랜치가 안 가리켜도 커밋은 살아 있다", t:"`reset --hard` 로 뒤로 갔거나 브랜치를 지웠어도, 그 **커밋 객체 자체는 저장소에 남아 있다**. 사라진 것은 그것을 가리키던 이름뿐이다. `git reflog` 는 HEAD 가 거쳐 간 모든 자리를 시간순으로 보여 주므로, 거기서 해시를 찾아 `git switch -c 복구 해시` 하면 그대로 되살아난다."},
        {h:"영원하지는 않다", t:"가리키는 이름이 없는 객체는 기본 **약 90일**(reflog 만료) 뒤 정리(`git gc`) 대상이 된다. 즉 reflog 는 무한 백업이 아니라 **최근 사고를 되돌리는 안전망**이다. 그리고 reflog 는 **내 컴퓨터에만** 있다 — 클론한 저장소에는 내 reflog 가 없다."}],
      code:{ c:"git reflog                    # HEAD 가 거쳐 간 모든 자리\n# 1a2b3c HEAD@{2}: commit: 중요한 작업\ngit switch -c rescue 1a2b3c   # 그 자리로 새 브랜치를 만들어 구조", cap:"해시만 알면 되살릴 수 있다" },
      key:["이름이 사라져도 커밋은 남는다","reflog 에서 해시를 찾아 되살린다","약 90일 뒤 정리 대상","reflog 는 내 컴퓨터에만 있다"] },
    q:[
      I("범인 찾기 시작","이분 탐색으로 문제 커밋을 찾기 시작하는 명령은? (git bisect 와 낱말까지)",
        ["git bisect start","start"],
        "정답은 `git bisect start` 입니다. 이어서 `git bisect bad` 와 `git bisect good 옛날해시` 를 알려 주면 탐색이 시작됩니다.\n끝나면 `git bisect reset` 으로 원래 브랜치로 돌아와야 합니다 — 이걸 잊으면 이상한 자리에 남아 있게 됩니다."),
      C("사고 복구","실수로 <code>git reset --hard</code> 해서 커밋을 잃었다. 가장 먼저 볼 것은?",
        ["git log","git reflog","git status 로 지금 상태를 확인한다","원격 저장소"],1,
        "`git log` 는 **현재 브랜치에서 닿는** 커밋만 보여 줍니다. 잘라 낸 커밋은 거기 없습니다.\n`git reflog` 는 HEAD 가 **거쳐 간 모든 자리**를 남기므로, reset 직전의 해시가 그대로 적혀 있습니다.\n그 해시로 `git switch -c 복구 해시` 하면 되살아납니다.",
        P("echo a>f && git add f && git commit -qm one && echo b>f && git add f && git commit -qm two && git reset -q --hard HEAD~1 && git reflog | grep -c 'commit: two'","1")),
      C("원리","브랜치를 지웠는데도 커밋을 되살릴 수 있는 이유는?",
        ["Git 이 모든 명령마다 몰래 자동 백업을 따로 남겨 두기 때문에","이름만 사라졌기 때문",
         "같은 커밋이 원격에도 남아 있어서","지운 것이 휴지통에 남아 있어서"],1,
        "Git 에서 삭제되는 것은 대개 **이름(ref)** 이고, 객체 자체는 저장소에 남습니다.\n아무도 가리키지 않는 객체를 '도달 불가(unreachable)' 라 하고, `git gc` 가 정리하기 전까지는 해시만 알면 접근할 수 있습니다.\n이것이 Git 에서 '진짜로 잃는' 경우가 커밋한 적 없는 변경뿐인 이유입니다."),
      C("복구 불가","reflog 로 되살릴 수 <b>없는</b> 것은?",
        ["reset 을 써서 뒤로 잘라 낸 커밋","삭제한 브랜치의 커밋","한 번도 커밋한 적 없는 수정","rebase 이전의 커밋"],2,
        "**커밋된 적 없는 변경**은 Git 이 본 적이 없으므로 어떤 방법으로도 되살릴 수 없습니다.\n`git reset --hard` 나 `git restore` 가 위험한 진짜 이유가 이것입니다.\n확신이 없을 때 `git stash` 를 습관으로 두면, stash 도 커밋으로 저장되므로 이 위험이 사라집니다."),
      I("기록 보기","HEAD 가 이동한 기록을 보는 명령은? (git 포함)",
        ["git reflog","git reflog show"],
        "정답은 `git reflog` 입니다. `HEAD@{0}` 이 가장 최근 위치입니다.\n특정 브랜치의 이동 기록만 보려면 `git reflog show 브랜치이름` 을 씁니다. 브랜치가 언제 어디서 만들어졌는지까지 나옵니다."),
      C("범위","동료가 내 저장소를 클론했다. 내 reflog 도 함께 가는가?",
        ["간다","가지 않는다 (로컬 전용)","일부만 따라간다","clone 할 때의 설정에 따라 갈 수도 있고 안 갈 수도 있다"],1,
        "reflog 는 `.git/logs/` 에 있는 **로컬 기록**이고 절대 전송되지 않습니다.\n그래서 '내 컴퓨터에서 한 실수' 는 내 reflog 로 되살리지만, **다른 사람의 컴퓨터에서 잃어버린 것**은 내가 도와줄 수 없습니다.\n팀의 안전망은 reflog 가 아니라 **원격에 자주 푸시하는 것**입니다."),
      C("bisect","'언제부터 깨졌는지' 를 커밋 단위로 찾는 도구는?",
        ["git blame 으로 작성자 추적","git bisect","git grep 으로 내용 검색","git diff 로 변경 비교"],1,
        "`git bisect` 는 **이분 탐색**으로 문제가 처음 생긴 커밋을 찾습니다. 커밋이 1000개여도 10번만 확인하면 됩니다.\n`git bisect start` → `git bisect bad` → `git bisect good 옛날해시` 로 시작하고, Git 이 골라 주는 커밋마다 확인 후 `good`/`bad` 를 알려 주면 됩니다.\n판정을 스크립트로 자동화하려면 `git bisect run ./test.sh` 한 줄이면 됩니다."),
      C("cherry-pick","다른 브랜치의 <b>커밋 하나만</b> 가져오려면?",
        ["git merge 로 브랜치 통째로 합치기","git cherry-pick 해시","git rebase 로 브랜치 위에 다시 얹기","git pull 로 원격 변경 받아오기"],1,
        "`git cherry-pick 해시` 는 그 커밋의 변경만 **현재 브랜치에 새 커밋으로 얹습니다**.\n긴급 수정을 `main` 과 릴리스 브랜치 양쪽에 넣을 때 흔히 씁니다.\n남용하면 같은 변경이 여러 해시로 흩어져 나중에 '이게 반영됐나?' 를 판단하기 어려워지므로, 정기적인 병합이 가능하면 그쪽이 낫습니다.")
    ] },

  { t:"내부 동작 — 객체 네 가지면 전부다", xp:30,
    th:{ sum:"Git 은 사실 **내용을 해시로 저장하는 작은 파일 시스템**이다. 객체 네 가지를 알면 Git 의 거의 모든 행동이 설명된다.",
      body:[
        {h:"blob · tree · commit · tag", t:"**blob** 은 파일의 내용(이름은 모른다). **tree** 는 폴더로, 이름과 blob·tree 를 짝지어 담는다. **commit** 은 tree 하나 + 부모 커밋 + 작성자 + 메시지다. **tag** 는 특정 객체에 붙는 이름표다. 커밋이 tree 를 가리키고 tree 가 blob 을 가리키는 이 사슬이 곧 '스냅숏' 의 실체다."},
        {h:"이름이 내용에서 나온다", t:"객체의 주소는 **내용을 SHA-1 로 해시한 값**이다. 내용이 같으면 주소가 같으므로, 똑같은 파일이 100군데 있어도 저장은 한 벌이다. 반대로 한 글자만 바꿔도 주소가 완전히 달라진다. 커밋 해시가 부모까지 포함해 계산되므로, 과거 커밋 하나를 몰래 바꾸면 **그 뒤 모든 해시가 바뀌어** 즉시 들통난다 — Git 이 위변조에 강한 이유다."}],
      code:{ c:"commit 9a3f…\n  └ tree 4b1c…\n      ├ blob 8e2d…  README.md\n      └ tree 77aa…  src/\n           └ blob 1f0b…  app.js", cap:"커밋 → 트리 → blob 의 사슬" },
      key:["객체는 blob·tree·commit·tag 넷","주소 = 내용의 해시","같은 내용은 한 벌만 저장","해시 사슬이 위변조를 막는다"] },
    q:[
      I("객체 종류","해시가 커밋인지 트리인지 blob 인지 확인하는 옵션은? (git cat-file 뒤 옵션만)",
        ["-t","-type"],
        "정답은 `-t` 입니다. `git cat-file -t 해시` 가 `commit`·`tree`·`blob`·`tag` 중 하나를 돌려줍니다.\n`-s` 는 크기, `-p` 는 내용입니다. 저장소 내부를 처음 들여다볼 때 이 셋이면 충분합니다."),
      C("blob","Git 의 blob 객체가 담는 것은?",
        ["이름과 내용","파일의 내용만","폴더 구조","커밋에 적은 메시지"],1,
        "blob 은 **내용만** 담습니다. 파일 이름은 그것을 가리키는 **tree** 에 들어 있습니다.\n그래서 이름만 바꾸고 내용이 같으면 blob 은 새로 만들어지지 않고 tree 만 바뀝니다.\n같은 내용의 파일이 여러 경로에 있어도 blob 은 하나만 저장되는 것도 이 구조 덕분입니다."),
      C("커밋의 구성","commit 객체가 담고 있지 <b>않은</b> 것은?",
        ["최상위 tree 해시","부모 커밋을 가리키고 있는 해시","작성자와 시각","직전 커밋과의 diff"],3,
        "커밋은 **그 순간의 tree 전체**를 가리킬 뿐, diff 를 저장하지 않습니다.\n화면에 보이는 diff 는 부모 커밋의 tree 와 비교해 **그때그때 계산한 결과**입니다.\n그래서 어떤 두 커밋이든 즉시 비교할 수 있고, 중간 커밋을 거치지 않아도 됩니다."),
      C("주소","Git 객체의 주소(해시)는 무엇으로 정해지는가?",
        ["그 객체를 만든 시각","내용","파일 경로","작성자"],1,
        "**내용을 해시한 값**이 곧 주소입니다(content-addressable). 내용이 같으면 언제 누가 만들어도 같은 주소입니다.\n덕분에 중복 저장이 사라지고, 전송할 때도 '이미 있는 객체' 를 바로 건너뛸 수 있습니다.\n커밋만은 시각·작성자·부모까지 포함해 해시하므로, 내용이 같아도 커밋 해시는 다를 수 있습니다."),
      C("위변조","과거 커밋 하나의 내용을 몰래 바꾸면?",
        ["해시 값이 그대로 남아 아무도 알아채지 못한다","뒤 해시가 전부 달라진다","Git 이 어긋남을 발견하는 즉시 원래 내용으로 자동 복구해 준다","확인할 때 경고 문구만 뜬다"],1,
        "커밋 해시에 **부모 해시가 포함**되므로, 하나를 바꾸면 그 뒤 사슬 전체의 해시가 달라집니다.\n다른 사람이 가진 해시와 대조하는 순간 어긋남이 드러납니다 — 블록체인이 쓰는 것과 같은 원리입니다.\n그래서 Git 의 역사는 '지울 수는 있어도 **몰래 고칠 수는 없다**' 고 말합니다."),
      I("객체 들여다보기","해시로 객체의 내용을 보는 명령은? (git 과 -p 옵션 포함, 해시는 제외)",
        ["git cat-file -p","git cat-file"],
        "정답은 `git cat-file -p 해시` 입니다. 커밋·트리·blob 어느 것이든 사람이 읽을 수 있게 풀어 줍니다.\n`git cat-file -t 해시` 로 종류만 확인할 수도 있습니다. Git 내부를 처음 탐험할 때 가장 좋은 도구입니다.",
        P("echo a>f && git add f && git commit -qm c && git cat-file -t HEAD","commit")),
      C("패킹","저장소가 커졌을 때 <code>git gc</code> 가 하는 일은?",
        ["일정 기간이 지난 오래된 커밋을 찾아 모두 삭제한다","객체를 팩 파일로 압축하고 도달 불가 객체를 정리한다",
         "저장소 전체를 원격 서버에 백업해 둔다","병합이 끝난 브랜치를 정리한다"],1,
        "느슨한 객체들을 **팩 파일** 하나로 묶고 델타 압축을 적용해 크기를 크게 줄입니다. 동시에 아무도 가리키지 않는 오래된 객체를 지웁니다.\n도달 가능한 커밋은 절대 지우지 않으므로 역사는 안전합니다.\n다만 이 정리 이후에는 reflog 로도 되살릴 수 없으니, 되찾을 것이 있다면 gc 전에 해야 합니다."),
      C("SHA-1","Git 이 SHA-1 을 쓰면서도 보안 문제가 크지 않은 이유로 가장 적절한 것은?",
        ["SHA-1 충돌은 이론으로만 알려졌을 뿐 실제로 만들어진 적이 한 번도 없어서","Git 은 길이 접두사를 붙여 해시하고 충돌 탐지를 넣었으며 SHA-256 으로 옮겨 가는 중이다",
         "저장소가 비공개라 외부에서 볼 수 없어서","같은 내용의 해시를 두 번 계산해 서로 비교하고 있어서"],1,
        "SHA-1 충돌은 2017년에 실증되었습니다. Git 은 이후 **충돌 탐지 SHA-1** 을 도입해 알려진 공격 패턴을 걸러내고, `object-format=sha256` 저장소도 지원합니다.\n또 Git 은 내용 앞에 타입과 길이를 붙여 해시하므로 일반적인 충돌 예제를 그대로 쓸 수 없습니다.\n무엇보다 커밋 서명(GPG/SSH)이 진짜 무결성 보장 수단입니다 — 해시만으로 신뢰를 세우지는 않습니다.")
    ] }
]}

];

/* 손으로 쓰면 정답이 두 번째 자리에 몰린다. 결정적 셔플로 네 자리에 고르게 편다. */
module.exports=shuffleUnits(UNITS);
