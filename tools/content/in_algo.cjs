/* 알고리즘 단답 15 — 복잡도 표기와 자료구조의 성질을 손으로 적어 본다.
   검증:  node ver_input.cjs ./in_algo.cjs
   출력값 대조:  node chk_predict.cjs ./in_algo.cjs */
module.exports = [

{ track:"algo", t:"input", cat:"internals", k:"반으로 나누는 깊이",
  q:"원소 여덟 개를 병합 정렬로 정렬하면 <b>재귀 깊이</b>는 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(Math.log2(8));",
  a:["3"],
  ex:"8 → 4 → 2 → 1 로 세 번 나누면 한 개가 됩니다. 각 깊이에서 전체를 한 번씩 훑으므로 <code>O(n log n)</code> 입니다.\n💡 개념: '<b>몇 번 반으로 접으면 1 이 되는가</b>' 가 곧 밑 2 로그입니다.\n🛠 실무: 재귀 깊이가 로그라서 스택이 넘칠 걱정이 없습니다 — 반대로 <b>한 칸씩 줄이는</b> 재귀(퀵 정렬 최악)는 깊이가 n 이라 넘칩니다." },

{ track:"algo", t:"input", cat:"internals", k:"높이 3인 이진 트리의 크기",
  q:"루트의 높이를 0 으로 셀 때, 높이가 3 인 <b>포화 이진 트리</b>의 노드 개수는 몇 개일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(Math.pow(2, 4) - 1);",
  a:["15"],
  ex:"각 층에 <code>1, 2, 4, 8</code> 개가 있어 합이 15 입니다 — 높이 h 면 <code>2^(h+1) - 1</code> 입니다.\n💡 개념: 마지막 층 하나가 <b>나머지 전부보다 많다</b>는 점이 트리 알고리즘의 비용을 좌우합니다.\n🛠 실무: 힙의 <code>build</code> 가 O(n log n) 이 아니라 O(n) 인 이유가 여기입니다 — 아래쪽 노드가 많지만 옮길 거리가 짧습니다." },

{ track:"algo", t:"input", cat:"internals", k:"최소 힙에서 두 번째로 나오는 값",
  q:"값 <code>[5, 3, 8, 1]</code> 을 최소 힙에 넣고 두 번 꺼내면 <b>두 번째로</b> 나오는 값은 무엇일까요? 출력되는 값을 적으세요. (숫자)",
  code:"const s = [5, 3, 8, 1].slice().sort((a, b) => a - b);\nconsole.log(s[1]);",
  a:["3"],
  ex:"최소 힙은 <b>가장 작은 것부터</b> 나오므로 1, 3 순서입니다.\n💡 개념: 힙은 전체를 정렬해 두지 않습니다 — <b>루트만</b> 최솟값이고 형제끼리는 순서가 없습니다.\n🛠 실무: 상위 k 개만 필요하면 전체 정렬(O(n log n)) 대신 크기 k 짜리 힙(O(n log k))을 쓰세요." },

{ track:"algo", t:"input", cat:"internals", k:"배열로 만든 힙의 부모 자리",
  q:"0 부터 세는 배열로 이진 힙을 만들 때, 자리 5 의 <b>부모</b>는 몇 번 자리일까요? 출력되는 값을 적으세요. (숫자)",
  code:"const i = 5;\nconsole.log(Math.floor((i - 1) / 2));",
  a:["2"],
  ex:"0 기반에서 부모는 <code>(i-1)/2</code> 의 내림, 자식은 <code>2i+1</code> 과 <code>2i+2</code> 입니다.\n💡 개념: 1 부터 세면 부모가 <code>i/2</code>, 자식이 <code>2i</code>·<code>2i+1</code> 로 더 깔끔해서 교과서는 1 기반을 즐겨 씁니다.\n🛠 실무: 두 방식을 한 코드에 섞으면 반드시 한 칸이 어긋납니다 — 어느 쪽인지 주석에 못 박아 두세요." },

{ track:"algo", t:"input", cat:"internals", k:"너비 우선 탐색의 비용",
  q:"정점 V 개, 간선 E 개인 그래프를 인접 리스트로 두고 너비 우선으로 훑을 때의 시간 복잡도를 빅오 표기로 적으세요. (예: O(n log n))",
  a:["O(V+E)", "O(|V|+|E|)", "O(E+V)"],
  ex:"모든 정점을 한 번씩, 모든 간선을 (방향 그래프면 한 번, 무방향이면 양쪽에서) 훑으므로 둘의 합입니다.\n💡 개념: 인접 <b>행렬</b>로 두면 이웃을 찾을 때마다 V 칸을 훑어 <code>O(V²)</code> 가 됩니다 — 자료구조가 복잡도를 바꿉니다.\n🛠 실무: 간선이 적은(희소) 그래프는 리스트, 빽빽한 그래프는 행렬이 유리합니다." },

{ track:"algo", t:"input", cat:"internals", k:"빠른 정렬이 느려질 때",
  q:"이미 정렬된 입력에 첫 원소를 기준으로 삼는 퀵 정렬을 돌리면 <b>최악의</b> 시간 복잡도는 얼마일까요? 빅오 표기로 적으세요. (예: O(n log n))",
  a:["O(n^2)", "O(n²)", "O(n * n)"],
  ex:"기준이 언제나 한쪽 끝이면 한 칸씩만 줄어 <code>n</code> 번 나누고 각 단계에서 <code>n</code> 번 견줍니다.\n💡 개념: 평균은 <code>O(n log n)</code> 이지만 <b>최악은 정렬된 입력</b>이라, 실제 데이터에서 오히려 자주 만납니다.\n🛠 실무: 기준을 가운데 값이나 무작위로 고르고(또는 median-of-three), 깊이가 너무 깊어지면 힙 정렬로 갈아타는 <b>인트로 정렬</b>이 표준 라이브러리의 답입니다." },

{ track:"algo", t:"input", cat:"internals", k:"같은 값의 순서를 지키는 정렬",
  q:"값이 같은 원소들의 <b>원래 순서</b>가 정렬 뒤에도 그대로 유지되는 성질을 무엇이라 부를까요? (영문 한 단어)",
  a:["stable", "안정", "안정정렬", "안정성"],
  ex:"이 성질이 있어야 '먼저 부서로 정렬하고 다시 이름으로 정렬' 같은 <b>여러 단계 정렬</b>이 뜻대로 됩니다.\n💡 개념: 병합 정렬은 이 성질이 있고, 힙 정렬과 대부분의 퀵 정렬 구현은 없습니다.\n🛠 실무: 자바스크립트의 <code>Array.sort</code> 는 2019 년부터 이 성질이 <b>보장</b>됩니다. 예전 코드는 이를 못 믿어 2 순위 기준을 직접 붙였습니다." },

{ track:"algo", t:"input", cat:"internals", k:"이진 탐색 트리를 가운데부터 훑으면",
  q:"이진 탐색 트리를 중위 순회하면 값들이 어떤 순서로 나올까요? (네 글자, 예: 무작위순)",
  a:["오름차순", "정렬순서", "증가순", "작은순"],
  ex:"왼쪽 → 자기 → 오른쪽 순서로 훑으면 정의상 작은 것부터 나옵니다.\n💡 개념: 그래서 '정렬된 목록이 필요한가' 가 이진 탐색 트리와 해시 테이블을 고르는 갈림길입니다 — 해시는 순서를 주지 못합니다.\n🛠 실무: DB 인덱스가 B-tree 인 이유도 같습니다. 범위 조회와 <code>ORDER BY</code> 를 인덱스만으로 처리할 수 있습니다." },

{ track:"algo", t:"input", cat:"internals", k:"위상 정렬이 되는 그래프",
  q:"위상 정렬이 가능한 그래프의 조건을 세 글자 영문 약어로 적으세요. (영문 약어)",
  a:["DAG"],
  ex:"방향이 있고 사이클이 없는 그래프(Directed Acyclic Graph)여야 합니다. 사이클이 있으면 '먼저 해야 할 일' 이 서로 물려 순서를 못 정합니다.\n💡 개념: 그래서 위상 정렬 알고리즘은 <b>사이클 탐지기</b>이기도 합니다 — 결과 개수가 정점 수보다 적으면 사이클이 있습니다.\n🛠 실무: 빌드 순서·마이그레이션 순서·작업 의존성이 전부 이 구조라, 순환 의존성 오류가 바로 이 검사에서 나옵니다." },

{ track:"algo", t:"input", cat:"internals", k:"다익스트라가 못 다루는 간선",
  q:"다익스트라 최단경로 알고리즘이 <b>다룰 수 없는</b> 간선은 어떤 간선일까요? (세 글자, 예: 양방향)",
  a:["음수간선", "음수", "음의간선", "음수가중치"],
  ex:"'한 번 확정한 최단거리는 더 줄지 않는다' 는 전제가 깨지기 때문입니다. 나중에 큰 마이너스가 나타나면 이미 확정한 값이 틀립니다.\n💡 개념: 그때는 벨만-포드(<code>O(VE)</code>)를 쓰고, 이 알고리즘은 <b>음수 사이클</b>까지 찾아냅니다.\n🛠 실무: 환전 차익 거래나 '보상' 이 섞인 경로 탐색이 대표적인 음수 간선 문제입니다." },

{ track:"algo", t:"input", cat:"internals", k:"스무 번째 피보나치 수",
  q:"1, 1 로 시작하는 피보나치 수열의 <b>스무 번째</b> 값은 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"let a = 1, b = 1;\nfor (let i = 3; i <= 20; i += 1) { const c = a + b; a = b; b = c; }\nconsole.log(b);",
  a:["6765"],
  ex:"반복으로 구하면 스무 번 만에 끝나지만, 메모 없는 재귀로 구하면 함수 호출이 <b>13,529 번</b>입니다.\n💡 개념: 겹치는 부분 문제를 다시 계산하는 것이 지수 시간의 원인이고, 메모이제이션이 그것을 선형으로 바꿉니다.\n🛠 실무: 값 두 개만 들고 도는 이 형태는 메모리도 <code>O(1)</code> 입니다 — 표 전체가 필요 없으면 굳이 만들지 마세요." },

{ track:"algo", t:"input", cat:"internals", k:"창을 옮기는 횟수",
  q:"길이 10 인 배열에서 크기 3 짜리 창을 한 칸씩 옮기면 창은 모두 몇 개일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(10 - 3 + 1);",
  a:["8"],
  ex:"시작 자리가 0 부터 7 까지이므로 여덟 개입니다 — <code>n - k + 1</code> 입니다.\n💡 개념: <code>k</code> 가 <code>n</code> 보다 크면 개수가 0 이하가 되므로, 그 경우를 <b>따로 막지 않으면</b> 음수 길이 반복이 됩니다.\n🛠 실무: 창을 옮길 때마다 새로 합을 구하면 <code>O(nk)</code>, 들어온 값을 더하고 나간 값을 빼면 <code>O(n)</code> 입니다." },

{ track:"algo", t:"input", cat:"internals", k:"같은 값이 여럿일 때의 첫 자리",
  q:"정렬된 배열 <code>[1, 2, 2, 3]</code> 에서 2 <b>이상인 첫 자리</b>는 몇 번일까요? 0 부터 셉니다. 출력되는 값을 적으세요. (숫자)",
  code:"const a = [1, 2, 2, 3];\nlet lo = 0, hi = a.length;\nwhile (lo < hi) { const m = (lo + hi) >> 1; if (a[m] < 2) lo = m + 1; else hi = m; }\nconsole.log(lo);",
  a:["1"],
  ex:"중복이 있으면 '찾으면 반환' 은 가운데 아무 자리나 줍니다. <code>&lt;</code> 하나로만 나눠야 <b>첫 자리</b>가 정해집니다.\n💡 개념: 이상인 첫 자리(lower bound)와 초과인 첫 자리(upper bound)는 부등호 한 글자 차이이고, 둘의 간격이 곧 <b>같은 값의 개수</b>입니다.\n🛠 실무: 이진 탐색은 '찾는다' 가 아니라 '<b>어느 경계를 돌려줄 것인가</b>' 를 먼저 정해야 합니다." },

{ track:"algo", t:"input", cat:"internals", k:"값의 범위를 아는 정렬",
  q:"값이 0 부터 k 사이의 정수라는 것을 알 때 쓰는 계수 정렬의 시간 복잡도를 빅오 표기로 적으세요. (예: O(n log n))",
  a:["O(n+k)", "O(k+n)"],
  ex:"값마다 개수를 세고(n) 값 범위를 훑어 늘어놓으므로(k) 둘의 합입니다. 비교를 하지 않아 <code>O(n log n)</code> 하한을 피해 갑니다.\n💡 개념: 비교 정렬의 하한 <code>Ω(n log n)</code> 은 '<b>비교만으로</b>' 정렬할 때의 이야기입니다.\n🛠 실무: <code>k</code> 가 아주 크면(예: 32비트 정수 전체) 메모리가 폭발하므로, 값 범위가 좁을 때만 씁니다 — 나이·점수·색상 같은 데이터가 딱 맞습니다." },

{ track:"algo", t:"input", cat:"internals", k:"거의 상수라고 부르는 이유",
  q:"경로 압축과 랭크를 함께 쓴 유니온-파인드의 한 번 연산 비용은 '거의 상수' 라고 합니다. 이때 등장하는 아주 느리게 자라는 함수의 이름은? (사람 이름이 들어간 표현)",
  a:["애커만", "역애커만", "애커만역함수", "inverseackermann", "ackermann"],
  ex:"정확히는 애커만 함수의 <b>역함수</b> <code>α(n)</code> 이고, 우주의 원자 수만 한 입력에서도 5 를 넘지 않아 사실상 상수로 봅니다.\n💡 개념: 경로 압축만 쓰거나 랭크만 쓰면 이 보장이 나오지 않습니다 — <b>둘을 함께</b> 써야 합니다.\n🛠 실무: 연결 요소·크루스칼 최소 신장 트리·동적 연결성 판정이 전부 이 자료구조 위에서 돕니다." },

];
