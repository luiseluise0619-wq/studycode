module.exports = {
  track: "test", guide: "", xp: 80,
  unit: "직접 구현 — 테스트를 굴리는 쪽의 계산",
  source: "./test_exec2.cjs",
  lessons: [
    { t: "단언과 매처 — 무엇을 확인하지 않고 있는가", n: 5,
      th: { sum: "매처는 '같다' 를 판정하는 함수가 아니라 **무엇을 같다고 볼지 정하는 함수**다. 그 정의가 곧 테스트가 지켜 주는 범위다.",
        body: [
          { h: "느슨한 단언은 조용히 통과한다", t: "부분 비교 매처는 기대 객체에 적은 열쇠만 본다. 필드가 하나 늘어도 안 깨지는 것이 장점이지만, **없어야 할 필드가 생겨도 통과한다.** 비밀번호 해시가 응답에 섞여 나오는 사고가 이런 단언 아래에서 오래 살아남는다. 오류 확인도 같은 성격이다 — 메시지를 견주지 않으면 오타로 생긴 `undefined is not a function` 으로도 통과하므로, 깨진 코드가 초록불을 받는다. 기대 객체를 빈 채로 넘긴 테스트는 **아무것도 확인하지 않으면서 통과**한다." },
          { h: "실패 메시지가 도구의 값어치다", t: "'기대와 다릅니다' 만 나오면 100개짜리 배열에서 눈으로 찾아야 한다. **처음 어긋난 자리**를 짚어 주면 그 줄만 보면 되고, 앞뒤 문맥을 함께 보여 주면 스크롤조차 필요 없다. 요약 한 줄에는 전체 개수를 함께 적는다 — '1 실패' 만으로는 3개 중 1개인지 300개 중 1개인지 모르는데 **비율이 곧 심각도**다. 빈 목록에 '0 전부 통과' 가 뜨는 것은 논리적으로는 맞지만, 테스트를 하나도 못 찾은 상태가 초록불로 보이는 위험한 자리다." }],
        code: { c: "// every 는 빈 배열에 true — 기대가 비면 언제나 통과한다\nreturn Object.keys(expected).every((k) => actual[k] === expected[k]);", cap: "확인하지 않는 것이 무엇인지 보라" },
        key: ["부분 비교는 '없음' 을 못 지킨다", "오류는 메시지까지 견준다", "실패 메시지에 자리를 담는다", "0건 통과는 의심할 신호"] } },

    { t: "준비와 정리 — 흔적을 남기지 않는다", n: 5,
      th: { sum: "테스트가 서로를 밟기 시작하면 결과를 믿을 수 없게 된다. 막는 방법은 두 가지뿐이다 — **이름을 나누거나, 끝나고 되돌리거나.**",
        body: [
          { h: "정리는 준비의 역순으로", t: "서버가 DB 를 쓰고 있으므로 DB 를 먼저 닫으면 서버가 멈추다 오류를 낸다. 어려운 자리는 **중간에 실패했을 때**다 — 성공한 것만 정리해야 하는데, 못 하면 아직 만들지 않은 것을 정리하려다 터지거나 이미 만든 것이 남아 다음 테스트를 오염시킨다. 준비를 **스택처럼** 쌓고 각 단계가 성공한 직후에 자기 정리를 등록하면 이 계산이 저절로 맞는다. 정리 중 오류가 나도 나머지는 계속해야 하므로, 오류는 모아 두었다가 마지막에 보고한다." },
          { h: "이름과 시간을 손에 쥔다", t: "병렬 실행에서 먼저 부딪히는 것은 **이름 충돌**이다. 워커 번호는 같은 시각의 충돌을, 실행 번호는 앞 실행이 남긴 것과의 충돌을 막으므로 둘 다 필요하다. 무작위 이름은 재현이 어렵고 정리에 실패하면 **이름을 모르는 쓰레기**가 쌓인다. 시간도 같다 — 만료·타임아웃·요금 계산이 전부 '지금' 에 기대는데 진짜 시계로는 같은 상황을 다시 만들 수 없다. 가짜 시계는 **앞으로만 가야** 하고, 더 나은 답은 시각을 인자로 받게 만들어 가짜 시계 자체를 없애는 것이다." }],
        code: { c: "const at = steps.indexOf(failedAt);\nconst done = at < 0 ? steps.slice() : steps.slice(0, at);\nreturn done.reverse();   // 성공한 것만, 거꾸로", cap: "실패한 자리 앞까지만 되돌린다" },
        key: ["정리는 준비의 역순", "성공한 것만 정리한다", "워커와 실행 번호를 둘 다 넣는다", "테스트하기 쉬운 설계는 바깥을 인자로 받는다"] } },

    { t: "비동기와 흔들림 — 재시도로 덮지 않는다", n: 5,
      th: { sum: "흔들리는 테스트는 고장 난 테스트다. 재시도를 넣으면 초록불이 되지만 **흔들림 자체는 그대로 남고**, 그 사이에 진짜 신호가 묻힌다.",
        body: [
          { h: "자지 말고 확인하라", t: "`sleep(2000)` 은 빠른 기계에서 낭비고 느린 기계에서 모자란다. 같은 코드가 어떤 기계에서는 통과하고 어떤 기계에서는 실패하는 전형적인 원인이다. **짧게 여러 번 확인**하면 대개 훨씬 빨리 끝나고 느린 기계에서도 안 깨진다. 가장 좋은 것은 폴링조차 없애는 것이라, 사건을 받을 수 있다면 즉시 반응하는 쪽이 옳다 — 폴링은 사건을 받을 수 없을 때의 차선이다. 재시도는 **진짜 불확실한 바깥 세계**에만 쓴다. 순수한 계산에 재시도가 필요하다면 그 코드에 경합이 있다는 뜻이다." },
          { h: "재기 전에는 고칠 수 없다", t: "느낌으로는 '가끔' 인데 재 보면 30% 인 경우가 흔하다. 1% 라면 재시도로 넘기고 기록해 둘 수 있지만 30% 라면 **그 테스트는 정보를 주지 않는다** — 초록불도 빨간불도 믿을 수 없기 때문이다. 100% 실패는 흔들림이 아니라 진짜 버그이므로 목록에서 빼고 곧바로 고친다. 순서 의존을 찾는 가장 싼 방법은 **뒤집어 돌려 보는 것**이고, 결과가 달라진 테스트가 있으면 앞선 무언가가 흔적을 남기고 있다는 뜻이다." }],
        code: { c: "for (let i = 0; i < maxTries; i++)\n  if (results[i]) return [true, i + 1];\nreturn [false, maxTries];   // 몇 번 만에 붙었는지 함께 남긴다", cap: "통과만 기록하면 신호를 잃는다" },
        key: ["자는 대신 확인한다", "재시도 횟수를 함께 기록한다", "재기 전에는 고칠 수 없다", "뒤집어 돌리면 오염이 드러난다"] } },

    { t: "회귀와 선별 — 무엇을 언제 돌릴까", n: 5,
      th: { sum: "스위트가 커지면 '전부 돌린다' 가 불가능해진다. 그때부터는 **무엇을 돌릴지 고르는 일**과 **실패를 어떤 순서로 볼지 정하는 일**이 테스트의 절반이다.",
        body: [
          { h: "고르는 만큼 놓친다", t: "바뀐 파일에 닿는 테스트만 고르면 시간이 크게 줄지만, 의존 목록이 **실제보다 좁으면** 돌려야 할 것을 빼먹는다. 설정 파일·환경 변수·데이터 파일처럼 코드가 아닌 의존은 특히 놓치기 쉽다. 그래서 실제로는 두 겹으로 쓴다 — 매 커밋에는 고른 것만, **병합 전이나 밤에는 전부** 돌려 빠른 되먹임과 안전망을 함께 가져간다. 의존 목록은 손으로 적으면 곧 낡으므로, 실행 중 실제로 읽은 파일을 기록해 자동으로 만든다." },
          { h: "실패를 언제 아느냐가 속도다", t: "20분짜리 스위트라도 첫 30초에 알려 주면 그동안 다른 일을 할 수 있다. **최근 실패한 것**은 또 실패할 확률이 높고, 같은 확률이면 **빠른 것**이 먼저다 — 단위 테스트를 통합 테스트보다 먼저 돌리는 관행이 이 계산의 결과다. 전제가 하나 있다: 순서를 바꿔도 결과가 같아야 한다. 성능 회귀도 절대 시간이 아니라 **예전 대비 배수**로 본다. 측정이 늘 1.1배쯤 흔들리므로 허용 배수를 두지 않으면 아무도 안 보는 경고가 된다." }],
        code: { c: "// 우회 불가 먼저, 그 안에서 영향 큰 순\nreturn bugs.slice().sort((a, b) =>\n  (Number(a[2]) - Number(b[2])) || (b[1] - a[1]));", cap: "우선순위는 규칙으로 적어 둔다" },
        key: ["고른 실행에는 밤의 전체 실행을 짝지운다", "최근 실패와 빠른 것을 앞으로", "성능은 배수로 본다", "우회 가능 여부가 사람 수보다 먼저"] } },
  ],
};
