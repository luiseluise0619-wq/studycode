module.exports = {
  track: "security", guide: "🔍", xp: 70,
  unit: "코드 리뷰 — 결함 찾기",
  source: "./rev_security.cjs",
  lessons: [
    { t: "자격 증명과 토큰", n: 4,
      th: { sum: "비밀을 다루는 코드는 **느리게 해싱하고, 예측 불가능하게 만들고, 일정한 시간으로 비교**해야 한다.",
        body: [
          { h: "저장과 비교", t: "빠른 해시는 공격자의 계산 속도를 그대로 무기로 만들고, 솔트가 없으면 같은 비밀번호가 같은 해시가 되어 한 번에 여러 계정이 뚫린다. 비교는 앞에서 끊지 않는 <b>일정 시간 비교</b>로 하고, 없는 계정에서도 같은 시간이 걸리게 맞춘다." },
          { h: "만들고 거두기", t: "토큰은 암호학적 난수로 충분히 길게 만들고 <b>짧게 살며 한 번만</b> 쓰이게 한다. 검증할 때는 허용 알고리즘을 못박아 토큰이 검증 방식을 고르지 못하게 하고, 권한은 토큰의 주장이 아니라 서버가 아는 현재 상태로 판단한다." }],
        code: { c: "await argon2.verify(hash, password)\ncrypto.randomBytes(32).toString('base64url')\njwt.verify(t, key, { algorithms: ['RS256'] })", cap: "주장 대신 사실로 판단한다" },
        key: ["비밀번호는 느린 해시로", "솔트가 없으면 함께 뚫린다", "토큰은 짧게 살고 한 번만", "알고리즘을 못박는다"] } },
    { t: "경계를 넘는 입력", n: 4,
      th: { sum: "사고의 공통점은 하나다 — **입력이 목적지나 구조를 정하게 두었다**.",
        body: [
          { h: "값이 코드가 될 때", t: "쿼리에 값을 이어 붙이면 데이터와 코드의 경계가 사라지고, <code>innerHTML</code> 이나 인라인 이벤트 속성에 문자열을 조립하면 남의 브라우저에서 우리 권한으로 코드가 돈다. 값은 언제나 <b>구조 밖에서</b> 넘긴다." },
          { h: "누가 무엇을 할 수 있는가", t: "화면에서 버튼을 감추는 것은 방어가 아니다 — 서버가 <b>누구인지</b>와 <b>그 자원의 주인인지</b>를 모두 확인해야 한다. 업로드는 이름과 확장자를 믿지 말고 서버가 이름을 정하며, 실행될 수 있는 위치에 두지 않는다." }],
        code: { c: "db.query('… WHERE id = ? AND user_id = ?', [id, me])\nel.textContent = c.body\nsave(randomName() + safeExt(mime))", cap: "입력은 값으로만 다룬다" },
        key: ["값은 구조 밖에서 넘긴다", "출력 맥락에 맞게 이스케이프", "인가는 서버에서", "업로드 이름은 서버가 정한다"] } },
  ],
};
