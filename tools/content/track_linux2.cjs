/* Linux 트랙 후반부 — 유닛 7~12.
   패키지 · systemd · 네트워크 진단 · 로그와 디스크 · 서버 보안 · 상황별 처방.
   오답은 정답만큼 구체적으로 쓴다 — 길이로 찍는 길을 만들지 않기 위해서다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex,p)=>{ const o={t:"input",k,q,a,ex}; if(p) o._p=p; return o; };
const P=(sh,want)=>({sh,want});

const UNITS=[

/* ═══════════════ 유닛 7 ═══════════════ */
{ t:"패키지 — 소프트웨어를 설치한다", l:[

  { t:"패키지 매니저가 대신 해 주는 일", xp:25,
    th:{ sum:"패키지 매니저는 '내려받아 압축을 푸는 도구' 가 아니다. **의존성을 풀고, 서명을 확인하고, 나중에 지울 수 있게 기록을 남기는** 도구다.",
      body:[
        {h:"의존성과 서명", t:"패키지 하나를 설치하면 그것이 필요로 하는 라이브러리까지 함께 계산해 설치한다. 그리고 배포판이 **서명한 저장소**에서만 받으므로, 중간에 바꿔치기된 파일을 걸러 낸다. 인터넷에서 받은 `curl … | sudo bash` 는 이 두 가지가 모두 없다 — 무엇이 설치되는지도, 나중에 어떻게 지울지도 알 수 없다."},
        {h:"update 와 upgrade 는 다르다", t:"`apt update` 는 **어떤 버전이 있는지 목록만** 새로 받는다. 실제로 설치된 것을 올리는 것은 `apt upgrade` 다. 이름이 헷갈려 '`update` 했는데 왜 그대로냐' 는 질문이 끊이지 않는다. `apt list --upgradable` 로 무엇이 올라갈지 먼저 보고, 서버에서는 **보안 업데이트만** 자동으로 적용하는 설정을 많이 쓴다."}],
      code:{ c:"sudo apt update              # 목록만 갱신\napt list --upgradable        # 무엇이 올라가나\nsudo apt upgrade             # 실제로 올린다\napt-cache policy nginx       # 어느 버전이 어디서 오는가", cap:"update 는 목록, upgrade 가 실제 설치" },
      key:["의존성 계산과 서명 확인이 본체","update 는 목록만 갱신한다","무엇이 올라갈지 먼저 본다","curl | bash 는 기록도 검증도 없다"] },
    q:[
      I("설치된 파일","특정 패키지가 설치한 파일 목록을 보는 데비안 계열 명령은? (패키지명 제외)",
        ["dpkg -L","dpkg --listfiles"],
        "정답은 `dpkg -L 패키지` 입니다. 레드햇 계열은 `rpm -ql` 입니다.\n반대로 `dpkg -S 경로` 는 '이 파일은 어느 패키지의 것인가' 를 알려 줍니다. 정체 모를 파일을 만났을 때 첫 단서가 됩니다."),
      C("패키지 매니저의 역할","패키지 매니저가 단순 다운로드와 다른 결정적인 점은?",
        ["압축 해제 속도가 훨씬 빠르다는 점","의존성을 계산하고 서명을 확인하며 설치 기록을 남긴다는 점",
         "인터넷 연결이 아예 되지 않는 곳에서도 그대로 설치할 수 있다는 점","설치 파일 크기를 줄여 준다는 점"],1,
        "**의존성·서명·기록** 셋이 핵심입니다. 그래서 나중에 정확히 되돌릴 수 있습니다.\n`curl … | sudo bash` 는 무엇이 어디에 설치되는지 알 수 없고, 지우는 방법도 스크립트 작성자에게 달려 있습니다.\n부득이 그런 스크립트를 쓸 때는 최소한 먼저 내려받아 **읽어 본 뒤** 실행하는 것이 예의이자 안전입니다."),
      C("update","<code>sudo apt update</code> 만 실행하면 무엇이 달라지나?",
        ["설치되어 있는 모든 패키지가 최신 버전으로 한꺼번에 올라간다","어떤 버전이 있는지 목록만 새로 받아 온다",
         "쓰지 않는 패키지가 자동으로 정리되어 사라진다","커널이 최신 버전으로 교체된다"],1,
        "`update` 는 **목록 갱신**입니다. 실제 설치는 `upgrade` 가 합니다.\n이름 때문에 가장 많이 오해되는 명령이고, dnf·yum 에서는 `update` 가 실제 업그레이드라 더 헷갈립니다.\n서버에서는 `apt list --upgradable` 로 무엇이 바뀔지 먼저 확인하는 습관이 사고를 줄입니다."),
      I("설치","데비안·우분투 계열에서 패키지를 설치하는 명령은? (sudo 제외, 패키지명 제외)",
        ["apt install","apt-get install"],
        "정답은 `apt install` 입니다. 예전의 `apt-get install` 도 그대로 동작합니다.\n`apt` 는 사람이 쓰기 좋게 만든 것이라 진행 표시줄과 색이 붙고, 스크립트에서는 출력이 안정적인 `apt-get` 을 권장합니다."),
      C("배포판 차이","RHEL·CentOS·Fedora 계열에서 쓰는 패키지 매니저는?",
        ["데비안 계열과 똑같이 apt 와 dpkg 를 쓴다","dnf(또는 yum)와 rpm 을 쓴다",
         "pacman 과 makepkg 두 가지를 조합해서 쓴다","zypper 와 snap 만 쓴다"],1,
        "레드햇 계열은 `rpm` 패키지와 `dnf`(예전 `yum`)를 씁니다. 데비안 계열은 `deb` 와 `apt` 입니다.\n설정 파일 위치와 서비스 이름도 조금씩 달라, 문서를 볼 때 **어느 계열인지 먼저 확인**해야 헛수고가 없습니다.\n컨테이너 이미지를 고를 때도 이 차이가 그대로 따라옵니다."),
      C("버전 고정","특정 버전을 유지하고 자동 업그레이드를 막으려면?",
        ["설치 파일을 따로 백업해 두었다가 필요할 때 그것으로 되돌린다","apt-mark hold 로 그 패키지를 고정한다",
         "저장소 주소를 지운다","해당 패키지를 소스에서 직접 다시 빌드한다"],1,
        "`apt-mark hold 패키지` 로 고정하고 `unhold` 로 풉니다.\n특정 버전에 의존하는 애플리케이션이 있을 때 쓰지만, **보안 패치도 함께 막힌다**는 점을 알아야 합니다.\n오래 고정해 둘 것이라면 그 이유와 해제 조건을 어딘가에 적어 두어야 나중에 아무도 손대지 못하는 상태가 되지 않습니다."),
      C("설치 위치","패키지로 설치한 프로그램의 파일이 어디에 갔는지 확인하려면?",
        ["find / -name 프로그램명 으로 시스템 전체를 훑어 본다","dpkg -L 패키지명 으로 파일 목록을 본다",
         "설치할 때 남은 로그를 직접 뒤져 찾는다","확인할 방법이 없다"],1,
        "`dpkg -L`(레드햇 계열은 `rpm -ql`)이 그 패키지가 설치한 **모든 파일 목록**을 보여 줍니다.\n반대로 `dpkg -S 경로` 는 '이 파일은 어느 패키지의 것인가' 를 알려 줍니다 — 정체 모를 파일을 만났을 때 유용합니다.\n패키지 매니저가 기록을 남기기 때문에 가능한 일이고, 손으로 설치한 것에는 이 추적이 되지 않습니다."),
      C("스냅·플랫팩","snap·flatpak 같은 방식이 전통적 패키지와 다른 점은?",
        ["설치 속도가 훨씬 빠르다는 점","의존성을 함께 담아 배포판과 무관하게 같은 버전을 쓸 수 있다는 점",
         "설치가 진행되는 동안에는 인터넷 연결이 전혀 필요하지 않다는 점","루트 권한이 필요 없다는 점"],1,
        "**의존성을 통째로 안고 다니므로** 배포판이 달라도 같은 버전이 돕니다. 대신 용량이 크고 시작이 느릴 수 있습니다.\n샌드박스로 격리되어 보안상 이점이 있지만, 그 때문에 파일 접근이 막혀 '왜 이 폴더를 못 읽지' 하는 문제가 생기기도 합니다.\n서버에서는 여전히 전통적 패키지가 주류이고, 데스크톱 앱에서 더 널리 쓰입니다.")
    ] },

  { t:"직접 빌드와 컨테이너 사이", xp:25,
    th:{ sum:"패키지에 없는 버전이 필요할 때 선택지는 셋이다. **소스 빌드**, **공식 저장소 추가**, **컨테이너**. 각각 비용이 다르다.",
      body:[
        {h:"소스 빌드의 진짜 비용", t:"`./configure && make && make install` 은 한 번은 쉽다. 문제는 **그 다음**이다 — 보안 패치가 나와도 알려 주지 않고, 지우려 해도 어떤 파일이 어디 갔는지 모른다. 꼭 필요하면 `/usr/local` 아래에 두고 `checkinstall` 이나 `stow` 로 **되돌릴 수 있게** 만든다."},
        {h:"공식 저장소를 추가하는 편이 낫다", t:"많은 프로젝트가 자기 apt/yum 저장소를 제공한다. 키를 등록하고 저장소를 추가하면 **최신 버전을 쓰면서도 패키지 매니저의 장점을 유지**할 수 있다. 키를 `/etc/apt/keyrings` 에 두고 그 저장소에만 적용하는 것이 요즘 방식이다 — 예전처럼 전역에 키를 등록하면 그 키로 **아무 패키지나** 서명할 수 있게 된다."}],
      code:{ c:"# 저장소 추가 (키를 그 저장소에만 묶는다)\ncurl -fsSL https://example.com/key.gpg \\\n  | sudo tee /etc/apt/keyrings/x.gpg > /dev/null\necho \"deb [signed-by=/etc/apt/keyrings/x.gpg] https://example.com/apt stable main\" \\\n  | sudo tee /etc/apt/sources.list.d/x.list", cap:"키를 전역이 아니라 그 저장소에만" },
      key:["소스 빌드의 비용은 설치가 아니라 유지","되돌릴 수 있게 만들어 둔다","공식 저장소가 대개 최선","키는 그 저장소에만 묶는다"] },
    q:[
      I("apt 이력 파일","apt 로 무엇을 언제 설치했는지 남는 로그 파일 경로는? (/var 로 시작)",
        ["/var/log/apt/history.log"],
        "정답은 `/var/log/apt/history.log` 입니다.\n장애가 최근 변경 때문인지 판단할 때 가장 먼저 볼 파일 중 하나입니다. 결국 이런 이력을 뒤지지 않아도 되도록 설정을 코드로 관리하는 것이 다음 단계입니다."),
      C("소스 빌드","<code>make install</code> 로 설치했을 때 가장 큰 장기적 문제는?",
        ["소스를 컴파일하느라 설치 과정이 훨씬 오래 걸리게 된다는 점","보안 패치를 알려 주지 않고 깨끗하게 지우기도 어렵다는 점",
         "실행 속도가 느려진다는 점","루트 권한이 필요하다는 점"],1,
        "**유지보수 비용**이 문제입니다. 취약점이 발표되어도 아무도 알려 주지 않고, 어떤 파일이 어디 갔는지 기록도 없습니다.\n1년 뒤 '이 서버에 무엇이 설치되어 있는가' 를 아무도 답하지 못하는 상태가 됩니다.\n부득이하다면 `/usr/local` 에 모으고 버전과 설치 이유를 문서로 남기는 것이 최소한의 대비입니다."),
      C("저장소 키","저장소 키를 <code>signed-by</code> 로 특정 저장소에만 묶는 이유는?",
        ["설치 속도가 빨라지기 때문","전역에 등록하면 그 키로 어떤 패키지든 서명할 수 있게 되기 때문",
         "키 파일 크기를 줄이기 위해서","한 시스템 안에서는 여러 개의 키를 동시에 등록해 쓸 수 없기 때문에"],1,
        "전역 키링에 넣으면 그 키를 가진 쪽이 **시스템의 어떤 패키지든** 서명할 수 있게 됩니다. 신뢰 범위가 필요 이상으로 넓어집니다.\n`signed-by` 로 묶으면 '이 저장소의 패키지만 이 키로 검증' 이 되어 피해 범위가 제한됩니다.\n예전 문서의 `apt-key add` 방식은 이 때문에 폐기되었습니다."),
      C("컨테이너 선택","특정 버전의 런타임이 여러 개 필요할 때 가장 깔끔한 방법은?",
        ["호스트 한 대에 필요한 여러 버전을 함께 설치한다","서비스마다 컨테이너로 격리해 각자의 버전을 담는다",
         "가장 최신 버전 하나로 통일한다","가상 머신을 서비스 수만큼 만든다"],1,
        "컨테이너는 **의존성을 이미지 안에 가둡니다.** 호스트를 어지럽히지 않고 서비스마다 다른 버전을 쓸 수 있습니다.\n호스트에 여러 버전을 설치하면 `PATH` 와 라이브러리 충돌로 결국 무엇이 실행되는지 알 수 없게 됩니다.\n가상 머신은 같은 격리를 주지만 자원 비용이 훨씬 큽니다."),
      I("어느 저장소에서","패키지가 어느 저장소의 어느 버전에서 오는지 확인하는 명령은? (패키지명 제외)",
        ["apt-cache policy","apt policy"],
        "정답은 `apt-cache policy 패키지` 입니다. 설치된 버전과 후보 버전, 그리고 그 출처를 함께 보여 줍니다.\n저장소를 여러 개 추가했을 때 '왜 이 버전이 설치되지' 를 푸는 첫 도구이고, 우선순위(pin) 설정도 여기서 확인합니다."),
      C("자동 보안 업데이트","서버에서 <code>unattended-upgrades</code> 로 보안 패치만 자동 적용하는 이유는?",
        ["관리자가 손으로 직접 해야 할 일을 완전히 없애 버리기 위해서","사람 손을 기다리다 패치가 밀리는 것을 막되 변경 범위는 좁게 유지하려고",
         "디스크 공간을 절약하기 위해서","재부팅을 자동화하기 위해서"],1,
        "패치가 밀리는 가장 큰 이유는 **사람이 바빠서**입니다. 자동화하면 그 지연이 사라집니다.\n대신 전체 업그레이드가 아니라 **보안 저장소만** 대상으로 좁혀야 예상치 못한 동작 변경을 피할 수 있습니다.\n커널 업데이트는 재부팅이 필요하므로, 재부팅 시점은 여전히 사람이 정하는 구성이 흔합니다."),
      C("정리","더 이상 필요 없는 의존 패키지를 정리하는 명령은?",
        ["apt clean 으로 캐시를 비운다","apt autoremove 로 아무도 필요로 하지 않는 패키지를 지운다",
         "apt purge 로 설정까지 지운다","apt update 를 한 번 더 실행해서 전체 패키지 목록을 새로 갱신한다"],1,
        "`autoremove` 는 **자동으로 딸려 왔지만 지금은 아무도 필요로 하지 않는** 패키지를 지웁니다.\n`clean` 은 내려받은 `.deb` 캐시를 비우는 다른 일이고, `purge` 는 특정 패키지의 설정 파일까지 지웁니다.\n`autoremove` 를 돌리기 전에 목록을 확인하는 습관이 좋습니다 — 드물게 필요한 것이 섞여 나오기도 합니다."),
      C("설치 이력","서버에 무엇이 언제 설치되었는지 확인하려면?",
        ["ls -lt /usr/bin 으로 수정 시간 순서대로 정렬해 본다","/var/log/apt/history.log 를 확인한다",
         "dpkg -l 로 목록만 본다","확인할 방법이 없다"],1,
        "`/var/log/apt/history.log` 에 **언제 무엇을 설치·제거했는지** 남습니다. 장애 원인이 최근 변경인지 판단할 때 첫 단서입니다.\n`dpkg -l` 은 지금 무엇이 있는지만 알려 줄 뿐 시점을 모릅니다.\n결국 이런 이력이 필요 없도록 **설정을 코드로 관리**(Ansible·Terraform)하는 것이 다음 단계입니다.")
    ] }
]},

/* ═══════════════ 유닛 8 ═══════════════ */
{ t:"systemd — 서비스로 돌린다", l:[

  { t:"터미널을 닫아도 살아 있으려면", xp:30,
    th:{ sum:"`./app &` 로 띄운 프로그램은 **터미널이 닫히면 함께 죽고, 죽어도 아무도 되살리지 않는다.** 서비스로 등록한다는 것은 그 둘을 시스템에 맡기는 일이다.",
      body:[
        {h:"유닛 파일 하나가 전부", t:"`/etc/systemd/system/앱.service` 에 몇 줄을 적으면 된다. `ExecStart` 로 무엇을 실행할지, `User` 로 어느 계정으로 돌릴지, `Restart=always` 로 죽으면 되살릴지, `WantedBy=multi-user.target` 으로 부팅 시 자동 시작할지를 정한다. 파일을 고친 뒤에는 반드시 **`systemctl daemon-reload`** 로 systemd 에게 다시 읽게 해야 한다."},
        {h:"로그가 저절로 모인다", t:"서비스가 표준 출력에 찍은 것은 **journald 가 모아 준다.** 그래서 앱이 파일 로그를 직접 관리할 필요가 없고, `journalctl -u 앱 -f` 로 어디서든 같은 방식으로 본다. 컨테이너에서 '로그는 표준 출력으로' 라고 하는 관례도 같은 발상이다 — **로그의 수집과 회전은 앱의 일이 아니다.**"}],
      code:{ c:"[Unit]\nDescription=My App\nAfter=network.target\n\n[Service]\nUser=appsvc\nWorkingDirectory=/srv/app\nExecStart=/usr/bin/node server.js\nRestart=always\n\n[Install]\nWantedBy=multi-user.target", cap:"고친 뒤 daemon-reload 를 잊지 않는다" },
      key:["서비스 등록 = 자동 시작 + 자동 재시작","유닛 파일을 고치면 daemon-reload","표준 출력은 journald 가 모은다","enable 은 부팅 시 시작, start 는 지금 시작"] },
    q:[
      I("한 번에 켜기","서비스를 지금 시작하면서 부팅 시 자동 시작도 함께 거는 옵션은? (systemctl enable 뒤 옵션)",
        ["--now"],
        "정답은 `--now` 입니다. `systemctl enable --now 서비스` 로 씁니다.\n반대로 `systemctl disable --now` 는 지금 멈추면서 자동 시작도 함께 해제합니다. 둘을 따로 치다 하나를 빠뜨리는 실수를 없애 줍니다."),
      C("enable 과 start","<code>systemctl enable</code> 과 <code>start</code> 의 차이는?",
        ["enable 은 지금 당장 시작시키고 start 는 부팅하는 때에 시작시킨다","enable 은 부팅 시 자동 시작을 걸고 start 는 지금 실행한다",
         "둘은 이름만 다르고 같은 일을 한다","enable 은 설정만 검사하고 실행하지는 않는다"],1,
        "`enable` 은 **부팅 시 자동 시작 연결**을 만들고, `start` 는 **지금 당장** 띄웁니다.\n둘 다 필요하면 `systemctl enable --now 앱` 으로 한 번에 할 수 있습니다.\n'서버를 재부팅했더니 서비스가 안 떴다' 의 원인은 대부분 `enable` 을 빠뜨린 것입니다."),
      C("daemon-reload","유닛 파일을 고친 뒤 <code>daemon-reload</code> 가 필요한 이유는?",
        ["유닛 파일에 걸린 접근 권한을 처음부터 다시 계산해야 하기 때문에","systemd 가 유닛 파일을 메모리에 읽어 두고 쓰기 때문에",
         "로그를 다시 열어야 하기 때문에","네트워크를 다시 연결해야 하기 때문에"],1,
        "systemd 는 유닛 파일을 **메모리에 읽어 두고** 씁니다. 파일만 고치면 반영되지 않습니다.\n`daemon-reload` 로 다시 읽게 한 뒤 `restart` 해야 새 설정이 적용됩니다.\n'분명 고쳤는데 그대로다' 의 가장 흔한 원인이고, systemd 도 경고로 알려 주지만 놓치기 쉽습니다."),
      I("상태 확인","서비스의 현재 상태와 최근 로그를 함께 보는 명령은? (서비스명 제외)",
        ["systemctl status"],
        "정답은 `systemctl status 서비스` 입니다. 활성 여부, PID, 메모리, 최근 로그 몇 줄을 함께 보여 줍니다.\n서비스가 뜨지 않을 때 가장 먼저 칠 명령이고, 대개 여기 나온 마지막 몇 줄에 원인이 있습니다."),
      C("재시작 정책","<code>Restart=always</code> 를 걸 때 함께 고려해야 할 것은?",
        ["서비스가 계속 남기게 되는 로그 파일의 최대 크기 제한과 보관 기간","시작하자마자 죽는 경우 무한 재시작이 되지 않도록 간격과 횟수를 정하는 것",
         "메모리 사용량 상한","네트워크 대역폭 제한"],1,
        "설정 오류로 즉시 죽는 서비스에 `always` 를 걸면 **초당 수십 번 재시작**하며 로그와 CPU 를 태웁니다.\n`RestartSec=5` 로 간격을 두고, `StartLimitBurst`·`StartLimitIntervalSec` 로 '몇 번 실패하면 포기' 를 정합니다.\n포기한 상태(`failed`)로 두는 것이 무한 재시도보다 낫습니다 — 알림이 울려야 사람이 알아차립니다."),
      C("로그 위치","systemd 서비스가 표준 출력에 찍은 내용은 어디로 가는가?",
        ["/var/log/messages 파일 한 곳에만 기록되어 그대로 남는다","journald 가 수집해 journalctl 로 조회할 수 있다",
         "화면에만 표시되고 사라진다","서비스가 지정한 파일에만 저장된다"],1,
        "journald 가 모으므로 앱이 로그 파일을 직접 관리할 필요가 없습니다. `journalctl -u 앱` 으로 봅니다.\n덕분에 회전·보존 기간·용량 제한을 시스템이 일괄 관리합니다 — 앱마다 다른 로그 설정을 하지 않아도 됩니다.\n다만 기본 설정으로 두면 디스크를 채울 수 있어 `SystemMaxUse` 를 정해 두는 것이 좋습니다."),
      C("의존 순서","<code>After=network.target</code> 이 뜻하는 것은?",
        ["네트워크가 완전히 연결될 때까지 기다렸다가 서비스를 시작한다는 뜻","네트워크 초기화가 시작된 뒤에 이 서비스를 시작한다는 순서 지정",
         "네트워크가 끊기면 서비스를 중지한다는 뜻","네트워크 인터페이스를 직접 설정한다는 뜻"],1,
        "`After` 는 **시작 순서**만 정합니다. 네트워크가 실제로 연결되었음을 보장하지는 않습니다.\n주소 할당까지 기다리려면 `After=network-online.target` 과 `Wants=network-online.target` 을 함께 써야 합니다.\n'부팅 직후에만 실패한다' 는 서비스의 흔한 원인이 이 구분입니다."),
      C("권한 제한","systemd 유닛에서 서비스의 권한을 좁히는 설정으로 적절한 것은?",
        ["User 를 root 로 그대로 두고 권한 제한은 애플리케이션 쪽에서 알아서 처리하도록 맡긴다","User 를 전용 계정으로 두고 ProtectSystem·PrivateTmp 같은 옵션을 켠다",
         "SUID 비트를 실행 파일에 설정한다","파일 권한을 777 로 열어 둔다"],1,
        "systemd 는 **샌드박스 옵션**을 여럿 제공합니다. `ProtectSystem=strict`, `PrivateTmp=yes`, `NoNewPrivileges=yes` 만 켜도 사고 범위가 크게 줄어듭니다.\n컨테이너 없이도 상당한 격리를 얻을 수 있어, 서비스 하나를 위해 도커를 도입하기 전에 먼저 검토할 만합니다.\n`systemd-analyze security 서비스` 로 현재 노출 수준을 점수로 확인할 수 있습니다.")
    ] },

  { t:"타이머와 정기 작업", xp:30,
    th:{ sum:"정기 실행에는 **cron** 과 **systemd timer** 두 갈래가 있다. 간단하면 cron, 서비스와 엮이거나 실패를 추적해야 하면 timer 다.",
      body:[
        {h:"cron 의 함정은 환경이다", t:"cron 은 **로그인 셸이 아니다.** `.bashrc` 를 읽지 않아 `PATH` 가 짧고, 가상환경도 언어 설정도 없다. '터미널에서는 되는데 cron 에서는 안 된다' 는 거의 언제나 이 문제다. 스크립트 안에서 절대 경로를 쓰고, 출력을 파일로 남겨 두면 원인 파악이 빨라진다."},
        {h:"timer 가 주는 것", t:"systemd timer 는 서비스 유닛을 시각에 맞춰 실행한다. **실패가 `systemctl` 상태로 남고**, `journalctl` 로 로그가 모이며, 서버가 꺼져 있어 놓친 실행을 `Persistent=true` 로 따라잡을 수 있다. cron 에는 이 셋이 모두 없다. 대신 설정 파일이 둘(`.timer`+`.service`)이라 손이 더 간다."}],
      code:{ c:"# backup.timer\n[Timer]\nOnCalendar=daily\nPersistent=true      # 꺼져 있어 놓친 실행을 따라잡는다\n\n[Install]\nWantedBy=timers.target\n\n# 확인\nsystemctl list-timers", cap:"놓친 실행을 따라잡는 것이 cron 과의 차이" },
      key:["cron 실패의 대부분은 환경 차이","timer 는 실패가 상태로 남는다","Persistent 로 놓친 실행을 따라잡는다","list-timers 로 다음 실행을 확인"] },
    q:[
      I("사용자 crontab","현재 사용자의 정기 작업 목록을 편집하는 명령은? (옵션 포함)",
        ["crontab -e"],
        "정답은 `crontab -e` 입니다. `-l` 은 목록 보기, `-r` 은 **전체 삭제**입니다.\n`-e` 와 `-r` 이 키보드에서 가까워 실수로 전부 지우는 사고가 실제로 납니다. 편집 전에 `crontab -l > backup.txt` 로 남겨 두는 습관이 안전합니다."),
      C("cron 출력","cron 작업의 출력을 아무 데도 보내지 않으면 어떻게 되나?",
        ["출력이 그냥 버려져 흔적이 남지 않는다","기본적으로 계정 메일로 발송되며, 메일이 설정되지 않았다면 사실상 사라진다",
         "자동으로 /var/log/cron 파일에 기록되므로 나중에 확인할 수 있다","cron 이 실행을 거부하고 오류를 남긴다"],1,
        "cron 은 출력이 있으면 **작업 소유자에게 메일로** 보내려 합니다. 메일이 설정되지 않은 서버가 대부분이라 결국 사라집니다.\n그래서 `>> /var/log/myjob.log 2>&1` 로 **직접 파일에 남기는** 것이 실무의 기본입니다.\n실패했는지조차 모르는 정기 작업은 없는 것보다 나쁩니다 — 하고 있다고 착각하게 만들기 때문입니다."),
      C("timer 의 장점","cron 대신 systemd timer 를 쓰면 얻는 것으로 가장 적절한 것은?",
        ["실행 간격을 초 단위까지 아주 세밀하게 나눠서 정할 수 있다는 점","실패가 서비스 상태로 남고 로그가 journald 에 모인다는 점",
         "루트 권한 없이 실행할 수 있다는 점","설정 파일이 하나로 줄어든다는 점"],1,
        "cron 은 실패해도 **조용합니다.** 메일 설정이 없으면 아무도 모릅니다.\ntimer 는 `systemctl status` 에 실패가 남고 `journalctl -u` 로 로그를 봅니다 — 모니터링에 그대로 연결됩니다.\n설정 파일은 오히려 둘로 늘어나는 것이 단점입니다."),
      I("타이머 목록","등록된 타이머와 다음 실행 시각을 보는 명령은? (systemctl 로 시작)",
        ["systemctl list-timers","systemctl list-timers --all"],
        "정답은 `systemctl list-timers` 입니다. 다음 실행까지 남은 시간과 마지막 실행 시각을 함께 보여 줍니다.\n'이 작업이 정말 돌고 있나' 를 확인하는 가장 빠른 방법이고, 마지막 실행이 한참 전이면 그 자체가 경보입니다."),
      C("놓친 실행","서버가 꺼져 있어 지나간 정기 작업을 다음 부팅 때 실행하려면?",
        ["OnBootSec 를 짧게 설정한다","Persistent=true 를 설정한다","OnCalendar 항목을 여러 번 겹쳐서 적어 둔다","cron 으로 바꾼다"],1,
        "`Persistent=true` 는 마지막 실행 시각을 기록해 두었다가, 놓친 실행이 있으면 **부팅 후 곧바로** 실행합니다.\n노트북이나 자주 껐다 켜는 서버에서 백업 작업이 영영 안 돌던 문제를 해결합니다.\ncron 에는 이 기능이 없어 `anacron` 이라는 별도 도구를 씁니다."),
      C("cron 표기","<code>0 3 * * *</code> 가 뜻하는 실행 시각은?",
        ["3분마다 실행","매일 새벽 3시 정각에 실행","매시 3분에 실행","매월 3일이 되면 한 번 실행"],1,
        "필드 순서는 **분 시 일 월 요일** 입니다. `0 3 * * *` 는 매일 3시 0분입니다.\n`*/3 * * * *` 이면 3분마다이고, 이 둘을 헷갈리는 것이 흔한 실수입니다.\n표현이 헷갈릴 때는 주석으로 사람 말을 함께 적어 두는 것이 서로에게 친절합니다."),
      C("중복 실행","정기 작업이 이전 실행이 끝나기 전에 또 시작되는 것을 막으려면?",
        ["실행 간격을 넉넉하게 늘린다","flock 같은 잠금으로 동시에 하나만 돌게 한다",
         "cron 대신 at 을 쓴다","스크립트가 훨씬 더 빠르게 끝나도록 코드를 고쳐 둔다"],1,
        "`flock -n /var/lock/job.lock 명령` 처럼 **잠금 파일**로 막습니다. 이미 돌고 있으면 새 실행이 즉시 종료됩니다.\n간격을 늘리는 것은 근본 해결이 아닙니다 — 언젠가 그 간격보다 오래 걸리는 날이 옵니다.\nsystemd 서비스는 기본적으로 같은 유닛이 겹쳐 실행되지 않아 이 문제가 덜합니다."),
      C("시간대","서버의 정기 작업 시간대를 정할 때 권장되는 방식은?",
        ["서버 시간대를 현지 시간에 맞춘다","서버는 UTC 로 두고 필요할 때만 변환한다",
         "각 작업마다 필요한 시간대를 하나씩 따로 지정한다","시간대를 신경 쓰지 않는다"],1,
        "서버를 **UTC 로 통일**하면 서머타임과 지역차로 인한 사고가 사라집니다.\n현지 시간으로 두면 서머타임 전환일에 작업이 두 번 돌거나 건너뛰는 일이 실제로 생깁니다.\n사람에게 보여 줄 때만 변환하는 것이 원칙이고, 이는 애플리케이션의 시각 저장에도 똑같이 적용됩니다.")
    ] }
]},

/* ═══════════════ 유닛 9 ═══════════════ */
{ t:"네트워크 진단 — 어디서 막혔나", l:[

  { t:"층을 따라 내려가며 좁힌다", xp:30,
    th:{ sum:"'접속이 안 된다' 는 신고는 층으로 나눠 좁힌다. **이름이 풀리나 → 패킷이 닿나 → 포트가 열렸나 → 응답이 오나.** 순서대로 확인하면 원인은 한 곳에서 걸린다.",
      body:[
        {h:"DNS 인가 연결인가", t:"`ping example.com` 이 '이름을 못 찾음' 이면 DNS 문제이고, IP 로는 되는데 이름으로 안 되면 확실하다. `dig`·`getent hosts` 로 확인한다. 리눅스는 `/etc/hosts` 를 먼저 보고 그 다음 DNS 를 보므로, **누군가 hosts 에 적어 둔 줄** 때문에 벌어지는 사고가 의외로 많다."},
        {h:"열려 있나, 듣고 있나", t:"`ss -ltnp` 로 **서버가 그 포트를 듣고 있는지** 먼저 본다. `127.0.0.1:8080` 으로만 듣고 있으면 바깥에서는 절대 닿지 않는다 — `0.0.0.0` 이어야 한다. 듣고 있는데도 안 되면 방화벽(`ufw`·`iptables`)이나 클라우드 보안 그룹을 본다. `curl -v` 는 연결·TLS·응답을 한 번에 보여 줘 마지막 확인에 좋다."}],
      code:{ c:"getent hosts example.com   # 이름이 풀리나\nss -ltnp | grep 8080       # 듣고 있나 (어느 주소로?)\ncurl -v http://호스트:8080/   # 연결·TLS·응답을 한 번에\nsudo ufw status            # 방화벽이 막고 있나", cap:"127.0.0.1 로만 듣고 있으면 바깥에서 못 닿는다" },
      key:["이름 → 경로 → 포트 → 응답 순서로","0.0.0.0 과 127.0.0.1 은 완전히 다르다","ss -ltnp 로 누가 듣는지 본다","curl -v 는 한 번에 많이 보여 준다"] },
    q:[
      I("DNS 조회","도메인의 DNS 응답을 자세히 확인하는 명령은? (명령어만)",
        ["dig","nslookup","host"],
        "정답은 `dig` 입니다(간단히는 `host`, 예전에는 `nslookup`).\n`dig +short 도메인` 이면 값만 짧게 나옵니다. 시스템이 최종적으로 무엇을 쓰는지는 `/etc/hosts` 까지 고려하는 `getent hosts` 가 더 정확합니다."),
      C("바인드 주소","서비스가 <code>127.0.0.1:8080</code> 으로 듣고 있으면?",
        ["같은 네트워크 안에 있는 다른 서버에서도 문제없이 접속할 수 있다","같은 서버 안에서만 접속할 수 있고 바깥에서는 닿지 않는다",
         "방화벽만 열면 바깥에서도 접속된다","포트 번호를 바꾸면 바깥에서도 접속된다"],1,
        "`127.0.0.1` 은 **자기 자신**만 가리키는 주소입니다. 방화벽을 열어도 바깥에서는 닿지 않습니다.\n바깥에 열려면 `0.0.0.0`(모든 인터페이스)이나 특정 IP 로 바인드해야 합니다.\n반대로 DB 처럼 **바깥에 열리면 안 되는 것**은 일부러 `127.0.0.1` 로 두는 것이 좋은 기본값입니다."),
      I("듣는 포트","어떤 프로세스가 어떤 포트를 듣고 있는지 보는 현대적인 명령은? (옵션 포함)",
        ["ss -ltnp","ss -tlnp","ss -lntp"],
        "정답은 `ss -ltnp` 입니다. l=listening, t=TCP, n=숫자 그대로, p=프로세스.\n예전의 `netstat -ltnp` 와 같은 일을 하며 훨씬 빠릅니다. 프로세스 이름을 보려면 루트 권한이 필요합니다."),
      C("DNS 확인","이름으로는 접속이 안 되는데 IP 로는 되는 상황에서 다음으로 볼 것은?",
        ["호스트 방화벽 규칙과 클라우드 보안 그룹 설정","DNS 응답과 /etc/hosts 에 적힌 줄",
         "서버의 CPU 사용률","디스크 남은 공간"],1,
        "이름만 실패하면 **이름 풀이 단계**의 문제입니다. `dig 도메인` 으로 실제 응답을, `getent hosts 도메인` 으로 시스템이 최종적으로 무엇을 쓰는지 봅니다.\n리눅스는 `/etc/hosts` 를 DNS 보다 먼저 보므로, 예전에 테스트하려고 적어 둔 줄이 원인인 경우가 잦습니다.\n캐시가 남아 있을 수도 있어 `resolvectl flush-caches` 로 비워 보는 것도 방법입니다."),
      C("ping 실패","<code>ping</code> 은 안 되는데 웹 접속은 되는 경우로 가장 흔한 이유는?",
        ["서버가 다운되어 있어서","많은 서버와 방화벽이 ICMP 를 막아 두어서",
         "TCP 포트가 닫혀 있어서","DNS 설정이 잘못되어 도메인 이름이 풀리지 않아서"],1,
        "`ping` 은 **ICMP** 를 쓰는데, 보안상 막아 두는 곳이 많습니다. ping 실패가 곧 서버 다운은 아닙니다.\n그래서 실무에서는 `curl` 이나 `nc -zv 호스트 포트` 로 **실제 쓰는 포트**를 확인합니다.\n반대로 ping 이 되는데 서비스가 안 되면 네트워크 경로는 살아 있고 애플리케이션 쪽 문제입니다."),
      C("경로 추적","패킷이 어디까지 가서 막히는지 확인하려면?",
        ["ping 을 여러 번 반복한다","traceroute 나 mtr 로 홉 단위 경로를 본다",
         "ss 로 연결 목록을 본다","dig 명령을 써서 DNS 응답 내용을 확인한다"],1,
        "`traceroute`(또는 실시간으로 보는 `mtr`)가 **어느 구간에서 멈추는지** 보여 줍니다.\n다만 중간 라우터가 응답을 막으면 별표만 나오므로, 결과를 해석할 때 주의가 필요합니다.\n같은 구간에서 손실률이 높게 나온다면 그 구간을 관리하는 쪽에 문의할 근거가 됩니다."),
      C("연결 확인","특정 호스트의 특정 포트가 열려 있는지 빠르게 확인하려면?",
        ["ping 호스트 로 응답이 오는지 본다","nc -zv 호스트 포트","dig 호스트","ip addr"],1,
        "`nc -zv 호스트 443` 은 연결만 시도하고 끊습니다. `-z` 가 데이터 전송 없이 확인만 한다는 뜻입니다.\n`curl -v telnet://호스트:포트` 나 `timeout 3 bash -c '</dev/tcp/호스트/포트'` 도 같은 일을 합니다.\n방화벽 문제인지 애플리케이션 문제인지 가르는 데 결정적인 정보를 줍니다."),
      C("연결 상태","<code>ss -tan</code> 에서 <code>TIME-WAIT</code> 이 많이 보이면?",
        ["서버 자체에 장애가 발생하고 있다는 확실한 신호다","연결을 먼저 닫은 쪽에 정상적으로 남는 상태이며 대개 문제가 아니다",
         "방화벽이 연결을 끊고 있다","메모리가 부족한 상태다"],1,
        "`TIME-WAIT` 은 **먼저 닫은 쪽**이 늦게 도착할 패킷을 처리하려고 잠시 유지하는 정상 상태입니다.\n짧은 연결을 많이 만드는 서비스라면 자연스럽게 쌓이며, 대개 문제가 아닙니다.\n진짜 문제는 포트 고갈인데, 그때는 연결 재사용(keep-alive)이나 커널 파라미터 조정을 검토합니다.")
    ] },

  { t:"방화벽과 포트", xp:30,
    th:{ sum:"서버 보안의 첫 줄은 **열려 있는 포트를 줄이는 것**이다. 열지 않은 것은 공격당하지 않는다.",
      body:[
        {h:"기본은 전부 막고 필요한 것만", t:"`ufw default deny incoming` 으로 **들어오는 것을 기본 차단**하고 필요한 포트만 연다. 나가는 것은 대개 허용한다. 규칙을 넣는 순서가 중요한데, SSH 를 열기 전에 방화벽을 켜면 **자기 자신이 잠긴다** — 원격 서버에서 가장 흔한 자책골이다."},
        {h:"클라우드에서는 두 겹이다", t:"클라우드 인스턴스는 **보안 그룹**(클라우드 쪽)과 **호스트 방화벽**(OS 쪽) 두 겹을 지난다. 둘 중 하나만 열려 있으면 통하지 않는다. '분명 열었는데 안 된다' 의 절반은 다른 한 겹을 잊은 것이다. 어느 쪽에서 막혔는지는 서버에서 `ss` 로 듣는지 확인한 뒤, 바깥에서 `nc -zv` 로 확인하면 갈린다."}],
      code:{ c:"sudo ufw default deny incoming\nsudo ufw allow 22/tcp        # SSH 를 먼저 연다\nsudo ufw allow 443/tcp\nsudo ufw enable              # 그 다음에 켠다\nsudo ufw status numbered", cap:"SSH 를 열기 전에 켜면 스스로 잠긴다" },
      key:["기본 차단, 필요한 것만 연다","방화벽을 켜기 전에 SSH 부터","클라우드는 보안 그룹까지 두 겹","열지 않은 포트는 공격당하지 않는다"] },
    q:[
      I("방화벽 켜기","ufw 방화벽을 활성화하는 명령은? (sudo 제외)",
        ["ufw enable"],
        "정답은 `ufw enable` 입니다.\n원격 서버에서는 **SSH 허용 규칙을 먼저 넣은 뒤에** 실행해야 합니다. 순서를 바꾸면 그 순간 연결이 끊기고 다시 들어갈 수 없게 됩니다."),
      C("순서","원격 서버에서 방화벽을 켤 때 반드시 먼저 해야 할 일은?",
        ["돌고 있는 모든 서비스를 미리 중지시켜 둔다","SSH 포트를 허용 규칙에 넣는다",
         "서버를 재부팅한다","루트 로그인을 활성화한다"],1,
        "SSH 를 허용하지 않고 방화벽을 켜면 **그 순간 연결이 끊기고 다시 들어갈 수 없습니다.**\n콘솔 접근 수단이 없으면 인스턴스를 다시 만들어야 하는 상황까지 갑니다.\n안전을 위해 '몇 분 뒤 방화벽을 끄는' 예약 작업을 미리 걸어 두고 작업하는 방법도 널리 쓰입니다."),
      C("기본 정책","방화벽 기본 정책으로 권장되는 조합은?",
        ["들어오는 것과 나가는 것 모두 허용","들어오는 것은 기본 차단, 나가는 것은 허용",
         "들어오는 것은 전부 허용하고 나가는 것만 차단","모든 트래픽을 차단"],1,
        "**들어오는 것을 기본 차단**하고 필요한 포트만 여는 것이 표준입니다. 열지 않은 것은 공격 대상이 되지 않습니다.\n나가는 것까지 막으면 패키지 설치와 외부 API 호출이 전부 막혀 운영이 어려워집니다.\n다만 보안 등급이 높은 환경에서는 나가는 것도 화이트리스트로 제한합니다 — 침해 후 데이터 유출을 막기 위해서입니다."),
      I("규칙 확인","ufw 의 현재 규칙을 번호와 함께 보는 명령은? (sudo 제외)",
        ["ufw status numbered","ufw status"],
        "정답은 `ufw status numbered` 입니다. 번호가 붙어 있어야 `ufw delete 3` 처럼 특정 규칙만 지울 수 있습니다.\n규칙이 쌓이면 무엇이 왜 열려 있는지 알 수 없게 되므로, 정기적으로 훑어 쓰지 않는 것을 닫는 것이 좋습니다."),
      C("두 겹","클라우드 인스턴스에서 포트를 열었는데도 접속이 안 되는 흔한 이유는?",
        ["인스턴스를 재부팅하지 않아서","보안 그룹과 호스트 방화벽 중 한쪽만 열려 있어서",
         "지정한 포트 번호가 1024 보다 작은 값이라서","DNS 가 아직 전파되지 않아서"],1,
        "클라우드는 **보안 그룹**과 **OS 방화벽** 두 겹을 지납니다. 둘 다 열려야 통합니다.\n어느 쪽인지 가르려면 서버 안에서 `curl localhost:포트` 로 먼저 확인합니다 — 되면 애플리케이션은 정상이고 네트워크 층의 문제입니다.\n그다음 `ss -ltnp` 로 바인드 주소를 확인하면 세 후보 중 하나로 좁혀집니다."),
      C("특권 포트","1024 미만 포트를 일반 사용자가 열 수 없는 이유는?",
        ["기술적 제약으로 불가능하기 때문에","잘 알려진 포트를 아무나 가로채지 못하게 하는 오랜 보호 장치이기 때문에",
         "커널이 그 포트 범위를 통째로 미리 예약해 두었기 때문에","방화벽이 기본으로 막기 때문에"],1,
        "80·443 같은 **잘 알려진 포트**를 일반 사용자가 선점해 가짜 서비스를 띄우지 못하게 하는 장치입니다.\n그래서 앱은 8080 에서 돌리고 앞에 nginx 를 두거나, `CAP_NET_BIND_SERVICE` 능력만 부여하는 방식을 씁니다.\n루트로 서비스를 돌리는 것보다 이쪽이 훨씬 안전합니다."),
      C("포트 스캔","자기 서버에 열린 포트를 바깥에서 점검하려면?",
        ["서버 안에서 ss 명령으로 한 번 확인해 보면 충분하다","다른 호스트에서 nmap 등으로 스캔해 실제 노출을 확인한다",
         "방화벽 설정 파일만 읽어 본다","클라우드 콘솔의 설명을 믿는다"],1,
        "서버 안에서 보는 것과 **바깥에서 보이는 것**은 다릅니다. 방화벽·NAT·보안 그룹이 사이에 있기 때문입니다.\n실제 노출을 확인하려면 외부에서 스캔해 봐야 합니다. 자기 소유 서버에 한정해야 하고, 클라우드 제공자의 정책도 확인해야 합니다.\n'열려 있다고 생각한 것' 과 '실제로 열린 것' 의 차이가 사고의 출발점인 경우가 많습니다."),
      C("SSH 포트 변경","SSH 포트를 22 에서 다른 번호로 바꾸는 조치의 효과는?",
        ["침입을 확실하게 막아 주는 가장 근본적인 보안 대책이다","자동 스캔 로그를 줄여 주지만 그 자체가 보안 대책은 아니다",
         "아무 효과가 없어 하지 말아야 한다","성능이 향상되는 부수 효과가 있다"],1,
        "무차별 스캔 봇의 로그를 줄여 주는 **소음 감소** 효과는 분명합니다. 다만 포트 스캔 한 번이면 드러나므로 방어라 부르기는 어렵습니다.\n진짜 대책은 **비밀번호 로그인 비활성화와 키 인증**, 그리고 fail2ban 같은 차단입니다.\n'숨기는 것' 과 '막는 것' 을 구분하는 것이 보안 사고의 기본입니다.")
    ] }
]},

/* ═══════════════ 유닛 10 ═══════════════ */
{ t:"로그와 디스크 — 서버를 죽이는 흔한 원인", l:[

  { t:"디스크가 차면 전부 멈춘다", xp:30,
    th:{ sum:"서버 장애의 놀랄 만큼 흔한 원인은 **디스크가 꽉 찬 것**이다. 로그를 못 쓰고, 임시 파일을 못 만들고, DB 가 쓰기를 거부한다.",
      body:[
        {h:"공간만이 아니라 inode 도 찬다", t:"`df -h` 는 넉넉한데 '공간이 없다' 는 오류가 나면 **inode** 를 의심한다. `df -i` 로 확인한다. 작은 파일을 수백만 개 만드는 세션 저장소나 캐시에서 실제로 벌어진다. 파일 개수에도 상한이 있다는 사실을 모르면 원인을 영영 못 찾는다."},
        {h:"지운 파일이 공간을 돌려주지 않는 경우", t:"프로세스가 **열고 있는 파일**을 지우면 이름은 사라지지만 공간은 그대로다. 그래서 `du` 와 `df` 의 값이 어긋난다. `lsof +L1` 로 그런 파일을 찾아, 해당 프로세스를 재시작하거나 로그를 **비우는(`: > file`)** 방식으로 처리한다. 로그 파일을 `rm` 하지 않고 회전시키는 이유가 여기 있다."}],
      code:{ c:"df -h            # 공간\ndf -i            # inode (파일 개수)\ndu -sh /var/* | sort -h\nsudo lsof +L1    # 지웠는데 열려 있는 파일", cap:"du 와 df 가 어긋나면 열린 채 지워진 파일" },
      key:["공간과 inode 는 따로 찬다","열린 채 지운 파일은 공간을 안 돌려준다","로그는 rm 이 아니라 회전시킨다","/var 가 가장 흔한 범인"] },
    q:[
      I("열린 채 지워진 파일","지워졌지만 프로세스가 열고 있는 파일을 찾는 명령은? (옵션 포함)",
        ["lsof +L1","sudo lsof +L1"],
        "정답은 `lsof +L1` 입니다. 링크 수가 1보다 작은, 즉 이름이 사라진 파일을 찾습니다.\n`du` 와 `df` 의 값이 어긋날 때의 정답인 경우가 많습니다. 해당 프로세스를 재시작하면 공간이 즉시 돌아옵니다."),
      C("inode","<code>df -h</code> 는 여유가 있는데 파일을 만들 수 없다면 무엇을 의심하나?",
        ["파일 시스템이 읽기 전용으로 바뀌었는지","inode 가 소진되었는지를 df -i 로 확인한다",
         "메모리가 부족한지 free 로 확인한다","CPU 부하가 지나치게 높은 상태인지 top 으로 확인한다"],1,
        "파일 시스템은 **공간과 파일 개수(inode)를 따로** 관리합니다. 작은 파일이 수백만 개면 공간이 남아도 더 못 만듭니다.\n세션 파일, 캐시 조각, 메일 큐에서 실제로 벌어집니다.\n읽기 전용 전환도 가능한 원인이라 `mount | grep ro` 로 함께 확인하면 좋습니다."),
      C("du 와 df","<code>du</code> 로 센 크기보다 <code>df</code> 의 사용량이 훨씬 크다면?",
        ["파일 시스템이 손상된 것이다","프로세스가 열고 있는 파일이 지워져 공간이 반환되지 않은 것이다",
         "du 명령이 숨김 파일을 제대로 세지 못하고 빠뜨린 것이다","스왑 파일이 공간을 차지한 것이다"],1,
        "지워진 파일이라도 **프로세스가 열고 있으면** 공간이 유지됩니다. `du` 는 이름이 없어 못 세고 `df` 는 셉니다.\n`lsof +L1` 로 찾아 해당 프로세스를 재시작하면 공간이 즉시 돌아옵니다.\n로그 파일을 `rm` 한 뒤 '왜 공간이 안 늘지' 하는 상황의 정확한 설명입니다."),
      I("inode 확인","inode 사용량을 확인하는 명령은? (df 와 옵션)",
        ["df -i","df -ih"],
        "정답은 `df -i` 입니다. `IUse%` 가 100% 면 공간과 무관하게 파일을 더 만들 수 없습니다.\n어느 폴더가 파일을 많이 갖고 있는지는 `find /path -xdev -printf '%h\\n' | sort | uniq -c | sort -rn` 으로 좁힐 수 있습니다."),
      C("로그 회전","로그 파일을 <code>rm</code> 하지 않고 회전(rotate)시키는 이유는?",
        ["회전 방식이 삭제보다 더 빠르게 처리되기 때문에","프로세스가 열고 있는 파일을 지우면 공간이 반환되지 않고 기록도 끊기기 때문에",
         "삭제 권한이 필요하기 때문에","백업 정책에 어긋나기 때문에"],1,
        "쓰고 있는 파일을 지우면 **공간은 그대로이고** 애플리케이션은 사라진 파일에 계속 씁니다.\n`logrotate` 는 파일을 옮긴 뒤 프로세스에 신호를 보내 **새 파일을 열게** 합니다(`copytruncate` 방식도 있습니다).\n로그 회전 설정이 없는 서비스는 언젠가 반드시 디스크를 채웁니다."),
      C("journald 용량","systemd 로그가 디스크를 채우는 것을 막으려면?",
        ["로그를 완전히 끈다","journald 설정에서 SystemMaxUse 로 상한을 정한다",
         "매일 수동으로 삭제한다","서비스를 자주 재시작해서 로그를 처음부터 새로 시작한다"],1,
        "`/etc/systemd/journald.conf` 의 `SystemMaxUse=500M` 처럼 상한을 정하면 그 안에서 오래된 것부터 지워집니다.\n`journalctl --vacuum-time=7d` 로 즉시 정리할 수도 있습니다.\n로그를 끄는 것은 장애 대응 능력을 버리는 것이라 거의 언제나 잘못된 선택입니다."),
      C("모니터링","디스크가 차기 전에 알아채려면 무엇을 걸어 두어야 하나?",
        ["매일 사람이 직접 df 를 확인하도록 정해 둔 일정","사용률이 임계값을 넘으면 알리는 모니터링과 경보",
         "디스크를 두 배로 늘려 두는 것","로그를 아예 남기지 않는 설정"],1,
        "사람의 규율은 바쁠 때 가장 먼저 무너집니다. **80% 에서 경보**를 걸어 두면 대응할 시간이 생깁니다.\n용량을 늘리는 것은 시간을 벌 뿐 원인(무한히 쌓이는 무언가)을 해결하지 않습니다.\n증가 속도를 함께 보면 '며칠 뒤 찬다' 는 예측까지 가능해집니다."),
      C("임시 파일","<code>/tmp</code> 를 tmpfs 로 두면 생기는 특징은?",
        ["디스크에 그대로 저장되므로 재부팅한 뒤에도 남아 있다","메모리에 올라가 빠르지만 재부팅하면 사라지고 메모리를 차지한다",
         "쓰기 권한이 루트로 제한된다","파일 크기 제한이 없어진다"],1,
        "`tmpfs` 는 **메모리 기반**이라 빠르고 재부팅하면 비워집니다. 임시 파일의 성격에 잘 맞습니다.\n대신 큰 파일을 쓰면 메모리를 잡아먹어 OOM 을 부를 수 있으므로 크기 상한을 정합니다.\n큰 임시 파일을 다루는 작업이라면 `/var/tmp` 처럼 디스크 기반 경로를 쓰는 것이 맞습니다.")
    ] },

  { t:"로그를 읽는 법", xp:30,
    th:{ sum:"로그는 **많이 남기는 것보다 찾을 수 있게 남기는 것**이 중요하다. 장애 상황에서 필요한 것은 전부가 아니라 그 요청 하나의 흔적이다.",
      body:[
        {h:"시간과 범위를 먼저 좁힌다", t:"`journalctl --since '10 min ago' -u 서비스` 처럼 **시간과 대상**을 먼저 좁힌다. 전체를 grep 하면 관련 없는 것이 섞여 판단이 흐려진다. 여러 서비스가 얽힌 문제라면 시간만 좁혀 **전체를 시간순으로** 보는 것이 오히려 낫다 — 원인과 결과의 순서가 보인다."},
        {h:"구조화된 로그가 이긴다", t:"사람이 읽기 좋은 문장보다 **JSON 한 줄**이 낫다. `jq` 로 거르고 셀 수 있고, 수집기에 넣으면 그대로 검색된다. 무엇보다 **요청 ID** 를 모든 줄에 붙이면 분산된 서비스에서도 한 요청의 흐름을 이어 볼 수 있다. 로그 설계에서 가장 값어치 있는 한 필드다."}],
      code:{ c:"journalctl -u app --since '10 min ago' -f\njournalctl --since '10:00' --until '10:05'   # 시간만 좁혀 전체를\n\n# 구조화된 로그라면\ntail -f app.log | jq 'select(.level==\"error\") | {t:.time, m:.msg, r:.req_id}'", cap:"요청 ID 하나가 추적을 가능하게 한다" },
      key:["시간과 대상을 먼저 좁힌다","얽힌 문제는 시간순 전체가 낫다","구조화된 로그가 검색을 가능하게 한다","요청 ID 는 가장 값어치 있는 필드"] },
    q:[
      I("로그 정리","journald 로그를 7일치만 남기고 정리하는 옵션은? (journalctl 뒤 옵션 전체)",
        ["--vacuum-time=7d"],
        "정답은 `journalctl --vacuum-time=7d` 입니다. 용량 기준으로는 `--vacuum-size=500M` 입니다.\n근본적으로는 `journald.conf` 의 `SystemMaxUse` 로 상한을 정해 두어, 손으로 정리할 일이 없게 만드는 것이 낫습니다."),
      C("좁히기","장애가 난 시각을 알 때 로그를 보는 가장 효율적인 순서는?",
        ["전체 로그를 처음부터 끝까지 하나씩 차례로 읽는다","시간 범위를 좁힌 뒤 서비스별로 나눠 본다",
         "오류 문자열만 grep 해서 본다","가장 큰 로그 파일부터 연다"],1,
        "**시간으로 먼저 좁히는 것**이 언제나 이깁니다. 관련 없는 잡음이 사라져 인과 순서가 보입니다.\n오류 문자열만 보면 원인이 아니라 결과만 보게 됩니다 — 진짜 원인은 대개 그 몇 줄 앞에 경고로 남아 있습니다.\n여러 서비스가 얽혔다면 서비스로 나누지 말고 시간순 전체를 보는 편이 낫습니다."),
      C("요청 ID","분산된 서비스에서 한 요청의 흐름을 이어 보려면 로그에 무엇이 필요한가?",
        ["정확한 타임스탬프만 남아 있다면 그것만으로 충분하다","모든 서비스가 같은 요청 ID 를 함께 기록해야 한다",
         "로그 파일을 하나로 합치면 된다","서비스 이름만 남기면 된다"],1,
        "**요청 ID**(trace id)를 앞단에서 만들어 모든 하위 호출에 전달하고, 각 서비스가 로그에 함께 남깁니다.\n타임스탬프만으로는 동시에 들어온 수천 건 중 어느 것인지 가릴 수 없습니다.\n이 한 필드가 있으면 '이 사용자의 이 요청' 을 서비스 경계를 넘어 따라갈 수 있어, 분산 시스템 디버깅의 출발점이 됩니다."),
      I("실시간 추적","특정 서비스의 로그를 실시간으로 따라가는 옵션은? (journalctl 뒤 옵션 하나)",
        ["-f","--follow"],
        "정답은 `-f` 입니다. `journalctl -u nginx -f` 로 씁니다.\n`tail -f` 와 같은 감각이지만 서비스 단위로 필터링된다는 점이 다릅니다. 재현 가능한 문제라면 이걸 켜 두고 재현하는 것이 가장 빠릅니다."),
      C("구조화","로그를 JSON 한 줄로 남기는 방식의 이점은?",
        ["파일 크기가 줄어든다는 점","기계가 필드를 정확히 뽑을 수 있어 필터링과 집계가 쉬워진다는 점",
         "사람이 읽기 더 편하다는 점","저장하는 시점에 암호화가 자동으로 적용된다는 점"],1,
        "**구조가 있으면 도구가 정확히 다룹니다.** `jq` 로 거르고, 수집기가 필드별로 색인합니다.\n사람이 읽기에는 오히려 불편해서, 개발 환경에서는 예쁘게 출력하고 운영에서는 JSON 으로 내보내는 구성이 흔합니다.\n메시지 안에 값을 문장으로 섞으면 정규식으로 다시 파싱해야 하는데, 그 정규식은 언젠가 반드시 깨집니다."),
      C("로그 수준","운영 환경에서 <code>DEBUG</code> 수준을 기본으로 켜 두면 생기는 문제는?",
        ["로그가 아예 기록되지 않는다","디스크와 I/O 를 소모하고 정작 중요한 줄이 묻힌다",
         "애플리케이션이 크게 느려져서 아예 동작하지 않는다","보안 검사에서 자동으로 차단된다"],1,
        "양이 많아지면 **찾는 비용**이 올라갑니다. 중요한 경고가 잡음에 묻히는 것이 더 큰 문제입니다.\n디스크와 I/O 소모도 실제로 서비스 성능에 영향을 줍니다.\n필요할 때만 특정 모듈의 수준을 올릴 수 있게 만들어 두는 것이 좋은 설계입니다."),
      C("민감 정보","로그에 절대 남기지 말아야 할 것으로 가장 적절한 것은?",
        ["요청한 경로와 그 응답에 걸린 처리 시간","비밀번호·토큰·주민번호 같은 인증 정보와 개인정보",
         "오류 스택 트레이스","서비스 이름과 버전"],1,
        "로그는 **여러 사람이 보고 오래 보관되며 외부 도구로 전송**됩니다. 비밀이 여기 들어가면 통제 범위를 벗어납니다.\n실수로 남았다면 로그 저장소에서 지우는 것만으로 끝나지 않고, 해당 토큰을 폐기해야 합니다.\n요청 본문을 통째로 찍는 미들웨어가 가장 흔한 유출 경로이므로, 마스킹 규칙을 먼저 넣어야 합니다."),
      C("보관 기간","로그 보관 기간을 정할 때 고려할 것으로 가장 적절한 것은?",
        ["디스크 용량이 허락하는 한 최대한 오래 보관해 둔다","장애 조사에 필요한 기간과 법적 요구, 개인정보 보관 원칙을 함께 본다",
         "일주일이면 어떤 경우에도 충분하다","보관하지 않고 즉시 삭제한다"],1,
        "기간은 **필요와 위험의 균형**입니다. 너무 짧으면 조사할 수 없고, 너무 길면 유출 시 피해가 커지고 비용도 늡니다.\n금융·의료처럼 법으로 기간이 정해진 영역도 있고, 개인정보는 '목적을 달성하면 파기' 가 원칙입니다.\n최근 것은 빠른 저장소에, 오래된 것은 값싼 저장소로 옮기는 계층 구성이 실무적인 답입니다.")
    ] }
]},

/* ═══════════════ 유닛 11 ═══════════════ */
{ t:"서버 보안 기초 — 최소한의 방어선", l:[

  { t:"SSH 를 단단하게", xp:30,
    th:{ sum:"공개된 서버의 SSH 포트에는 **하루에도 수천 번** 자동 로그인 시도가 들어온다. 비밀번호 로그인을 켜 둔 서버는 시간 문제일 뿐이다.",
      body:[
        {h:"비밀번호를 끄고 키만 남긴다", t:"`PasswordAuthentication no` 와 `PermitRootLogin no` 두 줄이 방어의 대부분이다. 키 인증만 남기면 무차별 대입은 통하지 않는다. **끄기 전에 반드시 키로 접속이 되는지 확인**해야 하는데, 순서를 바꾸면 자기 자신이 잠긴다 — 다른 터미널을 하나 열어 둔 채 작업하는 것이 관례다."},
        {h:"막는 것과 숨기는 것", t:"포트를 22에서 바꾸면 로그의 소음이 줄지만 **방어는 아니다**. `fail2ban` 처럼 실패가 반복되는 IP 를 일정 시간 차단하는 것이 실질적이다. 그 위에 **접속 가능한 IP 를 제한**하면 훨씬 낫고, 더 나아가 SSH 를 공개 인터넷에 열지 않고 VPN·배스천 뒤에 두는 것이 가장 확실하다."}],
      code:{ c:"# /etc/ssh/sshd_config\nPasswordAuthentication no\nPermitRootLogin no\nPubkeyAuthentication yes\n\nsudo sshd -t                    # 문법 먼저 검사\nsudo systemctl reload sshd      # 그 다음 반영", cap:"반영 전에 sshd -t 로 문법을 검사한다" },
      key:["비밀번호 끄고 키만","끄기 전에 키 접속을 먼저 확인","포트 변경은 소음 감소이지 방어가 아니다","reload 전에 sshd -t"] },
    q:[
      I("공개키 등록 파일","서버에서 접속을 허용할 공개키를 적어 두는 파일 경로는? (~ 로 시작)",
        ["~/.ssh/authorized_keys"],
        "정답은 `~/.ssh/authorized_keys` 입니다. 한 줄에 공개키 하나씩 들어갑니다.\n퇴사자의 접근을 끊으려면 **모든 서버에서** 이 줄을 지워야 합니다. 서버가 여러 대라면 반드시 빠지는 곳이 생기므로 중앙 관리가 필요해집니다."),
      C("가장 효과적인 조치","공개 서버의 SSH 를 지키는 가장 효과적인 조치는?",
        ["SSH 포트를 22에서 다른 번호로 바꿔 둔다","비밀번호 인증을 끄고 공개키 인증만 허용한다",
         "로그인 배너에 경고 문구를 넣는다","접속 로그를 자주 확인한다"],1,
        "무차별 대입은 **비밀번호가 없으면 성립하지 않습니다.** 키 인증만 남기는 것이 근본적인 차단입니다.\n포트 변경은 로그 소음을 줄일 뿐이고, 배너와 로그 확인은 사후 대응입니다.\n키를 잃어버렸을 때의 복구 수단(콘솔 접근·백업 키)을 미리 마련해 두는 것이 함께 필요합니다."),
      C("작업 순서","<code>PasswordAuthentication no</code> 를 적용하기 전에 반드시 할 일은?",
        ["서버를 재부팅해서 설정을 처음 상태로 되돌린다","다른 터미널에서 키로 접속이 되는지 확인한다",
         "루트 비밀번호를 새로 설정한다","방화벽을 잠시 끈다"],1,
        "**지금 연결이 살아 있을 때** 새 방식으로 접속되는지 확인해야 합니다. 안 되면 되돌릴 수 있습니다.\n확인 없이 적용하고 연결을 끊으면 다시 들어갈 방법이 없어질 수 있습니다.\n클라우드라면 콘솔 접속이나 복구 모드가 있지만, 없는 환경도 많으므로 습관으로 굳히는 편이 안전합니다."),
      I("문법 검사","sshd 설정 파일의 문법을 적용 전에 검사하는 명령은? (sudo 제외)",
        ["sshd -t","/usr/sbin/sshd -t"],
        "정답은 `sshd -t` 입니다. 문법 오류가 있으면 알려 주고, 없으면 조용히 끝납니다.\n오타가 든 설정으로 `restart` 하면 sshd 가 아예 뜨지 않아 접속이 막힙니다. 이 한 줄이 그 사고를 막습니다."),
      C("fail2ban","<code>fail2ban</code> 이 하는 일은?",
        ["모든 외부 접속을 기본으로 차단한다","로그에서 반복 실패를 감지해 해당 IP 를 일정 시간 차단한다",
         "계정 비밀번호를 주기적으로 더 복잡한 것으로 바꿔 준다","SSH 키를 주기적으로 교체한다"],1,
        "로그를 감시하다 **실패가 반복되는 IP** 를 방화벽 규칙으로 잠시 막습니다.\nSSH 뿐 아니라 웹 로그인, 메일 서버에도 적용할 수 있습니다.\n주의할 점은 오탐입니다 — 사무실 공용 IP 가 막히면 팀 전체가 못 들어오므로 화이트리스트를 함께 설정합니다."),
      C("근본 대책","SSH 노출을 줄이는 가장 확실한 구성은?",
        ["포트를 자주 바꿔 가며 운영한다","VPN 이나 배스천 호스트 뒤에 두고 공개 인터넷에 열지 않는다",
         "접속이 필요할 때마다 방화벽을 잠깐 열었다 다시 닫는다","로그인 시도 횟수 제한만 건다"],1,
        "**아예 노출하지 않는 것**이 가장 확실합니다. 공격자가 닿을 수 없으면 취약점이 있어도 소용이 없습니다.\n배스천(점프 호스트) 하나만 노출하고 나머지는 내부망에 두는 구성이 표준입니다.\n요즘은 SSM·IAP 처럼 SSH 포트를 아예 열지 않고 접속하는 클라우드 서비스도 널리 쓰입니다."),
      C("키 관리","팀원이 퇴사했을 때 SSH 접근을 확실히 차단하려면?",
        ["그 사람이 쓰던 계정의 비밀번호를 새로 변경해 둔다","모든 서버의 authorized_keys 에서 해당 공개키를 제거한다",
         "SSH 포트를 변경한다","서버를 재부팅한다"],1,
        "SSH 키 인증은 **서버에 등록된 공개키**로 이루어집니다. 그 줄이 남아 있으면 계속 들어올 수 있습니다.\n서버가 여러 대면 손으로 지우는 것은 반드시 빠지는 곳이 생기므로, 설정 관리 도구나 중앙 인증(LDAP·SSO)으로 관리해야 합니다.\n'누가 어느 서버에 들어갈 수 있는가' 를 즉시 답할 수 없다면 그 자체가 문제입니다."),
      C("에이전트 포워딩","<code>ssh -A</code>(에이전트 포워딩)를 조심해야 하는 이유는?",
        ["접속 속도가 크게 느려지기 때문에","접속한 서버의 루트가 내 키로 다른 서버에 접속할 수 있기 때문에",
         "키가 서버에 파일로 복사되기 때문에","접속하는 순간 방화벽 규칙이 자동으로 바뀌기 때문에"],1,
        "포워딩된 에이전트는 그 서버에서 **내 키로 서명을 요청**할 수 있습니다. 서버 관리자나 침입자가 이를 이용해 내 권한으로 다른 서버에 들어갈 수 있습니다.\n키 파일이 복사되지는 않지만, 접속해 있는 동안 사실상 키를 빌려 준 상태입니다.\n대안으로 `ProxyJump`(`-J`)를 쓰면 중간 서버에 에이전트를 노출하지 않고 목적지까지 갈 수 있습니다.")
    ] },

  { t:"최소 노출과 정기 점검", xp:30,
    th:{ sum:"서버 보안은 한 번 설정하고 끝나는 일이 아니다. **노출을 줄이고, 패치를 따라가고, 이상을 알아채는** 세 가지를 계속한다.",
      body:[
        {h:"안 쓰는 것을 끈다", t:"설치되어 있지만 쓰지 않는 서비스는 **순수한 위험**이다. 열린 포트, 켜진 데몬, 남아 있는 계정을 주기적으로 훑어 줄인다. 공격 표면을 줄이는 것은 패치를 따라가는 것보다 값싸고 확실하다 — 없는 것은 취약할 수 없다."},
        {h:"알아채는 능력", t:"완벽한 방어는 없으므로 **침해를 빨리 알아채는 것**이 다음 방어선이다. 로그인 성공·실패, sudo 사용, 파일 변경, 나가는 연결의 이상 패턴을 본다. 무엇보다 **정상이 어떤 모습인지** 알아야 이상이 보이므로, 평소의 지표를 기록해 두는 것이 그 자체로 보안 활동이다."}],
      code:{ c:"systemctl list-units --type=service --state=running\nss -ltnp                  # 열린 포트\nlastb | head              # 실패한 로그인 시도\nsudo grep sudo /var/log/auth.log | tail", cap:"안 쓰는 것을 끄는 것이 가장 값싸다" },
      key:["안 쓰는 서비스는 순수한 위험","공격 표면을 줄이는 것이 가장 값싸다","침해를 알아채는 능력이 다음 방어선","정상을 알아야 이상이 보인다"] },
    q:[
      I("돌고 있는 서비스","systemd 에서 지금 실행 중인 서비스만 보는 명령은? (systemctl 뒤 전부)",
        ["list-units --type=service --state=running","systemctl list-units --type=service --state=running"],
        "정답은 `systemctl list-units --type=service --state=running` 입니다.\n'이 서버에서 무엇이 돌고 있는가' 를 답할 수 없다면 그 자체가 문제입니다. 안 쓰는 서비스를 끄는 것이 공격 표면을 줄이는 가장 값싼 방법입니다."),
      C("가장 값싼 대책","서버의 공격 표면을 줄이는 가장 값싸고 확실한 방법은?",
        ["최신 침입 탐지 시스템을 도입한다","쓰지 않는 서비스와 포트를 끄고 불필요한 계정을 지운다",
         "전용 보안 장비를 네트워크 맨 앞단에 새로 추가한다","로그 보관 기간을 늘린다"],1,
        "**없는 것은 취약할 수 없습니다.** 안 쓰는 서비스를 끄는 것은 비용이 거의 들지 않으면서 효과가 확실합니다.\n도구를 추가하는 것은 그 도구 자체가 새로운 공격 표면이 되기도 합니다.\n'이 서버에서 무엇이 돌고 있는지' 를 정기적으로 확인하는 것부터가 시작입니다."),
      C("탐지","침해를 빨리 알아채기 위해 관찰할 가치가 가장 큰 것은?",
        ["메인보드 센서가 보고하는 CPU 온도와 팬 회전 속도","로그인 성공·실패, 권한 상승, 평소와 다른 외부 연결",
         "디스크 조각화 정도","커널 버전 문자열"],1,
        "침해는 대개 **인증과 권한, 그리고 나가는 연결**에서 흔적을 남깁니다.\n특히 서버가 평소 연결하지 않던 곳으로 나가는 트래픽은 유출이나 C2 통신의 신호일 수 있습니다.\n무엇이 정상인지 기준선을 먼저 알아야 이상이 눈에 띕니다 — 그래서 평소 지표 기록이 보안 활동이 됩니다."),
      I("실패한 로그인","실패한 로그인 시도 기록을 보는 명령은?",
        ["lastb","sudo lastb"],
        "정답은 `lastb` 입니다(성공 기록은 `last`). 루트 권한이 필요합니다.\n공개 서버라면 자동 스캔 때문에 목록이 아주 길어집니다. 그 자체는 정상이고, **성공한 낯선 접속**이 있는지가 진짜 확인 대상입니다."),
      C("계정 점검","서버에서 정기적으로 확인할 계정 관련 항목은?",
        ["각 계정의 비밀번호가 충분히 복잡한지를 하나씩 직접 확인한다","쓰지 않는 계정, sudo 권한 목록, 등록된 SSH 공개키를 점검한다",
         "계정 이름의 길이와 형식을 통일한다","로그인 셸을 모두 bash 로 맞춘다"],1,
        "퇴사자 계정, 임시로 준 sudo 권한, 잊힌 공개키가 **가장 흔한 뒷문**입니다.\n비밀번호 강도는 애초에 비밀번호 로그인을 끄면 논점에서 사라집니다.\n이런 점검은 사람의 기억이 아니라 설정 관리 도구로 자동화하는 것이 옳은 방향입니다."),
      C("파일 무결성","중요한 시스템 파일이 몰래 바뀌었는지 알아채려면?",
        ["파일 크기를 눈으로 비교한다","해시 기반 무결성 검사 도구로 기준선과 비교한다",
         "수정 시각만 확인한다","따로 보관해 둔 백업본과 매번 손으로 비교해 본다"],1,
        "`AIDE`·`Tripwire` 같은 도구가 **기준선 해시**를 만들어 두고 변경을 알려 줍니다.\n수정 시각은 공격자가 손쉽게 되돌려 놓을 수 있어 신뢰할 수 없습니다.\n기준선 자체를 서버 밖에 보관해야 의미가 있습니다 — 서버 안에 두면 함께 조작됩니다."),
      C("백업 검증","백업 정책에서 가장 자주 빠지는 것은?",
        ["백업 주기를 정하는 일","복원이 실제로 되는지 정기적으로 시험해 보는 일",
         "백업 파일을 압축하는 일","무엇을 백업할 것인지 대상 목록을 문서로 남기는 일"],1,
        "**복원해 본 적 없는 백업은 백업이 아닙니다.** 형식이 깨졌거나 필요한 것이 빠져 있는 경우가 실제로 흔합니다.\n정기적으로 다른 환경에 복원해 보고, 그 소요 시간까지 재 두어야 장애 때 판단할 수 있습니다.\n랜섬웨어를 고려하면 **오프라인 또는 변경 불가 사본**이 하나 필요하다는 점도 함께 봐야 합니다."),
      C("사고 대응","침해가 의심될 때 가장 먼저 하지 말아야 할 일은?",
        ["네트워크에서 격리하는 것","의심스러운 프로세스를 죽이고 로그를 정리하는 것",
         "관련자에게 알리는 것","지금의 상태를 있는 그대로 기록해 두는 것"],1,
        "성급하게 정리하면 **증거가 사라집니다.** 무엇이 어떻게 들어왔는지 모르면 같은 일이 반복됩니다.\n순서는 격리 → 증거 보존(메모리·디스크 이미지) → 분석 → 복구입니다.\n무엇보다 침해된 서버는 '고쳐 쓰는' 것이 아니라 **깨끗한 상태에서 다시 만드는** 것이 원칙입니다.")
    ] }
]},

/* ═══════════════ 유닛 12 ═══════════════ */
{ t:"직접 해 보는 리눅스 — 상황별 처방", l:[

  { t:"느리다 · 안 된다 · 꽉 찼다", xp:35,
    th:{ sum:"서버 신고는 대개 세 문장으로 온다. '느리다', '안 된다', '꽉 찼다'. 각각 **확인 순서**가 정해져 있다.",
      body:[
        {h:"느리다 — 네 곳을 순서대로", t:"`uptime` 으로 load 를 코어 수와 비교하고, `top` 으로 범인을 찾고, `free -h` 의 available 을 보고, `iostat` 의 `%iowait` 를 본다. CPU 가 한가한데 느리면 I/O 나 외부 의존(DB·API) 대기다. **먼저 재고 나서 고친다** — 재지 않고 고치면 무엇이 효과가 있었는지도 알 수 없다."},
        {h:"안 된다 — 층을 따라", t:"이름이 풀리나(`getent hosts`) → 서버가 듣고 있나(`ss -ltnp`) → 바깥에서 닿나(`nc -zv`) → 응답이 오나(`curl -v`). 층마다 확인 명령이 하나씩 있으므로, 순서대로 내려가면 **어느 층에서 끊겼는지** 반드시 한 곳에서 걸린다."}],
      code:{ c:"uptime; free -h; df -h        # 첫 30초에 보는 셋\ntop -b -n1 | head -15         # 무엇이 먹고 있나\nss -ltnp | grep :8080         # 듣고 있나\njournalctl -u app --since '5 min ago' | tail -50", cap:"재고 나서 고친다" },
      key:["load 는 코어 수와 비교","available 을 본다","층을 따라 순서대로 좁힌다","고치기 전에 먼저 잰다"] },
    q:[
      I("디스크 사용량","현재 폴더의 항목별 크기를 사람이 읽기 좋게 재는 명령은? (du 와 옵션)",
        ["du -sh *","du -h -s *","du -sh"],
        "정답은 `du -sh *` 입니다. `-s` 는 합계만, `-h` 는 읽기 좋은 단위입니다.\n`| sort -h` 를 붙이면 큰 것부터 정렬되어 범인을 바로 찾을 수 있습니다. 큰 폴더로 들어가며 좁혀 가는 것이 디스크 조사의 기본 동작입니다."),
      C("첫 확인","'서버가 느리다' 는 신고를 받았을 때 첫 30초에 볼 것으로 가장 적절한 것은?",
        ["애플리케이션 소스 코드를 열어 처음부터 훑어본다","uptime·free·df 로 부하·메모리·디스크를 한 번에 훑는다",
         "서버를 먼저 재부팅해 본다","네트워크 케이블 상태를 확인한다"],1,
        "**넓게 훑어 범위를 좁히는 것**이 먼저입니다. 세 명령이면 흔한 원인의 대부분이 걸립니다.\n재부팅은 증상을 지우고 원인을 숨기므로, 원인을 모른 채 하는 재부팅은 같은 장애를 반복하게 만듭니다.\n소스 코드는 어느 부분이 문제인지 좁힌 뒤에 볼 것입니다."),
      C("CPU 는 한가한데","CPU 사용률은 낮은데 응답이 느리다면 다음으로 볼 것은?",
        ["지금 돌아가고 있는 커널 버전과 배포판 정보","디스크 I/O 대기와 외부 의존(DB·API) 응답 시간",
         "설치된 패키지 목록","방화벽 규칙 개수"],1,
        "CPU 가 한가하다는 것은 **무언가를 기다리고 있다**는 뜻입니다. 디스크나 네트워크 건너편입니다.\n`iostat` 의 `%iowait`, DB 의 느린 쿼리 로그, 외부 API 응답 시간을 봅니다.\n리눅스 load average 에는 I/O 대기도 포함되므로, load 는 높은데 CPU 는 한가한 조합이 이 상황의 전형입니다."),
      C("디스크 꽉 참","<code>df</code> 가 100% 인데 큰 파일이 안 보인다면?",
        ["파일 시스템을 통째로 지우고 새로 만들어야 한다","열린 채 지워진 파일이나 소진된 inode 를 확인한다",
         "메모리를 늘려야 한다","커널을 업그레이드해야 한다"],1,
        "두 가지 흔한 함정입니다 — `lsof +L1` 로 **열린 채 지워진 파일**을, `df -i` 로 **inode 소진**을 확인합니다.\n둘 다 `du` 로는 보이지 않아 원인을 못 찾고 헤매기 쉽습니다.\n마운트 지점 아래에 다른 파일 시스템이 겹쳐 있어 가려진 파일도 드물게 원인이 됩니다."),
      C("서비스가 안 뜬다","서비스가 시작되지 않을 때 가장 먼저 볼 것은?",
        ["서버를 재부팅한 뒤 다시 시도한다","systemctl status 와 journalctl 로 실패 이유를 읽는다",
         "설정 파일을 통째로 지우고 처음부터 다시 작성해 본다","포트 번호를 바꿔 본다"],1,
        "**이유가 이미 로그에 적혀 있습니다.** `systemctl status` 의 마지막 몇 줄이 대개 원인을 그대로 말해 줍니다.\n권한 부족, 포트 충돌, 설정 문법 오류가 흔한 셋입니다.\n읽지 않고 짐작으로 고치면 우연히 되더라도 무엇 때문이었는지 알 수 없습니다."),
      I("빠른 훑기","부하·메모리·디스크를 각각 확인하는 세 명령을 순서대로 쓰면? (공백으로 구분)",
        ["uptime free df","uptime free -h df -h"],
        "정답은 `uptime`·`free`·`df` 입니다.\n서버에 들어가면 손이 저절로 치게 되는 조합이고, 각각 부하·메모리·디스크를 한 줄로 알려 줍니다. 여기서 이상이 없으면 애플리케이션 층으로 내려갑니다."),
      C("재현","간헐적으로만 발생하는 문제를 다룰 때 가장 중요한 것은?",
        ["문제가 다시 발생할 때까지 화면을 계속 지켜보고 있는 것","발생 시각과 조건을 기록해 패턴을 찾고 관련 지표를 함께 남기는 것",
         "서버를 주기적으로 재부팅하는 것","로그 수준을 최대로 올려 두는 것"],1,
        "간헐적 문제는 **패턴을 찾는 것**이 곧 해결입니다. 특정 시각, 특정 요청, 특정 사용자에 몰려 있는 경우가 많습니다.\n그러려면 발생 시점의 지표(부하·메모리·연결 수)가 함께 남아 있어야 합니다 — 사후에는 만들 수 없습니다.\n로그 수준을 무작정 올리면 양이 많아져 오히려 찾기 어려워집니다."),
      C("변경 이력","장애 원인을 좁힐 때 가장 값어치 있는 질문은?",
        ["이 서버를 운영한 지 몇 년이나 됐는가","최근에 무엇이 바뀌었는가",
         "다른 서버는 정상인가","사용자가 몇 명인가"],1,
        "**대부분의 장애는 변경 직후 발생합니다.** 배포, 설정 변경, 패키지 업데이트, 데이터 증가가 후보입니다.\n`/var/log/apt/history.log`, 배포 기록, 설정 관리 도구의 커밋 로그를 함께 봅니다.\n'다른 서버는 정상인가' 도 좋은 질문이지만, 변경 이력이 먼저입니다 — 차이가 곧 원인일 가능성이 높기 때문입니다.")
    ] },

  { t:"자동화로 넘어가기", xp:35,
    th:{ sum:"손으로 서버를 만지는 일이 반복되면 **재현 가능한 형태**로 옮길 때가 된 것이다. 그 지점을 알아채는 것이 리눅스 학습의 마지막 단계다.",
      body:[
        {h:"기억이 아니라 파일에 남긴다", t:"'이 서버는 어떻게 설정했더라' 를 사람의 기억에 두면 언젠가 반드시 잃는다. 셸 스크립트에서 시작해 Ansible 같은 도구로 옮기면 **설정이 곧 문서**가 되고, 새 서버를 같은 상태로 만들 수 있다. 무엇보다 **변경 이력이 남아** 장애 원인을 추적할 수 있다."},
        {h:"가축이지 애완동물이 아니다", t:"서버를 하나하나 손으로 돌보면(애완동물) 그 서버가 특별해져 대체할 수 없게 된다. 언제든 지우고 다시 만들 수 있는 상태(가축)로 두면, 장애 대응이 '고치기' 에서 **'다시 만들기'** 로 바뀐다. 훨씬 빠르고 결과가 예측 가능하다. 컨테이너와 클라우드가 이 방향을 밀어붙인 것이다."}],
      code:{ c:"# 손으로 하던 것을 파일로\n# setup.sh → Ansible playbook → 이미지 빌드\n\n# 목표: 이 명령 하나로 같은 서버가 만들어진다\nansible-playbook -i inventory site.yml", cap:"설정이 곧 문서가 된다" },
      key:["반복되면 파일로 옮긴다","설정이 곧 문서이자 이력","서버는 가축이지 애완동물이 아니다","다시 만들 수 있으면 대응이 빨라진다"] },
    q:[
      I("설정 관리 도구","에이전트 없이 SSH 로 서버 설정을 자동화하는 대표적인 도구 이름은?",
        ["Ansible","ansible"],
        "정답은 **Ansible** 입니다. 대상 서버에 별도 프로그램을 설치하지 않고 SSH 만으로 동작하는 점이 도입 장벽을 크게 낮췄습니다.\n작업을 YAML 로 적어 두면 그것이 곧 문서가 되고, 여러 번 실행해도 결과가 같도록(멱등적으로) 설계되어 있습니다."),
      C("전환 시점","서버 설정을 자동화로 옮길 때가 된 신호로 가장 적절한 것은?",
        ["관리해야 하는 서버 대수가 100대를 넘어섰을 때","같은 설정 작업을 두 번 이상 손으로 반복하게 될 때",
         "장애가 실제로 발생했을 때","새로운 도구를 배우고 싶을 때"],1,
        "**두 번째부터**가 기준입니다. 두 번 했다면 세 번째가 반드시 옵니다.\n대수가 적어도 값어치가 있습니다 — 자동화의 진짜 이익은 규모가 아니라 **재현 가능성과 이력**입니다.\n장애가 난 뒤에 시작하면 이미 늦고, 급한 상황에서 만든 자동화는 대개 신뢰하기 어렵습니다."),
      C("설정의 문서화","설정 관리 도구를 쓸 때 얻는 부수적이지만 큰 이점은?",
        ["서버 성능이 향상된다는 점","설정 자체가 문서가 되고 변경 이력이 남는다는 점",
         "라이선스 비용이 줄어든다는 점","알려진 보안 취약점이 저절로 사라져 준다는 점"],1,
        "'이 서버는 어떻게 설정되어 있는가' 의 답이 **코드에 있습니다.** 사람의 기억이나 낡은 위키가 아닙니다.\n버전 관리에 넣으면 누가 언제 왜 바꿨는지도 남아, 장애 원인 추적이 훨씬 쉬워집니다.\n성능이나 보안은 자동으로 좋아지지 않지만, 같은 보안 설정을 **빠짐없이** 적용할 수 있게 되는 것은 큰 이점입니다."),
      C("가축과 애완동물","서버를 '가축처럼' 다룬다는 말의 뜻은?",
        ["여러 대를 한꺼번에 관리한다는 뜻","언제든 지우고 같은 것을 다시 만들 수 있는 상태로 둔다는 뜻",
         "값싼 하드웨어를 쓴다는 뜻","서버 한 대 한 대에 따로 이름을 붙이지 않는다는 뜻"],1,
        "**대체 가능성**이 핵심입니다. 문제가 생기면 고치는 대신 새로 만들어 교체합니다.\n손으로 조금씩 고친 서버는 시간이 지나면 아무도 그 상태를 재현할 수 없게 됩니다(스노플레이크).\n이 원칙이 성립하려면 데이터가 서버 밖(DB·오브젝트 스토리지)에 있어야 한다는 전제가 함께 필요합니다."),
      C("첫 단계","자동화의 첫 단계로 가장 현실적인 것은?",
        ["쿠버네티스 클러스터를 먼저 구축해 놓고 시작한다","지금 손으로 치는 명령들을 셸 스크립트로 모으는 것",
         "모든 서버를 컨테이너로 옮긴다","상용 자동화 플랫폼을 도입한다"],1,
        "**지금 하는 일을 그대로 파일로 옮기는 것**부터가 자동화입니다. 큰 도구를 먼저 도입하면 배우다 지칩니다.\n스크립트가 쌓이면 자연스럽게 '이건 Ansible 로 옮기면 낫겠다' 는 지점이 보입니다.\n순서를 건너뛰면 도구는 있는데 아무도 쓰지 않는 상태가 되기 쉽습니다."),
      I("멱등성","같은 작업을 여러 번 실행해도 결과가 같은 성질을 뜻하는 말은? (한국어)",
        ["멱등성","멱등"],
        "정답은 **멱등성**(idempotency)입니다. 설정 관리 도구의 핵심 성질입니다.\n'파일에 이 줄을 추가한다' 는 여러 번 실행하면 줄이 여러 개 생기지만, '파일이 이 상태가 되게 한다' 는 몇 번을 실행해도 같습니다. 후자로 쓰는 것이 자동화의 기본기입니다."),
      C("스크립트의 한계","셸 스크립트에서 Ansible 같은 도구로 넘어갈 만한 신호는?",
        ["직접 작성한 스크립트가 100줄을 넘어섰을 때","여러 서버에 적용해야 하고 이미 적용된 상태를 건너뛰어야 할 때",
         "실행 속도가 느려졌을 때","다른 팀이 요청했을 때"],1,
        "**여러 대**와 **멱등성**이 갈림길입니다. 스크립트로 이 둘을 제대로 하려면 결국 도구를 다시 만들게 됩니다.\n한 대에서 한 번 돌리는 일이라면 스크립트가 더 단순하고 빠릅니다.\n도구는 문제를 풀기 위해 쓰는 것이지, 도구를 쓰기 위해 문제를 만들 필요는 없습니다."),
      C("다음 단계","리눅스 기초를 익힌 뒤 가장 실용적인 다음 단계는?",
        ["커널 소스 코드를 읽어 보는 것","작은 서버 하나를 직접 세워 서비스를 배포하고 운영해 보는 것",
         "자주 쓰는 명령들의 man 페이지를 처음부터 끝까지 정독하는 것","다른 운영체제를 함께 배우는 것"],1,
        "리눅스는 **쓰면서** 익혀집니다. 도메인을 연결하고, HTTPS 를 붙이고, 서비스를 등록하고, 로그를 보고, 백업을 걸어 보면 배운 것이 전부 연결됩니다.\n그 과정에서 만나는 문제가 곧 다음에 배울 것을 알려 줍니다.\nman 페이지와 커널은 필요해진 다음에 읽어야 남습니다.")
    ] }
]}

];

module.exports=shuffleUnits(UNITS);
