/* sql — 정답률 15% 아래 끝맺음의 오답 30개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "sql", fixes: [
  { k: "wal-durability", from: "장애에도 사라지지 않기 때문이다", to: "버퍼 풀이 비휘발성 메모리라서 장애가 나도 그대로 남아 있어서다" },
  { k: "mvcc-snapshot", from: "영향을 받지 않기 때문이다", to: "읽기는 디스크를 보지 않고 버퍼 캐시만 보므로 쓰기가 디스크를 잠가도 닿지 않아서다" },
  { k: "mvcc-snapshot", from: "겹칠 일이 없기 때문이다", to: "MVCC 는 모든 쓰기를 직렬화해 한 번에 하나만 실행하므로 읽기가 쓰기와 겹칠 틈이 사라져서다" },
  { k: "비교 변환", from: "오류가 난다", to: "타입 불일치로 쿼리가 거부된다" },
  { k: "LAST_VALUE 함정", from: "결과가 전혀 달라지지 않기 때문이다", to: "LAST_VALUE는 원래 현재 행 하나만 보도록 설계된 함수라서, 프레임을 어떻게 지정해도 결과는 그대로여서다" },
  { k: "클러스터링", from: "페이지 분할이 없기 때문이다", to: "자동 증가 정수여야 하므로 삽입이 끝에 붙어 페이지 분할이 일어나지 않아서다" },
  { k: "호환 규칙", from: "조건이 다르면 오류가 난다", to: "두 SELECT의 WHERE 절 조건이 서로 정확히 같아야 한다. 조건이 다르면 결합이 거부된다" },
  { k: "NULL 구분", from: "구분할 방법이 없다", to: "두 NULL 은 저장 형식이 같아 구분이 원리적으로 불가능하다" },
  { k: "COUNT 주의", from: "오류가 난다", to: "ELSE 가 없는 CASE 는 문법 오류로 거부된다" },
  { k: "인덱싱", from: "인덱스가 필요 없다", to: "인덱스 대신 애플리케이션 캐시에 결과를 담아 두면 DB 를 거치지 않으므로 인덱스는 군더더기다" },
  { k: "하이브리드", from: "JSON을 절대 쓰지 않는다", to: "JSON 컬럼은 금지하고 전부 정규 컬럼으로 편다" },
  { k: "HashJoin", from: "별도 정렬이 필요 없다", to: "정렬된 출력이 필요할 때 — 해시 순서가 곧 정렬 순서라 정렬 단계를 건너뛴다" },
  { k: "형변환", from: "별도 작업이 필요 없다", to: "조건에 맞춰 인덱스가 자동으로 새로 생성되어 버린다. 그래서 저장 공간이 늘어난다" },
  { k: "YEAR() 비-SARGable", from: "쿼리를 손댈 필요가 없다", to: "created_at을 DATE 타입으로 바꾸면 해결된다 → 타입이 날짜로 바뀌면 YEAR() 함수가 인덱스를 그대로 활용할 수 있게 되어 Full Scan이 사라지고, 범위 조건으로 다시 쓰지 않아도 같은 성능이 나오므로 쿼리는 그대로 두면 된다" },
  { k: "Phantom Read", from: "격리 수준을 올릴 필요가 없다", to: "Dirty Read이며 인덱스로 해결된다 → 조건 컬럼에 인덱스를 만들면 같은 행 집합을 다시 읽어도 결과가 고정되므로 격리 수준은 그대로 두면 된다" },
  { k: "데드락 대응", from: "순환 대기가 생기지 않는다", to: "모든 트랜잭션을 SERIALIZABLE로 바꾸면 데드락이 사라진다 → 직렬화가 보장되면 잠금 순서가 자동으로 정리되어 순환 대기가 끊긴다" },
  { k: "데드락 대응", from: "잠금을 쓰지 않는다", to: "잠금을 끄고 마지막 쓰기가 이기게 둔다" },
  { k: "Index Only Scan", from: "행 전체를 담고 있기 때문이다", to: "조건이 기본키에 대한 등호 비교여야 하고 다른 컬럼은 쓸 수 없다 — 기본키 인덱스만 행 전체를 담고 있어서다" },
  { k: "반복되는 안쪽 스캔", from: "정상 동작이므로 조치할 필요가 없다", to: "정상 동작이므로 통계만 갱신하고 지켜본다" },
  { k: "반복되는 안쪽 스캔", from: "인덱스는 따로 필요 없다", to: "쿼리 끝에 LIMIT을 추가하면 반복 횟수가 줄어들어 문제가 자연스럽게 해결된다. 인덱스는 그 다음 문제다" },
  { k: "OR 조건", from: "별도 재작성은 필요 없다", to: "두 컬럼을 하나의 복합 인덱스 (a, b)로 묶어 주면 어떤 경우에도 해결된다. 쿼리는 그대로 두면 된다" },
  { k: "타이브레이커", from: "하나만 쓰면 오류가 난다", to: "정렬 컬럼은 두 개 이상이어야 한다는 규칙이 표준에 정해져 있기 때문이며, 하나만 쓰면 파서가 거부한다" },
  { k: "갱신 이상", from: "인덱스를 만들 수 없게 된다", to: "주소 컬럼이 길어져 인덱스를 만들지 못하게 된다" },
  { k: "갱신 이상", from: "기본키를 만들 수 없게 된다", to: "중복 값 때문에 기본키를 잡지 못하게 된다" },
  { k: "1NF 위반", from: "트랜잭션을 사용할 수 없게 된다", to: "콤마가 든 컬럼은 트랜잭션 로그에 기록되지 않는다" },
  { k: "주문 스냅샷", from: "정확성 자체에는 문제가 없다", to: "조인 때문에 조회 성능이 조금 나빠질 뿐 값은 최신 가격이므로 정확성은 오히려 좋아진다" },
  { k: "낙관적 락 동작", from: "재시도 로직을 만들어 둘 필요가 없다", to: "충돌이 나면 데이터베이스가 두 값을 자동으로 병합해 주므로 애플리케이션 쪽 재시도 로직은 군더더기다" },
  { k: "방식 선택", from: "낙관적 락은 쓰지 않는다", to: "비관적 락이 안전하므로 낙관적 락은 읽기 전용 화면에만 남긴다" },
  { k: "ROWS vs RANGE", from: "곧바로 문법 오류가 난다", to: "ROWS는 숫자 컬럼에만 쓸 수 있고 RANGE는 날짜 컬럼에만 쓸 수 있으며, 두 문법을 서로 바꿔 쓰면 파서가 곧바로 거부한다" },
  { k: "WHERE에서 참조", from: "어떤 경우에도 사용할 수 없기 때문이다", to: "별칭을 SELECT 절에서 정의했기 때문에 처리 순서상 어떤 절에서도 참조되지 않는 구조여서다" },
  { k: "LAG", from: "자기 조인 외에는 방법이 없다", to: "자기 조인으로 전날 행을 붙이는 것이 유일한 길이다" }
] };
