module.exports = {
  track: "git", guide: "", xp: 80,
  unit: "직접 구현 — Git 을 손으로 만들어 본다",
  source: "./git_exec.cjs",
  lessons: [
    { t: "커밋 그래프 — 부모를 가리키는 링크뿐이다", n: 5,
      th: { sum: "Git 의 역사는 표가 아니라 **부모를 가리키는 링크로 이어진 그래프**다. 이 사실 하나에서 로그·병합·브랜치가 전부 따라 나온다.",
        body: [
          { h: "방향이 한쪽이다", t: "커밋은 **부모를 알지만 자식은 모른다**. 그래서 '조상은 누구인가' 는 즉시 답할 수 있고, '이 커밋 다음에 뭐가 왔나' 는 전체를 훑어야 한다. `git log` 가 뒤에서 앞으로 거슬러 오르는 것도, 브랜치를 지워도 커밋이 남는 것도 이 방향성 때문이다. 병합 커밋은 부모가 둘이라 `--first-parent` 로 주 줄기만 따라갈 수 있다." },
          { h: "닿는가, 그것이 전부다", t: "'fast-forward 가 되나', '이 브랜치는 병합됐나', '몇 커밋 앞서 있나' 는 전부 **도달 가능성** 하나로 답한다. 훑을 때 `seen` 집합이 반드시 필요하다 — 병합으로 갈라진 두 길이 다시 만나면 같은 커밋을 지수 번 방문하게 된다. 3-way 병합의 기준점인 **공통 조상**도 같은 훑기에서 나오는데, 가장 가까운 것을 잡아야 이미 합쳐진 변경이 다시 충돌하지 않는다." }],
        code: { c: "const stack = [b];\nwhile (stack.length) {\n  const id = stack.pop();\n  if (id === a) return true;      // 닿았다\n  byId.get(id).ps.forEach((p) => stack.push(p));\n}", cap: "부모 방향으로만 훑는다" },
        key: ["커밋은 부모만 안다", "판정은 대개 도달 가능성 하나", "seen 없이 훑으면 지수로 터진다", "공통 조상은 가장 가까운 것"] } },
    { t: "병합 — 기준점이 있어야 누가 바꿨는지 안다", n: 5,
      th: { sum: "병합의 규칙은 한 줄이다. **바꾼 쪽의 것을 쓴다.** 둘 다 바꿨는데 결과가 다를 때만 사람을 부른다.",
        body: [
          { h: "2-way 로는 안 되는 이유", t: "`ours` 와 `theirs` 만 보면 '다르다' 는 것만 알 뿐 **누가 바꿨는지** 모른다. 안 바꾼 쪽의 옛 내용으로 덮어쓰는 사고가 여기서 난다. 기준점(공통 조상)을 함께 보면 세 경우로 깔끔히 갈린다 — 같으면 그대로, 한쪽만 바뀌었으면 그쪽, 둘 다 다르게 바뀌었으면 충돌. 양쪽이 **똑같이** 바꾼 것은 충돌이 아니다." },
          { h: "diff 는 저장되어 있지 않다", t: "커밋에는 스냅숏만 있고, 화면의 `+3 -1` 은 두 스냅숏을 비교해 **그때 계산한 결과**다. 계산의 기준은 순서를 지키며 살아남는 줄을 최대로 잡는 **최장 공통 부분수열**이고, 그래서 diff 는 **유일하지 않다** — 같은 길이의 답이 여럿이면 어느 것을 고르느냐로 모양이 달라진다. `patience`·`histogram` 알고리즘이 따로 있는 이유다." }],
        code: { c: "if (ours === theirs) return ours;\nif (ours === base)   return theirs;\nif (theirs === base) return ours;\nreturn \"CONFLICT\";", cap: "병합의 전부" },
        key: ["바꾼 쪽의 것을 쓴다", "기준점 없이는 누가 바꿨는지 모른다", "같은 파일이라도 다른 줄이면 안 싸운다", "diff 는 계산되고 유일하지 않다"] } },
    { t: "세 공간 — status 와 reset 을 계산해 본다", n: 5,
      th: { sum: "`git status` 는 마법이 아니라 **두 번의 비교**다. `reset` 은 '어디까지 끌고 내려갈 것인가' 세 단계일 뿐이다.",
        body: [
          { h: "두 번 비교하면 status 가 나온다", t: "`head↔index` 가 'Changes to be committed', `index↔work` 가 'Changes not staged' 다. `add` 한 뒤 또 고치면 **같은 파일이 양쪽에 동시에** 나타나는데, 오류가 아니라 정확한 보고다. 추적 여부는 `head` 와 `index` **양쪽**으로 판단해야 한다 — 새로 만들어 `add` 만 한 파일은 `head` 에 없어도 이미 추적 중이다." },
          { h: "재생과 잃어버림", t: "`rebase`·`cherry-pick`·`revert` 는 커밋을 옮기는 것이 아니라 **변경을 새 자리에 재생**하는 것이다. 그래서 순서를 바꾸면 결과가 달라질 수 있다. 반대로 `reset` 으로 잘라 낸 커밋은 **사라지지 않고 이름만 잃는다** — `git fsck --unreachable` 이 그 목록이고, `reflog` 가 임시 이름 역할을 해 되살릴 수 있게 해 준다. 다만 `gc` 가 정리한 뒤에는 방법이 없다." }],
        code: { c: "if (index[f] !== head[f])  push(\"staged:\" + f);\nif (work[f]  !== index[f]) push(\"modified:\" + f);", cap: "두 번의 비교가 곧 status" },
        key: ["status = head↔index, index↔work", "reset 은 어디까지 내려갈지 세 단계", "hard 만 작업 파일을 지운다", "이름을 잃어도 커밋은 남는다"] } },
    { t: "탐색과 도구 — bisect · blame · ignore", n: 5,
      th: { sum: "Git 의 도구들은 화려한 알고리즘이 아니라 **그래프 위의 단순한 계산**이다. 조건만 알면 직접 만들 수 있다.",
        body: [
          { h: "bisect 는 단조성을 전제한다", t: "커밋이 1000개여도 10번이면 범인을 찾는다. 다만 '**한 번 나빠지면 계속 나쁘다**' 가 참일 때만 성립한다 — 중간에 고쳐졌다 다시 깨졌다면 아무 결과나 나온다. 마지막에 한 번 더 확인하지 않으면 전부 정상인 경우에 **멀쩡한 커밋을 범인으로** 지목한다. 판정을 스크립트로 만들면 `git bisect run` 이 전 과정을 자동화한다(0 정상, 1 이상, 125 건너뜀)." },
          { h: "blame 은 맥락을 찾는 도구", t: "줄에서 해시를 얻어 그 커밋의 **메시지**를 읽는 것이 진짜 목적이다. 그래서 메시지가 '수정' 뿐이면 blame 도 쓸모없어진다. 오래된 것부터 덮어쓰는 구조라 전체 포맷 커밋 하나가 blame 을 통째로 가리는데, `.git-blame-ignore-revs` 로 건너뛰게 한다. `.gitignore` 는 **뒤 규칙이 앞을 덮으므로** 예외(`!`)를 아래에 적어야 하고, 무엇이 걸렸는지는 `git check-ignore -v` 가 알려 준다." }],
        code: { c: "if (isBad(commits[mid])) hi = mid;\nelse lo = mid + 1;\n// 끝나고 한 번 더 확인해야 '전부 정상' 을 구분한다", cap: "확인 한 번을 빠뜨리면 무고한 커밋이 범인이 된다" },
        key: ["bisect 는 단조성이 전제", "판정 스크립트로 자동화된다", "blame 의 목적은 커밋 메시지", "gitignore 는 뒤 규칙이 이긴다"] } },
    { t: "도구를 직접 만들어 본다", n: 4,
      th: { sum: "Git 위에 얹는 작은 도구들은 대개 **줄을 읽고 규칙에 맞는지 보는 일**이다. 어렵지 않지만, 무엇을 읽을지와 무엇을 막을지는 판단이 필요하다.",
        body: [
          { h: "사람용 출력을 파싱하지 않는다", t: "`git status` 의 문장은 버전과 언어 설정에 따라 바뀌지만 `--porcelain` 은 **바꾸지 않겠다고 약속된** 형식이다. 두 글자가 각각 head↔index 와 index↔작업 폴더를 뜻해, `MM` 이면 'add 한 뒤 또 고쳤다' 는 뜻이다. 경로는 고정 폭 뒤부터 잘라야 한다 — 공백으로 나누면 이름에 공백이 든 파일에서 깨진다. 도구가 기계용 출력(`--porcelain`·`--format`·`--json`)을 준다면 언제나 그쪽을 쓴다." },
          { h: "규약이 있으면 나머지는 기계가 한다", t: "커밋 메시지 규약을 지키면 릴리스 노트가 기계적으로 만들어지고, 버전도 `feat`→minor·`fix`→patch 로 자동으로 오른다. 규약의 값어치는 보기 좋아서가 아니라 **기계가 읽을 수 있어서**다. 버전을 올릴 때 **아래 자리를 0으로 내리는 것**과, `0.10.0` 이 `0.9.0` 보다 크다는 것(문자열로 비교하면 틀린다)이 흔한 함정이다. 검사 도구는 **막을지 경고만 할지**를 신중히 정해야 한다 — 너무 엄격하면 우회하는 문화가 생긴다." }],
        code: { c: "const x = line.charAt(0);   // head ↔ index\nconst y = line.charAt(1);   // index ↔ 작업 폴더\nconst file = line.slice(3); // 고정 폭 뒤부터", cap: "공백으로 나누면 이름에 공백이 든 파일에서 깨진다" },
        key: ["기계용 출력을 파싱한다", "porcelain 두 글자는 서로 다른 공간", "버전은 자리별 숫자로 비교", "규약이 있어야 자동화가 된다"] } },
  ],
};
