module.exports = {
  track: "cs", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 비트와 표현 범위",
  source: "./in_cs.cjs",
  lessons: [
    { t: "비트로 세고 비트로 고르기", n: 8,
      th: { sum: "비트 연산은 외우는 게 아니라 **자리마다 무슨 질문을 하는지**를 보면 된다 — 둘 다 켜졌나, 한쪽만 켜졌나, 몇 칸 밀 것인가.",
        body: [
          { h: "AND·XOR·시프트가 하는 질문", t: "AND 는 '<b>둘 다 1 인 자리</b>' 만 남겨 마스킹에 쓰고(<code>x &amp; 0xFF</code> 는 하위 한 바이트), XOR 은 '<b>서로 다른 자리</b>' 만 1 로 만들어 같은 값을 두 번 걸면 원래대로 돌아온다. 왼쪽 시프트 한 칸은 2 를 곱하는 것이라 <code>1 &lt;&lt; n</code> 은 'n 번 비트만 켠 값' 이다. 오른쪽 시프트는 두 가지다 — <code>&gt;&gt;</code> 는 부호로 채우고 <code>&gt;&gt;&gt;</code> 는 0 으로 채운다." },
          { h: "글자와 바이트는 다른 단위", t: "ASCII 는 대문자 65·소문자 97·숫자 48 부터 이어지고, 대소문자는 정확히 <b>비트 하나(32)</b> 차이다. UTF-8 은 가변 길이라 ASCII 1 바이트, 한글 <b>3 바이트</b>, 대부분의 이모지 4 바이트다 — '글자 수' 와 '바이트 수' 는 전혀 다른 값이라, 길이 제한의 단위가 무엇인지 먼저 확인해야 한글이 잘리지 않는다. 2 의 보수에서 -1 은 <b>모든 비트가 1</b> 이라 부호 없는 타입의 최댓값이 된다." }],
        code: { c: "x & 0xFF          // 하위 한 바이트만 남긴다\na ^ b ^ b === a   // 두 번 걸면 되돌아온다\n1 << 20           // 1MB — 자릿수를 잘못 셀 일이 없다", cap: "자리마다 무슨 질문을 하는지 본다" },
        key: ["AND 는 마스킹, XOR 은 되돌리기", "1 << n 은 n 번 비트", ">> 와 >>> 는 채우는 값이 다르다", "글자 수와 바이트 수를 구분한다"] } },
    { t: "표현 범위와 오차", n: 7,
      th: { sum: "'담을 수 있는 값의 가짓수' 가 곧 표현 범위이고, **그 범위를 넘거나 촘촘하지 못한 자리**에서 조용한 버그가 난다.",
        body: [
          { h: "몇 비트면 되는가", t: "담을 값의 <b>가짓수에 로그를 취해 올림</b>하면 필요한 비트 수다 — 0~1000 이면 1001 가지라 10 비트다. IEEE 754 배정밀도는 부호 1 + 지수 11 + 가수 52 인데, 맨 앞의 1 을 저장하지 않아 <b>실질 53 비트</b>다. 그래서 정수는 <code>2⁵³ - 1</code>(9,007,199,254,740,991)까지만 정확하고, 그 위에서는 이웃한 두 정수가 같은 값으로 접힌다 — 64비트 ID 는 <b>문자열</b>로 실어 보낸다." },
          { h: "오차는 조합에 따라 갈린다", t: "<code>0.1 + 0.2 !== 0.3</code> 이지만 <code>0.1 + 0.1 === 0.2</code> 는 참이다. '부동소수는 언제나 틀린다' 가 아니라 <b>어떤 조합에서 틀린다</b> 가 정확한 표현이고, 그래서 '한 번 해 보니 맞더라' 는 근거가 못 된다. 정수로 만드는 방법도 갈린다 — <b>0 쪽으로 버리기</b>(trunc)와 <b>더 작은 쪽으로 내리기</b>(floor)는 음수에서만 달라지고, C 와 파이썬의 정수 나눗셈이 바로 이 차이다." }],
        code: { c: "Math.ceil(Math.log2(가짓수))   // 필요한 비트 수\nMath.abs(a - b) < 1e-9        // 실수는 오차를 허용해 견준다\nMath.trunc(-2.5)  // -2 ·  Math.floor(-2.5)  // -3", cap: "범위를 넘는 자리를 미리 세어 둔다" },
        key: ["가짓수에 로그를 취해 올린다", "정확한 정수는 2⁵³ 까지", "실수 비교는 오차 허용으로", "음수에서 버림과 내림이 갈린다"] } },
  ],
};
