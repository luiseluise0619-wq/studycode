/* git·javascript·react·dbt·algo·arch·cloud·mobile — 정답이 단독으로 가장 긴 문항의 오답을 다시 쓴다. */
module.exports = [
{ track: "git", fixes: [
  { k: "성능 점검", o: [
    "브랜치가 몇 개나 남아 있는지 센다 — 브랜치마다 커밋 사본이 따로 저장되므로 오래된 브랜치를 지우는 것이 첫 처방이다",
    "git count-objects -vH 로 크기를 확인하고 큰 객체를 찾는다",
    "전체 커밋이 몇 개인지 세어 본다 — 커밋 수가 곧 저장소 크기이므로 히스토리를 squash 해 줄이는 것이 답이다",
    "처음부터 다시 클론한다 — 로컬에 쌓인 찌꺼기가 원인일 때가 많아 새로 받으면 원인을 찾지 않아도 가벼워진다" ] },
  { k: "상황 처방 5", o: [
    "파일을 전부 복사해 둔 다음 새 브랜치를 만들어 처음부터 똑같이 다시 만든다 — 커밋은 브랜치에 묶여 있어 옮길 수 없기 때문이다",
    "git stash 로 옮긴다 — stash 는 커밋까지 포함해 저장하므로 브랜치를 바꾼 뒤 pop 하면 커밋이 그대로 따라온다",
    "git switch -c 새브랜치 후 main 을 git reset --hard origin/main 으로 되돌린다",
    "main 인 채로 그냥 원격에 푸시한다 — 이미 커밋된 내용은 어차피 main 의 일부이므로 브랜치를 따로 만들 이유가 없다" ] },
  { k: "표현", o: [
    "이건 틀렸어요 — 짧고 명확해서 작성자가 어디를 봐야 하는지 바로 알 수 있고, 리뷰어의 시간도 아낀다",
    "응답이 빈 배열이면 아래 [0] 에서 터지지 않을까요. 재현: /api/items?q=없는값",
    "왜 이런 방식으로 짜셨는지 배경이 되는 이유를 알려 주실 수 있을까요 — 판단은 작성자에게 맡기고 리뷰어는 묻기만 한다",
    "다시 하세요 — 고칠 점이 많을 때는 하나씩 짚는 것보다 전체를 다시 쓰게 하는 편이 결과가 낫다" ] },
  { k: "시크릿", o: [
    "README 에 적는다 — 문서에 있어야 다음 사람이 어떤 키가 필요한지 알 수 있고, 저장소가 비공개면 밖으로 새지 않는다",
    "값을 채워 둔 환경 변수 파일을 그대로 저장소에 커밋해 둔다 — 워크플로가 그 파일을 읽으므로 설정이 한곳에 모인다",
    "저장소 Secrets 에 저장하고 ${{ secrets.NAME }} 으로 참조한다",
    "YAML 에 직접 적는다 — 워크플로 파일은 Actions 만 읽으므로 키를 env 블록에 두어도 외부에서 볼 수 없다" ] },
  { k: "잘못 단 태그", o: [
    "가리키는 객체가 어긋나면서 저장소 전체가 못 쓰게 손상된다 — 태그는 객체 그래프의 일부라 옮기면 참조 무결성이 깨진다",
    "아무 문제 없다 — 태그는 브랜치처럼 움직이는 포인터라 다시 푸시하면 받아 간 쪽도 다음 fetch 에서 새 위치를 본다",
    "옮긴 순간 원래 태그가 사라진다 — 같은 이름은 하나만 있을 수 있으므로 옛 커밋은 태그를 잃고 찾을 수 없게 된다",
    "이미 받아 간 사람의 태그는 갱신되지 않아 같은 이름이 서로 다른 커밋을 가리킨다" ] },
  { k: "SHA-1", o: [
    "저장소가 비공개라 외부에서 볼 수 없어서 — 충돌 객체를 만들려면 저장소 내용을 알아야 하는데 공개되지 않으면 공격이 성립하지 않는다",
    "같은 내용의 해시를 두 번 계산해 서로 비교하고 있어서 — 두 번의 결과가 다르면 충돌로 판단하고 객체를 거부한다",
    "Git 은 길이 접두사를 붙여 해시하고 충돌 탐지를 넣었으며 SHA-256 으로 옮겨 가는 중이다",
    "SHA-1 충돌은 이론으로만 알려졌을 뿐 실제로 만들어진 적이 한 번도 없어서 — 실증되기 전까지는 안전하다고 보아도 된다" ] },
  { k: "판단 1", o: [
    "지금 작업하고 있는 브랜치의 이름과 위치 — main 인지 기능 브랜치인지에 따라 쓸 수 있는 되돌리기 명령이 다르다",
    "되돌릴 대상이 커밋인지, 스테이지인지, 작업 폴더의 수정인지",
    "연결된 원격 저장소의 주소 — 원격이 GitHub 인지 GitLab 인지에 따라 되돌린 결과를 푸시하는 방법이 갈린다",
    "설치된 Git 의 버전 번호 — restore 와 switch 는 새 버전에만 있으므로 버전에 따라 reset 을 써야 할 수도 있다" ] },
] },
{ track: "javascript", fixes: [
  { k: "js-fetch-preflight", o: [
    "GET 요청에서만 프리플라이트가 발생한다 — 읽기 요청은 캐시될 수 있어 브라우저가 먼저 서버의 허용 여부를 확인해야 한다",
    "HTTPS 요청에서만 프리플라이트가 발생한다 — 암호화된 연결에서는 인증서와 함께 CORS 정책도 사전 협상 대상이다",
    "모든 교차 오리진 요청은 프리플라이트를 먼저 보낸다 — 오리진이 다르면 브라우저가 예외 없이 OPTIONS 로 허용 여부를 묻는다",
    "PUT·DELETE 같은 메서드이거나 Content-Type: application/json, 커스텀 헤더처럼 단순 요청 조건을 벗어날 때" ] },
  { k: "js2-d4-12", o: [
    "둘 다 UTC 기준이며 형식만 다르다 — 하나는 기계용 ISO 문자열, 다른 하나는 사람용 서식일 뿐 시각 자체는 같은 기준이다",
    "toLocaleString 은 어떤 환경에서도 영어 형식으로만 출력된다 — 로케일 인자를 넘기지 않으면 en-US 가 기본값으로 고정된다",
    "toISOString 은 로컬 타임존을 반영한다 — 실행 환경의 오프셋을 붙여 돌려주므로 서울에서 찍으면 +09:00 이 붙는다",
    "toISOString 은 항상 UTC 기준 고정 형식, toLocaleString 은 실행 환경의 타임존·로케일에 따라 달라진다" ] },
  { k: "js-tree-cjs", o: [
    "require 는 실행 시점에 결정되는 동적 호출이라 어떤 export 가 안 쓰이는지 정적으로 증명하기 어렵다",
    "CommonJS 는 압축이 불가능해 번들이 커진다 — module.exports 객체는 minifier 가 이름을 줄일 수 없어 원문 그대로 남는다",
    "CommonJS 는 캐시가 안 되어 매번 다시 로드된다 — require 는 호출할 때마다 파일을 다시 읽으므로 번들에 사본이 여러 개 생긴다",
    "CommonJS 모듈은 브라우저에서 실행되지 않기 때문이다 — 번들러가 ESM 으로 변환하는 과정에서 모든 export 를 살려 두어야 한다" ] },
  { k: "js2-t8-10", o: [
    "타입 단언(as)으로 컴파일 오류를 없앤 뒤 진행한다 — 외부 데이터의 형태는 문서로 보장되므로 검사 대신 타입을 선언하면 충분하다",
    "검증을 생략하고 실패했을 때 try...catch 로만 대응한다 — 잘못된 데이터는 어차피 예외를 내므로 사전 검사는 중복이다",
    "모든 함수 진입점마다 같은 검사를 반복해 방어한다 — 어느 경로로 들어오든 안전하도록 검사를 여러 겹으로 두는 것이 원칙이다",
    "데이터가 들어오는 경계에서 한 번 검증해 신뢰할 수 있는 타입으로 변환하고, 내부 코드는 그 타입을 신뢰한다" ] },
  { k: "js-retry-jitter", o: [
    "재시도하는 간격을 일정하게 만들어 두기 위해서다 — 무작위 값을 더해 평균을 맞추면 지수 증가의 들쭉날쭉함이 고르게 펴진다",
    "동시에 실패한 클라이언트들이 같은 시각에 재시도해 서버를 다시 때리는 현상을 흩뜨리기 위해",
    "재시도 횟수를 줄이기 위해 — 지터가 붙으면 간격이 길어지는 쪽으로 치우쳐 같은 시간 안에 시도가 적게 일어난다",
    "브라우저 캐시를 무효화하기 위해 — 요청 시각이 매번 달라지면 캐시 키가 바뀌어 옛 응답을 재사용하지 않는다" ] },
  { k: "js-cache-swr", o: [
    "캐시를 두 벌 저장해 번갈아 사용한다 — 하나가 만료되는 동안 다른 하나를 쓰므로 언제나 유효한 사본이 남아 있다",
    "만료된 캐시를 일단 즉시 사용해 응답을 빠르게 주고, 백그라운드에서 새 응답을 받아 캐시를 갱신한다",
    "서버가 응답할 때까지 요청을 지연시킨다 — 만료된 사본은 쓰지 않고 재검증이 끝난 뒤에야 화면에 그린다",
    "만료된 캐시를 버리고 새 요청이 끝날 때까지 기다리게 한다 — 낡은 데이터를 보여 주지 않는 것이 이 지시자의 목적이다" ] },
  { k: "js2-d4-18", o: [
    "ISO 형식 외의 파싱은 구현체 재량이라 브라우저·런타임마다 결과가 달라질 수 있다",
    "UTC 로 해석되어 타임존이 무시된다 — 문자열에 오프셋이 없으면 Date 는 언제나 UTC 자정으로 잡아 하루가 밀린다",
    "입력 문자열의 길이가 10자를 넘으면 파싱에 실패하기 때문 — Date 생성자는 YYYY-MM-DD 열 자리까지만 읽고 나머지는 버린다",
    "new Date 는 문자열 인자를 받지 못한다 — 숫자 타임스탬프만 받으므로 문자열은 먼저 Date.parse 로 바꿔야 한다" ] },
] },
{ track: "react", fixes: [
  { k: "react3-reset-6", o: [
    "계산으로 항상 얻을 수 있으면 파생 값이고, 사용자가 이후에 독립적으로 바꿀 수 있는 값이면 상태로 두되 재설정 전략이 필요하다",
    "둘 다 이펙트로 처리해야 한다 — props 가 바뀔 때 setState 를 부르는 useEffect 하나면 파생이든 재설정이든 같은 코드로 끝난다",
    "재설정이 필요한 상태는 파생 값으로 대체할 수 있다 — 초기값을 props 에서 매번 계산하면 사용자가 바꾼 값도 그 안에 담긴다",
    "파생 값은 useMemo 로 감싸야 한다 — 감싸지 않으면 렌더마다 다시 계산되어 파생 값이 아니라 상태처럼 동작한다" ] },
  { k: "react3-lc-6", o: [
    "정리 함수가 빠졌거나 이펙트가 재실행에 안전하지 않은 코드를 개발 중에 드러내기 위해 마운트·언마운트를 한 번 더 시뮬레이션한다",
    "성능을 측정하기 위해 — 첫 실행은 준비 시간을 재고 두 번째 실행은 순수 이펙트 시간을 재어 개발자 도구에 차이를 보여 준다",
    "React 의 버그다 — 동시성 렌더러가 커밋을 두 번 처리하는 결함이 개발 빌드에만 남아 있어 다음 버전에서 고쳐질 예정이다",
    "프로덕션에서도 두 번 실행되기 때문에 미리 보여 주는 것이다 — 배포 뒤 같은 동작을 보게 되므로 개발 중에 익숙해지라는 뜻이다" ] },
  { k: "react2-ctx-childrenopt", o: [
    "children 으로 전달된 컴포넌트는 자동으로 React.memo 가 적용된다 — Provider 가 자식을 감쌀 때 얕은 비교 래퍼를 붙여 준다",
    "children 은 Provider 바깥에서 만들어진 엘리먼트라 참조가 그대로면 재사용되고, 그 안의 소비자만 리렌더된다",
    "Context 는 children prop 에는 전파되지 않는다 — Provider 의 value 는 JSX 로 직접 적은 자식에게만 내려가고 prop 으로 받은 자식은 제외된다",
    "Provider 는 children 을 정적으로 취급해 리렌더하지 않는다 — value 가 바뀌면 소비자에게 알림만 보내고 트리 자체는 건드리지 않는다" ] },
  { k: "transition", o: [
    "즉시 동기로 처리되어 최우선 반영된다 — transition 은 '전환' 이라는 이름대로 화면 전환 업데이트를 가장 먼저 그리게 한다",
    "네트워크 요청을 자동 재시도한다 — 감싼 콜백 안의 fetch 가 실패하면 React 가 pending 상태를 유지하며 다시 부른다",
    "긴급하지 않은 업데이트로 표시되어 급한 업데이트에 의해 중단/양보될 수 있다",
    "다른 스레드(웹워커)에서 실행된다 — 무거운 렌더를 메인 스레드 밖으로 옮겨 입력이 막히지 않게 하는 것이 목적이다" ] },
  { k: "rx-state-server", o: [
    "서버 상태는 변하지 않으므로 한 번만 읽으면 된다 — 서버에서 받은 값은 다음 배포까지 고정이라 컴포넌트가 다시 물을 이유가 없다",
    "서버 상태는 useState 에 담을 수 없기 때문이다 — 비동기로 도착하는 값은 훅의 초기값이 될 수 없어 별도 저장소가 필요하다",
    "서버 상태는 리렌더를 일으키지 않기 때문이다 — 외부에서 온 값은 React 의 변경 감지 밖에 있어 화면이 갱신되지 않는다",
    "진실의 원천이 서버에 있어 화면의 값은 캐시일 뿐이므로 신선도·재검증·무효화가 필요하다" ] },
  { k: "react2-rt-restore", o: [
    "라우터가 스크롤 값을 자동으로 지우기 때문 — 경로가 바뀔 때마다 history 항목의 스크롤 정보를 초기화하도록 되어 있다",
    "브라우저가 뒤로가기 시 스크롤 위치를 0 으로 강제하기 때문 — SPA 에서는 history.scrollRestoration 이 항상 manual 로 꺼져 있다",
    "목록 데이터가 아직 로딩 중이라 문서 높이가 부족해 목표 위치까지 스크롤할 수 없기 때문",
    "뒤로가기에서는 스크롤 API 를 호출할 수 없기 때문 — popstate 이벤트 중에는 window.scrollTo 가 무시된다" ] },
  { k: "rx-comp-context-when", o: [
    "성능을 높이고 싶을 때는 언제나 — Context 는 props 를 거치지 않으므로 중간 컴포넌트의 리렌더가 사라져 항상 빠르다",
    "트리 곳곳의 많은 컴포넌트가 필요로 하고 자주 바뀌지 않는 값(테마, 로케일, 현재 사용자)일 때",
    "값이 객체일 때는 항상 — 객체는 props 로 넘기면 참조가 바뀌어 리렌더를 일으키므로 Context 로 감싸 두는 것이 원칙이다",
    "props 를 두 단계 이상 전달해야 할 때는 Context 를 쓴다 — 한 단계만 건너뛰어도 prop drilling 이므로 곧바로 Context 로 옮긴다" ] },
] },
{ track: "dbt", fixes: [
  { k: "미리 계산해 두기", o: [
    "미리 계산한 값은 최신으로 유지되므로 낡을 일이 없다 — 원본이 바뀔 때마다 결과도 함께 갱신되도록 데이터베이스가 연결해 두기 때문이다",
    "저장 공간만 조금 더 쓸 뿐 다른 대가는 없다 — 결과 표는 원본보다 훨씬 작으므로 공간 부담도 미미하고 조회는 그대로 빠르다",
    "조회는 빨라지지만 결과가 갱신 시점만큼 낡는다. 얼마나 낡아도 되는지를 정하고, 그에 맞춰 다시 계산하는 주기와 비용을 정해야 한다",
    "원본 데이터를 지울 수 있게 되므로 오히려 공간이 준다 — 집계 결과가 있으면 수억 행의 원본은 보관할 이유가 없어진다" ] },
  { k: "필요한 열만 읽는다", o: [
    "열 지향은 데이터를 메모리에 전부 올려 두기 때문이다 — 메모리에 올려 두면 어떤 질의든 디스크를 거치지 않으므로 열이 몇 개든 빠르게 끝난다",
    "쓰는 두 열만 읽고 나머지 98개는 건드리지 않는다. 행 지향은 행 전체가 붙어 있어 필요 없는 열까지 읽으므로 읽는 양이 수십 배 차이 난다",
    "열 지향은 인덱스를 자동으로 모든 열에 만들어 두기 때문이다 — 지역 열의 인덱스로 그룹을 바로 찾고 금액 열의 인덱스로 합을 구한다",
    "열 지향은 집계 함수를 하드웨어로 처리하므로 계산이 빠르다 — SUM 과 GROUP BY 를 벡터 명령으로 돌려 CPU 시간이 크게 준다" ] },
  { k: "누가 시각을 찍나", o: [
    "앱에서 넣으면 서버마다 시계가 조금씩 달라 순서가 뒤집힐 수 있고, 데이터베이스에서 넣으면 시계가 하나뿐이라 일관된다",
    "앱에서 넣는 편이 정확하다 — 요청이 도착한 시각을 아는 것은 앱뿐이고, 데이터베이스는 커밋 시각이라 실제보다 늦게 찍힌다",
    "두 방식의 결과는 같으므로 취향의 문제다 — 어느 쪽이든 NTP 로 맞춘 시계를 쓰므로 밀리초 이하의 차이만 나고 순서에는 영향이 없다",
    "데이터베이스에서 넣으면 쓰기 성능이 크게 떨어진다 — 행마다 시스템 시계를 읽는 호출이 끼어들어 대량 삽입에서 병목이 된다" ] },
  { k: "인덱스미사용", o: [
    "조건에 매칭되는 행의 예상 비율(selectivity)이 높아, 랜덤 I/O 로 인덱스를 타는 비용이 순차 스캔보다 크다고 추정하기 때문",
    "인덱스가 손상되어 있기 때문 — 통계 갱신이 밀리면 인덱스 항목과 실제 행이 어긋나므로 옵티마이저가 신뢰하지 않고 테이블을 읽는다",
    "인덱스는 OR 조건에서만 쓸 수 있기 때문 — AND 로 묶인 조건은 한 인덱스로 풀 수 없어 옵티마이저가 처음부터 seq scan 을 고른다",
    "seq scan 이 더 정확한 결과를 주기 때문 — 인덱스는 갱신이 늦을 수 있어 옵티마이저는 최신 데이터가 필요하면 테이블을 직접 읽는다" ] },
  { k: "WAL체크포", o: [
    "체크포인트는 WAL 을 잘라내지 못한다 — 로그는 감사 목적으로 영구 보관되므로 계속 쌓이기만 하고 용량은 줄지 않는다",
    "WAL 은 데이터 페이지보다 나중에 기록된다 — 데이터가 먼저 안전하게 반영된 뒤에야 그 사실을 로그에 남기는 순서라 이름이 그렇다",
    "체크포인트는 더티 버퍼를 데이터 파일에 flush 해 복구 시작 지점을 앞당기고, 그 이전 WAL 은 재활용/제거할 수 있게 한다",
    "체크포인트가 잦을수록 복구 시간이 길어진다 — 체크포인트마다 복구 지점이 추가되어 재생할 구간이 늘어나기 때문이다" ] },
  { k: "카디널리티", o: [
    "조인은 nested loop 만 쓰기 때문 — 그래서 조인 순서를 옵티마이저에 맡기면 어떤 통계가 있든 결과 플랜은 같다",
    "해시 조인은 카디널리티를 쓰지 않기 때문 — 해시 테이블은 크기와 무관하게 만들어지므로 추정이 틀려도 플랜이 바뀌지 않는다",
    "통계가 실시간으로 언제나 정확하기 때문 — 정확한 통계로 계산한 플랜이 틀린다면 옵티마이저 비용 모델 자체의 결함이다",
    "다중 컬럼 간 상관관계(correlation)를 무시하고 조건들의 선택도를 독립적으로 곱해 행 수를 과소/과대 추정하기 때문" ] },
] },
{ track: "algo", fixes: [
  { k: "algo-dp-knapsack-order", o: [
    "정순으로 순회하면 같은 물건을 여러 번 사용한 값을 참조하게 되어 완전 배낭 문제가 되어 버리기 때문",
    "역순으로 돌아야만 전체 시간복잡도가 정확히 O(nW) 로 맞춰지기 때문 — 정순은 갱신된 칸을 다시 읽어 반복 횟수가 늘어난다",
    "역순으로 돌아야 배열 인덱스가 음수로 내려가지 않기 때문 — w 가 weight 보다 작아지는 순간 dp[w - weight] 가 범위 밖으로 나간다",
    "역순 접근이 캐시 적중률이 더 좋아 빠르기 때문 — 배열 끝에서 시작하면 프리페처가 뒤쪽 블록을 미리 올려 둔다" ] },
  { k: "algo2-opt-early-exit", o: [
    "함께 쓰는 메모리 양이 눈에 띄게 줄어들기 때문 — 끝까지 돌지 않으면 뒤쪽 원소를 위한 임시 저장소가 필요 없어진다",
    "빅오 복잡도 자체가 낮아지기 때문 — 평균적으로 절반에서 멈추면 O(n) 이 O(n/2) 가 되고 그것이 곧 복잡도 개선이다",
    "데이터가 정렬된 상태가 되기 때문 — 조건을 만나 멈춘 지점을 경계로 앞쪽은 검사가 끝난 정렬 구간이 된다",
    "평균적으로 불필요한 연산을 건너뛰어 실제 실행 시간을 크게 줄이기 때문" ] },
  { k: "algo-dp-pseudo-poly", o: [
    "계산하는 과정에서 O(W) 만큼의 추가 메모리를 계속 붙들고 있어야 하기 때문 — 시간과 공간을 함께 따지면 다항이 아니다",
    "겉보기와 달리 실제 수행 시간이 O(n^2) 이기 때문 — W 가 n 에 비례하는 입력이 대부분이라 결국 제곱 시간이 된다",
    "W 는 입력의 크기가 아니라 입력에 적힌 값이며, 그 값을 표현하는 비트 수에 대해서는 지수적이기 때문",
    "입력에서 n 값이 매우 커질 수 있기 때문 — 물건 수가 수백만이면 nW 가 실질적으로 지수 함수처럼 자란다" ] },
  { k: "algo-gr-local-global", o: [
    "문제의 크기가 커지기만 하면 어떤 문제에서든 참이 되는 명제 — 입력이 크면 지역 최적들의 평균이 전역 최적으로 수렴한다",
    "정렬을 먼저 해 두면 어떤 문제에서든 참이 된다 — 정렬된 입력에서는 매 단계 최선의 선택이 뒤의 선택을 방해하지 않는다",
    "모든 최적화 문제에서 참인 명제다 — 최적해는 최적 부분해로 이루어지므로 부분마다 최적을 고르면 전체도 최적이 된다",
    "탐욕 선택 속성이 성립하는 특수한 문제 구조에서만 참이며 일반적으로는 거짓이다" ] },
  { k: "algo-str-collision", o: [
    "해시 충돌이라는 것은 이론적으로만 존재할 뿐 데이터를 다룰 때에는 일어나지 않는다 — 64비트 해시라면 충돌 확률은 사실상 0 이다",
    "해시값이 같으면 두 문자열은 같은 문자열이다 — 좋은 해시 함수는 서로 다른 입력에 서로 다른 값을 주도록 설계되어 있다",
    "해시값이 다르더라도 두 문자열이 같을 수 있다 — 같은 문자열도 계산 순서에 따라 값이 달라질 수 있으므로 다시 비교해야 한다",
    "해시값이 같아도 다른 문자열일 수 있으므로 실제 비교를 하거나 서로 다른 모듈러의 이중 해시를 쓴다" ] },
] },
{ track: "arch", fixes: [
  { k: "빨리 끝내고 자기", o: [
    "B 다 — 어떤 주파수에서도 빨리 끝내는 쪽이 이기므로 주파수는 높을수록 좋다. 전력이 뛰어도 시간이 그보다 더 크게 줄어 총 에너지는 언제나 내려가고, 예외는 없다",
    "대개 B 다 — 쉴 때 전력이 거의 0 이라 활성 시간을 줄이는 이득이 순간 전력의 손해를 넘어서는 경우가 많다. 다만 주파수를 아주 높이면 역전될 수 있다",
    "A 다 — 순간 전력이 낮을수록 총 에너지도 적다. 에너지는 전력에 비례하므로 절반 주파수로 두 배 오래 돌리면 전력이 반의 반이 되어 총량은 절반으로 준다",
    "둘은 같은 일을 하므로 총 에너지도 같다 — 명령어 수가 같으면 스위칭 횟수가 같고, 에너지는 스위칭 횟수로 정해지므로 속도는 무관하다" ] },
  { k: "나눗셈 비용", o: [
    "정수 나눗셈은 컴파일러가 지워 주므로 신경 쓸 필요가 없다 — 어떤 나눗셈이든 곱셈으로 바꾸므로 소스에 남아 있어도 비용이 없다",
    "나눗셈과 곱셈은 같은 유닛을 쓰므로 비용이 같고 다른 이유가 있다 — 빨라진 것은 루프 안의 분기가 함께 사라졌기 때문이다",
    "나눗셈이 느린 이유는 결과가 부동소수로 승격되기 때문이다 — 정수끼리 나눠도 내부적으로 double 로 계산한 뒤 다시 정수로 바꾼다",
    "나눗셈은 곱셈보다 훨씬 느리고(수십 사이클) 파이프라인도 잘 안 되므로, 상수 나눗셈은 곱셈과 시프트로 바꾸는 것이 정석이다" ] },
  { k: "노트북과 서버는 다르다", o: [
    "노트북과 서버는 명령어 집합이 달라 비교할 수 없다 — 같은 코드라도 다른 기계어로 번역되므로 한쪽에서 잰 성능 수치를 다른 쪽에 옮겨 적는 것은 애초에 의미가 없다",
    "서버는 가상화 오버헤드 때문에 어떤 개선도 반영되지 않는다 — 하이퍼바이저가 CPU 시간을 다른 VM 과 나누므로 코드 수준의 개선은 그 잡음에 묻혀 지표에 나타나지 않는다",
    "지속 가능한 주파수와 코어 수가 크게 달라, 노트북에서 열에 막혀 있던 것이 서버에서는 안 막히고 그 반대도 생긴다 — 노트북 개선의 상당 부분이 '스로틀링을 덜 유발한 것' 일 수 있어서",
    "서버는 더 빠르므로 개선 폭이 작아 보이는 것뿐이다 — 같은 절대 시간을 줄여도 전체가 짧으면 비율이 작아지므로 착시일 뿐이고, 개선 자체는 그대로 옮겨 가므로 다시 잴 필요가 없다" ] },
  { k: "잔여 처리", o: [
    "그래서 배열 크기를 8의 배수로 강제해야만 벡터화할 수 있다 — 나머지가 생기는 입력은 벡터 명령이 다룰 수 없으므로 패딩으로 채워 넣는다",
    "남은 3개를 하나씩 따로 처리해야 한다 — 벡터 반복문 뒤에 스칼라 반복문이 붙는 것이 보통의 형태다",
    "벡터 명령이 알아서 3개만 처리하므로 추가 작업이 없다 — 마지막 묶음은 하드웨어가 길이를 보고 남는 칸을 비워 둔다",
    "남은 3개는 무시해도 된다 — 벡터 처리에서는 0.3% 정도의 오차를 허용 범위로 보고, 정확한 값이 필요하면 스칼라 코드를 쓴다" ] },
  { k: "쓴 것보다 많이 쓰인다", o: [
    "정상적인 오차 범위이며 성능이나 수명과는 관계가 없다 — 카운터가 세는 단위가 달라 생기는 표시 차이일 뿐이고, 펌웨어를 올리면 값이 맞춰지기도 한다",
    "쓰기 증폭이다. 정리 과정에서 살아 있는 페이지를 옮겨 적기 때문에 생긴다 — 여유 공간을 늘리고, 작고 흩어진 쓰기 대신 크고 순차적인 쓰기를 하면 준다",
    "쓰기 증폭이며 컨트롤러 결함이므로 펌웨어 업데이트 외에는 방법이 없다 — 호스트 쪽 쓰기 패턴은 내부 카운터에 영향을 주지 못하므로 제조사가 고쳐야 한다",
    "압축이 풀린 것이며 압축을 켜면 1 에 가까워진다 — 컨트롤러가 데이터를 압축하지 않고 저장하면 페이지마다 메타데이터가 붙어 세 배까지 부풀 수 있다" ] },
] },
{ track: "cloud", fixes: [
  { k: "리뷰의 값어치", o: [
    "적용 시간이 짧아진다 — 리뷰가 끝난 변경은 검증을 건너뛰므로 apply 가 곧바로 돌아간다",
    "요금이 자동으로 계산된다 — 리뷰 화면이 바뀐 자원의 월 비용을 붙여 주므로 승인 전에 예산을 확인할 수 있다",
    "상태 파일 잠금이 필요 없어진다 — 한 번에 한 변경만 리뷰를 통과하므로 동시 적용이 생기지 않는다",
    "보안 그룹이 넓게 열리는 변경을 배포 전에 잡을 수 있다" ] },
  { k: "아웃바운드 제한", o: [
    "네트워크 요금이 줄어든다 — 나가는 트래픽에 요금이 붙으므로 목적지를 좁히면 청구액이 눈에 띄게 준다",
    "인스턴스의 성능이 좋아진다 — 허용된 곳으로만 패킷을 보내므로 네트워크 스택이 처리할 규칙이 줄어 지연이 낮아진다",
    "침해당했을 때 데이터가 밖으로 나가는 것을 늦출 수 있다",
    "들어오는 공격 자체를 막을 수 있다 — 응답 패킷이 나갈 수 없으면 공격자는 연결을 맺지 못하므로 인바운드 규칙과 같은 효과다" ] },
  { k: "옮기지 않는 선택", o: [
    "새 서비스를 빠르게 시작해야 할 때 — 장비를 사서 세팅하는 것보다 클라우드 계정을 만드는 편이 며칠은 더 걸리기 때문이다",
    "부하가 계절에 따라 크게 오르내릴 때 — 변동이 크면 클라우드 요금도 함께 출렁여 예산을 세울 수 없으므로 자체 장비가 낫다",
    "여러 지역의 사용자에게 서비스해야 할 때 — 지역마다 리전을 따로 계약해야 해 관리 부담이 자체 데이터센터보다 커진다",
    "부하가 일정하고 이미 감가상각이 끝난 장비로 잘 돌고 있을 때" ] },
  { k: "단일 장애점 찾기", o: [
    "가장 오래된 서버를 찾아 교체한다 — 오래된 장비가 가장 먼저 고장 나므로 나이순으로 바꾸면 장애점이 사라진다",
    "구성도에서 요소를 하나씩 지워 보며 서비스가 멈추는지 본다",
    "가장 비싼 자원을 찾아 이중화한다 — 비싼 자원이 곧 핵심 자원이므로 그것부터 두 벌로 두면 단일 장애점이 없어진다",
    "CPU 사용률이 가장 높은 곳을 찾는다 — 부하가 몰린 곳이 먼저 죽으므로 사용률 상위 자원이 곧 단일 장애점이다" ] },
  { k: "과한 구성", o: [
    "비용과 복잡함이 늘고 다룰 사람이 줄어든다",
    "가용성이 낮아진다 — 구성 요소가 많을수록 고장 날 부품도 많아지므로 전체가 멈출 확률이 그만큼 올라간다",
    "성능이 떨어진다 — 거쳐야 하는 계층이 늘어 요청마다 홉이 추가되고, 그 지연이 쌓여 응답이 느려진다",
    "보안이 자동으로 약해진다 — 자원이 많으면 공격 표면도 넓어지므로 쓰지 않는 구성이 곧 침입 경로가 된다" ] },
] },
{ track: "mobile", fixes: [
  { k: "언어와 지역", o: [
    "영어를 모르는 사용자가 곤란해진다 — 언어 하나만 저장하면 기본값이 영어로 잡혀 다른 언어 사용자에게 영어 문구가 나간다",
    "영어를 읽지만 유럽에 사는 사용자처럼 언어와 지역이 다른 경우다 — 영국식 날짜와 유로화를 원하는데 미국식으로 나온다",
    "곤란해지는 사용자는 없다 — 언어를 알면 지역도 정해지므로 언어 코드 하나로 날짜·통화·숫자 형식까지 모두 결정할 수 있다",
    "여러 언어를 함께 쓰는 사용자가 곤란해진다 — 두 언어를 오가는 사람은 저장된 언어 하나로는 화면이 번갈아 바뀌지 않는다" ] },
  { k: "mob-etag", o: [
    "조건부 요청이 붙으면 인증 절차까지 자동으로 처리된다 — ETag 가 세션 토큰 역할을 하므로 별도 Authorization 헤더가 필요 없다",
    "최신 데이터를 두 배 빠르게 받는다 — 서버가 ETag 로 변경분만 계산해 차이만 내려 주므로 전체를 받는 것보다 빠르다",
    "서버가 응답을 압축해 보내 준다 — If-None-Match 가 있으면 서버는 클라이언트가 압축을 지원한다고 보고 gzip 으로 응답한다",
    "변경이 없으면 서버가 304 를 반환해 본문 전송을 생략하므로 데이터와 배터리를 아낀다" ] },
  { k: "mob-offline-first", o: [
    "UI 는 로컬 저장소를 진실 공급원으로 읽고, 네트워크는 로컬을 갱신하는 역할을 한다",
    "네트워크가 없으면 보여 줄 데이터도 없으므로 앱을 종료한다 — 낡은 데이터를 보여 주는 것보다 아무것도 보여 주지 않는 편이 정직하다",
    "캐시를 절대 쓰지 않는다 — 오프라인 상태는 예외 상황이므로 평소에는 서버 응답만 신뢰하고 캐시는 혼란만 키운다",
    "모든 데이터를 매번 서버에서 새로 받는다 — 오프라인일 때만 마지막 응답을 잠시 보여 주고, 연결되면 곧바로 전부 다시 받는다" ] },
  { k: "mob-ios-states", o: [
    "백그라운드로 간 뒤 곧 suspended 상태가 되어 코드 실행이 멈추고, 메모리 압박 시 종료될 수 있다",
    "앱은 active 상태로만 존재하며, 백그라운드로 넘어가는 순간 프로세스가 정리된다 — 다시 열면 언제나 처음부터 시작된다",
    "suspended 상태에서는 메모리에서 즉시 지워진다 — 실행이 멈춘 앱을 메모리에 두는 것은 낭비이므로 시스템이 곧바로 회수한다",
    "백그라운드에서도 무제한으로 코드를 계속 실행할 수 있다 — 화면만 가려질 뿐 프로세스는 포그라운드와 같은 자원을 받는다" ] },
  { k: "mob-runtime-perm", o: [
    "실행 중에 요청하며 사용자가 언제든 설정에서 취소할 수 있으므로 매번 보유 여부를 확인해야 한다",
    "설치 시점에 목록을 보여 주고 동의를 받으므로 한 번 승인되면 영구히 유지된다 — 앱을 지우기 전까지 권한은 바뀌지 않는다",
    "설치할 때 한 번에 모두 승인되므로 실행 중 확인이 필요 없다 — 매니페스트에 적은 권한은 설치와 함께 자동으로 부여된다",
    "앱이 코드로 권한을 강제 승인할 수 있다 — 사용자가 거부해도 시스템 API 로 다시 켤 수 있으므로 거부 상태를 처리할 필요가 없다" ] },
] },
];
