module.exports = {
  track: "algo", guide: "", xp: 70,
  unit: "코드 리뷰 — 결함 찾기",
  source: "./rev_algo.cjs",
  lessons: [
    { t: "경계와 전제", n: 4,
      th: { sum: "알고리즘 코드의 사고는 **경계 한 칸**과 **말하지 않은 전제**에서 난다.",
        body: [
          { h: "구간이 줄어드는가", t: "이진 탐색에서 <code>lo = mid</code> 는 구간이 그대로일 수 있어 무한 반복이 된다. 반복문을 볼 때는 <b>매 바퀴 반드시 줄어드는 양</b>이 있는지 먼저 확인한다. 반열린 구간 <code>[lo, hi)</code> 를 쓰기로 했으면 조건과 갱신을 그 규칙에 맞춰 통일해야 한다." },
          { h: "무엇을 가정했는가", t: "투 포인터는 <b>정렬</b>을, 메모이제이션은 <b>같은 입력에 같은 출력</b>을, 다익스트라는 <b>비음수 가중치</b>를 전제한다. 전제가 이름이나 문서에 드러나지 않으면 언젠가 지켜지지 않고, 그때 오류 대신 <b>틀린 답</b>이 나온다." }],
        code: { c: "while (lo < hi) { mid = lo + ((hi-lo)>>1);\n  if (a[mid] < t) lo = mid + 1; else hi = mid; }", cap: "매 바퀴 구간이 반드시 줄어든다" },
        key: ["줄어드는 양이 있는가", "구간 규칙을 통일한다", "전제를 이름과 문서에 적는다", "전제가 깨지면 오답이 나온다"] } },
    { t: "복잡도와 자료구조", n: 4,
      th: { sum: "겉보기에 짧은 코드가 **제곱**이 되는 자리가 있다 — 그 자리를 알아보는 것이 리뷰다.",
        body: [
          { h: "숨은 순회", t: "<code>indexOf</code>·<code>includes</code> 는 매번 처음부터 훑고, 배열 <code>shift</code> 는 뒤를 전부 당긴다. 반복문 안에 이런 호출이 있으면 전체가 제곱이 되므로, <b>집합·머리 인덱스</b>로 바꾸면 한 번에 사라진다." },
          { h: "값과 참조", t: "비교 함수 없는 <code>sort</code> 는 사전순이고, <code>===</code> 는 <code>NaN</code> 을 자기 자신과 다르다고 본다. 그리고 인자로 받은 배열을 제자리에서 바꾸면 호출자가 모르는 사이 원본이 흔들린다 — <b>복사본에 작업</b>하는 것이 기본이다." }],
        code: { c: "const seen = new Set();          // O(1) 조회\nlet head = 0; const q = [x];      // shift 대신 인덱스\narr.slice().sort((a,b)=>a-b);     // 복사본을 정렬", cap: "반복 안의 훑기를 찾아낸다" },
        key: ["반복 안 indexOf 는 제곱", "shift 는 상수 시간이 아니다", "sort 는 기본이 사전순", "원본을 바꾸지 않는다"] } },
  ],
};
