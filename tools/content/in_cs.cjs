/* 컴퓨터 기초 단답 15 — 비트·진법·표현 범위를 손으로 적어 본다.
   검증:  node ver_input.cjs ./in_cs.cjs
   출력값 대조:  node chk_predict.cjs ./in_cs.cjs */
module.exports = [

{ track:"cs", t:"input", cat:"internals", k:"2진수를 10진수로",
  q:"아래 2진수 리터럴을 10진수로 적으면 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(0b1011);",
  a:["11"],
  ex:"각 자리는 오른쪽부터 1·2·4·8 이므로 <code>1+2+8 = 11</code> 입니다.\n개념: 2진수 <code>n</code> 자리는 0 부터 <code>2ⁿ - 1</code> 까지 담습니다 — 네 자리면 0~15 입니다.\n실무: 4비트가 16진수 한 글자와 정확히 대응하기 때문에, 비트를 눈으로 읽을 때는 16진수로 적는 편이 편합니다." },

{ track:"cs", t:"input", cat:"internals", k:"모두 1인 바이트",
  q:"8비트 2의 보수에서 -1 은 모든 비트가 1 입니다. 이를 16진수 두 글자로 적으면? (16진수, 0x 없이)",
  code:"console.log(((-1 >>> 0) & 0xFF).toString(16));",
  a:["ff"],
  ex:"2의 보수에서 -1 은 <b>모든 비트가 1</b> 인 값이라 한 바이트로는 <code>0xFF</code> 입니다.\n개념: 그래서 -1 은 비트 마스크로 '전부 켜기' 를 뜻하기도 합니다.\n실무: 부호 없는 타입에 -1 을 넣으면 그 타입의 최댓값이 됩니다 — C 에서 <code>(size_t)-1</code> 이 최대값 관용구인 이유입니다." },

{ track:"cs", t:"input", cat:"internals", k:"둘 다 켜진 비트만",
  q:"두 수를 비트 단위로 <b>AND</b> 하면 얼마가 될까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(5 & 3);",
  a:["1"],
  ex:"<code>101</code> 과 <code>011</code> 에서 <b>둘 다 1 인 자리</b>만 남으므로 <code>001</code>, 곧 1 입니다.\n개념: AND 는 '<b>마스킹</b>' 에 씁니다 — <code>x &amp; 0xFF</code> 는 하위 한 바이트만 남깁니다.\n실무: 짝수 판정 <code>x &amp; 1</code>, 2 의 거듭제곱 나머지 <code>x &amp; (n-1)</code> 이 대표적인 쓰임입니다." },

{ track:"cs", t:"input", cat:"internals", k:"한쪽만 켜진 비트",
  q:"두 수를 비트 단위로 <b>XOR</b> 하면 얼마가 될까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(5 ^ 3);",
  a:["6"],
  ex:"<code>101</code> 과 <code>011</code> 에서 <b>서로 다른 자리</b>만 1 이 되어 <code>110</code>, 곧 6 입니다.\n개념: 같은 값을 두 번 XOR 하면 원래대로 돌아옵니다 — <code>a ^ b ^ b === a</code>.\n실무: 이 성질 덕분에 '한 번만 나온 값 찾기', 간단한 체크섬, 패리티 계산에 두루 쓰입니다." },

{ track:"cs", t:"input", cat:"internals", k:"왼쪽으로 열 칸 밀기",
  q:"1 을 왼쪽으로 열 칸 밀면 얼마가 될까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(1 << 10);",
  a:["1024"],
  ex:"왼쪽 시프트 한 칸은 <b>2 를 곱하는 것</b>과 같으므로 <code>2¹⁰ = 1024</code> 입니다.\n개념: 그래서 <code>1 &lt;&lt; n</code> 은 'n 번 비트만 켠 값' 을 만드는 관용구입니다.\n실무: KB·MB 를 <code>1 &lt;&lt; 10</code>·<code>1 &lt;&lt; 20</code> 으로 적으면 자릿수를 잘못 셀 일이 없습니다." },

{ track:"cs", t:"input", cat:"internals", k:"부호를 채우지 않는 시프트",
  q:"-8 을 <b>부호 없는</b> 오른쪽 시프트로 한 칸 밀면 얼마가 될까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(-8 >>> 1);",
  a:["2147483644"],
  ex:"<code>&gt;&gt;&gt;</code> 는 왼쪽을 <b>0 으로</b> 채우므로, 32비트로 본 <code>-8</code>(<code>0xFFFFFFF8</code>)이 <code>0x7FFFFFFC</code> 가 됩니다.\n개념: 반면 <code>&gt;&gt;</code> 는 <b>부호 비트로</b> 채우므로 <code>-8 &gt;&gt; 1</code> 은 -4 입니다 — 나누기 2 와 같습니다.\n실무: 음수를 다룰 때 두 시프트를 섞으면 조용히 틀립니다. '부호를 지킬 것인가' 를 먼저 정하고 고르세요." },

{ track:"cs", t:"input", cat:"internals", k:"대문자 A의 번호",
  q:"영문 대문자 A 의 ASCII 코드 번호는 얼마일까요? 출력되는 값을 적으세요. (10진수)",
  code:"console.log('A'.charCodeAt(0));",
  a:["65"],
  ex:"대문자는 65 부터, 소문자는 97 부터, 숫자 <code>'0'</code> 은 48 부터 이어집니다.\n개념: 대소문자는 정확히 <b>32(비트 하나)</b> 차이라, <code>c | 32</code> 로 소문자를 만들 수 있습니다.\n실무: <code>'0'</code> 을 빼서 숫자로 바꾸는 <code>c - '0'</code> 관용구도 이 연속성 덕분입니다." },

{ track:"cs", t:"input", cat:"internals", k:"한글 한 글자의 바이트 수",
  q:"UTF-8 로 한글 음절 한 글자를 담으면 몇 바이트일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(new TextEncoder().encode('한').length);",
  a:["3"],
  ex:"UTF-8 은 가변 길이라 ASCII 는 1 바이트, 한글·한자는 <b>3 바이트</b>, 대부분의 이모지는 4 바이트입니다.\n개념: 그래서 '글자 수' 와 '바이트 수' 는 전혀 다른 값입니다.\n실무: DB 컬럼 길이를 바이트로 잡는 곳(예전 오라클·MySQL 설정)에서 한글이 잘리는 사고가 여기서 납니다 — 길이 단위가 글자인지 바이트인지 먼저 확인하세요." },

{ track:"cs", t:"input", cat:"internals", k:"배정밀도의 가수 비트",
  q:"IEEE 754 배정밀도(double)에서 가수(mantissa)에 쓰이는 비트는 몇 개일까요? (숫자)",
  a:["52"],
  ex:"부호 1 + 지수 11 + 가수 52 = 64 비트입니다. 정규화 덕분에 맨 앞의 1 은 저장하지 않아 <b>실질 정밀도는 53 비트</b>입니다.\n개념: 그래서 정수는 2⁵³ 까지만 정확히 담깁니다.\n실무: 64비트 정수 ID 를 JSON 으로 주고받으면 뒷자리가 조용히 뭉개집니다 — ID 는 <b>문자열</b>로 실어 보내세요." },

{ track:"cs", t:"input", cat:"internals", k:"정확히 담기는 가장 큰 정수",
  q:"자바스크립트에서 정수로 정확히 다룰 수 있는 가장 큰 값은 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(Number.MAX_SAFE_INTEGER);",
  a:["9007199254740991"],
  ex:"<code>2⁵³ - 1</code> 입니다. 이보다 크면 이웃한 두 정수가 <b>같은 부동소수 값</b>으로 접혀 버립니다.\n개념: 그래서 <code>9007199254740992 + 1 === 9007199254740992</code> 가 참이 됩니다.\n실무: 눈금·카운터·ID 가 이 값을 넘길 수 있으면 <code>BigInt</code> 나 문자열로 다뤄야 합니다." },

{ track:"cs", t:"input", cat:"internals", k:"어떤 소수 더하기는 맞는다",
  q:"<code>0.1 + 0.1</code> 이 <code>0.2</code> 와 같은지 물으면 무엇이 나올까요? 출력되는 값을 적으세요. (true 또는 false)",
  code:"console.log(0.1 + 0.1 === 0.2);",
  a:["true"],
  ex:"놀랍게도 참입니다. <code>0.1</code> 의 오차가 <b>두 배로 커진 값</b>이 <code>0.2</code> 의 표현과 정확히 같기 때문입니다.\n개념: 반대로 <code>0.1 + 0.2 !== 0.3</code> 입니다 — '부동소수는 언제나 틀린다' 가 아니라 <b>어떤 조합에서 틀린다</b> 가 정확한 표현입니다.\n실무: 참·거짓이 값에 따라 갈리므로 '한 번 해 보니 맞더라' 는 근거가 못 됩니다. 실수 비교는 언제나 오차를 허용하세요." },

{ track:"cs", t:"input", cat:"internals", k:"1000을 담는 비트 수",
  q:"0 부터 1000 까지의 값을 모두 담으려면 최소 몇 비트가 필요할까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(Math.ceil(Math.log2(1001)));",
  a:["10"],
  ex:"<code>2¹⁰ = 1024</code> 라 1001 가지를 담을 수 있고, <code>2⁹ = 512</code> 로는 모자랍니다.\n개념: <b>담을 값의 가짓수</b>에 로그를 취하고 올림하면 필요한 비트 수입니다.\n실무: 상태 플래그·열거값을 비트로 접을 때, 가짓수를 세어 미리 자리를 넉넉히 잡아 두면 나중에 형식을 안 바꿔도 됩니다." },

{ track:"cs", t:"input", cat:"internals", k:"패리티가 잡아내는 오류",
  q:"패리티 비트 하나로 <b>검출</b>할 수 있는 것은 비트 오류가 몇 개일 때까지일까요? (숫자)",
  a:["1", "1개", "홀수개", "홀수"],
  ex:"패리티는 1 의 개수가 짝수(또는 홀수)인지만 보므로 <b>홀수 개의 오류</b>를 잡습니다 — 가장 작은 경우가 1 개입니다. 두 비트가 함께 뒤집히면 못 잡습니다.\n개념: 검출과 <b>정정</b>은 다릅니다. 패리티는 어디가 틀렸는지는 모릅니다.\n실무: 1 비트를 정정하고 2 비트를 검출하려면 해밍 코드(SECDED)가 필요합니다 — 서버 ECC 메모리가 바로 이것입니다." },

{ track:"cs", t:"input", cat:"internals", k:"두 값이 다른 비트의 수",
  q:"두 수의 <b>해밍 거리</b>(서로 다른 비트의 개수)는 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"const d = (9 ^ 14).toString(2).split('').filter(c => c === '1').length;\nconsole.log(d);",
  a:["3"],
  ex:"<code>1001</code> 과 <code>1110</code> 은 세 자리가 다릅니다. XOR 한 뒤 <b>1 의 개수를 세는</b> 것이 곧 해밍 거리입니다.\n개념: 1 의 개수를 세는 연산을 <b>popcount</b> 라 하고, 요즘 CPU 에는 전용 명령이 있습니다.\n실무: 이미지 유사도(perceptual hash)나 오류 정정 부호의 거리 계산이 전부 이 셈입니다." },

{ track:"cs", t:"input", cat:"internals", k:"버림과 내림이 갈리는 곳",
  q:"음수를 정수로 만들 때 <b>0 쪽으로 버리는</b> 연산과 <b>더 작은 쪽으로 내리는</b> 연산은 결과가 다릅니다. 아래 두 값의 차이는 얼마일까요? 출력되는 값을 적으세요. (숫자)",
  code:"console.log(Math.trunc(-2.5) - Math.floor(-2.5));",
  a:["1"],
  ex:"<code>Math.trunc(-2.5)</code> 는 -2, <code>Math.floor(-2.5)</code> 는 -3 이라 차이가 1 입니다.\n개념: 양수에서는 둘이 같아서, 음수를 넣기 전까지는 차이를 모릅니다.\n실무: C 계열의 정수 나눗셈과 <code>%</code> 는 <b>0 쪽으로 버리고</b>, 파이썬은 <b>내림</b>입니다 — 언어를 옮길 때 음수 나눗셈이 조용히 달라집니다." },

];
