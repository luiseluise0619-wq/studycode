module.exports = {
  track: "numpy", guide: "", xp: 60,
  unit: "단답으로 확인하기 — 모양·타입·뷰",
  source: "./in_numpy.cjs",
  lessons: [
    { t: "모양과 타입이 정하는 것", n: 8,
      th: { sum: "넘파이에서 틀리는 자리는 대개 셋이다 — **모양이 몰래 늘어났거나**, **타입이 기본값이었거나**, **자른 것이 사본이 아니었거나**.",
        body: [
          { h: "모양은 늘어난다", t: "길이가 1 인 축은 상대 쪽 길이만큼 <b>늘어나 맞춰진다</b>(브로드캐스팅). 규칙은 뒤 축부터 견주어 '같거나 한쪽이 1' 이면 통과이고, 축 수가 다르면 앞쪽에 1 을 채워 맞춘다 — 그래서 <code>(3,)</code> 과 <code>(2,1)</code> 을 곱하면 <code>(2,3)</code> 이 된다. 의도치 않게 큰 배열이 만들어지는 사고가 여기서 나고, 행렬 곱이 목적이면 <code>*</code> 가 아니라 <code>@</code> 를 써야 한다. <code>axis</code> 는 '<b>그 축이 사라진다</b>' 로 외운다." },
          { h: "타입은 기본값을 따른다", t: "정수 리스트로 만들면 <code>int64</code>(윈도우는 32), <code>zeros</code>·<code>ones</code> 는 <b><code>float64</code></b> 가 기본이라 인덱스 배열을 만들려다 실수가 나오는 일이 흔하다. 정수의 넘침은 <b>예외 없이 조용히</b> 일어난다. 원소가 모두 같은 타입이라 메모리가 촘촘한데(64비트 정수 3 개 = 24 바이트), 파이썬 리스트는 객체 포인터를 담아 원소마다 수십 바이트를 쓴다 — 큰 데이터는 <code>float32</code> 로 낮추기만 해도 절반이 된다." }],
        code: { c: "reshape(2, -1)         # 나머지는 알아서\nsum(axis=0)            # 축 0 이 사라진다\nnp.zeros(n, dtype=int) # 정수 인덱스가 필요하면", cap: "모양과 타입을 먼저 확인한다" },
        key: ["뒤 축부터 견주어 1 이면 늘어난다", "axis 는 사라지는 축", "zeros 는 실수가 기본", "넘침은 조용히 일어난다"] } },
    { t: "자르고 고르고 이어 붙이기", n: 7,
      th: { sum: "슬라이스는 **뷰**이고 팬시 인덱싱은 **복사**다. 이 한 줄을 알면 '왜 원본이 바뀌었지' 가 없어진다.",
        body: [
          { h: "뷰와 복사", t: "기본 슬라이스는 같은 메모리를 가리키는 <b>뷰</b>라 조각을 고치면 원본이 바뀐다 — 파이썬 리스트 슬라이스가 복사인 것과 정반대다. 덕분에 큰 배열을 잘라도 메모리를 더 쓰지 않는다. 원본을 지키려면 <code>.copy()</code> 를 명시하고, 반대로 정수 배열·불리언 마스크로 고르는 <b>팬시 인덱싱</b>은 언제나 복사다. 이어 붙이기는 <b>새 메모리에 통째로 복사</b>하므로 반복문 안에서 하나씩 붙이면 <code>O(n²)</code> 가 된다." },
          { h: "고르는 방법", t: "비교 결과는 참·거짓 배열이라 더하면 <b>조건을 만족하는 개수</b>가 되고, 인덱스로 넣으면 그 원소만 골라진다 — 반복문 없이 거르는 표준 방법이다. 여러 조건은 <code>and</code> 가 아니라 <code>&amp;</code>·<code>|</code> 로 엮고 <b>괄호를 꼭</b> 친다. 슬라이스의 끝은 <b>제외</b>라 조각 길이가 <code>끝 - 시작</code> 으로 딱 떨어지고, <code>linspace</code> 는 개수를 주고 <b>끝을 포함</b>해 부동소수 오차로 마지막 점이 빠지지 않는다." }],
        code: { c: "b = a[1:3]          # 뷰 — 고치면 원본도 바뀐다\na[(a > 1) & (a < 5)]  # 괄호를 꼭 친다\nnp.concatenate(조각들)  # 마지막에 한 번만", cap: "자른 것이 사본인지 먼저 묻는다" },
        key: ["슬라이스는 뷰, 팬시는 복사", "마스크를 더하면 개수", "조건은 & | 와 괄호로", "이어 붙이기는 마지막에 한 번"] } },
  ],
};
