/* 10차 이론 — python FastAPI, 시니어 면접 심화, 시니어 디버깅.

   세 유닛 모두 레슨 셋이 같은 글을 나눠 쓰고 있었고, '동기·비동기·블로킹',
   '인스턴스·바인딩·참조' 같은 말을 풀지 않고 썼다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

python: [
{
  unit: "FastAPI 아키텍처와 요청 처리", lesson: "ASGI · uvicorn · gunicorn 워커",
  th: {
    sum: "웹 서버와 파이썬 코드 사이에는 **약속된 규격**이 있다. 요즘 것이 ASGI 다.",
    body: [
      { h: "규격이 있어야 갈아 끼운다", t: "서버와 앱이 정해진 방식으로 주고받기로 약속하면, 서버를 바꿔도 앱을 고치지 않아도 된다. 이 약속이 없으면 서버마다 앱을 다시 써야 한다." },
      { h: "옛 규격은 한 번에 하나였다", t: "이전 규격(WSGI)은 요청 하나를 끝내야 다음 요청을 처리했다. 그래서 응답을 기다리는 동안 그 일꾼은 아무것도 못 했다. ASGI 는 기다리는 동안 다른 요청을 처리할 수 있게 한다." },
      { h: "uvicorn 이 실제로 돌린다", t: "`uvicorn` 은 ASGI 약속을 지키는 서버다. 요청을 받아 우리 앱 함수를 부르고 결과를 돌려준다. 개발할 때는 이것 하나로 충분하다." },
      { h: "여러 개를 띄워 나눠 받는다", t: "일꾼 하나로는 CPU 한 개밖에 못 쓴다. `gunicorn` 같은 관리자가 uvicorn 여러 개를 띄우고 요청을 나눠 준다. 하나가 죽어도 관리자가 다시 띄워 준다 — 안 그러면 그 순간 서비스가 멈춘다." },
    ],
    code: { c: "uvicorn app:api --host 0.0.0.0 --port 8000\n\ngunicorn -k uvicorn.workers.UvicornWorker -w 4 app:api", cap: "약속을 지키면 갈아 끼울 수 있다" },
    key: ["서버와 앱 사이의 약속", "기다리는 동안 다른 요청을 받는다", "여러 개를 띄워 나눠 받는다"],
  },
},
{
  unit: "FastAPI 아키텍처와 요청 처리", lesson: "의존성 주입과 미들웨어",
  th: {
    sum: "필요한 것을 **함수 인자로 받아 오게** 하는 것이 의존성 주입이다. 공통 처리는 미들웨어가 맡는다.",
    body: [
      { h: "필요한 것을 선언만 한다", t: "핸들러가 데이터베이스 연결을 직접 만들면, 시험할 때 진짜 데이터베이스가 있어야 한다. 인자로 받게 해 두면 가짜를 넣어 볼 수 있다. 무엇이 필요한지가 함수 서명에 그대로 적히는 것도 이점이다." },
      { h: "뒷정리까지 맡길 수 있다", t: "의존성 함수에서 `yield` 로 넘겨주면, 응답이 끝난 뒤 그 아래 줄이 실행된다. 연결을 닫는 자리다. 안 닫으면 요청이 쌓일수록 연결이 남아 언젠가 더 못 받는다." },
      { h: "미들웨어는 모든 요청을 지나간다", t: "로그 남기기, 처리 시간 재기, 공통 헤더 붙이기처럼 모든 요청에 필요한 일은 미들웨어에 둔다. 핸들러마다 복사해 두면 하나를 빠뜨렸을 때 그 경로만 조용히 기록이 없다." },
      { h: "무거운 일을 넣지 않는다", t: "미들웨어는 모든 요청에서 돈다. 여기에 오래 걸리는 일을 넣으면 전체가 그만큼 느려진다. 특정 경로에만 필요한 것은 그 경로의 의존성으로 두는 편이 낫다." },
    ],
    code: { c: "def get_db():\n    db = connect()\n    try:\n        yield db\n    finally:\n        db.close()\n\n@api.get(\"/users\")\ndef list_users(db = Depends(get_db)): ...", cap: "선언하면 넣어 준다" },
    key: ["필요한 것을 인자로 받는다", "`yield` 아래가 뒷정리", "공통 처리는 미들웨어로"],
  },
},
{
  unit: "FastAPI 아키텍처와 요청 처리", lesson: "pydantic 요청 검증과 async 블로킹 함정",
  th: {
    sum: "들어오는 값은 **타입으로 검사**하고, 오래 걸리는 일은 다른 곳으로 넘긴다.",
    body: [
      { h: "검사를 한 곳에 모은다", t: "요청 본문을 모델로 선언해 두면 형식이 틀린 요청은 핸들러에 닿기 전에 걸러진다. 핸들러 안에서 하나씩 확인하면 빠뜨리는 자리가 생기고, 그 값이 그대로 저장된다." },
      { h: "틀린 이유를 자동으로 알려 준다", t: "어떤 항목이 왜 틀렸는지가 응답으로 나간다. 직접 만들면 메시지가 제각각이 되어, 쓰는 쪽에서 다시 규칙을 맞춰야 한다." },
      { h: "async 안에서 막으면 전부 멈춘다", t: "`async def` 안에서 `time.sleep` 이나 무거운 계산을 하면, 그동안 다른 요청도 처리되지 않는다. 일꾼 하나가 그 자리에 붙들려 있기 때문이다. 요청 몇 개만 겹쳐도 응답이 확 느려진다." },
      { h: "막는 코드는 넘긴다", t: "기다림이 아니라 계산이거나, 비동기를 지원하지 않는 라이브러리라면 별도 스레드로 넘긴다. FastAPI 는 일반 `def` 로 선언하면 알아서 그렇게 처리해 준다 — 굳이 `async` 를 붙이지 않는 것도 방법이다." },
    ],
    code: { c: "class NewUser(BaseModel):\n    name: str\n    age: int\n\n@api.post(\"/users\")\ndef create(u: NewUser): ...    # 무거우면 async 를 붙이지 않는다", cap: "검사는 모델로, 막는 일은 밖으로" },
    key: ["검사는 모델 한 곳에", "틀린 이유가 자동으로 나간다", "`async` 안에서 막지 않는다"],
  },
},
{
  unit: "시니어 면접 심화", lesson: "디스크립터와 property",
  th: {
    sum: "속성을 읽고 쓸 때 **함수가 대신 도는 장치**가 있다. `property` 가 그 가장 쉬운 얼굴이다.",
    body: [
      { h: "겉모습은 그대로 두고 속을 바꾼다", t: "`t.celsius` 처럼 그냥 값처럼 쓰면서, 실제로는 함수가 돌게 할 수 있다. 그래서 나중에 검사나 계산을 끼워 넣어도 쓰는 쪽 코드를 고치지 않아도 된다. 처음부터 `get_celsius()` 로 만들 필요가 없다는 뜻이다." },
      { h: "검사 자리가 생긴다", t: "값을 넣는 함수(세터)에서 범위를 확인하면, 잘못된 값이 들어오는 순간 막힌다. 그냥 속성이면 어떤 값이든 들어오고, 한참 뒤 화면에 이상한 값이 뜨고 나서야 알게 된다." },
      { h: "디스크립터는 그 일반형이다", t: "`__get__`·`__set__` 을 가진 객체를 클래스 속성으로 두면, 그 속성을 다룰 때마다 그 메서드가 불린다. `property` 도 이것으로 만들어져 있다. 같은 규칙을 여러 속성에 재사용할 때 직접 만든다." },
      { h: "클래스에 두어야 동작한다", t: "디스크립터는 **클래스** 속성일 때만 동작한다. 인스턴스에 붙이면 그냥 평범한 값이 되어 아무 일도 일어나지 않는다. 동작하지 않는다면 어디에 두었는지부터 확인한다." },
    ],
    code: { c: "class Temp:\n    @property\n    def celsius(self): return self._c\n\n    @celsius.setter\n    def celsius(self, v):\n        if v < -273.15: raise ValueError\n        self._c = v", cap: "값처럼 쓰지만 함수가 돈다" },
    key: ["겉모습은 값, 속은 함수", "세터가 검사 자리", "디스크립터는 클래스에 둔다"],
  },
},
{
  unit: "시니어 면접 심화", lesson: "GC, 인터닝, 동일성",
  th: {
    sum: "`==` 는 **값이 같은가**, `is` 는 **같은 물건인가**를 묻는다. 파이썬은 일부 값을 미리 만들어 재사용한다.",
    body: [
      { h: "작은 수와 짧은 글자는 재사용된다", t: "파이썬은 자주 쓰는 작은 정수와 짧은 문자열을 미리 만들어 두고 돌려쓴다. 그래서 `a is b` 가 참이 되기도 한다. 이건 최적화일 뿐이라 언제나 그런 것은 아니다." },
      { h: "그래서 is 로 값을 비교하면 안 된다", t: "작은 수에서는 맞고 큰 수에서는 틀리는, 가장 헷갈리는 버그가 된다. 값을 견줄 때는 언제나 `==` 다. `is` 는 `None` 처럼 하나뿐인 것을 확인할 때만 쓴다." },
      { h: "쓰지 않게 되면 치워진다", t: "파이썬은 그 값을 가리키는 곳이 몇 군데인지 세다가, 0이 되면 메모리를 돌려준다. 그래서 대개는 신경 쓸 일이 없다." },
      { h: "서로 가리키면 안 치워진다", t: "두 객체가 서로를 가리키면 세는 값이 0이 되지 않는다. 그래서 파이썬은 따로 도는 청소기를 두어 이런 덩어리를 찾아 치운다. 이 청소가 돌 때 잠깐 멈추므로, 아주 예민한 곳에서는 잠시 꺼 두기도 한다." },
    ],
    code: { c: "a, b = 256, 256\na is b       # True — 미리 만들어 둔 것\n\nx, y = 257, 257\nx is y       # False 일 수 있다\nx == y       # 값 비교는 언제나 이것", cap: "값은 ==, 물건은 is" },
    key: ["작은 값은 재사용된다", "값 비교는 언제나 `==`", "서로 가리키면 청소기가 치운다"],
  },
},
{
  unit: "시니어 면접 심화", lesson: "MRO, __slots__, GIL",
  th: {
    sum: "여러 부모를 물려받으면 **찾는 순서**가 필요하다. 그것이 MRO 다.",
    body: [
      { h: "찾는 순서가 정해져 있다", t: "메서드를 부르면 파이썬은 정해진 순서로 클래스들을 훑으며 처음 찾은 것을 쓴다. `Klass.__mro__` 로 그 순서를 눈으로 볼 수 있다. 상속이 얽혔을 때 '왜 저쪽 것이 불리지' 는 이걸 찍어 보면 바로 풀린다." },
      { h: "super 는 부모가 아니라 다음 차례다", t: "`super()` 는 '부모' 가 아니라 **그 순서에서 다음 것**을 가리킨다. 여러 부모를 물려받았을 때 이 차이가 드러난다. 부모를 이름으로 직접 부르면 순서가 깨져 어떤 부모의 준비 코드가 통째로 건너뛰어진다." },
      { h: "__slots__ 는 속성을 못 박는다", t: "보통 객체는 속성을 담는 딕셔너리를 하나씩 갖는다. `__slots__` 로 쓸 속성을 미리 적어 두면 그 딕셔너리가 없어져 메모리가 줄고 조금 빨라진다. 대신 적어 두지 않은 속성은 붙일 수 없다." },
      { h: "GIL 은 한 번에 하나만 돌린다", t: "한 프로세스 안에서 파이썬 코드는 한 번에 한 줄만 실행된다. 그래서 계산이 많은 일은 스레드를 늘려도 빨라지지 않는다. 기다리는 일이 많을 때만 스레드가 효과를 낸다." },
    ],
    code: { c: "class C(A, B): ...\nC.__mro__        # 찾는 순서를 눈으로 본다\n\nclass P:\n    __slots__ = (\"x\", \"y\")", cap: "순서를 눈으로 확인한다" },
    key: ["찾는 순서가 정해져 있다", "`super` 는 다음 차례", "`__slots__` 는 속성을 못 박는다"],
  },
},
],

};
