/* java 트랙 — 정답이 5자 이상 단독 최장인 54문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "java", fixes: [
  { k: "java2-build-testscope", o: [
    "테스트 클래스패스가 짧아져 테스트 실행이 빨라지고, 컴파일러가 테스트 코드만 따로 최적화할 수 있기 때문",
    "test 스코프만 전이 의존성을 끌어오므로 JUnit 이 필요로 하는 hamcrest 같은 라이브러리가 함께 들어오기 때문",
    "test 스코프는 버전 충돌 해결에서 다른 스코프보다 우선하므로 테스트가 원하는 버전을 확실히 쓰기 때문",
    "테스트 코드에서만 필요하고 실제 배포 산출물과 런타임 클래스패스에는 들어가지 않아야 하기 때문" ] },
  { k: "java3-ver-principle", o: [
    "필드 순서만 지키면 이름과 타입은 무엇이든 바꿔도 되므로 순서를 문서로 고정해 두는 편이 안전하다",
    "필드의 타입은 소비자가 자동 변환하므로 언제든 바꿔도 되고, 이름만 유지하면 호환성이 지켜진다",
    "보낼 때는 관대하게 아무 필드나 더하고, 받을 때는 모르는 필드를 거부하도록 엄격해야 안전하다",
    "받을 때는 모르는 필드를 견디고, 보낼 때는 약속한 형식을 엄격히 지키는 편이 안전하다" ] },
  { k: "java3-fw-processor", o: [
    "애노테이션 프로세서는 실행 중에 소스를 다시 컴파일하므로 리플렉션보다 런타임 비용이 오히려 더 크다",
    "둘 다 런타임에 애노테이션을 읽어 동작하며, 프로세서는 캐시를 두고 리플렉션은 매번 조회한다는 점만 다르다",
    "애노테이션 프로세서는 실행 중에 이미 로딩된 클래스의 바이트코드를 직접 고쳐 리플렉션 없이 동작하게 만든다",
    "애노테이션 프로세서는 컴파일 시점에 코드를 생성해, 런타임 비용과 런타임에야 드러나는 실패를 줄인다" ] },
  { k: "java-cc-chm-null", o: [
    "정상적으로 저장된다 — HashMap 과 같은 규칙이라 null 키 하나와 null 값 여러 개를 아무 제약 없이 담을 수 있다",
    "NullPointerException이 발생한다 — 동시성 환경에서 '값이 null'과 '키가 없음'을 구분할 수 없기 때문",
    "경고만 로그에 출력되고 그대로 무시된 채 넘어가므로 null 키와 null 값을 넣어도 아무 예외 없이 정상 동작한다",
    "값만 null 이 허용된다 — 키는 해시 코드를 계산해야 하지만 값은 그대로 저장만 하면 되므로 제약이 없기 때문" ] },
  { k: "java2-null-emptycoll", o: [
    "GC 가 null 을 참조하던 컬렉션을 회수하지 못해 호출이 반복될수록 메모리가 계속 새게 된다",
    "호출부마다 null 검사가 늘어나고, 빠뜨리면 for-each에서 곧바로 NPE가 난다",
    "null 은 타입이 없으므로 제네릭 타입 정보가 소거되어 호출부에서 원소 타입을 알 수 없게 된다",
    "List&lt;T&gt; 반환형에 null 을 돌려주는 것은 타입이 맞지 않아 컴파일 오류가 발생한다" ] },
  { k: "java-cc-fixed", o: [
    "작업이 몰리면 스레드를 n 개 넘게 계속 새로 만들어 CPU 를 소진하고 컨텍스트 스위칭 비용이 폭증한다",
    "스레드가 모두 바쁠 때 들어온 작업을 큐에 넣지 않고 즉시 버리므로 요청이 아무 흔적 없이 조용히 사라진다",
    "작업 대기 큐가 사실상 무한이라 작업이 쌓이면 메모리를 소진해 OutOfMemoryError로 이어질 수 있다",
    "스레드가 하나뿐이라 병렬 처리가 되지 않고 순차 실행으로만 동작하므로 n 을 크게 줘도 아무 의미가 없다" ] },
  { k: "java-jvm-young", o: [
    "Eden 1개와 Survivor 2개(S0, S1)로 이뤄지며 객체는 Eden에서 생성된다",
    "Old 영역과 같은 구조로 나뉘며 Survivor 없이 살아남은 객체가 곧바로 Old 로 간다",
    "Eden 1개만 있으며 Minor GC 에서 살아남은 객체는 Eden 안에서 나이를 먹는다",
    "Eden 2개와 Survivor 1개로 이뤄지며 객체는 두 Eden 을 번갈아 가며 생성된다" ] },
  { k: "java2-str-substring", o: [
    "인덱스가 int 리터럴이라 substring(int, int) 오버로드를 찾지 못해 컴파일 오류가 난다",
    "원본 문자열이 null 로 바뀌어 버리므로 반환값을 받기 전에 원본을 다른 변수에 옮겨야 한다",
    "원본은 그대로이고 잘라 낸 내용을 담은 새 String이 만들어진다",
    "원본이 인덱스 1 부터 3 까지만 남도록 잘려서 짧아지고 그 결과가 그대로 반환된다" ] },
  { k: "java3-hc-missing", o: [
    "put 호출 자체가 실패하면서 곧바로 예외가 발생하므로 저장 단계에서 계약 위반을 알 수 있다",
    "정상적으로 값을 찾을 수 있다 — HashMap 은 버킷을 정한 뒤 equals 로 다시 비교하기 때문이다",
    "hashCode 없이 equals 만 재정의한 클래스는 키로 쓸 수 없어 put 줄에서 컴파일 에러가 발생한다",
    "논리적으로 같은 값의 새 키로 get을 호출하면 대개 null이 나온다 — 해시가 달라 다른 버킷을 뒤지기 때문이다" ] },
  { k: "java3-refl-nature", o: [
    "private 멤버에는 어떤 방법으로도 접근할 수 없으므로 리플렉션은 public API 만 다룰 수 있다",
    "메서드 핸들을 캐시해 두므로 직접 호출보다 항상 빠르게 동작하고 JIT 최적화도 더 잘 받는다",
    "이름 문자열로 클래스·메서드를 다루므로 컴파일 타임 검사를 우회하고, 오류가 런타임 예외로 미뤄진다",
    "컴파일 타임에 문자열로 적은 이름까지 모두 검증해 주므로 오타가 있으면 컴파일이 실패한다" ] },
  { k: "java2-build-compileonly", o: [
    "컴파일 시점에만 필요한 애너테이션 프로세서·Lombok처럼 런타임에는 불필요한 의존성",
    "실행할 때만 필요하고 컴파일에는 쓰이지 않는 JDBC 드라이버 같은 의존성",
    "테스트 코드에서만 쓰고 운영 산출물에는 들어가면 안 되는 단언 라이브러리",
    "공개 API 시그니처에 그대로 등장해 소비자도 컴파일에 필요로 하는 핵심 타입" ] },
  { k: "java3-poly-ctor", o: [
    "자식 필드가 모두 초기화된 뒤에 실행되므로 언제나 안전하고, 오히려 초기화 훅으로 권장된다",
    "생성자 안에서는 동적 디스패치가 꺼져 부모의 구현이 호출되므로 자식 필드를 건드릴 일이 없다",
    "생성자에서 재정의 가능한 메서드를 부르는 것은 언어가 금지하므로 컴파일 에러가 발생한다",
    "자식의 구현이 자식 필드 초기화 전에 실행되어 null이나 0을 보게 될 수 있다" ] },
  { k: "java3-enum-emap-impl", o: [
    "연결 리스트를 순회해 키를 찾으므로 상수가 많아지면 조회가 느려진다",
    "키의 compareTo 순서로 균형 이진 탐색 트리를 만들어 O(log n) 에 찾는다",
    "키의 ordinal을 인덱스로 쓰는 배열 기반이라 해시 충돌이 없고 매우 빠르다",
    "내부적으로 HashMap 을 그대로 쓰되 키의 hashCode 를 ordinal 로 바꿔 저장한다" ] },
  { k: "java-gen-raw", o: [
    "원소를 꺼낼 때마다 리플렉션으로 타입을 확인하므로 런타임 성능이 크게 저하된다",
    "자바 9 부터 raw type 은 허용되지 않아 컴파일 에러가 발생하고 쓸 수 없다",
    "List&lt;?&gt; 와 완전히 동일하게 동작하며 경고만 추가로 나는 표기상의 차이다",
    "제네릭 타입 검사가 통째로 꺼져 컴파일러 경고와 함께 타입 안전성을 잃는다" ] },
  { k: "java-jvm-heapstack", o: [
    "StringBuilder 인스턴스는 메타스페이스에, 지역 변수 sb(참조)는 힙에 놓인다",
    "둘 다 힙에 놓인다 — 지역 변수도 메서드가 끝날 때까지 힙에서 관리된다",
    "둘 다 스택에 놓인다 — 지역에서 만든 객체는 탈출하지 않으면 스택에 산다",
    "StringBuilder 인스턴스는 힙, 지역 변수 sb(참조)는 해당 스레드의 스택 프레임" ] },
  { k: "java3-annot-retention", o: [
    "@Target(ElementType.METHOD) 만 붙여 두면 충분하며 보존 정책은 기본값이 이미 RUNTIME 이라 신경 쓸 필요가 없다",
    "@Retention(RetentionPolicy.RUNTIME)을 붙여야 한다 — 기본값인 CLASS는 런타임 조회가 되지 않는다",
    "@Inherited 를 붙여야 한다 — 그래야 클래스 로더가 애노테이션 테이블을 만들어 리플렉션에 노출한다",
    "아무 설정 없이도 항상 읽을 수 있다 — 바이트코드에 기록된 애노테이션은 모두 런타임에 조회된다" ] },
  { k: "java2-io-vs-finally", o: [
    "finally 안에서는 try 본문의 예외를 잡을 수 없어 자원을 닫기 전에 예외가 밖으로 나가 버린다",
    "close()가 던진 예외가 본문의 원인 예외를 덮어써 버리고, 자원이 여러 개면 중첩 try가 필요하다",
    "finally 블록은 예외가 났을 때 실행이 보장되지 않아 자원이 닫히지 않은 채 남을 수 있다",
    "finally 블록은 JIT 최적화 대상에서 빠져 자원을 닫는 경로가 눈에 띄게 느려진다" ] },
  { k: "java3-ser-transient", o: [
    "필드를 JSON 직렬화에는 포함하되 자바 기본 직렬화에서만 제외한다",
    "필드를 읽기 전용으로 만들어 생성자 이후에는 값을 바꿀 수 없게 한다",
    "필드를 스레드마다 별도로 갖게 해 ThreadLocal 처럼 동작하게 한다",
    "직렬화 대상에서 제외하며, 역직렬화 후에는 기본값(null 또는 0)이 된다" ] },
  { k: "java3-perf-boxing", o: [
    "Long 은 값을 캐시하므로 성능 차이가 전혀 없고 메모리도 같다",
    "래퍼 타입이 내부적으로 최적화되어 기본형보다 오히려 더 빨라진다",
    "Long 변수에는 += 연산자를 쓸 수 없어 컴파일 에러가 발생한다",
    "반복마다 박싱으로 새 Long 객체가 만들어져 할당과 GC 부담이 커진다" ] },
  { k: "java2-io-suppressed", o: [
    "본문 예외가 던져지고 close()의 예외는 suppressed로 붙는다",
    "두 예외가 하나의 체인으로 묶여 cause 관계로 동시에 던져진다",
    "close() 의 예외가 던져지고 본문 예외는 사라져 원인을 알 수 없다",
    "두 예외 모두 무시되고 try 블록이 정상 종료된 것으로 처리된다" ] },
  { k: "java2-io-relativize", o: [
    "target 을 base 아래로 복사하고 새 경로를 돌려준다",
    "target 의 심볼릭 링크를 따라가 실제 경로로 바꾼다",
    "base를 기준으로 target까지 가는 상대 경로를 만든다",
    "두 경로를 정규화해 같은 파일인지 비교한 결과를 준다" ] },
  { k: "java3-inh-acc", o: [
    "접근제어자는 부모와 완전히 같아야 하며 조금이라도 다르면 컴파일 에러다",
    "접근제어자는 오버라이딩과 무관하므로 어느 쪽으로든 자유롭게 바꿀 수 있다",
    "부모보다 좁힐 수 없고, 같거나 더 넓게만 지정할 수 있다",
    "부모보다 더 좁은 접근제어자로 바꿔야 자식의 구현이 밖으로 새지 않는다" ] },
  { k: "java-hm-reverse", o: [
    "참 — HashMap 이 같은 버킷의 원소는 equals 가 true 가 되도록 보장한다",
    "거짓 — 해시는 충돌할 수 있으므로 hashCode가 같아도 equals는 false일 수 있다",
    "참 — 계약은 양방향이라 hashCode 가 같으면 equals 도 true 여야 한다",
    "거짓 — hashCode 가 같은 두 객체는 계약상 오히려 equals 가 false 가 된다" ] },
  { k: "추상메서드", o: [
    "구현부(body) 없이 선언만 하고 자식이 반드시 구현해야 한다",
    "private 으로 선언해야 하며 자식은 같은 이름으로 새로 만든다",
    "static 으로만 선언되고 클래스 이름으로 직접 호출한다",
    "반드시 본문을 가져야 하며 자식은 그 본문을 물려받는다" ] },
  { k: "java3-refl-refactor", o: [
    "문자열로 쓴 이름도 컴파일러가 상수로 검증해 주므로 오타는 잡히지만 이름 변경은 못 따라간다",
    "리플렉션은 컴파일 시 일반 호출 코드로 치환되므로 이름 변경도 함께 따라가 오히려 안전하다",
    "리플렉션 코드는 IDE 가 자동으로 함께 변경해 주므로 이름 변경 후에도 문제 없이 동작한다",
    "메서드나 필드 이름이 문자열이라, 이름 변경 리팩터링이나 난독화 후에 조용히 깨진다" ] },
  { k: "java3-ver-enum", o: [
    "모르는 값은 ordinal 0 인 첫 번째 상수로 해석되므로 첫 상수를 기본값으로 두면 된다",
    "구버전 소비자는 모르는 값을 만나 파싱에 실패할 수 있으므로 UNKNOWN 같은 기본 처리 경로를 미리 마련해야 한다",
    "enum 값을 새로 추가하는 것은 어떤 경우에도 불가능하며, 값 추가는 형식 자체를 깨는 파괴적 변경이다",
    "구버전 소비자도 새 값을 자동으로 이해한다 — enum 은 이름 문자열로 전달되므로 값이 늘어도 문제가 없다" ] },
  { k: "java3-perf-linkedlist", o: [
    "LinkedList 는 원소당 메모리를 ArrayList 보다 적게 쓰고 노드 오버헤드도 없어 큰 목록에 유리하다",
    "중간 삽입이 잦으면 LinkedList 가 언제나 유리하다 — 노드 연결만 바꾸면 되므로 O(1) 이다",
    "LinkedList 는 인덱스 접근이 O(1) 이다 — 내부에 인덱스 표를 두어 노드를 바로 찾는다",
    "중간 삽입도 위치를 찾는 데 O(n)이 들고 캐시 지역성이 나빠, 실무에서는 대개 ArrayList가 더 빠르다" ] },
  { k: "java3-bench-warmup", o: [
    "처음 몇 번의 실행은 인터프리터로 돌고 JIT 최적화 이전이라 유난히 느려, 워밍업 없이 재면 결과가 왜곡된다",
    "워밍업은 C 언어에서만 필요한 개념이다 — 캐시가 비어 있는 첫 실행을 제외하기 위한 것이라 JVM 에는 해당 없다",
    "JVM 은 첫 실행이 언제나 가장 빠르다 — 시간이 지날수록 GC 와 프로파일링 부담이 쌓여 느려지기 때문이다",
    "워밍업은 결과에 아무 영향을 주지 않는 관습일 뿐이며, 클래스가 로딩되는 순간 이미 최적화가 끝나 있다" ] },
  { k: "java2-time-immutable-vs-old", o: [
    "java.time 타입은 불변이라 스레드 안전하지만, SimpleDateFormat은 가변 상태를 가져 공유하면 결과가 깨진다",
    "java.time 은 밀리초 단위만 지원하므로 나노초가 필요하면 옛 API 를 써야 한다는 점이 가장 중요한 차이다",
    "SimpleDateFormat 쪽이 정밀도가 더 높고 나노초 단위까지 다룰 수 있다는 점이 가장 중요한 차이다",
    "java.time 은 타임존을 지원하지 않아 UTC 로만 다루고, 옛 API 는 지역 시각을 다룰 수 있다는 점이 다르다" ] },
  { k: "java3-enum-switch-default", o: [
    "switch 는 enum 에 쓸 수 없으므로 default 를 두든 말든 논의 대상 자체가 아니다",
    "새 상수를 추가했을 때 처리 누락이 컴파일 단계에서 드러나지 않고 런타임까지 숨는다",
    "새 상수는 자동으로 첫 번째 case 로 처리되므로 첫 case 의 동작이 의도와 어긋난다",
    "컴파일러가 default 가 있어도 모든 상수를 검사하므로 위험이 없고 경고만 사라진다" ] },
  { k: "java-jvm-suppressed", o: [
    "close 에서 난 예외가 던져지고 본문 예외는 그대로 사라져 호출자는 원래 원인을 끝내 확인할 수 없게 된다",
    "본문 예외가 던져지고 close 예외는 suppressed 예외로 붙어 getSuppressed()로 확인할 수 있다",
    "두 예외가 각각 따로 던져지므로 호출자는 반드시 catch 절을 두 개 두어 발생한 순서대로 받아 처리해야 한다",
    "두 예외가 모두 조용히 무시되고 try-with-resources 블록은 정상 종료된 것으로 처리되어 아무것도 알 수 없다" ] },
  { k: "java2-build-api-impl", o: [
    "api 는 자바 9 모듈 시스템 전용이고 implementation 은 클래스패스 전용이라 용도가 다르다",
    "api는 소비자의 컴파일 클래스패스로 전파되고, implementation은 전파하지 않고 감춘다",
    "implementation 이 더 넓은 범위로 전파되고 api 는 컴파일 시점에만 쓰이는 좁은 범위다",
    "둘은 완전히 같으며 이름만 다르다 — api 는 옛 compile 설정의 새 이름일 뿐이다" ] },
  { k: "java3-inh-finalpriv", o: [
    "둘 다 자식 클래스에서 자유롭게 오버라이딩할 수 있으며 @Override 를 붙이면 컴파일러가 서명을 확인해 준다",
    "final 메서드는 재정의가 금지되고, private 메서드는 자식에게 보이지 않아 애초에 오버라이딩 대상이 아니다",
    "static 메서드만 오버라이딩할 수 없고 나머지는 접근 제어자나 final 여부와 무관하게 모두 가능하다",
    "private 메서드는 오버라이딩되지만 final 메서드는 되지 않는다 — 접근 제어와 재정의 가능 여부는 서로 무관하다" ] },
  { k: "java3-abs-diamond", o: [
    "런타임에 둘 중 하나가 임의로 선택되므로 실행할 때마다 다른 구현이 불릴 수 있다",
    "먼저 선언한 인터페이스 쪽의 구현이 자동으로 선택되므로 implements 순서가 우선순위를 정한다",
    "컴파일 에러가 나며, 구현 클래스가 재정의해 A.super.m() 처럼 어느 쪽인지 명시해야 한다",
    "두 메서드가 implements 에 적은 순서대로 모두 실행되므로 부작용이 두 번 일어난다" ] },
  { k: "java2-str-buffer", o: [
    "StringBuffer 는 String 처럼 불변 객체이고 StringBuilder 는 가변 객체라서 문자열을 반복해 이어붙일 때 쓰임새가 갈린다",
    "StringBuilder 는 자바 5 에서 deprecated 처리된 뒤 제거되었으므로 새로 짜는 코드에서는 StringBuffer 만 쓸 수 있다",
    "StringBuffer는 메서드가 synchronized라 스레드 안전하지만 느리고, StringBuilder는 동기화가 없어 단일 스레드에서 빠르다",
    "StringBuilder 만 append 를 지원하고 StringBuffer 는 insert 와 reverse 만 지원하므로 두 클래스의 용도가 다르다" ] },
  { k: "java3-perf-intstream", o: [
    "Stream&lt;Integer&gt; 가 IntStream 보다 더 빠르다 — 캐시된 Integer 를 재사용하기 때문이다",
    "IntStream은 기본형 int를 그대로 다뤄 박싱·언박싱 오버헤드와 객체 할당을 피한다",
    "IntStream 은 내부적으로 박싱을 두 번 하므로 큰 수열에서는 오히려 메모리를 더 쓴다",
    "둘은 완전히 동일한 바이트코드로 컴파일되므로 성능 차이는 없고 API 표기만 다르다" ] },
  { k: "커스텀예외", o: [
    "Object 클래스만 상속해서 만들어야 하며 예외 계층과는 무관하게 throw 할 수 있다",
    "enum 으로 정의하고 각 상수를 예외 종류로 쓰면 throw 문에 바로 넘길 수 있다",
    "Throwable 인터페이스를 구현하면 어떤 클래스든 예외로 던질 수 있다",
    "Exception 또는 RuntimeException을 상속하는 클래스를 만든다" ] },
  { k: "java2-time-period-duration", o: [
    "Period는 년·월·일 같은 달력 기반, Duration은 초·나노 같은 시간 기반 양이다",
    "Duration 이 년·월·일 같은 날짜를, Period 가 초·나노 같은 시간 단위를 다룬다",
    "둘 다 초 단위로만 동작해 사실상 같고 이름만 달력용과 시각용으로 나눈 것이다",
    "Period 는 자바 8, Duration 은 자바 11 에 추가된 같은 기능의 신구 버전이다" ] },
  { k: "스레드풀 크기", o: [
    "CPU 바운드 작업은 대략 코어 수(±1) 정도가 적절하고, I/O 바운드 작업은 대기 비율에 비례해 더 많은 스레드가 유리하다",
    "I/O 바운드 작업은 코어 수보다 적게 설정하는 것이 언제나 최적이다 — 스레드가 적을수록 컨텍스트 스위칭이 줄기 때문이다",
    "스레드가 많을수록 처리량이 선형으로 증가하므로 메모리가 허용하는 한 크게 잡고 필요하면 줄이는 편이 안전하다",
    "CPU 바운드 작업은 코어 수의 수백 배로 설정해야 한다 — 그래야 어떤 스레드가 멈춰도 다른 스레드가 코어를 채운다" ] },
  { k: "java2-str-intern", o: [
    "NullPointerException — 풀에 없는 문자열을 intern 하면 null 이 온다",
    "컴파일 오류 — intern() 의 반환형은 String 이 아니라 비교할 수 없다",
    "false — intern() 은 풀에 복사본을 새로 만들어 돌려주기 때문",
    "true — intern()은 같은 내용의 풀 참조를 돌려주기 때문" ] },
  { k: "java2-fn-diamond", o: [
    "A 가 알파벳 순으로 앞이라 A 의 구현이 자동으로 선택되며, 이름 순서가 우선순위를 정한다",
    "implements 절에 먼저 적은 인터페이스의 것이 자동 선택되므로 순서를 바꿔 원하는 쪽을 고른다",
    "두 default 가 implements 순서대로 모두 실행되므로 한 번의 호출에 부작용이 두 번 생긴다",
    "C가 반드시 그 메서드를 오버라이드해야 하며, 본문에서 A.super.m()으로 골라 부를 수 있다" ] },
  { k: "java3-hc-mutable", o: [
    "아무 영향이 없다 — HashSet 은 원소를 순차 탐색하므로 해시가 바뀌어도 equals 로 찾아낸다",
    "즉시 ConcurrentModificationException 이 발생한다 — 컬렉션이 원소 변경을 감지하기 때문이다",
    "해시가 바뀌어 원래 버킷에서 찾지 못하고, contains는 false인데 순회에는 보이는 유령 원소가 된다",
    "컬렉션이 변경을 감지해 자동으로 위치를 재배치하므로 contains 도 remove 도 정상 동작한다" ] },
  { k: "java3-ver-remove", o: [
    "소비자가 여전히 그 필드를 읽고 있다면 파괴적 변경이므로, 유예 기간 동안 옛 필드와 새 필드를 함께 내보내는 단계적 전환이 필요하다",
    "제거된 필드는 소비자 쪽에서 자동으로 기본값이 채워져 문제가 없으므로, 생산자는 소비자를 신경 쓰지 않고 언제든 필드를 지워도 된다",
    "필드 제거는 컴파일러가 막아 주므로 사고가 날 수 없다 — 소비자 코드가 그 필드를 참조하면 생산자 쪽 빌드가 실패하기 때문이다",
    "이름 변경은 값이 같으므로 언제나 안전하다 — 소비자는 위치로 필드를 읽기 때문에 이름이 바뀌어도 같은 값을 얻는다" ] },
  { k: "static 특징", o: [
    "프로그램이 도는 동안 한 번만 실행되고 두 번째 호출은 무시된다",
    "객체를 먼저 만들어야만 호출할 수 있고 클래스 이름으로는 못 부른다",
    "값을 반환할 수 없으므로 반환형은 언제나 void 여야 한다",
    "객체 생성 없이 클래스 이름으로 호출할 수 있다" ] },
  { k: "java-st-boxing", o: [
    "원소마다 발생하는 박싱·언박싱과 객체 생성을 피할 수 있기 때문",
    "IntStream 은 지연 평가를 하지 않아 중간 연산 비용이 없기 때문",
    "IntStream 은 정렬이 O(n) 으로 끝나 합산 전 정렬이 빠르기 때문",
    "IntStream 은 자동으로 병렬 처리되어 여러 코어를 쓰기 때문" ] },
  { k: "java2-time-store", o: [
    "사용자 지역의 LocalDateTime 을 그대로 저장한다 — 화면에 보이는 시각과 같아 변환이 필요 없다",
    "사람이 읽기 쉬운 문자열로 저장한다 — 어느 시각인지 로그처럼 바로 읽을 수 있기 때문이다",
    "UTC 기준 Instant(또는 timestamptz)로 저장하고, 화면에 보일 때만 타임존을 적용한다",
    "밀리초 값을 int 컬럼에 저장한다 — 숫자라 정렬과 비교가 빠르고 타임존 문제가 없다" ] },
  { k: "java2-test-methodsource", o: [
    "public 이며 void 를 반환하고 인자를 받지 않는 메서드여야 한다",
    "static이며 Stream·Collection 등 인자 목록을 반환해야 한다",
    "@Test 애너테이션이 붙어 있어야 JUnit 이 인자 공급용으로 인식한다",
    "테스트 메서드와 이름이 같아야 하며 반환형은 무엇이든 상관없다" ] },
  { k: "java2-build-snapshot", o: [
    "같은 좌표라도 내용이 언제든 바뀔 수 있어 빌드가 재현 불가능해지기 때문",
    "SNAPSHOT 은 Maven 에서만 지원되므로 Gradle 로 빌드하는 소비자가 받지 못하기 때문",
    "SNAPSHOT 은 컴파일이 되지 않는 미완성 코드라 릴리스 빌드가 실패하기 때문",
    "SNAPSHOT 은 최신 릴리스보다 낮은 버전으로 취급되어 충돌 해결에서 밀리기 때문" ] },
  { k: "java3-bld-telescoping", o: [
    "매개변수가 많아지면 순서와 의미를 알기 어렵고, 같은 타입 인자를 서로 바꿔 넣어도 컴파일러가 잡지 못한다",
    "생성자 오버로드가 네 개를 넘으면 시그니처가 모호해져 컴파일 에러가 발생하므로 그 이상 늘릴 수 없다",
    "생성자마다 필드를 다시 대입하므로 final 필드를 둘 수 없고, 그래서 불변 객체를 만들 수 없다",
    "자바에서는 생성자를 두 개 이상 정의할 수 없어 매개변수를 늘릴 방법이 아예 없다는 점이 문제다" ] },
  { k: "java3-enum-ctor", o: [
    "protected 로 선언해야 하며, 상속받은 클래스에서 super() 로 호출해 새 상수를 만든다",
    "public 이어야 다른 클래스에서 new 로 상수를 자유롭게 추가할 수 있게 된다",
    "enum 은 생성자를 가질 수 없으므로 필드 값은 static 블록에서 채워야 한다",
    "private 또는 package-private만 가능하며, 외부에서 new로 인스턴스를 만들 수 없다" ] },
  { k: "ForkJoin", o: [
    "유휴 스레드는 다른 스레드의 deque 반대쪽(FIFO 끝)에서 작업을 훔쳐 부하를 분산한다",
    "각 워커 스레드는 자신의 작업을 double-ended queue(deque)의 LIFO 끝에서 처리한다",
    "블로킹 I/O를 자주 수행하는 작업에 ForkJoinPool의 common pool을 그대로 쓰는 것이 이상적이다",
    "RecursiveTask 는 결과를 반환하고 RecursiveAction 은 반환값 없이 부작용만 남긴다" ] },
  { k: "java2-str-loopconcat", o: [
    "반복마다 새 String 객체와 전체 복사가 생겨 전체 비용이 O(n²)에 가까워진다",
    "메모리는 늘지만 시간 복잡도는 선형이라 문자열이 아주 길지 않으면 문제 없다",
    "String 은 공유 풀에 놓이므로 여러 스레드가 누적하면 스레드 안전성이 깨진다",
    "컴파일러가 자동으로 StringBuilder 하나로 합쳐 주므로 성능 문제가 생기지 않는다" ] },
  { k: "java2-fn-objectmethod", o: [
    "추상 메서드가 두 개라 함수형 인터페이스가 아니게 된다 — Object 의 메서드도 개수에 포함된다",
    "equals는 Object의 public 메서드를 재선언한 것이라 세지 않으므로 여전히 함수형 인터페이스다",
    "equals 만 함수형 메서드로 남고 다른 추상 메서드는 무시되므로 람다는 equals 를 구현하게 된다",
    "인터페이스에서 Object 의 메서드를 다시 선언하는 것은 허용되지 않아 컴파일 오류가 난다" ] },
  { k: "java3-fw-junit", o: [
    "테스트 메서드 이름이 test 로 시작해야만 찾아내며, 애노테이션은 보조 표시일 뿐이다",
    "소스 파일을 정규식으로 파싱해 @Test 문자열이 붙은 메서드 이름을 찾아 실행한다",
    "@Test가 붙은 메서드를 리플렉션으로 찾아 인스턴스를 만들고 호출한다",
    "테스트 클래스는 컴파일 시점에 main 메서드를 가진 실행 클래스로 변환되어 실행된다" ] }
] };
