module.exports = {
  track: "compiler", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 단계 이름과 표현 형식",
  source: "./in_compiler.cjs",
  lessons: [
    { t: "앞단의 단계와 이름", n: 8,
      th: { sum: "컴파일러 앞단은 **글자 → 토큰 → 트리 → 이름**의 네 걸음이다. 각 걸음의 이름을 알면 오류 메시지가 어디서 왔는지 바로 짚인다.",
        body: [
          { h: "나누고 세우기", t: "<b>어휘 분석</b>은 글자를 토큰으로 나누며 <b>최장 일치</b>가 원칙이고, 공백·주석을 버리는 대신 <b>줄·칸 정보</b>를 붙인다 — 오류 밑줄이 엉뚱한 자리에 그이면 여기가 범인이다. <b>구문 분석</b>은 토큰을 문법에 따라 트리로 세우며 우선순위와 결합 방향을 정한다. 결과물인 <b>AST</b> 는 군더더기를 뺀 트리라, 이후의 모든 단계와 린터·포매터가 이 위에서 돈다." },
          { h: "이름과 문법을 적어 두기", t: "<b>심볼 테이블</b>은 이름이 가리키는 것을 모아 둔 표이고, 렉시컬 스코프를 구현하려면 <b>스택으로 쌓아야</b> 한다 — 표 하나로 두면 안쪽 선언이 바깥 이름을 영구히 덮는다. 문법은 <b>BNF</b> 로 적는데, 규칙 맨 앞에 자기 자신이 나오는 <b>좌재귀</b>는 재귀 하강 파서를 무한 재귀에 빠뜨리므로 반복 형태로 고쳐 쓴다 — 그러면 좌결합도 함께 얻는다." }],
        code: { c: "expr := term (('+' | '-') term)*   // 좌재귀를 반복으로\nscope.push(new Map())              // 블록마다 얹고 벗긴다\n토큰마다 {line, col} 을 함께 들고 다닌다", cap: "각 걸음의 이름이 곧 지도다" },
        key: ["어휘 분석은 최장 일치", "AST 위에서 모든 단계가 돈다", "심볼 테이블은 스택으로", "좌재귀는 반복으로 고친다"] } },
    { t: "뒷단의 표현과 최적화", n: 7,
      th: { sum: "뒷단은 **가운데 표현(IR)** 을 두고 그 위에서 고친다. 모든 최적화의 전제는 하나 — **관찰 가능한 동작을 바꾸지 않는다**.",
        body: [
          { h: "가운데를 두는 이유", t: "<b>IR</b> 은 앞단(언어)과 뒷단(기계)을 끊어 주는 층이라, 언어 m 개와 기계 n 개가 있어도 <code>m × n</code> 이 아니라 <code>m + n</code> 개만 만들면 된다. <b>SSA</b> 는 변수마다 대입을 한 번만 하도록 이름을 나눠 붙인 형태로, 정의가 하나뿐이라 상수 전파·공통 부분식 제거가 훨씬 간단해진다. <b>JIT</b> 는 실제로 어떤 타입이 오는지 보고 최적화하는 대신 <b>워밍업</b>이 필요하다 — 벤치마크의 첫 몇 초는 버려야 한다." },
          { h: "지워도 되는 것과 안 되는 것", t: "<b>DCE</b> 는 결과를 아무도 쓰지 않는 명령을 지우지만, <b>부수효과가 있는 명령</b>은 결과를 안 써도 남겨야 한다 — 로그와 저장이 함께 지워지는 사고가 여기서 난다. <b>TCO</b> 는 마지막 동작이 호출뿐일 때 현재 프레임을 재사용해 재귀를 반복문으로 바꾼다. <b>스택 프레임</b>은 호출마다 쌓이고 반환하면 사라지므로, 지역 변수의 주소를 반환하면 허상 포인터가 된다. 링커의 <b>undefined reference</b> 는 선언은 있고 정의가 없을 때다." }],
        code: { c: "it.eff || used.has(it.dst)      // 부수효과도 살아 있음\nm + n 개의 번역기               // IR 이 있을 때\nφ(x₁, x₂)                       // 갈림길에서 합칠 때", cap: "밖에서 보이는 것만 지키면 된다" },
        key: ["IR 이 번역기 수를 곱셈에서 덧셈으로", "SSA 는 정의가 하나뿐", "부수효과 있는 명령은 못 지운다", "링크 오류는 정의가 없을 때"] } },
  ],
};
