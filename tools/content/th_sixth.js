/* 6차 이론 — python 에서 점수가 가장 높던(가장 어렵던) 세 유닛.

   '동시성 선택 실무' · '이터레이터와 제너레이터 심화' · '테스트 실무' 는
   세 레슨이 똑같은 글을 나눠 쓰고 있었다. 게다가 한 문장이 90자를 넘고,
   '직렬화'·'블로킹'·'이터러블' 같은 말을 풀지 않고 썼다.
   레슨마다 자기 주제를 갖도록 따로 쓰고, 용어는 그 자리에서 풀었다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

python: [
{
  unit: "동시성 선택 실무: threading·multiprocessing·asyncio", lesson: "GIL과 작업 유형",
  th: {
    sum: "파이썬은 한 프로세스 안에서 **한 번에 한 줄만** 실행한다. 그 자물쇠를 GIL 이라 부른다.",
    body: [
      { h: "자물쇠가 하나뿐이다", t: "스레드를 여러 개 만들어도, 파이썬 코드를 실제로 돌리는 것은 한 번에 하나다. 자물쇠를 쥔 스레드만 일하고 나머지는 기다린다. 그래서 계산이 많은 일은 스레드를 늘려도 빨라지지 않는다." },
      { h: "기다리는 일은 다르다", t: "네트워크 응답이나 파일을 기다리는 동안에는 자물쇠를 놓아 준다. 그래서 기다리는 일이 많은 작업은 스레드를 늘리면 실제로 빨라진다. 열 곳에 요청을 보내고 함께 기다리는 식이다." },
      { h: "먼저 어느 쪽인지 가린다", t: "내 작업이 '계산이 많은 쪽' 인지 '기다림이 많은 쪽' 인지부터 본다. 이걸 안 가리고 스레드부터 늘리면, 계산 작업에서는 아무 효과 없이 코드만 복잡해진다." },
      { h: "계산이 많으면 프로세스로", t: "계산이 많은 일은 프로세스를 여러 개 띄운다. 프로세스마다 자물쇠가 따로 있어 진짜로 동시에 돈다. 대신 프로세스끼리는 메모리를 나눠 쓰지 않아 값을 주고받는 비용이 든다." },
    ],
    code: { c: "# 기다림이 많다 → 스레드\nwith ThreadPoolExecutor() as ex: ex.map(fetch, urls)\n\n# 계산이 많다 → 프로세스\nwith ProcessPoolExecutor() as ex: ex.map(heavy, chunks)", cap: "어느 쪽 일인지부터 가린다" },
    key: ["자물쇠가 하나뿐이다", "기다리는 일은 스레드로", "계산이 많으면 프로세스로"],
  },
},
{
  unit: "동시성 선택 실무: threading·multiprocessing·asyncio", lesson: "threading과 multiprocessing",
  th: {
    sum: "스레드는 **메모리를 함께 쓰고**, 프로세스는 **따로 쓴다.** 이 차이가 나머지를 다 정한다.",
    body: [
      { h: "스레드는 같은 것을 본다", t: "같은 변수를 여러 스레드가 함께 본다. 그래서 값을 주고받는 비용이 거의 없다. 대신 두 스레드가 같은 값을 동시에 고치면 하나가 덮어써져 조용히 틀린 값이 남는다 — `Lock` 으로 한 번에 하나만 고치게 막는다." },
      { h: "프로세스는 따로 본다", t: "프로세스마다 메모리가 따로라 서로 건드릴 수 없다. 그래서 덮어쓰는 사고가 없다. 대신 값을 넘기려면 한 번 포장했다 푸는 과정을 거친다 — 이걸 직렬화라고 하고, 큰 자료일수록 이 비용이 커진다." },
      { h: "넘길 수 없는 값도 있다", t: "열린 파일이나 이름 없는 함수(람다)는 포장할 수 없어 프로세스로 넘기지 못한다. 넘기려 하면 그 자리에서 오류가 난다. 프로세스에는 숫자·문자열·간단한 자료만 넘기는 편이 안전하다." },
      { h: "고르는 기준은 하나다", t: "기다림이 많으면 스레드, 계산이 많으면 프로세스다. 둘 다 아니라면 그냥 한 줄로 도는 코드가 제일 빠르고 제일 안 틀린다. 동시성은 필요할 때만 꺼내는 도구다." },
    ],
    code: { c: "lock = threading.Lock()\nwith lock:\n    total += x        # 한 번에 하나만 고친다", cap: "함께 쓰면 막아야 하고, 따로 쓰면 넘겨야 한다" },
    key: ["스레드는 메모리를 함께 쓴다", "프로세스는 포장해서 넘긴다", "람다는 넘기지 못한다"],
  },
},
{
  unit: "동시성 선택 실무: threading·multiprocessing·asyncio", lesson: "asyncio 실무",
  th: {
    sum: "`asyncio` 는 스레드를 늘리지 않고, **기다리는 동안 다른 일을 하게** 만든다.",
    body: [
      { h: "기다림에 자리를 내준다", t: "`await` 를 만나면 그 함수는 잠깐 멈추고 다른 일에 순서를 넘긴다. 응답이 오면 멈춘 자리부터 이어서 돈다. 그래서 스레드 하나로도 요청 수백 개를 동시에 기다릴 수 있다." },
      { h: "한 곳이 막으면 전부 멈춘다", t: "일꾼이 하나라, `await` 없이 오래 도는 코드가 끼면 그동안 아무 일도 못 한다. `time.sleep` 이나 무거운 계산이 그렇다. 잠깐 쉬려면 `asyncio.sleep`, 무거운 계산은 다른 곳으로 넘긴다." },
      { h: "동시에 하려면 모아서 기다린다", t: "`await` 를 하나씩 줄줄이 쓰면 결국 순서대로 기다린 것과 같다. `asyncio.gather` 로 한꺼번에 넘겨야 실제로 겹쳐서 진행된다. 이걸 빠뜨리면 비동기로 짜고도 속도가 그대로다." },
      { h: "취소를 챙긴다", t: "기다리던 일이 취소되면 그 자리에서 예외가 올라온다. 뒷정리 코드를 `finally` 에 두지 않으면 연결이 열린 채 남는다. 오래 도는 서버에서는 이런 것이 쌓여 문제가 된다." },
    ],
    code: { c: "results = await asyncio.gather(*[fetch(u) for u in urls])\n\nawait asyncio.sleep(1)      # time.sleep 은 전체를 멈춘다", cap: "모아서 기다려야 겹쳐 돈다" },
    key: ["`await` 는 자리를 내준다", "막는 코드 하나가 전부를 멈춘다", "`gather` 로 모아서 기다린다"],
  },
},
{
  unit: "이터레이터와 제너레이터 심화", lesson: "iterator protocol",
  th: {
    sum: "`for` 가 도는 원리는 간단하다. **다음 값을 달라고 계속 물어보는 것**이다.",
    body: [
      { h: "두 가지 약속", t: "`iter(x)` 는 '값을 꺼낼 수 있는 것' 을 돌려주고, 거기에 `next()` 를 부르면 다음 값이 나온다. `for` 문은 이 둘을 반복할 뿐이다. 리스트든 파일이든 이 약속만 지키면 `for` 로 돌 수 있다." },
      { h: "값이 떨어지면 신호를 보낸다", t: "더 줄 값이 없으면 `StopIteration` 이라는 신호를 던진다. `for` 는 이 신호를 받고 조용히 멈춘다. 직접 `next()` 를 쓸 때는 이 신호가 그대로 올라오므로, 기본값을 함께 주면 터지지 않는다." },
      { h: "내 클래스도 for 로 돌게 만들기", t: "`__iter__` 와 `__next__` 를 만들어 주면 내가 만든 객체도 `for` 로 돌 수 있다. 없으면 값이 있어도 `TypeError` 가 난다 — 파이썬은 그 약속을 안 지킨 객체에서 값을 꺼낼 방법이 없다." },
      { h: "리스트와는 다르다", t: "리스트는 값을 다 갖고 있어서 몇 번이든 다시 돌 수 있다. 꺼내 쓰는 쪽은 한 번 쓰면 끝나는 경우가 많다. 이 차이를 모르면 '두 번째 반복문에서 왜 아무것도 안 나오지' 로 헤맨다." },
    ],
    code: { c: "it = iter([1, 2])\nnext(it)          # 1\nnext(it)          # 2\nnext(it, None)    # 기본값을 주면 안 터진다", cap: "물어보고, 없으면 신호" },
    key: ["`iter` 와 `next` 두 약속", "값이 없으면 `StopIteration`", "한 번 쓰면 끝나는 것이 많다"],
  },
},
{
  unit: "이터레이터와 제너레이터 심화", lesson: "yield from과 지연 평가",
  th: {
    sum: "제너레이터는 값을 **미리 다 만들지 않는다.** 달라고 할 때 하나씩 만들어 준다.",
    body: [
      { h: "필요한 만큼만 만든다", t: "백만 줄짜리 파일을 리스트로 읽으면 메모리에 다 올라간다. 제너레이터로 한 줄씩 내주면 그때그때 한 줄만 들고 있으면 된다. 자료가 커질수록 이 차이가 그대로 메모리 사용량이 된다." },
      { h: "yield 는 멈추는 자리", t: "`yield` 는 값을 하나 내주고 거기서 멈춘다. 다음에 달라고 하면 멈춘 자리부터 이어서 돈다. 그래서 끝이 없는 수열도 만들 수 있다 — 필요한 만큼만 꺼내 쓰면 되기 때문이다." },
      { h: "yield from 은 통째로 넘긴다", t: "안쪽 제너레이터의 값을 그대로 흘려보낼 때 `yield from` 을 쓴다. `for` 로 하나씩 다시 내주는 것과 결과는 같지만 짧고, 안쪽으로 값을 보내는 경우까지 제대로 이어 준다." },
      { h: "게을러서 생기는 함정", t: "제너레이터는 만들어 둔 시점이 아니라 **꺼내 쓰는 시점**에 돈다. 그사이 바깥 변수가 바뀌면 바뀐 값으로 계산된다. '분명 그때 값을 넣었는데' 하는 혼란이 여기서 온다." },
    ],
    code: { c: "def lines(path):\n    with open(path) as f:\n        yield from f      # 한 줄씩 흘려보낸다", cap: "달라고 할 때 만든다" },
    key: ["필요한 만큼만 만든다", "`yield` 는 멈추는 자리", "꺼낼 때 돈다는 것을 잊지 않는다"],
  },
},
{
  unit: "이터레이터와 제너레이터 심화", lesson: "소진과 next 기본값",
  th: {
    sum: "제너레이터는 **한 번 훑고 나면 빈다.** 리스트처럼 다시 돌 수 없다.",
    body: [
      { h: "두 번째 반복문은 비어 있다", t: "`for` 로 한 번 다 돌면 그 제너레이터는 끝난 상태다. 다시 `for` 를 돌리면 아무것도 나오지 않는다. 오류가 아니라 그냥 빈 결과라, '왜 두 번째 계산이 0이지' 로 나타난다." },
      { h: "다시 쓰려면 리스트로 담는다", t: "여러 번 훑어야 한다면 `list(gen)` 으로 한 번 담아 둔다. 대신 메모리를 그만큼 쓴다. 한 번만 쓸 것인지 여러 번 쓸 것인지 먼저 정하고 고른다." },
      { h: "next 에는 기본값을 준다", t: "값이 없을 때 `next(it)` 는 예외를 던진다. `next(it, None)` 처럼 기본값을 주면 예외 대신 그 값이 나온다. '첫 번째 것 하나만' 꺼낼 때 이 형태가 안전하다." },
      { h: "길이를 물을 수 없다", t: "제너레이터에는 `len()` 을 쓸 수 없다. 아직 만들지 않은 값이 몇 개인지 알 수 없기 때문이다. 개수가 필요하면 다 꺼내 세야 하고, 그 순간 제너레이터는 비게 된다." },
    ],
    code: { c: "g = (x for x in [1, 2])\nsum(g)    # 3\nsum(g)    # 0 — 이미 비었다\n\nfirst = next(iter(xs), None)", cap: "한 번 쓰면 끝난다" },
    key: ["한 번 훑으면 빈다", "여러 번 쓰려면 리스트로", "`next` 에는 기본값을"],
  },
},
{
  unit: "테스트 실무: pytest 픽스처와 모킹", lesson: "픽스처와 테스트 구조",
  th: {
    sum: "테스트마다 필요한 **준비물을 만들어 주는 함수**가 픽스처다. 준비와 확인을 나눠 준다.",
    body: [
      { h: "준비를 한 곳에 모은다", t: "테스트마다 같은 자료를 만드는 코드를 복사해 두면, 자료 모양이 바뀔 때 전부 고쳐야 한다. 픽스처에 모아 두면 한 곳만 고치면 된다. 테스트 본문에는 '무엇을 확인하는가' 만 남는다." },
      { h: "테스트끼리 영향을 주지 않게", t: "픽스처는 테스트마다 새로 만들어 준다. 그래서 앞 테스트가 값을 바꿔 놓아도 다음 테스트는 깨끗한 상태에서 시작한다. 이걸 안 지키면 따로 돌리면 통과하는데 함께 돌리면 실패하는, 가장 짜증나는 실패가 생긴다." },
      { h: "뒷정리까지 맡긴다", t: "픽스처에서 `yield` 로 준비물을 넘기면, 테스트가 끝난 뒤 그 아래 줄이 실행된다. 임시 파일을 지우거나 연결을 닫는 자리다. 안 지우면 다음 실행에 옛 자료가 남아 결과가 흔들린다." },
      { h: "한 테스트에 한 가지만", t: "하나의 테스트에서 여러 가지를 확인하면, 실패했을 때 무엇이 잘못됐는지 바로 알기 어렵다. 이름도 '무엇이 어떻게 되어야 한다' 로 적어 두면 실패 목록만 읽고도 상황이 보인다." },
    ],
    code: { c: "@pytest.fixture\ndef db():\n    conn = connect()\n    yield conn        # 여기까지가 준비\n    conn.close()      # 끝난 뒤 뒷정리", cap: "준비와 뒷정리를 한 곳에" },
    key: ["준비는 픽스처에 모은다", "테스트마다 새로 만든다", "`yield` 아래가 뒷정리"],
  },
},
{
  unit: "테스트 실무: pytest 픽스처와 모킹", lesson: "모킹: patch 대상과 검증",
  th: {
    sum: "바깥에 나가는 일(네트워크·시각·파일)을 **가짜로 바꿔 두는 것**이 모킹이다.",
    body: [
      { h: "왜 바꿔 두나", t: "진짜 서버를 부르는 테스트는 인터넷이 끊기면 실패하고, 매번 결과가 달라진다. 가짜로 바꿔 두면 언제 돌려도 같은 답이 나온다. 테스트는 내 코드를 확인하는 것이지 남의 서버를 확인하는 것이 아니다." },
      { h: "쓰는 자리를 바꾼다", t: "가장 많이 틀리는 곳이다. `requests` 를 만든 곳이 아니라 **내 코드가 그것을 가져다 쓰는 이름**을 바꿔야 한다. 엉뚱한 곳을 바꾸면 가짜가 끼어들지 못해 진짜 요청이 그대로 나간다." },
      { h: "불렸는지도 확인한다", t: "돌려주는 값만 맞추면, 실제로는 안 불렸는데 통과하는 테스트가 된다. 몇 번 어떤 인자로 불렸는지까지 확인하면 그런 빈틈이 없어진다." },
      { h: "너무 많이 바꾸지 않는다", t: "안쪽 함수까지 전부 가짜로 바꾸면, 코드를 조금만 손봐도 테스트가 우수수 깨진다. 정말 바깥으로 나가는 경계에서만 바꾸는 것이 오래가는 테스트다." },
    ],
    code: { c: "with patch(\"myapp.service.requests.get\") as g:\n    g.return_value.json.return_value = {\"ok\": True}\n    ...\n    g.assert_called_once()", cap: "쓰는 자리를 바꾼다" },
    key: ["바깥 의존을 가짜로", "만든 곳 말고 쓰는 곳", "불렸는지도 확인한다"],
  },
},
{
  unit: "테스트 실무: pytest 픽스처와 모킹", lesson: "파라미터화와 실패 진단",
  th: {
    sum: "같은 검사를 **입력만 바꿔 여러 번** 돌리는 것이 파라미터화다. 경계값을 늘어놓기에 좋다.",
    body: [
      { h: "복사해 붙이지 않는다", t: "입력만 다른 테스트를 여러 개 복사해 두면, 검사 내용이 바뀔 때 전부 고쳐야 한다. 입력 목록만 늘어놓으면 한 줄 추가로 경우를 늘릴 수 있다." },
      { h: "어느 입력에서 깨졌는지 보인다", t: "파라미터화한 테스트는 실패했을 때 그 입력이 이름에 함께 찍힌다. 반복문 안에서 확인하면 첫 실패에서 멈춰, 나머지 입력이 괜찮은지 알 수 없다." },
      { h: "경계값을 먼저 넣는다", t: "0, 빈 값, 음수, 딱 경계인 값부터 넣는다. 버그는 대개 한가운데가 아니라 이런 자리에 있다. 평범한 값만 넣은 테스트는 통과해도 별로 알려 주는 것이 없다." },
      { h: "실패 메시지가 절반이다", t: "무엇을 기대했고 무엇이 나왔는지가 메시지에 있어야 바로 고칠 수 있다. '실패' 라고만 적혀 있으면 어디부터 봐야 할지 몰라 한참 헤맨다." },
    ],
    code: { c: "@pytest.mark.parametrize(\"n,want\", [(0, 0), (1, 1), (-1, 0)])\ndef test_clamp(n, want):\n    assert clamp(n) == want", cap: "입력만 늘어놓는다" },
    key: ["입력 목록으로 늘린다", "어느 입력인지 이름에 찍힌다", "경계값을 먼저"],
  },
},
],

};
