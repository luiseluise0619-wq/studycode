module.exports = {
  track: "fp", guide: "", xp: 80,
  unit: "실행형 실전 — 순수해 보이지만 새는 코드 고치기",
  source: "./dbg_fp.cjs",
  lessons: [
    { t: "조합자의 계약이 어긋난 곳", n: 6,
      th: { sum: "조합자는 **작고 대칭인 예제에서 전부 통과한다** — 방향·인자 개수·멈추는 시점이 어긋나도 드러나지 않는다.",
        body: [
          { h: "대칭인 예제가 버그를 숨긴다", t: "`pipe` 와 `compose` 는 방향만 다르므로 `+1` 두 개로 테스트하면 같은 답이 나온다. `takeWhile` 과 `filter` 는 조건 만족 원소가 앞에만 몰려 있으면 같다. 테스트는 반드시 **교환 불가능한 조합**과 **중간에 조건이 끊기는 자료**로 짜야 한다." },
          { h: "약속을 절반만 지킨 함수", t: "`once` 는 '한 번만 실행' 과 '**같은 값을 준다**' 두 가지를 약속하는데 뒤를 빠뜨리기 쉽다. `memo` 는 키가 **인자 전체**를 대표해야 하고, `curry` 는 값이 아니라 `args.length` 로 인자 개수를 세야 한다. 셋 다 반환값 하나만 확인하는 테스트를 통과한다." }],
        code: { c: "return xs.reduce((v, f) => f(v), x);   // pipe 는 왼쪽부터\nif (!done) { done = true; value = fn(...args); }\nreturn value;                          // 결과도 보관한다", cap: "계약의 절반만 지키기 쉽다" },
        key: ["방향은 비대칭 예제로 검증", "캐시 키는 인자 전체", "once 는 값도 보관", "인자 개수는 args.length 로"] } },
    { t: "겉보기에 순수한데 새는 곳", n: 6,
      th: { sum: "'불변' 과 '원본 보존' 은 **반환값에 드러나지 않는 계약**이라, 따로 테스트하지 않으면 조용히 깨진다.",
        body: [
          { h: "한 겹만 얼고 한 겹만 복사된다", t: "`Object.freeze` 는 한 겹만 얼고, `shift`·`sort`·`splice` 는 원본을 바꾼다. 비엄격 모드에서 언 객체에 대입하면 **오류 없이 무시**되므로, 테스트는 '오류가 나는가' 가 아니라 '**값이 그대로인가**' 로 확인해야 한다. 바꾸는 메서드와 새로 만드는 메서드를 갈라 두고, 앞의 것은 복사한 뒤에만 쓴다." },
          { h: "빈칸과 바깥 입력", t: "보통 객체는 `Object.prototype` 을 물려받아 `'constructor'` 가 이미 있는 것처럼 보인다 — 바깥 문자열이 키가 되는 자리는 `Object.create(null)` 이나 `Map` 을 쓴다. 전진 폭이 변수인 반복문은 **0 가드**가 없으면 멈추지 않고, 깊이 인자를 안 줄이는 재귀는 바닥까지 판다." }],
        code: { c: "const out = Object.create(null);\nif (!(n >= 1)) return [];      // NaN 도 함께 걸린다", cap: "가드는 반복문 앞에" },
        key: ["동결·복사는 한 겹뿐", "'원본이 그대로인가' 를 테스트한다", "바깥 문자열 키는 Map 으로", "전진 폭이 변수면 0 을 막는다"] } },
  ],
};
