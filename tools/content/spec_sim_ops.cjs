const UNIT = "시뮬레이션 — 되감아 보며 이해한다";
module.exports = {
  source: "./sim_ops.cjs", xp: 70,
  entries: [

    { track: "git", guide: "", unit: UNIT, lesson: "병합과 되살리기를 단계로 본다", slice: [0, 2],
      th: { sum: "Git 의 두 장면 — **왜 세 갈래를 견주는가**, 그리고 **지운 것이 왜 되살아나는가**.",
        body: [
          { h: "기준이 있어야 누가 바꿨는지 안다", t: "두 갈래만 견주면 `b` 와 `B` 중 어느 쪽이 바뀐 것인지 알 수 없다. **기준(공통 조상)** 을 함께 보면 정해진다 — 한쪽만 기준과 다르면 그쪽이 손댄 것이고, 둘 다 다르면 그때가 충돌이다. 그래서 병합은 언제나 **세 갈래**이고, 공통 조상을 못 찾으면(관계없는 이력) 병합 자체가 거부된다. 충돌이 많게 느껴지는 것은 판정이 까다로워서가 아니라 **갈라져 있던 시간이 길어** 서로 건드린 줄이 많아졌기 때문이다." },
          { h: "reset 은 지우지 않는다", t: "`reset` 은 **HEAD 를 옮길 뿐**이다. 옛 커밋은 그대로 있고 아무도 가리키지 않게 될 뿐이라, 가리키기만 하면 되살아난다. 그 '가리키는 법' 을 적어 둔 것이 **reflog** 이고, HEAD 가 지나온 자리를 시간순으로 남기므로 `HEAD@{1}` 은 '한 번 전의 자리' 다. 다만 기한이 있다 — 아무도 안 가리키는 커밋은 정리 작업에서 결국 지워지고, reflog 는 **내 저장소에만** 있어 남의 이력을 덮어쓴 것은 이 방법으로 못 되돌린다." }],
        code: { c: "git reflog                 # HEAD 가 지나온 자리\ngit reset --hard HEAD@{1}  # 한 번 전으로", cap: "잘못 reset 했다면 거의 언제나 돌아올 수 있다" },
        key: ["병합은 세 갈래를 견준다", "둘 다 다르게 고친 줄만 충돌", "reset 은 HEAD 를 옮길 뿐", "reflog 는 내 저장소에만 있다"] } },

    { track: "linux", guide: "", unit: UNIT, lesson: "흐름과 권한을 단계로 본다", slice: [2, 4],
      th: { sum: "셸의 두 기초 — **줄이 어떻게 흘러가는가**, 그리고 **접근이 어디서 갈리는가**.",
        body: [
          { h: "순서가 결과를 정한다", t: "파이프라인은 줄의 흐름이라, 각 단계는 앞에서 온 줄을 고치거나 걸러 다음으로 넘길 뿐이다. 그래서 **순서를 바꾸면 결과가 달라진다** — `uniq` 는 붙어 있는 같은 줄만 접으므로 앞에 `sort` 가 없으면 아무것도 안 접힌다. 단계마다 '들어온 줄 수'와 '나간 줄 수'를 함께 보는 것이 실무의 습관이다: 갑자기 0이 되면 그 조건이 잘못된 것이고, 안 줄면 그 단계가 아무 일도 안 한 것이다. 실제 셸에서는 단계들이 **동시에** 돌아 큰 파일도 통째로 메모리에 올리지 않는다." },
          { h: "맞는 부류에서 끝난다", t: "권한 판정은 소유자 → 그룹 → 나머지 순으로 보다가 **맞는 부류를 찾으면 거기서 멈춘다.** 뒤는 보지 않는다. 그래서 `604` 인 파일은 소유자와 남들은 읽을 수 있지만 **그룹에 속한 사람은 아무것도 못 한다** — 그룹 자리가 0이고 거기서 판정이 끝나기 때문이다. 디렉터리에서는 뜻이 달라진다: `x` 는 실행이 아니라 **들어가기**, `r` 은 **목록 보기**라, `x` 만 있으면 이름을 아는 파일에는 닿아도 목록은 못 본다." }],
        code: { c: "sort | uniq -c | sort -rn   # sort 없이 uniq 는 아무것도 못 접는다\nchmod 604 f                 # 그룹만 아무것도 못 한다", cap: "판정은 맞는 부류에서 끝난다" },
        key: ["파이프는 순서가 결과를 정한다", "uniq 는 붙어 있는 것만 접는다", "권한은 맞는 부류에서 멈춘다", "디렉터리의 x 는 들어가기"] } },

    { track: "test", guide: "", unit: UNIT, lesson: "오염과 좁히기를 단계로 본다", slice: [4, 6],
      th: { sum: "테스트가 어려워지는 두 지점 — **서로를 밟는 것**, 그리고 **범인을 찾는 것**.",
        body: [
          { h: "첫 테스트만 깨끗하다", t: "'혼자 돌리면 되는데 같이 돌리면 실패한다' 의 정체는 앞 테스트가 남긴 것이 뒤 테스트의 전제를 깨뜨리는 것이다. 되감아 보면 **첫 테스트만** 깨끗한 상태에서 도는 것이 보이고, 테스트 수가 늘수록 나빠진다. 무서운 점은 **순서가 바뀌면 결과도 바뀐다**는 것이라, 어제 통과한 것이 오늘 실패한다. 그래서 순서를 뒤집어 돌려 보는 것이 오염을 찾는 가장 싼 방법이다. 정리를 **기억해서 하는 일**로 두면 언젠가 빠지므로, 준비와 정리를 한 자리에 묶는다." },
          { h: "반씩 자르면 열 번이면 된다", t: "커밋 100개에서 범인을 찾는 데 **일곱 번**이면 된다. 후보가 100 → 50 → 25 … 로 반씩 줄기 때문이고, 천 개여도 열 번 남짓이다. 전제가 하나 있다 — **한 번 깨지면 계속 깨져 있어야** 한다. 중간에 고쳐졌다 다시 깨지면 엉뚱한 답이 나오므로, 확인이 흔들리는 테스트라면 그것부터 고쳐야 한다. 같은 방법이 어느 설정·어느 테스트·어느 데이터 줄인지를 찾는 데 그대로 쓰인다: **확인만 자동으로 만들 수 있으면** 나머지는 기계가 한다." }],
        code: { c: "git bisect start && git bisect bad && git bisect good v1.0\n# 확인만 자동이면 나머지는 기계가 한다", cap: "확률이 아니라 절반씩 줄이는 것이 핵심" },
        key: ["첫 테스트만 깨끗하다", "순서를 뒤집어 돌리면 오염이 드러난다", "반씩 자르면 100개도 일곱 번", "깨진 뒤 계속 깨져 있어야 한다"] } },

    { track: "cloud", guide: "", unit: UNIT, lesson: "따라가기와 흘려보내기를 단계로 본다", slice: [6, 8],
      th: { sum: "클라우드의 두 조절 장치 — **대수를 따라 늘리는 것**, 그리고 **요청을 흘려보내는 것**. 둘 다 계산은 쉽고 **시차**가 어렵다.",
        body: [
          { h: "계산은 쉽고 반응이 늦다", t: "오토스케일의 계산은 나눗셈 하나다. 어려움은 **새 인스턴스가 준비되는 시간**에 있다 — 부하가 뛴 그 분에는 아직 못 따라가고, 준비가 3분이면 3분 동안 요청이 밀린다. 그래서 예상되는 부하는 지표를 보고 늘리는 대신 **일정으로 미리** 늘려 둔다. 늘리는 것과 줄이는 것도 다르게 다뤄야 한다: 늘릴 때는 빠르게, 줄일 때는 **천천히** — 잠깐 내려간 부하에 줄였다가 다시 늘리면 준비 시간이 또 든다." },
          { h: "몰림은 허용하고 평균은 지킨다", t: "토큰 버킷이 널리 쓰이는 이유는 **쌓아 둔 만큼은 한꺼번에** 통과시키면서 길게 보면 채워지는 속도로만 통과시키기 때문이다. `cap` 과 `rate` 가 각각 다른 것을 정한다 — **cap 은 한꺼번에 얼마나**, **rate 는 길게 보아 얼마나**. 상한이 있어 아무리 오래 쉬어도 cap 을 넘지 않는데, 이것이 없으면 하루 쉰 뒤 하루치를 한꺼번에 쏟아붓게 된다." }],
        code: { c: "want = ceil(load / perNode)      # 계산은 이 한 줄\ntokens = min(tokens + 지난시간*rate, cap)", cap: "어려운 것은 계산이 아니라 시차다" },
        key: ["준비 시간이 반응의 한계", "늘릴 때 빠르게 줄일 때 천천히", "cap 은 몰림, rate 는 평균", "상한 없는 버킷은 한꺼번에 쏟아진다"] } },
  ],
};
