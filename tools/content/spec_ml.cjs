module.exports={
  track:"ml", guide:"🤖", xp:80,
  unit:"실행형 실전 — 학습 준비를 직접 만든다",
  source:"./exec_mlmet.cjs", slice:[0,6],
  lessons:[
    { t:"누수 없는 분할과 전처리", n:3,
      th:{ sum:"모델을 고르기 전에 **평가가 정직한지**를 먼저 보장해야 한다 — 누수된 점수는 나쁜 모델을 좋다고 말한다.",
        body:[
          {h:"행이 아니라 그룹으로 나눈다", t:"같은 환자·같은 사용자·같은 문서의 조각이 학습과 평가에 동시에 들어가면 모델은 '그 개체를 외우는 것' 으로 정답을 맞힌다. 그래서 분할의 단위는 **행이 아니라 그룹**이어야 하고(`GroupKFold`), 시계열이라면 **시간**이어야 한다. 검증법은 하나뿐이다 — 분할 후 양쪽 그룹 집합의 교집합이 비어 있는지 단정하는 테스트."},
          {h:"전처리도 모델의 일부다", t:"평균·표준편차·최소최대·어휘 사전·타깃 인코딩은 모두 데이터에서 **배우는 값**이다. 전체 데이터로 계산하면 그 순간 평가셋 정보가 학습에 흘러든다. `fit` 과 `transform` 을 분리하고 `Pipeline` 으로 묶어, 교차검증의 각 폴드에서 `fit` 이 **학습 폴드에만** 적용되게 만든다. 그리고 상수 열(표준편차 0, min==max)은 예외가 아니라 평범한 현실이므로 가드가 필요하다."}],
        code:{ c:"# 이 한 줄이 있으면 누수가 아니다\nassert not (set(train_groups) & set(test_groups))", cap:"누수는 결과 형태가 정상이라 눈에 보이지 않는다" },
        key:["분할 단위는 행이 아니라 그룹·시간","통계는 학습셋에서만 배운다","fit / transform 을 분리하고 Pipeline 으로 묶는다","상수 열 가드는 필수"] } },
    { t:"폴드·기울기·가중치", n:3,
      th:{ sum:"학습 루프를 지탱하는 셋 — **데이터를 빠짐없이 나누고, 기울기의 방향을 맞추고, 불균형을 손실에 반영한다**.",
        body:[
          {h:"빠짐없이 나눈다", t:"k-폴드의 요구사항은 '균등하게' 가 아니라 '**모든 인덱스가 정확히 한 번씩**' 이다. `divmod(n, k)` 로 나머지를 앞쪽 폴드에 하나씩 나눠 주면 샘플을 잃지 않는다 — 나머지를 버리는 구현은 형태가 그럴듯해서 테스트 없이는 통과한다. 실무 기본값은 여기에 셔플과 계층화를 더한 것이다."},
          {h:"방향과 무게", t:"기울기는 '손실이 커지는 방향' 을 가리키므로 갱신은 `값 − lr·기울기` 다. 오차를 `(y − ŷ)` 로 쓰면 부호가 뒤집혀 **경사 상승**이 되고, 학습률을 낮춰도 느리게 발산할 뿐이다 — 구현한 기울기는 수치 미분과 비교해(gradient checking) 검증한다. 불균형 가중치 `n/(k·count)` 는 '모든 클래스가 전체의 1/k 만큼 기여하게' 만드는 정규화이며, 확률 출력을 왜곡하므로 사후 보정이 필요해진다."}],
        code:{ c:"# 해석적 기울기와 수치 미분이 맞는지 본다\n(L(w + eps) - L(w - eps)) / (2 * eps)", cap:"부호 오류는 여기서 바로 드러난다" },
        key:["모든 인덱스가 정확히 한 번씩","divmod 로 나머지를 앞쪽에 분배","갱신은 값 − lr·기울기","가중치는 확률 출력을 왜곡한다"] } },
  ]
};
