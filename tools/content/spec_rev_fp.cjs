module.exports = {
  track: "fp", guide: "", xp: 70,
  unit: "코드 리뷰 — 결함 찾기",
  source: "./rev_fp.cjs",
  lessons: [
    { t: "순수한 척하는 코드", n: 4,
      th: { sum: "함수형처럼 <b>보이는</b> 코드가 실제로는 바깥을 바꾸는 자리를 찾는다.",
        body: [
          { h: "변환 자리의 부수효과", t: "<code>map</code> 안에서 바깥 변수를 바꾸거나, 파이프라인 한가운데 제자리 <code>sort</code> 를 두면 순서와 실행 횟수에 결과가 달린다. 값을 만드는 단계와 바꾸는 단계를 <b>나누는 것</b>이 규칙이다." },
          { h: "복사의 깊이", t: "전개는 한 겹만 복사하므로 중첩된 객체는 공유된 채로 남는다. 참조 동일성으로 변경을 감지하는 도구에서는 <b>값은 바뀌었는데 화면은 그대로</b>인 증상이 되고, 원본까지 함께 바뀌어 되돌리기와 비교가 어긋난다." }],
        code: { c: "const [labels, total] = rows.reduce(…)   // 함께 접기\nrows.slice().sort(cmp)                    // 복사본\n{ ...s, a: { ...s.a, xs: [...s.a.xs, x] } }", cap: "만드는 단계와 바꾸는 단계를 나눈다" },
        key: ["map 은 변환만 한다", "제자리 정렬은 입력을 바꾼다", "전개는 얕다", "참조가 같으면 안 바뀐 것으로 본다"] } },
    { t: "합성이 무너지는 자리", n: 4,
      th: { sum: "합성하려면 조각이 **예측 가능**해야 한다 — 상태·예외·게으름이 그 성질을 깬다.",
        body: [
          { h: "기억하는 함수", t: "클로저에 상태를 쌓으면 같은 입력에 다른 출력이 나오고, 메모이제이션은 순수하지 않은 함수를 감싸는 순간 첫 결과를 영원히 굳힌다. 캐시를 모듈에 하나만 두면 서로 다른 함수의 결과가 섞이기도 한다." },
          { h: "예외와 순회 비용", t: "실패를 던지고 곧바로 잡아 값으로 바꿀 것이라면 처음부터 <b>값으로 돌려주는</b> 편이 합성하기 쉽다. 그리고 배열 메서드는 게으르지 않으므로, 첫 하나만 필요한데 <code>map</code> 을 먼저 돌리면 전부 계산한다." }],
        code: { c: "const memo = (fn) => { const c = new Map(); return (x) => … };\nfor (const r of rows) { const v = f(r); if (ok(v)) return v; }", cap: "캐시는 감싼 함수마다 하나씩" },
        key: ["클로저에 변하는 상태를 두지 않는다", "메모이제이션은 순수함에 기댄다", "실패는 값으로 돌려준다", "배열 메서드는 게으르지 않다"] } },
  ],
};
