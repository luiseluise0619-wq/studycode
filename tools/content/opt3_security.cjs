/* security — 정답률 15% 아래 끝맺음의 오답 23개를 구체적인 틀린 답으로 (72차) */
module.exports = { track: "security", fixes: [
  { k: "session-fixation", from: "구조상 방어할 방법이 없다", to: "세션 하이재킹 - TLS 를 켜는 것 말고는 손쓸 길이 없다" },
  { k: "sec1-csp-role", from: "설정할 필요가 없다", to: "CSP 는 오래된 브라우저를 위한 호환 기능이라 최신 환경에서는 설정해도 무시된다" },
  { k: "sec1-traversal", from: "따로 검사하지 않아도 된다", to: "확장자가 허용 목록에 있으면 경로 자체는 검사를 생략해도 된다" },
  { k: "sec3-mass-assignment", from: "보안 위험은 생기지 않는다", to: "매핑 비용이 늘어나는 성능 문제만 있을 뿐 보안 위험과는 무관하다" },
  { k: "sec3-admin-route", from: "권한 검증은 따로 하지 않아도 된다", to: "관리자 화면을 별도 프론트엔드로 분리해 내부망에서만 배포하면 외부에서 닿을 수 없으므로 API 쪽 권한 검증은 생략해도 된다" },
  { k: "sec3-least-priv-db", from: "굳이 더 좁히지 않아도 된다", to: "계정 비밀번호를 충분히 길고 복잡하게 설정해 두면 그것만으로 권한 범위는 지금 그대로 두어도 된다" },
  { k: "sec5-obscurity", from: "따로 두지 않아도 된다", to: "외부에 노출되지 않는 내부 시스템이라면 구조를 숨기는 것만으로 충분한 보안이 되므로, 내부망 서비스에는 인증이나 암호화를 생략해도 된다" },
  { k: "sec5-upload", from: "따로 신경 쓰지 않아도 된다", to: "업로드 시점에 파일 확장자 허용목록만 검사하면 실행 가능한 파일이 들어올 수 없으므로, 저장 위치를 웹 루트 바깥으로 옮기거나 다운로드 응답 헤더를 서버가 지정하는 일은 다음 순위로 미뤄도 된다" },
  { k: "sec5-upload", from: "이름을 바꿔 저장할 필요가 없다", to: "업로드된 파일이 보내온 Content-Type 헤더를 그대로 믿고 그 값에 맞춰 저장하면 브라우저가 올바른 형식으로 해석하므로, 서버가 굳이 파일 내용을 따로 검사하거나 이름을 바꿔 저장하지 않아도 안전하다" },
  { k: "CSP bypass", from: "인라인 스크립트까지 허용해 주기 때문", to: "self 지시자가 같은 출처의 인라인 스크립트까지 허용해 주어서" },
  { k: "리플레이", from: "별도 대책이 필요 없다", to: "TLS 를 적용하면 요청이 암호화되므로 재전송 자체가 성립하지 않아 그것으로 끝이다" },
  { k: "CSRF 방어", from: "추가 대책이 전혀 필요 없다", to: "SameSite=Strict 는 모든 브라우저에서 100% 지원되고 크로스사이트 요청을 전부 차단하므로 토큰 같은 추가 대책은 중복일 뿐이다" },
  { k: "SSRF 방어", from: "실제로 연결되는 IP 는 확인하지 않아도 된다", to: "요청을 보내기 전에 호스트 문자열이 사내 도메인이나 메타데이터 IP 와 일치하는지 정규식으로만 검사해 걸러 내고, 실제로 연결되는 IP 는 확인을 생략한다" },
  { k: "논스·IV 관리", from: "보안상 아무 문제가 없다", to: "nonce 는 애초에 암호문과 함께 공개되는 값이므로 같은 키에서 재사용해도 보안상 달라지는 것은 없다" },
  { k: "sec2-logout", from: "별도 처리가 필요 없다", to: "클라이언트에서 쿠키를 지우면 브라우저가 서버에 종료 신호를 보내 서버 세션도 함께 무효화되므로 서버 쪽 코드는 비워 두어도 된다" },
  { k: "sec2-timeout", from: "무한히 연장되는 일이 생기지 않는다", to: "유휴 타임아웃 하나만 두면 사용자 활동이 계속 있는 한 세션이 무한히 연장되는 일은 막힌다" },
  { k: "sec4-base64", from: "안전하게 보호해 주기 때문", to: "전송 구간에서 값이 그대로 보이지 않게 해 주므로 중간 도청으로부터 키를 안전하게 보호해 주어서" },
  { k: "sec4-weak-hash", from: "연산 성능에 부담을 주기 때문", to: "출력 길이가 지나치게 길어 저장 공간과 연산 성능에 부담을 주어서" },
  { k: "sec4-hybrid", from: "따로 만들어 다룰 필요가 없다", to: "파일 전체를 수신자의 RSA 공개키로 암호화해 보내면 대칭키를 따로 만들어 다루는 단계가 사라진다" },
  { k: "sec4-cost-factor", from: "따로 조정할 필요가 없다", to: "작업 인자는 솔트 문자열 안에 들어 있으므로 솔트를 바꾸면 함께 바뀐다" },
  { k: "sec4-key-mgmt", from: "회전이 필요 없다", to: "환경변수에 평문으로 두면 배포마다 값이 새로 주입되므로 회전은 배포가 대신한다" },
  { k: "sec6-lockfile", from: "안전한 버전으로 올려 주기 때문", to: "알려진 취약점이 있는 버전을 만나면 자동으로 안전한 버전으로 올려 주어서" },
  { k: "sec6-typosquat", from: "검토하지 않아도 된다", to: "개발 단계에서만 쓰는 의존성은 배포에 포함되지 않으므로 검토 대상에서 빼도 된다" },
  { k: "sec6-hsts", from: "별도의 검증 절차가 필요 없다", to: "HSTS 헤더는 브라우저가 인증서 체인을 대신 검증해 주므로 서버 쪽 검증 절차는 생략된다" }
] };
