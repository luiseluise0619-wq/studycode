module.exports = {
  track: "arch", guide: "", xp: 60,
  unit: "단답으로 확인하기 — 원칙과 패턴의 이름",
  source: "./in_arch.cjs",
  lessons: [
    { t: "경계를 어디에 그을 것인가", n: 8,
      th: { sum: "설계 원칙은 대부분 한 가지를 말한다 — **바뀌는 이유가 다른 것을 갈라 두고, 화살표를 한쪽으로 몰아라.**",
        body: [
          { h: "무엇이 함께 바뀌는가", t: "<b>SRP</b> 는 '하나만 한다' 가 아니라 '<b>한 종류의 이해관계자에게만 답한다</b>' 이고, 파일 하나가 여러 팀의 요청으로 자꾸 열린다면 깨진 것이다 — 커밋 이력이 경계를 알려 준다. <b>OCP</b> 는 새 경우가 생겨도 기존 코드를 안 고치게 하라는 뜻이라, <code>switch</code> 가 타입마다 길어지면 신호다(다만 <b>두 번째로 고칠 때</b> 열어도 늦지 않다). <b>응집도</b>는 높고 결합도는 낮아야 하는데, 기술 종류별이 아니라 <b>기능별</b>로 나누면 저절로 그렇게 된다." },
          { h: "화살표를 안쪽으로", t: "<b>DIP</b> 의 핵심은 인터페이스를 <b>쓰는 쪽</b>에 두는 것이다 — 구현하는 쪽에 두면 화살표가 그대로다. 클린 아키텍처의 의존 방향은 언제나 <b>안쪽</b>이라, 업무 규칙은 UI·DB·프레임워크를 몰라야 한다. 같은 생각을 접점 쪽에서 부르는 이름이 <b>포트와 어댑터</b>이고, 진짜 이점은 바깥을 전부 가짜로 바꿔 <b>업무 규칙만 빠르게 테스트</b>할 수 있다는 것이다. 그리고 시스템 구조는 조직 구조를 닮는다(<b>콘웨이</b>)." }],
        code: { c: "// 업무 규칙 쪽에 인터페이스를 둔다\ninterface OrderRepo { save(o): void }\n// CI: 업무 규칙 파일에 프레임워크 import 금지", cap: "경계는 문서가 아니라 검사로" },
        key: ["바뀌는 이유가 다르면 가른다", "인터페이스는 쓰는 쪽에", "의존 화살표는 안쪽으로", "기능별로 나누면 응집도가 오른다"] } },
    { t: "나눈 뒤에 생기는 문제들", n: 7,
      th: { sum: "서비스를 나누면 트랜잭션·정합성·장애 전파라는 **새 문제**가 생긴다. 패턴들은 전부 그 대가를 다루는 방법이다.",
        body: [
          { h: "묶을 수 없을 때", t: "서비스마다 저장소를 나누면(<b>DB 분리</b>) 하나의 커밋으로 묶을 수 없어, 단계마다 보상 동작을 두는 <b>사가</b> 로 푼다 — 보상은 '되돌리기' 가 아니라 <b>새 사실을 더하는 일</b>이다(환불 기록). 상태 대신 <b>이벤트</b>를 쌓으면 과거 시점 복원과 감사 기록이 따라오지만, 이미 쌓인 기록의 형식을 못 고친다는 어려움이 생긴다. 읽기와 쓰기 요구가 충돌하면 <b>CQRS</b> 로 나누되, 나눈 순간 동기화라는 새 문제가 온다." },
          { h: "실패를 전제로 짜기", t: "네트워크는 응답을 잃으므로 보낸 쪽은 <b>모르는 채 재시도</b>한다 — <b>멱등성</b>이 없으면 재시도가 곧 중복 결제다. 서킷 브레이커는 차단 뒤 <b>half-open</b> 으로 몇 개만 시험해 보고, 차단 중에 무엇을 돌려줄지 미리 정해 둬야 한다. 인기 키가 만료될 때 몰려가는 <b>스탬피드</b> 는 잠금·흔들림·미리 갱신으로 막고, 배포 직후 캐시가 빈 상황도 용량 계획에 넣는다. 설정은 <b>환경변수</b>로 빼 같은 산출물을 어느 환경에나 올린다." }],
        code: { c: "Idempotency-Key: <uuid>       // 같은 키엔 저장된 결과를\nhalf-open → 성공이면 닫고, 실패면 대기 시간 늘리기\nTTL + jitter                   // 한꺼번에 만료되지 않게", cap: "나눈 대가를 패턴으로 치른다" },
        key: ["보상은 새 사실을 더하는 일", "멱등 키가 재시도를 안전하게 한다", "차단 중 무엇을 줄지 미리 정한다", "만료 시각에 흔들림을 준다"] } },
  ],
};
