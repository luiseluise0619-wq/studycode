/* python 트랙 — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다.
   '— 원인: / 해결:' 꼴의 시니어 문항은 오답에도 같은 밀도의 (틀린) 근거를 붙인다.
   node fix_opts.cjs --apply opt_python.cjs */
module.exports = { track: "python", fixes: [
  { k: "DB 갑자기 느림", o: [
    "실행 중 쿼리와 실행 계획(EXPLAIN)·대기 이벤트·락을 확인해 느린 쿼리와 원인을 특정한다 — 원인: 데이터 증가·통계 변화·락으로 계획이 나빠질 수 있음 / 해결: 병목 쿼리를 찾아 인덱스·통계·락을 조치",
    "DB 를 재시작한다 — 원인: 오래 켜 둔 DB 는 캐시와 커넥션 상태가 꼬여 느려지며, 코드가 그대로라면 서버 상태 말고는 의심할 곳이 없음 / 해결: 껐다 켜서 메모리와 커넥션을 초기화하고, 그래도 느리면 그때 쿼리를 본다",
    "애플리케이션 코드를 전면 리팩터링한다 — 원인: 쿼리가 느려졌다면 결국 코드가 느린 것이므로 로직부터 손봐야 한다는 판단 / 해결: 병목으로 의심되는 함수를 처음부터 다시 작성하고 쿼리도 함께 새로 짜서 배포한다",
    "타임아웃을 무한대로 늘린다 — 원인: 느려진 쿼리가 타임아웃에 걸려 실패하는 것이 실제 장애이고, 느린 것 자체는 참을 수 있음 / 해결: 대기를 무한 허용해 실패를 없애고, 원인 분석은 트래픽이 줄어든 뒤로 미룬다" ] },
  { k: "락 경합", o: [
    "모든 쿼리를 SELECT 로 바꾼다 — 원인: 쓰기가 락을 잡으니 쓰기를 없애면 경합도 사라짐 / 해결: 재고 변경을 읽기 전용 뷰로 대체하고 실제 차감은 야간 배치로 몰아 낮에는 잠금이 생기지 않게 한다",
    "트랜잭션을 없앤다 — 원인: 트랜잭션이 행을 오래 붙들어 대기가 생기므로 트랜잭션 자체가 병목 / 해결: autocommit 으로 바꿔 문장 하나가 끝나는 즉시 락이 풀리게 하고, 일관성은 애플리케이션에서 다시 맞춘다",
    "락 대기·데드락을 확인하고, 트랜잭션을 짧게·일관된 순서로 잠그며 핫 로우 경합을 줄인다 — 원인: 같은 행에 쓰기가 몰려 직렬화 / 해결: 트랜잭션 범위 축소·잠금 순서 정렬·필요 시 원자적 감소나 큐잉",
    "격리 수준을 SERIALIZABLE 로 올린다 — 원인: 일관성이 가장 중요하고, 격리가 약해서 요청들이 서로 끼어드는 것 / 해결: 최고 격리 수준으로 전부 잠가 버린다. 잠금 범위를 넓히면 경합이 오히려 줄어든다" ] },
  { k: "정수 인터닝", o: [
    "int 는 같은 값이면 항상 같은 객체다 — 원인: 정수는 불변이라 인터프리터가 값마다 객체 하나만 두어도 안전하고 메모리도 아낌 / 해결: 어떤 정수든 is 비교가 True 이므로 == 대신 is 를 써도 결과가 같다",
    "CPython 이 -5~256 작은 정수를 미리 캐시(인터닝)하므로 256 은 같은 객체, 그 밖은 보장되지 않음 — 원인: 작은 수를 재사용 / 해결: 범위 밖은 구현 의존이라 값 비교엔 == 를 씀",
    "파이썬 인터프리터의 버그다 — 원인: 같은 값을 다른 객체로 만드는 것은 값 의미론에 어긋나며 언어 명세에도 없는 동작 / 해결: 이슈로 신고하고, 그때까지는 is 대신 == 를 써서 우회한다",
    "257 이 더 커서 메모리가 다르다 — 원인: 값이 클수록 더 많은 바이트를 차지하므로 서로 다른 주소에 놓이게 됨 / 해결: 큰 수는 다른 객체가 되므로 is 비교는 작은 수에서만 True 가 된다고 외워 둔다" ] },
  { k: "try/else", o: [
    "예외가 발생했을 때 실행되는 두 번째 except 블록으로, 앞의 except 가 잡지 못한 예외를 받는다",
    "try 보다 먼저 실행되어 사전 조건을 검사하고, 조건이 맞지 않으면 try 블록을 건너뛴다",
    "finally 와 동일하게 예외 여부와 관계없이 항상 실행되며, 두 블록의 차이는 실행 순서뿐이다",
    "try 블록에서 예외가 발생하지 않았을 때만 실행되어, try 로 감싸는 범위를 최소화할 수 있다" ] },
  { k: "쓰기 후 일관성", o: [
    "가격 수정 후 캐시를 무효화(또는 갱신)하지 않아 낡은 값이 남음 — 원인: 쓰기 경로에 무효화 누락 / 해결: 쓰기 트랜잭션 커밋 후 해당 캐시 키를 삭제·갱신",
    "DB 가 느려서 — 원인: 쓰기가 디스크에 반영되기까지 지연이 있어 그 사이 읽기가 옛 행을 봄 / 해결: DB 를 증설하고 쓰기 지연을 줄이면 캐시는 손대지 않아도 새 값이 보인다",
    "브라우저가 페이지를 캐시했기 때문 — 원인: 클라이언트가 이전 응답을 재사용해 서버에 묻지 않음 / 해결: Cache-Control 을 no-store 로 바꾸고 사용자에게 강제 새로고침을 안내한다",
    "TTL 이 너무 짧아서 생긴 문제 — 원인: 캐시가 너무 자주 만료되어 갱신 도중의 불완전한 값이 노출됨 / 해결: TTL 값을 크게 늘려 만료 빈도를 낮추고 갱신 시점을 안정시킨다" ] },
  { k: "데이터 vs 비데이터", o: [
    "인스턴스 __dict__ 가 최우선이다 — 원인: 인스턴스에 직접 담긴 값이 클래스에 정의된 것보다 구체적이므로 먼저 찾아야 함 / 해결: 속성 조회 시 dict 를 먼저 보고 없을 때만 클래스 쪽 디스크립터를 본다",
    "비데이터 디스크립터가 우선한다 — 원인: __get__ 만 있는 가벼운 디스크립터가 먼저 정의되고 더 자주 쓰이므로 조회 순서의 앞에 둠 / 해결: 메서드처럼 __get__ 만 가진 것이 인스턴스 dict 를 이기고, __set__ 이 있으면 뒤로 밀린다",
    "둘은 완전히 같다 — 원인: 데이터·비데이터는 문서상 이름일 뿐 인터프리터가 구분하는 속성이 아님 / 해결: 어느 쪽이든 인스턴스 __dict__ 가 먼저 조회되므로 우선순위를 따로 기억할 필요가 없다",
    "__set__(또는 __delete__)이 있으면 데이터 디스크립터로, 인스턴스 __dict__ 보다 우선한다 — 원인: 검증·저장 제어를 위해 인스턴스 속성이 덮지 못하게 해야 함 / 해결: 데이터 디스크립터가 인스턴스 dict 를 이겨 접근을 가로챔" ] },
  { k: "메서드도 디스크립터", o: [
    "클래스가 호출 때마다 self 를 복사해 넣는다 — 원인: 인스턴스마다 메서드 본체를 따로 두어야 상태가 격리됨 / 해결: 클래스 생성 시 메서드를 인스턴스 수만큼 복제하고 각 사본에 self 를 박아 둔다",
    "함수 객체가 __get__ 을 가진 비데이터 디스크립터라, 인스턴스로 접근하면 self 가 묶인 메서드 객체를 돌려준다 — 원인: 함수 접근을 가로채 바인딩이 필요 / 해결: 디스크립터 프로토콜이 bound method 를 생성",
    "self 는 인터프리터가 관리하는 전역 변수다 — 원인: 현재 실행 중인 인스턴스를 어디서나 알아야 하므로 공유 저장소가 필요함 / 해결: 메서드에 진입할 때 전역 self 를 갱신하고 본문은 그 값을 읽는다",
    "파이썬이 self 를 마법처럼 넣어 준다 — 원인: 인스턴스 메서드 호출에는 언어 차원의 특별한 규칙이 따로 적용되기 때문 / 해결: 인터프리터가 호출 구문을 가로채 첫 인자 자리에 인스턴스를 예외적으로 끼워 넣어 준다" ] },
  { k: "순환참조 GC", o: [
    "서로를 참조하는 순환은 refcount 가 0 이 안 되므로, 세대별 표시-수집 GC 가 도달 불가 순환을 찾아 회수한다 — 원인: 순환은 카운트가 0 에 도달 못 함 / 해결: gc 모듈의 세대별 수집기가 해제",
    "참조 카운팅이 모든 것을 다 잡는다 — 원인: 객체가 사라질 때마다 카운트가 정확히 줄어들기 때문에 순환도 예외가 아님 / 해결: 별도의 GC 없이 refcount 만으로 회수가 끝나고 gc 모듈은 디버깅용이다",
    "OS 가 프로세스 종료 때 알아서 지운다 — 원인: 순환은 인터프리터가 못 잡지만 커널은 프로세스 메모리 전체를 회수함 / 해결: 장시간 도는 서비스가 아니면 신경 쓰지 않고, 서비스라면 주기적으로 재시작해 되돌린다",
    "순환은 영원히 누수된다 — 원인: refcount 가 0 이 되지 않으면 어떤 장치도 그 객체에 닿을 수 없음 / 해결: weakref 로 순환을 만들지 않는 것만이 방법이고, 이미 생긴 순환은 재시작으로만 정리된다" ] },
  { k: "backgroundtasks-vs-celery", o: [
    "BackgroundTasks 는 같은 프로세스에서 응답 후 실행돼 재시도·영속 큐·수평 확장이 없어서",
    "BackgroundTasks 는 async 함수를 받지 못해서 — 이메일 클라이언트가 비동기면 동기 래퍼를 따로 만들어야 해 코드가 늘어난다",
    "BackgroundTasks 가 이메일 발송을 지원하지 않기 때문 — 네트워크를 쓰는 작업은 큐에 넣을 수 없어 SMTP 호출이 막힌다",
    "Celery 가 항상 더 빨라서 — 별도 워커가 전담하므로 같은 작업이라도 응답 후 처리보다 먼저 끝나 사용자가 빨리 받는다" ] },
  { k: "서킷 브레이커", o: [
    "실패율이 임계치를 넘으면 회로를 열어 즉시 실패시키고, 일정 시간 뒤 반열림에서 소수 요청으로 회복을 탐지해 닫는다 — 원인: 죽은 의존성에 계속 호출하면 자원 낭비·전파 / 해결: 회로를 열어 빠른 실패로 보호하고 점진 재개",
    "회로는 호출마다 상태를 검사하므로 성능만 떨어뜨린다 — 원인: 정상일 때도 카운터와 타이머를 갱신하는 오버헤드가 붙음 / 해결: 재시도와 타임아웃만 두고 브레이커는 쓰지 않으며, 장애는 모니터링 경보로 사람이 대응한다",
    "실패가 이어지면 전체 서버를 종료한다 — 원인: 하위 서비스가 계속 실패하면 우리 서버도 정상이 아니라고 보아야 함 / 해결: 프로세스를 kill 해 재시작을 유도하고 오케스트레이터가 다시 띄우게 맡긴다",
    "실패해도 호출을 계속 보낸다 — 원인: 하위 서비스가 언젠가 회복되면 그때부터는 성공하므로 시도를 멈출 이유가 없음 / 해결: 짧은 간격으로 계속 재시도해 회복 순간을 놓치지 않고, 실패 응답은 로그만 남긴다" ] },
  { k: "Protocol vs ABC", o: [
    "Protocol 은 런타임 검사만 하고 ABC 는 정적 검사만 한다 — 방향이 정반대라 mypy 는 Protocol 을 보지 못한다",
    "Protocol 은 메서드를 하나만 가질 수 있다 — 여러 메서드가 필요하면 Protocol 을 여러 개 만들어 조합해야 한다",
    "Protocol 은 구조적 서브타이핑이라 상속 선언 없이 메서드 모양만 맞으면 되고, ABC 는 명시적 상속이 필요하다",
    "ABC 는 타입 힌트에 사용할 수 없다 — 추상 클래스는 인스턴스를 만들 수 없으므로 매개변수 타입으로도 쓸 수 없다" ] },
  { k: "프로세스 전달 비용", o: [
    "계산량이 매우 커서 모든 코어를 다 쓰고 싶을 때 — 프로세스는 코어 수만큼만 뜨므로 스레드보다 병렬도가 낮아진다",
    "작업들이 서로 완전히 독립적일 때 — 공유할 것이 없으면 프로세스를 나눌 이유가 없고 스레드로도 같은 병렬성이 나온다",
    "작업당 계산량은 작은데 주고받는 데이터가 커서 피클 직렬화 비용이 계산 시간을 넘어설 때",
    "전역 인터프리터 락을 우회하고 싶을 때 — GIL 은 프로세스 단위로 걸리므로 여러 프로세스를 띄워도 한 번에 하나만 돈다" ] },
  { k: "디스크립터 정의", o: [
    "property 는 메타클래스다 — 원인: 클래스가 만들어지는 시점에 개입해야 속성 접근을 바꿀 수 있기 때문 / 해결: 메타클래스로 속성 접근 로직을 심어 두고 인스턴스마다 적용되게 만든다",
    "property 는 이름을 함수에 매핑하는 딕셔너리다 — 원인: getter·setter·deleter 세 함수를 이름별로 보관해야 함 / 해결: 접근 시 dict 에서 해당 함수를 꺼내 호출하고, 없으면 AttributeError 를 낸다",
    "property 는 __get__/__set__/__delete__ 를 구현한 디스크립터의 한 사례다 — 원인: 속성 접근을 가로채는 프로토콜이 필요 / 해결: 디스크립터 프로토콜로 클래스 속성 접근 시 지정한 로직을 실행",
    "property 는 그냥 함수를 감싼 데코레이터 함수다 — 원인: @property 문법이 함수를 받아 함수를 돌려주는 꼴이기 때문 / 해결: 속성처럼 보이도록 호출 괄호를 생략하게 해 주는 문법 설탕으로 처리한다" ] },
  { k: "__slots__", o: [
    "인스턴스별 __dict__ 를 없애 고정 슬롯에 속성을 저장하므로 인스턴스 메모리가 줄고, 정의 안 된 속성 추가를 막는다 — 원인: 기본 인스턴스는 dict 오버헤드가 큼 / 해결: 슬롯 배열로 대체해 대량 인스턴스 메모리를 절감",
    "__slots__ 는 실행 속도만 올린다 — 원인: 속성 이름을 미리 알고 있으므로 조회가 캐시에 잘 맞아 빨라지지만 저장 방식은 그대로임 / 해결: 메모리가 목적이면 슬롯이 아니라 array 나 numpy 로 좌표를 묶어야 한다",
    "__slots__ 는 상속을 금지한다 — 원인: 슬롯 배열의 배치가 고정되어 자식 클래스가 속성을 더할 수 없음 / 해결: 상속이 필요한 클래스에는 쓰지 않고, final 클래스에서만 캡슐화 목적으로 쓴다",
    "__slots__ 는 클래스 속성을 지운다 — 원인: 클래스에 남아 있는 불필요한 속성 정의를 정리해야 메모리가 줄어듦 / 해결: 선언된 이름만 남기고 나머지 클래스 속성을 제거해 인스턴스 크기를 줄인다" ] },
  { k: "상관관계 ID", o: [
    "동시 요청 수를 제한하기 위해 — ID 를 발급한 개수를 세어 상한을 넘으면 새 요청을 거절한다",
    "로그 양을 줄이기 위해 — 같은 ID 를 가진 줄은 한 번만 남기고 나머지는 참조로 대체할 수 있다",
    "여러 모듈·서비스에 흩어진 로그를 하나의 요청 흐름으로 묶어 추적할 수 있다",
    "로그 레벨을 요청마다 자동으로 조정하기 위해 — ID 뒤 자리로 샘플링 비율을 정해 일부 요청만 DEBUG 로 남긴다" ] },
  /* 2차 — 16~25 */
  { k: "jwt-verify-scope", o: [
    "토큰 디코드·권한 확인을 Depends 의존성으로 만들어 필요한 라우트에 주입한다",
    "프런트에서 admin 여부를 판단해 관리자 메뉴를 숨긴다 — 화면에 보이지 않으면 호출도 일어나지 않으므로 서버 검증은 생략한다",
    "각 라우트 함수 안에서 직접 토큰을 파싱한다 — 라우트마다 필요한 클레임이 다르므로 공통화하면 오히려 조건이 복잡해진다",
    "미들웨어에서 모든 라우트를 admin 권한으로 막아 둔다 — 라우트별 예외는 경로 목록으로 관리하면 되므로 의존성보다 단순하다" ] },
  { k: "race condition", o: [
    "스레드가 하나뿐일 때에만 생기는 현상으로, 같은 스레드가 자기 자신이 바꾼 값을 다시 읽으면서 순서가 꼬이는 문제다",
    "CPU 가 과열되어 클럭을 낮추는 현상으로, 스레드가 많을수록 코어 온도가 올라 실행 속도가 예측 불가능해지는 문제다",
    "여러 스레드가 공유 자원에 동시에 접근해 결과가 실행 순서에 따라 달라지는 문제",
    "네트워크가 느려져 응답 순서가 요청 순서와 달라지는 현상으로, 먼저 보낸 요청의 답이 나중에 도착하는 문제다" ] },
  { k: "I/O에서 스레드", o: [
    "인터프리터가 I/O 코드만 기계어로 컴파일하기 때문 — 소켓과 파일 호출은 JIT 을 거쳐 바이트코드 해석 없이 돈다",
    "I/O 작업에는 GIL 이 존재하지 않기 때문 — 파일과 소켓 모듈은 C 로 짜여 있어 락의 대상이 아니다",
    "네트워크·파일 응답을 기다리는 동안 GIL 이 해제되어 다른 스레드가 실행될 수 있기 때문",
    "스레드가 I/O 작업을 자동으로 여러 코어에 분산해 주기 때문 — 운영체제가 대기 중인 스레드를 다른 코어로 옮겨 동시에 읽는다" ] },
  { k: "string concat join", o: [
    "+= 대신 result = result + token 으로 쓰면 최적화된다 — 복합 대입은 제자리 연산을 시도하다 실패해 두 번 복사하기 때문이다",
    "result 를 전역으로 두면 재할당이 없어 빨라진다 — 지역 변수는 루프마다 프레임에 다시 묶이므로 복사가 한 번 더 일어난다",
    "token 이 유니코드 문자열이라 느린 것이므로 encode() 로 바이트열로 바꾼 뒤 이어붙이면 복사 비용이 사라져 빨라진다",
    "문자열은 불변이라 += 마다 새 문자열을 만들어 누적 복사가 O(n^2) 에 가깝다. ''.join(tokens) 로 한 번에 이어붙이면 O(n) 이 된다" ] },
  { k: "커넥션 풀링", o: [
    "풀은 SQL 을 자동 최적화한다 — 원인: 같은 쿼리가 반복되어 파싱과 계획 수립이 매번 일어남 / 해결: 풀이 실행 계획을 캐시해 두고 다음 요청부터는 계획 없이 바로 실행한다",
    "풀은 트랜잭션을 없앤다 — 원인: 요청마다 BEGIN/COMMIT 이 오가며 왕복이 두 번 늘어남 / 해결: 풀이 커넥션을 autocommit 으로 유지해 트랜잭션 왕복을 생략한다",
    "풀은 조회 결과를 캐싱한다 — 원인: DB 자체가 느려서 생기는 문제 / 해결: 결과를 메모리에 담아 두고 DB 를 아예 치지 않게 만든다",
    "커넥션 생성에는 TCP·인증 핸드셰이크 비용이 크다 — 원인: 매 요청 연결 수립·해제가 반복 / 해결: 미리 만든 커넥션을 재사용해 지연과 DB 부하를 줄인다" ] },
  { k: "N+1 쿼리", o: [
    "DB 서버가 약해서 — 원인: 101 번의 쿼리 자체는 정상이고 처리할 하드웨어가 모자란 것 / 해결: CPU 와 메모리를 증설해 초당 처리 가능한 쿼리 수를 늘린다",
    "각 항목마다 연관 데이터를 개별 조회하는 N+1 문제 — 원인: 루프 안에서 행마다 추가 쿼리 발생 / 해결: eager loading·JOIN·IN 절로 한 번에 모아 조회",
    "인덱스가 너무 많아서 생긴 문제 — 원인: 인덱스 과다로 조회 계획이 꼬임 / 해결: 불필요한 인덱스를 지운다. 인덱스를 줄이면 쿼리 계획이 단순해진다",
    "네트워크가 느려서 — 원인: 앱 서버와 DB 사이의 왕복 지연이 쿼리마다 더해짐 / 해결: DB 를 같은 호스트로 옮기거나 회선을 증설해 왕복 시간을 줄인다" ] },
  { k: "성급최적화", o: [
    "가독성을 해치면서 전체 성능에 무의미한 곳을 최적화하는 성급한 최적화다",
    "문제 없다 — 빠를수록 항상 좋고, 처음부터 최적화해 두면 나중에 병목을 찾아 고치는 비용이 들지 않는다",
    "최적화는 배포 후에만 해야 한다 — 실제 트래픽이 없으면 병목을 알 수 없으므로 개발 중에는 어떤 최적화도 금물이다",
    "마이크로 최적화는 어떤 경우에도 병목을 해결해 준다 — 작은 개선이 쌓여 전체 성능이 되므로 지금 해 두는 편이 낫다" ] },
  { k: "I/O 작업 선택", o: [
    "순차 반복문으로 요청하되 요청마다 sleep 을 넣는다 — 상대 서버의 속도 제한을 피하는 것이 총 시간보다 중요하다",
    "asyncio + 비동기 HTTP 클라이언트, 또는 ThreadPoolExecutor 로 동시 요청",
    "ProcessPoolExecutor 로 프로세스를 500 개 만들어 나눠 보낸다 — 프로세스마다 GIL 이 따로라 가장 확실한 병렬성이 나온다",
    "GIL 을 해제하는 C 확장을 직접 작성한다 — 파이썬 수준의 동시성은 어떤 방식이든 GIL 에 막히므로 C 로 내려가야 한다" ] },
  { k: "동시성 vs 병렬성", o: [
    "둘은 완전히 같은 말이다 — 동시성은 이론 용어, 병렬성은 실무 용어일 뿐 가리키는 현상은 하나다",
    "동시성은 여러 작업을 번갈아 진행, 병렬성은 물리적으로 동시에 실행",
    "병렬성은 단일 코어에서만 일어난다 — 코어 하나가 명령어를 파이프라인으로 겹쳐 실행하는 것이 병렬성의 정의다",
    "동시성은 CPU 코어가 여러 개일 때에만 가능하다 — 코어 하나면 한 번에 한 작업만 있으므로 동시성이 성립하지 않는다" ] },
  { k: "property 활용", o: [
    "같은 이름 temp 를 property 로 바꿔 게터/세터에 검증을 넣는다 — 원인: 접근 구문을 유지하며 로직을 추가해야 함 / 해결: property 가 속성 접근을 가로채므로 호출부 변경 없이 검증 삽입",
    "전역 함수 validate(obj) 로 검증한다 — 원인: 클래스 내부를 건드리면 배포된 API 가 바뀔 위험이 있음 / 해결: 호출자가 값을 넣은 뒤 함수를 한 번 불러 검사하도록 문서에 안내한다",
    "검증을 포기하고 문서로만 경고한다 — 원인: public 속성은 이미 계약이라 어떤 로직도 끼워 넣을 수 없음 / 해결: 잘못된 값은 사용 시점에 예외로 드러나게 두고 docstring 에 허용 범위를 적는다",
    "속성 이름을 바꿔 강제로 메서드로 만든다 — 원인: 검증 로직이 필요함 / 해결: temp 를 get_temp() 로 바꾼다. 호출부는 모두 새 메서드 이름으로 고치도록 안내하면 된다" ] },
] };
