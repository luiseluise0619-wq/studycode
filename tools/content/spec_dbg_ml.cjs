module.exports = {
  track: "ml", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 학습은 도는데 결론이 틀린 코드 고치기",
  source: "./dbg_ml.cjs",
  lessons: [
    { t: "누수와 지표가 거짓말하는 곳", n: 6,
      th: { sum: "가장 비싼 ML 버그는 **오류를 내지 않는다** — 점수가 좋아지고, 그 점수가 거짓이다.",
        body: [
          { h: "검증 점수가 놀랍도록 좋다면", t: "시계열의 검증 집합을 **앞쪽**에서 떼면 모델이 미래를 보고 과거를 맞힌다. 정규화 파라미터를 전체 데이터에서 구하는 것도 같은 누수다. '점수가 놀랍게 좋다' 는 축하가 아니라 **누수를 의심할 신호**이고, 시간·사용자 경계가 학습과 검증 사이에서 새지 않았는지 먼저 봐야 한다." },
          { h: "지표는 클래스 비율을 알고 고른다", t: "양성이 1% 인 자료에서 '전부 음성' 은 정확도 99% 다. **균형 정확도**는 클래스별 재현율을 평균해 이 착시를 없앤다. F1 이 조화평균인 이유도 같다 — 한쪽이 0 이면 0 이어야 하는데 산술평균은 절반을 준다. 그리고 문턱 끝에서는 분모가 0 이 되므로 지표 함수마다 그 경우를 계약에 넣는다." }],
        code: { c: "const cut = rows.length - Math.floor(rows.length * frac);\nreturn [rows.slice(0, cut), rows.slice(cut)];   // 검증은 최근 구간", cap: "기준선 모델의 점수를 먼저 재 본다" },
        key: ["시계열 검증은 뒤에서 뗀다", "불균형에는 균형 정확도", "F1 은 조화평균", "분모 0 을 계약에 넣는다"] } },
    { t: "전처리와 학습 루프가 조용히 망가지는 곳", n: 6,
      th: { sum: "`NaN` 하나가 가중치 전체를 오염시키고, 방향이 반대인 가중치가 불균형을 키운다 — **전부 오류 없이** 일어난다.",
        body: [
          { h: "정의역이 있는 연산 앞에 가드", t: "상수 특성은 최소최대 정규화에서 0 으로 나누고, 확률 0 은 로그 손실을 무한으로 만든다. 둘 다 **클리핑이나 분기**로 막아야 하며, 학습 루프에 '유한한 값인가' 검사를 두면 그 자리에서 멈춘다. 결측을 0 으로 바꾸는 것도 '모른다' 를 '0 이다' 로 바꾸는 조용한 왜곡이다." },
          { h: "방향과 단위를 검산한다", t: "클래스 가중치는 **빈도의 역수**여야 하고, 학습률 감쇠의 지수는 에폭이 아니라 `floor(epoch/step)` 이다. 조기 종료는 직전이 아니라 **지금까지의 최고 기록**과 견줘야 한다. 셋 다 균등하거나 단조로운 입력에서는 우연히 맞는다." }],
        code: { c: "const p = Math.min(1 - eps, Math.max(eps, ps[i]));\nreturn lr0 * gamma ** Math.floor(epoch / step);", cap: "극단값을 넣어 방향을 검산한다" },
        key: ["로그·나눗셈 앞에 클리핑", "결측은 빼고 센다", "가중치는 빈도의 역수", "조기 종료는 최고 기록 기준"] } },
  ],
};
