module.exports = {
  track: "pandas", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 행이 사라지거나 값이 어긋나는 코드 고치기",
  source: "./dbg_pandas.cjs",
  lessons: [
    { t: "행이 조용히 사라지는 곳", n: 6,
      th: { sum: "판다스 버그는 **예외 없이 행을 줄인다** — 숫자가 이상하다는 말이 나올 때까지 아무도 모른다.",
        body: [
          { h: "기본값이 버린다", t: "`merge(how='inner')` 는 한쪽에만 있는 키를 버리고, `groupby` 는 `dropna=True` 라 키가 결측인 행을 버린다. 둘 다 정보를 '붙이려던' 의도와 정반대다. 조인 전후로 **행 수를 확인**하고, 집계 뒤에는 '**합이 전체와 같은가**' 를 확인하면 이 두 버그가 한 줄로 막힌다." },
          { h: "축과 방향의 기본값", t: "`sum(axis=1)` 은 행별 합계라 결과 인덱스가 행 번호가 되고, `sort_values` 의 기본은 오름차순이라 '상위' 를 뽑는 코드가 정확히 반대로 돈다. `drop_duplicates` 는 `keep='first'` 라 가장 **오래된** 행이 남는다. 셋 다 이름만 봐서는 기본값이 드러나지 않는다." }],
        code: { c: "m = orders.merge(users, on='uid', how='left')\nassert len(m) == len(orders)   # 행이 줄지도 늘지도 않았다", cap: "조인 전후 행 수를 못 박는다" },
        key: ["inner 는 행을 버린다", "groupby 는 결측 키를 버린다", "정렬 방향을 명시한다", "keep 을 명시한다"] } },
    { t: "값과 타입이 어긋나는 곳", n: 6,
      th: { sum: "결측·인덱스·기본 집계 — **판다스의 편리함이 그대로 함정**이 되는 세 자리.",
        body: [
          { h: "결측을 어떻게 볼 것인가", t: "센서 값의 결측은 '측정 못 함' 이지 0 이 아니다 — `fillna(0)` 은 그래프에 없던 급락을 만든다. `NaN` 이 있는 열은 `astype(int)` 가 예외를 던지고, `str.contains` 는 `NaN` 을 돌려줘 마스크 인덱싱을 터뜨린다(`na=False` 로 막는다). 결측 처리는 **도메인 결정**이라 함수마다 계약에 적어야 한다." },
          { h: "인덱스는 조용히 정렬된다", t: "걸러 낸 표는 인덱스에 구멍이 있어, 인덱스가 `0..n-1` 인 시리즈를 대입하면 겹치는 자리만 채워지고 나머지는 `NaN` 이 된다. `concat` 은 인덱스를 그대로 이어 붙여 중복을 만든다(`ignore_index=True`). 그리고 `pivot_table` 의 기본 집계는 **평균**이라, 조합마다 행이 하나뿐인 표본에서는 절대 드러나지 않는다." }],
        code: { c: "big['double'] = big['v'] * 2       // 같은 표의 열로 계산\npd.concat([a, b], ignore_index=True)", cap: "인덱스가 고유한지 확인한다" },
        key: ["결측을 0 으로 바꾸지 않는다", "str 접근자에는 na= 를 적는다", "대입은 인덱스를 맞춘다", "집계 함수를 명시한다"] } },
  ],
};
