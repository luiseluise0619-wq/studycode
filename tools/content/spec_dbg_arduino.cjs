module.exports = {
  track: "arduino", guide: "", xp: 80,
  unit: "실행형 실전 — 며칠 돌리면 어긋나는 코드 고치기",
  source: "./dbg_arduino.cjs",
  lessons: [
    { t: "값을 읽고 셈하는 곳", n: 6,
      th: { sum: "임베디드의 계산 버그는 **표현 범위와 양 끝값**에서 난다 — 몇 단계인지, 최댓값이 얼마인지, 넘치면 어떻게 되는지.",
        body: [
          { h: "단계 수와 최댓값은 하나 차이", t: "10비트 ADC 는 <b>1024 단계</b>이고 <b>최댓값은 1023</b> 이다 — 1024 로 나누면 최대 전압이 영영 안 나오고, <code>floor</code> 는 오차를 한쪽으로만 쌓는다. 비례 변환은 <b>범위 밖에서도 그대로 계산</b>되므로 하드웨어로 나가기 직전에 잘라야 한다. 이동 평균은 창이 차기 전에 <b>받은 개수</b>로 나눠야 켜자마자의 값이 맞는다. 변환 함수의 테스트에는 언제나 <b>양 끝값</b>을 넣는다." },
          { h: "누산기의 폭과 실수 비교", t: "8비트 마이크로컨트롤러에서 <code>int</code> 는 <b>16비트</b>다. ADC 표본 1023 을 65 개만 더해도 넘치고, 경고 없이 값만 작아진다 — 누적·곱셈 자리마다 '가장 큰 값이 몇인가' 를 손으로 계산해 적어 둔다. 아날로그 값이 임계와 <b>정확히 같아지는 일</b>은 없고 <code>0.1 + 0.1 !== 0.2</code> 이므로, 판정은 <code>==</code> 가 아니라 <b>구간</b>으로 한다. 입력 극성도 회로가 정한다 — 풀업은 <b>눌렸을 때 0</b> 이다." }],
        code: { c: "Math.round(raw * 5000 / 1023);      // 최댓값이 1023 이다\nMath.min(hi, Math.max(lo, v));      // 나가기 직전에 자른다\nprev < th && cur >= th;             // 같은지가 아니라 넘었는지", cap: "표현 범위를 손으로 계산해 둔다" },
        key: ["단계 수와 최댓값을 구분한다", "하드웨어로 나가는 값은 자른다", "누산기 폭을 계산해 둔다", "실수는 == 로 견주지 않는다"] } },
    { t: "시간·인터럽트·주변장치", n: 6,
      th: { sum: "책상에서 몇 시간 돌려 보는 시험으로는 **되돌아옴·튐·끼어듦·조각남**을 절대 만날 수 없다. 넷 다 코드로 가정해 두어야 한다.",
        body: [
          { h: "시간은 차이로, 접점은 연속으로", t: "<code>millis()</code> 는 약 <b>49.7 일</b>마다 0 으로 되돌아온다. <code>last + interval</code> 은 넘칠 수 있지만 <code>now - last</code> 는 되돌아온 뒤에도 <b>지난 시간을 정확히</b> 낸다 — 시각 비교는 언제나 <b>차이로</b> 적는다. 기계 접점은 붙는 순간 수십 번 튀므로, 눌림은 '변화' 가 아니라 '<b>연속으로 n 번 같은 값</b>' 으로 판정하고 <code>delay()</code> 대신 표본이나 경과 시간으로 센다." },
          { h: "끼어듦과 조각남을 가정한다", t: "인터럽트와 공유하는 값은 <b>읽고 쓰는 사이에 끼어들 수 있다</b>고 가정한다 — 0 으로 덮지 말고 <b>가져간 만큼만 빼고</b>, 여러 바이트 값은 원자적으로 읽는다. 시리얼은 <b>바이트 흐름</b>이라 조각의 경계가 의미의 경계와 다르다 — 남은 조각을 이어 붙이고 <b>마지막 미완성 조각</b>은 남긴다. I²C 주소는 <b>7비트</b>인데 데이터시트는 8비트로 적고, EEPROM 은 쓰기 수명이 <b>10만 회</b>다." }],
        code: { c: "((now - last) >>> 0) >= interval;   // 되돌아와도 맞는다\nstate.count -= n;                   // 덮지 말고 뺀다\nconst rest = pieces.pop();          // 미완성 조각은 남긴다", cap: "며칠 뒤에 일어날 일을 지금 적어 둔다" },
        key: ["시각은 차이로 비교한다", "디바운스는 연속 표본으로", "공유 변수는 차이로 갱신한다", "값이 바뀔 때만 EEPROM 에 쓴다"] } },
  ],
};
