/* security 트랙 로그 분석 2 — 침해·오설정의 시작점을 로그에서 짚는다(방어 관점). */
module.exports = [
{
  t: "log", k: "서명을 보지 않은 토큰", cat: "logs", d: 3, track: "security",
  file: "/var/log/gateway/auth.log",
  q: "권한이 없는 계정이 관리자 API 를 호출했습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "09:00:00 INFO  gw: 인증 미들웨어 통과율 100%, 만료 검사 통과, 오류 0" },
    { txt: "09:00:02 INFO  gw: 토큰 파싱에 decode() 사용 — verify() 아님", bad: true },
    { txt: "09:00:03 INFO  gw: 헤더 alg=none 인 토큰도 파싱 성공" },
    { txt: "09:04:00 WARN  gw: role=admin 클레임을 가진 토큰이 일반 사용자 IP 에서 도착" },
    { txt: "09:04:01 WARN  api: DELETE /admin/users/8812 200" },
    { txt: "09:20:00 ERROR audit: 관리자 작업 14건이 비관리자 계정에서 수행됨" },
  ],
  ex: "토큰을 <b>읽기만 하고 검증하지 않았습니다</b>. <code>decode</code> 는 서명을 보지 않으므로 누구나 클레임을 지어내 붙일 수 있고, <code>alg=none</code> 이면 서명 자체가 없어도 통과합니다. 통과율 100% 는 안심이 아니라 <b>아무도 걸러지지 않았다</b>는 뜻이었습니다. 처방은 <code>verify</code> 로 서명과 만료를 검사하고 <b>허용 알고리즘을 명시</b>하는 것이며, 권한은 토큰의 주장이 아니라 서버가 아는 사실로 판단해야 합니다.",
},
{
  t: "log", k: "남의 번호를 넣으면 남의 것이 나온다", cat: "logs", d: 2, track: "security",
  file: "/var/log/gateway/api.log",
  q: "사용자가 다른 사람의 주문서를 볼 수 있었습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "10:00:00 INFO  api: GET /orders/48120 200 user=1041" },
    { txt: "10:00:00 INFO  api: 세션 유효, 권한 스코프 orders:read" },
    { txt: "10:00:01 INFO  api: 인증 미들웨어 통과 (유효한 세션)" },
    { txt: "10:00:02 INFO  api: 핸들러가 경로의 주문 번호로 바로 조회 — 소유자 비교 없음", bad: true },
    { txt: "10:00:10 INFO  api: GET /orders/48121 200 user=1041" },
    { txt: "10:00:11 WARN  api: 같은 세션이 1초 간격으로 연속 번호 4,000건 조회" },
    { txt: "10:40:00 ERROR audit: 타 사용자 주문 3,912건 열람 확인" },
  ],
  ex: "<b>인증</b>(누구인가)은 했지만 <b>인가</b>(그것을 볼 자격이 있는가)를 하지 않았습니다. 번호만 바꾸면 남의 자원이 나오므로 연속 번호로 훑는 것만으로 데이터가 새어 나갑니다. 처방은 조회 조건에 <b>소유자를 함께</b> 넣는 것(<code>WHERE id=? AND user_id=?</code>)이고, 예방은 순차 번호 대신 추측하기 어려운 식별자를 쓰되 <b>그것을 방어로 착각하지 않는 것</b>입니다.",
},
{
  t: "log", k: "서버가 대신 내부를 열어 준다", cat: "logs", d: 4, track: "security",
  file: "/var/log/gateway/api.log",
  q: "외부에 노출된 적 없는 내부 주소의 응답이 사용자 화면에 나타났습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "11:00:00 INFO  api: 미리보기 요청 처리 정상, 오류율 0%" },
    { txt: "11:00:00 INFO  api: 요청량 평시 수준" },
    { txt: "11:00:00 INFO  api: 사용자 인증·권한 검사 통과" },
    { txt: "11:00:00 INFO  api: 이미지 미리보기 기능 — 사용자가 준 URL 을 서버가 받아 옴" },
    { txt: "11:00:01 INFO  api: 허용 목록 없이 http/https 면 모두 요청", bad: true },
    { txt: "11:00:02 WARN  api: 요청 URL http://169.254.169.254/latest/meta-data/ 응답 200, 본문을 그대로 반환" },
    { txt: "11:00:03 WARN  api: 같은 방식으로 http://10.0.3.14:8500/v1/kv/ 조회" },
    { txt: "11:30:00 ERROR audit: 내부 서비스 목록과 자격 증명 노출 정황" },
  ],
  ex: "서버가 <b>사용자가 지정한 주소로 대신 요청</b>했습니다. 방화벽 안쪽에서 나가는 요청이라 내부 전용 주소에도 닿고, 응답을 그대로 돌려주니 그대로 유출됩니다. 처방은 <b>허용 목록</b>으로 도메인을 제한하고, 해석된 IP 가 사설·링크로컬 대역이면 거절하며(리다이렉트마다 다시 검사), 미리보기 요청은 <b>바깥으로만 나가는 프록시</b>를 통해 보내는 것입니다.",
},
{
  t: "log", k: "로그에 남은 비밀", cat: "logs", d: 2, track: "security",
  file: "/var/log/gateway/api.log",
  q: "협력사 API 키가 유출되어 교체했습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "12:00:00 INFO  api: 협력사 연동 호출 정상, 실패율 0.3%" },
    { txt: "12:00:00 INFO  api: 로그 레벨은 운영에서도 DEBUG 로 유지" },
    { txt: "12:00:01 INFO  api: 외부 호출 실패 시 요청 전체를 디버그 로그로 남김", bad: true },
    { txt: "12:00:02 INFO  api: 로그에는 Authorization 헤더가 그대로 포함" },
    { txt: "12:00:02 INFO  log: 수집기가 로그를 중앙 저장소로 전송" },
    { txt: "12:00:03 INFO  log: 저장소는 개발팀 전체에 읽기 권한" },
    { txt: "12:00:10 WARN  audit: 최근 90일 로그에서 유효한 토큰 41개 발견" },
    { txt: "12:40:00 ERROR ops: 키 전량 교체, 협력사 통보" },
  ],
  ex: "비밀이 <b>로그를 타고</b> 훨씬 넓은 곳으로 퍼졌습니다. 로그 저장소는 대개 애플리케이션보다 접근 권한이 넓고 보관 기간도 길어, 한 번 찍히면 회수할 수 없습니다. 처방은 로깅 계층에서 <code>Authorization</code>·쿠키·카드번호 같은 필드를 <b>가려서 찍는 것</b>이고, 예방은 로그에 비밀이 들어가는지 검사하는 규칙을 CI 와 수집기 양쪽에 두는 것입니다.",
},
{
  t: "log", k: "경로에 섞인 상위 디렉터리", cat: "logs", d: 3, track: "security",
  file: "/var/log/gateway/api.log",
  q: "첨부파일 다운로드 기능으로 서버 설정 파일이 유출됐습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "13:00:00 INFO  api: GET /files?name=invoice-91.pdf 200" },
    { txt: "13:00:00 INFO  api: 다운로드 권한 검사 통과" },
    { txt: "13:00:00 INFO  api: 요청자 세션 유효" },
    { txt: "13:00:01 INFO  api: 저장 경로는 /srv/uploads 로 고정" },
    { txt: "13:00:02 INFO  api: 파일명을 그대로 이어 붙여 open (정규화·검증 없음)", bad: true },
    { txt: "13:00:03 INFO  api: name=../../etc/app/secrets.yml 요청 수신" },
    { txt: "13:00:03 WARN  api: 200 응답, 본문 4.1KB" },
    { txt: "13:20:00 ERROR audit: DB 자격 증명과 서명 키 유출 확인" },
  ],
  ex: "사용자가 준 이름을 <b>그대로 경로에 이어 붙였습니다</b>. <code>..</code> 가 들어가면 정해 둔 디렉터리 밖으로 나가고, 파일 서버는 시키는 대로 읽어 줍니다. 처방은 경로를 <b>정규화한 뒤 지정 디렉터리 안인지 확인</b>하는 것이며, 더 확실한 방법은 사용자에게 파일명이 아니라 <b>내부 식별자</b>를 주고 서버가 이름을 찾아 주는 것입니다.",
},
{
  t: "log", k: "로그인해도 그대로인 세션 번호", cat: "logs", d: 4, track: "security",
  file: "/var/log/gateway/auth.log",
  q: "피싱 링크를 클릭한 사용자의 계정이 탈취됐습니다. 링크 자체는 정상 도메인이었습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "14:00:00 INFO  auth: 방문 유입 경로 메일 링크" },
    { txt: "14:00:00 INFO  auth: TLS·도메인 정상, 인증서 유효" },
    { txt: "14:00:00 INFO  auth: 방문자에게 세션 발급 sid=Z9K…(로그인 전)" },
    { txt: "14:00:01 INFO  auth: 링크의 쿼리로 전달된 sid 를 그대로 사용" },
    { txt: "14:02:00 INFO  auth: 로그인 성공 user=3391" },
    { txt: "14:02:00 INFO  auth: 로그인 뒤에도 세션 식별자를 새로 발급하지 않음", bad: true },
    { txt: "14:02:05 WARN  api: 같은 sid 가 다른 IP·다른 기기에서 사용됨" },
    { txt: "14:10:00 ERROR audit: 해당 계정에서 배송지 변경·주문 발생" },
  ],
  ex: "공격자가 <b>미리 정한 세션 번호</b>를 피해자에게 쓰게 하고, 피해자가 로그인하는 순간 그 번호가 인증된 세션이 됩니다(세션 고정). 링크가 정상 도메인인 이유가 그것입니다. 처방은 <b>로그인 성공 시 세션 식별자를 새로 발급</b>하고 옛 것을 폐기하는 것이며, 세션 번호를 URL 로 받지 않고 <b>쿠키로만</b> 다루어야 합니다.",
},
{
  t: "log", k: "같은 자리로 돌려보내지 않았다", cat: "logs", d: 2, track: "security",
  file: "/var/log/gateway/api.log",
  q: "우리 도메인 링크가 피싱 메일에 그대로 쓰였습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "15:00:00 INFO  api: 로그인 뒤 next 파라미터로 원래 페이지 복귀 지원" },
    { txt: "15:00:00 INFO  api: 로그인 자체는 정상, 계정 탈취 흔적 없음" },
    { txt: "15:00:01 INFO  api: next 값 검증은 비어 있지 않은지만 확인", bad: true },
    { txt: "15:00:02 INFO  api: 요청 /login?next=https://login-shop.example.net/" },
    { txt: "15:00:03 WARN  api: 302 Location: https://login-shop.example.net/" },
    { txt: "15:10:00 WARN  abuse: 해당 링크가 포함된 메일 12,000통 신고" },
    { txt: "15:40:00 ERROR audit: 도메인 평판 하락, 메일 차단 목록 등재" },
  ],
  ex: "돌아갈 주소를 <b>검증 없이</b> 받아 그대로 보냈습니다. 사용자는 우리 도메인을 보고 눌렀는데 최종 목적지는 공격자의 사이트이므로, 우리 신뢰가 공격에 쓰입니다. 처방은 <b>같은 출처로만</b> 이동을 허용하는 것 — 절대 URL 을 거절하고 <code>/</code> 로 시작하는 경로만 받거나, 미리 정한 목적지 목록에서 <b>키로 고르게</b> 하는 것입니다.",
},
{
  t: "log", k: "한 글자씩 알려 주는 비교", cat: "logs", d: 4, track: "security",
  file: "/var/log/gateway/auth.log",
  q: "무차별 대입을 막았는데도 API 키가 뚫렸습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "16:00:00 INFO  auth: API 키 검증(실패 시 지연 없음), 속도 제한 초당 5회 적용 중" },
    { txt: "16:00:02 INFO  auth: 키 비교에 문자열 == 사용 — 다르면 즉시 반환", bad: true },
    { txt: "16:00:10 WARN  auth: 같은 출처에서 응답 시간이 12.1µs~48.7µs 로 갈리는 요청 반복" },
    { txt: "16:40:00 WARN  auth: 시도 횟수는 제한 이내지만 접두사가 점점 길어지는 패턴" },
    { txt: "17:10:00 ERROR audit: 유효한 키로 인증 성공, 데이터 대량 조회" },
    { txt: "17:20:00 ERROR ops: 키 폐기·재발급, 접근 로그 전수 조사" },
  ],
  ex: "앞에서부터 비교하다 <b>다른 글자를 만나면 바로 멈추는</b> 비교는, 맞은 글자 수만큼 시간이 더 걸립니다. 그 미세한 차이를 재면 키를 한 글자씩 알아낼 수 있어 속도 제한 안에서도 뚫립니다. 응답 시간이 갈린다는 경고가 그 흔적입니다. 처방은 <b>길이와 무관하게 같은 시간</b>이 걸리는 비교(<code>hmac.compare_digest</code> 등)를 쓰는 것이고, 키는 해시로 저장해 원문 비교 자체를 없애는 편이 낫습니다.",
},
{
  t: "log", k: "외부 실체를 읽어 오는 파서", cat: "logs", d: 4, track: "security",
  file: "/var/log/gateway/api.log",
  q: "협력사가 올린 XML 을 처리한 뒤 서버 파일 내용이 응답에 섞여 나왔습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "17:00:00 INFO  api: 협력사 업로드 인증 통과" },
    { txt: "17:00:00 INFO  api: 파일 크기·확장자 검사 통과" },
    { txt: "17:00:00 INFO  api: 정산 XML 업로드 처리 시작" },
    { txt: "17:00:01 INFO  api: 파서 기본 설정 사용 — 외부 실체 해석 허용", bad: true },
    { txt: "17:00:02 INFO  api: 문서에 <!ENTITY xxe SYSTEM \"file:///etc/passwd\"> 포함" },
    { txt: "17:00:03 INFO  api: 파싱 성공, 검증 오류 없음" },
    { txt: "17:00:04 WARN  api: 응답 본문에 시스템 계정 목록 포함" },
    { txt: "17:30:00 ERROR audit: 내부 경로·계정 정보 유출 확인" },
  ],
  ex: "XML 파서가 <b>문서가 시키는 대로 파일을 읽어</b> 왔습니다. 외부 실체 기능은 표준이지만 신뢰할 수 없는 입력에는 그대로 취약점이 되고, 파싱은 성공하니 오류로도 잡히지 않습니다. 처방은 파서에서 <b>DTD 와 외부 실체를 끄는 것</b>이며, 가능하면 신뢰할 수 없는 입력은 XML 대신 <b>스키마로 검증되는 단순한 형식</b>으로 받는 것이 낫습니다.",
},
{
  t: "log", k: "설치 중에 실행되는 남의 코드", cat: "logs", d: 3, track: "security",
  file: "/var/log/ci/build.log",
  q: "빌드 서버에서 알 수 없는 외부 통신이 발견됐습니다. 원인 줄을 고르세요.",
  items: [
    { txt: "18:00:00 INFO  ci: 파이프라인 시작, 소스 체크아웃 정상" },
    { txt: "18:00:00 INFO  ci: 빌드 이미지 해시 어제와 동일" },
    { txt: "18:00:00 INFO  ci: 테스트 통과, 코드 변경은 문서뿐" },
    { txt: "18:00:10 INFO  ci: 의존성 설치 시작 (락파일 없음, 최신 버전 해석)", bad: true },
    { txt: "18:00:20 INFO  ci: 새 전이 의존성 2개 추가 설치" },
    { txt: "18:00:21 INFO  ci: 설치 스크립트(postinstall)가 빌드 사용자 권한으로 실행" },
    { txt: "18:00:22 WARN  net: 빌드 컨테이너에서 외부 호스트로 아웃바운드 연결, 환경변수를 읽는 프로세스 관찰" },
    { txt: "18:10:00 ERROR audit: 배포 자격 증명 유출 가능성, 키 회수" },
  ],
  ex: "버전을 고정하지 않아 <b>어제와 다른 코드</b>가 설치됐고, 설치 스크립트는 빌드 권한으로 그대로 실행됩니다. 빌드 서버에는 배포 자격 증명이 있으므로 피해가 큽니다. 처방은 <b>락파일을 커밋하고 CI 에서 그대로만 설치</b>하는 것(<code>npm ci</code> 등), 설치 스크립트 실행을 끄는 것, 그리고 빌드 컨테이너의 <b>아웃바운드를 허용 목록으로</b> 제한하는 것입니다.",
},
];
