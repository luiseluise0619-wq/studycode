module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 해시와 문자열",
  source: "./algo_hashstr.cjs",
  lessons: [
    { t: "해시 — 이미 본 것을 다시 찾지 않는다", n: 4,
      th: { sum: "해시가 하는 일은 하나다. **이미 본 것을 다시 찾지 않게** 만드는 것. 이중 반복이 보이면 '지나온 것을 기억해 두면 되지 않나' 를 먼저 묻는다.",
        body: [
          { h: "먼저 찾고 나중에 넣는다", t: "두 수의 합에서 순서가 뒤집히면 자기 자신과 짝을 짓는다. 쌍을 셀 때도 마찬가지로 **새 값을 넣기 전에** 짝을 세면 모든 쌍이 정확히 한 번씩 세어져 중복 계산을 막는 규칙이 공짜로 생긴다. 차이가 0인 경우처럼 **대칭인 특수 상황**만 따로 막아 주면 된다. `Set` 은 존재 여부, `Map` 은 개수·위치 — 무엇을 물었는지가 자료 구조를 정한다." },
          { h: "한 번에 다 하려 하지 않는다", t: "'한 번만 나온 첫 글자' 는 한 번의 훑기로 판정할 수 없다. 뒤에 또 나오는지를 아직 모르기 때문이다. **세는 훑기와 찾는 훑기를 나누면** 두 번 돌아도 여전히 O(n) 이고 코드가 훨씬 읽기 쉽다. 반대로 연속 수열 문제는 `if (pool.has(x-1)) return;` **한 줄**이 O(n²) 를 O(n) 으로 바꾼다 — 맞는 답과 빠른 답이 한 줄 차이인 자리는 그 근거를 말할 수 있어야 한다." }],
        code: { c: "if (seen.has(need)) return [seen.get(need), i];\nseen.set(xs[i], i);   // 찾은 다음에 넣는다", cap: "순서가 뒤집히면 자기 자신과 짝을 짓는다" },
        key: ["이중 반복 → 지나온 것을 기억", "먼저 찾고 나중에 넣는다", "존재는 Set · 개수는 Map", "두 번 훑어도 O(n)"] } },
    { t: "키를 설계한다 — 어려운 건 코드가 아니다", n: 4,
      th: { sum: "묶기·매칭 문제의 전부는 **무엇을 같은 것으로 볼지**다. 키가 정해지면 코드는 기계적으로 나온다.",
        body: [
          { h: "같음의 정의가 곧 키다", t: "애너그램은 '글자 구성이 같다' 이므로 **정렬한 글자열**이 키다. 알파벳뿐이라면 26칸 개수 배열이 더 싸지만, 배열은 그대로 키가 못 되어 문자열로 이어야 하고 **구분자 없이 이으면** `[1,11]` 과 `[11,1]` 이 같은 키가 된다. 실무의 중복 파일 찾기(내용 해시), 동일 요청 묶기(정규화한 URL), 사용자 병합(정규화한 이메일)이 전부 같은 골격이다." },
          { h: "매핑은 양방향을 봐야 한다", t: "글자 대응 판정에서 한 방향만 확인하면 **서로 다른 두 글자가 한 글자로 뭉치는 것**을 못 잡는다. 일대일이라는 말은 곧 양방향 유일성이라, 맵을 둘 두거나 값의 가짓수를 비교해야 한다. 같은 함정이 외부 스키마를 내부 컬럼에 매핑할 때 나온다 — 되돌리는 마이그레이션을 짤 때가 되어서야 데이터가 뭉개진 것을 발견한다. 집계에서는 **동점 규칙**을 먼저 정하는 것이 같은 급의 문제다." }],
        code: { c: "const key = w.split(\"\").sort().join(\"\");\nbucket.get(key).push(w);", cap: "키가 정해지면 나머지는 기계적" },
        key: ["같음의 정의 = 키", "배열을 키로 쓸 땐 구분자", "매핑은 역방향도 막는다", "동점 규칙을 먼저 정한다"] } },
    { t: "문자열 — 직접 훑고 직접 만든다", n: 4,
      th: { sum: "문자열 문제는 화려한 알고리즘보다 **경계 조건**에서 갈린다. 마지막 묶음, 맨 앞 글자, 빈 입력, 두 자리 숫자.",
        body: [
          { h: "구간의 시작과 끝을 손에 쥔다", t: "'같은 것이 이어지는 구간' 은 `i` 를 시작, `j` 를 구간 밖 첫 자리로 두고 처리한 뒤 `i = j` 로 건너뛴다. 한 글자씩 이전과 비교하는 방식보다 **마지막 묶음을 흘리는 실수**가 적다. 부분 문자열 탐색도 같은 손놀림이다 — 시작 자리를 옮기며 끝까지 맞는지 본다. 최악은 O(n·m) 이지만 실제 문자열은 몇 글자에서 어긋나 대개 충분히 빠르고, KMP 는 **먼저 재고 나서** 꺼낸다." },
          { h: "중첩이 보이면 스택이다", t: "`3[a2[c]]` 처럼 접힌 구조는 **여는 기호에서 지금 상태를 밀어 넣고 닫는 기호에서 꺼내** 이어 붙인다. 숫자는 `num*10 + 자릿수` 로 쌓아야 두 자리가 갈리지 않고, 꺼낸 뒤 이어 붙이는 **순서**를 뒤집으면 바깥 글자가 뒤로 간다. 같은 골격이 수식 계산기·JSON 파서·템플릿 전개에 그대로 들어간다. 집계에 앞선 **정규화**(대소문자·구두점)도 문자열 작업의 절반이다." }],
        code: { c: "else if (ch === \"]\") cur = strs.pop() + cur.repeat(nums.pop());", cap: "순서를 뒤집으면 바깥 글자가 뒤로 간다" },
        key: ["구간은 i·j 로 잡고 건너뛴다", "마지막 묶음을 잊지 않는다", "중첩 → 스택", "두 자리 수를 반드시 테스트"] } },
  ],
};
