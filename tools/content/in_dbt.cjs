/* 데이터베이스 이론 단답 15 — 트랜잭션·인덱스·격리 수준을 낱말로 적어 본다.
   검증:  node ver_input.cjs ./in_dbt.cjs */
module.exports = [

{ track:"dbt", t:"input", cat:"internals", k:"전부 되거나 전부 안 되거나",
  q:"트랜잭션의 네 성질 가운데 '중간에 멈추면 아무것도 안 한 것으로 되돌린다' 를 뜻하는 것은? (세 글자)",
  a:["원자성", "atomicity", "atomic"],
  ex:"계좌에서 빼기만 하고 넣기 전에 죽는 일을 막습니다 — 되돌리기(rollback)가 이 성질을 실현하는 수단입니다.\n💡 개념: 실패를 <b>중간 상태로 남기지 않는다</b>는 약속이라, 부분 성공을 처리하는 코드를 아예 안 써도 됩니다.\n🛠 실무: 여러 서비스에 걸친 작업에는 이 성질이 공짜로 오지 않습니다 — 보상 트랜잭션(사가)이나 아웃박스로 직접 만들어야 합니다." },

{ track:"dbt", t:"input", cat:"internals", k:"동시에 해도 따로 한 것처럼",
  q:"트랜잭션의 네 성질 가운데 '여럿이 동시에 돌아도 하나씩 차례로 한 것과 같은 결과' 를 뜻하는 것은? (세 글자)",
  a:["격리성", "isolation", "고립성"],
  ex:"완전히 지키면 느려지므로, 실제 DB 는 <b>수준을 골라</b> 성능과 맞바꿉니다.\n💡 개념: 수준을 낮출수록 더티 리드·반복 불가능 읽기·팬텀 리드 같은 <b>이상 현상</b>이 하나씩 허용됩니다.\n🛠 실무: 어떤 수준을 쓰는지 모르고 짠 코드가 가장 위험합니다 — 잔액 검사 후 차감처럼 <b>읽고 쓰는 사이</b>가 있는 로직은 수준을 먼저 확인하세요." },

{ track:"dbt", t:"input", cat:"internals", k:"커밋한 것은 사라지지 않는다",
  q:"트랜잭션의 네 성질 가운데 '커밋했다고 답했으면 전원이 나가도 남아 있다' 를 뜻하는 것은? (세 글자)",
  a:["지속성", "내구성", "durability"],
  ex:"디스크에 <b>먼저 기록</b>을 남기고 나서 커밋을 알리기 때문에 보장됩니다.\n💡 개념: 이 기록을 미리 쓰는 방식이 WAL 이고, 데이터 파일은 나중에 천천히 따라갑니다.\n🛠 실무: 복제본에 아직 안 갔는데 커밋을 알리면 장애 시 이 성질이 깨집니다 — 동기 복제 여부가 곧 이 약속의 범위입니다." },

{ track:"dbt", t:"input", cat:"internals", k:"이상 현상을 전부 막는 수준",
  q:"표준 격리 수준 가운데 팬텀 리드까지 막는 <b>가장 엄격한</b> 수준의 이름은? (영문)",
  a:["SERIALIZABLE", "직렬화가능", "직렬화"],
  ex:"결과가 '<b>어떤 순서로든 하나씩 실행한 것</b>' 과 같아야 합니다. 가장 안전하지만 충돌이 잦아지고 재시도가 늘어납니다.\n💡 개념: 구현은 두 갈래입니다 — 락으로 막거나(2PL), 충돌을 감지해 한쪽을 되돌리거나(SSI).\n🛠 실무: 이 수준을 쓰면 <b>트랜잭션이 실패할 수 있다</b>는 전제로 짜야 합니다. 재시도 코드를 함께 넣지 않으면 오히려 장애가 늘어납니다." },

{ track:"dbt", t:"input", cat:"internals", k:"InnoDB 의 기본 수준",
  q:"MySQL InnoDB 의 기본 격리 수준은 무엇일까요? (영문 두 낱말)",
  a:["REPEATABLE READ", "반복읽기", "반복가능읽기"],
  ex:"한 트랜잭션 안에서 같은 행을 다시 읽으면 <b>처음과 같은 값</b>이 보입니다.\n💡 개념: InnoDB 는 갭 락 덕분에 이 수준에서도 팬텀 리드가 대체로 막히지만, 표준이 요구하는 것은 아닙니다.\n🛠 실무: DB 를 옮길 때 이 기본값 차이가 조용한 버그를 만듭니다 — 애플리케이션이 어느 수준을 전제하는지 코드나 문서에 적어 두세요." },

{ track:"dbt", t:"input", cat:"internals", k:"PostgreSQL 의 기본 수준",
  q:"PostgreSQL 의 기본 격리 수준은 무엇일까요? (영문 두 낱말)",
  a:["READ COMMITTED", "커밋된읽기", "커밋읽기"],
  ex:"커밋된 값만 보이지만, <b>구문마다</b> 스냅숏을 새로 뜨므로 같은 트랜잭션 안에서도 값이 달라질 수 있습니다.\n💡 개념: 그래서 '읽고 판단한 뒤 쓴다' 는 로직은 이 수준에서 안전하지 않습니다 — <code>SELECT … FOR UPDATE</code> 로 잠그거나 조건부 갱신으로 바꿔야 합니다.\n🛠 실무: 재고 차감·중복 가입 검사가 대표적인 자리입니다. 유니크 제약을 함께 걸어 DB 가 마지막으로 막게 하세요." },

{ track:"dbt", t:"input", cat:"internals", k:"인덱스를 떠받치는 자료구조",
  q:"관계형 DB 의 기본 인덱스가 쓰는 자료구조의 이름은? (영문, 하이픈 포함)",
  a:["B-tree", "btree", "B+tree", "비트리"],
  ex:"값이 <b>정렬된 채</b> 유지되므로 같음 검색뿐 아니라 <b>범위 조회와 정렬</b>까지 인덱스만으로 처리합니다.\n💡 개념: 해시 인덱스는 같음 검색만 되고 순서를 주지 못합니다 — 그 차이가 곧 쓰임의 차이입니다.\n🛠 실무: 디스크 블록 하나에 자식을 수백 개 담아 높이가 3~4 로 낮습니다. 그래서 수억 행에서도 몇 번의 읽기로 끝납니다." },

{ track:"dbt", t:"input", cat:"perf", k:"인덱스만 읽고 끝내는 조회",
  q:"조회에 필요한 컬럼이 인덱스 안에 다 들어 있어 원본을 더 읽지 않아도 되는 인덱스를 무엇이라 부를까요? (네 글자 + 인덱스)",
  a:["커버링인덱스", "covering index", "커버링"],
  ex:"인덱스에서 값을 찾은 뒤 <b>본체 행을 다시 읽는 일</b>(랜덤 I/O)이 사라져 훨씬 빠릅니다.\n💡 개념: 그래서 <code>SELECT *</code> 대신 <b>필요한 컬럼만</b> 고르는 것이 성능에 직접 영향을 줍니다.\n🛠 실무: 자주 쓰는 조회의 조건 컬럼과 선택 컬럼을 한 인덱스에 담으면 극적으로 빨라지지만, 인덱스가 커져 쓰기 비용이 올라갑니다 — 조회·갱신 비율을 보고 정하세요." },

{ track:"dbt", t:"input", cat:"perf", k:"두 번째 컬럼만으로 찾으면",
  q:"<code>(a, b)</code> 순서로 만든 복합 인덱스를 <code>b</code> 조건만으로 조회하면 그 인덱스를 효율적으로 쓸 수 있을까요? (예 또는 아니오)",
  a:["아니오", "아니요", "no", "불가"],
  ex:"인덱스는 <b>a 로 먼저 정렬</b>돼 있어서, a 를 모르면 b 값들이 흩어져 있습니다 — 전화번호부를 이름 없이 '전화번호로' 찾는 것과 같습니다.\n💡 개념: 이를 <b>왼쪽 접두사 규칙</b>이라 하고, <code>(a)</code>·<code>(a, b)</code> 조건에는 쓰이지만 <code>(b)</code> 만으로는 못 씁니다.\n🛠 실무: 컬럼 순서를 정할 때는 <b>같음 조건에 자주 쓰이는 컬럼</b>을 앞에, 범위 조건 컬럼을 뒤에 두세요." },

{ track:"dbt", t:"input", cat:"perf", k:"목록 한 번, 상세 여러 번",
  q:"목록을 한 번 조회한 뒤 각 행마다 연관 데이터를 다시 조회해 쿼리가 폭증하는 문제를 무엇이라 부를까요? (기호와 숫자, 예: 2+3 문제)",
  a:["N+1", "N+1문제", "n+1 problem"],
  ex:"목록 1 번 + 행마다 1 번씩 N 번이라 이름이 그렇습니다. 개발용 데이터 열 건에서는 안 보이고 <b>운영에서 터집니다</b>.\n💡 개념: 해법은 셋입니다 — 조인으로 한 번에 가져오기, <code>IN</code> 절로 모아 두 번에 가져오기(배치 로딩), 아예 비정규화해 두기.\n🛠 실무: ORM 의 지연 로딩이 기본값이라 무심코 쓰면 바로 이 모양이 됩니다. 쿼리 개수를 로그로 세는 검사를 테스트에 넣으세요." },

{ track:"dbt", t:"input", cat:"design", k:"정규화가 없애려는 것",
  q:"정규화의 가장 큰 목적은 같은 사실이 여러 곳에 적히는 것, 곧 무엇을 없애는 것일까요? (두 글자)",
  a:["중복", "중복저장", "redundancy"],
  ex:"같은 사실이 두 곳에 있으면 <b>한쪽만 고쳐지는 순간</b> 어느 쪽이 맞는지 알 수 없게 됩니다(갱신 이상).\n💡 개념: 3NF 까지는 '모든 컬럼이 <b>기본키에만</b> 의존한다' 로 요약됩니다.\n🛠 실무: 읽기가 압도적으로 많으면 일부러 되돌리기도 합니다 — 다만 그때는 <b>어디가 원본인지</b>와 <b>언제 다시 맞추는지</b>를 반드시 정해 두어야 합니다." },

{ track:"dbt", t:"input", cat:"perf", k:"처음부터 끝까지 읽기",
  q:"실행 계획에서 인덱스를 쓰지 않고 테이블을 처음부터 끝까지 읽는 방식을 무엇이라 부를까요? (네 글자, 한글)",
  a:["풀스캔", "전체스캔", "full scan", "seq scan"],
  ex:"언제나 나쁜 것은 아닙니다 — 테이블의 <b>큰 부분을 어차피 읽어야 하면</b> 인덱스를 거치는 것보다 빠릅니다.\n💡 개념: 옵티마이저는 통계를 보고 고릅니다. 통계가 낡으면 잘못 고르므로 갱신이 중요합니다.\n🛠 실무: 인덱스가 있는데도 이 방식이 나온다면 컬럼에 함수를 씌웠거나, 타입이 안 맞아 <b>암묵적 형변환</b>이 일어났거나, 조건이 너무 넓은 것입니다." },

{ track:"dbt", t:"input", cat:"internals", k:"서로 기다리려면 몇이 필요한가",
  q:"교착 상태(데드락)가 생기려면 최소 몇 개의 트랜잭션이 필요할까요? (숫자)",
  a:["2", "2개", "둘"],
  ex:"A 가 가진 것을 B 가 기다리고, B 가 가진 것을 A 가 기다리면 영원히 풀리지 않습니다.\n💡 개념: 필요한 조건은 넷입니다 — 상호 배제, 점유하며 대기, 비선점, <b>순환 대기</b>. 하나만 깨도 안 생깁니다.\n🛠 실무: 가장 쉬운 예방은 <b>자원을 언제나 같은 순서로</b> 잠그는 것입니다. DB 가 자동으로 한쪽을 되돌리므로, 애플리케이션에는 재시도가 있어야 합니다." },

{ track:"dbt", t:"input", cat:"perf", k:"열로 눕혀 담는 저장",
  q:"같은 컬럼의 값들을 모아서 저장해 대량 집계에 유리한 저장 방식을 무엇이라 부를까요? (다섯 글자, 한글)",
  a:["열지향", "컬럼지향", "columnar", "열기반"],
  ex:"한 컬럼만 읽으면 되니 <b>읽는 양이 확 줄고</b>, 같은 종류의 값이 이어져 있어 압축률도 높습니다.\n💡 개념: 반대로 한 행 전체를 자주 읽고 쓰는 업무 처리(OLTP)에는 행 지향이 유리합니다 — 쓰임이 저장 방식을 정합니다.\n🛠 실무: 분석용 창고와 운영 DB 를 나누는 이유가 여기 있습니다. 같은 데이터라도 <b>질문의 모양</b>이 다르면 담는 방식도 달라야 합니다." },

{ track:"dbt", t:"input", cat:"internals", k:"먼저 적고 나중에 반영한다",
  q:"데이터 파일을 고치기 전에 변경 내용을 기록 파일에 먼저 남겨 두는 기법을 영문 약어 세 글자로 적으세요. (영문 약어)",
  a:["WAL", "미리쓰기로그", "write ahead log"],
  ex:"Write-Ahead Log 입니다. 순차 쓰기라 빠르고, 갑자기 죽어도 이 기록을 다시 재생해 <b>일관된 상태로 복구</b>합니다.\n💡 개념: 커밋은 이 기록이 디스크에 안전히 닿은 시점이고, 데이터 파일은 나중에 천천히 따라갑니다.\n🛠 실무: 복제도 대개 이 기록을 흘려보내 만듭니다 — 복제 지연을 볼 때 보는 수치가 바로 이 기록의 위치 차이입니다." },

];
