module.exports = {
  track: "backend", guide: "", xp: 80,
  unit: "실행형 실전 — 돌아가는 것처럼 보이는 고장난 코드",
  source: "./dbg_be.cjs",
  lessons: [
    { t: "경계와 규약을 잘못 읽은 자리", n: 6,
      th: { sum: "운영에서 터지는 버그는 대개 **작은 입력에서는 통과하는 코드**다 — 경계 한 칸, 규약 한 줄이 어긋나 있다.",
        body: [
          { h: "1부터 세는 값과 0부터 세는 값", t: "페이지 번호·행 번호·사람이 읽는 순번은 **1부터** 세고, 배열 자리와 `slice` 는 **0부터** 센다. 한 함수 안에서 둘이 섞이면 반드시 한 칸이 어긋나며, 그 어긋남은 <b>첫 페이지가 통째로 사라지는</b> 식으로 드러난다. 경계 테스트는 늘 세 개다 — 첫 칸, 마지막 칸, 범위 밖." },
          { h: "규약은 '보통' 이 아니라 '언제나' 를 적어 둔다", t: "HTTP 헤더 이름은 대소문자를 가리지 않고, `If-None-Match` 는 목록이며 `W/` 가 붙을 수 있고, 질의 문자열의 값에는 `&`·`=` 가 들어올 수 있다. 규약 문서에 적힌 이 세 줄을 안 읽으면 **개발 서버에서는 전부 통과**한다. 프록시·브라우저·다른 클라이언트가 끼는 순간 드러난다." }],
        code: { c: "const from = (page - 1) * size;   // 1부터 세는 값을 0부터로\nreturn items.slice(from, from + size);", cap: "page * size 는 첫 묶음을 건너뛴다" },
        key: ["1-based 와 0-based 를 한 자리에서 바꾼다", "헤더 이름은 소문자로 정규화", "커서는 다음 쪽이 있을 때만 준다", "값에 구분자가 들어갈 수 있는지 먼저 묻는다"] } },
    { t: "숫자·문자열·공유 상태가 새는 자리", n: 6,
      th: { sum: "합계가 안 맞고, 첫 자리만 바뀌고, 두 번째 호출부터 결과가 달라진다 — **셋 다 재현이 어렵다는 공통점**이 있다.",
        body: [
          { h: "나눗셈과 반올림은 합계를 깨뜨린다", t: "각자에게 반올림한 같은 값을 주면 합계가 원금과 달라진다. 몫을 **내림**으로 잡고 나머지를 앞에서부터 1씩 나눠 주면 합계 보존이 계산으로 보장된다. 돈은 언제나 **정수 최소 단위**로 다룬다 — 부동소수점으로 두면 `0.1 + 0.2 !== 0.3` 이 그대로 회계 오차가 된다." },
          { h: "제자리 수정은 다음 요청으로 샌다", t: "`sort` 와 `Object.assign(base, over)` 는 **인자를 직접 고친다**. 모듈에서 한 번 만들어 공유하는 기본 설정이나 캐시된 목록이라면, 첫 요청의 흔적이 이후 모든 요청에 남는다. 반환값만 검사하는 테스트로는 절대 잡히지 않으므로 '**원본이 그대로인가**' 를 따로 못 박아야 한다." }],
        code: { c: "const base = Math.floor(total / n);\nconst rest = total - base * n;   // 합계는 언제나 total", cap: "반올림 대신 내림 + 나머지 분배" },
        key: ["돈은 정수 최소 단위로", "합계 보존은 성질 테스트로", "sort 전에 복사한다", "동점에는 전순서를 정하는 기준을 더한다"] } },
  ],
};
