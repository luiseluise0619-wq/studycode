module.exports = {
  track: "algo", guide: "", xp: 80,
  unit: "직접 구현 — 동적 계획법",
  source: "./algo_dp.cjs",
  lessons: [
    { t: "1차원 DP — 마지막 한 걸음을 묻는다", n: 4,
      th: { sum: "DP 는 **마지막 한 걸음이 무엇이었나**를 묻는 데서 시작한다. 그 한 문장이 점화식이 되고, 코드는 그것을 옮긴 것에 불과하다.",
        body: [
          { h: "초기값은 감이 아니라 역산이다", t: "`f(0) = 1` 이 이상해 보여도, `f(2) = f(1) + f(0) = 2` 가 성립하려면 반드시 그 값이어야 한다. **초기값은 점화식이 성립하도록 거꾸로 정한다.** 최대 부분 합에서 초기값을 0으로 두면 값이 전부 음수일 때 **있지도 않은 빈 구간**이 답이 되는데, '구간은 비어 있을 수 없다' 는 조건이 곧 첫 원소로 초기화하라는 지시다. 자명한 경우를 먼저 정의하지 않으면 나중에 코드에 맞춰 정의를 끼워 맞추게 된다." },
          { h: "그리디가 왜 틀리는지를 말할 수 있어야 한다", t: "동전 `[1,3,4]` 로 6을 만들 때 큰 것부터 쓰면 3개, 정답은 `3+3` 으로 2개다. 우리 화폐가 우연히 그리디로 풀릴 뿐이다. 이웃을 못 고르는 최대 합에서도 큰 값부터 잡으면 양 끝을 놓친다. **지금의 최선이 나중을 막는** 구조면 그리디가 아니라 DP다. 상태를 둘 이상 굴릴 때는 **동시에 바꾸는 것처럼** 임시 변수를 써야 한다 — 먼저 갱신한 값이 뒤 계산에 새어 들어가면 제약이 깨진다." }],
        code: { c: "const nextTake = skip + x;\nskip = Math.max(skip, take);\ntake = nextTake;      // 동시에 바꾼 셈", cap: "순서를 지키지 않으면 이웃을 함께 고른다" },
        key: ["마지막 한 걸음을 묻는다", "초기값은 점화식에서 역산", "몇 칸 뒤까지 보는지 세면 메모리가 준다", "그리디 반례를 만들어 본다"] } },
    { t: "표를 채우는 DP — 정의를 한 문장으로", n: 4,
      th: { sum: "2차원 DP 는 **`표[i][j]` 가 무엇인지**를 한 문장으로 쓰는 순간 끝난다. 그 문장을 못 쓰면 인덱스가 계속 어긋난다.",
        body: [
          { h: "한 칸 늘려서 경계 분기를 없앤다", t: "표를 `(m+1) × (n+1)` 로 두면 0번째 행·열이 '한쪽이 빈 문자열' 을 뜻해 답이 저절로 정해지고, 첫 행·첫 열을 따로 채우는 코드가 사라진다. 대신 문자열을 볼 때 **`a[i-1]` 처럼 한 칸 어긋난 인덱스**를 써야 하고 실수가 거기 몰린다. 최소 비용 경로에서 초기값을 `Infinity` 로 두는 것도 같은 수법이다 — **불가능을 큰 값으로 표현**하면 `min` 이 알아서 유효한 쪽을 고른다." },
          { h: "골격은 같고 합치는 연산만 바뀐다", t: "경로 수는 더하기, 최소 비용은 `Math.min`, 공통 부분수열은 '같으면 대각선 +1, 다르면 위·왼쪽 중 큰 쪽' 이다. 편집 거리는 세 이웃이 **교체·삭제·삽입**에 하나씩 대응한다 — 한 번 그려 보면 외울 것이 없어진다. 한 줄만 굴리면 메모리가 줄지만, **실제 답을 복원**하려면 표 전체가 필요하다. `diff` 도구가 그 표를 그대로 쓴다." }],
        code: { c: "if (a[i-1] === b[j-1]) cur[j] = prev[j-1] + 1;\nelse cur[j] = Math.max(prev[j], cur[j-1]);", cap: "같으면 둘 다 쓰고, 다르면 하나를 포기" },
        key: ["표의 정의를 한 문장으로", "한 칸 늘리면 분기가 사라진다", "골격은 같고 연산만 다르다", "복원이 필요하면 표를 남긴다"] } },
    { t: "선택의 DP — 반복 방향이 정의를 바꾼다", n: 4,
      th: { sum: "배낭 계열에서는 **안쪽 반복을 어느 방향으로 도느냐**가 '한 번만 쓴다' 와 '여러 번 쓴다' 를 가른다. 코드 한 줄이 문제의 정의다.",
        body: [
          { h: "거꾸로 돌면 한 번, 앞에서 돌면 여러 번", t: "`c` 를 작은 쪽부터 돌면 `best[c-w]` 가 **이번 물건을 이미 쓴 값**이라 같은 물건이 여러 번 담긴다. 거꾸로 돌면 아직 이전 물건까지의 값이라 0-1 배낭이 된다. 절반으로 나누기도 같은 골격에 값 대신 **가능/불가능만** 담은 것이다. 문제를 '두 묶음' 이 아니라 '**절반을 만들 수 있는가**' 로 바꿔 쓰는 것이 풀이의 절반이었다는 점도 함께 본다." },
          { h: "유사 다항과 제약 확인", t: "배낭의 시간은 `물건 수 × cap` 인데, `cap` 은 값의 크기라 **입력 길이의 다항식이 아니다**. `cap` 이 10억이면 쓸 수 없다는 뜻이므로 제약을 먼저 본다. 최장 증가 부분수열은 O(n²) DP 와 O(n log n) 이분 탐색 두 가지가 있고, 후자의 `tails` 는 **실제 수열이 아니라 길이만 정확**하다 — 복원하려면 따로 기록해야 한다. 데이터가 작으면 이해하기 쉬운 쪽이 더 나은 코드다." }],
        code: { c: "for (let c = cap; c >= w; c--)   // 거꾸로 = 한 번만\n  best[c] = Math.max(best[c], best[c-w] + value);", cap: "방향 하나가 문제를 바꾼다" },
        key: ["거꾸로 = 0-1, 앞으로 = 무제한", "문제를 바꿔 쓰면 쉬워진다", "cap 은 유사 다항 — 제약 확인", "길이만 맞는 표와 복원 가능한 표를 구분"] } },
  ],
};
