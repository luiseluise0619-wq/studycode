/* cpp 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "cpp", fixes: [
  { k: "빈칸", ex0: "이 필요하다", ex: "cout 과 cin 은 &lt;iostream&gt; 헤더에 선언되어 있으므로 #include &lt;iostream&gt; 이 필요하다. C 의 &lt;stdio.h&gt; 는 printf 용이라 cout 을 알지 못하고, &lt;string&gt; 은 std::string 의 헤더다. 빠뜨리면 'cout 이 선언되지 않았다' 는 오류가 난다." },
  { k: "연산자", ex0: "cout << 값 (스트림 삽입 연산자)", ex: "cout 뒤에 값을 붙일 때는 스트림 삽입 연산자 << 를 쓴다. 값이 출력 스트림으로 흘러 들어가는 방향이다. >> 는 반대로 cin 에서 변수로 값을 꺼내는 추출 연산자이고, :: 는 std::cout 처럼 이름 공간을 가리키며, -> 는 포인터로 멤버에 접근하는 기호다." },
  { k: "개념", ex0: "std::cout << 로 출력", ex: "C++ 의 표준 출력은 std::cout << 값 이다. printf 도 C 호환으로 쓸 수는 있지만 C++ 의 방식은 아니고, print 와 echo 는 Python 과 PHP 의 것이라 컴파일되지 않는다. cout 은 타입에 맞춰 알아서 찍어 주므로 %d 같은 지정자가 필요 없다." },
  { k: "입력", ex0: "cin >> 변수 로 입력받는다", ex: "키보드 입력은 cin >> 변수 로 받는다. 추출 연산자 >> 가 입력 스트림에서 값을 꺼내 변수에 넣는다. cout << 는 출력이고, input() 은 Python, scan() 은 어느 언어의 표준도 아니다(C 는 scanf 다). 공백 단위로 끊어 읽으므로 한 줄 전체는 getline 을 쓴다." },
  { k: "줄바꿈", ex0: "std::endl 또는 \\n 을 쓴다", ex: "줄바꿈은 std::endl 이나 \\n 으로 한다. endl 은 줄을 바꾸고 버퍼까지 비우므로 반복문 안에서는 \\n 이 더 빠르다. break 는 반복문을 빠져나가는 키워드이고, \\r 은 커서를 줄 앞으로 되돌릴 뿐이며, newline 이라는 이름의 조작자는 없다." },
  { k: "불리언", ex0: "bool 은 true / false 를 담는다", ex: "참과 거짓을 담는 타입은 bool 이고 값은 true 와 false 다. bit 와 binary, flag 는 C++ 타입 이름이 아니다. 정수와 섞으면 true 는 1, false 는 0 으로 바뀌고, cout 으로 찍으면 기본적으로 1 과 0 이 나온다(boolalpha 를 켜면 true/false 로 찍힌다)." },
  { k: "빈칸", ex0: "int n = 5; 형태", ex: "정수 변수는 int n = 5; 로 선언한다. C++ 은 정적 타입 언어라 타입 없이 n = 5; 만 쓰면 선언되지 않은 이름이라는 오류가 난다. auto n = 5; 도 int 로 추론되어 같은 결과이지만 문제는 정수 타입 이름을 묻고 있으므로 int 다." },
  { k: "개념", ex0: "auto x = 3; 이면 int 로 추론", ex: "auto 는 초기값을 보고 타입을 컴파일러가 추론하게 한다. auto x = 3; 은 int, auto y = 3.0; 은 double 이 된다. var 와 let 은 C#/JavaScript 의 키워드이고, type 은 C++ 에서 그런 용도로 쓰이지 않는다. 반복자처럼 타입 이름이 긴 곳에서 특히 유용하다." },
  { k: "문자열", ex0: "의 std::string 을 쓴다", ex: "C++ 표준 문자열 타입은 &lt;string&gt; 헤더의 std::string 이다. 길이를 알고 + 로 이어붙일 수 있다. char* 는 C 식 문자 배열 포인터라 길이와 메모리를 직접 관리해야 하고, 대문자 String 은 Java 의 이름이며, text 라는 타입은 없다." },
  { k: "개념", ex0: "C 와 동일하게 if / else 를 쓴다", ex: "조건 분기는 C 와 똑같이 if 와 else 다. when 은 Kotlin, cond 는 Lisp 의 것이고, test 는 셸 명령이지 C++ 키워드가 아니다. C++17 부터는 if (auto it = m.find(k); it != m.end()) 처럼 초기화 문을 함께 쓸 수도 있다." },
  { k: "범위 기반", ex0: "for (auto x : arr) 처럼 쓴다", ex: "C++11 의 범위 기반 for 는 for (auto x : arr) 처럼 콜론을 쓴다. for x in arr 은 Python, for (x of arr) 는 JavaScript, foreach 는 C# 과 PHP 의 문법이라 C++ 에서는 오류다. 원소를 복사하지 않고 바꾸려면 auto& x 로 받는다." },
  { k: "반복", ex0: "for(int i=0;i<n;i++) 형태", ex: "정해진 횟수만큼 도는 반복문은 for 다. for (int i = 0; i < n; i++) 처럼 시작, 조건, 증감을 한 줄에 둔다. repeat, loop, each 는 C++ 키워드가 아니다(loop 는 Rust, repeat 는 Swift 의 것이다). 횟수가 정해지지 않았으면 while 을 쓴다." },
  { k: "비교", ex0: "!= 는 같지 않음", ex: "'같지 않다' 는 != 다. =/= 와 not= 는 C++ 에 없는 표기이고 <> 는 SQL 이나 BASIC 의 것이다. C++ 에는 대체 토큰 not_eq 도 있지만 거의 쓰지 않는다. == 로 같음을, != 로 다름을 비교하며 대입 = 와 혼동하지 않아야 한다." },
  { k: "추가", ex0: "push_back() 으로 뒤에 추가", ex: "vector 끝에 값을 붙이는 함수는 push_back() 이다. append() 는 Python 리스트나 std::string 의 것이고, add() 는 Java 컬렉션의 것이며, insert_end() 는 존재하지 않는다. insert() 는 있지만 위치를 반복자로 줘야 해서 끝에 넣는 용도로는 push_back 이 간단하다." },
  { k: "빈칸", ex0: "v; 형태다", ex: "동적 배열은 std::vector&lt;int&gt; v; 처럼 꺾쇠 안에 원소 타입을 적어 선언한다. 꺾쇠를 빼면 어떤 타입을 담을지 몰라 오류다. array 는 크기가 고정된 std::array 이고, list 는 양방향 연결 리스트라 인덱스 접근이 안 된다. &lt;vector&gt; 헤더가 필요하다." },
  { k: "개념", ex0: "std::vector 는 동적 배열", ex: "크기가 자동으로 늘어나는 배열형 컨테이너는 vector 다. push_back 할 때마다 필요하면 메모리를 더 잡는다. array 는 컴파일 때 크기가 고정되고, list 는 연결 리스트라 v[i] 접근이 안 되며, set 은 정렬된 유일값 집합이라 순서 있는 배열이 아니다." },
  { k: "크기", ex0: "v.size() 로 개수를 얻는다", ex: "vector 의 원소 개수는 v.size() 다. length() 는 std::string 에는 있지만 vector 에는 없고, len() 은 Python 함수이며, count() 는 알고리즘 std::count 나 map 의 키 조회 함수라 용도가 다르다. size() 는 부호 없는 정수(size_t)를 돌려준다는 점도 기억해 두자." },
  { k: "개념", ex0: "함수는 입력을 받아 결과를 돌려준다", ex: "기능을 한 덩어리로 묶어 이름을 붙이고 여러 곳에서 다시 쓰는 단위는 함수다. 입력(매개변수)을 받아 결과를 돌려준다. 반복과 조건은 흐름을 제어하는 문장이고, 변수는 값을 담는 자리라 재사용 단위가 아니다. 클래스는 함수와 데이터를 더 크게 묶은 것이다." },
  { k: "객체지향", ex0: "클래스로 만든 실체가 객체", ex: "클래스라는 설계도로 실제로 만들어 낸 하나하나가 객체(인스턴스)다. Cat c1, c2; 라고 하면 객체가 두 개다. 변수는 값을 담는 이름, 함수는 동작의 묶음이고, 포인터는 주소를 담는 변수라 객체 그 자체가 아니다." },
  { k: "클래스", ex0: "class 로 객체의 틀을 정의", ex: "데이터(멤버 변수)와 기능(멤버 함수)을 하나로 묶는 설계도는 class 다. struct 도 C++ 에서는 거의 같지만 기본 접근이 public 이라 관례상 단순 데이터 묶음에 쓴다. object 는 클래스로 만든 실체이고, module 은 C++20 의 코드 단위라 설계도가 아니다." },
  { k: "참조 수정", ex0: "r은 a의 별명이므로 r을 바꾸면 a도 함께 20", ex: "int& r = a; 로 만든 참조 r 은 a 의 또 다른 이름이다. r = 20 은 a 자체를 20 으로 바꾸므로 a 를 찍으면 20 이다. 복사본이었다면 10 이 남았을 것이다. 참조 선언은 문법상 정상이라 컴파일 오류가 아니고, 0 이 될 이유도 없다." },
  { k: "결과값", ex0: "y는 x의 별명이므로 y=y+3 을 하면 x도 8", ex: "int& y = x; 는 y 를 x 의 별명으로 만든다. x 가 5 일 때 y = y + 3 을 하면 같은 메모리가 8 로 바뀌므로 x 를 출력해도 8 이다. 참조가 아니라 int y = x; 였다면 복사본만 바뀌어 x 는 5 로 남는다." },
  { k: "포인터 개념", ex0: "포인터는 다른 변수가 위치한 메모리 주소를 저장", ex: "포인터는 다른 변수가 놓인 메모리 주소를 저장한다. 그래서 *p 로 원본을 읽고 쓸 수 있다. 변수의 복사본을 담는 것은 일반 변수 대입이고, 타입이나 이름 문자열을 저장하지는 않는다. 참조와 달리 nullptr 을 담거나 다른 대상으로 옮겨 붙을 수 있다." },
  { k: "역참조 연산자", ex0: "*p 는 포인터 p가 가리키는 곳의 값", ex: "포인터가 가리키는 값에 접근하는 역참조 연산자는 * 다. *p = 10; 이면 p 가 가리키는 변수가 10 으로 바뀐다. & 는 반대로 주소를 얻는 연산자이고, -> 는 포인터로 멤버에 접근할 때(p->x) 쓰는 축약형이라 값 하나를 꺼내는 기호는 * 다." },
  { k: "전달 방식 선택", ex0: "const 참조는 복사를 피하면서도 함수 안에서 원본 수정", ex: "큰 객체는 const T& 로 넘기면 복사가 없고 const 덕분에 함수 안에서 원본을 못 바꾼다. 값으로 넘기면 매번 복사 비용이 들고, 전역 변수는 어디서나 바꿀 수 있어 원본 보호와 정반대이며 함수 재사용도 어렵다. '이름으로 전달' 은 C++ 에 없는 방식이다." },
  { k: "마지막 제거", ex0: "v.pop_back() 은 마지막 요소를 제거", ex: "vector 의 맨 뒤 요소를 지우는 함수는 pop_back 이다. 값을 돌려주지 않으므로 필요하면 back() 으로 먼저 읽는다. erase 는 반복자로 위치를 지정해 지우는 범용 함수이고, remove 는 알고리즘 헤더의 것이라 실제로 크기를 줄이지 않는다. 빈 vector 에 pop_back 하면 정의되지 않은 동작이다." },
  { k: "합계", ex0: "범위 기반 for로 모든 요소를 더하면 1+2+3+4 = 10", ex: "범위 기반 for 가 vector 의 원소 1, 2, 3, 4 를 차례로 sum 에 더하므로 결과는 10 이다. 인덱스를 직접 다루지 않아 한 원소도 빠뜨리지 않는다. 마지막 원소를 빼먹은 6 이나 첫 원소를 빼먹은 9 는 인덱스 범위를 잘못 잡았을 때의 값이다." },
  { k: "비어있나", ex0: "empty()는 컨테이너가 비어 있으면 true", ex: "비어 있는지 묻는 함수는 empty() 로 bool 을 돌려준다. size() == 0 으로도 알 수 있지만 empty() 가 의도가 분명하고 모든 컨테이너에서 상수 시간이다. front() 는 첫 원소를 돌려주는데 빈 vector 에서 부르면 정의되지 않은 동작이고, begin() 은 반복자를 줄 뿐이다." },
  { k: "크기", ex0: "키 \"a\"는 유일하므로 값만 덮어써지고 크기는 2", ex: "map 은 키가 유일하므로 같은 키 \"a\" 에 두 번 대입하면 항목이 늘지 않고 값만 덮어써진다. 그래서 키가 \"a\" 와 \"b\" 두 개뿐이고 size() 는 2 다. 대입 횟수 3 을 세면 틀린다. 같은 키를 여러 번 담고 싶다면 multimap 을 써야 한다." },
  { k: "map 개념", ex0: "map은 키와 값을 쌍으로 저장하며 키를 기준으로", ex: "std::map 은 키-값 쌍을 저장하고 키 기준으로 자동 정렬한다. 키는 유일하므로 중복 값 집합이 아니고, 값만 순서대로 담는 것은 vector, 고정 크기 배열은 std::array 다. m[\"a\"] = 1 처럼 키로 넣고 꺼내며, 없는 키를 [] 로 읽으면 기본값으로 새 항목이 생긴다." },
  { k: "set 크기", ex0: "set은 중복을 저장하지 않으므로 3은 한 번만", ex: "set 은 같은 값을 두 번 넣어도 하나만 유지한다. 3 을 두 번 insert 해도 한 번만 들어가므로 서로 다른 값은 두 개, size() 는 2 다. insert 횟수 3 을 세면 틀린다. 중복을 허용하면서 정렬까지 원하면 multiset 을 쓴다." },
  { k: "set 특징", ex0: "set은 유일한 값만 저장하며 기본적으로 오름차순", ex: "std::set 은 중복을 허용하지 않고 원소를 오름차순으로 자동 정렬해 둔다. 균형 트리로 구현되어 인덱스 접근 s[i] 는 없고 반복자로 순회한다. 값을 뒤에만 붙이는 것은 vector 의 push_back 이야기이고, 중복을 허용하며 순서가 없는 것은 unordered_multiset 쪽이다." },
  { k: "생성자 이름", ex0: "생성자는 클래스와 같은 이름을 가진다. ~Cat 은 소멸자", ex: "생성자는 클래스와 같은 이름 Cat 을 가지며 반환형이 없다. ~Cat 은 객체가 사라질 때 불리는 소멸자다. makeCat 이나 initCat 은 그냥 일반 멤버 함수 이름이라 new Cat() 이나 Cat c; 에서 자동으로 호출되지 않는다." },
  { k: "this 의미", ex0: "this는 멤버 함수를 호출한 바로 그 객체를 가리키는 포인터", ex: "멤버 함수 안의 this 는 그 함수를 호출한 객체 자신을 가리키는 포인터다. this->x 로 멤버에 접근하고, 매개변수와 멤버 이름이 겹칠 때 구별하는 데 쓴다. 정적 멤버는 객체에 속하지 않아 this 가 없고, 부모 클래스나 전역 변수를 가리키는 것이 아니다." },
  { k: "공개 키워드", ex0: "public 으로 지정된 멤버는 클래스 외부에서도 접근", ex: "클래스 밖에서 접근할 수 있게 여는 접근 지정자는 public 이다. private 은 클래스 안에서만, protected 는 자식 클래스까지만 허용한다. class 의 기본 접근은 private 이므로 밖에서 쓸 멤버 함수는 public: 아래에 두어야 하고, struct 는 기본이 public 이다." }
] };
