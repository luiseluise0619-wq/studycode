module.exports = {
  track: "sysd", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 여러 대에서만 갈라지는 코드 고치기",
  source: "./dbg_sysd.cjs",
  lessons: [
    { t: "겹침·수렴·경계를 잘못 잡은 곳", n: 6,
      th: { sum: "분산 버그는 **한 대에서는 절대 재현되지 않는다** — 부등호 하나, 동점 처리 하나가 복제본을 갈라 놓는다.",
        body: [
          { h: "겹치게 만드는 부등식", t: "`R + W > N` 은 읽는 집합과 쓰는 집합이 반드시 겹치게 하는 조건이고, `>=` 로 적으면 겹치지 않는 조합이 생겨 방금 쓴 값을 못 읽는다. 링 탐색은 **감기는 경우**를 빠뜨리기 쉽고, 슬라이딩 윈도는 `<=` 하나로 한도가 조용히 하나 넓어진다. 셋 다 **경계값 테스트**로만 잡힌다." },
          { h: "어느 순서로 합쳐도 같아야 한다", t: "복제본은 서로 다른 순서로 병합한다. 그래서 병합 함수는 **교환·결합·멱등**이어야 하고, 동점은 '먼저 온 쪽' 이 아니라 **값에서 결정되는 기준**(노드 이름·해시)으로 풀어야 한다. 이 성질이 깨지면 두 복제본은 영원히 수렴하지 않는다." }],
        code: { c: "if (a.ts !== b.ts) return a.ts > b.ts ? a : b;\nreturn a.node > b.node ? a : b;   // 순서와 무관하게 같은 답", cap: "동점은 값으로 푼다" },
        key: ["R + W > N — 등호는 위험하다", "링은 감긴다", "윈도는 반열린 구간", "병합은 교환·결합·멱등"] } },
    { t: "0·음수·과반에서 새는 곳", n: 6,
      th: { sum: "분모가 0 이 되는 순간, 해시가 음수가 되는 순간, 정확히 반으로 갈리는 순간 — **평소에는 오지 않는 입력**이 장애와 겹쳐서 온다.",
        body: [
          { h: "나눗셈과 나머지", t: "`0 / 0` 은 `NaN` 이고 `NaN` 은 어떤 비교에서도 거짓이라 '조용히 막는' 쪽으로 기운다 — 그게 배포 직후 첫 실패와 겹치면 스스로 회복하지 못한다. 자바스크립트의 `%` 는 **나머지**라 피제수의 부호를 따라가므로 해시 버킷이 음수가 된다. 둘 다 **나눗셈을 곱셈으로 옮겨 적으면** 사라진다." },
          { h: "과반과 상한", t: "'절반 이상' 과 '**절반 초과**' 는 짝수에서 갈린다 — 4대가 2대씩 갈리면 양쪽 모두 리더가 된다. 리스는 쥔 쪽이 **일찍 포기**하고 주는 쪽이 늦게 회수해야 안전 구간이 생긴다. 잘라 나눈 마지막 구간은 전체 범위를 넘지 않도록 씌운다." }],
        code: { c: "return votes * 2 > nodes;          // 나눗셈을 곱셈으로\nreturn ((acc % n) + n) % n;        // 언제나 0 이상", cap: "짝수·0·음수를 테스트에 넣는다" },
        key: ["분모 0 을 먼저 가른다", "% 는 모듈로가 아니다", "과반은 절반 초과", "마지막 구간은 잘라 씌운다"] } },
  ],
};
