module.exports = {
  track: "os", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 커널 계산이 한 칸씩 어긋난 코드 고치기",
  source: "./dbg_os.cjs",
  lessons: [
    { t: "정책과 올림 방향", n: 6,
      th: { sum: "교체·스케줄링 정책은 **한 줄이 정체성**이고, 자원 계산은 **올림 방향 한 글자**가 정답을 가른다.",
        body: [
          { h: "무엇을 기준으로 내보내는가", t: "FIFO 는 도착 시각, LRU 는 마지막 사용 시각, LFU 는 사용 횟수로 내보낸다. 적중했을 때 순서를 옮기면 그 순간 FIFO 가 LRU 가 된다. SJF 는 '짧은 것부터' 정렬 한 줄이 전부라, 그 줄이 없으면 FCFS 와 같아진다 — **입력이 우연히 정렬돼 있으면 테스트도 통과한다**." },
          { h: "쪼갤 수 없는 자원은 올림", t: "필요한 페이지 수·라운드로빈 스케줄 횟수는 **올림**이다. 반대로 '이미 정렬된 주소' 를 또 올리면 매핑마다 한 페이지씩 샌다. 경계에 딱 맞는 값을 첫 테스트로 두면 두 방향 실수가 모두 잡힌다." }],
        code: { c: "if (mem.indexOf(p) >= 0) continue;   // FIFO 는 적중해도 순서를 안 바꾼다\nreturn Math.ceil(bytes / pageSize);", cap: "정책 한 줄, 올림 한 글자" },
        key: ["FIFO 와 LRU 를 가르는 한 줄", "SJF 는 정렬이 전부", "필요한 개수는 올림", "경계값을 첫 테스트로"] } },
    { t: "상태·경계·거절", n: 6,
      th: { sum: "커널 흉내 코드가 가장 자주 놓치는 셋 — **무엇이 열려 있는지**, **어디서 시작했는지**, **모자라면 어떻게 할지**.",
        body: [
          { h: "개수만 세면 모자라다", t: "열린 fd 를 숫자 하나로 세면 중복 열기·이중 닫기에서 값이 어긋나고 음수까지 간다. 스트림 조각은 **줄 경계에서 잘리지 않으므로** 이어 붙인 뒤 나눠야 하고, SCAN 은 현재 위치와 방향을 봐야 한다. 셋 다 '**상태를 들고 있어야 풀리는**' 문제다." },
          { h: "조용히 고치지 말고 거절한다", t: "빈 배열에서 `pop` 은 조용히 아무 일도 안 해서 `/../x` 가 `/x` 라는 멀쩡한 값이 된다 — 루트를 넘으려 했다는 사실이 사라진다. 세마포어도 모자라면 음수로 내려가는 대신 **거절**해야 한다. 보안·자원 한도에서 조용한 보정은 우회의 발판이다." }],
        code: { c: "if (seg === '..') { if (!out.length) return null; out.pop(); }\nreturn n <= count ? count - n : null;", cap: "고쳐 주지 말고 거절한다" },
        key: ["짝을 이루는 이벤트는 식별자로 센다", "스트림은 이어 붙인 뒤 나눈다", "누적 카운터는 증가분으로", "잘못된 입력은 거절"] } },
  ],
};
