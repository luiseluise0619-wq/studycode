module.exports = {
  track: "compiler", guide: "🔍", xp: 70,
  unit: "코드 리뷰 — 결함 찾기",
  source: "./rev_compiler.cjs",
  lessons: [
    { t: "읽고 묶는 단계", n: 4,
      th: { sum: "렉서와 파서의 결함은 **문법은 통과하는데 뜻이 달라지는** 형태로 나타난다.",
        body: [
          { h: "무엇을 하나로 묶는가", t: "렉서의 임무는 이어진 글자를 하나의 토큰으로 묶는 것이다. 숫자를 한 글자씩 끊거나 이름과 키워드를 다루지 않으면 파서가 볼 입력 자체가 어긋난다. 빈칸 종류를 한데 묶어 건너뛰는 것도 같은 일의 일부다." },
          { h: "어느 쪽으로 묶이는가", t: "재귀 하강에서 좌재귀는 토큰을 소비하지 않고 자기를 불러 곧바로 스택이 넘친다. 오른쪽에서 자신을 다시 부르면 <b>우결합</b>이 되므로, 왼쪽 결합 연산자는 <code>while</code> 로 접어야 한다 — 결과가 틀리는데 문법 오류는 나지 않는다." }],
        code: { c: "let node = parseTerm();\nwhile (peek() === '-') { next(); node = { op:'-', left: node, right: parseTerm() }; }", cap: "왼쪽 결합은 반복으로 접는다" },
        key: ["렉서는 글자를 묶는다", "좌재귀는 무한 재귀", "재귀는 우결합을 만든다", "의미가 틀려도 문법은 통과한다"] } },
    { t: "이름·의미·산출물", n: 4,
      th: { sum: "뒤 단계의 결함은 **조용하다** — 스코프, 평가 순서, 최적화, 링크가 그렇다.",
        body: [
          { h: "이름과 순서", t: "스코프가 하나뿐이면 지역 변수가 전역처럼 살아남고 중복 선언이 조용히 덮인다. 평가기에서 단축 평가를 빠뜨리면 <code>p != null &amp;&amp; p.x</code> 가 터지는데, 이것은 최적화가 아니라 <b>언어 규칙</b>이다." },
          { h: "접는 것과 잇는 것", t: "상수 폴딩은 의미를 바꾸지 않는 변환이어야 한다 — 0 으로 나누기를 접으면 실행 시점 오류가 값으로 박히고, 호스트 산술로 계산하면 대상 언어의 수 체계와 갈린다. 헤더에 정의를 두면 포함한 곳마다 심볼이 생겨 링크가 깨진다." }],
        code: { c: "if (b === 0) return n;            // 접지 않는다\nscope = { parent: scope, vars: new Map() };\n// util.h: extern int counter;   (정의는 .c 에)", cap: "확신이 없으면 접지 않는다" },
        key: ["스코프는 층으로 쌓는다", "단축 평가는 언어 규칙", "최적화는 의미를 보존한다", "헤더에는 선언만"] } },
  ],
};
