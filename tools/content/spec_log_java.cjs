module.exports = {
  track: "java", guide: "📜", xp: 70,
  unit: "로그 분석 II — 런타임이 흘린 단서",
  source: "./log_java.cjs",
  lessons: [
    { t: "기동과 의존성", n: 5,
      th: { sum: "기동 실패와 '로컬에서는 되는데' 는 대개 **무엇이 클래스패스에 있는가**의 문제다.",
        body: [
          { h: "먼저 발견된 쪽이 이긴다", t: "같은 라이브러리의 두 버전이 섞이면 어느 쪽이 잡히는지는 <b>순서</b>에 달렸고, 순서는 환경마다 다릅니다. 컴파일은 새 버전으로 했으니 빌드는 통과하고, 실행 때 <code>NoSuchMethodError</code> 로 터집니다. 의존성 트리를 보고 <b>누가 옛 버전을 끌고 오는지</b> 찾는 것이 첫걸음입니다." },
          { h: "환경이 다르면 결과도 다르다", t: "기본 시간대·기본 로케일·기본 인코딩은 <b>장비가 정합니다</b>. 그래서 같은 코드가 서버마다 다르게 동작하고, 터키어 로케일에서만 로그인이 깨지는 일이 생깁니다. 기계적인 비교와 저장에는 항상 <b>고정된 값</b>(UTC·Locale.ROOT·UTF-8)을 쓰고, 사용자 설정은 <b>보여 줄 때만</b> 씁니다." }],
        code: { c: "mvn dependency:tree | grep guava\ntoUpperCase(Locale.ROOT)   // 비교용\nInstant / TIMESTAMPTZ       // 시점 저장", cap: "환경이 정하게 두지 않는다" },
        key: ["두 버전이 섞이면 순서가 결정한다", "기본값은 장비마다 다르다", "비교에는 고정 로케일", "시점은 UTC 로 저장"] } },
    { t: "멈춤·누수·조용한 중단", n: 5,
      th: { sum: "JVM 사고의 절반은 **힙 밖**에 있거나, **아무 로그도 남기지 않는다**.",
        body: [
          { h: "힙만 보면 놓친다", t: "직접 메모리·메타스페이스·스레드 스택은 GC 가 회수하지 않으므로, 힙 그래프가 평온해도 컨테이너가 OOMKilled 될 수 있습니다. 반대로 <b>배포 직후만 느린 것</b>은 누수가 아니라 JIT 예열입니다 — 힙과 GC 가 멀쩡하다는 사실이 그 방향을 가리킵니다." },
          { h: "안 도는 것을 경보로", t: "<code>scheduleAtFixedRate</code> 는 예외가 나면 조용히 멈추고, 동기 로그 appender 는 디스크가 느려지면 요청 스레드를 붙잡습니다. 둘 다 <b>오류 로그가 없다</b>는 것이 특징입니다. 그래서 지표는 실패 건수뿐 아니라 <b>마지막 성공 시각</b>을 함께 봐야 합니다." }],
        code: { c: "MaxDirectMemorySize 명시\ntry { work(); } catch (Throwable t) { log(t); }  // 스케줄 작업\n마지막 성공 시각 > 5분 → 경보", cap: "조용한 실패를 먼저 의심한다" },
        key: ["힙 밖도 한도를 준다", "배포 직후 느림은 예열", "예외는 스케줄을 죽인다", "안 도는 것을 지표로"] } },
  ],
};
