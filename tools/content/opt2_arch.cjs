/* arch 트랙 — 정답이 5자 이상 단독 최장인 18문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "arch", fixes: [
  { k: "어디가 막혔는지 가리기", o: [
    "큐 깊이가 32 나 되므로 이미 과부하 상태이며, 깊이를 1 로 낮춰 요청을 하나씩 보내야 응답 시간이 지금 수준으로 유지된다. 응답 시간이 낮은 것은 아직 큐가 흡수하고 있을 뿐이라 곧 나빠진다",
    "사용률 100% 는 장치가 쉴 틈 없이 돌고 있다는 뜻이므로 포화로 보고 즉시 장치를 늘려야 한다. 응답 시간이 낮은 것은 캐시에 맞은 요청이 섞여 평균이 낮게 보이는 것이라 안심할 근거가 못 된다",
    "응답 시간 0.3ms 는 장치가 아니라 캐시 계층에서 잰 값일 가능성이 높아 측정 오류로 본다. 사용률은 커널이 직접 세는 값이라 더 믿을 만하므로, 사용률을 기준으로 포화라고 판단하고 대응한다",
    "사용률은 '요청이 하나라도 진행 중인 시간의 비율' 일 뿐이라, 내부에서 여러 요청을 동시에 처리하는 장치에서는 포화의 근거가 되지 못한다. 응답 시간과 큐 대기 시간이 정상이면 여유가 있는 것으로 본다" ] },
  { k: "인터럽트가 너무 많을 때", o: [
    "패킷을 일부 버려 인터럽트 수를 줄이는 것이 유일한 방법이다 — 인터럽트는 하드웨어가 패킷마다 내는 신호라 소프트웨어가 빈도를 바꿀 수 없고, 버려진 패킷은 상위 프로토콜의 재전송으로 메워진다",
    "부하가 높을 때는 인터럽트를 끄고 드라이버가 큐를 직접 훑는 폴링으로 전환한다. 어차피 항상 일감이 있으므로 '알려 달라' 는 신호 자체가 낭비다 — 한산해지면 다시 인터럽트 방식으로 돌아온다",
    "네트워크 인터럽트의 우선순위를 최고로 올려 다른 인터럽트보다 먼저 처리되게 한다 — 처리 순서가 앞당겨지면 핸들러가 빨리 끝나 CPU 가 패킷 처리로 돌아올 시간이 생기므로 병목이 풀린다",
    "인터럽트 발생 방식은 네트워크 카드 하드웨어가 정하는 것이라 드라이버나 커널에서 바꿀 수 없다 — 그래서 이 구간에서는 더 빠른 CPU 를 쓰거나 카드를 여러 장 나눠 꽂아 인터럽트를 분산하는 수밖에 없다" ] },
  { k: "DMA 와 캐시가 어긋날 때", o: [
    "버퍼를 volatile 로 선언하면 컴파일러가 캐시를 우회해 메모리에서 직접 읽으므로 옛 값이 보이지 않게 된다 — 어느 시스템에서든 생길 수 있는 문제이고, 캐시 무효화를 따로 부를 필요는 없다",
    "DMA 는 CPU 캐시를 거쳐 데이터를 쓰므로 이런 상황은 원리적으로 생기지 않는다 — 옛 값이 보인다면 장치가 아직 전송을 끝내지 않은 것이므로 완료 인터럽트를 기다리지 않은 드라이버 쪽 순서 문제다",
    "메모리 배리어를 넣으면 해결되며 캐시와는 무관한 문제다 — CPU 가 읽기를 DMA 완료 확인보다 먼저 실행하도록 재배치한 것이라, 배리어로 순서만 고정하면 어떤 시스템에서든 새 값이 보인다",
    "DMA 는 메인 메모리를 고치지만 CPU 캐시에 남은 옛 줄은 그대로일 수 있다. 캐시 일관성이 하드웨어로 보장되지 않는 시스템에서는 읽기 전에 해당 구간의 캐시를 무효화해 주어야 한다" ] },
  { k: "오래 돌릴수록 느려진다", o: [
    "가비지 컬렉션이 누적되어 멈춤 시간이 점점 길어지는 것이므로 힙 크기를 늘리면 회복된다 — 온도나 주파수는 확인하지 않아도 되고, GC 로그의 멈춤 시간이 길어지는지만 보면 바로 드러난다",
    "캐시가 점점 오염되어 미스율이 올라가는 것이다 — 반복할수록 이전 결과가 캐시에 쌓여 새 데이터가 들어갈 자리가 줄어들므로, 반복 사이에 캐시를 비우는 코드를 넣으면 처음 속도로 돌아온다",
    "온도가 올라 칩이 스스로 주파수를 낮추고 있는지 본다 — 온도와 실제 동작 주파수를 함께 기록하면 바로 드러나고, 냉각을 개선하거나 지속 가능한 수준에서 재야 한다",
    "메모리 누수로 사용 가능한 메모리가 줄어드는 것이다 — 벤치마크가 할당한 메모리를 반복마다 놓치면 스왑이 시작되어 느려지므로, 메모리 사용량 그래프를 보고 누수 지점을 찾아 해제하면 된다" ] },
  { k: "두 번 캐시되는 문제", o: [
    "페이지 캐시를 우회하면 디스크 접근 자체가 사라져 빨라지기 때문이다 — 우회 경로는 커널의 캐시 계층을 거치지 않으므로 어떤 접근 패턴에서든 빠르고, 그래서 모든 DB 가 그렇게 동작한다",
    "페이지 캐시는 읽기만 담당하고 쓰기를 지원하지 않아 읽기 전용 DB 에서만 쓸 수 있기 때문이다 — 쓰기가 있는 DB 는 캐시를 거치면 갱신이 디스크에 반영되지 않으므로 우회할 수밖에 없다",
    "같은 데이터가 페이지 캐시와 DB 버퍼 풀에 두 번 올라가 메모리를 낭비하고, 무엇을 언제 버릴지를 DB 가 통제할 수 없기 때문이다. 접근 패턴을 아는 쪽이 직접 관리하는 편이 낫다",
    "우회 경로가 단순하고 빠르므로 모든 응용이 그렇게 하는 것이 옳다 — 페이지 캐시는 범용 응용을 위한 편의 장치일 뿐이라, 성능이 중요한 프로그램은 예외 없이 직접 디스크를 다루는 편이 유리하다" ] },
  { k: "쉴 때는 얼마나 쓰나", o: [
    "쓰지 않는 부분의 클럭을 멈추거나 전원을 끊어 전력을 크게 줄인다. 다만 깊이 잘수록 깨어나는 데 시간이 걸리므로, 곧 다시 일할 것 같으면 얕게 자는 편이 낫다",
    "동작 주파수만 낮출 뿐 전력을 쓰는 구조 자체는 그대로이며, 깨어나는 지연도 생기지 않는다 — 그래서 대가는 없고, 언제나 가장 낮은 주파수로 내려가 있다가 일이 오면 곧바로 올라온다",
    "깊이 자도 복귀 지연이 없으므로 가장 깊게 재우는 것이 옳다 — 전원을 끊었던 부분은 켜는 순간 상태가 그대로 살아나므로, 잠깐 쉴 때도 가장 깊은 상태로 보내는 것이 전력과 반응 모두에 이득이다",
    "휴면 상태는 배터리로 도는 기기에서만 쓰고 서버에서는 쓰지 않는다 — 서버는 언제 요청이 올지 모르므로 항상 최고 주파수로 깨어 있어야 하고, 전력은 전원이 넉넉해 아낄 이유가 없기 때문이다" ] },
  { k: "특화 회로가 이기는 이유", o: [
    "전용 회로가 CPU 보다 훨씬 높은 주파수로 동작하기 때문이며, 회로 구조와는 관계가 없다 — 같은 일을 더 빨리 끝내니 켜져 있는 시간이 짧고, 그래서 총 전력이 적게 든다는 주파수 차이만 보면 설명이 끝난다",
    "CPU 는 무슨 일이든 할 수 있게 만들어져 있어 명령어를 읽고 해석하고 순서를 정하는 데 많은 전력을 쓴다. 전용 회로는 그 과정 없이 연산 자체에만 전력을 쓴다",
    "전용 회로는 메모리를 쓰지 않고 레지스터 안에서만 계산하므로 전력이 적게 드는 것이다 — 영상 프레임 전체를 회로 안에 담아 두고 처리하기 때문에 메모리를 오가는 비용이 없고, 그 차이가 수십 배를 만든다",
    "CPU 는 영상 처리에 필요한 부동소수 연산을 하드웨어로 못 해 소프트웨어로 흉내 내기 때문이다 — 정수 연산 여러 개로 나눠 계산하느라 명령어 수가 수십 배 늘고, 그만큼 전력이 더 든다" ] },
  { k: "믿을 수 있게 재려면", o: [
    "가상 머신 안에서 재면 환경이 격리되어 주파수 변동과 다른 작업의 영향이 모두 사라진다 — 호스트의 잡음이 가상화 계층에서 걸러지므로 반복 횟수는 신경 쓰지 않아도 되고 한 번의 측정으로 충분하다",
    "주파수를 고정하고, 예열 뒤에 재고, 여러 번 반복해 중앙값과 분포를 보고, 다른 작업이 없는 상태에서 잰다 — 평균 하나만 보고하면 흔들림이 감춰진다",
    "여러 번 재서 가장 좋은 값을 골라 보고하면 잡음이 제거된다 — 방해는 시간을 늘리는 쪽으로만 작용하므로 최선값이 곧 코드의 진짜 성능이고, 분포나 중앙값은 잡음까지 섞어 보고하는 셈이다",
    "한 번만 재되 가능한 한 긴 시간을 재면 흔들림이 상쇄된다 — 몇 분 이상 돌리면 부스트와 스로틀링, 다른 작업의 끼어듦이 서로 평균되어 사라지므로 반복이나 예열 없이도 재현 가능한 값이 나온다" ] },
  { k: "속도와 전력의 관계", o: [
    "2배보다 훨씬 많이 늘어난다 — 주파수에 비례하는 몫만으로도 2배인데, 높은 주파수를 버티려면 전압도 올려야 하고 전력은 전압의 제곱에 비례하기 때문이다",
    "절반으로 준다 — 같은 일을 더 짧은 시간에 끝내기 때문이다. 활성 시간이 절반이 되면 총 소비 전력도 절반이 되고, 그래서 주파수를 올리는 것이 전력을 아끼는 가장 확실한 방법이다",
    "정확히 2배 늘어난다 — 전력은 주파수에 정비례한다. 스위칭이 두 배 자주 일어나면 스위칭에 드는 에너지도 두 배가 되고, 전압은 회로가 정하는 고정값이라 주파수와 함께 바뀌지 않기 때문이다",
    "거의 변하지 않는다 — 전력은 주로 트랜지스터 개수가 정한다. 같은 칩이면 트랜지스터 수가 같으므로 주파수를 올려도 전력은 거의 그대로이고, 대신 열이 조금 더 빨리 쌓이는 정도의 차이만 있다" ] },
  { k: "잠깐만 빨라진다", o: [
    "메모리 대역폭이 CPU 를 따라오지 못해 스스로 속도를 낮추는 것이며, 열이나 전력과는 관계가 없다 — 처음에는 캐시에 맞아 빠르다가 메모리를 기다리기 시작하면 주파수를 내려 보조를 맞춘다",
    "짧은 시간이라면 열이 아직 안 쌓여 높은 주파수를 견딜 수 있기 때문이다. 계속 돌리면 온도와 전력 예산에 걸리므로, 오래 유지할 수 있는 수준으로 내려온다",
    "최고 주파수는 광고용 수치이며 실제로는 도달하지 않는다 — 스펙표의 값은 이상적인 조건에서 순간적으로 찍힌 값이라, 실제 부하에서는 처음부터 그보다 낮은 주파수로 돌고 있는 것이다",
    "운영체제가 일정 시간이 지나면 전력 절약 모드로 강제 전환하기 때문이다 — 커널의 전원 관리 정책이 몇 초 이상 이어지는 부하를 배터리 소모로 보고 주파수 상한을 낮추므로, 정책을 바꾸면 계속 유지된다" ] },
  { k: "겹칠지도 모른다", o: [
    "n 이 8의 배수가 아니어서 마지막 묶음이 채워지지 않을 가능성이다 — 벡터 명령은 정해진 개수를 한 번에 처리하므로 나머지가 생기면 컴파일러가 벡터화 자체를 포기한다",
    "float 의 덧셈 순서를 바꾸면 결과가 달라질 가능성이다 — 부동소수는 결합 법칙이 성립하지 않으므로, 여러 칸을 동시에 더하면 순서가 바뀌어 원래 코드와 다른 값이 나올 수 있다",
    "배열이 캐시 라인 경계에 정렬되어 있지 않을 가능성이다 — 벡터 적재는 정렬된 주소에서만 안전하므로, 정렬을 보장받지 못한 포인터에는 컴파일러가 벡터 명령을 쓰지 않는다",
    "out 과 a 가 같은 메모리를 가리킬 수도 있다는 가능성이다 — 그러면 앞에서 쓴 값이 뒤에서 읽는 값을 바꾸므로 순서를 바꿀 수 없다" ] },
  { k: "스트라이드 충돌", o: [
    "폭이 홀수이면 프리페처가 접근을 순차로 인식해 다음 줄을 미리 읽어 두기 때문이다 — 1024 처럼 딱 떨어지는 폭은 건너뛰기 패턴으로 보여 프리페치가 꺼진다",
    "폭이 2의 거듭제곱이면 접근 주소들이 같은 캐시 집합에 몰려 서로를 계속 밀어낸다. 폭을 하나 늘리면 집합이 흩어져 충돌 미스가 사라진다",
    "1024 는 페이지 크기와 같아 열 방향으로 한 칸 내려갈 때마다 페이지가 바뀌어 TLB 미스가 나기 때문이다 — 1025 면 같은 페이지 안에서 몇 칸을 더 갈 수 있다",
    "1025 가 소수라 컴파일러가 나머지 연산을 곱셈으로 바꾸는 특수한 최적화를 적용하기 때문이다 — 인덱스 계산이 빨라져 접근당 명령 수가 줄어든다" ] },
  { k: "포인터 추적", o: [
    "연결 리스트는 원소마다 함수 호출이 끼어들어 호출 비용이 그대로 더해지기 때문이다 — 노드를 따라가는 코드가 함수로 분리되어 있어 호출과 반환이 원소 수만큼 반복된다",
    "포인터가 8바이트라 노드마다 메모리 사용량이 두 배이기 때문이다 — 같은 개수를 훑어도 읽어야 할 바이트가 두 배이므로 메모리 대역폭을 두 배 쓰고 그만큼 느려진다",
    "연결 리스트의 노드는 흩어져 있어 캐시에 올라가지 않기 때문이다 — 캐시는 연속된 주소만 담을 수 있으므로 노드마다 메인 메모리까지 가야 하고 미스 한 번의 비용이 훨씬 크다",
    "다음 주소를 알려면 현재 노드를 읽어야 해서 미스를 겹쳐서 처리할 수 없다. 배열은 여러 미스를 동시에 진행시킬 수 있다" ] },
  { k: "나란히 있어야 한다", o: [
    "객체 배열에서는 필요한 값 사이사이에 안 쓰는 값이 끼어 있어 캐시 라인이 낭비되고, 값들이 나란하지 않아 한 번에 담을 수도 없다",
    "Float64Array 가 내부적으로 합계를 캐시해 두었다가 그대로 돌려주기 때문이다 — 같은 배열을 두 번째 더할 때는 계산 없이 저장된 값을 반환하므로 시간이 거의 들지 않는다",
    "객체 배열은 원소마다 x, y, z 세 값을 담고 있어 실제 원소 개수가 세 배이고 반복 횟수도 그만큼 늘어나기 때문이다 — 같은 400만 개라도 훑어야 할 값이 1200만 개가 된다",
    "객체의 속성 접근은 이름으로 찾는 조회라 배열 첨자 접근보다 문법적으로 느리기 때문이다 — 엔진이 p.x 를 만날 때마다 속성 표를 뒤져야 하므로 단순 첨자보다 명령이 몇 배 더 든다" ] },
  { k: "비정규화 수", o: [
    "값이 비정규화 수 영역으로 들어가면 하드웨어가 느린 경로로 처리한다. 아주 작은 값을 0 으로 밀어내면(FTZ) 회복된다",
    "무음 구간에서는 값이 자주 바뀌지 않아 분기 예측이 어긋나기 때문이다 — 신호가 있을 때 학습된 패턴이 무음에서는 맞지 않아 예측이 실패하고, 실패한 만큼 파이프라인이 비워져 느려진다",
    "컴파일러가 무음 구간에서 벡터화를 포기하기 때문이다 — 값이 0 에 가까우면 곱셈 결과가 정밀도를 잃을 수 있어, 안전을 위해 벡터 명령 대신 스칼라 명령으로 되돌아가므로 처리량이 수십 배 떨어진다",
    "0 에 가까운 값이 분모가 되어 나눗셈 예외를 계속 내기 때문이다 — 필터 계수로 나누는 단계에서 거의 0 으로 나누는 셈이 되어 예외 처리 루틴이 매 샘플마다 실행되고, 그 비용이 계산 자체보다 훨씬 크다" ] },
  { k: "앞 칸을 기다리면", o: [
    "둘 다 벡터화할 수 있다 — 앞뒤 의존성은 컴파일러가 자동으로 풀어 준다. 반복문의 계산 순서를 분석해 의존이 있는 칸은 미리 계산해 두고 나머지를 한꺼번에 처리하므로 형태와 무관하게 벡터화된다",
    "둘 다 벡터화할 수 없다 — 배열 첨자를 쓰는 반복문은 벡터화 대상이 아니다. 첨자 계산이 매 반복마다 달라지므로 컴파일러는 포인터 증가 형태로 쓴 반복문만 벡터 명령으로 바꿀 수 있다",
    "B 다 — 각 칸이 바로 앞 칸의 결과를 쓰므로 순서대로 계산할 수밖에 없고, 여러 칸을 동시에 계산한다는 전제가 깨진다",
    "A 다 — 배열 세 개를 동시에 다뤄야 해서 벡터 레지스터가 부족하다. 두 입력과 한 출력을 각각 레지스터에 올리면 남는 레지스터가 없어 컴파일러가 스칼라 명령으로 되돌아간다" ] },
  { k: "이론과 실제", o: [
    "측정이 잘못됐다 — 연산을 네 배 늘렸으면 걸리는 시간도 네 배가 되어야 한다. 시간이 그대로라는 것은 타이머 해상도가 모자라거나 측정 구간이 잘못 잡힌 것이므로 결과 자체를 믿을 수 없다",
    "컴파일러가 연산을 미리 계산해 없애 버렸기 때문이다 — 반복문 안의 계산이 입력과 무관하다고 판단되면 결과를 상수로 접어 넣으므로, 연산을 늘려도 실제로 실행되는 명령은 늘지 않는다",
    "캐시가 결과를 재사용해 두 번째 측정부터는 계산을 건너뛰기 때문이다 — 같은 배열을 다시 더하면 캐시에 남은 이전 합계를 그대로 쓰므로, 연산을 늘려도 처음 한 번만 계산하고 나머지는 건너뛴다",
    "원소당 계산이 적은 구간에서는 데이터를 실어 오는 시간이 지배하고 있다 — 계산을 빠르게 해도 기다리는 시간은 그대로다" ] },
  { k: "메모리도 전기를 쓴다", o: [
    "데이터를 옮기는 데는 시간만 들고 에너지는 들지 않으므로 계산량만 줄이면 된다 — 메모리 접근 횟수는 지연을 만들 뿐 전력과는 무관하고, 전력을 정하는 것은 연산 장치가 얼마나 바쁜가뿐이다",
    "메모리는 전력을 쓰지 않으며 CPU 계산만 전력을 쓴다 — 저장된 값을 읽는 것은 이미 있는 것을 꺼내는 일이라 에너지가 들지 않고, 값을 바꾸는 연산에서만 트랜지스터가 스위칭하며 전력을 쓴다",
    "값을 멀리서 옮겨 오는 데 드는 에너지가 그 값으로 하는 계산보다 훨씬 크기 때문이다 — 그래서 캐시에 맞게 데이터를 배치하고 옮기는 양을 줄이는 것이 전력에도 직접 이득이 된다",
    "메모리 전력은 용량에만 비례하고 접근 횟수와는 무관하다 — 메모리 칩은 켜져 있는 동안 내용을 유지하느라 일정한 전력을 쓰므로, 읽고 쓰는 횟수를 줄여도 총 전력은 용량이 같은 한 달라지지 않는다" ] }
] };
