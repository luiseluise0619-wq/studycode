module.exports = {
  track: "os", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 커널이 쓰는 이름과 숫자",
  source: "./in_os.cjs",
  lessons: [
    { t: "메모리를 나눠 쓰는 방법", n: 8,
      th: { sum: "가상 메모리는 **필요할 때까지 미루는 기술**이다. 페이지·폴트·TLB·요구 페이징이 전부 그 한 문장의 부품이다.",
        body: [
          { h: "페이지 단위로 미룬다", t: "x86-64 리눅스의 기본 페이지는 <b>4,096 바이트</b>라 1 바이트만 써도 한 페이지가 잡힌다. 아직 올라오지 않은 페이지를 건드리면 <b>페이지 폴트</b>가 나고 커널이 채워 넣은 뒤 명령을 다시 실행시킨다 — 파일에서 읽어야 하는 메이저와 연결만 하면 되는 마이너는 비용이 수천 배 다르다. 주소 변환 결과는 <b>TLB</b> 에 캐시되므로, 흩어져 접근하는 코드는 그것만으로 느려진다." },
          { h: "미룬 결과와 스레드", t: "<b>요구 페이징</b> 덕분에 '할당 성공' 이 '쓸 수 있다' 를 뜻하지 않는다 — 리눅스에서는 할당이 아니라 <b>쓰는 순간</b> OOM 킬러가 죽인다. 메모리가 모자라 페이지만 오가는 상태가 <b>스래싱</b> 이고, CPU 는 한가한데 먹통인 증상으로 나타난다. <b>스레드</b>는 주소 공간을 공유해 통신이 공짜인 대신 하나가 망가지면 전부 죽는다 — 격리와 통신 비용의 맞바꿈이다." }],
        code: { c: "4096 바이트 · 하위 12비트가 페이지 안 위치\n메이저 폴트 급증 → 메모리 부족을 의심\ncopy-on-write: fork 는 쓸 때 비로소 베낀다", cap: "미룰수록 빠르지만 실패 지점이 뒤로 간다" },
        key: ["기본 페이지는 4KB", "폴트는 메이저와 마이너를 나눠 본다", "할당 성공이 사용 가능은 아니다", "스레드는 격리 대신 통신을 얻는다"] } },
    { t: "누가 먼저 도는가", n: 7,
      th: { sum: "스케줄링과 동기화는 '**누가 먼저 도는가**' 와 '**누가 기다리는가**' 를 정하는 일이다. 잘못 정하면 아무도 못 돈다.",
        body: [
          { h: "빼앗기와 기다리기", t: "<b>선점</b> 스케줄링은 타이머 인터럽트로 CPU 를 강제로 빼앗아, 무한 루프가 있어도 다른 프로그램이 돈다 — 자바스크립트의 이벤트 루프는 비선점이라 무거운 계산 하나가 화면을 얼린다. <b>교착</b>은 네 조건(상호 배제·점유하며 대기·비선점·<b>순환 대기</b>)이 동시에 성립할 때만 생기므로 하나만 깨면 예방된다 — 자원마다 번호를 매겨 낮은 번호부터 잠그는 것이 가장 쉽다." },
          { h: "소유자가 있는 자물쇠", t: "<b>뮤텍스</b>는 잠근 쪽만 풀 수 있어 <b>우선순위 상속</b> 같은 보정을 걸 수 있고, 세마포어는 개수를 세는 장치라 '자리 n 개' 를 다룰 때 맞다. 우선순위 역전은 낮은 순위가 잠금을 쥔 채 밀려나는 문제이고, 화성 탐사선 패스파인더의 재시작 사고가 그것이다. 커널에 일을 부탁하는 <b>시스템 콜</b>은 특권 수준이 바뀌어 비싸므로, 라이브러리가 버퍼에 모아 두었다가 한 번에 넘긴다." }],
        code: { c: "자원은 언제나 낮은 번호부터 잠근다\nstrace 로 시스템 콜을 세어 본다\n2>&1  — 2번을 1번이 가리키는 곳으로", cap: "기다림의 고리를 만들지 않는다" },
        key: ["선점은 타이머가 만든다", "순환 대기만 깨도 교착이 없다", "뮤텍스에는 소유자가 있다", "시스템 콜은 모아서 부른다"] } },
  ],
};
