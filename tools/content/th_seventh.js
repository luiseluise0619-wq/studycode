/* 7차 이론 — java 와 javascript 에서 점수가 가장 높던 유닛들.

   java '열거형 실무' 는 점수 25 로 전체 최상위였다. 세 레슨이 같은 글을 나눠 쓰면서
   한 문단에 개념이 넷씩 들어 있었다. java '빌드와 의존성' 도 마찬가지다.
   javascript 'Web API 실무' 는 '왜' 가 통째로 없었다.

   규칙은 앞 차수와 같다 (주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지'
     · 용어는 그 자리에서 풀기 */
module.exports = {

java: [
{
  unit: "열거형(enum) 실무 (중급)", lesson: "enum의 정체와 기본 API",
  th: {
    sum: "enum 은 '이 중 하나' 를 **타입으로 못 박는 것**이다. 정해 둔 값 말고는 들어올 수 없다.",
    body: [
      { h: "왜 문자열 대신 쓰나", t: "상태를 `\"ACTIVE\"` 같은 글자로 다루면 오타가 나도 컴파일러가 잡아 주지 못한다. `\"ACITVE\"` 라고 쓴 채로 배포되어 실행 중에야 드러난다. enum 으로 두면 없는 이름은 컴파일 단계에서 걸린다." },
      { h: "각 상수는 객체 하나다", t: "enum 상수는 프로그램이 시작할 때 딱 하나씩 만들어지고 그 뒤로 바뀌지 않는다. 그래서 `==` 로 비교해도 안전하다 — 같은 이름이면 언제나 같은 물건이기 때문이다." },
      { h: "기본으로 딸려 오는 것들", t: "`values()` 는 전체 목록, `name()` 은 적어 둔 이름, `ordinal()` 은 선언 순서를 준다. 목록을 돌며 확인하는 코드는 `values()` 하나로 끝난다." },
      { h: "ordinal 에 뜻을 걸지 않는다", t: "`ordinal()` 은 그저 몇 번째로 적혔는지다. 나중에 누가 상수를 가운데 끼워 넣으면 그 뒤 숫자가 전부 밀린다. 점수나 코드처럼 뜻이 있는 값은 필드로 직접 적어 둔다." },
    ],
    code: { c: "enum Level {\n    LOW(1), MID(5), HIGH(10);\n    final int score;\n    Level(int s) { this.score = s; }\n}", cap: "뜻이 있는 값은 필드로" },
    key: ["없는 이름은 컴파일에서 걸린다", "상수는 하나씩만 만들어진다", "`ordinal()` 은 순서일 뿐"],
  },
},
{
  unit: "열거형(enum) 실무 (중급)", lesson: "상태와 전략을 enum으로",
  th: {
    sum: "enum 은 값만 담는 것이 아니라 **동작도 담을 수 있다.** 상수마다 다르게 행동하게 만든다.",
    body: [
      { h: "상수마다 다른 몸통", t: "enum 안에 메서드를 두고 상수마다 다르게 구현할 수 있다. 그러면 `switch` 로 갈라 쓰던 코드가 사라진다. 상수를 하나 늘릴 때 구현을 안 적으면 컴파일이 거부하므로, 빠뜨릴 수가 없다." },
      { h: "switch 를 빠뜨리는 사고를 막는다", t: "`switch` 로 갈라 쓰면 상수를 새로 넣고 `switch` 고치는 것을 잊기 쉽다. 컴파일은 되고 실행 중에 기본값이 조용히 나간다. 동작을 enum 안에 두면 그 자리가 한 곳뿐이라 잊을 수 없다." },
      { h: "상태 전이도 담을 수 있다", t: "'주문됨 → 배송중 → 완료' 처럼 다음 상태가 정해져 있으면, 각 상수가 자기 다음 상태를 알게 만든다. 규칙이 한 곳에 모여, 엉뚱한 상태로 건너뛰는 코드를 막을 수 있다." },
      { h: "너무 키우지는 않는다", t: "enum 안에 화면·저장까지 넣으면 이 파일 하나가 계속 커진다. 상수마다 갈리는 짧은 규칙만 담고, 큰 일은 밖으로 뺀다." },
    ],
    code: { c: "enum Op {\n    ADD { int apply(int a, int b) { return a + b; } },\n    MUL { int apply(int a, int b) { return a * b; } };\n    abstract int apply(int a, int b);\n}", cap: "상수마다 자기 동작" },
    key: ["상수마다 다르게 구현한다", "빠뜨린 `switch` 사고가 사라진다", "짧은 규칙만 담는다"],
  },
},
{
  unit: "열거형(enum) 실무 (중급)", lesson: "EnumMap·EnumSet과 switch",
  th: {
    sum: "enum 을 열쇠로 쓸 때는 **전용 자료구조**가 따로 있다. 더 빠르고 더 작다.",
    body: [
      { h: "EnumMap 은 배열로 동작한다", t: "enum 상수는 순서가 정해져 있어서, 그 순서를 배열의 자리로 그대로 쓸 수 있다. 그래서 `EnumMap` 은 값을 계산해 자리를 찾는 과정 없이 바로 접근한다. 일반 맵보다 빠르고 메모리도 적게 쓴다." },
      { h: "EnumSet 은 비트로 담는다", t: "상수 몇 개가 들어 있는지를 비트 하나씩으로 표시한다. 그래서 수십 개를 담아도 아주 작다. '이 권한 중 하나라도 있나' 같은 검사도 아주 빠르다." },
      { h: "순서가 선언 순서로 나온다", t: "`EnumMap` 과 `EnumSet` 을 돌면 선언한 순서대로 나온다. 화면에 보여 줄 때 따로 정렬하지 않아도 되어 편하다. 일반 해시 자료구조는 이 순서를 보장하지 않는다." },
      { h: "switch 는 여전히 쓸 만하다", t: "동작이 짧고 한 곳에서만 쓰인다면 `switch` 가 읽기 쉽다. 다만 상수를 늘릴 때 여기도 같이 고쳐야 한다는 것을 기억해야 한다 — 빠뜨리면 기본값이 조용히 나간다." },
    ],
    code: { c: "EnumMap<Level, Integer> m = new EnumMap<>(Level.class);\nEnumSet<Level> high = EnumSet.of(Level.MID, Level.HIGH);", cap: "enum 전용이 더 빠르다" },
    key: ["`EnumMap` 은 배열처럼 빠르다", "`EnumSet` 은 비트로 작다", "선언 순서대로 나온다"],
  },
},
{
  unit: "중급 — 빌드와 의존성: Maven·Gradle·모듈", lesson: "좌표와 스코프",
  th: {
    sum: "라이브러리는 **이름 세 조각**으로 가리킨다. 그리고 어디까지 필요한지를 함께 적는다.",
    body: [
      { h: "세 조각이 하나의 주소다", t: "그룹·이름·버전 셋이 모여 라이브러리 하나를 가리킨다. 셋 중 하나만 달라도 다른 물건이다. 버전을 안 적으면 빌드할 때마다 다른 것이 딸려 와, 어제 되던 것이 오늘 안 되는 일이 생긴다." },
      { h: "스코프는 '언제 필요한가'", t: "테스트에만 쓰는 라이브러리를 실행에까지 딸려 보내면 결과물이 쓸데없이 커진다. 컴파일할 때만 필요한 것, 실행할 때도 필요한 것, 테스트에만 필요한 것을 나눠 적는다." },
      { h: "안 적으면 딸려 온 것에 기댄다", t: "내가 직접 쓰는 라이브러리는 반드시 직접 적는다. 다른 라이브러리를 통해 딸려 온 것에 기대면, 그쪽이 버전을 바꾸는 날 갑자기 컴파일이 안 된다. 내 코드가 쓰는 것은 내가 선언한다." },
      { h: "버전은 한 곳에서 관리한다", t: "여러 모듈이 같은 라이브러리를 쓰면 버전을 한 곳에 모아 둔다. 흩어져 있으면 하나만 올리고 나머지를 잊어, 실행할 때 서로 다른 버전이 섞인다." },
    ],
    code: { c: "implementation \"com.example:util:1.4.0\"   // 실행에도 필요\ntestImplementation \"junit:junit:4.13.2\"   // 테스트에만", cap: "무엇을 언제 쓰는지 적는다" },
    key: ["그룹·이름·버전 세 조각", "스코프는 '언제 필요한가'", "쓰는 것은 직접 적는다"],
  },
},
{
  unit: "중급 — 빌드와 의존성: Maven·Gradle·모듈", lesson: "버전 충돌 진단",
  th: {
    sum: "같은 라이브러리의 **다른 버전**이 여러 경로로 딸려 오면, 빌드 도구가 하나를 골라 준다.",
    body: [
      { h: "왜 충돌이 생기나", t: "A 는 util 1.0 을 쓰고 B 는 util 2.0 을 쓰는데, 내 프로젝트가 둘 다 쓰면 util 이 두 벌 필요해진다. 하지만 실행할 때는 한 벌만 올라간다. 그래서 도구가 하나를 골라야 한다." },
      { h: "고른 결과를 눈으로 본다", t: "의존성 목록을 뽑아 보면 어떤 버전이 골라졌고 무엇이 밀려났는지 나온다. 이걸 안 보고 짐작하면, 분명 2.0 을 적었는데 1.0 이 올라가 있는 상황을 이해할 수 없다." },
      { h: "없는 메서드 오류가 신호다", t: "컴파일은 됐는데 실행할 때 '그런 메서드가 없다' 는 오류가 나면 대개 버전 충돌이다. 컴파일할 때 본 것과 실행할 때 올라간 것이 다른 것이다." },
      { h: "고정할 수 있다", t: "어떤 버전을 쓸지 직접 못 박을 수 있다. 다만 못 박으면 그 라이브러리를 쓰는 다른 것이 깨질 수 있으니, 고정한 뒤에는 테스트를 꼭 돌려 본다." },
    ],
    code: { c: "./gradlew dependencies --configuration runtimeClasspath\n# util:1.0 -> 2.0  (밀려난 것이 화살표로 보인다)", cap: "고른 결과를 눈으로 확인한다" },
    key: ["실행에는 한 벌만 올라간다", "목록을 뽑아 확인한다", "없는 메서드 오류가 신호"],
  },
},
{
  unit: "중급 — 빌드와 의존성: Maven·Gradle·모듈", lesson: "클래스패스와 모듈",
  th: {
    sum: "자바는 실행할 때 **어디서 클래스를 찾을지** 목록을 받는다. 그 목록이 클래스패스다.",
    body: [
      { h: "찾는 순서가 있다", t: "같은 이름의 클래스가 여러 곳에 있으면 앞에 있는 것이 쓰인다. 그래서 옛 라이브러리가 앞에 있으면 새것을 넣어 두고도 옛 코드가 돈다. '분명 고쳤는데 그대로' 의 흔한 원인이다." },
      { h: "없으면 실행 중에 터진다", t: "클래스패스에 없는 클래스를 부르면 `NoClassDefFoundError` 가 난다. 컴파일할 때는 있었는데 실행할 때 빠진 경우다. 컴파일용과 실행용 목록이 다를 수 있다는 것을 알아야 이해가 된다." },
      { h: "모듈은 경계를 만든다", t: "모듈 시스템은 '이 패키지만 밖에 공개한다' 를 적어 둘 수 있게 한다. 공개하지 않은 것은 다른 모듈이 아예 쓰지 못한다. 남이 내부 구현에 기대는 것을 막아 준다." },
      { h: "섞이면 복잡해진다", t: "모듈로 만든 것과 아닌 것을 섞으면 규칙이 까다로워진다. 작은 프로젝트라면 굳이 모듈까지 가지 않아도 된다 — 얻는 것보다 설정에 드는 시간이 클 수 있다." },
    ],
    code: { c: "java -cp app.jar:libs/* com.example.Main\n\nmodule my.app { exports com.example.api; }", cap: "찾을 자리와 보여 줄 자리" },
    key: ["앞에 있는 것이 쓰인다", "없으면 실행 중에 터진다", "모듈은 공개 범위를 정한다"],
  },
},
],

javascript: [
{
  unit: "Web API 실무 — Observer·rAF·Worker (심화)", lesson: "Observer 계열 API",
  th: {
    sum: "Observer 는 '무슨 일이 생기면 알려 달라' 고 **등록해 두는** 방식이다. 계속 물어보지 않는다.",
    body: [
      { h: "계속 물어보면 느리다", t: "스크롤할 때마다 요소 위치를 재는 코드는 1초에 수십 번 돈다. 위치를 재는 일은 화면 계산을 강제하므로 그때마다 브라우저가 멈칫한다. Observer 는 브라우저가 알아서 알려 주므로 이 낭비가 없다." },
      { h: "화면에 들어왔는지 보기", t: "`IntersectionObserver` 는 요소가 화면에 보이기 시작하면 알려 준다. 이미지 늦게 불러오기나 무한 스크롤이 이걸로 만들어진다. 직접 위치를 계산하는 것보다 짧고 훨씬 싸다." },
      { h: "크기와 내용 변화 보기", t: "`ResizeObserver` 는 요소 크기가 바뀌면, `MutationObserver` 는 내용이 바뀌면 알려 준다. 창 크기 이벤트만으로는 요소 하나가 커진 것을 알 수 없어서, 예전에는 주기적으로 재는 수밖에 없었다." },
      { h: "끝나면 반드시 끊는다", t: "화면을 떠날 때 `disconnect()` 를 부르지 않으면 감시가 계속 남는다. 화면을 여러 번 들락거리면 그만큼 쌓여, 나중에는 한 번 움직일 때마다 여러 벌이 돈다." },
    ],
    code: { c: "const io = new IntersectionObserver((es) => { ... });\nio.observe(el);\n// 떠날 때\nio.disconnect();", cap: "물어보지 말고 등록해 둔다" },
    key: ["계속 재면 화면이 멈칫한다", "보임·크기·내용마다 전용 감시자", "떠날 때 `disconnect`"],
  },
},
{
  unit: "Web API 실무 — Observer·rAF·Worker (심화)", lesson: "프레임과 스케줄링",
  th: {
    sum: "브라우저는 1초에 약 60번 화면을 그린다. 애니메이션은 **그 박자에 맞춰야** 부드럽다.",
    body: [
      { h: "requestAnimationFrame 은 박자를 맞춰 준다", t: "다음 그리기 직전에 딱 한 번 불러 준다. `setInterval` 로 16밀리초마다 부르면 박자가 어긋나 어떤 프레임은 건너뛰고 어떤 프레임은 두 번 계산된다. 그래서 움직임이 툭툭 끊겨 보인다." },
      { h: "보이지 않으면 쉰다", t: "탭이 뒤로 가면 `requestAnimationFrame` 은 멈춘다. 배터리와 CPU 를 아끼기 위해서다. `setInterval` 은 계속 돌아서, 사용자가 보지도 않는 화면을 위해 자원을 쓴다." },
      { h: "재는 것과 바꾸는 것을 섞지 않는다", t: "위치를 재고 바로 스타일을 바꾸고 또 재면, 브라우저가 그때마다 화면 계산을 다시 한다. 한 프레임 안에서 '읽기 다 하고 → 쓰기 다 하기' 로 나누면 계산이 한 번으로 줄어든다." },
      { h: "긴 일은 쪼갠다", t: "한 프레임에 할 일이 16밀리초를 넘으면 그 프레임을 놓친다. 목록 1만 개를 한 번에 그리는 대신 나눠서 그리면, 총 시간은 비슷해도 화면은 계속 반응한다." },
    ],
    code: { c: "function tick() {\n  move();\n  requestAnimationFrame(tick);   // 다음 그리기에 맞춰\n}\nrequestAnimationFrame(tick);", cap: "그리는 박자에 맞춘다" },
    key: ["`rAF` 는 박자를 맞춘다", "안 보이면 쉰다", "읽기와 쓰기를 나눈다"],
  },
},
{
  unit: "Web API 실무 — Observer·rAF·Worker (심화)", lesson: "Worker와 데이터 전달",
  th: {
    sum: "Worker 는 **다른 일꾼**이다. 무거운 계산을 맡겨 두면 화면이 멈추지 않는다.",
    body: [
      { h: "왜 필요한가", t: "브라우저의 자바스크립트는 일꾼이 하나뿐이라, 오래 걸리는 계산이 돌면 그동안 클릭도 화면 갱신도 안 된다. Worker 에 맡기면 그 계산은 딴 데서 돌고, 화면은 계속 움직인다." },
      { h: "화면을 만질 수는 없다", t: "Worker 안에서는 `document` 를 쓸 수 없다. 화면은 여전히 원래 일꾼의 몫이다. 그래서 Worker 는 계산만 하고, 결과를 보내 주면 원래 쪽에서 화면에 반영한다." },
      { h: "값은 복사되어 건너간다", t: "`postMessage` 로 보낸 값은 복사본이다. 그래서 큰 자료를 자주 주고받으면 복사 비용이 계산으로 아낀 시간을 다 까먹는다. 보내는 양을 줄이거나, 소유권을 넘기는 방식을 쓴다." },
      { h: "함수는 보낼 수 없다", t: "복사할 수 있는 값만 건너간다. 함수나 DOM 요소를 넣으면 그 자리에서 오류가 난다. Worker 에는 숫자·문자열·배열처럼 단순한 자료만 보낸다." },
    ],
    code: { c: "const w = new Worker('calc.js');\nw.postMessage({ n: 1000000 });\nw.onmessage = (e) => show(e.data);", cap: "계산은 저쪽, 화면은 이쪽" },
    key: ["화면이 멈추지 않는다", "Worker 는 화면을 못 만진다", "값은 복사되어 건너간다"],
  },
},
],

};
