/* Linux 진단 유닛 — 서버에서 실제로 마주치는 증상들.
   'df 는 가득인데 du 로는 안 보인다' 처럼, 도구 두 개가 다른 답을 줄 때
   무엇을 믿어야 하는지를 아는 것이 진단의 절반이다.

   보기 규칙: 네 보기의 길이를 흩는다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const D=(k,q,o,a,ex)=>({t:"choice",cat:"debug",k,q,o,a,ex});
const I=(k,q,a,ex)=>({t:"input",cat:"debug",k,q,a,ex});

module.exports=shuffleUnits([

{ t:"진단 — 증상에서 원인으로", l:[

  { t:"자원이 없다고 한다", xp:40,
    th:{ sum:"'공간이 없다', '메모리가 없다' 는 메시지는 **대개 사실이지만 우리가 보는 곳이 틀렸다.** 도구 두 개가 다른 답을 줄 때가 진단의 출발점이다.",
      body:[
        {h:"df 와 du 가 다를 때", t:"`df` 는 파일 시스템이 보는 사용량이고 `du` 는 **이름이 붙어 있는 파일**의 합이다. 삭제되었지만 **아직 열려 있는 파일**은 이름이 없어 `du` 에 안 잡히면서 공간은 그대로 잡아먹는다. 로그를 `rm` 으로 지웠는데 공간이 안 돌아오는 사고가 정확히 이것이다 — 프로세스를 재시작하거나 파일을 열고 있는 쪽에 신호를 보내야 반환된다. 용량이 아니라 **inode** 가 고갈된 경우도 같은 메시지를 낸다."},
        {h:"메모리와 load 는 다른 것을 센다", t:"리눅스는 남는 메모리를 **파일 캐시로 전부 쓴다.** 그 캐시는 필요하면 즉시 회수되므로 `free` 열이 아니라 `available` 을 봐야 한다. load 는 CPU 뿐 아니라 **디스크를 기다리는 프로세스까지 센다** — CPU 는 한가한데 load 가 높으면 I/O 병목이다. 두 숫자 모두 **무엇과 비교하느냐**가 정해져야 판단이 된다."}],
      code:{ c:"$ df -h /   → 97% 사용\n$ du -sh /  → 절반뿐\n\n# 지웠지만 열려 있는 파일이 있다\n$ lsof +L1", cap:"이름이 없어도 공간은 잡아먹는다" },
      key:["df 와 du 가 다르면 열린 삭제 파일","용량 말고 inode 도 본다","free 말고 available","load 는 I/O 대기를 포함한다"] },
    q:[
      D("공간이 안 돌아온다","로그를 지웠는데 <code>df</code> 의 사용량이 그대로입니다. 원인으로 알맞은 것은?",
        ["프로세스가 그 파일을 아직 열고 있다",
         "파일 시스템의 사용량 캐시가 아직 갱신되지 않았다",
         "휴지통 디렉터리에 사본이 남아 있다",
         "지운 그 파일의 권한이 읽기 전용으로 되어 있었다"],0,
        "**열려 있는 파일은 지워도 공간이 반환되지 않습니다.** 이름만 사라지고 데이터는 그 프로세스가 닫을 때까지 남습니다.\n`lsof +L1` 로 찾을 수 있고, 해당 프로세스를 재시작하거나 로그 회전 신호를 보내면 돌아옵니다.\n그래서 로그는 `rm` 이 아니라 **옮긴 뒤 신호를 보내** 회전시킵니다."),
      D("공간은 있는데 못 만든다","디스크 여유가 충분한데 파일을 만들 수 없습니다. 확인할 것으로 알맞은 것은?",
        ["inode 사용량",
         "한 디렉터리에 담을 수 있는 최대 파일 개수 설정",
         "파일 시스템의 마운트 옵션",
         "사용자별 프로세스 수 제한"],0,
        "**inode 고갈**입니다. 작은 파일이 아주 많으면 용량은 남아도 파일을 더 만들 수 없습니다.\n`df -i` 로 확인합니다. 세션 파일이나 캐시 조각이 수백만 개 쌓인 경우가 대표적입니다.\n용량과 inode 는 각각 따로 잡히므로, '디스크 가득' 메시지를 볼 때 두 가지를 다 봐야 합니다."),
      D("메모리가 부족하다는데","<code>free</code> 열은 작은데 서비스는 잘 돌고 있습니다. 어떻게 판단해야 하나요?",
        ["available 을 봐야 실제 여유가 보인다",
         "free 열이 작으면 이미 위험한 상태라고 봐야 한다",
         "스왑 공간이 있으면 메모리는 문제가 되지 않는다",
         "캐시는 프로세스가 쓰는 메모리와 같다"],0,
        "리눅스는 남는 메모리를 **파일 캐시로 전부 씁니다.** 그래서 `free` 열은 늘 작게 보이는 것이 정상입니다.\n캐시는 필요하면 즉시 회수되므로 사실상 여유 공간이고, 그 값이 `available` 입니다.\n진짜 부족은 `available` 이 작아지고 스왑 입출력이 늘어날 때 드러납니다."),
      D("load 는 높은데","load 가 8인데 CPU 사용률은 낮습니다. 가장 그럴듯한 원인은?",
        ["디스크나 네트워크를 기다리는 프로세스가 많다",
         "코어 수가 부족해 계산이 뒤로 밀려 쌓여 있다",
         "메모리가 모자라 스왑 입출력이 계속 일어나고 있다",
         "프로세스 수가 상한에 걸려 있다"],0,
        "리눅스의 load 는 CPU 를 쓰는 프로세스뿐 아니라 **디스크를 기다리는 프로세스까지 셉니다.**\nCPU 가 한가한데 load 가 높으면 거의 언제나 I/O 병목입니다. `iostat` 이나 `iotop` 으로 확인합니다.\nload 자체는 코어 수와 비교해야 뜻이 생깁니다 — 코어 32개의 8은 한가하고 코어 1개의 4는 심각합니다."),
      D("열 수 있는 파일이 없다","<code>Too many open files</code> 오류가 납니다. 확인할 것으로 알맞은 것은?",
        ["프로세스의 파일 디스크립터 상한",
         "마운트된 디스크의 남은 용량",
         "파일 시스템의 종류",
         "파일의 소유자와 권한"],0,
        "**파일 디스크립터 상한(ulimit -n)** 에 걸린 것입니다. 소켓도 디스크립터를 쓰므로 연결이 많은 서버에서 흔합니다.\n닫지 않은 파일이나 소켓이 쌓이는 누수일 수도 있으니, 상한을 올리기 전에 `lsof -p` 로 무엇이 열려 있는지 봅니다.\nsystemd 서비스는 유닛 파일의 `LimitNOFILE` 로 따로 정해야 합니다 — 셸에서 올린 값은 적용되지 않습니다."),
      D("죽였는데 포트가 잡혀 있다","프로세스를 죽였는데 포트가 여전히 사용 중입니다. 원인으로 알맞은 것은?",
        ["자식 프로세스가 살아남아 포트를 물고 있다",
         "한 번 쓴 포트는 재부팅 전까지 해제되지 않는다",
         "방화벽이 포트를 잠가 두었다",
         "그 포트 번호가 시스템 예약 범위에 들어 있다"],0,
        "**부모만 죽이면 자식은 살아남습니다.** Git 이나 셸에서 띄운 프로세스가 자식을 남기는 경우가 흔합니다.\n`ss -lptn` 으로 실제로 누가 물고 있는지 확인하고 그 PID 를 처리합니다.\nsystemd 가 cgroup 으로 묶어 관리하는 이유가 이것입니다 — 서비스를 멈추면 자식까지 함께 정리됩니다."),
      D("/tmp 가 가득","<code>/</code> 는 여유가 있는데 <code>/tmp</code> 에 쓸 수 없습니다. 원인으로 알맞은 것은?",
        ["/tmp 가 별도 파일 시스템으로 마운트되어 있다",
         "/tmp 의 권한이 잘못 설정되어 있다",
         "/tmp 는 원래 일반 사용자의 쓰기가 제한된 디렉터리다",
         "/tmp 안의 파일은 일정 시간 뒤 자동으로 지워지기 때문"],0,
        "**별도로 마운트된 파일 시스템**이면 `/` 의 여유와 무관합니다. 메모리 기반(tmpfs)으로 잡혀 크기가 작은 경우도 흔합니다.\n`df -h /tmp` 로 그 지점의 사용량을 따로 봐야 합니다.\n`mount` 나 `findmnt` 로 어디가 어떻게 붙어 있는지 확인하는 것이 진단의 첫 단계입니다."),
      I("열린 삭제 파일","지워졌지만 아직 열려 있어 공간을 잡고 있는 파일을 찾는 명령은? (옵션 포함, 영어)",
        ["lsof +L1","lsof +L1 ","lsof"],
        "정답은 **lsof +L1** 입니다. 링크 수가 1보다 작은 파일, 즉 이름이 사라진 파일만 보여 줍니다.\n어느 프로세스가 물고 있는지 함께 나오므로 재시작하거나 신호를 보낼 대상이 바로 정해집니다.\n`df` 와 `du` 의 답이 다를 때 가장 먼저 돌려 볼 명령입니다.")
    ] },

  { t:"연결과 권한이 안 된다", xp:40,
    th:{ sum:"'권한 없음' 과 '연결 안 됨' 은 원인이 여러 겹이라, **바깥쪽부터 안쪽으로** 좁혀 가는 순서를 정해 두면 대개 몇 분 안에 끝난다.",
      body:[
        {h:"권한은 파일만의 문제가 아니다", t:"파일 권한이 맞는데도 막힌다면 **상위 디렉터리의 실행 권한(x)** 을 보아야 한다. 디렉터리의 `x` 는 '들어갈 수 있다' 는 뜻이라, 중간 한 곳만 막혀도 안쪽 파일에 닿지 못한다. SSH 키가 거부되는 것도 비슷하다 — 개인키가 남에게 읽히면 ssh 가 **아예 사용을 거부한다.** `600` 이 필요한 이유다."},
        {h:"환경이 다르면 결과가 다르다", t:"손으로는 되는데 cron 이나 systemd 에서 안 되는 일의 대부분은 **환경 변수**다. cron 은 아주 짧은 `PATH` 만 주고 셸 설정 파일을 읽지 않는다. 스크립트에서는 **절대 경로를 쓰거나 PATH 를 직접 정하고**, 로그를 남겨 실제로 어떤 환경에서 돌았는지 확인할 수 있게 한다."}],
      code:{ c:"$ ls -l app.conf   → -rw-r--r-- 읽을 수 있어 보인다\n$ namei -l /srv/app/app.conf\n  drwx------ srv   ← 여기서 막힌다\n\n# 디렉터리의 x 가 없으면 안쪽에 닿지 못한다", cap:"권한은 경로 전체를 본다" },
      key:["상위 디렉터리의 x 를 확인한다","개인키는 600 이라야 쓰인다","cron 은 PATH 가 다르다","바깥에서 안으로 좁힌다"] },
    q:[
      D("권한이 맞는데 거부","파일 권한은 맞는데 <code>Permission denied</code> 가 납니다. 확인할 것으로 알맞은 것은?",
        ["상위 디렉터리의 실행 권한",
         "파일의 확장자와 형식",
         "그 파일이 놓인 파일 시스템의 남은 용량",
         "그 파일을 만든 프로세스가 아직 살아 있는지"],0,
        "디렉터리의 **`x`(들어가기)** 가 없으면 안쪽 파일에 닿지 못합니다. 경로 중간 한 곳만 막혀도 그렇습니다.\n`namei -l <경로>` 로 각 단계의 권한을 한눈에 볼 수 있습니다.\nSELinux 나 AppArmor 같은 강제 접근 제어가 켜져 있으면 권한이 다 맞아도 막히므로 그것도 후보입니다."),
      D("SSH 키가 거부된다","키를 등록했는데 로그인이 거부됩니다. 먼저 볼 것으로 알맞은 것은?",
        ["개인키 파일의 권한이 600 인지",
         "키의 길이가 충분히 긴지",
         "접속하려는 서버의 시간이 정확히 맞는지",
         "개인키 파일의 이름이 정해진 규칙에 맞는지"],0,
        "**개인키가 남에게 읽히면 ssh 가 사용을 거부합니다.** `600` 이 아니면 조용히 건너뛰고 다음 방법으로 넘어갑니다.\n서버 쪽 `~/.ssh` 와 `authorized_keys` 의 권한도 함께 봐야 합니다 — 그쪽이 느슨해도 거부됩니다.\n`ssh -v` 로 어느 키를 시도하고 왜 거부되었는지 그대로 볼 수 있습니다."),
      D("cron 에서만 실패","손으로는 되는 스크립트가 cron 에서만 실패합니다. 가장 그럴듯한 원인은?",
        ["cron 의 PATH 가 짧아 명령을 찾지 못한다",
         "cron 은 스크립트에 붙은 실행 권한을 무시한다",
         "cron 은 표준 출력을 쓸 수 없다",
         "cron 은 상대 경로만 허용한다"],0,
        "**환경 변수 차이**가 압도적으로 흔합니다. cron 은 아주 짧은 `PATH` 만 주고 셸 설정 파일을 읽지 않습니다.\n절대 경로를 쓰거나 스크립트 안에서 `PATH` 를 직접 정하면 해결됩니다.\n언어 설정(`LANG`)이 달라 정렬이나 날짜 형식이 바뀌는 경우도 같은 종류의 함정입니다."),
      D("포트를 못 연다","1024 미만 포트에 바인딩하려는데 실패합니다. 원인으로 알맞은 것은?",
        ["특권 포트라 권한이 필요하다",
         "그 포트 번호는 시스템 예약이라 쓸 수 없다",
         "방화벽이 그 포트를 막고 있다",
         "다른 프로세스가 이미 쓰고 있다"],0,
        "**1024 미만은 특권 포트**라 루트 권한이나 별도의 능력(capability)이 필요합니다.\n서비스를 루트로 돌리는 대신 `CAP_NET_BIND_SERVICE` 만 주거나, 앞에 리버스 프록시를 두는 것이 안전한 방법입니다.\n이미 쓰이는 중이라면 오류 메시지가 '주소가 이미 사용 중' 으로 달라 구분됩니다."),
      D("명령을 못 찾는다","설치한 명령이 <code>command not found</code> 로 나옵니다. 확인할 것으로 알맞은 것은?",
        ["설치 경로가 PATH 에 들어 있는지",
         "그 명령 파일의 소유자가 지금 사용자와 같은지",
         "설치한 그 명령이 최신 버전인지",
         "터미널 종류가 맞는지"],0,
        "**PATH 에 없는 곳에 설치된 것**이 가장 흔합니다. `/usr/local/bin` 이나 사용자 홈 아래에 설치되는 경우가 많습니다.\n`which` 와 `command -v` 로 확인하고, 없으면 `find` 로 실제 위치를 찾아 PATH 에 더합니다.\n파일은 있는데 실행 권한이 없으면 메시지가 `Permission denied` 로 달라집니다."),
      D("링크가 깨졌다","심볼릭 링크를 따라가면 파일이 없다고 나옵니다. 원인으로 알맞은 것은?",
        ["링크가 가리키는 대상이 사라졌거나 경로가 어긋났다",
         "심볼릭 링크는 재부팅하고 나면 끊어진다",
         "링크는 같은 파일 시스템 안에서만 동작한다",
         "링크의 권한이 대상과 달라졌다"],0,
        "**대상이 없어졌거나 상대 경로가 어긋난 것**입니다. 상대 경로 링크는 옮기면 그대로 깨집니다.\n`ls -l` 로 어디를 가리키는지 보고 `readlink -f` 로 최종 경로를 확인합니다.\n심볼릭 링크는 파일 시스템을 넘어갈 수 있습니다 — 그 제약이 있는 것은 하드 링크입니다."),
      D("어디부터 볼까","서버에 접속이 안 될 때 확인 순서로 알맞은 것은?",
        ["경로 → 방화벽 → 서비스가 떠 있는지 → 서비스 설정",
         "서비스 설정부터 확인하고 문제가 없으면 재부팅해 본다",
         "서버 사양을 올려 보고 그래도 안 되면 조사한다",
         "로그를 전부 내려받아 분석한 뒤 판단한다"],0,
        "**바깥에서 안으로** 좁혀 갑니다. 경로가 없으면 나머지를 아무리 확인해도 소용없습니다.\n`ping`·`traceroute` 로 닿는지, `ss -lntp` 로 실제로 듣고 있는지 순서대로 봅니다.\n재부팅은 원인을 지우면서 증상만 없애는 일이 잦아, 마지막 수단으로 둡니다."),
      I("누가 쓰고 있나","어떤 포트를 어떤 프로세스가 듣고 있는지 보는 명령은? (옵션 포함, 영어)",
        ["ss -lptn","ss -tlnp","ss -lntp","netstat -lptn"],
        "정답은 **ss -lptn**(옵션 순서는 달라도 됩니다) 입니다. 듣고 있는 TCP 소켓과 그 프로세스를 함께 보여 줍니다.\n프로세스 이름을 보려면 대개 루트 권한이 필요합니다.\n예전에는 `netstat` 을 썼지만 지금은 `ss` 가 표준이고 훨씬 빠릅니다.")
    ] }
]}

]);
