/* javascript 트랙 — 정답이 5자 이상 단독 최장인 15문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "javascript", fixes: [
  { k: "js-tree-req", o: [
    "번들 크기가 특정 임계값을 넘어 번들러가 최적화 모드로 전환될 것 — 작은 번들은 분석 비용 때문에 건너뛴다",
    "모든 함수가 화살표 함수 형태로 작성되어 있을 것 — 일반 함수는 this 바인딩 때문에 제거 여부를 판단할 수 없다",
    "정적으로 분석 가능한 ESM 구조이고, 제거될 코드에 부수 효과가 없다고 판단될 것",
    "코드가 minify 되어 있을 것 — 이름이 짧아진 뒤에야 사용되지 않는 식별자를 찾을 수 있기 때문이다" ] },
  { k: "fe2-cl-let-block", o: [
    "for 헤더의 let 선언은 호이스팅되지 않아 반복마다 새로 선언되기 때문",
    "let 은 var 보다 빠르게 처리되어 클로저가 값을 놓치기 전에 캡처하기 때문",
    "let 은 클로저가 변수를 공유하는 것을 금지해 매번 복사본을 주기 때문",
    "for 헤더의 let은 반복마다 새 렉시컬 환경을 만듦" ] },
  { k: "js2-g2-03", o: [
    "키가 삽입 순서대로 순회된다 — for...in 과 같은 열거 규칙이 for...of 에도 적용되기 때문이다",
    "[키, 값] 쌍이 차례로 순회된다 — 일반 객체도 배열처럼 순서대로 돌 수 있게 되어 있기 때문이다",
    "값이 순서대로 순회된다 — 객체의 값 목록이 암묵적으로 이터러블로 취급되기 때문이다",
    "TypeError: obj is not iterable — Symbol.iterator가 없기 때문이다" ] },
  { k: "js2-w7-02", o: [
    "IntersectionObserver 가 별도 워커 스레드에서 JS 콜백을 실행해 주므로 메인 스레드가 막히지 않기 때문",
    "매 스크롤마다 강제 동기 레이아웃을 일으키지 않고, 교차 상태가 바뀔 때만 비동기로 통지받기 때문",
    "scroll 이벤트는 최신 브라우저에서 지원이 중단되어 IntersectionObserver 만이 유일한 대안이기 때문",
    "IntersectionObserver 는 첫 계산 결과를 캐싱해 두고 다시 계산하지 않으므로 비용이 거의 없기 때문" ] },
  { k: "js2-r3-15", o: [
    "HTML 은 태그 이름의 대소문자를 구분하지 않아 패턴이 두 배로 늘어나기 때문",
    "JS 정규식에는 태그를 인식하는 전용 문법이 없어 문자 단위로만 맞출 수 있기 때문",
    "정규식은 대상 문자열의 길이가 1000자를 넘어가면 동작하지 않도록 제한되어 있기 때문",
    "임의 깊이의 중첩 구조는 정규 언어가 아니라 정규식으로 정확히 표현할 수 없기 때문" ] },
  { k: "js-retry-when", o: [
    "429·503·네트워크 오류는 재시도 가치가 있고, 400·401·404는 다시 보내도 결과가 같다",
    "POST 는 서버가 처리하지 못했을 때 다시 보내야 하고, GET 은 캐시가 응답하므로 재시도가 의미 없다",
    "5xx 응답은 서버 쪽 잘못이므로 재시도하면 안 되고 4xx 쪽만 다시 재시도해야 한다",
    "모든 실패는 원인과 무관하게 3회까지 재시도하는 것이 안전하고 구현도 단순하다" ] },
  { k: "라이브바인딩", o: [
    "import 선언은 런타임에 값으로 평가되므로 호이스팅되지 않고 적힌 자리에서 실행된다",
    "import 된 값은 모듈 평가 시점의 복사본이라 원본이 바뀌어도 그대로 남는다",
    "import 한 변수는 일반 변수와 같아 필요하면 재할당해 다른 값을 넣을 수 있다",
    "import 바인딩은 라이브(live)라 export한 모듈이 값을 바꾸면 반영된다" ] },
  { k: "js2-g2-01", o: [
    "length 프로퍼티와 0 부터 시작하는 숫자 인덱스 — for...of 는 그것으로 원소를 센다",
    "Array.prototype 을 상속받고 있어야 한다는 것 — 배열이 아닌 값은 아예 순회할 수 없기 때문",
    "Symbol.iterator 메서드가 { value, done }을 반환하는 next()를 가진 객체를 돌려주는 것",
    "forEach 메서드 — for...of 는 내부적으로 forEach 를 호출해 각 원소를 넘겨받는다" ] },
  { k: "js2-g2-15", o: [
    "yield* 는 비동기 제너레이터 안에서는 쓸 수 없으므로 이 코드는 문법 오류로 실행되지 않는다",
    "필요한 페이지만 순차적으로 요청하고, break하면 다음 페이지 요청을 하지 않는다",
    "모든 페이지를 먼저 받아 배열에 모은 뒤 순회하므로 첫 항목이 나오기까지 오래 걸린다",
    "모든 페이지를 동시에 병렬로 요청해 가장 빠르지만 서버에 부하를 한꺼번에 준다" ] },
  { k: "js2-t8-14", o: [
    "as 는 런타임에 객체를 User 형태로 변환하는 작업을 수행하므로 큰 응답에서는 성능이 나쁘다",
    "as는 검사 없이 컴파일러를 설득할 뿐이라, 실제 데이터가 다르면 이후 코드가 런타임에 터진다",
    "as 는 JSON.parse 의 결과인 any 타입에는 사용할 수 없어 컴파일 오류가 나므로 빌드가 실패한다",
    "as 를 쓰면 User 의 모든 필드가 옵셔널로 바뀌어 이후 접근마다 undefined 검사가 필요해진다" ] },
  { k: "js2-n5-03", o: [
    "SyntaxError — 안전 정수 범위를 넘는 리터럴은 파서가 거부한다",
    "false — 아주 큰 정수는 자동으로 BigInt 타입으로 승격되기 때문",
    "true — 안전 정수 범위(2^53-1)를 넘어 두 값이 같은 부동소수로 표현된다",
    "false — 서로 다른 정수 리터럴이므로 값이 달라 같을 수 없다" ] },
  { k: "js-fetch-preflight", o: [
    "GET 요청에서만 프리플라이트가 발생한다 — 읽기 요청은 캐시될 수 있어 브라우저가 먼저 서버의 허용 여부를 확인해야 한다",
    "HTTPS 요청에서만 프리플라이트가 발생한다 — 암호화된 연결에서는 인증서와 함께 CORS 정책도 사전 협상 대상이다",
    "모든 교차 오리진 요청은 프리플라이트를 먼저 보낸다 — 오리진이 다르면 브라우저가 예외 없이 OPTIONS 로 허용 여부를 묻는다",
    "PUT·DELETE 같은 메서드이거나 Content-Type: application/json, 커스텀 헤더처럼 단순 요청 조건을 벗어날 때" ] },
  { k: "js2-g2-09", o: [
    "{ value: undefined, done: true } — 소진된 제너레이터는 되감을 수 없다",
    "TypeError: generator is already done — 끝난 제너레이터는 다시 부를 수 없다",
    "{ value: 1, done: false } — 소진되어도 다시 처음부터 시작하기 때문",
    "null — 더 줄 값이 없으므로 이터레이터 결과 객체 대신 null 을 돌려준다" ] },
  { k: "js2-g2-13", o: [
    "배열 메서드는 1억 건이 넘는 배열을 한 번에 처리할 수 없어 예외가 나기 때문",
    "필요한 10건이 나올 때까지만 계산하며 중간 배열을 만들지 않기 때문",
    "제너레이터가 여러 CPU 코어를 써서 필터링을 병렬로 처리하기 때문",
    "제너레이터는 한 번 계산한 결과를 자동으로 캐싱해 다시 쓰기 때문" ] },
  { k: "js2-w7-05", o: [
    "콜백 안에서 관찰 대상의 크기를 건드리는 것은 명세가 금지하므로 즉시 TypeError 가 발생해 콜백이 중단된다",
    "브라우저가 순환을 감지하면 observer 를 자동으로 해제해 관찰 자체를 끊어 버리므로 이후 변화는 통지되지 않는다",
    "콜백 안의 크기 변경은 브라우저가 무시하고 원래 크기로 되돌리므로 화면에는 아무 효과가 나타나지 않는다",
    "크기 변경이 또 콜백을 유발하는 순환이 생겨 'ResizeObserver loop' 경고가 나거나 프레임이 낭비된다" ] }
] };
