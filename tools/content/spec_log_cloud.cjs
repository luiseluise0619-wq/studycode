module.exports = {
  track: "cloud", guide: "", xp: 70,
  unit: "로그 분석 — 관리형 서비스가 남긴 것",
  source: "./log_ops.cjs", slice: [10, 20],
  lessons: [
    { t: "판정과 시차", n: 5,
      th: { sum: "관리형 서비스의 사고는 대개 **판정 기준**이나 **기다려 주는 시간**이 현실과 어긋나 생긴다.",
        body: [
          { h: "누가 무엇을 보고 판정하는가", t: "로드밸런서가 '건강하지 않다' 고 말할 때, 그것은 앱이 죽었다는 뜻이 아니라 <b>내가 정한 기준을 못 맞췄다</b>는 뜻입니다. 헬스 체크 경로가 인증 뒤에 있으면 앱은 규칙대로 로그인으로 보내고 판정은 실패가 됩니다. 그래서 이런 로그를 읽을 때는 <b>알리바이가 되는 줄</b>을 먼저 찾습니다 — 다른 경로로 들어온 요청이 200 을 받고 있다면 앱은 살아 있는 것이고, 남은 것은 판정 기준의 문제입니다." },
          { h: "준비되는 시간을 감안했는가", t: "새 인스턴스가 부팅을 마치기 전에 유예 시간이 끝나면, 아직 뜨지 않은 앱이 '고장' 으로 판정돼 죽고 또 띄우는 고리가 돕니다. 서버리스 함수도 같은 이야기입니다 — 준비(<code>init</code>)에 걸린 시간이 제한 시간을 먹어 버리면 <b>일을 시작하기도 전에</b> 초과가 납니다. 큐의 감춤 시간도 그렇습니다: 처리 시간보다 짧으면 아직 일하는 중인 메시지가 다시 나가 <b>두 번 처리</b>됩니다. 셋 다 계산은 쉽고 어려운 것은 시차입니다." }],
        code: { c: "health_check: HTTP:8080/health   # 인증 밖에 두기\ngrace_period > 실제 부팅 시간\nvisibility_timeout > 실제 처리 시간", cap: "판정 기준과 기다림을 현실에 맞춘다" },
        key: ["건강하지 않다 = 기준을 못 맞췄다", "유예 시간이 짧으면 고리가 돈다", "init 시간도 제한 시간을 먹는다", "감춤 시간이 짧으면 두 번 처리"] } },
    { t: "권한 · 경로 · 한도", n: 5,
      th: { sum: "'분명히 되는데 안 된다' 는 대개 **허가가 둘**이거나, **트래픽이 엉뚱한 길**로 가거나, **다른 층의 한도**에 걸린 것이다.",
        body: [
          { h: "허가는 하나가 아니다", t: "암호화된 버킷에 파일을 올리는 일은 저장소 권한과 키 사용 권한을 <b>둘 다</b> 요구합니다. 정책 시뮬레이터는 앞쪽만 보므로 통과라고 말하고, 결과는 그대로 거부입니다. 구별하는 법은 감사 로그를 보는 것입니다 — 누가 거부했는지가 거기 적혀 있습니다. 데이터베이스 연결도 비슷한 층 문제입니다: 함수는 부하만큼 늘어나도록 만들어졌고 데이터베이스는 연결 수가 정해져 있어, 늘어남 자체가 한도를 넘깁니다." },
          { h: "지나간 길이 요금을 정한다", t: "같은 계정의 객체 저장소로 보낸 트래픽이라도 <b>어느 길로 나갔는지</b>에 따라 요금이 완전히 달라집니다. 게이트웨이를 지나면 GB 마다 처리 요금이 붙고, 엔드포인트를 두면 그 항목이 거의 0이 됩니다. 쓰기 용량도 길과 비슷합니다 — 테이블 전체에 걸어 둔 용량이라도 실제로 일하는 것은 조각 하나하나라, 한 열쇠로 몰리면 <b>평균은 한가한데</b> 거절이 납니다. 배포 중 실패율이 정확히 절반이라면 두 판본이 반반 떠 있다는 뜻입니다." }],
        code: { c: "# 저장소 권한만이 아니라 키 권한도\nkms:GenerateDataKey on key/4f2a\n# 그리고 지나가는 길\nvpc endpoint com.amazonaws.<region>.s3", cap: "허가와 길과 한도를 따로 본다" },
        key: ["암호화 버킷은 허가가 둘", "감사 로그가 거부한 쪽을 말한다", "게이트웨이를 지나면 GB 마다 돈", "평균 용량은 쏠림을 감춘다"] } },
  ],
};
