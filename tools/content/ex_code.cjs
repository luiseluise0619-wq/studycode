/* code 트랙(직접 짜기) 짧은 해설 다시 쓰기 — 어떻게 푸는지와 흔히 틀리는 지점을 같이 적는다 */
module.exports = { track: "code", fixes: [
  { k: "코딩 과제", ex0: "console.log(\"Hello World\"); — 따옴표로", ex: "글자를 출력하려면 console.log(\"Hello World\"); 처럼 따옴표로 감싼다. 따옴표가 없으면 Hello 와 World 를 변수 이름으로 보고 '정의되지 않았다' 는 오류가 난다. 작은따옴표와 큰따옴표 어느 쪽이든 되지만 시작과 끝을 같은 것으로 맞춰야 한다." },
  { k: "코딩 과제", ex0: "숫자는 따옴표 없이 console.log(42)", ex: "숫자는 따옴표 없이 console.log(42); 로 출력한다. \"42\" 처럼 따옴표로 감싸면 화면에는 같아 보여도 문자열이라 42 + 1 이 421 이 되는 식으로 계산이 달라진다. 지금 과제는 숫자 값 자체를 출력하는 것이므로 따옴표를 빼야 한다." },
  { k: "코딩 과제", ex0: "console.log(10 + 5);", ex: "console.log(10 + 5); 를 쓰면 괄호 안의 식이 먼저 계산되어 15 가 찍힌다. \"10 + 5\" 처럼 따옴표로 감싸면 계산하지 않고 글자 그대로 10 + 5 가 나오므로 틀린다. 결과 15 를 직접 적어도 통과는 하지만 계산을 시키는 것이 과제의 뜻이다." },
  { k: "코딩 과제", ex0: "let city = \"Seoul\";", ex: "let city = \"Seoul\"; 로 변수를 만들고 console.log(city); 로 출력한다. 출력할 때 console.log(\"city\") 처럼 따옴표를 붙이면 변수의 값이 아니라 city 라는 글자가 찍히므로 주의한다. 값을 바꾸지 않을 것이라면 const 를 써도 된다." },
  { k: "코딩 과제", ex0: "console.log(a * b);", ex: "곱하기는 * 기호다. let a = 7, b = 3; 을 두고 console.log(a * b); 를 쓰면 21 이 나온다. x 나 × 를 쓰면 문법 오류이고, \"a * b\" 처럼 따옴표로 감싸면 글자가 그대로 찍힌다. 21 을 직접 쓰는 대신 변수를 곱해 계산시키는 것이 과제의 목적이다." },
  { k: "코딩 과제", ex0: "문자열 + 로 이어붙여요", ex: "문자열끼리 + 를 쓰면 이어붙여진다. console.log(\"안녕\" + \"코딩\"); 은 안녕코딩 을 출력한다. 쉼표로 나누어 console.log(\"안녕\", \"코딩\") 이라고 쓰면 사이에 공백이 들어가 '안녕 코딩' 이 되므로 다르다. 공백 없이 붙이려면 + 를 써야 한다." },
  { k: "코딩 과제", ex0: "console.log(price * count);", ex: "가격과 개수를 변수에 담고 console.log(price * count); 로 곱해 3000 을 출력한다. 1000 * 3 을 바로 써도 답은 같지만 변수를 두면 값이 바뀌어도 코드를 고치지 않아도 된다. 더하기 + 를 쓰면 4000 이 아니라 1003 이 되니 연산자를 확인한다." },
  { k: "코딩 과제", ex0: "if (x >= 10) 으로 비교", ex: "'10 이상' 은 10 도 포함하므로 if (x >= 10) 으로 비교한다. x > 10 이라고 쓰면 x 가 정확히 10 일 때 출력되지 않아 틀린다. = 하나만 쓰면 비교가 아니라 대입이 되어 항상 참이 되니 >= 로 정확히 적는다. 부등호와 등호의 순서는 >= 이고 => 는 화살표 함수라 다르다." },
  { k: "코딩 과제", ex0: "if (score < 60) 로 비교", ex: "'60 미만' 은 60 을 포함하지 않으므로 if (score < 60) 이다. score <= 60 으로 쓰면 60 점도 불합격이 되어 조건이 어긋난다. 45 는 60 보다 작으므로 조건이 참이 되어 \"불합격\" 이 출력된다. 출력문은 중괄호 { } 안에 넣어야 조건에 묶인다." },
  { k: "코딩 과제", ex0: "console.log(\"짝수\");", ex: "짝수인지는 2 로 나눈 나머지가 0 인지로 판단한다. if (n % 2 === 0) { console.log(\"짝수\"); } 이다. n / 2 === 0 은 나눗셈이라 n 이 0 일 때만 참이고, n % 2 === 1 은 홀수 조건이다. 4 % 2 는 0 이므로 조건이 참이 되어 짝수 가 찍힌다." },
  { k: "코딩 과제", ex0: "i <= 5 까지 반복", ex: "for (let i = 1; i <= 5; i++) { console.log(i); } 처럼 1 에서 시작해 5 까지 포함하도록 i <= 5 로 조건을 둔다. i < 5 로 쓰면 4 까지만 나오고, i = 0 에서 시작하면 0 이 먼저 찍힌다. 시작값과 조건의 등호를 함께 확인해야 한다." },
  { k: "코딩 과제", ex0: "console.log(\"별\");", ex: "같은 출력을 4 번 반복하려면 for (let i = 0; i < 4; i++) { console.log(\"별\"); } 처럼 반복문 안에 출력을 넣는다. i < 4 이면 0, 1, 2, 3 의 네 번 돈다. i <= 4 로 쓰면 다섯 번 찍히고, 출력을 반복문 밖에 두면 한 번만 나온다." },
  { k: "코딩 과제", ex0: "console.log(sum);", ex: "합을 담을 변수를 0 으로 시작해 let sum = 0; 반복문 안에서 sum += i; 로 더한 뒤 반복이 끝난 밖에서 console.log(sum); 을 한다. 출력을 반복문 안에 두면 중간 합이 10 번 찍히고, sum 을 초기화하지 않으면 undefined 와 더해져 NaN 이 된다. 1 부터 10 까지 포함하려면 i <= 10 이다." },
  { k: "코딩 과제", ex0: "i = i + 2 로 2씩 증가", ex: "짝수만 출력하려면 for (let i = 2; i <= 10; i += 2) 처럼 2 에서 시작해 2 씩 늘린다. i++ 로 1 씩 늘리면서 if (i % 2 === 0) 로 걸러도 같은 결과다. 시작값을 1 로 두고 2 씩 늘리면 홀수가 나오고, 조건을 i < 10 으로 두면 10 이 빠지니 주의한다." },
  { k: "코딩 과제", ex0: "return a + b;", ex: "함수 안에서 return a + b; 로 두 수의 합을 돌려준다. console.log(a + b) 만 쓰면 화면에는 찍히지만 돌려주는 값이 없어 add(3, 4) 의 결과가 undefined 가 된다. 마지막에 console.log(add(3, 4)); 를 호출해야 7 이 출력된다." },
  { k: "코딩 과제", ex0: "return n * n;", ex: "제곱은 자기 자신을 곱한 것이므로 return n * n; 이다. n ** 2 나 Math.pow(n, 2) 도 같다. n * 2 는 두 배라 12 가 나와 틀린다. 함수가 값을 돌려줘야 console.log(square(6)); 에서 36 이 찍히므로 return 을 빠뜨리지 않는다." },
  { k: "코딩 과제", ex0: "함수가 인사말을 만들어 돌려줘요", ex: "greet 함수는 이름을 받아 인사말 문자열을 만들어 return 으로 돌려준다. 그래서 console.log(greet(\"철수\")) 처럼 호출 결과를 출력해야 화면에 보인다. 함수를 정의만 하고 호출하지 않으면 아무것도 찍히지 않고, greet 뒤에 괄호를 빼면 함수 본문이 아니라 함수 자체가 출력된다." },
  { k: "코딩 과제", ex0: "인덱스는 0부터! arr[0]", ex: "배열의 첫 번째 값은 인덱스 0 이므로 console.log(arr[0]); 이 10 을 출력한다. arr[1] 은 두 번째 값 20 이다. 대괄호 대신 소괄호 arr(0) 을 쓰면 함수 호출로 해석되어 오류가 나고, arr.first 같은 속성은 배열에 없다." },
  { k: "코딩 과제", ex0: "각 원소를 더하면 30이에요", ex: "let sum = 0; 을 두고 for 문으로 배열을 돌며 sum += arr[i]; 를 하면 5 + 10 + 15 = 30 이 된다. 조건은 i < arr.length 로 두어야 마지막 원소까지 더하고 범위를 넘지 않는다. 출력은 반복이 끝난 뒤 한 번만 해야 중간 합이 여러 줄 찍히지 않는다." },
  { k: "코딩 과제", ex0: "return x > 3;", ex: "filter 는 콜백이 true 를 돌려주는 원소만 남긴다. arr.filter(x => x > 3) 이나 function(x) { return x > 3; } 처럼 조건을 돌려주면 [4, 5] 가 남는다. x >= 3 으로 쓰면 3 도 포함되어 [3, 4, 5] 가 되고, return 을 빼면 undefined 가 돌아가 빈 배열이 된다." },
  { k: "코딩 과제", ex0: "arr.length 로 개수를 구해요", ex: "배열의 개수는 arr.length 속성으로 얻는다. 괄호 없이 쓴다. arr.length() 처럼 괄호를 붙이면 함수가 아니라는 오류가 나고, len(arr) 은 Python 문법이며, arr.size 는 undefined 다. [3, 1, 2] 는 원소가 세 개이므로 3 이 출력된다." }
] };
