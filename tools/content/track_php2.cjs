/* PHP 트랙 후반부 — 유닛 7~12. 형식은 track_php1.cjs 와 같다. */

const C=(k,q,o,a,ex,p)=>{ const o2={t:"choice",k,q,o,a,ex}; if(p) o2._p=p; return o2; };
const I=(k,q,a,ex)=>({t:"input",k,q,a,ex});
const P=(code,want,pre)=>({code,want,pre});

module.exports=[

/* ═══════════════ 유닛 7 ═══════════════ */
{ t:"네임스페이스 · Composer · 오토로딩", l:[

  { t:"네임스페이스와 use", xp:25,
    th:{ sum:"네임스페이스는 **이름 충돌을 막는 접두어**다. 그리고 오토로딩과 짝을 이뤄 '이름만 보고 파일을 찾는' 규칙이 된다.",
      body:[
        {h:"이름은 경로다", t:"`App\\Service\\Mailer` 는 PSR-4 규칙에서 `src/Service/Mailer.php` 로 대응된다 — **네임스페이스가 곧 디렉터리 구조**다. `use App\\Service\\Mailer;` 는 파일을 불러오는 것이 아니라 **긴 이름에 별칭을 붙이는 것**뿐이고, 실제 로딩은 오토로더가 한다. 파일 하나에는 클래스 하나, 이름은 파일명과 같게 두는 것이 규칙이다."},
        {h:"백슬래시의 위치", t:"네임스페이스 안에서 `strlen()` 을 부르면 PHP 는 먼저 `App\\strlen` 을 찾고 없으면 전역으로 떨어진다 — 그래서 내장 함수 호출은 `\\strlen()` 처럼 앞에 백슬래시를 붙이면 조회 단계를 건너뛴다. 클래스는 이런 폴백이 **없어서**, 네임스페이스 안에서 `new Exception()` 은 `App\\Exception` 을 찾다 실패한다. `use Exception;` 이나 `new \\Exception()` 이 필요하다."}],
      code:{ c:"namespace App\\Service;\n\nuse App\\Mail\\Sender;      // 별칭일 뿐, 로딩은 오토로더가 한다\nuse RuntimeException;      // 전역 클래스도 use 해야 한다\n\nfinal class Mailer { … }", cap:"클래스에는 전역 폴백이 없다" },
      key:["PSR-4: 네임스페이스 = 디렉터리","use 는 별칭일 뿐 로딩이 아니다","함수는 전역 폴백, 클래스는 없다","파일 하나에 클래스 하나"] },
    q:[
      C("선언","네임스페이스를 선언하는 키워드는?",
        ["package","namespace","module","using"],1,
        "`namespace App\\Service;` 이며 파일의 **첫 문장**이어야 합니다(`declare` 뒤).\n💡 구분자는 슬래시가 아니라 **백슬래시(`\\`)** 입니다 — 문자열 안에 쓸 때는 `\"App\\\\Service\"` 처럼 이스케이프해야 합니다."),
      C("별칭","<code>use App\\Mail\\Sender;</code> 가 하는 일은?",
        ["파일을 불러온다","긴 클래스 이름에 짧은 별칭을 붙인다","클래스를 인스턴스화한다","네임스페이스를 만든다"],1,
        "`use` 는 **이름 별칭**일 뿐이고 파일을 읽지 않습니다 — 로딩은 오토로더의 몫입니다.\n💡 충돌하면 `use App\\Mail\\Sender as MailSender;` 로 다른 이름을 줍니다. 함수와 상수도 `use function`·`use const` 로 가져올 수 있습니다."),
      I("표준","클래스 이름과 파일 경로를 대응시키는 오토로딩 표준의 이름은? (예: PSR-N 형식)",
        ["PSR-4","psr-4","PSR4"],
        "정답은 **PSR-4** 입니다.\n💡 이전 표준인 PSR-0 은 클래스명의 밑줄까지 디렉터리로 바꿔서(`Foo_Bar` → `Foo/Bar.php`) 경로가 깊어졌습니다. PSR-4 는 그 규칙을 없애고 **네임스페이스 접두어를 특정 디렉터리에 매핑**하는 방식으로 단순해졌습니다."),
      C("전역 클래스","네임스페이스가 선언된 파일에서 <code>new Exception()</code> 을 쓰면?",
        ["전역 Exception 이 쓰인다","현재 네임스페이스의 Exception 을 찾다 실패한다","자동으로 use 가 추가된다","경고 후 전역으로 넘어간다"],1,
        "**클래스에는 전역 폴백이 없습니다** — `App\\Exception` 을 찾다 'Class not found' 가 납니다.\n🔧 `use Exception;` 을 적거나 `new \\Exception()` 이라고 씁니다.\n💡 함수와 상수는 반대로 **전역 폴백이 있어서** `strlen()` 이 그냥 동작합니다. 이 비대칭이 헷갈림의 근원입니다."),
      C("구조","한 파일에 여러 클래스를 정의하는 것이 <b>PSR-4 오토로딩과 충돌</b>하는 이유는?",
        ["PHP 가 파일당 클래스 하나만 허용해서",
         "오토로더는 클래스 이름으로 파일 경로를 계산하므로, 이름과 다른 파일에 있는 클래스는 찾을 수 없어서",
         "네임스페이스는 파일마다 하나만 선언할 수 있어서",
         "Composer 가 파일 크기를 제한해서"],1,
        "오토로딩은 **이름 → 경로 계산**입니다. `App\\Bar` 를 찾을 때 오토로더는 `src/Bar.php` 만 열어 보므로, `Foo.php` 안에 든 `Bar` 는 영원히 발견되지 않습니다.\n⚠️ 그런데 `Foo` 를 먼저 부른 뒤에는 `Bar` 도 이미 메모리에 있어 **되는 것처럼 보입니다** — 호출 순서에 따라 되었다 안 되었다 하는 최악의 버그가 됩니다.\n💡 PHP 자체는 파일당 클래스 개수를 제한하지 않습니다(1번이 틀린 이유)."),
      C("함수 호출","네임스페이스 안에서 <code>\\strlen($s)</code> 처럼 앞에 백슬래시를 붙이는 실질적 효과는?",
        ["동작이 달라진다",
         "현재 네임스페이스를 먼저 찾는 조회 단계를 건너뛰어, 의도가 분명해지고 아주 약간 빠르다",
         "함수가 전역으로 등록된다",
         "오토로딩이 비활성화된다"],1,
        "PHP 는 `App\\strlen` 을 먼저 찾고 없으면 전역으로 떨어집니다. `\\` 를 붙이면 그 조회를 건너뜁니다.\n🎯 실무적 의미는 속도보다 **의도의 명시**입니다 — 같은 네임스페이스에 같은 이름의 함수를 누가 추가해도 동작이 바뀌지 않습니다(테스트에서 함수를 가로채는 기법이 이 폴백을 이용합니다).\n💡 `use function App\\Util\\slug;` 로 명시적으로 가져오는 방식도 같은 목적을 달성합니다."),
    ] },

  { t:"Composer 와 의존성 관리", xp:25,
    th:{ sum:"Composer 는 **무엇에 의존하는지 선언하면 정확히 그 버전을 재현해 주는 도구**다. `composer.json` 은 의도, `composer.lock` 은 사실이다.",
      body:[
        {h:"json 과 lock", t:"`composer.json` 에는 `^8.1` 같은 **허용 범위**를 적고, `composer.lock` 에는 실제로 해결된 **정확한 버전과 해시**가 박힌다. lock 파일은 **반드시 커밋**해야 '내 노트북에서는 되던데' 가 사라진다. 배포에서는 `composer install`(lock 그대로) 을 쓰고, `composer update` 는 의존을 올리려고 **의도할 때만** 부른다."},
        {h:"운영과 개발을 나눈다", t:"테스트·정적 분석 도구는 `require-dev` 에 두고, 배포에서는 `composer install --no-dev --optimize-autoloader` 로 뺀다 — 운영 서버에 개발 도구가 있으면 공격 표면만 늘어난다. `^1.2.3` 은 **왼쪽에서 0 이 아닌 첫 자리**를 고정하므로 `1.x` 는 되고 `2.0` 은 안 된다. `0.x` 대에서는 규칙이 더 좁아져 `^0.3` 이 `0.3.x` 만 허용한다."}],
      code:{ c:"# 배포\ncomposer install --no-dev --optimize-autoloader\n\n# 의존을 올릴 때만\ncomposer update vendor/package --with-dependencies", cap:"install 은 재현, update 는 변경" },
      key:["composer.lock 은 반드시 커밋","배포는 install, 갱신은 update","운영 배포는 --no-dev","^1.2.3 은 2.0 을 허용하지 않는다"] },
    q:[
      I("파일","Composer 가 프로젝트의 의존성을 선언하는 파일 이름은? (확장자 포함)",
        ["composer.json"],
        "정답은 `composer.json` 입니다.\n💡 여기에는 **허용 범위**를 적고, 실제로 설치된 정확한 버전은 `composer.lock` 에 기록됩니다. 둘 다 저장소에 커밋합니다."),
      C("자동 로딩","Composer 가 만들어 주는 오토로더를 쓰려면 무엇을 불러와야 하는가?",
        ["vendor/composer.php","vendor/autoload.php","composer.lock","autoload.json"],1,
        "`require __DIR__ . '/../vendor/autoload.php';` 한 줄이면 됩니다.\n💡 진입점(front controller)에서 **딱 한 번만** 부르는 것이 관례입니다."),
      C("lock","<code>composer.lock</code> 을 저장소에 커밋해야 하는 이유는?",
        ["파일 크기를 줄이려고",
         "모든 환경에서 정확히 같은 버전이 설치되도록 재현성을 보장하려고",
         "Composer 가 요구해서",
         "보안 취약점을 자동으로 고치려고"],1,
        "lock 파일에는 **정확한 버전과 해시**가 박혀 있어, 개발·CI·운영이 같은 코드를 씁니다.\n⚠️ 커밋하지 않으면 배포 시점마다 `^` 범위 안에서 다른 버전이 깔려, **같은 커밋인데 동작이 다른** 상황이 생깁니다.\n💡 라이브러리(다른 프로젝트가 의존하는 패키지)에서는 lock 을 커밋해도 소비자에게 적용되지 않지만, CI 재현성을 위해 커밋하는 것이 요즘 관례입니다."),
      C("버전 범위","<code>\"^1.2.3\"</code> 이 허용하는 버전은?",
        ["1.2.3 만","1.2.3 이상 2.0.0 미만","1.2.3 이상 1.3.0 미만","모든 1.x 와 2.x"],1,
        "`^` 는 **왼쪽에서 0 이 아닌 첫 자리**를 고정합니다 — `>=1.2.3 <2.0.0` 입니다.\n💡 `~1.2.3` 은 더 좁아 `>=1.2.3 <1.3.0` 입니다. 그리고 `^0.3.1` 은 0 이 첫 자리라 규칙이 이동해 `>=0.3.1 <0.4.0` 이 됩니다 — 1.0 이전은 파괴적 변경이 잦다는 현실을 반영한 것입니다."),
      C("배포","운영 서버 배포에 알맞은 명령은?",
        ["composer update","composer install --no-dev --optimize-autoloader","composer require --dev","composer dump-autoload --dev"],1,
        "`install` 은 **lock 그대로** 설치해 재현성을 지키고, `--no-dev` 는 테스트 도구를 빼며, `--optimize-autoloader` 는 클래스맵을 미리 만들어 파일 탐색을 없앱니다.\n⚠️ 배포 스크립트에 `composer update` 를 넣는 것은 **배포할 때마다 의존성이 바뀌는** 것이라, 언젠가 반드시 사고가 납니다."),
      C("의존 판단","작은 기능 하나를 위해 외부 패키지를 추가할지 결정할 때 <b>가장 중요한 판단 기준</b>은?",
        ["GitHub 스타 수",
         "그 패키지의 유지보수 상태·의존성 트리 크기·보안 이력, 그리고 직접 구현했을 때의 유지 비용을 견주는 것",
         "다운로드 수가 가장 많은 것을 고른다",
         "가능한 한 모든 것을 직접 구현한다"],1,
        "추가하는 것은 코드가 아니라 **그 패키지의 의존성 전부와 유지보수 위험**입니다.\n⚠️ 디스트랙터 분석: 스타 수·다운로드 수는 인기의 지표일 뿐 **현재 유지되고 있는지**를 말해 주지 않습니다(left-pad 사건이 그 예입니다). 반대로 4번은 인증·암호화처럼 **직접 만들면 안 되는 것**까지 만들게 만듭니다.\n🔧 실무 체크리스트: 최근 커밋·릴리스, 열린 이슈의 응답 속도, `composer why-not`/`composer depends` 로 본 의존 트리, `composer audit` 의 취약점 이력."),
    ] },
]},

/* ═══════════════ 유닛 8 ═══════════════ */
{ t:"웹 요청 처리 — PHP 가 가장 잘하는 일", l:[

  { t:"슈퍼글로벌과 요청 수명", xp:25,
    th:{ sum:"PHP 는 요청의 모든 정보를 **슈퍼글로벌 배열**로 미리 채워 준다. 편리하지만, 그 값들은 전부 **믿을 수 없는 입력**이다.",
      body:[
        {h:"어디서 온 값인가", t:"`$_GET`(쿼리스트링)·`$_POST`(본문)·`$_COOKIE`·`$_FILES`·`$_SERVER`(헤더와 환경)가 있고, `$_REQUEST` 는 이들을 섞어 놓아 **출처를 알 수 없게 만들므로 쓰지 않는다**. `$_SERVER` 의 상당수는 사용자가 보낸 헤더 그대로다 — `HTTP_HOST`·`HTTP_X_FORWARDED_FOR`·`HTTP_REFERER` 는 위조할 수 있고, `REMOTE_ADDR` 만 TCP 연결에서 온 값이다."},
        {h:"검증은 경계에서", t:"입력은 **받는 즉시 형식과 범위를 검증**해 내부 타입으로 바꾼다(`filter_var`, 화이트리스트). 검증(들어와도 되는가)과 이스케이프(내보낼 때 안전한가)는 **다른 일**이며, 이스케이프는 출력 맥락(HTML·SQL·셸·URL)마다 다르다. `$_FILES` 는 특히 위험하다 — 클라이언트가 보낸 `type` 과 `name` 은 믿을 수 없고, 저장 이름은 서버가 새로 정해야 한다."}],
      code:{ c:"$page = filter_input(INPUT_GET, 'page', FILTER_VALIDATE_INT,\n    ['options' => ['default' => 1, 'min_range' => 1]]);\n// 형식·범위·기본값을 한 번에", cap:"검증을 통과한 값만 안쪽으로 보낸다" },
      key:["$_REQUEST 는 출처가 섞여 쓰지 않는다","$_SERVER 의 HTTP_* 는 위조 가능","검증과 이스케이프는 다른 일","업로드 파일명은 서버가 정한다"] },
    q:[
      C("쿼리스트링","URL 의 <code>?page=2</code> 값을 읽는 슈퍼글로벌은?",
        ["$_POST","$_GET","$_SERVER","$_ENV"],1,
        "`$_GET['page']` 입니다.\n💡 이름과 달리 HTTP 메서드와 무관하게 **쿼리스트링**을 담습니다 — POST 요청의 URL 에 쿼리가 있으면 그것도 `$_GET` 에 들어옵니다."),
      C("폼","POST 로 전송된 폼 데이터를 담는 슈퍼글로벌은?",
        ["$_GET","$_POST","$_FILES","$_COOKIE"],1,
        "`$_POST` 입니다.\n⚠️ 단, `Content-Type` 이 `application/json` 이면 `$_POST` 는 **비어 있습니다** — `file_get_contents('php://input')` 로 원문을 읽어 `json_decode` 해야 합니다. API 를 만들 때 가장 먼저 부딪히는 부분입니다."),
      I("함수","값을 형식에 맞게 <b>검증</b>할 때 쓰는 PHP 내장 함수는? (괄호 없이, 가장 대표적인 것)",
        ["filter_var"],
        "정답은 `filter_var()` 입니다.\n💡 `filter_var($e, FILTER_VALIDATE_EMAIL)`·`FILTER_VALIDATE_INT`·`FILTER_VALIDATE_URL` 처럼 씁니다. 통과하면 **변환된 값**을, 실패하면 `false` 를 돌려주므로 `=== false` 로 검사해야 합니다(값이 `0` 일 수 있기 때문입니다)."),
      C("$_REQUEST","<code>$_REQUEST</code> 사용이 권장되지 않는 이유는?",
        ["느려서","GET·POST·COOKIE 가 섞여 값의 출처를 알 수 없고, 쿠키로 파라미터를 덮어쓸 수 있어서","PHP 8 에서 제거되어서","배열이 아니라서"],1,
        "출처가 섞이면 **쿠키로 POST 파라미터를 덮어쓰는** 공격이 가능해집니다(`variables_order` 설정에 따라 우선순위가 달라집니다).\n💡 그리고 '이 값이 어디서 왔는가' 를 코드만 보고 알 수 없어 감사와 디버깅이 어려워집니다. 출처를 명시하는 것 자체가 보안입니다."),
      C("IP","클라이언트 IP 로 <b>가장 신뢰할 수 있는</b> 값은?",
        ["$_SERVER['HTTP_X_FORWARDED_FOR']","$_SERVER['HTTP_CLIENT_IP']","$_SERVER['REMOTE_ADDR']","$_GET['ip']"],2,
        "`REMOTE_ADDR` 만 **TCP 연결에서 얻은 값**이라 위조할 수 없습니다. `HTTP_*` 는 전부 클라이언트가 보낸 헤더입니다.\n⚠️ 프록시·CDN 뒤에서는 `REMOTE_ADDR` 가 프록시 IP 라 `X-Forwarded-For` 가 필요한데, 그때도 **내가 신뢰하는 프록시가 붙인 마지막 값만** 취해야 합니다. 헤더를 그대로 믿으면 레이트 리밋과 차단이 통째로 우회됩니다."),
      C("업로드","파일 업로드를 안전하게 처리하는 방법으로 <b>틀린</b> 것은?",
        ["$_FILES['f']['type'] 을 믿고 이미지 여부를 판단한다",
         "저장 파일명을 서버가 새로 생성한다",
         "저장 디렉터리에서 PHP 실행을 막는다",
         "is_uploaded_file / move_uploaded_file 을 쓴다"],0,
        "`$_FILES[...]['type']` 은 **클라이언트가 보낸 값**이라 `image/png` 라고 적어 보내면 그대로 들어옵니다.\n🔧 실제 내용으로 판별해야 합니다 — `finfo_file()` 로 MIME 을 확인하고, 이미지라면 `getimagesize()` 로 한 번 더 봅니다.\n⚠️ 그리고 가장 중요한 방어는 **업로드 디렉터리에서 실행을 막는 것**입니다. 확장자 검사만으로는 `.php.jpg`·`.phtml` 류 우회가 계속 나옵니다."),
    ] },

  { t:"세션 · 쿠키 · 상태", xp:25,
    th:{ sum:"HTTP 는 상태가 없다. PHP 는 **쿠키에 담긴 세션 ID** 하나로 서버 쪽 저장소를 찾아 상태를 흉내 낸다 — 그래서 그 ID 를 지키는 것이 전부다.",
      body:[
        {h:"세션의 실체", t:"`session_start()` 는 쿠키의 세션 ID 를 읽어 저장소(기본은 파일)를 열고 `$_SESSION` 에 채운다. **로그인·권한 변경 직후에는 `session_regenerate_id(true)`** 로 ID 를 새로 발급해야 한다 — 공격자가 미리 심어 둔 ID 를 피해자가 그대로 쓰게 만드는 **세션 고정** 공격을 이것으로 막는다. 파일 세션은 기본적으로 **같은 세션의 요청을 직렬화**하므로, 오래 걸리는 요청 중에는 `session_write_close()` 로 일찍 닫는다."},
        {h:"쿠키에 붙이는 표식", t:"세션 쿠키에는 `HttpOnly`(JS 접근 차단) · `Secure`(HTTPS 전용) · `SameSite=Lax` 이상이 기본이어야 한다. 이 셋은 각각 XSS 로 탈취, 평문 전송 도청, CSRF 를 줄인다. 그리고 쿠키에 **권한·금액 같은 값을 직접 담지 않는다** — 담아야 한다면 서명(HMAC)이 필수다."}],
      code:{ c:"session_set_cookie_params([\n    'httponly' => true, 'secure' => true,\n    'samesite' => 'Lax', 'path' => '/',\n]);\nsession_start();\n// 로그인 성공 직후\nsession_regenerate_id(true);", cap:"권한이 바뀌는 순간 ID 를 바꾼다" },
      key:["session_start 가 있어야 $_SESSION 을 쓴다","로그인 직후 session_regenerate_id(true)","쿠키에 HttpOnly·Secure·SameSite","쿠키 값은 서명 없이 믿지 않는다"] },
    q:[
      I("함수","<code>$_SESSION</code> 을 쓰기 전에 반드시 호출해야 하는 함수는? (괄호 없이)",
        ["session_start"],
        "정답은 `session_start()` 입니다.\n⚠️ 출력이 이미 시작된 뒤에 부르면 쿠키 헤더를 보낼 수 없어 `headers already sent` 오류가 납니다 — PHP 파일 끝의 `?>` 뒤 공백이 흔한 원인입니다."),
      C("저장 위치","세션 데이터의 <b>값</b>은 기본적으로 어디에 저장되는가?",
        ["브라우저 쿠키 안","서버(기본은 파일)","URL 쿼리스트링","데이터베이스"],1,
        "쿠키에는 **세션 ID 만** 들어가고 값은 서버에 있습니다(기본 `session.save_handler=files`).\n💡 서버가 여러 대면 파일 세션은 요청마다 다른 서버에 붙어 로그인이 풀립니다 — Redis 같은 공용 저장소로 옮기는 것이 정석입니다."),
      C("쿠키 플래그","JavaScript 가 쿠키를 읽지 못하게 하는 플래그는?",
        ["Secure","HttpOnly","SameSite","Path"],1,
        "`HttpOnly` 는 `document.cookie` 접근을 막아 **XSS 로 세션이 탈취되는 것**을 줄입니다.\n💡 `Secure` 는 HTTPS 전용 전송, `SameSite` 는 크로스 사이트 요청에 쿠키를 안 붙이는 것(CSRF 완화)입니다. 셋은 목적이 달라 **전부 함께** 켭니다."),
      C("세션 고정","로그인에 성공한 직후 <code>session_regenerate_id(true)</code> 를 부르는 이유는?",
        ["세션 데이터를 지우려고",
         "공격자가 미리 알려 준 세션 ID 가 로그인 후에도 유효한 상태(세션 고정)를 막으려고",
         "쿠키 크기를 줄이려고",
         "동시 접속을 허용하려고"],1,
        "**세션 고정(session fixation)** 방어입니다. 공격자가 링크로 특정 세션 ID 를 심어 두면, 피해자가 그 ID 로 로그인한 순간 공격자도 로그인 상태가 됩니다.\n💡 `true` 인자는 **옛 세션 파일을 삭제**하라는 뜻입니다 — 빼면 옛 ID 가 계속 유효합니다.\n🎯 규칙은 '**권한 수준이 바뀌는 모든 순간**' 입니다 — 로그인뿐 아니라 관리자 승격, 2단계 인증 통과 시점에도 필요합니다."),
      C("잠금","같은 사용자의 요청이 <b>순차적으로 처리</b>되어 느려질 때 알맞은 조치는?",
        ["session_start 를 두 번 부른다",
         "세션에 쓸 일이 끝난 뒤 session_write_close() 로 일찍 닫는다",
         "세션을 쓰지 않는다",
         "session_id() 를 매번 바꾼다"],1,
        "파일 세션 핸들러는 **세션 파일을 잠그므로** 같은 세션의 다른 요청이 대기합니다. 쓰기가 끝나면 `session_write_close()` 로 잠금을 놓아 줍니다.\n⚠️ 닫은 뒤의 `$_SESSION` 변경은 저장되지 않습니다 — 읽기는 그대로 됩니다.\n💡 이 증상은 '느린 API 하나가 페이지 전체를 멈추는' 형태로 나타나며, 원인을 잘못 짚어 서버를 늘리는 일이 흔합니다."),
      C("상태 설계","여러 대의 웹 서버로 확장할 때 세션 처리 방식으로 <b>가장 적절한</b> 것은?",
        ["로드밸런서의 sticky session 에만 의존한다",
         "세션 저장소를 Redis 같은 공용 저장소로 옮긴다",
         "세션 값을 전부 쿠키에 담는다",
         "서버마다 파일 세션을 그대로 쓰고 로그인을 자주 시킨다"],1,
        "**저장소를 밖으로 빼는 것**이 정답입니다 — 어느 서버가 받아도 같은 세션을 봅니다.\n⚠️ 디스트랙터 분석: sticky session 은 서버 하나가 죽으면 그 서버에 붙은 사용자가 전부 로그아웃되고, 배포 때마다 같은 일이 벌어집니다. 쿠키에 전부 담으면 4KB 한도와 **위변조 문제**가 생겨 서명·암호화가 필수가 되고, 매 요청 전송 비용이 붙습니다.\n💡 쿠키에 담는 방식(JWT 등)이 틀린 것은 아니지만, **즉시 무효화가 어렵다**는 대가를 반드시 계산해야 합니다."),
    ] },
]},

/* ═══════════════ 유닛 9 ═══════════════ */
{ t:"보안 — PHP 가 가장 많이 공격받는 자리", l:[

  { t:"인젝션과 XSS", xp:30,
    th:{ sum:"거의 모든 인젝션은 하나의 원인에서 나온다 — **데이터를 코드가 있는 자리에 문자열로 끼워 넣는 것**.",
      body:[
        {h:"경계를 언어에게 맡긴다", t:"SQL 은 **바인드 파라미터**로 데이터와 구문의 경계를 엔진이 알게 한다. 이스케이프로 막으려는 시도는 인코딩·컨텍스트마다 규칙이 달라 언젠가 뚫린다. 다만 테이블·컬럼 이름은 바인딩할 수 없으므로 **화이트리스트**로 검증해야 하고, 셸 호출은 `escapeshellarg()` 로 인자 단위로 감싸되 가능하면 **셸 자체를 거치지 않는 API**를 쓴다."},
        {h:"출력 맥락마다 다른 이스케이프", t:"XSS 방어는 '출력하는 자리' 가 정한다 — HTML 본문은 `htmlspecialchars`, 속성값은 따옴표로 감싸고 `ENT_QUOTES`, URL 자리는 `urlencode`, `<script>` 안은 `json_encode`(플래그 포함)다. **하나의 만능 함수는 없다**. 그래서 템플릿 엔진의 자동 이스케이프를 쓰고, 예외로 끄는 자리(`|raw`)만 검토하는 방식이 실무에서 안전하다."}],
      code:{ c:"$st = $pdo->prepare('SELECT * FROM u WHERE email = ?');\n$st->execute([$email]);\n\n// 정렬 컬럼은 바인딩 불가 → 화이트리스트\n$col = ['name','created_at'][$idx] ?? 'name';", cap:"값은 바인딩, 식별자는 화이트리스트" },
      key:["SQL 은 바인드 파라미터가 구조적 해법","식별자는 화이트리스트","이스케이프는 출력 맥락마다 다르다","자동 이스케이프를 기본값으로"] },
    q:[
      C("SQL 인젝션","SQL 인젝션을 막는 <b>구조적</b> 방법은?",
        ["입력에서 작은따옴표를 지운다","준비된 구문(prepared statement)에 값을 바인딩한다","입력 길이를 제한한다","쿼리를 대문자로 쓴다"],1,
        "바인딩하면 값은 **절대 구문으로 해석되지 않습니다** — 데이터와 코드의 경계를 엔진이 압니다.\n⚠️ 따옴표 제거·길이 제한은 우회 방법이 계속 나오고, 정상 입력(`O'Brien`)까지 망가뜨립니다."),
      C("XSS","사용자 입력을 HTML 본문에 출력할 때 알맞은 함수와 인자는?",
        ["htmlspecialchars($s)","htmlspecialchars($s, ENT_QUOTES, 'UTF-8')","strip_tags($s)","addslashes($s)"],1,
        "**인코딩과 따옴표 처리를 명시**해야 합니다. PHP 8.1 부터 기본 플래그가 `ENT_QUOTES | ENT_SUBSTITUTE | ENT_HTML401` 로 바뀌었지만, 하위 버전과 섞이는 코드에서는 명시가 안전합니다.\n💡 `ENT_QUOTES` 가 필요한 이유: 속성값을 작은따옴표로 감싼 경우 `'` 를 변환하지 않으면 속성을 탈출할 수 있습니다."),
      I("함수","셸 명령의 <b>인자</b>를 안전하게 감싸는 PHP 함수는? (괄호 없이)",
        ["escapeshellarg"],
        "정답은 `escapeshellarg()` 입니다.\n⚠️ `escapeshellcmd()` 와 혼동하기 쉬운데, 그쪽은 명령 전체를 대상으로 해 **불완전합니다**. 가장 좋은 것은 셸을 아예 거치지 않고 `proc_open` 에 인자 배열을 넘기는 방식입니다."),
      C("식별자","정렬 컬럼을 사용자가 고르게 할 때 <code>ORDER BY ?</code> 로 바인딩할 수 없는 이유와 해법은?",
        ["바인딩할 수 있다 — 그냥 쓰면 된다",
         "바인딩은 값 자리에만 되므로, 허용 컬럼 목록(화이트리스트)에서 고른다",
         "컬럼명을 htmlspecialchars 로 감싼다",
         "따옴표로 감싸면 안전하다"],1,
        "준비된 구문의 플레이스홀더는 **값 자리 전용**입니다 — 식별자를 바인딩하면 `ORDER BY 'name'`(문자열 상수)이 되어 정렬이 안 됩니다.\n🔧 `$allowed = ['name','created_at']; $col = in_array($c, $allowed, true) ? $c : 'name';` 처럼 **미리 정한 목록**에서만 고릅니다.\n⚠️ `htmlspecialchars` 는 HTML 용이라 SQL 에서 아무 역할도 하지 않습니다(3번이 틀린 이유)."),
      C("저장 시점 vs 출력 시점","XSS 방어를 <b>저장할 때 미리 이스케이프</b>하는 방식의 문제는?",
        ["느리다",
         "출력 맥락(HTML·속성·JS·URL)마다 필요한 이스케이프가 달라 맞지 않고, 원본 데이터가 훼손되어 다른 용도로 못 쓴다",
         "데이터베이스 용량이 커진다",
         "문제가 없다 — 권장되는 방식이다"],1,
        "이스케이프는 **출력하는 맥락**이 정합니다. 저장 시점에는 그 값이 HTML 본문에 나갈지, JSON API 로 나갈지, 이메일 제목이 될지 알 수 없습니다.\n⚠️ 그리고 이스케이프된 값이 저장되면 이름이 `O&#039;Brien` 으로 남아 검색·정렬·CSV 내보내기가 전부 어긋나고, 실수로 두 번 이스케이프되면 화면에 그대로 보입니다.\n🎯 원칙: **저장은 원본대로, 검증은 입력 시점에, 이스케이프는 출력 시점에**."),
      C("파일 포함","<code>include $_GET['page'] . '.php';</code> 의 위험을 가장 정확히 설명한 것은?",
        ["느려진다",
         "경로 조작으로 서버의 임의 파일을 읽히거나 실행시킬 수 있다(LFI) — 화이트리스트 매핑으로 바꿔야 한다",
         "캐시가 되지 않는다",
         "PHP 8 에서 문법 오류다"],1,
        "**로컬 파일 인클루전(LFI)** 입니다. `../../etc/passwd%00` 류나 업로드된 파일을 가리켜 코드 실행까지 이어질 수 있습니다.\n🔧 해법은 필터링이 아니라 **매핑**입니다: `$map = ['home'=>'home.php','about'=>'about.php']; include $map[$p] ?? '404.php';`\n💡 `allow_url_include` 가 켜져 있으면 원격 URL 까지 포함되어(RFI) 즉시 서버 장악입니다 — 기본값은 Off 이며 절대 켜지 않습니다."),
    ] },

  { t:"인증 · 비밀번호 · CSRF", xp:30,
    th:{ sum:"인증은 **직접 구현하지 않는 것**이 첫 번째 원칙이고, 해야 한다면 PHP 가 준 함수를 그대로 쓰는 것이 두 번째다.",
      body:[
        {h:"비밀번호는 해시한다", t:"`password_hash($pw, PASSWORD_DEFAULT)` 는 알고리즘·비용·**솔트를 결과 문자열 안에 함께** 담는다 — 그래서 솔트를 따로 저장할 필요가 없다. 검증은 `password_verify` 이며, `password_needs_rehash` 로 비용이 올라갔을 때 로그인 시점에 조용히 갱신한다. **md5·sha1 은 빠르다는 이유로 부적합하다** — 비밀번호 해시는 느려야 한다."},
        {h:"토큰은 비교도 조심한다", t:"CSRF 토큰은 세션에 두고 폼에 심어 **상수 시간 비교**(`hash_equals`)로 확인한다 — `==` 는 첫 다른 바이트에서 멈춰 시간 차이로 값을 추측당할 수 있다. 임의 값은 `random_bytes()`(암호학적 안전) 이며 `rand`·`mt_rand`·`uniqid` 는 예측 가능해 토큰에 쓰면 안 된다. `SameSite=Lax` 쿠키는 CSRF 를 크게 줄이지만 **완전한 대체는 아니다**."}],
      code:{ c:"$hash = password_hash($pw, PASSWORD_DEFAULT);\nif (password_verify($pw, $hash)) {\n    if (password_needs_rehash($hash, PASSWORD_DEFAULT)) {\n        $repo->updateHash($id, password_hash($pw, PASSWORD_DEFAULT));\n    }\n}", cap:"로그인 순간이 해시를 올릴 유일한 기회" },
      key:["password_hash / password_verify 를 쓴다","솔트는 해시 문자열에 포함된다","토큰 비교는 hash_equals","난수는 random_bytes"] },
    q:[
      I("함수","비밀번호를 안전하게 해시하는 PHP 내장 함수는? (괄호 없이)",
        ["password_hash"],
        "정답은 `password_hash()` 입니다.\n💡 `PASSWORD_DEFAULT` 를 쓰면 PHP 버전이 올라갈 때 더 강한 알고리즘으로 자동 이동합니다 — 그래서 DB 컬럼은 넉넉하게(255자) 잡아 둡니다."),
      C("검증","<code>password_hash</code> 로 만든 해시를 검증하는 함수는?",
        ["md5 비교","password_verify","hash_equals","crypt"],1,
        "`password_verify($입력, $저장된해시)` 입니다 — 해시 문자열에서 알고리즘·비용·솔트를 읽어 같은 방식으로 계산합니다.\n💡 그래서 `password_verify` 는 `===` 비교를 직접 하지 않아도 되고, 내부적으로 상수 시간 비교를 씁니다."),
      C("솔트","<code>password_hash</code> 를 쓸 때 솔트는?",
        ["직접 만들어 인자로 넘겨야 한다","자동 생성되어 결과 해시 문자열 안에 포함된다","필요 없다","전역 설정에 한 번만 둔다"],1,
        "**자동 생성되어 해시 문자열에 함께 담깁니다** — 별도 컬럼이 필요 없습니다.\n⚠️ PHP 7.0 부터 `salt` 옵션은 deprecated 이고 7.1 에서 무시됩니다. 직접 만든 솔트가 자동 생성보다 나을 이유가 없습니다.\n💡 사용자마다 솔트가 다르므로 **레인보우 테이블이 무력화**되고, 같은 비밀번호를 쓰는 두 계정의 해시도 달라집니다.",
        P("password_verify('pw', password_hash('pw', PASSWORD_DEFAULT))\n  && password_hash('pw', PASSWORD_DEFAULT) !== password_hash('pw', PASSWORD_DEFAULT)","true")),
      C("난수","보안 토큰용 난수를 만들 때 알맞은 함수는?",
        ["rand()","mt_rand()","random_bytes()","uniqid()"],2,
        "`random_bytes($n)` 만 **암호학적으로 안전한** 난수입니다(`bin2hex()` 로 문자열화).\n⚠️ `uniqid()` 는 **시간 기반**이라 예측 가능하고, `mt_rand` 는 시드를 알면 수열 전체를 복원할 수 있습니다. 비밀번호 재설정 토큰이 이런 것으로 만들어져 뚫린 사례가 많습니다."),
      C("비교","토큰을 <code>==</code> 대신 <code>hash_equals()</code> 로 비교하는 이유는?",
        ["더 빠르다","길이가 달라도 되게 한다","비교 시간이 값에 따라 달라지지 않아 타이밍 공격을 막는다","자동으로 해시한다"],2,
        "일반 비교는 **첫 다른 바이트에서 즉시 멈추므로**, 응답 시간 차이로 토큰을 앞에서부터 한 글자씩 알아낼 수 있습니다. `hash_equals` 는 항상 끝까지 비교합니다.\n💡 `==` 는 여기에 더해 **느슨한 비교** 문제까지 있습니다 — PHP 7 에서는 `\"0e123\" == \"0e456\"` 이 둘 다 0 으로 해석되어 참이 되는 magic hash 문제가 있었습니다."),
      C("CSRF","CSRF 방어 방식으로 <b>가장 견고한 조합</b>은?",
        ["Referer 헤더만 검사한다",
         "세션에 둔 토큰을 폼에 심고 hash_equals 로 검증하며, 쿠키에 SameSite 를 함께 건다",
         "POST 메서드만 쓰면 된다",
         "CAPTCHA 를 넣는다"],1,
        "**토큰(애플리케이션 계층) + SameSite(브라우저 계층)** 의 이중 방어가 실무 표준입니다.\n⚠️ 디스트랙터 분석: Referer 는 프라이버시 설정으로 비어 올 수 있어 '없으면 통과' 규칙을 만들게 되고 그게 곧 우회로가 됩니다. POST 만으로는 막히지 않습니다 — 공격자가 자동 제출되는 폼을 만들면 됩니다. CAPTCHA 는 자동화를 막지만 CSRF 의 본질(피해자 브라우저가 쿠키를 붙여 보내는 것)과는 다른 문제입니다.\n💡 토큰은 **상태를 바꾸는 요청**에만 필요합니다 — GET 조회에 걸면 링크 공유가 깨집니다."),
    ] },
]},

/* ═══════════════ 유닛 10 ═══════════════ */
{ t:"데이터베이스 — PDO", l:[

  { t:"PDO 연결과 준비된 구문", xp:30,
    th:{ sum:"PDO 는 여러 DB 를 **같은 인터페이스**로 다루는 계층이다. 기본 설정이 안전하지 않으므로, 연결할 때 세 가지를 반드시 바꾼다.",
      body:[
        {h:"연결할 때 정하는 세 가지", t:"`ERRMODE_EXCEPTION`(오류를 조용히 넘기지 않는다) · `EMULATE_PREPARES => false`(PHP 가 흉내 내지 말고 **DB 가 직접 준비하게** 한다) · `DEFAULT_FETCH_MODE => FETCH_ASSOC`(숫자 키 중복 제거). PHP 8.0 부터 기본 오류 모드가 예외로 바뀌었지만, 나머지 둘은 여전히 명시해야 한다. DSN 에 `charset=utf8mb4` 를 넣는 것도 필수다 — 빠뜨리면 이모지가 깨진다."},
        {h:"바인딩의 실제", t:"`execute([$a, $b])` 로 넘기면 모두 문자열로 바인딩되고, 타입이 중요하면 `bindValue($k, $v, PDO::PARAM_INT)` 를 쓴다. `IN (?)` 에는 배열을 바인딩할 수 없어 **개수만큼 물음표를 만들어야** 한다. 그리고 준비된 구문을 **반복문 안에서 매번 prepare 하면** 이득이 사라지므로, 밖에서 한 번 준비하고 안에서 `execute` 만 반복한다."}],
      code:{ c:"$in = implode(',', array_fill(0, count($ids), '?'));\n$st = $pdo->prepare(\"SELECT * FROM u WHERE id IN ($in)\");\n$st->execute($ids);", cap:"IN 절은 개수만큼 자리를 만든다" },
      key:["ERRMODE_EXCEPTION + EMULATE_PREPARES=false","DSN 에 charset=utf8mb4","IN 절은 물음표를 개수만큼","prepare 는 밖에서 한 번"] },
    q:[
      C("확장","PHP 에서 여러 종류의 데이터베이스를 같은 API 로 다루는 확장은?",
        ["mysql","mysqli","PDO","ODBC"],2,
        "**PDO**(PHP Data Objects) 입니다 — 드라이버만 바꾸면 MySQL·PostgreSQL·SQLite 를 같은 코드로 다룹니다.\n💡 `mysqli` 는 MySQL 전용이지만 준비된 구문을 지원하므로 보안 면에서는 동등합니다. 옛 `mysql_*` 함수는 PHP 7 에서 **제거**되었습니다."),
      C("에러 모드","PDO 연결 시 <code>PDO::ATTR_ERRMODE</code> 를 <code>ERRMODE_EXCEPTION</code> 으로 두는 이유는?",
        ["속도를 높이려고","쿼리 실패가 조용히 지나가지 않고 예외로 드러나게 하려고","로그를 줄이려고","트랜잭션을 켜려고"],1,
        "기본 모드(`ERRMODE_SILENT`)에서는 실패해도 `false` 만 돌려주므로, 검사를 빠뜨리면 **실패한 채로 진행**합니다.\n💡 PHP 8.0 부터 기본값이 예외로 바뀌었지만, 하위 버전과 섞이는 코드에서는 명시하는 편이 안전합니다."),
      I("메서드","PDO 에서 준비된 구문을 만드는 메서드 이름은? (괄호 없이)",
        ["prepare"],
        "정답은 `prepare()` 입니다.\n💡 `query()` 는 준비 없이 바로 실행하므로 **사용자 입력이 섞이면 절대 쓰면 안 됩니다** — 고정된 SQL 에만 씁니다."),
      C("에뮬레이션","<code>PDO::ATTR_EMULATE_PREPARES => false</code> 로 두는 이유로 옳은 것은?",
        ["속도가 항상 빨라진다",
         "PHP 가 값을 문자열로 끼워 넣어 흉내 내는 대신 DB 가 직접 구문과 값을 분리해, 인코딩 관련 우회 위험이 사라지고 타입이 제대로 전달된다",
         "연결이 자동으로 재시도된다",
         "트랜잭션이 자동 커밋된다"],1,
        "에뮬레이션 모드에서는 PHP 가 **직접 이스케이프해 문자열을 조립**합니다 — 잘못된 charset 설정과 겹치면 우회가 가능했던 역사가 있습니다.\n💡 부수 효과로 정수가 정수로 전달되어 인덱스 사용이 달라지기도 합니다. 다만 매 실행마다 왕복이 한 번 더 생겨 **단발성 쿼리는 조금 느려질 수 있습니다**(1번이 틀린 이유)."),
      C("IN 절","<code>WHERE id IN (?)</code> 에 배열을 바인딩하면?",
        ["배열이 자동으로 펼쳐진다","값 하나로 취급되어 의도대로 동작하지 않는다","문법 오류가 난다","전체 행이 반환된다"],1,
        "플레이스홀더 하나에는 **값 하나**만 들어갑니다.\n🔧 `str_repeat('?,', count($ids)-1) . '?'` 또는 `implode(',', array_fill(0, count($ids), '?'))` 로 개수만큼 만들고 배열을 그대로 `execute()` 에 넘깁니다.\n⚠️ `$ids` 가 빈 배열이면 `IN ()` 이 되어 문법 오류이므로, **빈 배열을 먼저 걸러야** 합니다.",
        P("implode(',', array_fill(0, 3, '?'))","'?,?,?'")),
      C("트랜잭션","여러 표에 걸친 갱신을 트랜잭션으로 묶을 때 <b>가장 흔한 실수</b>는?",
        ["beginTransaction 을 부르지 않는 것",
         "예외가 났을 때 rollBack 을 부르지 않아 연결이 열린 트랜잭션 상태로 반환되는 것",
         "commit 을 두 번 부르는 것",
         "트랜잭션 안에서 SELECT 를 하는 것"],1,
        "`try { … commit(); } catch (\\Throwable $e) { $pdo->rollBack(); throw $e; }` 형태가 아니면, 예외 경로에서 **트랜잭션이 열린 채로 남습니다**.\n⚠️ 커넥션 풀을 쓰는 환경에서는 그 연결을 받은 다음 요청이 남의 트랜잭션 안에서 동작하게 됩니다.\n💡 그리고 MySQL 에서 `CREATE TABLE`·`ALTER` 같은 DDL 은 **암묵적 커밋**을 일으켜 트랜잭션을 끊어 버립니다 — 마이그레이션을 롤백할 수 있다고 가정하면 안 됩니다."),
    ] },

  { t:"조회 · N+1 · 마이그레이션", xp:30,
    th:{ sum:"성능 문제의 대부분은 쿼리 하나가 느린 것이 아니라 **쿼리를 너무 많이 부르는 것**이다.",
      body:[
        {h:"N+1 을 알아보는 법", t:"목록 100개를 가져온 뒤 각각에 대해 작성자를 다시 조회하면 101번의 왕복이 생긴다 — 각 쿼리는 1ms 라 프로파일러에 걸리지도 않는데 합계는 100ms 다. 해법은 **미리 모아서 한 번에**(`WHERE id IN (…)`) 가져와 PHP 에서 짝을 맞추거나, ORM 의 eager loading 을 쓰는 것이다. 쿼리 **개수**를 로깅하면 이 문제는 즉시 보인다."},
        {h:"메모리와 스키마 변경", t:"`fetchAll()` 은 결과를 **전부 메모리에 올린다** — 큰 결과는 `foreach ($st as $row)` 로 한 행씩 흘려보낸다. 스키마 변경은 **되돌릴 수 있는 단계로 쪼개** 배포한다: 컬럼 추가 → 양쪽 쓰기 → 백필 → 읽기 전환 → 옛 컬럼 제거. 한 번에 이름을 바꾸면 **배포 중 옛 코드와 새 코드가 동시에 도는 순간** 반드시 깨진다."}],
      code:{ c:"// N+1 제거: 필요한 id 를 먼저 모은다\n$ids = array_column($posts, 'user_id');\n$st  = $pdo->prepare(\"SELECT id,name FROM users WHERE id IN (\"\n     . implode(',', array_fill(0, count($ids), '?')) . \")\");\n$st->execute($ids);\n$users = $st->fetchAll(PDO::FETCH_KEY_PAIR);", cap:"왕복 101번 → 2번" },
      key:["N+1 은 쿼리 개수를 세면 보인다","fetchAll 은 전부 메모리에 올린다","큰 결과는 한 행씩 순회","스키마 변경은 되돌릴 수 있는 단계로"] },
    q:[
      C("가져오기","결과를 컬럼명 기준 배열로 받는 fetch 모드는?",
        ["PDO::FETCH_NUM","PDO::FETCH_ASSOC","PDO::FETCH_BOTH","PDO::FETCH_LAZY"],1,
        "`FETCH_ASSOC` 입니다. 기본값인 `FETCH_BOTH` 는 숫자 키와 문자열 키를 **둘 다** 만들어 메모리를 두 배 쓰고 `var_dump` 를 어지럽힙니다.\n💡 연결할 때 `PDO::ATTR_DEFAULT_FETCH_MODE` 로 한 번에 정해 두는 것이 관례입니다."),
      I("용어","목록을 가져온 뒤 각 항목마다 추가 쿼리를 날려 쿼리 수가 폭증하는 문제를 무엇이라 하는가? (영문 표기)",
        ["N+1","n+1","N+1 problem","N+1 쿼리"],
        "정답은 **N+1** 문제입니다.\n💡 발견법이 중요합니다 — 요청당 쿼리 수를 로그로 남기고 임계치를 넘으면 경고하게 해 두면, 코드 리뷰에서 놓쳐도 잡힙니다."),
      C("메모리","수백만 행을 처리할 때 <code>fetchAll()</code> 대신 써야 할 방식은?",
        ["fetchAll 에 LIMIT 을 걸어 여러 번 부른다","결과 객체를 foreach 로 한 행씩 순회한다","fetchColumn 을 쓴다","메모리 한도를 늘린다"],1,
        "PDOStatement 는 Traversable 이라 `foreach ($st as $row)` 로 **한 행씩** 받을 수 있습니다.\n💡 2번이 가장 단순하고, 필요하면 제너레이터로 감싸 `yield` 하면 호출부는 배열처럼 쓰면서 메모리는 상수로 유지됩니다.\n⚠️ 4번(메모리 한도 상향)은 근본 해결이 아니라 **한계를 미루는 것**뿐입니다 — 데이터가 늘면 다시 터집니다."),
      C("N+1 해법","목록 100건에 대해 각각 작성자를 조회하는 코드를 고치는 가장 알맞은 방법은?",
        ["쿼리마다 캐시를 붙인다",
         "필요한 작성자 id 를 모아 IN 절로 한 번에 가져와 PHP 에서 매핑한다",
         "LIMIT 을 줄여 목록을 짧게 만든다",
         "인덱스를 추가한다"],1,
        "**왕복 횟수 자체를 줄이는 것**이 해법입니다 — 101번이 2번이 됩니다.\n⚠️ 디스트랙터 분석: 캐시는 첫 요청과 캐시 만료 직후에 그대로 터지고(무효화 문제까지 얻습니다), 목록을 줄이는 것은 기능을 줄이는 것이며, 인덱스는 **각 쿼리를 빠르게** 할 뿐 100번이 100번인 것은 그대로입니다.\n💡 ORM 을 쓴다면 같은 일을 `with('author')`(eager loading) 로 표현합니다."),
      C("마이그레이션","무중단 배포에서 컬럼 이름을 <code>name</code> → <code>full_name</code> 으로 바꿀 때 안전한 순서는?",
        ["ALTER TABLE 로 한 번에 바꾼다",
         "새 컬럼 추가 → 양쪽에 쓰기 → 기존 데이터 백필 → 읽기를 새 컬럼으로 전환 → 옛 컬럼 제거",
         "옛 컬럼을 지우고 새로 만든다",
         "배포 중 서비스를 내린다"],1,
        "배포 중에는 **옛 코드와 새 코드가 동시에 돕니다** — 한 번에 바꾸면 그 순간 한쪽이 없는 컬럼을 참조합니다.\n🎯 핵심은 '**모든 중간 상태에서 양쪽 코드가 동작해야 한다**' 는 것이고, 각 단계는 독립적으로 되돌릴 수 있어야 합니다.\n💡 같은 원리가 API 필드 변경·큐 메시지 스키마 변경에도 그대로 적용됩니다."),
      C("계산 위치","집계(합계·개수)를 PHP 에서 계산할지 DB 에서 계산할지 판단하는 <b>가장 실용적인 기준</b>은?",
        ["항상 PHP 가 낫다",
         "항상 DB 가 낫다",
         "네트워크로 옮겨야 하는 데이터의 양 — 원본 행 전체를 가져와야 한다면 DB 에서 집계하는 편이 낫다",
         "코드 가독성만 보면 된다"],2,
        "판단 기준은 **얼마나 많은 데이터를 옮기는가**입니다. 100만 행을 가져와 PHP 에서 더하는 것과 `SELECT SUM(...)` 한 줄을 받는 것은 비용이 다릅니다.\n⚠️ 다만 무조건 DB 가 낫지는 않습니다 — DB 는 **수평 확장이 가장 어려운 계층**이라, 이미 가져온 소량 데이터의 가공까지 DB 에 밀면 병목이 그쪽으로 몰립니다.\n💡 그래서 실무 규칙은 '**줄이는 일은 DB 에서, 꾸미는 일은 애플리케이션에서**' 입니다."),
    ] },
]},

/* ═══════════════ 유닛 11 ═══════════════ */
{ t:"성능과 런타임 — PHP 는 어떻게 빨라졌나", l:[

  { t:"OPcache · JIT · 메모리", xp:30,
    th:{ sum:"PHP 는 요청마다 소스를 다시 컴파일한다. **그 결과를 재사용하는 것(OPcache)** 이 PHP 성능의 8할이다.",
      body:[
        {h:"컴파일을 한 번만", t:"OPcache 는 파싱·컴파일 결과(opcode)를 **공유 메모리에 캐시**해 다음 요청부터 그 단계를 건너뛴다. 운영에서는 `opcache.validate_timestamps=0` 으로 파일 변경 검사까지 끄고 **배포 시 캐시를 비우는** 방식이 표준이다. `opcache.memory_consumption`·`max_accelerated_files` 가 부족하면 캐시가 계속 밀려나 효과가 사라진다."},
        {h:"JIT 의 자리", t:"PHP 8 의 JIT 는 opcode 를 기계어로 컴파일하지만, **전형적인 웹 요청에서는 이득이 거의 없다** — 시간의 대부분이 DB·네트워크 대기이기 때문이다. 이득이 나는 곳은 수치 연산·이미지 처리 같은 CPU 바운드다. 그리고 `memory_limit` 은 **요청 하나당** 한도라, 프로세스 수 × 한도가 서버 메모리를 넘지 않게 잡아야 한다."}],
      code:{ c:"; 운영 권장\nopcache.enable=1\nopcache.validate_timestamps=0   ; 배포 때 캐시 초기화\nopcache.memory_consumption=256\nopcache.max_accelerated_files=20000", cap:"파일 수보다 큰 값을 줘야 한다" },
      key:["OPcache 는 컴파일 결과를 재사용","운영은 validate_timestamps=0 + 배포 시 초기화","JIT 는 CPU 바운드에서만 이득","memory_limit 은 요청당 한도"] },
    q:[
      I("기능","PHP 소스의 컴파일 결과를 캐시해 성능을 높이는 내장 확장의 이름은?",
        ["OPcache","opcache","Opcache"],
        "정답은 **OPcache** 입니다.\n💡 PHP 5.5 부터 기본 포함이지만 **설정에서 켜야** 합니다. 켜기 전후로 처리량이 몇 배 차이 나는 것이 보통이라, 성능 문제를 조사할 때 가장 먼저 확인할 항목입니다."),
      C("무엇을 캐시하나","OPcache 가 캐시하는 것은?",
        ["데이터베이스 조회 결과","컴파일된 opcode","HTML 출력","세션 데이터"],1,
        "**컴파일 결과(opcode)** 입니다 — 소스 파싱과 컴파일 단계를 건너뜁니다.\n💡 조회 결과 캐시는 Redis·Memcached, 출력 캐시는 리버스 프록시(Varnish·CDN)의 몫으로 **계층이 다릅니다**."),
      C("배포","<code>opcache.validate_timestamps=0</code> 인 운영 서버에 새 코드를 배포하면?",
        ["자동으로 반영된다","캐시를 비우거나 PHP-FPM 을 재시작하기 전까지 옛 코드가 계속 돈다","오류가 난다","다음 요청부터 반영된다"],1,
        "파일 변경 검사를 끈 것이므로 **명시적으로 비우기 전까지 옛 코드가 돕니다**.\n🔧 배포 스크립트에 `opcache_reset()` 호출이나 FPM 리로드를 반드시 넣어야 합니다.\n⚠️ 심볼릭 링크로 배포 디렉터리를 바꾸는 방식은 realpath 캐시까지 얽혀 **일부만 새 코드인 상태**가 생길 수 있습니다 — 그래서 리로드가 더 안전합니다."),
      C("JIT","PHP 8 의 JIT 로 <b>큰 이득을 보기 어려운</b> 작업은?",
        ["만델브로 집합 계산","이미지 필터 처리","DB 조회가 대부분인 일반적인 웹 요청","수치 시뮬레이션"],2,
        "일반 웹 요청은 시간의 대부분이 **DB·네트워크 대기**라 CPU 를 빠르게 해도 총 시간이 거의 줄지 않습니다.\n💡 이것이 성능 작업의 일반 원칙과 같습니다 — **비중이 큰 곳을 고쳐야** 합니다. 전체의 5% 를 절반으로 줄여 봐야 2.5% 입니다.\n⚠️ JIT 는 메모리를 더 쓰고 디버깅을 어렵게 하므로, 이득이 측정되지 않으면 켜지 않는 편이 낫습니다."),
      C("메모리","<code>memory_limit = 256M</code> 의 의미는?",
        ["서버 전체가 쓸 수 있는 메모리","요청(스크립트) 하나가 쓸 수 있는 최대 메모리","OPcache 크기","세션 저장 한도"],1,
        "**요청 하나당** 한도입니다.\n⚠️ 그래서 `pm.max_children = 50` 이고 `memory_limit = 256M` 이면 최악의 경우 12.8GB 를 요구합니다 — 실제 평균 사용량으로 계산하되 여유를 두어야 OOM 킬러가 프로세스를 죽이지 않습니다.\n💡 CLI 는 기본이 무제한(-1)이라, 웹에서 터지던 배치가 CLI 에서는 잘 도는 착시가 생깁니다."),
      C("성능 순서","PHP 애플리케이션이 느릴 때 <b>가장 먼저 할 일</b>은?",
        ["JIT 를 켠다",
         "프로파일러나 쿼리 로그로 시간이 어디에 쓰이는지 측정한다",
         "서버를 늘린다",
         "모든 함수를 static 으로 바꾼다"],1,
        "**측정하지 않은 것을 고치지 않는다** 가 첫 규칙입니다.\n⚠️ 디스트랙터 분석: JIT·서버 증설은 원인을 모른 채 비용만 쓰는 것이고(N+1 이 원인이면 서버를 늘려도 DB 가 더 빨리 죽습니다), static 화는 성능에 거의 영향이 없으면서 테스트 가능성만 파괴합니다.\n🔧 도구는 Xdebug 프로파일러·Blackfire·`SET profiling` 이나 slow query log, 그리고 **요청당 쿼리 수 로깅** 이면 대부분 잡힙니다."),
    ] },

  { t:"PHP-FPM 과 동시성 모델", xp:30,
    th:{ sum:"PHP 는 스레드가 아니라 **프로세스로 동시성을 낸다**. 이 단순한 모델이 안정성과 한계를 동시에 만든다.",
      body:[
        {h:"프로세스 풀", t:"PHP-FPM 은 워커 프로세스 풀을 두고 요청 하나에 워커 하나를 붙인다 — 워커가 DB 응답을 기다리는 동안 그 워커는 **아무 일도 하지 않는다**. 그래서 동시 처리량의 상한은 `pm.max_children` 이고, 느린 외부 API 하나가 워커를 전부 점유하면 사이트 전체가 멈춘다. 타임아웃을 짧게 걸고 외부 호출을 큐로 미루는 것이 방어책이다."},
        {h:"장수 프로세스의 등장", t:"Swoole·RoadRunner·FrankenPHP 는 프로세스를 **살려 둔 채** 요청을 반복 처리해 부트스트랩 비용을 없앤다. 대신 shared-nothing 의 안전망이 사라져 **전역 상태·정적 프로퍼티·메모리 누수가 요청 사이로 샌다** — 기존 코드를 그대로 옮기면 사용자 A 의 데이터가 B 에게 보이는 사고가 난다."}],
      code:{ c:"; 워커 수는 (가용 메모리 / 요청당 평균 메모리) 로 잡는다\npm = dynamic\npm.max_children = 40\npm.max_requests = 500   ; 누수 대비 주기적 재생성", cap:"max_children 이 곧 동시 처리 상한" },
      key:["요청 하나에 워커 하나, 대기 중에도 점유","동시 처리 상한은 pm.max_children","느린 외부 호출이 전체를 멈춘다","장수 프로세스는 전역 상태가 샌다"] },
    q:[
      C("모델","PHP-FPM 이 동시 요청을 처리하는 방식은?",
        ["스레드 풀","프로세스 풀","단일 이벤트 루프","코루틴"],1,
        "**프로세스 풀**입니다 — 요청 하나에 워커 프로세스 하나가 배정됩니다.\n💡 그래서 한 요청이 크래시해도 다른 요청에 영향이 없습니다. 안정성을 얻고 메모리 효율을 내준 교환입니다."),
      C("상한","동시에 처리할 수 있는 요청 수를 결정하는 설정은?",
        ["memory_limit","max_execution_time","pm.max_children","post_max_size"],2,
        "`pm.max_children` 이 워커 수, 곧 **동시 처리 상한**입니다.\n💡 이 값을 넘는 요청은 큐에서 대기하다 `listen.backlog` 를 넘으면 502 가 됩니다 — 502 가 뜨는데 CPU 는 한가하다면 대개 여기입니다."),
      C("대기","워커가 외부 API 응답을 3초 기다리는 동안 그 워커는?",
        ["다른 요청을 처리한다","아무 일도 하지 않고 점유된 상태로 남는다","자동으로 종료된다","스레드를 하나 더 만든다"],1,
        "블로킹 모델이라 **점유된 채 놀고 있습니다**.\n⚠️ 그래서 외부 API 가 느려지면 워커가 순식간에 고갈되어 **사이트 전체가 멈춥니다** — 정작 그 API 를 쓰지 않는 페이지까지 함께 죽습니다.\n🔧 방어는 세 가지입니다: 짧은 타임아웃, 서킷 브레이커, 그리고 **즉시 응답이 필요 없는 호출은 큐로** 미루기."),
      I("설정","워커를 일정 요청 수마다 재생성해 메모리 누수를 완화하는 PHP-FPM 설정 키는?",
        ["pm.max_requests","max_requests"],
        "정답은 `pm.max_requests` 입니다.\n💡 근본 해결은 아니고 **완화책**입니다 — 확장 모듈의 누수처럼 애플리케이션이 고칠 수 없는 경우에 유용합니다. 값을 너무 작게 잡으면 재생성 비용이 커집니다."),
      C("장수 프로세스","Swoole·RoadRunner 처럼 프로세스를 살려 두는 런타임으로 옮길 때 <b>새로 생기는 위험</b>은?",
        ["OPcache 를 쓸 수 없다",
         "전역 변수·정적 프로퍼티·싱글턴에 남은 상태가 다음 요청으로 새어 사용자 간 데이터가 섞일 수 있다",
         "Composer 를 쓸 수 없다",
         "예외 처리가 동작하지 않는다"],1,
        "shared-nothing 이 사라지면 **요청이 끝나도 메모리가 초기화되지 않습니다**. 정적 캐시에 담아 둔 '현재 사용자' 가 다음 요청에 그대로 남는 것이 전형적인 사고입니다.\n⚠️ 메모리 누수도 이제 서비스를 죽입니다 — 기존 PHP 에서는 요청이 끝나며 자동 회수되던 것들입니다.\n🔧 그래서 이런 런타임은 요청마다 컨테이너를 초기화하고, 상태를 가진 싱글턴을 금지하는 규율을 함께 요구합니다."),
      C("용량 산정","워커 수를 정하는 방법으로 <b>가장 적절한</b> 것은?",
        ["CPU 코어 수와 같게 한다",
         "가용 메모리 ÷ 요청당 평균 메모리 로 상한을 잡고, 대기 시간 비중을 보며 조정한다",
         "가능한 한 크게 잡는다",
         "동시 접속자 수와 같게 한다"],1,
        "메모리가 1차 제약입니다 — 넘으면 스와핑이나 OOM 으로 **전체가 더 느려집니다**.\n💡 그다음 조정 축은 '워커가 CPU 를 쓰는 시간 대 대기하는 시간' 의 비율입니다. I/O 대기가 길면 코어 수보다 많이 잡는 것이 맞고(1번이 틀린 이유), CPU 바운드면 코어 수 근처가 맞습니다.\n⚠️ 크게 잡을수록 좋다는 발상은 부하가 몰릴 때 **모두가 조금씩 느려져 전부 타임아웃**되는 결과를 냅니다 — 차라리 큐에서 기다리게 하는 편이 낫습니다."),
    ] },
]},

/* ═══════════════ 유닛 12 ═══════════════ */
{ t:"모던 PHP — 8.x 가 바꾼 것들", l:[

  { t:"PHP 8 의 새 문법", xp:30,
    th:{ sum:"PHP 8 은 **보일러플레이트를 줄이고, 조용한 실패를 시끄럽게** 만드는 방향으로 움직였다.",
      body:[
        {h:"짧아진 것들", t:"생성자 프로퍼티 승격, `match` 표현식, 널세이프 연산자 `?->`, 이름 있는 인자, `str_contains`·`str_starts_with`, 배열 스프레드가 8.0 에 함께 들어왔다. `?->` 는 대상이 null 이면 **뒤 체인 전체를 건너뛰고 null 을 돌려준다** — 다만 '없어도 되는 값' 에만 써야 하고, 있어야 할 값을 `?->` 로 감추면 오류가 한참 뒤에 나타난다."},
        {h:"열거형과 불변", t:"8.1 의 `enum` 은 문자열 상수를 대체한다 — 타입 힌트에 쓸 수 있고 `from()`/`tryFrom()` 으로 안전하게 변환하며, 없는 값은 예외가 된다. backed enum 은 값을 갖고(`case Draft = 'draft'`), pure enum 은 갖지 않는다. `readonly` 프로퍼티, `never` 반환 타입, first-class callable 도 8.1 이다."}],
      code:{ c:"enum Status: string {\n    case Draft = 'draft';\n    case Live  = 'live';\n    public function label(): string {\n        return match($this) { self::Draft => '초안', self::Live => '공개' };\n    }\n}\nStatus::tryFrom($raw) ?? Status::Draft;", cap:"tryFrom 은 없으면 null, from 은 예외" },
      key:["생성자 승격·match·?->·이름 있는 인자 (8.0)","enum·readonly·never·first-class callable (8.1)","tryFrom 은 null, from 은 예외","?-> 는 없어도 되는 값에만"] },
    q:[
      C("널세이프","<code>$a?->b()?->c()</code> 에서 <code>$a</code> 가 null 이면?",
        ["오류가 난다","전체 표현식이 null 이 된다","b() 만 건너뛴다","빈 문자열이 된다"],1,
        "널세이프 연산자는 **뒤 체인 전체를 건너뛰고 null** 을 돌려줍니다.\n⚠️ 편한 만큼 위험합니다 — 반드시 있어야 할 객체가 없는 상황을 `null` 로 덮어 버리면, 진짜 문제는 한참 뒤 엉뚱한 곳에서 드러납니다. '없을 수도 있음' 이 **설계상 정상인 경우에만** 쓰세요."),
      C("생성자 승격","<code>public function __construct(private string $name) {}</code> 이 하는 일은?",
        ["인자를 검증한다",
         "프로퍼티 선언과 할당을 한 번에 처리한다",
         "name 을 상수로 만든다",
         "생성자를 private 으로 만든다"],1,
        "프로퍼티 선언 + `$this->name = $name;` 을 **한 줄로** 끝냅니다(PHP 8.0).\n💡 `readonly` 를 함께 붙이면 불변 값 객체가 세 줄로 완성됩니다. 다만 프로퍼티마다 PHPDoc 이 필요한 경우에는 오히려 읽기 어려워질 수 있습니다."),
      C("enum","<code>Status::tryFrom('unknown')</code> 의 결과는? (backed enum 기준)",
        ["예외","null","첫 번째 case","빈 문자열"],1,
        "`tryFrom` 은 없으면 **null**, `from` 은 **`ValueError` 예외**입니다.\n🎯 선택 기준이 분명합니다 — 외부 입력(쿼리스트링·API)에는 `tryFrom` + 기본값, 내부 데이터(DB 에 저장된 우리 값)에는 `from` 이 맞습니다. 후자에서 실패는 **데이터 손상 신호**라 조용히 넘기면 안 됩니다.",
        P("[Status::tryFrom('unknown'), Status::tryFrom('draft')->value]",
          "array (\n  0 => NULL,\n  1 => 'draft',\n)",
          "enum Status: string { case Draft = 'draft'; case Live = 'live'; }")),
      I("버전","<code>enum</code>·<code>readonly</code> 프로퍼티가 도입된 PHP 버전은? (예: 8.0 형식)",
        ["8.1","PHP 8.1"],
        "정답은 **8.1** 입니다.\n💡 8.0 은 match·널세이프·이름 있는 인자·생성자 승격·union 타입, 8.2 는 readonly 클래스와 DNF 타입, 8.3 은 타입 있는 클래스 상수와 `#[\\Override]`, 8.4 는 프로퍼티 훅과 비대칭 가시성입니다."),
      C("enum 의 이점","문자열 상수 대신 <code>enum</code> 을 쓰면 얻는 <b>가장 큰 이점</b>은?",
        ["실행 속도",
         "가능한 값의 집합이 타입으로 표현되어, 잘못된 값이 함수에 들어오는 것을 시그니처와 정적 분석이 막아 준다",
         "메모리 절약",
         "JSON 직렬화가 자동으로 된다"],1,
        "`function setStatus(Status $s)` 라고 쓰는 순간 **틀린 값이 들어올 수 없습니다** — 오타 난 `'drfat'` 은 문자열 상수에서는 통과하지만 enum 에서는 애초에 존재하지 않습니다.\n💡 여기에 `match($this)` 를 조합하면 **case 를 추가했을 때 처리하지 않은 곳이 예외로 드러납니다**. 이것이 '상태를 늘려도 안전한' 구조의 핵심입니다.\n⚠️ backed enum 은 `JsonSerializable` 없이도 값으로 직렬화되지만 pure enum 은 안 되므로, 4번은 단서 없이는 틀립니다."),
      C("이름 있는 인자","라이브러리 저자가 <b>이름 있는 인자를 지원할 때 주의할 점</b>은?",
        ["성능이 느려진다",
         "인자 이름이 공개 API 의 일부가 되어, 이름 변경이 파괴적 변경이 된다",
         "기본값을 쓸 수 없다",
         "인터페이스에서는 쓸 수 없다"],1,
        "호출자가 `f(limit: 10)` 이라고 쓰기 시작하면 **인자 이름을 바꾸는 순간 그 코드가 깨집니다**.\n💡 그래서 라이브러리에서는 인자 이름도 시맨틱 버저닝의 대상으로 다뤄야 하고, 내부용 함수에는 `@internal` 로 계약이 아님을 표시합니다.\n⚠️ 인터페이스 구현에서 **인자 이름을 바꾸는 것**도 같은 이유로 위험합니다 — 인터페이스 타입으로 호출하면 인터페이스의 이름이, 구현체 타입으로 호출하면 구현체의 이름이 쓰입니다."),
    ] },

  { t:"타입 시스템과 정적 분석", xp:30,
    th:{ sum:"PHP 의 타입은 **런타임 검사**이고, 정적 분석기는 **실행 전 검사**다. 둘은 경쟁이 아니라 보완이다.",
      body:[
        {h:"런타임이 지켜 주는 것", t:"프로퍼티 타입(7.4)·union 타입(8.0)·intersection 타입(8.1)·`never`(8.1) 로 계약을 코드에 적을 수 있다. `declare(strict_types=1)` 을 켜면 자동 변환 없이 정확히 일치해야 하고, 어기면 `TypeError` 가 **경계에서** 난다. 타입이 붙은 프로퍼티는 초기화 전에 읽으면 오류가 나므로, '초기화되지 않음' 과 'null' 이 구분된다."},
        {h:"정적 분석이 잡는 것", t:"PHPStan·Psalm 은 실행하지 않고 타입 불일치·도달 불가 코드·null 참조를 찾는다. 배열의 내부 구조처럼 PHP 타입 시스템이 표현하지 못하는 것은 `@param array<int, string>` 같은 **PHPDoc 제네릭**으로 알려 준다. 도입은 낮은 레벨에서 시작해 baseline 으로 기존 오류를 묶어 두고, **새 코드만 엄격하게** 올리는 방식이 현실적이다."}],
      code:{ c:"/** @param array<int, array{id:int, name:string}> $rows */\nfunction names(array $rows): array {\n    return array_column($rows, 'name');\n}", cap:"배열의 모양은 PHPDoc 으로 알린다" },
      key:["strict_types 는 경계에서 실패시킨다","타입 프로퍼티는 미초기화와 null 을 구분","배열 구조는 PHPDoc 제네릭으로","baseline 으로 점진 도입"] },
    q:[
      C("union","<code>function f(int|string $v)</code> 가 뜻하는 것은?",
        ["int 를 string 으로 바꾼다","int 또는 string 을 받는다","int 와 string 을 둘 다 받아야 한다","오류다"],1,
        "**union 타입**(PHP 8.0)으로 '둘 중 하나' 입니다.\n💡 `A&B`(intersection, 8.1)는 '둘 다 만족' 이고, 8.2 의 DNF 타입은 `(A&B)|null` 처럼 조합할 수 있게 했습니다."),
      C("nullable","<code>?string</code> 과 동등한 표기는?",
        ["string|false","string|null","string[]","string&null"],1,
        "`?string` 은 `string|null` 의 축약입니다.\n💡 다만 `?string` 은 **union 안에서는 쓸 수 없어서**(`?string|int` 불가) 여러 타입을 조합할 때는 `string|int|null` 로 풀어 씁니다."),
      I("도구","PHP 코드를 실행하지 않고 타입 오류를 찾는 대표적인 정적 분석 도구는? (하나만)",
        ["PHPStan","phpstan","Psalm","psalm"],
        "**PHPStan** 또는 **Psalm** 입니다.\n💡 둘 다 레벨(엄격도)을 단계적으로 올릴 수 있어서, 기존 프로젝트에 **한 번에 다 고치지 않고** 도입할 수 있습니다."),
      C("미초기화","<code>private string $name;</code> 라고 선언하고 값을 넣기 전에 읽으면?",
        ["null 이 반환된다","빈 문자열이 반환된다","Error 가 발생한다","경고 후 null"],2,
        "타입이 붙은 프로퍼티는 **'초기화되지 않음' 상태**를 가지며, 읽으면 `Error: Typed property must not be accessed before initialization` 입니다.\n🎯 이것이 오히려 좋은 설계입니다 — `null` 로 슬쩍 넘어가지 않고, **객체가 불완전하게 만들어졌다는 사실이 그 자리에서** 드러납니다.\n💡 그래서 생성자에서 모든 프로퍼티를 채우는 습관이 자연스럽게 강제됩니다.",
        P("(function(){ $o = new T12(); try { $o->read(); return 'ok'; } catch (\\Error $e) { return 'Error'; } })()",
          "'Error'",
          "class T12 { private string $name; public function read(){ return $this->name; } }")),
      C("PHPDoc","<code>@param array&lt;string, int&gt; $counts</code> 를 적는 이유는?",
        ["런타임에 검사된다",
         "PHP 타입 시스템이 표현하지 못하는 배열 내부 구조를 정적 분석기와 IDE 에 알려 주기 위해",
         "성능이 좋아진다",
         "array 타입 힌트를 대체한다"],1,
        "PHP 는 `array` 까지만 알고 **키와 값의 타입은 모릅니다**. PHPDoc 제네릭이 그 공백을 메웁니다.\n⚠️ 런타임에는 아무 효과가 없으므로(1번이 틀린 이유), 신뢰 경계에서는 여전히 실제 검증이 필요합니다.\n💡 배열 대신 **값 객체나 enum 을 쓰면** 애초에 PHPDoc 이 필요 없어집니다 — 주석으로 설명해야 하는 구조는 대개 타입으로 만들 수 있다는 신호입니다."),
      C("도입 전략","오래된 PHP 코드베이스에 정적 분석을 도입하는 <b>가장 현실적인</b> 방법은?",
        ["최고 레벨로 켜고 모든 오류를 고칠 때까지 배포를 멈춘다",
         "낮은 레벨에서 시작해 기존 오류를 baseline 으로 묶고, 새 코드와 손대는 파일부터 통과시키며 레벨을 올린다",
         "새 프로젝트에만 쓴다",
         "분석기 대신 테스트만 늘린다"],1,
        "**baseline** 은 기존 오류를 '알고 있음' 으로 기록해 CI 를 통과시키면서, **새로 생기는 오류는 막아 줍니다**.\n⚠️ 디스트랙터 분석: 1번은 배포를 멈추므로 조직이 받아들이지 않고, 3번은 정작 버그가 많은 옛 코드를 방치하며, 4번은 보완재를 대체재로 착각한 것입니다 — 테스트는 **실행한 경로**만, 분석기는 **모든 경로**를 봅니다.\n💡 파일을 수정할 때마다 그 파일의 baseline 항목을 지우는 규칙(boy scout rule)을 붙이면 자연스럽게 줄어듭니다."),
    ] },
]},

];
