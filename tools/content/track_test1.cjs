/* Testing 트랙 전반부 — 유닛 1~6.
   왜 테스트하나 · 좋은 테스트의 모양 · 테스트 더블 · 경계값 · 피라미드 · TDD.

   오답은 정답만큼 구체적으로 쓴다. 짧은 한 마디로 두면 '가장 긴 보기가 정답'
   이라는 규칙이 생겨, 읽지 않고도 맞히는 문항이 되기 때문이다. */

const {shuffleUnits}=require("./shuffle_choice.cjs");

const C=(k,q,o,a,ex)=>({t:"choice",k,q,o,a,ex});
const I=(k,q,a,ex)=>({t:"input",k,q,a,ex});

const UNITS=[

/* ═══════════════ 유닛 1 ═══════════════ */
{ t:"왜 테스트를 쓰는가", l:[

  { t:"테스트는 증명이 아니라 안전망이다", xp:15,
    th:{ sum:"테스트는 '이 코드가 옳다' 를 증명하지 못한다. **'이 경우에는 이렇게 동작한다'** 를 못 박아 둘 뿐이다. 그런데 그것만으로도 값어치가 충분하다.",
      body:[
        {h:"테스트가 실제로 사는 순간", t:"테스트를 처음 쓸 때의 이득은 크지 않다. 방금 짠 코드라 어차피 머릿속에 있기 때문이다. **진짜 값어치는 몇 달 뒤 그 코드를 고칠 때** 나온다. 무엇이 깨졌는지 즉시 알려 주므로 과감하게 구조를 바꿀 수 있다. 테스트가 없으면 '건드리면 무서운 코드' 가 되고, 그 코드는 점점 더 나빠지기만 한다."},
        {h:"없음을 증명할 수는 없다", t:"다익스트라의 말대로 테스트는 **버그가 있음을 보일 수는 있어도 없음을 보일 수는 없다.** 그래서 '테스트를 다 통과했으니 안전하다' 는 결론은 성립하지 않는다. 테스트가 주는 것은 확실성이 아니라 **되돌아갈 자리와 빠른 피드백**이다. 이 기대를 정확히 갖는 것이 테스트를 잘 쓰는 첫 조건이다."}],
      code:{ c:"// 이 테스트가 말하는 것\n//  \"할인율 10%, 가격 1000원이면 900원이다\"\n// 말하지 않는 것\n//  \"할인 계산에 버그가 없다\"\nexpect(discount(1000, 0.1)).toBe(900);", cap:"못 박는 것은 사례이지 정확성이 아니다" },
      key:["증명이 아니라 안전망","값어치는 고칠 때 나온다","버그가 없음은 보일 수 없다","되돌아갈 자리와 빠른 피드백"] },
    q:[
      C("가장 큰 이득","자동 테스트를 갖춰서 얻는 가장 큰 이득으로 알맞은 것은?",
        ["코드 어디에도 버그가 하나도 남아 있지 않다는 사실을 확실하게 증명해 준다는 점",
         "고칠 때 무엇이 깨졌는지 즉시 알 수 있다는 점",
         "테스트를 쓰는 동안 코드를 짜는 시간 자체가 크게 줄어든다는 점",
         "코드 리뷰를 아예 생략해도 되도록 만들어 준다는 점"],1,
        "테스트의 값어치는 **미래에 고칠 때** 나옵니다. 방금 짠 코드는 어차피 머릿속에 있어 테스트가 알려 줄 것이 적습니다.\n그래서 '한 번 쓰고 버릴 코드' 에는 테스트가 과할 수 있고, **오래 살면서 계속 고쳐질 코드**일수록 이득이 큽니다.\n버그가 없음을 증명하지는 못합니다 — 테스트는 확인한 경우에 대해서만 말합니다."),
      C("증명의 한계","'테스트를 전부 통과했다' 는 사실이 보장하지 <b>못하는</b> 것은?",
        ["테스트가 확인한 그 경우들에서 코드가 기대대로 동작한다는 사실",
         "확인하지 않은 입력에서도 코드에 결함이 없다는 사실",
         "이전에 고쳤던 버그가 다시 돌아오지는 않았다는 사실",
         "빌드가 깨지지 않고 코드가 실행은 된다는 사실"],1,
        "테스트는 **확인한 경우**에 대해서만 말합니다. 확인하지 않은 입력은 아무것도 보장하지 않습니다.\n그래서 커버리지 100% 여도 버그가 남습니다 — 모든 줄을 실행했다는 것과 모든 조건을 확인했다는 것은 다릅니다.\n이 한계를 알고 나면 '어떤 경우를 확인할지' 고르는 일이 테스트 작성의 본체라는 것이 보입니다."),
      C("무서운 코드","테스트가 없는 오래된 코드가 점점 더 나빠지는 이유로 알맞은 것은?",
        ["코드가 시간이 지나면 자연스럽게 성능이 떨어지기 때문이다",
         "무엇이 깨질지 몰라 정리하지 못하고 덧붙이기만 하기 때문이다",
         "오래전에 쓰던 언어 문법이 새 버전에서는 더 이상 동작하지 않게 되어 버리기 때문이다",
         "파일이 커지면 편집기가 느려져 작업이 어려워지기 때문이다"],1,
        "**건드리기 무서우면 정리하지 못합니다.** 그래서 필요한 것만 조심스레 덧붙이게 되고, 코드는 계속 부풀어 오릅니다.\n이것이 테스트의 진짜 값어치가 '버그 발견' 이 아니라 **'변경 가능성'** 인 이유입니다.\n레거시 코드를 다룰 때 먼저 테스트로 현재 동작을 감싸는(characterization test) 방법이 널리 쓰이는 것도 같은 이유입니다."),
      I("회귀","한 번 고친 버그가 나중에 다시 나타나는 것을 뜻하는 말은? (한국어)",
        ["회귀","리그레션","regression"],
        "정답은 **회귀(regression)** 입니다. 그 버그를 재현하는 테스트를 남겨 두면 다시 돌아오는 것을 막을 수 있습니다.\n버그를 고칠 때 **먼저 그 버그를 재현하는 테스트를 쓰는** 습관이 좋습니다 — 그러면 고쳤다는 것도 확인되고, 재발도 막힙니다."),
      C("테스트가 덜 필요한 곳","다음 중 자동 테스트의 이득이 상대적으로 가장 작은 것은?",
        ["여러 사람이 오래 고쳐 갈 결제 정산 로직",
         "한 번 실행하고 버릴 일회성 데이터 이전 스크립트",
         "외부에 공개되어 다른 여러 팀이 의존하고 있는 라이브러리의 인터페이스",
         "장애가 나면 사용자에게 바로 영향을 주는 인증 처리"],1,
        "**한 번 쓰고 버릴 코드**는 미래에 고칠 일이 없으므로 테스트의 주된 이득이 발생하지 않습니다.\n다만 데이터 이전은 되돌리기 어려운 작업이라, 테스트 대신 **예행연습과 백업**으로 위험을 다루는 것이 보통입니다.\n'테스트를 얼마나 쓸 것인가' 는 코드가 얼마나 오래 살고 얼마나 자주 바뀌는지로 판단합니다."),
      C("테스트의 부수 효과","테스트를 쓰다 보면 설계가 좋아지는 경우가 있는데 그 이유로 알맞은 것은?",
        ["테스트 도구가 코드 구조를 자동으로 정리해 주기 때문이다",
         "테스트하기 어려운 코드는 대개 의존이 뒤엉켜 있다는 신호이기 때문이다",
         "테스트를 함께 써 두면 본코드의 전체 줄 수가 자연스럽게 줄어들게 되기 때문이다",
         "테스트 프레임워크가 권장하는 패턴을 따르게 되기 때문이다"],1,
        "**테스트하기 어렵다는 것은 설계의 신호**입니다. 함수 하나를 확인하려고 데이터베이스와 외부 API 를 다 띄워야 한다면, 그 함수가 너무 많은 것에 얽혀 있다는 뜻입니다.\n의존을 인자로 받게 바꾸면 테스트도 쉬워지고 재사용도 쉬워집니다 — 두 이득이 같은 뿌리에서 나옵니다.\n그래서 테스트를 '검증 도구' 가 아니라 **'설계 피드백'** 으로 쓰는 사람들이 있습니다."),
      C("적정선","테스트를 얼마나 쓸지 정할 때의 판단 기준으로 알맞은 것은?",
        ["커버리지 목표 숫자를 정해 두고 그 숫자에 도달할 때까지 계속 쓴다",
         "깨졌을 때의 대가가 크고 자주 바뀌는 곳에 더 많이 쓴다",
         "파일 하나당 테스트 하나씩 균등하게 배분해서 쓴다",
         "새로 짠 코드에만 쓰고 기존 코드에는 쓰지 않는다"],1,
        "**위험과 변경 빈도**가 기준입니다. 결제·인증처럼 깨지면 대가가 큰 곳, 그리고 자주 손대는 곳에 집중합니다.\n커버리지 숫자를 목표로 삼으면 쉬운 곳만 채워 숫자만 좋아집니다 — 정작 위험한 곳은 테스트하기 어려워 뒤로 밀립니다.\n균등 배분도 같은 문제를 안고 있습니다. 모든 코드의 위험이 같지 않기 때문입니다.")
    ] },

  { t:"좋은 테스트의 모양", xp:15,
    th:{ sum:"좋은 테스트는 **실패했을 때 무엇이 왜 잘못됐는지 바로 알려 준다.** 그러려면 하나만 확인하고, 이름이 그것을 말하고, 준비·실행·확인이 나뉘어 있어야 한다.",
      body:[
        {h:"준비 · 실행 · 확인", t:"테스트 본문을 **Arrange(준비) → Act(실행) → Assert(확인)** 세 덩어리로 나누면 읽는 사람이 즉시 구조를 파악한다. 실행이 여러 번 섞여 있으면 어느 실행 때문에 실패했는지 알 수 없다. 확인이 여러 개면 첫 확인에서 멈춰 나머지는 보이지도 않는다."},
        {h:"이름이 곧 문서", t:"`test1`·`testUser` 같은 이름은 실패 목록에서 아무것도 알려 주지 않는다. **'무엇이, 어떤 조건에서, 어떻게 되어야 하는가'** 를 담으면 실패 메시지만 읽고도 원인을 짐작할 수 있다. 잘 지은 이름들의 목록은 그 자체로 **그 모듈이 무엇을 약속하는지에 대한 문서**가 된다."}],
      code:{ c:"test(\"재고가 0이면 주문은 거부된다\", () => {\n  const cart = makeCart({ stock: 0 });   // 준비\n  const result = order(cart);            // 실행\n  expect(result.ok).toBe(false);         // 확인\n});", cap:"이름이 실패 목록에서 그대로 읽힌다" },
      key:["준비·실행·확인으로 나눈다","하나의 테스트는 하나만 확인","이름에 조건과 기대를 담는다","실패 메시지가 곧 진단"] },
    q:[
      C("이름 짓기","테스트 이름으로 가장 적절한 것은?",
        ["할인 계산 함수가 올바르게 동작하는지 전반적으로 확인하는 테스트",
         "회원 등급이 없으면 할인율은 0이다",
         "test_discount_function_case_number_three_with_various_inputs",
         "할인 관련 로직 검증 및 예외 상황 처리 확인"],1,
        "**조건과 기대**가 한 문장에 들어 있어야 실패 목록만 보고도 무슨 일인지 알 수 있습니다.\n'올바르게 동작하는지' 같은 표현은 무엇을 확인했는지 알려 주지 않아, 실패했을 때 결국 코드를 열어 봐야 합니다.\n이름을 잘 지으면 테스트 목록이 그 모듈의 명세서가 됩니다."),
      C("확인의 개수","테스트 하나에 확인(assert)을 여러 개 넣을 때 생기는 문제로 알맞은 것은?",
        ["테스트를 실행하는 데 걸리는 시간이 크게 늘어난다",
         "첫 실패에서 멈춰 나머지 확인이 잘린다",
         "테스트 프레임워크가 그런 코드를 문법 오류로 처리해 아예 실행하지 않는다",
         "커버리지 계산에서 그 테스트가 제외되어 버린다"],1,
        "대부분의 프레임워크는 **첫 실패에서 멈춥니다.** 뒤의 확인이 어땠을지는 알 수 없어, 한 번에 하나씩만 고치며 여러 번 돌리게 됩니다.\n같은 대상의 여러 속성을 함께 보고 싶다면 객체 전체를 한 번에 비교하거나, 프레임워크가 제공하는 '모아서 확인' 기능을 씁니다.\n무엇보다 확인이 여러 개라는 것은 **테스트가 여러 가지를 확인하려 한다**는 신호인 경우가 많습니다."),
      C("실행 단계","테스트 본문에서 실행(Act)이 여러 번 나오면 생기는 문제로 알맞은 것은?",
        ["테스트 실행 순서가 무작위로 뒤바뀌어 결과를 전혀 예측할 수 없게 된다",
         "실패했을 때 어느 실행 때문에 깨졌는지 좁히기 어려워진다",
         "준비 단계에서 만든 데이터가 자동으로 초기화되어 사라진다",
         "테스트 프레임워크가 중복 실행으로 판단해 하나만 실행한다"],1,
        "실행이 여럿이면 **원인을 좁히는 데 시간이 걸립니다.** 실패 메시지가 결과만 알려 주지 어느 단계 때문인지는 말해 주지 않기 때문입니다.\n흐름 전체를 확인해야 한다면 그것은 통합·E2E 테스트의 몫이고, 단위 테스트는 한 동작에 집중하는 편이 좋습니다.\n준비를 여러 번 하는 것은 괜찮습니다 — 문제는 실행과 확인이 뒤섞이는 것입니다."),
      I("세 단계","테스트 본문을 나누는 세 단계를 영어 머리글자로 부르는 말은?",
        ["AAA","Arrange Act Assert","arrange-act-assert"],
        "정답은 **AAA**(Arrange·Act·Assert)입니다. Given-When-Then 이라고도 부르며 뜻은 같습니다.\n빈 줄 하나로 세 덩어리를 나눠 두기만 해도 읽는 속도가 눈에 띄게 달라집니다."),
      C("독립성","테스트가 서로 독립적이어야 하는 이유로 알맞은 것은?",
        ["테스트 코드의 전체 줄 수를 줄이는 데 도움이 되기 때문이다",
         "순서에 기대면 하나만 따로 돌릴 수 없기 때문이다",
         "테스트 프레임워크가 독립적이지 않은 테스트를 알아서 찾아내 자동으로 건너뛰어 버리기 때문이다",
         "독립적인 테스트만 커버리지 계산에 반영되기 때문이다"],1,
        "**하나만 따로 돌릴 수 있어야** 디버깅이 됩니다. 앞 테스트가 남긴 데이터에 기대면 그 테스트만 실행했을 때 실패합니다.\n실행 순서를 무작위로 섞어 돌려 보면 숨은 의존이 드러납니다 — 여러 프레임워크가 이 기능을 제공합니다.\n병렬 실행도 독립성이 전제라, 의존이 있으면 속도를 올릴 수도 없습니다."),
      C("공유 상태","여러 테스트가 같은 데이터베이스를 쓸 때 지켜야 할 원칙으로 알맞은 것은?",
        ["테스트마다 데이터베이스 서버를 새로 띄워 완전히 분리한다",
         "자기가 만든 데이터만 쓰고 끝나면 되돌린다",
         "테스트를 항상 정해진 순서대로만 실행하도록 강제한다",
         "데이터베이스를 쓰는 테스트는 대표가 되는 하나만 남겨 두고 나머지는 전부 지워 버린다"],1,
        "**자기가 만든 것만 쓰고 정리하는 것**이 원칙입니다. 트랜잭션으로 감싸 되돌리거나, 테스트마다 고유한 값을 쓰는 방법이 흔합니다.\n서버를 매번 새로 띄우면 확실하지만 너무 느려 아무도 돌리지 않게 됩니다.\n순서를 강제하는 것은 문제를 덮는 것이고, 하나를 뺀 나머지를 지우는 것은 검증을 버리는 선택입니다."),
      C("테스트 코드의 품질","테스트 코드에 대한 태도로 알맞은 것은?",
        ["테스트는 보조 코드이므로 중복이나 복잡함을 신경 쓰지 않아도 된다",
         "의도가 즉시 보여야 하므로 본코드만큼 읽기 쉬워야 한다",
         "테스트 코드는 짧을수록 좋으므로 최대한 압축해서 쓴다",
         "테스트 안에 조건문과 반복문을 적극적으로 넣어서 여러 경우를 한꺼번에 다루도록 한다"],1,
        "테스트는 **읽히려고 있는 코드**입니다. 실패했을 때 사람이 읽고 판단해야 하므로, 의도가 즉시 보여야 합니다.\n다만 본코드와 다른 점이 하나 있습니다 — 테스트에는 **조건문과 반복문을 되도록 넣지 않습니다.** 테스트 안에 로직이 있으면 그 로직 자체가 틀릴 수 있고, 무엇을 확인하는지도 흐려집니다.\n중복은 어느 정도 허용됩니다. 지나치게 추상화하면 테스트를 읽어도 무슨 상황인지 알 수 없게 됩니다.")
    ] }
]},

/* ═══════════════ 유닛 2 ═══════════════ */
{ t:"무엇을 확인할 것인가 — 경계와 예외", l:[

  { t:"버그는 경계에 산다", xp:20,
    th:{ sum:"결함은 한가운데가 아니라 **끝과 경계**에 몰린다. 0, 1, 최댓값, 빈 값, 음수 — 확인할 것을 고르는 일은 대개 이 목록에서 시작한다.",
      body:[
        {h:"경계값 세 개씩", t:"조건이 '10 이상' 이라면 **9, 10, 11** 을 확인한다. `>=` 를 `>` 로 잘못 쓴 실수가 정확히 이 지점에서 드러나기 때문이다. 한가운데 값(예: 50)은 이런 실수를 잡지 못한다. 범위가 있으면 양쪽 끝에서 각각 세 개씩 보는 것이 기본이다."},
        {h:"빈 것과 하나와 여럿", t:"목록을 다루는 코드는 **비어 있을 때 · 하나일 때 · 여럿일 때**를 각각 본다. 빈 목록에서 `[0]` 을 읽거나 평균을 내며 0으로 나누는 사고가 여기서 걸린다. '하나' 를 따로 보는 이유는 첫 원소와 마지막 원소가 같아지는 특수 상황이기 때문이다."}],
      code:{ c:"// 조건이 '10 이상' 이면\ntest(\"9는 통과하지 못한다\",  () => expect(ok(9)).toBe(false));\ntest(\"10은 통과한다\",       () => expect(ok(10)).toBe(true));\ntest(\"11도 통과한다\",       () => expect(ok(11)).toBe(true));", cap:"경계 바로 양옆을 본다" },
      key:["결함은 경계에 몰린다","경계 앞·경계·경계 뒤 셋","빈 것·하나·여럿을 각각","한가운데 값은 실수를 못 잡는다"] },
    q:[
      C("경계값","'점수가 60점 이상이면 합격' 인 함수를 확인할 때 가장 중요한 입력 조합은?",
        ["0점과 50점과 100점을 각각 넣어 전체 범위를 훑는다",
         "59점과 60점과 61점을 각각 넣어 경계 바로 양옆을 본다",
         "임의의 값 100개를 무작위로 만들어 한꺼번에 넣는다",
         "실제 사용에서 가장 자주 들어오는 값인 70점과 80점을 중심으로 본다"],1,
        "`>=` 를 `>` 로 잘못 쓴 실수는 **정확히 60에서만** 드러납니다. 50이나 100은 아무리 넣어도 잡지 못합니다.\n그래서 경계가 있는 조건은 **바로 앞·경계·바로 뒤** 셋을 보는 것이 기본입니다.\n무작위 값은 다른 종류의 이득이 있지만, 경계를 확실히 밟는다는 보장이 없습니다."),
      C("목록 처리","목록을 다루는 함수에서 반드시 확인해야 할 경우로 가장 중요한 것은?",
        ["원소가 1000개를 훌쩍 넘는 아주 큰 목록에서의 처리 속도",
         "목록이 비어 있을 때와 원소가 하나뿐일 때의 동작",
         "원소의 순서가 무작위로 섞여 있을 때의 결과",
         "목록 안에 같은 값이 여러 번 들어 있을 때의 결과"],1,
        "**빈 목록**은 `[0]` 접근과 0으로 나누기의 단골 무대입니다. **원소 하나**는 첫 원소와 마지막 원소가 같아지는 특수 상황이라 따로 봐야 합니다.\n큰 목록의 속도나 중복 처리도 중요할 수 있지만, 그것은 요구사항에 따라 달라지는 반면 빈 목록은 거의 언제나 들어옵니다.\n'설마 빈 값이 오겠어' 라고 생각한 곳에서 실제로 옵니다."),
      C("예외 확인","오류를 던져야 하는 상황을 테스트할 때의 원칙으로 알맞은 것은?",
        ["오류가 났다는 사실만 확인하면 충분하므로 종류는 보지 않는다",
         "어떤 종류의 오류가 어떤 메시지와 함께 나는지까지 확인한다",
         "오류가 나는 경우는 테스트하기 까다로우므로 사람의 수동 확인에 맡긴다",
         "오류를 잡아서 로그만 남기고 테스트는 통과시킨다"],1,
        "**종류까지 확인해야** 의미가 있습니다. 그러지 않으면 엉뚱한 이유로 난 오류(오타로 인한 `TypeError` 등)도 통과시켜 버립니다.\n실제로 '검증 실패' 를 기대했는데 다른 곳의 널 참조 오류가 나서 테스트가 통과하는 일이 흔합니다.\n메시지까지 확인할지는 판단입니다 — 사용자에게 보이는 메시지라면 확인할 값어치가 있습니다."),
      I("경계","0, 1, 최댓값처럼 범위의 끝을 확인하는 값들을 뭐라고 부르나? (한국어)",
        ["경계값","경계 값","boundary"],
        "정답은 **경계값(boundary value)** 입니다. 결함이 몰려 있는 자리라 가장 먼저 확인합니다.\n경계는 숫자만이 아닙니다 — 빈 문자열, 널, 첫날과 마지막 날, 권한의 최소·최대도 전부 경계입니다."),
      C("동치 분류","입력을 몇 개의 묶음으로 나눠 각 묶음에서 하나씩만 확인하는 방법의 근거로 알맞은 것은?",
        ["테스트 개수가 적을수록 실행 시간이 짧아지기 때문이다",
         "같은 경로로 처리되는 입력은 하나만 봐도 되기 때문이다",
         "무작위로 값을 골라 넣으면 우연히 같은 값이 여러 번 반복해서 나올 수 있기 때문이다",
         "묶음마다 담당자를 나눠 테스트를 작성하기 편하기 때문이다"],1,
        "**같은 경로로 처리되는 입력은 하나만 봐도 됩니다.** 이것이 동치 분할(equivalence partitioning)의 근거입니다.\n'음수 / 0 / 양수' 로 나뉜다면 각 묶음에서 하나씩만 확인하면 되고, 여기에 경계값을 더하면 대부분의 결함이 걸립니다.\n다만 '같은 경로' 라는 판단이 틀릴 수 있어, 코드 구조가 바뀌면 분류도 다시 봐야 합니다."),
      C("널과 빈 값","<code>null</code>·빈 문자열·공백만 있는 문자열을 각각 확인해야 하는 이유로 알맞은 것은?",
        ["세 값은 대부분의 프로그래밍 언어에서 서로 완전히 같은 것으로 취급되기 때문이다",
         "서로 다르게 처리되는데 검증이 하나만 막기 때문이다",
         "세 값 모두 데이터베이스에 저장할 수 없기 때문이다",
         "세 값은 사용자가 실제로 입력할 수 없는 값이기 때문이다"],1,
        "'이름을 입력하세요' 검증이 **빈 문자열만 막고 공백은 통과시키는** 일이 아주 흔합니다. `null` 은 또 다른 경로로 들어옵니다.\n세 경우가 각각 다른 코드 경로를 타므로 하나씩 확인해야 합니다.\n입력 검증은 경계가 몰려 있는 자리라, 테스트를 쓸 값어치가 특히 큰 영역입니다."),
      C("무작위 입력","무작위로 만든 입력을 대량으로 넣어 성질이 지켜지는지 보는 방식의 장점으로 알맞은 것은?",
        ["사람이 생각하지 못한 조합을 찾아내 경계 밖의 결함을 드러낸다는 점",
         "테스트 실행 시간이 예측 가능하게 짧아진다는 점",
         "실패한 경우를 항상 같은 방식으로 재현할 수 있다는 점",
         "테스트 코드를 사람이 직접 쓰지 않아도 된다는 점"],1,
        "**속성 기반 테스트(property-based testing)** 의 이득은 사람의 상상 밖에 있는 입력을 찾아 준다는 것입니다.\n'정렬한 결과의 길이는 원본과 같다' 같은 **성질**을 적어 두면 도구가 수천 개의 입력을 시도합니다.\n재현성은 오히려 주의할 점입니다 — 그래서 좋은 도구는 실패한 입력을 최소 형태로 줄여 주고 씨앗값을 알려 줍니다.")
    ] },

  { t:"테스트하기 쉬운 코드", xp:20,
    th:{ sum:"테스트가 어렵다면 대개 코드가 **바깥 세계에 직접 붙어 있기** 때문이다. 시간·난수·네트워크·파일이 함수 안에 박혀 있으면 확인할 방법이 없다.",
      body:[
        {h:"결정적이지 않은 것을 밖으로", t:"`new Date()`·`Math.random()`·`fetch()` 를 함수 안에서 직접 부르면 **실행할 때마다 결과가 달라져** 확인할 수 없다. 이것들을 **인자로 받거나 주입**하면 테스트에서 원하는 값을 넣을 수 있다. 부수 효과를 가장자리로 밀어내고 가운데를 순수하게 두는 설계가 여기서 나온다."},
        {h:"숨은 의존이 문제다", t:"함수 시그니처에 보이지 않는데 실제로는 쓰이는 것 — 전역 설정, 싱글턴, 환경 변수 — 이 테스트를 어렵게 만든다. **필요한 것을 인자로 드러내면** 테스트가 쉬워질 뿐 아니라 읽는 사람도 그 함수가 무엇에 기대는지 알게 된다."}],
      code:{ c:"// 어려움: 시간이 함수 안에 박혀 있다\nfunction isExpired(token) {\n  return token.exp < Date.now();\n}\n\n// 쉬움: 지금이 언제인지 밖에서 준다\nfunction isExpired(token, now) {\n  return token.exp < now;\n}", cap:"바깥 세계를 인자로 끌어낸다" },
      key:["시간·난수·네트워크를 밖으로","숨은 의존이 테스트를 막는다","인자로 드러내면 읽기도 쉬워진다","순수한 가운데 · 지저분한 가장자리"] },
    q:[
      C("시간 의존","<code>Date.now()</code> 를 함수 안에서 직접 부르면 테스트가 어려워지는 이유로 알맞은 것은?",
        ["시간을 읽어 오는 연산이 느려서 테스트 전체 실행이 오래 걸리게 되기 때문이다",
         "실행할 때마다 값이 달라져 기대 결과를 고정할 수 없기 때문이다",
         "시간 함수는 테스트 환경에서 아예 호출되지 않기 때문이다",
         "시간대 설정이 없으면 오류가 발생하기 때문이다"],1,
        "**결정적이지 않은 값**이 함수 안에 박혀 있으면 같은 입력에 같은 결과가 나오지 않습니다.\n해결은 '지금이 언제인지' 를 **인자로 받거나** 시계 객체를 주입하는 것입니다. 테스트에서는 고정된 시각을 넣습니다.\n프레임워크가 제공하는 가짜 타이머를 쓸 수도 있지만, 인자로 드러내는 편이 코드도 정직해집니다."),
      C("숨은 의존","함수 시그니처에 드러나지 않는 의존이 문제가 되는 이유로 알맞은 것은?",
        ["컴파일러가 그런 의존을 오류로 처리하기 때문이다",
         "무엇에 기대는지 알 수 없어 준비도 어렵기 때문이다",
         "실행 속도가 눈에 띄게 느려지기 때문이다",
         "그런 형태의 코드는 대부분의 프로그래밍 언어에서 문법적으로 아예 허용되지 않기 때문이다"],1,
        "전역 설정·싱글턴·환경 변수는 **시그니처에 보이지 않으면서 동작을 바꿉니다.** 테스트에서는 그것을 알아내 준비해야 하고, 읽는 사람은 그 존재를 모릅니다.\n필요한 것을 인자로 드러내면 테스트가 쉬워지고, 그 함수가 무엇에 기대는지도 분명해집니다.\n'테스트하기 어렵다' 는 느낌은 대개 이런 숨은 의존을 알려 주는 신호입니다."),
      C("순수 함수","같은 입력에 항상 같은 결과를 내고 바깥에 영향을 주지 않는 함수의 테스트 이점으로 알맞은 것은?",
        ["실행 속도가 다른 종류의 함수보다 근본적으로 빠르기 때문에 유리하다",
         "준비할 것이 없고 결과가 고정되어 확인이 단순해진다",
         "테스트를 쓰지 않아도 자동으로 검증되기 때문에 유리하다",
         "커버리지 계산에서 가중치를 더 받기 때문에 유리하다"],1,
        "**준비가 필요 없고 결과가 고정**되므로 테스트가 가장 단순해집니다. 인자를 넣고 결과를 비교하면 끝입니다.\n그래서 복잡한 판단 로직은 순수 함수로 빼 두고, 데이터베이스·네트워크 같은 부수 효과는 가장자리에 모으는 설계가 널리 쓰입니다.\n이 구조에서는 가장자리에 통합 테스트를, 가운데에 빠른 단위 테스트를 붙이게 됩니다."),
      I("주입","의존하는 것을 밖에서 넣어 주는 방식을 뭐라고 부르나? (한국어 네 글자)",
        ["의존성 주입","의존 주입","dependency injection"],
        "정답은 **의존성 주입(dependency injection)** 입니다. 거창한 프레임워크가 아니라 **인자로 넘기는 것**이 가장 단순한 형태입니다.\n테스트에서는 진짜 대신 가짜를 넣을 수 있게 되고, 운영에서는 설정에 따라 구현을 바꿀 수 있게 됩니다."),
      C("전역 상태","테스트에서 전역 상태가 문제를 일으키는 대표적인 방식으로 알맞은 것은?",
        ["전역 변수를 쓰고 있는 코드는 테스트 프레임워크가 실행 자체를 거부해 버린다",
         "앞 테스트가 남긴 값에 영향을 받는다",
         "전역 변수는 메모리를 많이 차지해 테스트가 느려진다",
         "전역 상태를 쓰는 코드는 커버리지가 측정되지 않는다"],1,
        "**테스트 사이에 상태가 새는 것**이 핵심 문제입니다. 혼자 돌리면 통과하는데 전체로 돌리면 실패하는 현상이 여기서 나옵니다.\n각 테스트 전에 초기화하는 것으로 완화할 수 있지만, 애초에 전역 상태를 줄이는 것이 근본 해결입니다.\n캐시·설정·연결 풀처럼 전역이 자연스러운 것도 있어, 그런 경우 테스트가 초기화 지점을 갖게 만들어 둡니다."),
      C("난수","난수를 쓰는 로직을 테스트하는 방법으로 알맞은 것은?",
        ["난수를 쓰는 부분은 자동 테스트가 아예 불가능하므로 사람의 수동 확인에 맡긴다",
         "난수 생성기를 주입하거나 씨앗값을 고정해 같은 값이 나오게 한다",
         "테스트를 여러 번 반복 실행해 통계적으로 확인한다",
         "난수 대신 고정된 값을 쓰도록 본코드를 바꾼다"],1,
        "**씨앗값을 고정하거나 생성기를 주입**하면 재현 가능해집니다. 테스트에서는 정해진 순서의 값을 돌려주는 가짜를 넣습니다.\n반복 실행으로 통계적으로 보는 방법도 있지만, 간헐적으로 실패하는 테스트가 되기 쉬워 권하지 않습니다.\n본코드에서 난수를 없애는 것은 기능 자체를 바꾸는 것이라 답이 될 수 없습니다."),
      C("파일과 네트워크","파일 시스템이나 외부 API 에 의존하는 코드를 다루는 방법으로 가장 적절한 것은?",
        ["실제 파일과 실제 외부 API 를 그대로 가져다 써서 현실과 완전히 똑같은 조건으로 테스트한다",
         "인터페이스 뒤로 감춰 층마다 다른 것을 쓴다",
         "그런 코드는 테스트하지 않고 운영에서 확인한다",
         "테스트 실행 전에 네트워크를 차단해 오류가 나는지만 본다"],1,
        "**층을 나눠 각각에 맞는 테스트**를 붙이는 것이 정석입니다. 로직은 가짜로 빠르게, 실제 연동은 통합 테스트로 확인합니다.\n전부 실제로 하면 느리고 불안정해져 아무도 돌리지 않게 되고, 전부 가짜로 하면 '연동이 실제로 되는가' 를 영영 확인하지 못합니다.\n어느 한쪽만 택하는 것이 아니라 **둘 다 필요한 이유**가 여기 있습니다.")
    ] }
]},

/* ═══════════════ 유닛 3 ═══════════════ */
{ t:"테스트 더블 — 가짜를 쓰는 법", l:[

  { t:"스텁 · 목 · 페이크", xp:25,
    th:{ sum:"테스트에서 진짜 대신 넣는 것을 통틀어 **테스트 더블**이라 한다. 무엇을 위해 넣느냐에 따라 이름과 쓰임이 갈린다.",
      body:[
        {h:"값을 돌려주는 것과 호출을 확인하는 것", t:"**스텁(stub)** 은 '이 요청에는 이 값을 돌려줘' 라고 정해 두는 것이다 — 테스트의 준비 단계에 속한다. **목(mock)** 은 '이 함수가 이런 인자로 불렸는가' 를 확인하는 것이다 — 확인 단계에 속한다. 이메일 발송처럼 **결과가 밖으로 나가는** 동작은 목으로 확인할 수밖에 없지만, 그 외에는 대개 스텁으로 충분하다."},
        {h:"페이크는 진짜처럼 동작한다", t:"**페이크(fake)** 는 간단하지만 실제로 동작하는 구현이다 — 메모리 안의 저장소가 대표적이다. 목처럼 호출을 하나하나 정해 두지 않아도 되어 테스트가 훨씬 읽기 좋아지고, 구현이 바뀌어도 잘 깨지지 않는다. 대신 페이크 자체가 진짜와 달라질 위험이 있어, **계약 테스트**로 둘이 같은 규칙을 지키는지 확인해 두면 좋다."}],
      code:{ c:"// 스텁 — 값을 정해 준다 (준비)\nconst rates = { get: () => 1300 };\n\n// 목 — 호출을 확인한다 (확인)\nexpect(mailer.send).toHaveBeenCalledWith(\"a@b.c\", \"영수증\");\n\n// 페이크 — 간단하지만 진짜로 동작한다\nconst repo = new InMemoryUserRepo();", cap:"스텁은 준비, 목은 확인" },
      key:["스텁은 값을 준다(준비)","목은 호출을 확인한다(확인)","페이크는 간단한 진짜 구현","밖으로 나가는 동작만 목으로"] },
    q:[
      C("스텁과 목","스텁(stub)과 목(mock)의 차이로 알맞은 것은?",
        ["스텁은 값을 정해 돌려주고, 목은 어떻게 호출되었는지를 확인한다",
         "스텁은 테스트에서만 쓰고, 목은 운영 코드에서도 그대로 쓸 수 있는 구현이다",
         "스텁은 느리고, 목은 실행 속도가 훨씬 빠른 대체물이다",
         "스텁은 클래스에만 쓰고, 목은 함수에만 쓸 수 있다"],0,
        "**스텁은 준비 단계, 목은 확인 단계**에 속합니다. 이 구분이 둘의 본질입니다.\n스텁이 필요한 이유는 결과를 고정하기 위해서이고, 목이 필요한 이유는 '실제로 불렸는가' 자체가 확인 대상이기 때문입니다.\n둘을 섞어 쓰면 준비인지 확인인지 모호해져 테스트가 읽기 어려워집니다."),
      C("목을 써야 할 때","호출 여부 자체를 확인해야 하는 경우로 가장 알맞은 것은?",
        ["환율 API 에서 오늘의 환율 값을 받아 계산에 쓰는 경우",
         "주문이 완료되면 사용자에게 확인 메일을 보내는 경우",
         "데이터베이스에서 사용자 정보를 조회해 화면에 그대로 보여 주는 경우",
         "설정 파일에서 타임아웃 값을 읽어 요청에 적용하는 경우"],1,
        "메일 발송은 **결과가 바깥으로 나가는** 동작이라 반환값으로는 확인할 수 없습니다. '보냈는가' 를 직접 확인해야 합니다.\n나머지는 전부 '값을 받아 오는' 동작이라 스텁으로 값을 정해 주고 **결과를 확인**하면 충분합니다.\n목을 남용하면 구현을 조금만 바꿔도 테스트가 깨지는 상태가 됩니다."),
      C("과도한 목","목을 지나치게 많이 쓴 테스트에서 생기는 문제로 알맞은 것은?",
        ["테스트 실행 시간이 실제 구현보다 오히려 길어진다",
         "구현 방식을 바꾸기만 해도 동작이 같은데 테스트가 깨진다",
         "테스트 프레임워크가 쓸 수 있는 목의 개수를 제한하고 있어 실행되지 않는다",
         "커버리지가 실제보다 낮게 계산되어 기준을 못 맞춘다"],1,
        "목은 **어떻게 구현했는지**에 결합합니다. 그래서 결과가 같아도 호출 순서나 방식이 바뀌면 테스트가 깨집니다.\n이런 테스트는 리팩터링을 막는 방향으로 작동해, 테스트가 있어서 오히려 코드를 못 고치는 상태가 됩니다.\n가능하면 **결과를 확인**하고, 밖으로 나가는 동작에만 목을 쓰는 것이 좋습니다."),
      I("총칭","스텁·목·페이크를 통틀어 부르는 말은? (한국어)",
        ["테스트 더블","테스트더블","test double"],
        "정답은 **테스트 더블(test double)** 입니다. 영화의 스턴트 더블에서 온 이름입니다.\n제라드 메자로스가 정리한 분류로 더미·스텁·스파이·목·페이크 다섯 가지가 있고, 실무에서는 스텁·목·페이크 셋을 주로 구분합니다."),
      C("페이크의 이점","메모리 안에서 동작하는 간단한 저장소 구현을 쓸 때의 이점으로 알맞은 것은?",
        ["실제 데이터베이스를 그대로 쓸 때보다 훨씬 더 많은 기능을 확인할 수 있다는 점",
         "호출을 정해 두지 않아도 되어 덜 깨진다는 점",
         "운영 환경에서도 그대로 쓸 수 있어 코드가 줄어든다는 점",
         "데이터가 영구히 보존되어 다음 테스트에서도 재사용된다는 점"],1,
        "페이크는 **실제로 동작하므로** 호출 하나하나를 정해 둘 필요가 없습니다. 테스트가 '무엇을 하는가' 만 말하게 되어 훨씬 읽기 좋습니다.\n구현을 바꿔도 결과가 같으면 통과하므로 리팩터링을 막지 않습니다.\n대신 페이크가 진짜와 달라질 수 있어, 같은 규칙을 지키는지 확인하는 테스트를 따로 두는 것이 안전합니다."),
      C("외부 API","외부 결제 API 를 쓰는 코드를 테스트할 때의 접근으로 가장 적절한 것은?",
        ["실제 결제 API 의 운영 환경을 그대로 호출해 확인한다",
         "응답은 정해 주고 실제 연동은 샌드박스로 확인한다",
         "결제 관련 코드는 테스트하지 않고 수동 확인만 한다",
         "결제 API 를 호출하는 부분만 주석으로 처리해 막아 두고 나머지 부분을 테스트한다"],1,
        "**층을 나눠 각각 확인**합니다. 로직은 정해진 응답으로 빠르게, 실제 연동은 제공자가 주는 샌드박스로 확인합니다.\n운영 API 를 부르면 실제 결제가 일어나거나 호출 한도를 소모합니다.\n실패 응답·타임아웃·중복 요청 같은 경우는 오히려 가짜로만 확인할 수 있어, 두 층이 서로를 보완합니다."),
      C("무엇을 확인하는가","테스트가 '어떻게' 가 아니라 '무엇' 을 확인해야 한다는 원칙의 뜻으로 알맞은 것은?",
        ["구현 과정 대신 드러나는 결과를 확인한다는 뜻",
         "테스트 코드를 가능한 한 짧은 줄 수로 줄여서 써야 한다는 뜻",
         "성능보다 기능을 먼저 확인해야 한다는 뜻",
         "테스트 이름을 명사로 지어야 한다는 뜻"],0,
        "**결과를 확인하면** 구현을 자유롭게 바꿀 수 있습니다. 내부 호출 순서를 확인하면 그 순서가 사실상 명세가 되어 버립니다.\n리팩터링은 '밖에서 본 동작을 유지하며 안을 바꾸는 일' 이므로, 안을 확인하는 테스트는 리팩터링과 정면으로 충돌합니다.\n좋은 테스트는 리팩터링을 도와야지 막아서는 안 됩니다.")
    ] },

  { t:"픽스처와 테스트 데이터", xp:25,
    th:{ sum:"테스트 준비가 길어지면 **무엇을 확인하는 테스트인지 보이지 않게 된다.** 데이터 준비를 다루는 방식이 테스트의 읽기 쉬움을 크게 좌우한다.",
      body:[
        {h:"기본값을 두고 필요한 것만 바꾼다", t:"객체를 만드는 헬퍼에 **합리적인 기본값**을 두고, 테스트마다 관심 있는 필드만 덮어쓴다. 그러면 테스트를 읽는 사람이 '이 테스트에서 중요한 것은 이 필드구나' 를 즉시 안다. 반대로 모든 필드를 매번 나열하면 무엇이 중요한지 묻혀 버린다."},
        {h:"공유 픽스처의 함정", t:"여러 테스트가 같은 준비 데이터를 공유하면 처음에는 편하지만, 시간이 지나면 **아무도 그 데이터를 못 고치는 상태**가 된다. 한 테스트를 위해 값을 바꾸면 다른 테스트가 깨지기 때문이다. 준비 비용이 비싸지 않다면 **각 테스트가 자기 데이터를 만드는 편**이 오래 간다."}],
      code:{ c:"function makeUser(overrides = {}) {\n  return { id: 1, name: \"홍길동\", grade: \"basic\",\n           active: true, ...overrides };\n}\n\n// 이 테스트에서 중요한 것은 grade 하나임이 바로 보인다\nconst user = makeUser({ grade: \"vip\" });", cap:"관심 있는 필드만 드러낸다" },
      key:["기본값 + 덮어쓰기","중요한 필드가 드러나게","공유 픽스처는 굳어 간다","준비가 길면 의도가 묻힌다"] },
    q:[
      C("빌더 헬퍼","테스트 데이터를 만드는 헬퍼에 기본값을 두는 이유로 알맞은 것은?",
        ["객체를 만들어 내는 속도가 빨라져 전체 테스트 실행 시간이 크게 줄어들기 때문이다",
         "관심 있는 필드만 덮어쓰면 되기 때문이다",
         "기본값이 있어야 프레임워크가 객체를 인식하기 때문이다",
         "필드를 빠뜨렸을 때 발생하는 오류를 막을 수 있기 때문이다"],1,
        "**중요한 것만 보이게** 하는 것이 목적입니다. 모든 필드를 매번 나열하면 그 테스트의 요점이 잡음에 묻힙니다.\n필드가 추가되어도 헬퍼 한 곳만 고치면 되므로 유지보수도 쉬워집니다.\n오류 방지는 부수 효과일 뿐 주된 이유가 아닙니다."),
      C("공유 픽스처","여러 테스트가 하나의 준비 데이터를 함께 쓸 때 시간이 지나며 생기는 문제로 알맞은 것은?",
        ["정리되지 않은 데이터가 메모리에 계속 쌓여 테스트 실행이 점점 느려진다",
         "값을 바꾸면 다른 테스트가 깨져 굳어 간다",
         "프레임워크가 중복 데이터를 감지해 경고를 출력한다",
         "커버리지 계산에서 공유 부분이 중복으로 집계된다"],1,
        "공유 픽스처는 **점점 굳어 갑니다.** 새 테스트가 필요로 하는 값을 넣으면 기존 테스트가 깨지므로, 결국 아무도 고치지 못하는 덩어리가 됩니다.\n그래서 준비 비용이 크지 않다면 각 테스트가 자기 데이터를 만드는 편이 오래 갑니다.\n준비가 정말 비싸다면(예: 큰 데이터 적재) 공유하되 **읽기 전용으로만** 쓰는 규칙을 두는 방법이 있습니다."),
      C("의미 있는 값","테스트 데이터에 <code>\"홍길동\"</code>·<code>\"aaa\"</code> 같은 값을 쓸 때 주의할 점으로 알맞은 것은?",
        ["실제 사람의 이름을 쓰면 개인정보 문제가 생기므로 무작위 문자열만 쓰도록 해야 한다",
         "그 값이 결과에 영향을 주는 값이라면 왜 그 값인지 드러나게 지어야 한다",
         "한글은 인코딩 문제를 일으키므로 영문만 써야 한다",
         "값이 짧을수록 테스트가 빨라지므로 한 글자로 줄여야 한다"],1,
        "값이 **결과를 가르는 값**이라면 그 이유가 드러나야 합니다. `\"a\"` 대신 `\"공백이 든 이름\"` 처럼 쓰면 의도가 보입니다.\n반대로 아무 값이나 상관없는 자리라면 오히려 눈에 띄지 않는 값이 낫습니다 — 읽는 사람이 거기 의미가 있다고 오해하지 않게 됩니다.\n한글이나 이모지는 오히려 인코딩 경계를 확인하는 좋은 테스트 데이터가 되기도 합니다."),
      I("준비","테스트 실행 전에 미리 만들어 두는 데이터나 상태를 뭐라고 부르나? (영어)",
        ["픽스처","fixture"],
        "정답은 **픽스처(fixture)** 입니다. 준비 코드와 준비된 데이터를 함께 가리킵니다.\n프레임워크마다 `beforeEach`·`setUp`·`@fixture` 같은 이름으로 준비 지점을 제공하는데, 여기에 너무 많은 것을 넣으면 각 테스트가 무엇에 기대는지 알 수 없게 됩니다."),
      C("정리","테스트가 만든 데이터를 끝나고 정리해야 하는 이유로 알맞은 것은?",
        ["정리해 두지 않으면 프레임워크가 다음 테스트의 실행 자체를 거부해 버리기 때문이다",
         "다른 테스트에 영향을 주고 쌓이면 느려지기 때문이다",
         "정리한 테스트만 커버리지에 반영되기 때문이다",
         "데이터가 남아 있으면 보안 검사에서 실패하기 때문이다"],1,
        "**간섭과 누적** 둘 다 문제입니다. 남은 데이터가 다음 테스트의 조회 결과에 섞이고, 반복 실행할수록 테이블이 커져 느려집니다.\n트랜잭션으로 감싸 롤백하는 방법이 가장 깔끔합니다 — 실패해도 자동으로 되돌아갑니다.\n정리 코드는 테스트가 실패해도 반드시 실행되도록 두어야 합니다."),
      C("무작위 데이터","테스트마다 무작위 값을 생성해 쓰는 방식의 위험으로 알맞은 것은?",
        ["무작위 값 생성이 느려 테스트 시간이 크게 늘어난다",
         "실패했을 때 어떤 값에서 실패했는지 재현하기 어려워진다",
         "무작위 값은 데이터베이스에 저장되지 않는다",
         "테스트 프레임워크가 무작위 값 생성 기능을 지원하지 않는 경우가 많다"],1,
        "**재현이 어려워지는 것**이 가장 큰 위험입니다. 간헐적으로 실패하는데 원인을 찾을 수 없는 상태가 됩니다.\n고유해야 하는 값(이메일 등)에는 무작위가 유용하므로, 그런 경우 **실패 시 그 값을 출력**하도록 해 두면 됩니다.\n속성 기반 테스트 도구들은 씨앗값을 알려 주고 실패한 입력을 최소 형태로 줄여 주어 이 문제를 해결합니다."),
      C("준비의 길이","테스트의 준비 코드가 30줄을 넘어갈 때 먼저 의심할 것으로 알맞은 것은?",
        ["테스트 프레임워크의 설정 파일이 잘못되어 있을 가능성",
         "대상이 너무 많은 것에 의존할 가능성",
         "테스트 데이터가 실제 데이터와 다를 가능성",
         "테스트를 병렬로 실행하지 않고 있을 가능성"],1,
        "**준비가 길다는 것은 대상이 많은 것에 얽혀 있다는 신호**입니다. 테스트의 문제가 아니라 설계의 문제인 경우가 많습니다.\n헬퍼로 감춰서 짧아 보이게 만들 수는 있지만, 그러면 신호도 함께 감춰집니다.\n의존을 줄이거나 순수한 부분을 떼어 내면 테스트도 저절로 짧아집니다.")
    ] }
]},

/* ═══════════════ 유닛 4 ═══════════════ */
{ t:"테스트의 층 — 피라미드와 그 비판", l:[

  { t:"단위 · 통합 · E2E", xp:25,
    th:{ sum:"테스트는 **무엇을 함께 확인하느냐**로 층이 갈린다. 층이 올라갈수록 현실에 가까워지지만 느려지고 불안정해진다.",
      body:[
        {h:"세 층의 성격", t:"**단위 테스트**는 함수·클래스 하나를 격리해 확인한다 — 밀리초 단위로 빠르고 실패 지점이 정확하다. **통합 테스트**는 여러 조각이 실제로 맞물리는지 본다 — 데이터베이스 쿼리, API 계약처럼 **가짜로는 확인할 수 없는 것**이 여기서 걸린다. **E2E** 는 사용자처럼 전체 흐름을 밟는다 — 가장 현실적이지만 가장 느리고 잘 깨진다."},
        {h:"비율은 팀이 정한다", t:"고전적인 '피라미드' 는 단위를 많이, E2E 를 적게 두라고 말한다. 근거는 **비용과 속도**다. 다만 조각이 다 맞는데 합치면 안 되는 경우가 흔해, 통합 층을 두껍게 가져가는 팀도 많다(트로피 모양). 정답은 하나가 아니고, **어디서 결함이 자주 나는지**를 보고 조정하는 것이 맞다."}],
      code:{ c:"      /\\        E2E     적게 — 느리고 잘 깨진다\n     /  \\\n    /----\\      통합    핵심 흐름 중심\n   /      \\\n  /--------\\    단위    많이 — 빠르고 정확하다", cap:"비용과 속도가 비율의 근거" },
      key:["층이 올라갈수록 현실적이고 느리다","단위는 실패 지점이 정확","통합은 가짜로 못 잡는 것을 잡는다","비율은 결함 분포를 보고 조정"] },
    q:[
      C("단위 테스트의 강점","단위 테스트가 가진 강점으로 알맞은 것은?",
        ["실제 사용자가 쓰고 있는 환경과 가장 가까운 조건에서 확인할 수 있다는 점",
         "빠르게 돌아가고 실패했을 때 원인 지점이 좁게 특정된다는 점",
         "여러 모듈이 함께 동작할 때의 문제를 잡아낼 수 있다는 점",
         "브라우저와 네트워크를 포함한 전체 흐름을 확인한다는 점"],1,
        "**속도와 정확한 실패 지점**이 단위 테스트의 값어치입니다. 수천 개를 몇 초에 돌릴 수 있어 개발 중에 계속 돌립니다.\n대신 조각이 맞물리는 문제는 잡지 못합니다 — 각각은 맞는데 합치면 안 되는 상황이 실제로 흔합니다.\n그래서 단위만으로는 부족하고 위층이 필요합니다."),
      C("통합 테스트가 필요한 이유","단위 테스트를 충분히 갖췄는데도 통합 테스트가 필요한 이유로 알맞은 것은?",
        ["단위 테스트는 실행 속도가 너무 느려서 자주 돌려 볼 수가 없기 때문이다",
         "맞물리는 지점에서 어긋날 수 있기 때문이다",
         "단위 테스트는 커버리지 계산에 반영되지 않기 때문이다",
         "단위 테스트는 새로 짠 코드에는 적용할 수 없기 때문이다"],1,
        "**맞물리는 지점**이 문제입니다. 가짜로 대체한 부분이 실제와 다르게 동작하면 단위 테스트는 전부 통과하는데 서비스는 깨집니다.\nSQL 쿼리, 직렬화 형식, API 응답 구조처럼 **가짜로는 확인할 수 없는 것**이 여기서 걸립니다.\n그래서 단위와 통합은 서로를 대체하는 관계가 아니라 보완하는 관계입니다."),
      C("E2E 를 적게 두는 이유","전체 흐름을 밟는 E2E 테스트를 적게 유지하라고 권하는 근거로 알맞은 것은?",
        ["E2E 테스트는 결함을 잡아내는 능력 자체가 근본적으로 많이 떨어지기 때문이다",
         "느리고 간헐적으로 실패하기 쉬워 유지 비용이 빠르게 커지기 때문이다",
         "E2E 테스트는 자동화할 수 없어 사람이 직접 해야 하기 때문이다",
         "E2E 테스트는 커버리지를 낮추는 방향으로 작용하기 때문이다"],1,
        "**비용**이 근거입니다. 실행이 느리고, 타이밍·네트워크·환경 때문에 이유 없이 실패하는 일이 잦습니다.\n간헐적 실패가 반복되면 사람들이 '또 그거겠지' 하며 무시하게 되고, 그 순간 테스트는 값어치를 잃습니다.\n결함을 잡는 능력 자체는 오히려 E2E 가 가장 좋아서, **핵심 흐름 몇 개**에 집중해 유지하는 것이 실무적인 답입니다."),
      I("전체 흐름","사용자처럼 처음부터 끝까지 흐름을 밟는 테스트를 뭐라고 부르나? (영문 약자)",
        ["E2E","e2e","end-to-end"],
        "정답은 **E2E**(end-to-end)입니다. 브라우저를 실제로 띄워 클릭하고 입력하는 방식이 대표적입니다.\n가장 현실적인 확인이지만 가장 비싸므로, '결제까지 가는 흐름' 같은 **핵심 경로 몇 개**로 좁혀 유지하는 것이 보통입니다."),
      C("트로피 모양","통합 테스트 층을 가장 두껍게 가져가자는 주장의 근거로 알맞은 것은?",
        ["통합 테스트가 실행 속도 면에서 단위 테스트보다 오히려 더 빠르기 때문이다",
         "실제 결함의 상당수가 조각이 맞물리는 지점에서 나오기 때문이다",
         "통합 테스트만으로 커버리지 100% 를 달성할 수 있기 때문이다",
         "단위 테스트는 최신 프레임워크에서 지원이 중단되었기 때문이다"],1,
        "'테스팅 트로피' 주장의 핵심은 **결함이 실제로 어디서 나오는가** 입니다. 각 조각이 맞는데 합치면 안 되는 경우가 많다는 관찰입니다.\n도구가 좋아져 통합 테스트가 예전보다 훨씬 빨라진 것도 배경입니다.\n어느 모양이 옳은지는 팀과 시스템에 따라 다르고, **자기 팀의 결함 분포를 보고 정하는 것**이 맞습니다."),
      C("실패 진단","같은 버그를 단위 테스트와 E2E 테스트가 각각 잡았을 때의 차이로 알맞은 것은?",
        ["단위는 어느 함수인지, E2E 는 화면만 알려 준다",
         "E2E 테스트가 더 정확한 실패 위치를 알려 준다",
         "둘이 내놓는 실패 메시지는 본질적으로 완전히 같은 정보를 담고 있다",
         "단위 테스트는 실패 위치를 알려 주지 않는다"],0,
        "**실패 지점의 좁기**가 다릅니다. 단위 테스트는 함수와 입력을 알려 주지만, E2E 는 '이 화면에서 이 버튼이 안 된다' 까지만 말합니다.\n그래서 E2E 가 실패하면 원인을 찾는 데 시간이 걸리고, 그 시간이 E2E 의 숨은 비용입니다.\n둘 다 있으면 E2E 가 문제를 발견하고 단위 테스트가 위치를 알려 주는 조합이 됩니다."),
      C("층의 선택","새 기능을 만들 때 어느 층에 테스트를 둘지 정하는 기준으로 알맞은 것은?",
        ["항상 세 층 모두에 같은 개수의 테스트를 만든다",
         "무엇이 잘못될지 보고 가장 값싼 층을 고른다",
         "가장 최근에 배운 테스트 방식을 우선 적용한다",
         "E2E 테스트로 먼저 만들어 두고 나중에 단위 테스트로 옮겨 간다"],1,
        "**무엇이 잘못될 수 있는가** 에서 시작합니다. 계산 규칙이 복잡하면 단위, 쿼리가 복잡하면 통합, 화면 흐름이 복잡하면 E2E 입니다.\n같은 것을 여러 층에서 반복 확인하면 비용만 늘고 얻는 것이 없습니다.\n'가장 값싸게 잡는 층' 이라는 기준 하나면 대부분의 경우 답이 정해집니다.")
    ] },

  { t:"커버리지 — 숫자를 다루는 법", xp:25,
    th:{ sum:"커버리지는 **실행된 코드의 비율**이지 확인된 동작의 비율이 아니다. 이 차이를 모르면 숫자에 속는다.",
      body:[
        {h:"실행과 확인은 다르다", t:"어서션이 하나도 없는 테스트도 코드를 실행하므로 커버리지는 올라간다. 그래서 **커버리지 100% 와 버그 없음은 아무 관계가 없다.** 커버리지가 정말 알려 주는 것은 반대 방향이다 — **낮은 커버리지는 확인되지 않은 영역이 있다는 확실한 신호**다. 낮으면 문제이고, 높다고 안전한 것은 아니다."},
        {h:"목표로 삼으면 우회한다", t:"'80% 미만이면 병합 차단' 같은 규칙은 사람들이 **숫자를 채우는 방향**으로 움직이게 만든다. 확인하기 쉬운 곳만 채워지고 정작 위험한 곳은 남는다. 측정값이 목표가 되면 측정값으로서의 값어치를 잃는다(굿하트의 법칙). **새로 추가된 코드의 커버리지를 보여 주는** 정도가 건강한 사용법이다."}],
      code:{ c:"// 커버리지는 100%, 확인한 것은 아무것도 없다\ntest(\"동작한다\", () => {\n  calculateTotal(cart);   // 실행은 됨\n  // 어서션 없음\n});", cap:"실행되었다는 것과 확인했다는 것은 다르다" },
      key:["커버리지는 실행 비율","낮으면 신호, 높다고 안전하진 않다","목표로 삼으면 우회를 부른다","보여 주되 차단은 신중히"] },
    q:[
      C("커버리지의 뜻","코드 커버리지가 실제로 측정하는 것으로 알맞은 것은?",
        ["테스트가 확인한 동작이 전체 요구사항 가운데에서 차지하는 비율",
         "실행된 코드가 전체에서 차지하는 비율",
         "발견된 버그의 수를 전체 코드 줄 수로 나눈 값",
         "테스트 코드의 줄 수를 본코드 줄 수로 나눈 값"],1,
        "커버리지는 **실행된 비율**입니다. 그 코드가 옳게 동작하는지 확인했는지는 전혀 말해 주지 않습니다.\n어서션 없는 테스트도 커버리지를 올린다는 사실이 이 차이를 가장 잘 보여 줍니다.\n그래서 커버리지는 '안전하다' 의 근거가 될 수 없습니다."),
      C("낮은 커버리지","커버리지 숫자가 유용하게 쓰이는 방향으로 알맞은 것은?",
        ["높게 나온 커버리지 숫자를 근거로 배포해도 안전하다고 판단한다",
         "낮은 영역을 찾아내는 데 쓴다",
         "팀원별 커버리지를 비교해 기여도를 평가하는 데 쓴다",
         "커버리지가 낮은 파일을 자동으로 삭제하는 데 쓴다"],1,
        "커버리지는 **한 방향으로만** 신뢰할 수 있습니다. 낮으면 확인되지 않은 곳이 있다는 확실한 신호입니다.\n반대로 높다고 안전한 것은 아닙니다 — 실행만 하고 확인하지 않았을 수 있습니다.\n이 비대칭을 이해하면 숫자를 올바르게 쓸 수 있습니다."),
      C("기준선 차단","'커버리지 80% 미만이면 병합 차단' 규칙이 역효과를 내는 이유로 알맞은 것은?",
        ["커버리지를 재는 측정 도구가 정확한 값을 제대로 계산하지 못하기 때문이다",
         "확인 없는 테스트로 숫자만 채우게 되기 때문이다",
         "차단 규칙이 있으면 테스트 실행 시간이 길어지기 때문이다",
         "80% 는 기술적으로 달성이 불가능한 수치이기 때문이다"],1,
        "**측정값이 목표가 되면 측정값만 좋아집니다.** 어서션 없는 테스트, 게터·세터 테스트로 숫자를 채우게 됩니다.\n정작 위험하고 복잡한 곳은 테스트하기 어려워 뒤로 밀립니다.\n'새로 추가된 코드의 커버리지를 PR 에 표시' 처럼 **보여 주는 방식**이 대개 더 건강합니다."),
      I("분기","조건문의 참·거짓 양쪽이 모두 실행되었는지 보는 커버리지 종류는? (한국어)",
        ["분기 커버리지","브랜치 커버리지","branch coverage"],
        "정답은 **분기 커버리지(branch coverage)** 입니다. 줄 커버리지보다 엄격합니다.\n`if (a && b)` 한 줄은 줄 커버리지로는 한 번만 실행해도 100% 지만, 분기로 보면 여러 조합을 확인해야 합니다. 그래서 줄 커버리지 숫자는 실제보다 후하게 나옵니다."),
      C("100%","커버리지 100% 인 프로젝트에 대해 말할 수 있는 것으로 알맞은 것은?",
        ["실행은 되었지만 옳은지는 알 수 없다",
         "모든 기능이 요구사항대로 동작함이 확인되었다",
         "버그가 남아 있을 가능성이 통계적으로 볼 때 거의 사라진다",
         "리팩터링을 해도 절대 문제가 생기지 않는다"],0,
        "**실행되었다는 것만** 말할 수 있습니다. 그 실행 결과가 옳은지는 어서션이 있어야 알 수 있습니다.\n게다가 커버리지는 '있는 코드' 만 봅니다 — 빠뜨린 처리, 쓰지 않은 조건은 애초에 측정 대상이 아닙니다.\n100% 를 달성하는 데 드는 비용이 그 이득보다 큰 경우도 많습니다."),
      C("측정하지 않는 것","커버리지가 원리적으로 알려 줄 수 없는 것으로 알맞은 것은?",
        ["어느 줄이 테스트를 돌리는 동안 한 번도 실행되지 않았는지",
         "필요한데 아예 작성되지 않은 처리가 무엇인지",
         "조건문의 어느 분기가 실행되지 않았는지",
         "어느 파일의 커버리지가 가장 낮은지"],1,
        "커버리지는 **있는 코드**만 봅니다. 빠뜨린 예외 처리, 고려하지 않은 경우는 코드가 없으니 측정되지도 않습니다.\n'누락된 요구사항' 은 커버리지로 절대 잡을 수 없고, 그래서 요구사항에서 출발하는 테스트 설계가 따로 필요합니다.\n이것이 커버리지를 유일한 지표로 삼으면 안 되는 근본적인 이유입니다."),
      C("뮤테이션 테스트","코드를 일부러 조금씩 바꿔 보고 테스트가 실패하는지 확인하는 방식의 목적으로 알맞은 것은?",
        ["테스트가 결함을 잡아내는지 재기 위해서",
         "코드의 실행 속도가 작은 변경에 대해 얼마나 민감한지 재기 위해서",
         "커버리지 숫자를 자동으로 올리기 위해서",
         "리팩터링할 부분을 자동으로 찾기 위해서"],0,
        "**뮤테이션 테스트**는 커버리지의 한계를 메웁니다. `>` 를 `>=` 로 바꿨는데 아무 테스트도 실패하지 않는다면, 그 부분은 실행만 되고 확인되지 않은 것입니다.\n어서션의 실효성을 재는 셈이라 커버리지보다 훨씬 정직한 지표입니다.\n대신 실행 비용이 매우 커서, 핵심 모듈에만 가끔 돌리는 방식으로 쓰입니다.")
    ] }
]},

/* ═══════════════ 유닛 5 ═══════════════ */
{ t:"TDD — 테스트를 먼저 쓴다", l:[

  { t:"빨강 · 초록 · 정리", xp:30,
    th:{ sum:"TDD 는 테스트 기법이라기보다 **설계 방법**이다. 쓸 코드를 사용하는 쪽에서 먼저 상상하게 만들어, 쓰기 좋은 인터페이스가 나오게 한다.",
      body:[
        {h:"세 걸음", t:"**빨강** — 아직 없는 동작을 확인하는 테스트를 써서 실패시킨다. **초록** — 통과할 만큼만 가장 단순하게 구현한다. **정리** — 통과 상태를 유지하며 구조를 다듬는다. 이 순환의 핵심은 **한 번에 한 걸음씩** 가는 것이다. 각 단계가 짧아 어디서 틀렸는지 항상 알 수 있다."},
        {h:"먼저 실패시키는 이유", t:"테스트가 실패하는 것을 **눈으로 확인해야** 그 테스트가 실제로 무언가를 확인하고 있음을 알 수 있다. 처음부터 통과하는 테스트는 조건이 잘못되었거나 아무것도 확인하지 않는 것일 수 있다. 이 한 단계를 건너뛰면 '있으나 마나 한 테스트' 가 조용히 쌓인다."}],
      code:{ c:"// 1. 빨강 — 아직 함수가 없다\ntest(\"빈 장바구니의 합계는 0원\", () =>\n  expect(total([])).toBe(0));\n\n// 2. 초록 — 통과할 만큼만\nfunction total(items) { return 0; }\n\n// 3. 정리 — 다음 테스트를 추가하며 일반화", cap:"한 번에 한 걸음" },
      key:["빨강 → 초록 → 정리","실패를 눈으로 확인한다","통과할 만큼만 구현","설계 방법에 가깝다"] },
    q:[
      C("첫 단계","TDD 에서 테스트를 먼저 써서 실패시키는 단계가 필요한 이유로 알맞은 것은?",
        ["테스트 프레임워크가 실패한 테스트만 따로 모아 추적하도록 설계되어 있기 때문이다",
         "그 테스트가 실제로 무언가를 확인하고 있음을 눈으로 확인하기 위해서다",
         "실패한 테스트가 있어야 커버리지가 정확히 계산되기 때문이다",
         "실패 로그를 남겨 두어야 나중에 참고할 수 있기 때문이다"],1,
        "**처음부터 통과하는 테스트는 의심해야 합니다.** 조건이 잘못되었거나 아무것도 확인하지 않는 것일 수 있습니다.\n실패를 한 번 보고 나면 그 테스트가 무엇을 감시하는지 확실해집니다.\n이 단계를 건너뛰면 '있으나 마나 한 테스트' 가 쌓여 안전망이 헐거워집니다."),
      C("두 번째 단계","테스트를 통과시키는 단계에서 '통과할 만큼만' 구현하라는 조언의 근거로 알맞은 것은?",
        ["구현 코드가 짧으면 짧을수록 실행 속도가 빨라지기 때문이다",
         "미리 짐작해 만들지 않기 위해서다",
         "긴 코드는 커버리지를 떨어뜨리기 때문이다",
         "프레임워크가 구현 길이를 제한하기 때문이다"],1,
        "**필요를 테스트가 이끌게** 하는 것이 요점입니다. 미리 짐작해 만든 기능은 쓰이지 않거나 잘못된 형태인 경우가 많습니다.\n처음에는 상수를 돌려주는 것처럼 보이는 구현도 괜찮습니다 — 다음 테스트가 일반화를 강요합니다.\n이 리듬이 '지금 필요한 것만 만든다' 는 습관을 만들어 줍니다."),
      C("TDD 의 성격","TDD 를 '설계 방법' 이라고 부르는 이유로 알맞은 것은?",
        ["테스트 코드가 자동으로 설계 문서로 변환되기 때문이다",
         "쓰는 쪽 입장에서 인터페이스를 먼저 정하게 되기 때문이다",
         "TDD 를 하면 클래스 다이어그램 같은 설계 문서를 그릴 필요가 없어지기 때문이다",
         "설계 패턴을 자동으로 적용해 주기 때문이다"],1,
        "테스트를 쓰려면 **그 코드를 어떻게 부를지 먼저 정해야** 합니다. 사용하는 쪽에서 보게 되므로 자연스럽게 쓰기 좋은 인터페이스가 나옵니다.\n또 테스트하기 어려우면 즉시 느껴져, 의존이 뒤엉키기 전에 바로잡게 됩니다.\n그래서 TDD 의 이득 중 상당 부분은 테스트 자체보다 설계 피드백에서 옵니다."),
      I("TDD 세 단계","TDD 의 세 단계를 색과 낱말로 부르는 이름은? (한국어, 가운뎃점으로 구분)",
        ["빨강·초록·정리","레드·그린·리팩터","red green refactor"],
        "정답은 **빨강·초록·정리**(Red-Green-Refactor)입니다.\n세 번째 단계를 빠뜨리는 경우가 가장 흔한데, 정리를 하지 않으면 통과는 하지만 점점 지저분해지는 코드가 쌓입니다. 테스트가 있으니 마음 놓고 정리할 수 있다는 것이 이 방식의 이득입니다."),
      C("정리 단계","세 번째 정리(리팩터) 단계에서 지켜야 할 원칙으로 알맞은 것은?",
        ["구조를 바꾸면서 동시에 새 기능도 함께 추가한다",
         "통과를 유지하며 밖에서 본 동작은 바꾸지 않는다",
         "테스트도 함께 고쳐 새 구조에 맞춘다",
         "정리를 하는 동안에는 테스트를 잠시 꺼 두었다가 나중에 다시 켠다"],1,
        "리팩터링은 **밖에서 본 동작을 유지하며 안을 바꾸는 일**입니다. 테스트가 계속 초록이어야 그것이 지켜졌음을 압니다.\n기능 추가와 구조 변경을 동시에 하면 무엇 때문에 깨졌는지 알 수 없어집니다 — 두 모자를 동시에 쓰지 않는다는 표현이 여기서 나옵니다.\n테스트를 함께 고쳐야 한다면 그것은 동작이 바뀌었다는 신호이므로, 리팩터링이 아닙니다."),
      C("TDD 가 어려운 곳","TDD 를 적용하기 상대적으로 어려운 상황으로 알맞은 것은?",
        ["입력과 출력이 분명하게 정해져 있는 계산 로직을 만들 때",
         "무엇을 만들지 모른 채 탐색해 볼 때",
         "이미 있는 버그를 재현하고 고칠 때",
         "여러 조건이 얽힌 판단 규칙을 구현할 때"],1,
        "**무엇을 만들지 모르는 상태**에서는 테스트를 먼저 쓸 수 없습니다. 기대하는 결과 자체가 없기 때문입니다.\n이럴 때는 먼저 시험 삼아 만들어 보고(스파이크), 방향이 정해지면 버리고 테스트부터 다시 쓰는 방식이 널리 쓰입니다.\n나머지 셋은 기대가 분명해 TDD 가 특히 잘 맞는 상황입니다."),
      C("버그 수정","버그를 고칠 때 TDD 방식을 적용하는 순서로 알맞은 것은?",
        ["코드를 먼저 고치고 나서 그 동작을 확인하는 테스트를 뒤이어 추가한다",
         "그 버그를 재현해 실패하는 테스트를 먼저 쓰고 나서 고친다",
         "테스트 없이 고치고 수동으로 확인한 뒤 넘어간다",
         "비슷한 기존 테스트를 복사해 수정하고 코드를 고친다"],1,
        "**재현하는 테스트를 먼저** 쓰면 두 가지를 한 번에 얻습니다 — 실제로 고쳐졌음이 확인되고, 같은 버그가 다시 돌아오는 것도 막힙니다.\n먼저 고치면 그 테스트가 정말 그 버그를 잡는지 알 수 없습니다. 실패를 본 적이 없기 때문입니다.\n이 습관 하나가 회귀를 크게 줄여 줍니다.")
    ] },

  { t:"플레이키 테스트 — 믿을 수 없게 되는 순간", xp:30,
    th:{ sum:"간헐적으로 실패하는 테스트는 **없는 것보다 나쁘다.** 사람들이 실패를 무시하기 시작하면 진짜 실패도 함께 묻히기 때문이다.",
      body:[
        {h:"흔들리는 원인 네 가지", t:"**시간**(지금 시각, 타임아웃, 시간대), **순서**(앞 테스트가 남긴 상태), **동시성**(경쟁 조건, 병렬 실행), **바깥 세계**(네트워크, 실제 API). 원인은 대개 이 넷 중 하나이고, 각각 처방이 다르다. '가끔 실패한다' 를 재시도로 덮으면 원인은 그대로 남아 언젠가 운영에서 나타난다."},
        {h:"발견 즉시 격리한다", t:"플레이키 테스트를 방치하면 **전체 스위트의 신뢰가 무너진다.** 빨간불을 무시하는 문화가 생기면 진짜 회귀도 놓치게 된다. 그래서 발견하면 즉시 격리(quarantine)하고 원인을 고칠 사람을 정한다. 며칠 안에 고치지 못하면 지우는 것도 선택지다 — 믿을 수 없는 테스트를 남겨 두는 것보다 낫다."}],
      code:{ c:"// 흔들리는 테스트\nawait sleep(500);          // 왜 500ms? 느린 기계에서는 실패한다\nexpect(list()).toHaveLength(1);\n\n// 고친 테스트\nawait waitFor(() => expect(list()).toHaveLength(1));", cap:"고정 대기 대신 조건을 기다린다" },
      key:["간헐 실패는 없는 것보다 나쁘다","원인은 시간·순서·동시성·바깥","재시도는 원인을 덮는다","발견 즉시 격리하고 담당을 정한다"] },
    q:[
      C("가장 큰 피해","간헐적으로 실패하는 테스트가 팀에 주는 가장 큰 피해로 알맞은 것은?",
        ["테스트 실행 시간이 반복되는 재시도 때문에 계속 길어진다는 점",
         "실패를 무시하게 되어 진짜 회귀도 놓친다는 점",
         "커버리지 숫자가 실행할 때마다 달라진다는 점",
         "테스트 코드의 줄 수가 불필요하게 늘어난다는 점",],1,
        "**신뢰가 무너지는 것**이 가장 큰 피해입니다. '또 그거겠지' 하며 빨간불을 넘기기 시작하면 진짜 문제도 같이 넘어갑니다.\n그 순간 테스트 스위트 전체가 값어치를 잃습니다 — 통과해도 안심할 수 없고 실패해도 믿지 않게 됩니다.\n실행 시간이나 줄 수는 부차적인 문제입니다."),
      C("고정 대기","<code>sleep(500)</code> 처럼 정해진 시간을 기다리는 방식의 문제로 알맞은 것은?",
        ["기다리고 있는 동안 다른 테스트가 실행되지 못해 전체 스위트가 느려진다",
         "느리면 부족하고 빠르면 낭비된다",
         "프레임워크가 고정 대기를 지원하지 않는 경우가 많다",
         "대기 중에는 커버리지가 측정되지 않는다"],1,
        "고정 대기는 **환경에 따라 맞기도 하고 틀리기도 합니다.** CI 서버가 붐비면 500ms 로는 부족해 실패합니다.\n그래서 시간이 아니라 **조건을 기다려야** 합니다 — '요소가 나타날 때까지', '목록 길이가 1이 될 때까지'.\n대기 시간을 늘리는 것으로 대응하면 테스트만 느려지고 언젠가 다시 실패합니다."),
      C("재시도","간헐적으로 실패하는 테스트에 자동 재시도를 거는 대응의 문제로 알맞은 것은?",
        ["재시도 기능은 대부분의 테스트 프레임워크에서 제공하지 않는다",
         "원인이 남아 운영에서 나타날 수 있다",
         "재시도한 테스트는 커버리지에 반영되지 않는다",
         "재시도하면 테스트 순서가 무작위로 바뀐다"],1,
        "재시도는 **증상을 가릴 뿐**입니다. 테스트가 흔들리는 원인이 경쟁 조건이라면, 그 경쟁 조건은 운영에서도 존재합니다.\n즉 플레이키 테스트는 종종 **진짜 버그의 신호**입니다. 덮으면 그 신호를 버리는 것입니다.\n다만 원인을 찾는 동안 전체 스위트를 살려 두기 위해 임시로 격리하는 것은 실용적인 선택입니다."),
      I("간헐 실패","같은 코드인데 실행할 때마다 결과가 달라지는 테스트를 뭐라고 부르나? (영어)",
        ["플레이키","flaky","flaky test"],
        "정답은 **플레이키(flaky) 테스트**입니다.\n큰 조직일수록 이 문제가 심각해서, 자동으로 플레이키를 감지해 격리하고 담당자를 지정하는 시스템을 따로 운영하는 곳도 있습니다."),
      C("순서 의존","전체로 돌리면 실패하는데 혼자 돌리면 통과하는 테스트의 원인으로 가장 유력한 것은?",
        ["그 테스트에 쓰인 어서션 조건 자체가 잘못 작성되어 있다",
         "앞 테스트가 남긴 상태에 영향을 받는다",
         "테스트 프레임워크의 버전이 오래되었다",
         "그 테스트만 커버리지 측정에서 제외되어 있다"],1,
        "**테스트 사이에 상태가 새는 것**이 전형적인 원인입니다. 전역 변수, 정리되지 않은 데이터베이스 행, 남아 있는 모듈 캐시가 흔한 통로입니다.\n실행 순서를 무작위로 섞어 돌려 보면 이런 의존이 빠르게 드러납니다.\n각 테스트가 자기 데이터를 만들고 끝나면 정리하는 규칙이 근본 해결입니다."),
      C("자정 실패","날짜가 바뀌는 자정 무렵에만 실패하는 테스트의 원인으로 알맞은 것은?",
        ["테스트 실행 시간이 자정 무렵이 되면 자동으로 길어지기 때문이다",
         "실행 시점의 현재 시각에 의존하기 때문이다",
         "CI 서버가 자정에 재시작되기 때문이다",
         "자정에는 네트워크가 불안정해지기 때문이다"],1,
        "'오늘 날짜' 를 실행 시점에 읽으면 **자정을 넘기며 준비와 확인의 기준이 달라집니다.**\n시간대까지 얽히면 특정 지역에서만 실패하는 형태로도 나타납니다.\n해결은 시각을 인자로 주입하거나 가짜 타이머로 고정하는 것입니다 — 시간에 의존하는 테스트는 반드시 시간을 통제해야 합니다."),
      C("발견했을 때","플레이키 테스트를 발견했을 때의 대응 순서로 가장 적절한 것은?",
        ["당장은 넘어가고 여유가 생기면 살펴본다",
         "즉시 격리하고 고칠 담당을 정한다",
         "그 테스트를 아예 지워 버리고 다시는 만들지 않는다",
         "재시도 횟수를 늘려 통과할 때까지 돌린다"],1,
        "**격리 + 담당 지정**이 실무적인 답입니다. 전체 스위트의 신뢰를 지키면서 원인을 추적할 시간을 법니다.\n그냥 넘어가면 같은 것이 쌓여 결국 아무도 믿지 않는 스위트가 됩니다.\n정해진 기한 안에 고치지 못하면 지우는 것도 선택지입니다 — 믿을 수 없는 테스트는 남아 있어도 값어치가 없기 때문입니다.")
    ] }
]},

/* ═══════════════ 유닛 6 ═══════════════ */
{ t:"자동화된 검증 — CI 안에서", l:[

  { t:"빠르지 않으면 아무도 기다리지 않는다", xp:30,
    th:{ sum:"테스트의 값어치는 **피드백 속도**에 비례한다. 20분 걸리는 스위트는 결과를 안 보고 다른 일을 하게 만들고, 그러면 없는 것과 비슷해진다.",
      body:[
        {h:"빠른 것과 느린 것을 나눈다", t:"단위 테스트는 커밋마다, 통합은 PR 마다, 무거운 E2E 는 병합 후나 야간에 돌리는 **2단 구성**이 널리 쓰인다. 개발자가 기다리는 구간을 짧게 유지하는 것이 목적이다. 병렬 실행과 **바뀐 부분만 테스트**하는 방식도 같은 목표를 향한다."},
        {h:"실패는 최우선으로 고친다", t:"`main` 이 빨간불인 상태를 방치하면 다음 사람들이 그 실패를 자기 것이라 착각하며 시간을 낭비한다. '**main 이 깨지면 다른 일보다 먼저 고친다**' 는 규칙이 있는 팀이 가장 건강하다. 실패를 채널로 알리고, 누가 고칠지가 분명해야 이 규칙이 작동한다."}],
      code:{ c:"# 빠른 것 — 커밋마다\nnpm run test:unit\n\n# 느린 것 — 병합 후 또는 야간\nnpm run test:e2e", cap:"기다리는 구간을 짧게 유지한다" },
      key:["피드백 속도가 곧 값어치","빠른 것과 느린 것을 나눈다","깨진 main 은 최우선 과제","실패를 알리고 담당을 분명히"] },
    q:[
      C("느린 스위트","테스트 스위트가 20분 넘게 걸릴 때 생기는 문제로 알맞은 것은?",
        ["실행 중에 발생한 오류가 로그에 기록되지 않는다",
         "기다리지 않게 되어 피드백 고리가 끊긴다",
         "테스트 프레임워크가 시간 초과라고 판단해 실행을 중단해 버린다",
         "커버리지 계산이 부정확해진다"],1,
        "**기다리지 않게 되는 것**이 핵심 문제입니다. 결과를 나중에 보면 그 사이 다른 변경이 쌓여 원인 추적이 어려워집니다.\n그래서 개발자가 기다리는 구간(커밋·PR)은 몇 분 안에 끝나게 만들고, 무거운 것은 뒤로 미룹니다.\n속도는 테스트의 부수적 성질이 아니라 **값어치를 결정하는 요소**입니다."),
      C("2단 구성","빠른 테스트와 느린 테스트를 나눠 실행하는 구성의 목적으로 알맞은 것은?",
        ["느린 테스트를 결국에는 없애 버리기 위한 중간 단계로 삼기 위해서",
         "기다리는 구간은 짧게, 깊은 검증은 그대로",
         "테스트 종류별로 담당자를 나누기 위해서",
         "커버리지를 종류별로 따로 계산하기 위해서"],1,
        "**둘 다 필요하지만 같은 시점에 돌릴 필요는 없다**는 것이 이 구성의 발상입니다.\n빠른 것으로 즉시 피드백을 받고, 무거운 검증은 병합 후나 야간에 받습니다.\n다만 야간 결과를 아무도 보지 않으면 의미가 없으므로, 실패 알림과 담당이 함께 정해져야 합니다."),
      C("깨진 main","<code>main</code> 브랜치의 테스트가 실패한 상태를 방치할 때의 문제로 알맞은 것은?",
        ["새 브랜치를 만들 수 없게 되어 그 이후의 작업이 막힌다",
         "다음 사람들이 자기 탓으로 오해해 시간을 쓴다",
         "커버리지 기록이 초기화되어 이력이 사라진다",
         "배포 파이프라인이 자동으로 삭제된다"],1,
        "**남의 시간을 태우는 것**이 가장 큰 비용입니다. 각자 자기 변경을 의심하며 원인을 찾다 결국 남의 문제였음을 알게 됩니다.\n그래서 'main 이 빨간불이면 최우선으로 고친다' 는 규칙이 널리 쓰입니다.\n실패 알림이 팀 채널로 가고 누가 고칠지가 분명해야 이 규칙이 실제로 작동합니다."),
      I("필수 검사","PR 이 병합되려면 반드시 통과해야 하는 검사로 지정하는 GitHub 기능의 이름은? (한국어)",
        ["브랜치 보호","브랜치 보호 규칙","branch protection"],
        "정답은 **브랜치 보호 규칙(branch protection)** 입니다. 워크플로는 결과를 보고할 뿐이고, 그것을 필수로 만드는 것은 저장소 설정입니다.\n이 설정이 없으면 CI 가 빨간불이어도 누구나 병합할 수 있어 사실상 장식이 됩니다."),
      C("테스트 선택","변경된 부분과 관련된 테스트만 골라 돌리는 방식의 위험으로 알맞은 것은?",
        ["선택된 테스트만 커버리지에 반영되어 숫자가 낮아진다",
         "의존 계산이 틀리면 테스트를 빠뜨린다",
         "테스트 실행 순서가 매번 달라지면서 결과까지 불안정해진다",
         "프레임워크가 부분 실행을 지원하지 않는 경우가 많다"],1,
        "**의존 관계 계산이 틀리면** 영향을 받는 테스트가 실행되지 않아 회귀를 놓칩니다.\n그래서 PR 에서는 선택 실행으로 빠르게, 병합 후에는 전체를 돌리는 이중 구성이 흔합니다.\n큰 모노레포에서는 이 최적화의 이득이 매우 커서, 의존 그래프를 정확히 관리하는 것 자체가 중요한 인프라 작업이 됩니다."),
      C("병렬 실행","테스트를 병렬로 돌리기 위한 전제 조건으로 알맞은 것은?",
        ["모든 테스트가 하나의 똑같은 데이터베이스를 공유하고 있어야 한다",
         "서로 독립적이어서 공유 상태에 기대지 않아야 한다",
         "테스트 개수가 일정 수 이상이어야 한다",
         "커버리지 측정을 꺼 두어야 한다"],1,
        "**독립성**이 전제입니다. 공유 상태에 기대는 테스트는 병렬로 돌리면 서로를 망가뜨립니다.\n그래서 병렬화를 시도하면 그동안 숨어 있던 순서 의존이 한꺼번에 드러납니다 — 그 자체로 유용한 진단입니다.\n데이터베이스를 쓰는 테스트는 워커마다 스키마를 분리하는 방식으로 독립성을 확보합니다."),
      C("무엇을 자동화할까","CI 에서 사람 대신 기계가 확인하기에 가장 적합한 것으로 알맞은 것은?",
        ["새로 만든 기능이 사용자에게 실제로 필요한 것인지에 대한 판단",
         "포맷·린트·테스트처럼 정답이 하나인 검사",
         "코드 구조가 앞으로의 변경에 적합한지에 대한 평가",
         "화면 디자인이 브랜드에 어울리는지에 대한 판단"],1,
        "**정답이 하나로 정해지는 일**이 자동화 대상입니다. 사람이 하면 실수하고, 지적하면 감정이 상하지만 기계가 하면 아무도 기분 나쁘지 않습니다.\n나머지 셋은 맥락과 판단이 필요해 사람이 봐야 하고, 자동화가 걸러 주면 사람은 그 위에 집중할 수 있습니다.\n그래서 자동화는 리뷰를 대체하는 것이 아니라 **리뷰의 질을 올리는** 장치입니다.")
    ] },

  { t:"무엇을 테스트하지 않을 것인가", xp:30,
    th:{ sum:"모든 것을 테스트하려 하면 유지 비용에 눌린다. **테스트하지 않기로 정하는 것**도 설계의 일부다.",
      body:[
        {h:"남의 코드를 다시 확인하지 않는다", t:"라이브러리와 프레임워크가 제대로 동작하는지는 **그쪽의 테스트가 확인할 일**이다. 우리가 확인할 것은 '우리가 그것을 올바르게 쓰고 있는가' 다. 게터·세터, 단순 위임, 설정값 전달 같은 것도 마찬가지로 테스트해 봐야 얻는 것이 거의 없다."},
        {h:"바뀔 것과 바뀌지 않을 것", t:"자주 바뀌는 세부(문구, 픽셀 위치, 내부 호출 순서)를 테스트하면 **기능은 멀쩡한데 테스트만 계속 깨진다.** 그런 테스트는 곧 미움받고 지워진다. 대신 **오래 유지될 약속**(입력에 대한 결과, 공개 API 의 계약)을 테스트하면 오래 값어치를 한다."}],
      code:{ c:"// 값어치 낮음 — 프레임워크를 확인하고 있다\nexpect(user.name).toBe(\"홍길동\");   // 방금 넣은 값\n\n// 값어치 높음 — 우리 규칙을 확인한다\nexpect(gradeOf({ purchases: 12 })).toBe(\"vip\");", cap:"우리가 정한 규칙을 확인한다" },
      key:["남의 코드는 그쪽이 확인한다","단순 위임은 얻는 것이 없다","자주 바뀌는 세부는 피한다","오래 갈 약속을 테스트한다"] },
    q:[
      C("라이브러리","외부 라이브러리의 동작을 우리 테스트에서 확인하는 것이 대개 값어치가 낮은 이유로 알맞은 것은?",
        ["외부 라이브러리는 테스트 환경에서 실행되지 않기 때문이다",
         "그것은 라이브러리 쪽의 책임이기 때문이다",
         "라이선스상 외부 코드를 테스트하는 것이 금지되기 때문이다",
         "외부 라이브러리는 버전이 자주 바뀌기 때문에 테스트하는 것이 무의미해지기 때문이다"],1,
        "**책임의 경계**가 근거입니다. 라이브러리가 제대로 동작하는지는 그쪽이 확인하고, 우리는 그것을 우리 맥락에서 올바르게 쓰는지 확인합니다.\n다만 예외가 있습니다 — 라이브러리의 동작에 우리가 크게 의존한다면, 업그레이드 시 그 가정이 깨지는지 확인하는 테스트는 값어치가 있습니다.\n그런 테스트는 '라이브러리 검증' 이 아니라 **'우리 가정의 검증'** 입니다."),
      C("깨지기 쉬운 테스트","화면의 문구를 그대로 비교하는 테스트가 문제가 되는 이유로 알맞은 것은?",
        ["문자열 비교는 실행 속도가 느리기 때문이다",
         "문구만 다듬어도 깨져 신뢰를 잃기 때문이다",
         "다국어 지원이 들어가게 되면 문자열 비교가 아예 불가능해지기 때문이다",
         "문자열은 커버리지 계산에 포함되지 않기 때문이다"],1,
        "**기능과 무관한 이유로 깨지는 테스트**는 곧 미움받습니다. 문구를 다듬을 때마다 테스트를 고쳐야 하면 아무도 문구를 다듬지 않게 됩니다.\n식별자나 역할로 요소를 찾고, 문구 자체가 요구사항인 경우에만 문구를 확인하는 것이 균형입니다.\n다국어도 실제 이유이긴 하지만, 근본 문제는 깨지는 이유가 기능과 무관하다는 점입니다."),
      C("게터와 세터","단순히 값을 넣고 빼는 코드를 테스트하는 것이 값어치가 낮은 이유로 알맞은 것은?",
        ["그런 코드는 커버리지 계산에서 자동으로 제외되기 때문이다",
         "틀릴 여지가 없고 구현을 그대로 옮긴 것이기 때문이다",
         "그런 코드는 대부분의 언어에서 자동 생성되기 때문이다",
         "값을 넣고 빼는 동작은 테스트 프레임워크에서 아예 지원하지 않기 때문이다"],1,
        "**틀릴 여지가 없는 곳**을 테스트하면 유지 비용만 늘어납니다. 테스트가 구현을 그대로 반복하는 형태가 되어, 구현이 바뀌면 함께 바뀌어야 합니다.\n커버리지 숫자를 올리기 위해 이런 테스트를 채우는 것이 가장 흔한 낭비입니다.\n테스트할 값어치는 **판단과 계산이 들어간 곳**에 있습니다."),
      I("계약","서로 다른 서비스가 주고받는 형식이 맞는지 양쪽에서 확인하는 테스트를 뭐라고 부르나? (한국어)",
        ["계약 테스트","컨트랙트 테스트","contract test"],
        "정답은 **계약 테스트(contract test)** 입니다. 소비자가 기대하는 형식을 명세로 만들어 제공자 쪽에서도 확인합니다.\n전체를 함께 띄우는 통합 테스트보다 훨씬 가볍고 빠르면서, '한쪽이 응답 형식을 바꿔 다른 쪽이 깨지는' 대표적인 사고를 막아 줍니다."),
      C("스냅숏 테스트","출력 전체를 저장해 두고 달라지면 알려 주는 방식의 위험으로 알맞은 것은?",
        ["스냅숏 파일이 커지면 테스트를 실행하는 것 자체가 불가능해진다",
         "보지 않고 갱신하게 되기 쉽다",
         "스냅숏은 버전 관리 시스템에 저장할 수 없다",
         "스냅숏 방식은 커버리지를 측정하지 못한다"],1,
        "**'일단 갱신' 습관**이 가장 큰 위험입니다. 차이를 꼼꼼히 보지 않고 승인하면 회귀가 그대로 통과합니다.\n스냅숏이 클수록 이 문제가 심해지므로, 작게 유지하고 **핵심 부분만** 담는 것이 좋습니다.\n의도가 분명한 어서션 몇 개가 큰 스냅숏 하나보다 대개 낫습니다."),
      C("테스트 삭제","기존 테스트를 지우는 것이 정당한 경우로 알맞은 것은?",
        ["실행 시간이 너무 길어져서 전체 스위트가 느려지게 될 때",
         "확인 대상이 없어졌거나 중복될 때",
         "최근에 실패한 적이 있어 신뢰가 떨어졌을 때",
         "작성자가 팀을 떠나 아무도 내용을 모를 때"],1,
        "**확인 대상이 없어졌거나 중복될 때** 지우는 것이 정당합니다. 테스트도 유지 비용이 있는 코드입니다.\n느리다는 이유로 지우면 검증을 버리는 것이고, 실패했다는 이유로 지우면 그 실패가 알리려던 문제를 버리는 것입니다.\n아무도 모른다면 지우기 전에 **무엇을 확인하는지 먼저 알아내는 것**이 순서입니다."),
      C("우선순위","테스트를 쓸 시간이 제한적일 때 먼저 다뤄야 할 곳으로 알맞은 것은?",
        ["코드 줄 수가 가장 많은 파일",
         "피해가 크고 규칙이 복잡한 핵심 로직",
         "가장 최근에 작성된 코드",
         "테스트를 쓰기에 가장 쉬워 보이는 부분"],1,
        "**위험과 복잡도**가 기준입니다. 결제 계산, 권한 판정, 데이터 정합성처럼 틀리면 대가가 큰 곳부터 다룹니다.\n쓰기 쉬운 곳부터 채우면 숫자는 오르지만 실제 위험은 그대로 남습니다.\n줄 수는 위험과 상관이 약하고, 최신 코드라고 더 위험한 것도 아닙니다.")
    ] }
]}

];

/* 단답형 보강 — 레슨마다 한 문항씩 더한다.
   개념의 이름을 보기에서 고르는 것과 직접 떠올려 적는 것은 다르다.
   적어 본 낱말이 대화에서 쓸 수 있는 낱말이 된다. */
const MORE={
  "테스트는 증명이 아니라 안전망이다":
    I("거짓 신호","코드에는 문제가 없는데 테스트가 실패하는 경우를 뭐라고 부르나? (한국어)",
      ["거짓 양성","오탐","false positive","위양성"],
      "정답은 **거짓 양성(false positive)** 입니다. 테스트가 '문제 있음' 이라고 말했는데 실제로는 없는 경우입니다.\n반대로 문제가 있는데 통과하는 것은 거짓 음성입니다. 둘 중 팀의 신뢰를 더 빨리 무너뜨리는 쪽은 거짓 양성입니다 — 사람들이 빨간불을 무시하기 시작하기 때문입니다.\n플레이키 테스트는 거짓 양성을 대량으로 만들어 내는 공장이라, 방치하면 스위트 전체가 값어치를 잃습니다."),

  "좋은 테스트의 모양":
    I("서로 영향 없음","테스트끼리 서로 영향을 주지 않아야 한다는 성질을 뭐라고 부르나? (한국어)",
      ["독립성","격리","독립","고립"],
      "정답은 **독립성(격리)** 입니다. 어떤 순서로 돌려도, 하나만 따로 돌려도 결과가 같아야 합니다.\n독립적이지 않으면 실패했을 때 '이 테스트가 문제인지 앞 테스트가 남긴 상태가 문제인지' 를 알 수 없어 조사 비용이 몇 배로 뜁니다.\n병렬 실행도 독립성 위에서만 가능합니다. 공용 데이터를 함께 쓰는 테스트는 병렬로 돌리는 순간 무작위로 깨집니다."),

  "버그는 경계에 산다":
    I("하나 차이","반복문이나 비교에서 하나가 어긋나 생기는 흔한 실수를 뭐라고 부르나? (영어)",
      ["off-by-one","오프바이원","off by one"],
      "정답은 **off-by-one** 입니다. `<` 를 `<=` 로 쓰거나 인덱스를 0이 아닌 1부터 세면서 생기는 실수입니다.\n이 실수는 범위 한가운데 값으로는 절대 드러나지 않습니다. 마지막 원소를 빼먹거나 하나 더 도는 것이라 경계에서만 보입니다.\n그래서 경계마다 바로 아래·경계·바로 위 세 값을 확인하는 습관이 이 종류의 버그를 통째로 막아 줍니다."),

  "테스트하기 쉬운 코드":
    I("같은 입력 같은 결과","같은 입력에 언제나 같은 결과를 주고 바깥 상태를 바꾸지 않는 함수를 뭐라고 부르나? (한국어)",
      ["순수 함수","순수함수","pure function"],
      "정답은 **순수 함수** 입니다. 시계도 난수도 파일도 건드리지 않고, 준 것만 보고 답을 돌려줍니다.\n순수 함수는 테스트하기가 가장 쉽습니다 — 준비할 것도 정리할 것도 없이 넣고 비교하면 끝나기 때문입니다.\n'단위 테스트를 늘리자' 는 말은 실제로는 '순수한 부분을 넓히자' 는 말과 거의 같습니다. 계산을 입출력에서 떼어 내는 것이 그 방법입니다."),

  "스텁 · 목 · 페이크":
    I("기록만 하는 것","진짜 동작은 그대로 두고 호출된 사실만 기록하는 테스트 더블은? (한국어)",
      ["스파이","spy"],
      "정답은 **스파이(spy)** 입니다. 결과값에 드러나지 않는 부수 효과를 확인할 때 씁니다.\n'알림이 정확히 한 번 갔는가' 처럼 반환값으로는 볼 수 없는 것이 스파이의 자리입니다.\n다만 남용하면 '무엇을 하는가' 대신 '어떻게 하는가' 를 못 박게 되어, 구조를 바꿀 때마다 테스트가 함께 깨집니다."),

  "픽스처와 테스트 데이터":
    I("기본값 위에 덮어쓰기","기본값을 갖춘 테스트 객체를 만들고 필요한 것만 바꿔 쓰게 해 주는 도우미를 뭐라고 부르나? (한국어)",
      ["빌더","builder","테스트 데이터 빌더"],
      "정답은 **빌더(builder)** 입니다. `aUser().withGrade(\"gold\").build()` 처럼 씁니다.\n빌더를 쓰면 테스트 본문에 **그 테스트에 중요한 값만** 남습니다. 나머지 열 개 필드가 기본값으로 채워지므로 읽는 사람이 무엇이 요점인지 바로 압니다.\n필드가 하나 늘어날 때 테스트 수십 개를 고치지 않아도 되는 것도 큰 이득입니다 — 빌더 한 곳만 고치면 됩니다."),

  "단위 · 통합 · E2E":
    I("가장 넓은 층","테스트 피라미드에서 가장 넓게 깔려야 하는 층은? (한국어)",
      ["단위 테스트","단위","유닛 테스트"],
      "정답은 **단위 테스트** 입니다. 가장 빠르고 값싸며 실패 지점이 좁아, 많이 두어도 부담이 적습니다.\n위로 갈수록 현실에 가깝지만 느리고 잘 흔들려서 수를 줄입니다. 이 모양이 뒤집힌 것이 아이스크림콘입니다.\n다만 비율은 규칙이 아니라 출발점입니다. 화면 조립이 대부분인 앱과 순수 로직이 많은 라이브러리는 답이 다릅니다."),

  "커버리지 — 숫자를 다루는 법":
    I("줄만 보는 것","각 줄이 한 번이라도 실행됐는지만 보는 커버리지 종류는? (한국어)",
      ["구문 커버리지","줄 커버리지","라인 커버리지","statement coverage"],
      "정답은 **구문 커버리지** 입니다. 가장 흔히 보고되는 숫자이고, 동시에 가장 헐거운 기준입니다.\n`if (a && b)` 한 줄은 실행되기만 하면 100% 로 세어지지만, 실제로는 네 가지 조합 중 하나만 지난 것일 수 있습니다.\n그래서 분기 커버리지, 나아가 조건 조합까지 보는 기준이 따로 있습니다. 숫자를 볼 때는 **무엇을 세는 숫자인지**를 먼저 확인해야 합니다."),

  "빨강 · 초록 · 정리":
    I("먼저 확인할 것","TDD 에서 코드를 쓰기 전에 새 테스트가 어떤 상태인지 반드시 확인해야 하나? (한국어 두 글자)",
      ["실패","레드","빨강"],
      "정답은 **실패** 입니다. 실패하는 것을 보지 않고 넘어가면, 그 테스트가 정말 무언가를 확인하고 있는지 알 수 없습니다.\n오타로 조건이 항상 참이거나, 어서션이 실행되지 않는 자리에 있어도 통과는 통과입니다. **처음의 빨간불이 그 테스트의 유효성 검사**입니다.\n같은 이유로, 특성화 테스트를 붙일 때도 일부러 기대값을 틀리게 넣어 실패를 확인한 뒤 올바른 값으로 고치는 방법을 씁니다."),

  "플레이키 테스트 — 믿을 수 없게 되는 순간":
    I("무조건 기다리기","정해진 시간만큼 무조건 기다리는 방식을 뭐라고 부르나? (한국어)",
      ["고정 대기","하드 슬립","sleep","슬립"],
      "정답은 **고정 대기(sleep)** 입니다. 프론트엔드와 E2E 플레이키의 가장 흔한 원인입니다.\n느린 환경에서는 부족해서 실패하고 빠른 환경에서는 낭비합니다. 실패할 때마다 시간을 늘리는 대응은 스위트만 느려지게 하고 언젠가 다시 실패합니다.\n답은 '조건이 만족될 때까지' 기다리는 것입니다 — 확인하려는 조건 자체를 대기 조건으로 삼으면 두 문제가 한꺼번에 사라집니다."),

  "빠르지 않으면 아무도 기다리지 않는다":
    I("나눠 돌리기","테스트를 여러 대에 나눠 병렬로 돌릴 때 나눈 조각을 뭐라고 부르나? (영어)",
      ["shard","샤드","shards"],
      "정답은 **샤드(shard)** 입니다. 10분짜리 스위트를 4개로 나누면 이론상 2분 30초가 됩니다.\n실제로는 가장 오래 걸리는 샤드가 전체 시간을 정하므로, **긴 테스트부터 가장 한가한 샤드에 넣는** 방식으로 균형을 맞춥니다.\n파일 이름 알파벳순으로 나누는 흔한 방법이 자주 불균형해지는 이유가 여기 있습니다. 지난 실행 시간을 기록해 두었다가 쓰는 것이 낫습니다."),

  "무엇을 테스트하지 않을 것인가":
    I("어디부터 쓸까","틀렸을 때 대가가 큰 곳부터 확인 자원을 쓰는 방식을 뭐라고 부르나? (한국어)",
      ["위험 기반 테스트","위험 기반","리스크 기반 테스트","리스크 기반"],
      "정답은 **위험 기반 테스트** 입니다. '전부 확인한다' 가 불가능하다는 것을 인정하는 데서 출발합니다.\n결제 금액이 틀리는 것과 목록 정렬이 틀리는 것은 대가가 다릅니다. 같은 시간을 쓴다면 앞쪽에 쓰는 것이 옳습니다.\n무엇을 비워 뒀는지 **알고 있는 것**이 이 방식의 핵심입니다. 모르고 빠뜨린 것과 알고 비워 둔 것은 전혀 다릅니다.")
};
UNITS.forEach(u=>u.l.forEach(L=>{ if(MORE[L.t]) L.q.push(MORE[L.t]); }));

module.exports=shuffleUnits(UNITS);
