module.exports = {
  track: "security", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 검사는 했는데 우회되는 코드 고치기",
  source: "./dbg_security.cjs",
  lessons: [
    { t: "경계 없는 문자열 검사", n: 6,
      th: { sum: "보안 버그의 절반은 **문자열 포함으로 적힌 검사**다 — 정상 입력은 전부 통과하고 공격 입력만 뚫는다.",
        body: [
          { h: "포함·접두사에는 경계가 없다", t: "`origin.includes('a.com')` 은 `a.com.evil.net` 을 통과시키고, `target.startsWith('/var/www')` 는 `/var/www2` 를 통과시키며, `granted.includes('read')` 는 `reading` 을 통과시킨다. 접근 제어에서 `includes`·`startsWith`·`endsWith` 는 거의 언제나 잘못된 도구다 — **정확히 일치**하거나 **구분자 경계**를 함께 봐야 한다." },
          { h: "지우는 방식은 겹쳐 쓰기로 뚫린다", t: "`replace('../','')` 는 첫 자리 하나만 바꾸므로 `....//` 는 지운 뒤 다시 `../` 가 된다. 위험한 것을 지우는 블랙리스트 대신 **허용 문자만 남기는** 화이트리스트가 원칙이다. `//evil.com` 처럼 규약이 특별하게 읽는 꼴도 함께 막아야 한다." }],
        code: { c: "return t === r || t.startsWith(r + '/');   // 경계를 함께 본다\nreturn granted.split(/\\s+/).indexOf(need) >= 0;", cap: "포함이 아니라 목록의 원소로" },
        key: ["접근 제어에 includes 를 쓰지 않는다", "접두사에는 구분자 경계", "지우기보다 허용 목록", "//·/\\ 는 외부 주소다"] } },
    { t: "'없음' 과 '아무거나' 를 통과시키는 검사", n: 6,
      th: { sum: "검사가 있는데도 뚫리는 두 번째 갈래 — **비어 있는 값**, **규격에 있는 특수값**, **모양을 안 보는 값**.",
        body: [
          { h: "없음 == 없음", t: "`sent === cookie` 는 둘 다 `undefined` 일 때 참이 되어, 공격자는 토큰을 **아예 보내지 않으면** 된다. `alg: 'none'` 은 규격에 있는 값이라 '값이 있는가' 만 보면 서명 없는 토큰이 유효해진다. 두 경우 모두 **허용 목록과 빈 값 검사**가 답이다." },
          { h: "길이는 알고리즘이 아니다", t: "MD5 는 32글자라 '길면 안전' 검사를 통과하지만 초당 수십억 번 계산된다. 토큰도 '문자열이고 비어 있지 않다' 는 아무것도 검증하지 않는 것과 같다 — **길이와 글자 집합**을 못 박아야 한다. 그리고 이스케이프 규칙에서는 **이스케이프 문자 자신**을 먼저 막아야 막이 풀리지 않는다." }],
        code: { c: "if (!sent || !cookie) return false;\nreturn ['$2b$','$argon2'].some(p => s.startsWith(p));", cap: "빈 값과 특수값을 먼저 가른다" },
        key: ["양쪽이 비었을 때를 테스트한다", "알고리즘은 검증하는 쪽이 정한다", "형식 검증은 가장 싼 방어", "이스케이프 문자 자신을 먼저 막는다"] } },
  ],
};
