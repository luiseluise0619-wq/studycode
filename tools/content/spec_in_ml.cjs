module.exports = {
  track: "ml", guide: "✍️", xp: 60,
  unit: "단답으로 확인하기 — 개념 이름과 지표",
  source: "./in_ml.cjs",
  lessons: [
    { t: "왜 안 맞는가를 가르기", n: 8,
      th: { sum: "성능이 나쁠 때 먼저 할 일은 고치는 것이 아니라 **어느 쪽 문제인지 가르는 것**이다 — 편향인가 분산인가, 규제가 모자란가 데이터가 모자란가.",
        body: [
          { h: "두 갈래로 가른다", t: "학습 오차부터 높으면 <b>편향</b>(모델이 너무 단순), 학습은 잘하는데 검증만 나쁘면 <b>과적합</b>이다 — 잡음까지 외운 상태이고, 검증 곡선이 갈라지는 지점이 멈춰야 할 때다. 처방은 데이터 늘리기·모델 줄이기·규제·조기 종료인데 <b>데이터를 늘리는 것</b>이 가장 확실하다. 규제 중 <b>L1</b> 은 계수를 0 으로 만들어 특성 선택 역할까지 하고, L2 는 작게만 만든다." },
          { h: "검증을 제대로 하기", t: "<b>교차검증</b>은 모든 데이터가 한 번씩 검증에 쓰여 운의 영향을 줄이지만, 시간이 있는 데이터에는 그대로 쓰면 미래로 과거를 맞히게 된다. 더 조심할 것은 <b>누수</b>다 — 전체 데이터로 스케일러를 학습하면 검증 정보가 전처리에 새어 든다. 전처리는 <b>훈련으로만 학습하고 검증에는 적용만</b> 하며, 파이프라인으로 묶으면 실수하기 어려워진다." }],
        code: { c: "Pipeline([('sc', StandardScaler()), ('m', 모델)])\n# 시간 데이터: 앞을 학습, 뒤를 검증\nL1 → 계수가 0 이 된다 · L2 → 작아진다", cap: "고치기 전에 어느 쪽인지 가른다" },
        key: ["학습 오차부터 보면 갈래가 정해진다", "데이터를 늘리는 것이 가장 확실", "전처리는 훈련으로만 학습", "시간 데이터에 무작위 분할 금지"] } },
    { t: "모델을 고르고 재는 법", n: 7,
      th: { sum: "앙상블의 힘은 개별 성능이 아니라 **서로 얼마나 다른가**에서 나오고, 지표는 **틀렸을 때의 비용**이 정한다.",
        body: [
          { h: "다르게 만드는 방법", t: "<b>배깅</b>은 표본을 다르게 뽑아 따로 학습해 <b>분산</b>을 줄이고 병렬화가 되며, <b>부스팅</b>은 앞의 실수를 뒤가 메워 <b>편향</b>을 줄이는 대신 순차적이다. 랜덤 포레스트는 배깅에 <b>특성 무작위 선택</b>을 더해 트리끼리 닮는 것을 막는다. 표 형태 데이터에서는 여전히 부스팅 계열이 가장 강한 기본기라, 딥러닝을 꺼내기 전에 먼저 시도한다. 트리 기준으로는 계산이 가벼운 <b>지니</b> 가 기본값이다." },
          { h: "재는 법과 찾는 법", t: "불균형 데이터에서 정확도는 무의미하다 — 1% 양성이면 전부 음성이라 해도 99% 다. <b>재현율</b>(실제 양성 중 찾은 비율)과 정밀도(양성이라 한 것 중 맞은 비율)는 임계값을 옮기면 반대로 움직이므로 함께 본다. 거리 기반 알고리즘(<b>KNN</b>)은 스케일링이 필수이고, 범주가 많은 열을 원-핫 하면 <b>차원</b>이 폭발한다. 하이퍼파라미터는 격자보다 <b>랜덤 서치</b>가 같은 예산에서 중요한 축을 더 촘촘히 훑는다." }],
        code: { c: "정밀도 = TP / (TP + FP)   ·   재현율 = TP / (TP + FN)\nmax_features = √p (분류) · p/3 (회귀)\nRandomizedSearchCV(...)", cap: "무엇이 더 아픈지가 지표를 정한다" },
        key: ["배깅은 분산, 부스팅은 편향", "앙상블은 서로 달라야 는다", "불균형에서는 정확도를 버린다", "랜덤 서치가 격자보다 효율적"] } },
  ],
};
