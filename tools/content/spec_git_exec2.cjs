module.exports = {
  track: "git", guide: "", xp: 80,
  unit: "직접 구현 — 패치와 이력을 읽는 도구",
  source: "./git_exec2.cjs",
  lessons: [
    { t: "패치를 읽는다 — 헝크와 통계", n: 5,
      th: { sum: "diff 는 사람이 읽으라고 만든 글이 아니라 **기계가 붙일 수 있는 형식**이다. 헝크 머리 한 줄만 읽을 줄 알면 나머지는 따라온다.",
        body: [
          { h: "@@ 한 줄이 자리를 말한다", t: "`@@ -12,7 +12,9 @@` 는 '원본의 12번째 줄부터 7줄이 새 파일의 12번째 줄부터 9줄이 되었다' 는 뜻이다. 줄 수가 1이면 **생략된다** — `@@ -3 +3,2 @@` 는 원본 1줄이라는 말이라, 이 규칙을 모르고 파싱하면 한 줄짜리 변경에서만 조용히 어긋난다. 뒤에 붙는 글자는 그 헝크가 어느 함수 안인지 알려 주는 **참고 문구**일 뿐 형식의 일부가 아니므로 읽되 믿지는 않는다." },
          { h: "센 것과 세지 않은 것", t: "`+`/`-` 로 시작하는 줄만 세고 **공백으로 시작하는 문맥 줄은 세지 않는다.** 헷갈리는 자리가 하나 있다 — 패치 머리의 `+++`/`---` 는 파일 이름 줄이지 추가·삭제가 아니다. 이것을 빼지 않으면 모든 파일에서 정확히 한 줄씩 부풀고, 통계는 '조금씩 틀린 숫자' 가 되어 아무도 믿지 않게 된다. 낱말 단위 비교는 줄 단위 diff 위에 한 겹 더 얹는 것이라, 줄이 통째로 바뀐 것처럼 보이던 오탈자 수정이 한 낱말로 줄어든다." }],
        code: { c: "const m = line.match(/^@@ -(\\d+)(?:,(\\d+))? \\+(\\d+)(?:,(\\d+))? @@/);\n// 줄 수가 없으면 1이다 — 생략된 것이지 0이 아니다\nconst oldLen = m[2] === undefined ? 1 : Number(m[2]);", cap: "생략된 줄 수는 1로 읽는다" },
        key: ["`@@` 는 양쪽의 시작 줄과 줄 수", "줄 수 생략은 1을 뜻한다", "문맥 줄과 `+++` 머리는 세지 않는다", "함수 이름 꼬리는 참고일 뿐이다"] } },

    { t: "객체와 이름 — 내용이 곧 주소다", n: 5,
      th: { sum: "Git 에는 파일 이름으로 찾는 자리가 없다. 모든 것이 **내용을 해시한 값**으로 저장되고, 사람이 쓰는 이름은 그 위에 붙인 꼬리표다.",
        body: [
          { h: "같은 내용은 같은 객체다", t: "객체 이름은 `타입 길이\\0내용` 을 해시한 값이다. 그래서 **같은 내용이면 언제 어디서 만들어도 같은 이름**이 되고, 파일을 복사해 두어도 저장소는 커지지 않는다. 이 성질이 브랜치를 싸게 만들고, 원격과 주고받을 때 '이미 가진 것' 을 이름만으로 걸러 내게 해 준다. 반대로 한 글자만 달라도 완전히 다른 이름이 되므로, 줄 끝 문자나 마지막 개행 하나가 통째로 다른 객체를 만든다." },
          { h: "이름은 순서를 모른다", t: "태그 `v1.10.0` 과 `v1.9.0` 을 글자로 견주면 `1` 이 `9` 보다 앞서 **1.10 이 1.9 보다 작아진다.** 버전은 반드시 점으로 쪼개 **숫자로** 견주어야 한다. `describe` 가 붙이는 `v1.2.0-5-gabc123` 도 이름이 아니라 계산 결과다 — '가장 가까운 태그로부터 5개 더 왔고 지금은 abc123' 이라는 뜻이라, 태그 위에 정확히 서 있으면 꼬리가 아예 붙지 않는다." }],
        code: { c: "// 글자 비교가 아니라 자리별 숫자 비교\nconst pa = a.split(\".\").map(Number), pb = b.split(\".\").map(Number);\nfor (let i = 0; i < 3; i++) if (pa[i] !== pb[i]) return pa[i] - pb[i];", cap: "1.10 은 1.9 보다 크다" },
        key: ["이름은 내용에서 나온다", "같은 내용은 하나의 객체", "버전은 숫자로 견준다", "describe 의 꼬리는 태그로부터의 거리"] } },

    { t: "이력을 고른다 — 옮기고 합치고 되짚는다", n: 5,
      th: { sum: "커밋을 옮기고 합치는 명령들은 겉보기와 달리 **원본을 고치지 않는다.** 언제나 새 커밋을 만들고, 옛 커밋은 자리만 잃는다.",
        body: [
          { h: "같은 변경, 다른 이름", t: "cherry-pick 은 저쪽 커밋을 **가져오는 것이 아니라 같은 변경으로 새 커밋을 만드는 것**이다. 그래서 내용이 같아도 이름은 달라지고, 나중에 그 브랜치를 병합하면 같은 변경이 두 번 보인다. 이미 저쪽에 든 변경을 다시 고르지 않으려면 커밋 이름이 아니라 **변경의 내용**으로 견주어야 한다. squash 도 마찬가지다 — 여러 커밋이 한 커밋이 되고 메시지는 합쳐지지만, 원본들은 사라지는 것이 아니라 아무도 가리키지 않게 될 뿐이다." },
          { h: "되짚을 수 있는 길이 남는다", t: "reset 이나 rebase 로 '잃어버린' 것처럼 보이는 커밋은 **reflog** 에 남아 있다. reflog 는 브랜치가 지나온 자리를 시간순으로 적어 둔 기록이라, 몇 시점 전으로 돌아갈지만 정하면 그대로 되살릴 수 있다. bisect 는 반대 방향의 도구다 — 이력을 **반씩 잘라 가며** 처음 고장 난 커밋을 찾으므로, 커밋 1000개도 10번 남짓이면 좁혀진다. 두 도구 모두 '이력이 남아 있다' 는 같은 성질 위에 서 있다." }],
        code: { c: "// 이름이 아니라 변경 내용으로 견준다\nconst there = new Set(target.map((c) => c.change));\nreturn source.filter((c) => !there.has(c.change)).map((c) => c.id);", cap: "cherry-pick 은 새 커밋을 만든다" },
        key: ["cherry-pick 은 복사가 아니라 재생성", "같은 변경은 내용으로 걸러 낸다", "reflog 에 지나온 자리가 남는다", "bisect 는 반씩 잘라 log2 번"] } },

    { t: "협업의 규칙 — 누가 언제 무엇을", n: 5,
      th: { sum: "팀에서 Git 이 하는 일의 절반은 저장이 아니라 **책임을 되짚는 것**이다. 이름 바뀜을 알아보고, 줄의 출처를 찾고, 규칙을 걸어 둔다.",
        body: [
          { h: "이름 바뀜은 기록되지 않는다", t: "Git 은 파일을 옮겼다는 사실을 저장하지 않는다. 삭제와 추가가 있을 뿐이고, **내용이 충분히 닮았으면 이름이 바뀐 것으로 짐작한다.** 그래서 파일을 옮기면서 크게 고치면 이력이 끊겨 보이고, 옮기는 커밋과 고치는 커밋을 나누면 그대로 이어진다. 이것은 저장 방식의 부작용이 아니라 선택이다 — 이름 바뀜을 기록하지 않으므로 나중에 더 좋은 판정 기준이 나와도 옛 이력에 그대로 적용된다." },
          { h: "규칙은 사람보다 먼저 말한다", t: "blame 은 그 줄을 **마지막으로 건드린** 커밋을 알려 줄 뿐, 그 줄을 쓴 사람이 아니다. 형식 정리 한 번이면 파일 전체의 blame 이 한 사람으로 덮이므로, 답을 얻는 도구가 아니라 실마리를 찾는 도구로 쓴다. 보호 규칙은 그 앞단이다 — 리뷰 몇 명, 어떤 검사가 통과해야 하는가, 강제 푸시를 막을 것인가를 저장소에 적어 두면 사람이 매번 기억하지 않아도 된다. 규칙은 사람을 못 믿어서가 아니라, **급할 때의 판단을 미리 해 두려고** 있다." }],
        code: { c: "// 삭제·추가 중 내용이 닮은 짝을 이름 바뀜으로 본다\nconst score = same / Math.max(a.lines.length, b.lines.length);\nif (score >= 0.5) pairs.push([a.path, b.path]);", cap: "이름 바뀜은 저장이 아니라 추정" },
        key: ["이름 바뀜은 닮은 정도로 짐작한다", "옮기기와 고치기를 나눠 커밋한다", "blame 은 마지막에 건드린 사람", "보호 규칙은 급할 때를 위해 미리"] } },
  ],
};
