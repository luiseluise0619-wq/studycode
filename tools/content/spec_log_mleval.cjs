module.exports = {
  track: "mleval", guide: "", xp: 70,
  unit: "로그 분석 — 평가가 거짓말하는 자리",
  source: "./log_mleval.cjs",
  lessons: [
    { t: "자를 잘못 대면", n: 6,
      th: { sum: "평가가 틀리는 이유는 대개 모델이 아니라 **자를 대는 방식**이다.",
        body: [
          { h: "무엇으로 골랐는가", t: "테스트 세트로 모델을 고르면 그 순간 테스트는 검증이 되고, 남은 '한 번도 안 본 데이터' 는 사라진다. 40개 후보 중 최고를 집었다면 그 점수에는 <b>40번 중 가장 운이 좋았던 만큼</b>이 섞여 있다. 로그에서 <b>몇 번 열었는지</b>를 세는 습관이 필요하다." },
          { h: "어떻게 나눴는가", t: "같은 문서가 여러 행이면 행 단위 분할은 훈련과 테스트를 섞는다. 시간이 있는 데이터를 섞어 나누면 미래로 과거를 맞힌다. 분할 직후 <b>두 쪽의 교집합 크기</b>와 <b>시간 경계</b>를 로그에 남기면 이런 사고는 눈에 보인다." }],
        code: { c: "선택은 valid 로, 테스트는 마지막 한 번\n그룹 분할: 같은 문서 id 는 한쪽에만\n시간 데이터: TimeSeriesSplit, 최신 구간을 검증에", cap: "점수보다 먼저 분할을 본다" },
        key: ["테스트로 고르면 테스트가 아니다", "중복은 그룹 분할로", "시간 데이터에 무작위 분할 금지", "교집합 0 을 로그로 확인"] } },
    { t: "숫자를 견줄 자격", n: 6,
      th: { sum: "두 숫자를 견주려면 **같은 자**로, **같은 것**을, **잡음보다 큰 차이**로 재야 한다.",
        body: [
          { h: "차이가 잡음보다 큰가", t: "표본 1,200건에서 f1 의 부트스트랩 폭이 ±0.021 이면 0.004 차이는 언제든 뒤집힌다. 점 추정만 비교한 결론은 <b>동전 던지기</b>다. 보고서에는 차이와 함께 <b>구간</b>을 싣고, 겹치면 '차이 없음' 이 결론이다. 더 작은 차이를 보고 싶으면 모델이 아니라 표본을 늘린다." },
          { h: "같은 자로 재고 있는가", t: "임계값이 다르면 다른 분류기이고, 골든셋이 바뀌면 어제와 오늘을 견줄 수 없다. 캐시가 옛 예측을 돌려주면 무엇을 올려도 같은 점수가 나온다. 평가 로그에는 <b>모델 해시·데이터 버전·임계값·실제 추론 건수</b>가 남아야 한다." }],
        code: { c: "A 0.8412 vs B 0.8455, 부트스트랩 ±0.021 → 판정 불가\n캐시 키에 모델 해시 없음     → 적중 100%, 점수 동일\ngolden/latest → golden/v3    → 버전을 고정한다", cap: "견줄 수 없는 두 숫자를 견주지 않는다" },
        key: ["구간이 겹치면 차이 없음", "임계값이 다르면 다른 모델", "데이터는 버전으로 고정", "추론 건수 0 이면 실패"] } },
  ],
};
