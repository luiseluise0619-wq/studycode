/* 작은 트랙 15개 — 정답률 15% 아래 끝맺음의 오답을 구체적인 틀린 답으로 (72차) */
module.exports = [
{ track: "algo", fixes: [
  { k: "algo2-hash-order-need", from: "다른 구조는 필요 없다", to: "삽입 순서를 그대로 유지하는 일반 배열 하나면 충분하고 범위 질의는 선형 탐색으로 처리한다" },
  { k: "algo2-sort-quick-worst", from: "최악의 경우를 걱정할 필요가 없다", to: "재귀를 쓰지 않고 반복문으로 구현했을 때만 발생하므로, 교과서대로 재귀 형태를 유지하면 최악의 경우는 오지 않는다" },
  { k: "algo-dp-tab-order", from: "상관하지 않아도 된다", to: "메모리 접근이 연속되도록 인덱스 순서만 맞춰 주면 충분하고 나머지 의존 관계는 캐시가 알아서 처리해 준다" },
  { k: "algo-bs-invariant", from: "조건식이 참이 되도록 짜여 있기 때문이다", to: "조건식이 결국 참이 되도록 짜여 있어서다" },
  { k: "algo-bs-ceil-mid", from: "아무런 문제가 생기지 않는다", to: "mid 를 lo + (hi - lo) / 2 로 그대로 두어도 루프는 정상적으로 끝난다" },
  { k: "algo-str-rolling", from: "1 로 고정되어 있기 때문이다", to: "윈도우의 크기가 1 로 고정되어 있어서다" },
  { k: "algo-str-amortized", from: "한 번도 실행되지 않기 때문이다", to: "while 루프가 실제로는 한 번도 실행되지 않는 탓이다" },
  { k: "그리디실패", from: "같아 차이가 생기지 않는다", to: "그리디와 최적해가 모두 2개로 같아 차이는 0 이다" },
  { k: "문자열연결", from: "전체 성능에는 아무 문제가 생기지 않는다", to: "O(n) — 문자열이 제자리에서 그대로 늘어나기 때문에 몇 번을 이어 붙이더라도 전체 성능은 선형으로 유지된다" }
] },
{ track: "arch", fixes: [
  { k: "arch-load-store", from: "캐시를 아예 쓰지 않는다", to: "캐시 없이 메모리를 직접 읽는다" },
  { k: "겹칠지도 모른다", from: "컴파일러가 벡터 명령을 쓰지 않는다", to: "배열이 캐시 라인 경계에 정렬되어 있지 않을 가능성이다 — 벡터 적재는 정렬된 주소에서만 안전하므로, 정렬을 보장받지 못한 포인터에는 컴파일러가 벡터 명령을 넣지 못한다" },
  { k: "일을 두 배 한다", from: "벡터화 이득이 커지므로 문제가 없다", to: "조건이 드물수록 벡터화 이득이 커지므로 오히려 유리하다" },
  { k: "시간으로 읽어 내기", from: "눈으로도 구분할 수 있기 때문이다", to: "8.6배 차이면 사람이 눈으로도 구분할 수 있어서다" },
  { k: "검사와 실행의 순서", from: "하드웨어가 알 수 없기 때문이다", to: "권한 확인은 소프트웨어가 하는 일이라 하드웨어가 알 길이 없어서다" },
  { k: "여기서 멈춰라", from: "멈춤은 생기지 않는다", to: "그 아래 코드가 캐시에 미리 올라가도록 만든다. 그 지점부터는 필요한 코드가 미리 캐시에 올라와 오히려 빨라진다. 멈춤은 없다" },
  { k: "속도와 전력의 관계", from: "주파수와 함께 바뀌지 않기 때문이다", to: "정확히 2배 늘어난다 — 전력은 주파수에 정비례한다. 스위칭이 두 배 자주 일어나면 스위칭에 드는 에너지도 두 배가 되고, 전압은 회로가 정하는 고정값이라 주파수와 함께 바뀌지 않아서다" },
  { k: "쉴 때는 얼마나 쓰나", from: "아낄 이유가 없기 때문이다", to: "휴면 상태는 배터리로 도는 기기에서만 쓰고 서버에서는 쓰지 않는다 — 서버는 언제 요청이 올지 모르므로 최고 주파수로 깨어 있어야 하고, 전력은 전원이 넉넉해 아낄 이유가 없어서다" },
  { k: "노트북과 서버는 다르다", from: "다시 잴 필요가 없다", to: "서버는 더 빠르므로 개선 폭이 작아 보이는 것뿐이다 — 같은 절대 시간을 줄여도 전체가 짧으면 비율이 작아지므로 착시일 뿐이고, 개선 자체는 그대로 옮겨 가므로 다시 재는 것은 낭비다" }
] },
{ track: "os", fixes: [
  { k: "cs2-proc-isolation-fault", from: "메모리 누수가 아예 생기지 않는다", to: "프로세스에는 가비지 컬렉터가 없으므로 메모리 누수는 커널이 막아 준다" },
  { k: "cs2-ctxsw-mode-vs-context", from: "따로 떼어서 볼 필요가 없다", to: "모드 전환과 컨텍스트 스위칭은 짝을 이뤄 일어나므로, 두 가지의 비용은 하나로 묶어 본다" },
  { k: "cs2-ctxsw-mode-vs-context", from: "특권 레벨은 건드리지 않기 때문이다", to: "컨텍스트 스위칭이 모드 전환보다 더 저렴하게 끝난다 — 레지스터만 바꾸면 되고 특권 레벨은 건드리지 않아서다" },
  { k: "osfs-safe-write", from: "fsync 는 따로 부르지 않아도 된다", to: "임시 파일에 쓰기 → rename → fsync 는 rename 이 대신한다" },
  { k: "osipc-signal-data", from: "보낼 방법이 아예 없기 때문이다", to: "한 프로세스에 한 종류의 시그널만 등록해 둘 수 있어서, 값을 실어서 여러 정보를 동시에 보낼 길이 아예 막혀 있어서다" },
  { k: "코루틴", from: "양보 지점을 적을 필요가 없다", to: "커널 스케줄러가 타임 슬라이스마다 코루틴을 선점적으로 갈아 끼우며 실행하므로 양보 지점은 적지 않는다" },
  { k: "ospg-clock", from: "같은 결과를 보장해 주기 때문", to: "OPT 와 완전히 같은 결과를 보장해 주어서" },
  { k: "ospg-swap-used", from: "아무런 영향을 주지 않기 때문이다", to: "아니다 — 스왑은 성능에 아무런 영향을 주지 않아서다" },
  { k: "osobs-load", from: "손댈 방법이 없다", to: "CPU 가 온도나 전력 제한 때문에 하드웨어 스로틀링에 걸려 있는 것이며, 게스트 안에서는 손댈 길이 막혀 있다" }
] },
{ track: "linux", fixes: [
  { k: "따옴표", from: "작은따옴표로 감싸면 문법 오류가 난다", to: "작은따옴표 안의 $ 는 셸이 거부한다" },
  { k: "리다이렉션", from: "오류가 난다", to: "파일이 이미 있다며 셸이 거부한다" },
  { k: "폴더의 x", from: "아무것도 할 수 없게 된다", to: "그 폴더는 통째로 접근이 막힌다" },
  { k: "설치 위치", from: "확인할 방법이 없다", to: "패키지 관리자는 위치를 기록하지 않는다" },
  { k: "설치 이력", from: "확인할 방법이 없다", to: "설치 기록은 남지 않는다" },
  { k: "시간대", from: "시간대를 신경 쓰지 않는다", to: "시간대는 기본값 그대로 둔다" }
] },
{ track: "rust", fixes: [
  { k: "복제의 값과 대가", from: "실행 중에 반드시 오류가 난다", to: "복제는 컴파일 오류를 숨길 뿐 실행 중에 같은 문제로 패닉한다" },
  { k: "빌림 오류 읽기", from: "이 오류는 생기지 않는다", to: "벡터 대신 고정 크기 배열을 쓰면 이 오류는 사라진다" },
  { k: "원본보다 오래 살 수 없다", from: "방법밖에 남지 않기 때문이다", to: "함수 안에서 새로 만든 값은 어떤 형태로도 함수 밖으로 내보낼 수 없게 되어 있어서, 결과를 돌려주려면 인자로 받은 값을 그 자리에서 고쳐 넘겨 주는 길밖에 남지 않아서다" },
  { k: "원본보다 오래 살 수 없다", from: "허용하지 않기 때문이다", to: "반환 타입에 mut 이 빠져 있어서 컴파일러가 참조를 돌려주는 것을 허용하지 않는 탓이다" },
  { k: "String 과 &str", from: "안전하게 읽을 수 있기 때문이다", to: "String 이 낫다 — 소유권을 온전히 넘겨받아야 함수 안에서 안전하게 읽을 수 있어서다" },
  { k: "unwrap 을 쓸 자리", from: "편하게 써도 문제가 없다", to: "실패하면 미리 정해 둔 기본값을 돌려주기 때문에 안전하고, 어느 자리에서든 편하게 써도 된다" },
  { k: "빠뜨린 경우를 막는다", from: "그 자리에서 오류가 난다", to: "match 는 처음 컴파일할 때 항목 개수를 세어 두기 때문에, 나중에 개수가 달라지면 그 자리에서 컴파일러가 거부한다" }
] },
{ track: "ai", fixes: [
  { k: "ai-serve-p99", from: "값이 변하지 않기 때문이다", to: "백분위수는 트래픽이 아무리 늘어도 값이 변하지 않아서다" },
  { k: "ai-rag-vs-keyword", from: "오타에 강해서 임베딩이 필요 없다", to: "BM25 는 오타에 강해서 임베딩을 대신한다" },
  { k: "ai-rag-cross-enc", from: "1차 검색이 필요 없게 된다", to: "임베딩 검색을 완전히 대체해 1차 검색 단계를 없앤다" },
  { k: "ai-mlops-data-ver", from: "버전 관리를 하지 않아도 된다", to: "데이터는 버전 관리 대상에서 뺀다" },
  { k: "ai-eval-peeking", from: "따로 하지 않아도 된다", to: "표본 크기 계산을 건너뛸 수 있다" }
] },
{ track: "cloud", fixes: [
  { k: "TLS 종료", from: "인증서 만료를 신경 쓰지 않아도 된다", to: "인증서 만료가 자동으로 연장된다" },
  { k: "약정의 위험", from: "다른 리전의 자원을 쓸 수 없게 된다", to: "약정을 걸면 다른 리전의 자원은 잠긴다" },
  { k: "모듈의 이점", from: "상태 파일을 따로 두지 않아도 된다", to: "모듈이 자원을 기억하므로 상태 파일이 사라진다" },
  { k: "리뷰의 값어치", from: "동시 적용이 생기지 않는다", to: "상태 파일 잠금이 필요 없어진다 — 한 번에 한 변경만 리뷰를 통과하므로 동시 적용이 막힌다" },
  { k: "카디널리티", from: "그래프로 그릴 수 없게 된다", to: "라벨 값이 문자열이라 지표가 그래프에 그려지지 않는다" }
] },
{ track: "git", fixes: [
  { k: "역사의 위치", from: "시스템 오류가 난다", to: "OS 가 보호 폴더라며 삭제를 거부한다" },
  { k: "분산", from: "잠금을 걸어 주기 때문", to: "여러 사람이 같은 파일을 동시에 편집하도록 잠금을 걸어 주어서" },
  { k: "잘못 단 태그", from: "태그를 잃고 찾을 수 없게 된다", to: "옮긴 순간 원래 태그가 사라진다 — 같은 이름은 하나만 있을 수 있으므로 옛 커밋은 태그를 잃고 찾기 어려워진다" },
  { k: "바이너리 문제", from: "태그를 달 수 없게 된다", to: "해당 커밋에는 태그가 붙지 않는다" },
  { k: "상황 처방 5", from: "옮길 수 없기 때문이다", to: "파일을 전부 복사해 둔 다음 새 브랜치를 만들어 처음부터 똑같이 다시 만든다 — 커밋은 브랜치에 묶여 있어 옮기지 못하는 탓이다" }
] },
{ track: "net", fixes: [
  { k: "cs2-net-slow-start", from: "상한을 따로 두지 않아도 된다", to: "손실이 한 번도 일어나지 않는다면 상한 없이 계속해서 두 배씩 늘어나기만 하고 멈추지 않는다. 그래서 상한은 손실이 대신한다" },
  { k: "cs2-net-timewait", from: "보낼 데이터가 남아 있기 때문이다", to: "상대에게 아직 보낼 데이터가 남아 있어서다" },
  { k: "일관해싱", from: "노드를 늘려도 재배치가 필요 없다", to: "모든 키가 같은 노드로 향하게 되어 노드들 사이의 부하가 완벽하게 균등해진다는 점 그래서 노드를 늘려도 재배치는 일어나지 않는다" },
  { k: "연결풀", from: "닫히지 않으므로 아무 문제가 없다", to: "풀에 들어간 연결은 닫히지 않으므로 그대로 재사용하면 된다" },
  { k: "netsk-twrecycle", from: "정말 아무 문제가 없다", to: "tcp_tw_reuse 에 새로 붙은 다른 이름일 뿐이다. 그래서 그대로 켜 두어도 동작은 같다" }
] },
{ track: "numpy", fixes: [
  { k: "뷰", from: "오류가 난다", to: "읽기 전용 뷰라 대입이 거부된다" },
  { k: "조건 대입", from: "오류가 난다", to: "불리언 배열은 인덱스로 쓸 수 없어 거부된다" },
  { k: "속도", from: "오류가 난다", to: "타입이 맞지 않아 실행이 멈춘다" },
  { k: "빠른 이유", from: "병렬로 돌려 주기 때문", to: "파이썬 for 문을 여러 스레드로 자동 변환해 병렬로 돌려 주어서" },
  { k: "오버플로 대응", from: "애초에 신경 쓸 필요가 없다", to: "NumPy 가 연산할 때 dtype 을 알아서 더 넓은 쪽으로 자동 승격해 주기 때문에 그래서 오버플로는 애초에 일어나지 않는다" }
] },
{ track: "stat", fixes: [
  { k: "CLT 효용", from: "정규분포로 변환해 주기 때문", to: "데이터를 정규분포로 변환해 주어서" },
  { k: "CLT 효용", from: "이상치를 자동으로 제거해 주기 때문", to: "이상치를 자동으로 제거해 주어서" },
  { k: "CLT 효용", from: "정확히 알아낼 수 있게 해 주기 때문", to: "표본을 따로 뽑지 않고도 모집단의 평균값을 바로 그대로 정확히 알아낼 수 있게 해 주어서" },
  { k: "확률 오해", from: "그 사이의 값은 쓸 수 없기 때문이다", to: "확률이라는 값은 애초에 0 또는 1 두 가지만 가질 수 있고 그 사이의 값은 쓰지 못하는 탓이다" },
  { k: "α 사전 결정", from: "보고할 수조차 없게 된다", to: "신뢰구간 자체를 계산할 수 없게 되어 그래서 결과를 보고하는 길이 막힌다" },
  { k: "무작위 배정", from: "작게 나오도록 만들어 주기 때문", to: "무작위 배정을 하면 그래서 p 값이 더 작게 나오도록 만들어 주어서" },
  { k: "무작위 배정", from: "표본크기를 줄여 주기 때문", to: "표본크기를 줄여 주어서" },
  { k: "피킹", from: "영향을 주지 않으므로 아무 문제가 없다", to: "매일 결과를 들여다보는 것은 통계적으로 영향을 주지 않으므로 그대로 해도 된다" }
] },
{ track: "dl", fixes: [
  { k: "dl-bp-zero", from: "문제도 생기지 않는다", to: "기울기가 스텝마다 자동으로 0 이 되어 학습은 그대로 진행된다" },
  { k: "dl-rnn-lstm", from: "기울기가 살아 있기 때문이다", to: "활성함수로 ReLU 만 써서 기울기가 살아 있어서다" },
  { k: "dl-rnn-parallel", from: "한 번에 올릴 수 없기 때문이다", to: "학습해야 할 파라미터 수가 지나치게 많아 한 번에 올리지 못하는 탓이다" },
  { k: "dl-rnn-parallel", from: "GPU 연산을 지원하지 않기 때문이다", to: "GPU 연산을 지원하지 않아서다" },
  { k: "소실 원인", from: "줄어들도록 설계돼 있기 때문이다", to: "층을 하나 지날 때마다 학습률이 0 에 가깝게 줄어들도록 설계돼 있어서다" },
  { k: "소실 원인", from: "갱신할 값이 남지 않기 때문이다", to: "학습할 파라미터가 없어서 갱신할 값이 남지 않는 탓이다" },
  { k: "소실 원인", from: "차이가 생기지 않기 때문이다", to: "출력이 1 로 고정되어 층 사이에 차이가 나지 않는 까닭이다" }
] },
{ track: "ml", fixes: [
  { k: "정확도오해", from: "다른 지표는 볼 필요가 없다", to: "정확도는 완벽한 지표이므로 다른 지표는 참고용일 뿐이다" },
  { k: "고차원 범주", from: "트리 모델에서는 사용할 수 없게 된다", to: "트리 모델에서는 입력으로 거부된다" },
  { k: "사후 정보", from: "아무 문제가 없다", to: "정상적인 특성으로 쓰인다" },
  { k: "학습률", from: "따로 더 조정할 필요가 없다", to: "그 밖에 다른 것은 그대로 두어도 성능이 유지된다" }
] },
{ track: "code", fixes: [
  { k: "반례가 작아진다", from: "다시 재현되지 않기 때문이다", to: "줄이지 않으면 반례가 제대로 다시 재현되지 않아서다" },
  { k: "무엇을 검사하는 게 아닌가", from: "별도 측정이 필요 없다", to: "성능이 충분히 빠르다는 것까지 함께 보장해 주므로 별도 측정은 생략된다" }
] },
{ track: "mleval", fixes: [
  { k: "LOOCV", from: "연속값 오차를 계산할 수 없기 때문이다", to: "회귀 문제에는 쓸 수 없다 — 샘플 하나로는 연속값 오차를 계산하지 못하는 탓이다" },
  { k: "층화 목적", from: "클래스 비율은 신경 쓰지 않는다", to: "데이터를 무작위로 섞기만 하고 클래스 비율은 그대로 둔다" }
] }
];
