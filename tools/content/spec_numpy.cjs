module.exports={
  track:"numpy", guide:"🔢", xp:80,
  unit:"실행형 실전 — NumPy 의 의미를 직접 만든다",
  source:"./exec_npd.cjs", slice:[0,6],
  lessons:[
    { t:"모양·스트라이드·인덱스", n:3,
      th:{ sum:"NumPy 가 빠른 이유는 연산이 아니라 **데이터를 옮기지 않는 것**에 있다 — 그 열쇠가 모양과 스트라이드다.",
        body:[
          {h:"뒤에서부터 맞춘다", t:"브로드캐스팅 규칙은 '뒤에서부터 맞추고, 같거나 1' 이 전부다. 뒤에서부터인 이유는 관례가 아니라 **메모리 배치** 때문이다 — C 순서에서 마지막 축이 연속으로 놓이므로, 뒤쪽 축을 맞추면 복사 없이 stride 를 0 으로 두어 '같은 값을 반복해 읽는' 것으로 확장할 수 있다. 그래서 `(3,5)` 에 `(5,)` 는 더할 수 있고 `(3,)` 은 못 더하며, `v[:, None]` 로 모양을 `(3,1)` 로 만들어야 한다."},
          {h:"스트라이드가 설명하는 것", t:"스트라이드는 '그 축으로 한 칸 움직이려면 몇 바이트를 건너뛰는가' 다. 이것을 알면 **전치가 공짜인 이유**(스트라이드 순서만 뒤집는다)와 **reshape 이 때로 복사하는 이유**(기존 스트라이드로 표현할 수 없는 모양이 있다)가 한꺼번에 설명된다. 다차원 인덱스를 평탄 인덱스로 바꾸는 계산은 자릿수 올림과 같다 — `flat = flat * s + i`."}],
        code:{ c:"# (n,1) + (n,) 은 오류가 아니라 (n,n) 이 된다\nassert pred.shape == y.shape", cap:"모양을 단정하지 않으면 엉뚱한 손실이 나온다" },
        key:["브로드캐스팅은 뒤에서부터 맞춘다","크기 1 인 축은 stride 0 으로 확장된다","C 순서는 마지막 축이 가장 촘촘하다","평탄 인덱스는 자릿수 올림 계산"] } },
    { t:"축·정렬·dtype", n:3,
      th:{ sum:"결과가 '그럴듯한 숫자' 라서 조용히 틀리는 셋 — **축 방향, 반환값의 정체, 고정 폭 정수**.",
        body:[
          {h:"axis 는 없어지는 축", t:"`axis` 를 헷갈리지 않는 유일한 방법은 '**없어지는 축**' 으로 읽는 것이다 — `(2,3)` 에 `axis=0` 이면 결과가 `(3,)` 이다. 축을 뒤바꾼 버그는 **정사각 행렬에서는 개수가 같아 통과**하므로, 테스트는 반드시 행과 열 수가 다른 입력을 써야 한다. `argsort` 는 값이 아니라 **인덱스**를 돌려주는데, 값을 돌려주는 실수는 결과가 정상적인 정렬 리스트라서 '인덱스로 써 보는' 검증 없이는 통과한다."},
          {h:"파이썬 정수와 dtype", t:"파이썬 정수는 무한 정밀도라 넘치지 않지만 NumPy 배열은 고정 폭이라 넘치고, 기본 동작은 예외가 아니라 **조용한 래핑**이다. `uint8` 이미지에 밝기를 더하면 밝은 픽셀이 검게 변하고, `int16` 카운터는 32767 을 넘는 순간 음수가 된다 — 둘 다 오류 없이 진행된다. 방어는 계산 전 `astype` 으로 넓히거나 `np.seterr` 로 오버플로를 경고·예외로 올리는 것이다."}],
        code:{ c:"# int8 래핑: 0 기준으로 옮겨 감싸고 되돌린다\n((x + 128) % 256) - 128", cap:"파이썬 % 는 음수에도 양수를 준다" },
        key:["axis 는 없어지는 축","비정사각 입력으로 축을 테스트한다","argsort 는 인덱스를 준다","NumPy 의 오버플로는 조용하다"] } },
  ]
};
