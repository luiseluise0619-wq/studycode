/* java 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "java", fixes: [
  { k: "특징", ex0: "모든 코드는 클래스 안에", ex: "Java 는 모든 코드가 클래스 안에 들어간다. main 조차 어떤 클래스의 메서드다. 함수는 클래스 밖에 홀로 둘 수 없고, 모듈과 패키지는 클래스를 묶는 더 큰 단위이지 코드가 놓이는 최소 단위가 아니다." },
  { k: "출력", ex0: "System.out.println() 으로 출력", ex: "System.out.println() 이 한 줄을 찍고 줄을 바꾼다. print() 만 홀로 쓰면 Java 에는 그런 전역 함수가 없어 컴파일이 안 되고, cout 은 C++ 의 것이다. System.err.printf() 는 오류 출력 스트림에 서식을 붙여 찍는 것이라 보통 출력용이 아니다." },
  { k: "문장 끝", ex0: "세미콜론(;) 으로 문장을", ex: "Java 는 문장이 세미콜론(;)으로 끝난다. 빠뜨리면 컴파일 오류가 난다. 마침표는 멤버 접근(obj.x), 콜론은 case 나 삼항 연산자에 쓰이고, 닫는 중괄호는 블록을 닫을 뿐 문장을 끝내지 않는다." },
  { k: "빈칸", ex0: "int x = 5; 형태로 선언", ex: "정수 변수는 int x = 5; 처럼 타입을 먼저 쓰고 이름과 초기값을 적는다. Java 는 정적 타입 언어라 타입 없이 x = 5; 만 쓰면 선언이 되지 않고, Integer 는 객체 포장 타입이라 기본형 정수에는 int 를 쓴다." },
  { k: "조건", ex0: "if / else 로 분기", ex: "조건 분기는 if 로 하고 else 로 나머지 경우를 받는다. case 는 switch 안에서만 쓰는 가지 표시이고, cond 와 when 은 Java 키워드가 아니다(when 은 Kotlin 의 것이다)." },
  { k: "자료형", ex0: "int 는 정수형", ex: "정수는 int 에 담는다(32비트). integer 는 소문자라 타입이 아니고 대문자 Integer 는 포장 클래스다. num 은 존재하지 않는 타입이고 str 도 Java 에는 없다(문자열은 String 이다)." },
  { k: "문자열", ex0: "대문자로 시작하는 String", ex: "문자열 타입은 대문자로 시작하는 String 이다. 클래스이기 때문에 클래스 이름 규칙대로 대문자로 쓴다. 소문자 string 은 정의된 타입이 없어 컴파일 오류가 나고, str 과 text 는 Python 이나 다른 언어의 이름이다." },
  { k: "for 출력", ex0: "i는 0, 1, 2일 때 실행되고", ex: "i 가 0 에서 시작해 i < 3 인 동안 돌므로 0, 1, 2 가 찍히고 i 가 3 이 되는 순간 조건이 거짓이라 멈춘다. 1 2 는 시작값을 1 로 본 것이고, 0 1 2 3 은 i <= 3 일 때의 결과다." },
  { k: "증감식", ex0: "i++는 i를 1 증가시키는", ex: "for 문의 세 번째 자리에는 반복마다 실행할 증감식을 둔다. i++ 가 가장 흔하고 i += 1 이나 i = i + 1 도 같은 뜻이다. 셋 다 i 를 1 늘리므로 어느 것을 써도 되지만 관례는 i++ 다." },
  { k: "for 구조", ex0: "for(초기화; 조건; 증감)", ex: "for 는 for(초기화; 조건; 증감) 순서다. 처음 한 번 초기화하고, 매 반복 전에 조건을 검사하고, 본문이 끝난 뒤 증감을 실행한다. 순서를 바꾸면 문법은 통과할 수 있어도 조건 자리에 증감식이 들어가 엉뚱하게 동작한다." },
  { k: "반복 횟수", ex0: "i가 1부터 5까지 조건을 만족하므로", ex: "i 가 1 에서 시작해 i <= 5 인 동안 돌므로 1, 2, 3, 4, 5 의 다섯 번 실행된다. 4 는 i < 5 로 잘못 본 것이고, 6 은 0 부터 셌을 때의 수다. 시작값과 부등호의 등호 유무를 같이 봐야 한다." },
  { k: "while 조건", ex0: "while은 조건이 참(true)인 동안", ex: "while 은 조건을 먼저 검사해 참(true)인 동안만 본문을 반복한다. 처음부터 거짓이면 한 번도 실행되지 않는다. 조건이 false 인 동안 도는 것이 아니고, 조건과 무관하게 무한히 도는 것은 while(true) 처럼 조건을 항상 참으로 둔 특수한 경우뿐이다." },
  { k: "do-while", ex0: "do-while은 본문을 먼저 실행한 뒤", ex: "do-while 은 본문을 먼저 실행하고 나서 조건을 검사하므로 조건이 처음부터 거짓이어도 최소 한 번은 돈다. while 과 for 는 조건을 먼저 보기 때문에 처음 조건이 거짓이면 한 번도 실행하지 않는다. 메뉴 입력처럼 '일단 한 번은 해야 하는' 일에 쓴다." },
  { k: "무한 반복", ex0: "i++가 없어 조건이 계속 참이므로", ex: "본문에 i++ 같은 증감이 없어 i 값이 그대로이므로 조건이 영원히 참이고 무한 반복된다. 문법은 맞아서 컴파일 오류는 아니고, 조건이 처음부터 참이라 한 번만 실행되거나 아무것도 출력하지 않는 경우도 아니다." },
  { k: "배열 선언", ex0: "int[] 이름 = new int[크기];", ex: "배열은 int[] arr = new int[5]; 처럼 타입 뒤에 [] 를 붙이고 new 로 크기를 정한다. int arr = new int[5]; 는 배열을 int 변수에 넣으려는 것이라 타입 오류다. int[5] arr 처럼 타입 자리에 크기를 쓰거나 array 라는 키워드를 쓰는 문법은 Java 에 없다." },
  { k: "첫 인덱스", ex0: "자바 배열의 인덱스는 0부터", ex: "Java 배열은 인덱스가 0 부터 시작한다. 길이가 5 이면 arr[0] 부터 arr[4] 까지이고, arr[5] 를 읽으면 ArrayIndexOutOfBoundsException 이 난다. 1 부터 세는 언어(Lua, MATLAB)와 헷갈리기 쉬운 부분이다." },
  { k: "반환형", ex0: "메서드 이름 앞의 int가 반환형", ex: "메서드 선언에서 이름 바로 앞에 오는 타입이 반환형이다. int add(int a, int b) 에서 int 가 반환형, add 는 이름, a + b 는 return 문의 값이다. void 는 돌려줄 값이 없을 때 쓰는 반환형이므로 여기엔 해당하지 않는다." },
  { k: "return", ex0: "return 키워드로 값을 반환", ex: "메서드가 값을 돌려줄 때는 return 값; 을 쓴다. 이 문장이 실행되면 메서드가 그 자리에서 끝나고 호출한 쪽에 값이 전달된다. 반환형이 void 가 아닌 메서드는 모든 경로에서 return 을 만나야 컴파일된다." },
  { k: "매개변수", ex0: "호출 시 전달된 값을 받는 변수를", ex: "메서드 정의의 괄호 안에서 값을 받는 변수가 매개변수(parameter)다. 호출할 때 넘기는 실제 값은 인자(argument)라고 구별한다. 반환형은 돌려주는 값의 타입이고, 생성자는 객체를 초기화하는 특별한 메서드이며, 지역 상수는 final 로 고정한 변수다." },
  { k: "void 의미", ex0: "void는 돌려줄 값이 없음을", ex: "반환형 void 는 돌려줄 값이 없다는 뜻이다. 실행은 정상적으로 되고 return 을 값 없이 쓰거나 아예 생략할 수 있다. 정수를 돌려주려면 int 를 써야 하고, void 메서드에서 return 5; 처럼 값을 돌려주면 컴파일 오류다." },
  { k: "매개변수 수", ex0: "name, age, height 로 3개", ex: "괄호 안에 쉼표로 구분된 선언이 name, age, height 세 개이므로 매개변수는 3 개다. 매개변수 수는 타입이 아니라 이름의 개수로 센다. 같은 타입이 여러 개여도 각각 따로 세고, 반환형은 개수에 들어가지 않는다." },
  { k: "조건 반환", ex0: "5는 0보다 크므로 return 1이", ex: "test(5) 에서 5 > 0 이 참이므로 첫 분기의 return 1 이 실행되고 메서드는 거기서 끝난다. 뒤에 오는 return -1 이나 0 은 조건이 거짓일 때만 도달한다. 인자 5 를 그대로 돌려주는 코드는 없다." },
  { k: "단독 return", ex0: "값 없는 return;은 그 지점에서", ex: "void 메서드 안의 return; 은 값 없이 메서드를 그 자리에서 끝내는 문장이다. 반환형이 void 이므로 값을 안 쓰는 것이 오히려 맞고, 컴파일 오류는 return 5; 처럼 값을 붙였을 때 난다. 0 을 돌려주지도 반복하지도 않는다." },
  { k: "키워드", ex0: "static 키워드를 붙이면 클래스 소속", ex: "static 을 붙인 메서드는 객체가 아니라 클래스에 속하므로 Math.max() 처럼 클래스이름.메서드() 로 바로 부른다. static 이 없는 인스턴스 메서드는 new 로 객체를 만든 뒤 그 객체를 통해서만 호출할 수 있다." },
  { k: "static 특징", ex0: "static 메서드는 클래스이름.메서드()", ex: "static 메서드는 클래스에 속해 객체 없이 클래스이름.메서드() 로 호출한다. 한 번만 실행된다는 제한은 없고, 값을 반환할 수도 있다. 객체를 먼저 만들어야 하는 것은 static 이 없는 인스턴스 메서드다." },
  { k: "static 호출", ex0: "6 * 2 = 12", ex: "twice 는 인자를 두 배로 돌려주는 static 메서드이므로 Util.twice(6) 은 6 * 2 = 12 다. 객체를 만들지 않고 클래스 이름으로 바로 부른 것이 static 의 쓰임이다. 36 은 제곱, 6 은 그대로 돌려줬을 때의 값이다." },
  { k: "main static", ex0: "프로그램 시작점이라 객체 없이", ex: "main 은 프로그램의 시작점이라 아직 어떤 객체도 없는 상태에서 JVM 이 불러야 한다. 그래서 객체 없이 호출 가능한 static 으로 둔다. main 을 여러 개 만들거나 반복 실행하려고 붙이는 것이 아니고, 반환형은 void 라 정수를 돌려주지도 않는다." },
  { k: "객체 문장", ex0: "타입 이름 = new 생성자();", ex: "객체는 Dog d = new Dog(); 처럼 타입, 변수 이름, new, 생성자 호출 순서로 만든다. new 없이 Dog() 만 쓰면 그런 메서드가 없다는 오류가 나고, new Dog; 처럼 괄호를 빼면 생성자 호출이 아니라 문법 오류다. new Dog d; 는 순서가 뒤집혔다." },
  { k: "설계도", ex0: "class 키워드로 클래스를 정의", ex: "필드와 메서드를 묶어 객체의 설계도를 만드는 키워드는 class 다. class Dog { ... } 처럼 쓰고, 이 설계도로 new Dog() 를 해야 실제 객체가 생긴다. interface 는 구현 없는 약속이고 struct 는 Java 에 없다." },
  { k: "객체 생성", ex0: "new 키워드로 객체(인스턴스)를", ex: "객체는 new 키워드로 만든다. new Dog() 가 메모리를 잡고 생성자를 호출해 인스턴스를 돌려준다. class 는 설계도를 정의하는 키워드이고, create 와 object 는 Java 키워드가 아니다(Object 는 모든 클래스의 조상 클래스 이름이다)." },
  { k: "관계", ex0: "클래스는 설계도이고 객체는 그 설계도로", ex: "클래스가 설계도이고 객체는 그 설계도로 찍어낸 실체다. 설계도 하나로 객체를 여러 개 만들 수 있고, 각 객체는 자기만의 필드 값을 가진다. 순서가 반대인 보기는 역할을 뒤집은 것이고, 클래스가 객체보다 먼저 있어야 하므로 '나중에 만들어지는' 것도 틀렸다." },
  { k: "생성자", ex0: "생성자는 new로 객체를 만들 때 자동", ex: "생성자는 new 로 객체를 만들 때 자동으로 호출되어 필드를 초기화한다. 이름은 클래스 이름과 같아야 하고 반환형을 적지 않는다. 반환형을 int 로 적으면 생성자가 아니라 일반 메서드가 되고, 객체를 만들 때마다 반드시 한 번 호출된다." },
  { k: "키워드", ex0: "new 키워드가 생성자를 호출하고", ex: "new 가 메모리를 확보한 뒤 생성자를 호출해 객체를 만든다. 생성자는 직접 이름으로 부를 수 없고 new 를 통해서만 실행된다. class 는 설계도를 정의할 뿐 객체를 만들지 않고, this 는 이미 만들어진 객체 자신을 가리킬 때 쓴다." },
  { k: "생성자 이름", ex0: "생성자 이름은 클래스 이름과 같아야", ex: "생성자는 이름이 클래스 이름과 똑같아야 한다. 그래야 컴파일러가 생성자로 인식한다. main 은 시작점 메서드 이름이고 init 은 Python 의 __init__ 같은 관습이지 Java 규칙이 아니다. 변수 이름과는 아무 관계가 없다." },
  { k: "반환형 함정", ex0: "생성자는 반환형이 없다. 적으면", ex: "생성자에 반환형을 적으면 컴파일러가 그것을 생성자가 아니라 클래스와 이름이 같은 평범한 메서드로 본다. 그러면 new 할 때 호출되지 않아 초기화가 빠진다. 오류는 나지 않아 찾기 어렵고, 객체가 두 개 생기거나 void 로 바뀌는 일은 없다." },
  { k: "필드", ex0: "클래스에 속한 이런 변수를 필드", ex: "클래스 안에 선언되어 객체의 상태를 담는 변수를 필드(멤버 변수, 인스턴스 변수)라고 한다. 메서드 안에 선언한 지역 변수와 달리 객체가 살아 있는 동안 값이 유지되고, 초기화하지 않아도 0 이나 null 같은 기본값을 가진다." },
  { k: "기본값", ex0: "int 필드의 기본값은 0이다", ex: "int 필드는 초기화하지 않아도 자동으로 0 이 들어간다. null 은 참조 타입(String 등)의 기본값이지 int 에는 못 들어가고, 오류가 나는 것은 초기화 없이 쓴 지역 변수의 경우다. size 는 변수 이름이지 값이 아니다." },
  { k: "this 의미", ex0: "this는 현재 실행 중인 객체 자신을", ex: "this 는 지금 메서드를 실행하고 있는 객체 자신을 가리킨다. 매개변수와 필드 이름이 같을 때 this.name = name 처럼 필드 쪽을 구별하는 데 쓴다. 부모 클래스는 super 로 가리키고, static 변수는 객체가 아니라 클래스에 속하므로 this 로 가리킬 대상이 아니다." },
  { k: "리스트접근", ex0: "get(1)은 두 번째 요소", ex: "ArrayList 의 get(i) 는 인덱스 i 의 요소를 돌려주고 인덱스는 0 부터 센다. 따라서 get(1) 은 두 번째 요소 \"B\" 다. get(0) 이었다면 \"A\", get(2) 였다면 \"C\" 가 나온다. 배열의 arr[1] 과 같은 규칙이다." },
  { k: "맵값", ex0: "key \"age\"에 저장된 값 20", ex: "HashMap 의 get(key) 는 그 키에 저장된 값을 돌려준다. \"age\" 키에 20 을 put 했으므로 get(\"age\") 는 20 이다. 없는 키를 조회하면 null 이 나오므로 키 문자열의 철자가 정확해야 한다." },
  { k: "셋크기", ex0: "Set은 중복을 저장하지 않으므로 2가 한 번만", ex: "HashSet 은 같은 값을 두 번 넣어도 하나만 유지한다. 1, 2, 2, 3 을 add 하면 2 가 한 번만 들어가 size() 는 3 이다. 순서대로 전부 담는 ArrayList 였다면 4 가 나왔을 것이다. 중복 제거가 필요할 때 Set 을 쓰는 이유다." },
  { k: "항상실행", ex0: "finally 블록은 예외 발생 여부와 무관하게", ex: "try 뒤의 finally 블록은 예외가 나든 안 나든, catch 에서 잡히든 아니든 항상 실행된다. 파일이나 연결을 닫는 정리 코드를 두는 자리다. catch 는 예외가 났을 때만, try 본문은 예외가 나는 지점까지만 실행된다." },
  { k: "접근제어", ex0: "private은 해당 클래스 내부에서만", ex: "private 은 선언한 클래스 안에서만 접근할 수 있게 막는다. public 은 어디서나 열려 있고, protected 는 같은 패키지와 자식 클래스까지 허용한다. 아무것도 안 붙인 default 는 같은 패키지 안에서 열린다. 필드를 감추고 메서드로만 다루게 하는 캡슐화에 private 을 쓴다." }
] };
