/* security 트랙 짧은 해설 다시 쓰기 — 왜 맞는지와 나머지는 왜 아닌지를 같이 적는다 */
module.exports = { track: "security", fixes: [
  { k: "개념", ex0: "HTTPS 는 암호화된 안전한 통신", ex: "HTTPS 는 HTTP 위에 TLS 암호화를 얹어 중간에서 내용을 엿보거나 바꾸지 못하게 한다. HTTP/2 는 속도를 높인 새 버전일 뿐 그 자체로 암호화를 뜻하지 않고, TELNET 은 평문으로 원격 접속하는 오래된 프로토콜이며, SMTP 는 메일을 보내는 프로토콜이다." },
  { k: "개념", ex0: "SQL 인젝션은 입력으로 DB 쿼리를 조작", ex: "입력값에 SQL 조각을 섞어 서버가 만드는 쿼리를 바꿔 버리는 공격이 SQL 인젝션이다. 무차별 대입은 비밀번호를 하나씩 시도하는 것, 세션 고정은 공격자가 정한 세션 ID 를 쓰게 만드는 것, 캐시 오염은 잘못된 응답을 캐시에 심는 것이라 데이터베이스 명령과 관계없다. 방어는 매개변수화 쿼리다." },
  { k: "개념", ex0: "XSS 는 악성 JS 를 페이지에 주입", ex: "페이지에 악성 스크립트를 심어 그 페이지를 보는 다른 사용자의 브라우저에서 실행시키는 공격이 XSS 다. 무차별 대입은 비밀번호 추측, DDoS 는 트래픽으로 서비스를 마비시키는 것, 피싱은 가짜 사이트로 유인해 정보를 빼내는 것이라 스크립트 주입과 다르다. 출력 시 이스케이프가 기본 방어다." },
  { k: "개념", ex0: "비밀번호는 평문이 아니라 해시(+솔트)로 저장", ex: "비밀번호는 원래 값으로 되돌릴 수 없는 해시로 저장한다. 로그인 때 입력값을 같은 방식으로 해시해 비교한다. AES 같은 대칭키 암호화는 키만 있으면 복호화되므로 유출 시 위험하고, base64 는 누구나 되돌릴 수 있는 인코딩이며, gzip 은 압축이라 보안과 무관하다. bcrypt 나 argon2 같은 느린 해시에 솔트를 더한다." },
  { k: "판단", ex0: "짧고 단순한 비밀번호는 쉽게 뚫린다", ex: "'1234' 같은 단순한 비밀번호는 사전 공격이나 무차별 대입에 순식간에 뚫린다. 길고 복잡하게 만들기, 사이트마다 다르게 쓰기, 비밀번호 관리자에 맡기기는 모두 권장 습관이다. 특히 사이트마다 다르게 써야 한 곳에서 유출되어도 다른 계정으로 번지지 않는다." },
  { k: "개념", ex0: "2FA 는 비밀번호가 유출돼도 추가 방어선", ex: "비밀번호에 더해 문자나 인증 앱 코드를 요구하는 것이 2 단계 인증(2FA)이다. 비밀번호가 새어도 두 번째 요소가 없으면 못 들어온다. 자동 로그인과 기기 기억하기는 편의 기능이라 오히려 확인을 줄이고, 캡차는 사람인지 봇인지 가릴 뿐 계정 주인을 확인하지 않는다." },
  { k: "실무", ex0: "비밀키는 환경변수 등으로 분리하고 코드에 넣지", ex: "API 키와 비밀번호는 저장소에 올리는 순간 공개된 것과 같고 커밋 기록에 남아 지우기도 어렵다. 환경변수나 비밀 관리 도구로 분리하고 .gitignore 로 막는다. 함수 이름, 주석과 설계 문서, 테스트 코드는 공개되어도 계정이나 서버를 뚫는 데 쓰이지 않는다." },
  { k: "개념", ex0: "OWASP Top 10 은 대표적인 웹 보안 위험 10가지 목록", ex: "OWASP Top 10 은 웹 애플리케이션에서 가장 흔하고 치명적인 보안 위험 열 가지를 정리한 목록이다. 인젝션, 접근 제어 실패, 암호화 실패 같은 항목이 들어 있다. 해킹 도구나 프로그래밍 언어 순위, 암호 알고리즘 표준을 정리한 문서가 아니다." },
  { k: "개념", ex0: "CSRF 는 사용자의 인증 상태를 도용해 요청을 위조", ex: "로그인된 사용자의 브라우저가 자동으로 붙이는 쿠키를 이용해 사용자가 모르는 요청을 보내게 하는 것이 CSRF 다. XSS 는 스크립트를 주입하는 것, SSRF 는 서버가 내부로 요청을 보내게 하는 것, SQLi 는 데이터베이스 쿼리를 조작하는 것이다. 방어는 CSRF 토큰과 SameSite 쿠키다." },
  { k: "개념", ex0: "SSRF 는 서버를 이용해 내부망을 공격", ex: "서버가 사용자가 준 URL 로 요청을 보내는 기능을 악용해 내부망 주소나 메타데이터 서비스에 접근하게 만드는 것이 SSRF 다. XSS 는 브라우저에서 스크립트가 실행되는 것, DoS 는 서비스를 마비시키는 것, 피싱은 사용자를 속이는 것이라 서버가 대신 요청하는 구조가 아니다. 방어는 허용 목록과 내부 주소 차단이다." },
  { k: "개념", ex0: "의존성은 최신으로 유지해 알려진 취약점을 막아야", ex: "알려진 취약점이 있는 옛 라이브러리를 그대로 쓰면 공격 방법이 공개된 상태에서 문을 열어 두는 셈이라 OWASP 의 '취약하고 오래된 컴포넌트' 항목에 해당한다. 빌드 크기나 속도, 호환성의 문제로 축소해서 보면 안 된다. 의존성 스캐너로 주기적으로 확인하고 업데이트한다." },
  { k: "개념", ex0: "SQL·명령어 인젝션 등이 이 범주에 속한다", ex: "검증 없는 입력을 쿼리나 명령에 그대로 이어 붙여 생기는 취약점을 인젝션이라 부르고 SQL, OS 명령, LDAP 인젝션이 모두 여기 속한다. 접근 제어 실패는 권한 검사가 빠진 것, 암호 실패는 약한 암호화나 평문 저장, 설정 오류는 기본 계정이나 열린 디버그 모드 같은 문제라 범주가 다르다." },
  { k: "원칙", ex0: "'절대 입력을 믿지 마라' — 검증·이스케이프가 기본", ex: "사용자 입력은 어디서 왔든 신뢰하지 않고 검증한 뒤 쓰는 것이 원칙이다. 형식만 맞으면 통과시키면 형식을 지킨 악성 값을 막지 못하고, 일단 저장한 뒤 검사하면 저장 과정에서 이미 공격이 성립하며, 우리 앱에서 온 것처럼 보여도 요청은 얼마든지 위조할 수 있다." },
  { k: "개념", ex0: "기록(로그)이 없으면 침입을 알아차릴 수 없다", ex: "공격을 탐지하고 사고가 난 뒤 경위를 추적하려면 로그와 모니터링이 있어야 한다. OWASP 도 로깅과 모니터링 부족을 별도 항목으로 둔다. 웹폰트, 고해상도 이미지, 애니메이션은 화면 품질에 관한 것이라 보안 사고를 알아차리는 데 아무 도움이 안 된다." },
  { k: "원칙", ex0: "최소 권한 원칙으로 피해 범위를 줄인다", ex: "계정이나 프로그램에 꼭 필요한 권한만 주는 것이 최소 권한 원칙이다. 뚫리더라도 그 권한 안에서만 피해가 난다. 전체 권한은 반대로 피해를 최대로 키우고, 권한 공유는 누가 무엇을 했는지 추적을 어렵게 하며, 무권한 실행은 아무 일도 할 수 없어 현실적인 원칙이 아니다." },
  { k: "개념", ex0: "솔트를 더하면 같은 비밀번호도 다른 해시", ex: "해시할 때 비밀번호에 섞는 무작위 값이 솔트다. 사용자마다 다른 솔트를 쓰면 같은 비밀번호라도 해시가 달라져 미리 계산한 표(레인보우 테이블)가 무용지물이 된다. 토큰은 인증 증표, 쿠키는 브라우저 저장소, 논스는 재전송 방지용 일회성 값이라 해시에 섞는 값이 아니다." },
  { k: "개념", ex0: "TLS 로 데이터를 암호화해 도청·변조를 막는다", ex: "HTTPS 의 암호화는 TLS(전송 계층 보안) 프로토콜이 담당한다. 서버 인증서를 확인하고 세션 키를 교환해 이후 통신을 암호화한다. HTML 은 문서 구조, JSON 은 데이터 형식, SMTP 는 메일 전송 프로토콜이라 암호화 기반이 아니다. SSL 은 TLS 의 옛 이름이다." },
  { k: "도구", ex0: "의존성·코드 스캐너로 알려진 취약점을 조기에 발견", ex: "알려진 취약점을 자동으로 찾아 주는 것은 취약점 스캐너다. npm audit 처럼 의존성 버전을 취약점 데이터베이스와 대조하거나 코드를 정적 분석한다. 브라우저는 페이지를 보여 줄 뿐이고 계산기와 압축 도구는 보안과 무관하다. 스캐너 결과를 CI 에 물려 두면 배포 전에 걸러진다." },
  { k: "습관", ex0: "정기적인 업데이트가 알려진 취약점을 막는 첫걸음", ex: "라이브러리 취약점은 대부분 새 버전에서 고쳐지므로 최신 버전으로 업데이트하는 것이 가장 기본적인 방어다. 버전을 절대 바꾸지 않으면 공개된 취약점을 안고 가고, 안 쓰는 것만 지우는 것은 쓰고 있는 취약한 것을 남기며, 경고를 무시하면 알려 주는 정보를 버리는 셈이다." }
] };
