/* java 트랙 — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다.
   node fix_opts.cjs --apply opt_java.cjs */
module.exports = { track: "java", fixes: [
  { k: "java2-test-mockwhat", o: [
    "테스트 속도와 격리를 위해 모든 협력 객체를 모킹해야 한다 — 실제 구현이 하나라도 섞이면 단위 테스트가 아니다",
    "값 객체(record·DTO)는 모킹하는 것이 원칙이다 — 생성자 인자를 일일이 채우는 것보다 스텁으로 반환값만 정하는 편이 짧다",
    "DB·HTTP 같은 외부 경계만 모킹하고, 값 객체나 내가 소유하지 않은 서드파티 타입은 실제 구현을 쓰는 편이 안전하다",
    "List 나 Map 같은 컬렉션도 반드시 모킹해야 한다 — 실제 컬렉션을 쓰면 테스트가 자료구조 구현에 의존하게 된다" ] },
  { k: "ZGC", o: [
    "compaction 을 하지 않아 오래 돌면 단편화가 심해지고, 큰 객체를 할당할 때 연속 공간을 찾지 못해 결국 Full GC 로 떨어지는 컬렉터다",
    "세대 구분 없이 전체 힙을 한 번에 mark-sweep 하며 정지시간이 힙 크기에 그대로 비례하므로 수십 GB 힙에는 맞지 않는 컬렉터다",
    "colored pointers 와 load barrier 로 대부분의 작업을 concurrent 하게 수행해 정지시간을 힙 크기와 무관하게 짧게 유지한다",
    "single-threaded 컬렉터라 코어를 하나만 쓰므로 소규모 힙과 임베디드 환경에만 적합하고 서버에서는 쓰지 않는다" ] },
  { k: "java2-build-cp-vs-mp", o: [
    "클래스패스가 항상 더 빠르다 — 모듈 경로는 접근 검사를 실행 시점마다 하므로 클래스 로딩이 느려진다",
    "클래스패스는 사실상 모든 public 타입이 접근 가능하지만, 모듈 경로는 exports 된 패키지만 접근할 수 있다",
    "모듈 경로는 jar 를 지원하지 않아 jmod 로 다시 포장해야 하고, 기존 라이브러리는 그대로 올릴 수 없다",
    "모듈 경로에서는 리플렉션을 쓸 수 없다는 점이 가장 큰 차이다 — 프레임워크가 내부 필드에 접근하는 길이 막힌다" ] },
  { k: "java3-poly-composition", o: [
    "합성은 어떤 경우에도 상속보다 실행 속도가 빠르다 — 가상 메서드 호출이 사라져 JIT 이 인라인하기 쉽기 때문이다",
    "상속은 부모 구현의 변경이 자식을 깨뜨리는 강한 결합을 만들기 때문에, is-a 관계가 아니면 합성이 낫다",
    "자바에서 상속은 권장되지 않아 문법적으로 제한되어 있다 — 한 단계까지만 extends 할 수 있고 그 이상은 컴파일 오류다",
    "합성을 쓰면 다형성을 쓸 수 없게 되어 코드가 단순해진다 — 인터페이스 없이 구체 클래스만 다루면 되기 때문이다" ] },
  { k: "java-cc-increment", o: [
    "항상 0 이다 — volatile 변수는 스레드마다 사본을 가지므로 메인 스레드의 값은 바뀌지 않는다",
    "10000보다 작을 수 있다 — count++ 은 읽기-수정-쓰기 3단계라 갱신이 유실된다",
    "컴파일 에러가 발생한다 — volatile 필드에는 ++ 처럼 읽고 쓰는 복합 연산자를 쓸 수 없다",
    "어떤 경우에도 정확히 10000 이 된다 — volatile 이 모든 스레드에 최신 값을 보이게 하므로 유실이 없다" ] },
  { k: "java2-time-instant", o: [
    "사용자 지역의 달력 날짜 — 연·월·일만 담고 시각과 타임존은 없어 생일이나 기념일을 표현하는 데 쓴다",
    "날짜를 문자열로 바꿀 때 쓰는 형식 정의 — 패턴 문자를 해석해 LocalDate 와 문자열을 서로 바꿔 준다",
    "UTC 에포크(1970-01-01T00:00:00Z) 기준의 절대 시점, 즉 타임라인 위의 한 점",
    "타임존 규칙의 모음 — 지역마다 언제 서머타임이 시작되고 끝나는지, 오프셋이 얼마인지를 담고 있다" ] },
  { k: "java3-cmp-consistency", o: [
    "일치가 강제되지는 않지만, 어기면 TreeSet·TreeMap 같은 정렬 컬렉션이 equals 기반 컬렉션과 다르게 동작한다",
    "일치가 강제되며 어기면 컴파일 에러가 난다 — Comparable 을 구현한 클래스는 equals 도 같은 기준으로 재정의해야 한다",
    "둘 사이에는 아무런 관계가 없어 신경 쓸 필요가 없다 — 정렬은 compareTo, 동등성은 equals 가 각각 따로 맡는다",
    "compareTo 가 0 이면 hashCode 도 같아야 한다는 규칙이 있다 — 정렬 컬렉션이 해시로 버킷을 나누기 때문이다" ] },
  { k: "java3-ver-add", o: [
    "형식에는 필드를 추가할 수 없고 새 API 를 만들어야 한다 — 이미 배포된 스키마는 어떤 방향으로도 바꾸지 않는 것이 원칙이다",
    "필드 추가는 메이저 버전을 올려야 하는 파괴적 변경이다 — 구버전 소비자는 새 필드를 만나면 파싱에 실패한다",
    "선택적 필드 추가는 대체로 호환되며, 구버전 소비자가 모르는 필드를 무시하도록 관대하게 읽는 것이 전제다",
    "필드를 추가하면 구버전 소비자가 반드시 깨진다 — 직렬화 형식은 필드 순서와 개수를 함께 검사하기 때문이다" ] },
  { k: "java2-build-nosuchmethod", o: [
    "메서드 이름에 오타가 있어 컴파일러가 놓치고 넘어간 것이다 — 리플렉션으로 부르는 메서드는 컴파일 시점에 검사되지 않는다",
    "컴파일에 사용된 버전과 런타임 클래스패스에 올라온 버전이 서로 달라, 있던 메서드가 사라졌거나 시그니처가 바뀌었다",
    "소스 파일 인코딩이 잘못되어 메서드 이름의 한글이나 특수문자가 클래스 파일에 다르게 기록됐다",
    "JVM 힙이 부족해 클래스를 끝까지 로딩하지 못했고, 로딩되지 않은 메서드를 부르는 순간 이 오류가 난다" ] },
  { k: "java3-fw-module", o: [
    "setAccessible 은 자바 17 에서 제거되어 더 이상 부를 수 없고, 리플렉션으로 private 멤버에 닿는 길이 아예 사라졌다",
    "모듈 시스템이나 보안 설정이 열어 주지 않은 내부 패키지에 쓰면 InaccessibleObjectException 으로 막힐 수 있다",
    "setAccessible(true) 는 어떤 상황에서도 성공한다 — 모듈 설정과는 무관하며 private 은 컴파일러만 지키는 규칙이다",
    "리플렉션은 어떤 경우에도 모듈 경계를 넘지 못한다 — opens 를 선언해도 다른 모듈의 private 필드는 읽을 수 없다" ] },
  { k: "java2-time-YYYY", o: [
    "소문자 yyyy 와 완전히 동일하다 — 패턴 문자는 대소문자를 구분하지 않으므로 어느 쪽을 써도 같은 연도가 나온다",
    "주 기반 연도(week-based year)라서 연말·연초에 1년이 어긋날 수 있다",
    "두 자리 연도를 뜻한다 — 대문자는 짧은 형식이라 2026 이 26 으로 찍히고, 네 자리를 원하면 소문자를 써야 한다",
    "언제나 0 을 채워 네 자리로 만든다는 뜻이다 — 소문자 yyyy 는 자릿수를 보장하지 않아 연도 999 가 세 자리로 찍힌다" ] },
  { k: "false sharing", o: [
    "GC 가 아직 살아 있는 젊은 객체를 잘못된 세대로 승격시켜, 이후 마이너 GC 가 그 객체를 회수하지 못하는 현상이다",
    "JIT 컴파일러가 서로 다른 스레드의 지역 변수를 같은 레지스터에 배치해 값이 섞이는 최적화 버그다",
    "서로 다른 변수들이 같은 캐시 라인에 있어 한 스레드의 쓰기가 다른 코어의 캐시 라인을 무효화시켜 성능이 저하되는 현상이다",
    "여러 스레드가 같은 변수를 동기화 없이 동시에 수정해 갱신이 유실되는 논리적 데이터 경쟁 현상이다" ] },
  { k: "java3-json-longid", o: [
    "id 는 규약상 반드시 숫자로 보내야 한다 — 문자열로 보내면 클라이언트가 정렬이나 비교를 할 수 없어 API 가 깨진다",
    "long 은 JSON 으로 표현할 수 없다 — JSON 숫자는 32비트까지만 규정되어 있어 그보다 큰 값은 파싱 오류가 난다",
    "자바스크립트 클라이언트는 숫자를 double 로 다뤄 2의 53승을 넘는 값의 정밀도가 깨질 수 있어, id 를 문자열로 보내기도 한다",
    "JSON 숫자는 규격상 임의 정밀도라 어떤 값도 안전하다 — 정밀도 손실은 소비자가 아니라 규격이 막아 주므로 걱정할 필요가 없다" ] },
  { k: "java3-ver-format", o: [
    "두 방식의 호환성 특성은 사실상 동일하다 — 어느 쪽이든 필드를 더하거나 빼면 구버전 소비자는 같은 방식으로 깨진다",
    "JSON 으로는 자바 객체의 상태를 저장할 수 없으므로, 저장 용도로는 자바 직렬화만 쓸 수 있고 JSON 은 통신 전용이다",
    "자바 직렬화가 언어 간 상호 운용성이 가장 좋다 — 바이너리 형식이라 파싱이 빠르고 어떤 언어든 읽는 라이브러리가 마련되어 있다",
    "자바 직렬화는 클래스 구조에 강하게 묶여 언어 간 교환과 스키마 진화에 취약하므로, 외부 통신에는 JSON 같은 명시적 포맷이 낫다" ] },
  { k: "java-st-parallel-when", o: [
    "각 원소 처리가 서로 독립적인 경우 — 원소끼리 상태를 공유하지 않으면 스레드 사이의 동기화 비용이 병렬 이득을 잠식한다",
    "원소 수가 수백만 개이고 계산이 CPU 집약적인 경우 — 큰 데이터를 나누고 합치는 비용이 계산 비용을 넘어선다",
    "소스가 배열이나 ArrayList 인 경우 — 연속 메모리를 여러 스레드가 나눠 읽으면 캐시 라인이 충돌해 오히려 느려진다",
    "원소 수가 적고 원소당 계산이 가벼우며, 소스가 LinkedList 처럼 분할이 어려운 경우" ] },
  { k: "java2-str-sbequals", o: [
    "컴파일 오류가 난다 — StringBuilder 는 equals 를 final 로 막아 두어 다른 StringBuilder 와 비교하는 코드를 컴파일러가 거부한다",
    "길이만 같으면 true 다 — StringBuilder 의 equals 는 내부 버퍼 크기만 비교하도록 구현되어 있어 내용은 보지 않는다",
    "두 객체의 내용이 같으면 true 를 반환하므로 toString() 없이 그대로 비교해도 된다",
    "StringBuilder 는 equals 를 오버라이드하지 않아 참조 비교가 되므로 false 다. toString() 후 비교해야 한다" ] },
  { k: "java3-enum-ordinal", o: [
    "ordinal 은 JVM 이 실행할 때마다 무작위로 정하는 값이라, 같은 상수라도 다음 실행에서는 다른 번호가 나올 수 있기 때문",
    "ordinal 은 상수의 문자열 이름을 반환하므로 용량이 크고, 이름이 길수록 저장 공간과 전송량이 함께 늘어나기 때문",
    "ordinal 은 선언 순서에 따른 번호라, 상수 순서를 바꾸거나 중간에 추가하면 저장된 값의 의미가 통째로 달라진다",
    "ordinal 은 상수마다 개발자가 직접 지정해야 하는 값이라 실수로 겹치게 적기 쉽고, 겹치면 어느 상수인지 알 수 없기 때문" ] },
  { k: "java-gen-heap", o: [
    "같은 객체를 여러 스레드가 동기화 없이 동시에 수정해 필드 값이 서로 어긋나는 상태로, 제네릭 컬렉션에서 특히 자주 생긴다",
    "매개변수화된 타입의 변수가 실제로는 다른 타입 인자의 객체를 가리키게 되어, 나중에 꺼낼 때 ClassCastException 이 나는 상태",
    "힙 메모리가 가득 차 OutOfMemoryError 가 나는 상태를 말하며, 제네릭 컬렉션이 원시 타입을 박싱해 담을 때 특히 빨리 찬다",
    "GC 가 회수하지 못하는 객체가 쌓이는 상태로, 제네릭 캐시가 강한 참조로 값을 붙들고 있을 때 생기는 누수의 다른 이름이다" ] },
  { k: "java-st-optional", o: [
    "둘은 완전히 동일하다 — orElseGet 은 람다를 받는 편의 문법일 뿐이고 평가 시점은 orElse 와 같다",
    "orElseGet 은 값이 없을 때 예외를 던진다 — 기본값 대신 예외 공급자를 받는 orElseThrow 의 옛 이름이다",
    "orElse 는 값이 없을 때만 평가되고 orElseGet 은 언제나 평가된다 — 그래서 비싼 기본값에는 orElse 를 써야 한다",
    "orElse 의 인자는 값이 있어도 항상 평가되고, orElseGet 의 Supplier 는 값이 없을 때만 실행된다" ] },
  /* 2차 — 20~44 */
  { k: "java2-fn-capture", o: [
    "반드시 원시 타입이어야 한다 — 객체 참조는 람다가 끝난 뒤 바뀔 수 있어 캡처할 수 없다",
    "final 이거나 사실상 final(초기화 후 재할당이 없어야)이어야 한다",
    "아무 제약이 없다 — 람다는 클로저라 바깥 변수를 참조로 붙들고 있어 나중에 바뀐 값도 그대로 본다",
    "static 으로 선언되어 있어야 한다 — 람다는 인스턴스와 무관하게 만들어지므로 정적 저장소만 볼 수 있다" ] },
  { k: "java3-hc-contract", o: [
    "equals 로 같은 두 객체는 반드시 같은 해시코드를 가져야 하지만, 그 역은 성립하지 않아도 된다",
    "같은 객체의 해시코드는 JVM 을 재시작해도 동일해야 한다 — 그래야 직렬화해 저장한 HashMap 을 다시 읽을 수 있다",
    "서로 다른 객체는 서로 다른 해시코드를 가져야 한다 — 충돌이 나면 HashMap 이 두 키를 같은 것으로 취급한다",
    "해시코드가 같으면 두 객체는 equals 로도 같아야 한다 — HashMap 은 해시가 같으면 equals 를 부르지 않는다" ] },
  { k: "java3-fw-startup", o: [
    "시작은 빠르지만 캐싱이 불가능해 호출 비용이 계속 커진다 — Method 객체는 매번 새로 조회해야 하기 때문이다",
    "리플렉션은 애플리케이션 시작 시간에 영향을 주지 않으며, 스캔이든 프록시 생성이든 첫 요청이 올 때까지 미뤄진다",
    "스캔과 프록시 생성 같은 리플렉션 작업이 시작 시점에 몰려 시작이 느려지는 대신, 이후 반복 호출은 캐싱으로 완화된다",
    "리플렉션은 JIT 컴파일 자체를 완전히 막는다 — 리플렉션으로 한 번이라도 부른 메서드는 끝까지 인터프리터에서만 돈다" ] },
  { k: "java2-pat-deconstruct", o: [
    "레코드 분해(deconstruction) 패턴으로, 타입 검사와 컴포넌트 추출을 동시에 한다",
    "제네릭 와일드카드로, Rect 의 타입 인자를 double 로 추론해 그 자리에서 고정한다",
    "람다 파라미터 선언으로, Rect 를 받는 함수형 인터페이스를 case 안에서 곧바로 구현하는 문법이다",
    "가변 인자 선언으로, Rect 생성자에 넘길 수 있는 인자 개수를 두 개로 제한한다" ] },
  { k: "java3-fw-di", o: [
    "클래스패스를 스캔해 애노테이션이 붙은 타입을 찾고, 리플렉션으로 생성자를 호출해 의존성을 주입한다",
    "런타임에 소스 코드를 다시 컴파일해 애노테이션이 지시한 생성 코드를 끼워 넣고, 그 결과를 클래스로더에 올린다",
    "모든 객체 생성 코드를 개발자가 컴파일 시점에 직접 하드코딩해 두고, 컨테이너는 그 코드를 순서대로 부를 뿐이다",
    "JVM 내부 API 를 수정해 new 연산자를 가로채고, 애노테이션이 붙은 타입이 생성될 때마다 의존성을 끼워 넣는다" ] },
  { k: "java3-json-dto", o: [
    "직렬화 대상 필드는 라이브러리가 자동으로 안전하게 걸러 준다 — password 같은 이름의 민감 필드는 기본 설정에서 빠진다",
    "DTO 는 매핑 비용과 클래스 수만 늘리는 성능 저하 요인이므로 쓰지 않는 것이 원칙이고, 엔티티를 그대로 내보낸다",
    "엔티티를 그대로 쓰면 API 가 더 안정적이 된다 — 저장 구조와 응답이 같으니 둘이 어긋날 일이 없기 때문이다",
    "내부 필드 변경이 곧 API 파괴가 되고 비밀번호 같은 필드가 새어 나갈 수 있어, 응답 전용 DTO 로 분리하는 것이 좋다" ] },
  { k: "java3-perf-contains", o: [
    "banned 를 정렬해 두면 contains 가 자동으로 이진 탐색으로 바뀌므로 정렬만 해 두면 탐색 비용이 O(log m) 으로 준다",
    "List.contains 는 O(n) 선형 탐색이라 전체가 약 O(n*m) 이다 — banned 를 HashSet 으로 바꾸면 조회가 평균 O(1) 이다",
    "ArrayList 의 contains 는 인덱스 기반이라 O(1) 이고, 문제는 바깥 for 문이 두 번 도는 것이므로 두 루프를 하나로 합치면 된다",
    "for 문을 while 문으로 바꾸고 인덱스 변수를 밖으로 빼면 JIT 이 루프를 펼칠 수 있어 비교 횟수가 크게 준다" ] },
  { k: "java3-bench-dce", o: [
    "JIT 는 죽은 코드를 제거하지 않으므로 무관하다 — 자바는 부수 효과가 없어도 모든 문장을 실행하도록 규격이 정한다",
    "결과를 쓰지 않아도 측정값은 언제나 정확하다 — 벤치마크 프레임워크가 결과를 대신 소비해 주기 때문이다",
    "JIT 가 계산 자체를 제거해(죽은 코드 제거) 말도 안 되게 빠른 수치가 나온다",
    "결과를 쓰지 않으면 컴파일 에러가 난다 — 자바는 사용하지 않는 지역 변수를 오류로 취급한다" ] },
  { k: "java3-refl-slow", o: [
    "멤버 조회·접근 검사·인자 배열 박싱이 매 호출에 끼어들고, 호출 대상이 런타임에 결정되어 JIT 가 인라인하기 어렵기 때문이다",
    "리플렉션 코드는 어떤 경우에도 인터프리터 모드로만 실행되기 때문이다 — invoke 로 부른 메서드는 JIT 컴파일 대상에서 빠진다",
    "리플렉션 호출은 별도 스레드에서 실행되기 때문이다 — Method.invoke 는 호출을 큐에 넣고 결과가 돌아올 때까지 기다린다",
    "리플렉션 호출은 클래스 정보를 매번 디스크에서 다시 읽기 때문이다 — Class 객체는 캐시되지 않고 호출마다 로딩된다" ] },
  { k: "java3-bench-order", o: [
    "먼저 프로파일러로 실제 병목을 찾고, 알고리즘·자료구조 수준의 복잡도를 개선한 뒤 미세 최적화로 내려간다",
    "모든 코드를 골고루 조금씩 최적화한다 — 병목은 한 곳이 아니라 흩어져 있으므로 전체를 조금씩 줄이는 편이 안전하다",
    "감으로 짚은 부분을 먼저 고친다 — 경험 있는 개발자의 직감이 프로파일러보다 빠르고, 측정은 시간만 잡아먹는다",
    "어떤 경우에도 자료구조보다 문자열 연결을 먼저 고친다 — 자바 프로그램의 병목은 거의 언제나 String 덧셈이다" ] },
  { k: "java2-null-requirenonnull", o: [
    "성능이 빨라지기 때문 — null 검사를 한 번 해 두면 JIT 이 그 뒤의 null 검사를 모두 생략할 수 있다",
    "컴파일 시점에 null 을 검사해 주기 때문 — 이 호출이 있으면 컴파일러가 null 을 넘기는 호출부를 오류로 잡는다",
    "잘못된 null 을 그 자리에서 NPE 로 드러내 원인 지점을 호출 시점으로 앞당기기 때문",
    "인자가 null 이면 자동으로 기본값으로 바꿔 주기 때문 — 두 번째 인자 \"arg\" 가 그 기본값으로 쓰인다" ] },
  { k: "java2-test-aaa", o: [
    "JUnit 이 세 구간을 각각 다른 스레드에서 실행하기 때문 — 준비를 병렬로 돌려 전체 테스트 시간을 줄인다",
    "실행 시간이 세 배 빨라지기 때문 — 구간이 나뉘어 있어야 JUnit 이 준비 결과를 캐시해 다음 테스트에 재사용한다",
    "그렇게 쓰지 않으면 컴파일되지 않기 때문 — @Test 메서드는 세 구간을 주석으로 표시해야 JUnit 이 인식한다",
    "무엇을 준비했고 무엇을 검증하는지가 구조로 드러나 실패 시 원인을 빨리 찾을 수 있기 때문" ] },
  { k: "java-cc-submit", o: [
    "execute 만 예외를 삼키고 submit 은 즉시 예외를 출력한다 — 그래서 디버깅할 때는 submit 을 쓰는 편이 낫다",
    "submit 은 Future 를 반환하고 작업 중 발생한 예외를 Future 에 담아 두므로 get 을 호출하지 않으면 예외가 드러나지 않는다",
    "둘 다 Future 를 반환한다 — execute 는 Future 를 버리는 편의 메서드일 뿐 예외 처리 방식은 같다",
    "execute 는 Callable 만 받고 submit 은 Runnable 만 받는다는 점이 결정적인 차이다 — 반환값이 있으면 execute 를 쓴다" ] },
  { k: "java-jvm-systemgc", o: [
    "호출 즉시 모든 쓰레기 객체가 회수된다 — 그래서 큰 객체를 버린 직후에 불러 두면 메모리를 확실히 되찾을 수 있다",
    "GC 수행을 '요청'할 뿐 즉시 실행이 보장되지 않으며, 대개 Full GC 를 유발해 운영 환경에서는 피한다",
    "호출해도 아무 일도 일어나지 않도록 규격에 명시되어 있다 — 명시적 호출은 항상 무시되므로 있어도 해가 없다",
    "Young 영역만 정리한다 — 마이너 GC 를 강제로 한 번 돌리는 메서드라 정지 시간이 짧아 자주 불러도 된다" ] },
  { k: "java3-json-money", o: [
    "금액은 float 으로 저장하는 것이 표준이다 — 소수점 두 자리까지만 쓰므로 단정밀도로도 정확히 표현된다",
    "JSON 은 소수를 문자열로만 표현할 수 있어 선택의 여지가 없다 — 숫자 타입은 정수만 허용한다",
    "double 대신 BigDecimal 이나 최소 단위 정수(예: 원 단위 long)로 표현한다",
    "double 이 BigDecimal 보다 항상 더 정확하다 — 하드웨어가 직접 계산하므로 소프트웨어 구현보다 오차가 적다" ] },
  { k: "java3-perf-treemap", o: [
    "TreeMap 은 해시 충돌이 많을 때만 쓰는 대체품이며, 정렬이나 범위 조회 용도로는 쓰지 않는다",
    "TreeMap 이 단순 키 조회에서도 항상 더 빠르다 — 해시 계산이 없고 트리 탐색은 캐시에 잘 맞기 때문이다",
    "정렬 순서나 범위 조회가 필요하면 TreeMap(O(log n)), 단순 조회만 필요하면 HashMap(평균 O(1))이 낫다",
    "HashMap 은 키를 정렬해 보관한다 — 순회하면 키 순서대로 나오므로 정렬이 필요할 때도 HashMap 이면 충분하다" ] },
  { k: "java-gen-extends-add", o: [
    "add 와 get 모두 컴파일 에러다 — 와일드카드 타입은 읽기와 쓰기 어느 쪽에도 구체 타입을 확정할 수 없다",
    "Number n = list.get(0); 은 되지만 list.add(1); 은 컴파일 에러다",
    "add 와 get 모두 자유롭게 가능하다 — Integer 는 Number 의 하위 타입이므로 넣는 것도 꺼내는 것도 안전하다",
    "list.add(1); 은 되지만 get 은 Object 만 반환한다 — extends 는 쓰기 쪽 상한이라 읽을 때는 타입 정보가 사라진다" ] },
  { k: "java2-build-provided", o: [
    "테스트에서만 사용되고 배포에는 포함되지 않는다 — JUnit 이나 Mockito 같은 테스트 라이브러리에 쓰는 스코프다",
    "런타임에만 필요하고 컴파일에는 쓰이지 않는다 — JDBC 드라이버처럼 코드에서 직접 참조하지 않는 구현체에 쓴다",
    "컴파일에는 필요하지만 실행 시에는 컨테이너·런타임이 제공하므로 패키징에서 제외한다",
    "로컬 파일 시스템 경로로 직접 지정해 둔 의존성을 뜻한다 — 저장소에 없는 jar 를 systemPath 로 가리킬 때 쓴다" ] },
  { k: "java2-fn-anno", o: [
    "이 애너테이션이 없으면 람다로 구현할 수 없게 막힌다 — 컴파일러는 표시된 인터페이스만 람다 대상으로 인정한다",
    "인터페이스를 자동으로 구현해 준다 — 추상 메서드 하나를 람다 본문으로 채운 익명 클래스를 컴파일러가 만든다",
    "런타임에 성능 최적화를 활성화한다 — 표시된 인터페이스의 람다는 invokedynamic 대신 직접 호출로 컴파일된다",
    "필수는 아니고, 추상 메서드가 하나가 아니면 컴파일 오류로 잡아 주는 안전장치" ] },
  { k: "java3-eq-override-annot", o: [
    "매개변수 타입을 Object 가 아닌 것으로 잘못 적었을 때 컴파일 단계에서 잡아 준다",
    "hashCode 를 컴파일러가 자동으로 생성해 준다 — equals 를 재정의했다는 표시를 보고 짝이 되는 hashCode 를 만든다",
    "런타임 비교 성능이 개선된다 — 재정의된 메서드임을 JVM 이 알아 가상 호출 대신 직접 호출로 연결한다",
    "equals 호출을 생략해도 되게 만들어 준다 — == 비교가 자동으로 equals 로 바뀌어 참조 비교 실수를 막는다" ] },
  { k: "java3-cmp-contract-violation", o: [
    "정렬 결과만 조금 이상해질 뿐 예외는 발생하지 않는다 — 라이브러리가 모순을 무시하고 끝까지 정렬하며, 계약 위반은 경고 한 줄 없이 조용히 넘어간다",
    "정렬이 모순을 감지해 'Comparison method violates its general contract' 예외를 던질 수 있다",
    "정렬이 무한 루프에 빠진다 — 비교 결과가 앞뒤로 어긋나면 원소가 두 자리 사이를 계속 오가며 병합 단계가 영원히 끝나지 않고 CPU 만 태운다",
    "StackOverflowError 가 발생한다 — 병합 정렬이 재귀로 구현되어 있어 모순된 비교가 분할을 멈추지 못하게 하고 재귀 깊이를 끝없이 늘리기 때문이다" ] },
  { k: "java2-build-bom", o: [
    "의존성을 자동으로 최신 버전으로 올려 주기 위해서 쓴다 — 빌드할 때마다 저장소에서 가장 새 버전을 골라 온다",
    "빌드 캐시를 활성화한다 — 한 번 받은 의존성을 모듈 사이에 공유해 다시 내려받지 않게 한다",
    "여러 모듈·의존성의 버전을 한곳에서 선언해 프로젝트 전체에서 일관되게 고정한다",
    "소스 코드를 난독화한다 — 배포용 jar 의 클래스 이름과 필드 이름을 짧은 기호로 바꿔 역컴파일을 어렵게 한다" ] },
  { k: "java2-build-lifecycle", o: [
    "package 단계만 단독으로 실행된다 — 앞 단계가 필요하면 mvn compile package 처럼 명시해서 이어 붙여야 한다",
    "validate·compile·test 등 package 보다 앞선 라이프사이클 단계가 모두 실행된 뒤 패키징된다",
    "테스트는 항상 건너뛴다 — package 는 산출물을 만드는 단계라 검증 단계인 test 와는 별개의 흐름이다",
    "install 과 deploy 단계까지 함께 실행된다 — 라이프사이클 전체가 한 번에 돌아 로컬 저장소와 원격 저장소에 올라간다" ] },
  { k: "java3-def-array", o: [
    "배열도 불변이므로 필드를 그대로 반환해도 안전하다 — 길이가 고정되어 있어 호출자가 원소를 더하거나 뺄 수 없다",
    "List.of 로 만든 리스트는 add 를 허용한다 — 초기 원소만 정해 줄 뿐 그 뒤로는 보통의 ArrayList 처럼 자란다",
    "배열은 언제나 가변이므로 외부에 넘길 때 clone() 이나 복사본을 반환해야 한다",
    "Arrays.asList 로 만든 리스트는 완전히 불변이다 — 원본 배열을 복사해 감싸므로 set 을 불러도 예외가 난다" ] },
  { k: "java-cc-chm-lock", o: [
    "맵 전체가 아니라 버킷(빈) 단위로 잠그고 빈 버킷 삽입은 CAS 로 처리한다",
    "어떤 연산에서든 맵 전체에 하나의 락을 걸어 직렬화한다 — Hashtable 과 같은 방식이지만 락 구현이 더 가볍다",
    "읽기에도 항상 배타적 락을 건다 — 그래야 다른 스레드가 쓰는 도중의 값을 읽는 일이 없기 때문이다",
    "락을 쓰지 않고 순수 CAS 만 쓴다 — 모든 갱신을 원자적 교체로 처리하므로 어떤 경우에도 스레드가 멈추지 않는다" ] },
  /* 3차 — 45~52 */
  { k: "java3-enum-emap-vs-hash", o: [
    "해시 계산과 충돌 처리가 없고, 순회 순서가 예측 가능하며 메모리도 더 적게 쓴다",
    "성능과 메모리 모두 차이가 없고 취향 문제다 — EnumMap 은 내부적으로 HashMap 을 감싼 얇은 껍데기일 뿐이다",
    "EnumMap 이 더 많은 메모리를 써서 캐시 효율이 좋아진다 — 상수마다 큰 슬롯을 미리 잡아 두기 때문이다",
    "EnumMap 은 null 키를 허용해 더 유연하다 — '값 없음' 을 뜻하는 항목을 따로 상수로 만들지 않아도 된다" ] },
  { k: "java-jvm-stw", o: [
    "Minor GC 가 Full GC 보다 더 오래 걸리며 Stop-The-World 시간도 훨씬 길다 — 젊은 객체가 훨씬 많기 때문이다",
    "Minor GC 는 Old 영역만 정리한다 — 오래된 객체부터 걷어내야 승격할 자리가 생기기 때문이다",
    "Full GC 는 Stop-The-World 가 없다 — 전체를 훑는 작업은 백그라운드 스레드가 애플리케이션과 나란히 돌린다",
    "Minor GC 는 Young 영역만 정리해 대개 짧고, Full GC 는 Old 를 포함한 전체를 훑어 Stop-The-World 가 길다" ] },
  { k: "java2-str-tb-indent", o: [
    "항상 4칸씩 제거된다 — 텍스트 블록은 한 단계 들여쓰기를 전제하므로 그만큼을 일괄로 걷어낸다",
    "부수적 들여쓰기는 제거되지 않고 입력한 그대로 남는다 — 공백도 문자열의 일부이므로 컴파일러가 손대지 않는다",
    "비어 있지 않은 모든 내용 줄과 닫는 \"\"\" 줄 중 가장 작은 들여쓰기만큼 모든 줄에서 제거된다",
    "여는 \"\"\" 줄의 들여쓰기만큼 제거된다 — 블록이 시작된 열을 기준으로 삼아 그 왼쪽 공백을 잘라 낸다" ] },
  { k: "java2-fn-primitive", o: [
    "Integer 박싱·언박싱과 그에 따른 객체 생성 비용을 피할 수 있기 때문",
    "제네릭보다 문법이 짧아 보기 좋기 때문 — Predicate<Integer> 보다 IntPredicate 가 읽기 쉽고 꺾쇠가 없다",
    "스레드 안전성을 보장하기 때문 — 원시 값은 불변이라 여러 스레드가 같은 람다를 불러도 상태가 섞이지 않는다",
    "람다만 쓸 수 있고 메서드 참조를 막을 수 있기 때문 — 원시 특화 인터페이스는 메서드 참조 대상이 되지 않는다" ] },
  { k: "java2-io-exists", o: [
    "그 경로에 파일이 확실히 존재하지 않는다고 단정할 수 있다 — 권한 문제는 예외로 따로 던지므로 false 와 섞이지 않는다",
    "그 경로는 디렉터리다 — exists 는 일반 파일만 검사하므로 디렉터리에는 false 를 돌려준다",
    "경로 문법이 잘못되었다 — 올바른 경로라면 존재 여부와 무관하게 true 가 나오고, false 는 파싱 실패를 뜻한다",
    "존재하지 않거나, 권한 등으로 확인 자체가 실패했을 수 있다. notExists(p) 도 false 일 수 있다" ] },
  { k: "java2-io-buffered", o: [
    "파일 내용을 자동으로 암호화해 주기 때문 — 버퍼에 담긴 블록을 디스크에 쓰기 전에 키로 감싼다",
    "예외를 자동으로 무시하기 때문 — 버퍼 안에서 난 IOException 은 삼켜지고 빈 바이트로 채워진다",
    "한 번에 블록 단위로 읽어 시스템 콜 횟수를 크게 줄이기 때문",
    "문자셋을 자동으로 감지하기 때문 — 첫 블록의 바이트 패턴을 보고 UTF-8 인지 EUC-KR 인지 정한다" ] },
  { k: "java2-test-paramtest", o: [
    "@ParameterizedTest 가 @Test 역할을 대신하므로 함께 붙이면 안 된다",
    "권장되는 사용법이며 두 방식으로 모두 실행되어 검증이 두 배가 된다 — 기본 실행 한 번과 인자별 실행이 모두 돈다",
    "파라미터 개수가 두 배가 된다 — @Test 가 인자 목록을 한 번 더 붙여 소스가 두 벌로 합쳐진다",
    "@Test 가 무시되어 아무 문제가 없다 — JUnit 5 는 더 구체적인 애노테이션을 우선하고 나머지는 조용히 버린다" ] },
  { k: "java-hm-untreeify", o: [
    "리사이즈로 버킷이 쪼개질 때 노드가 6개 이하로 줄면 리스트로 되돌린다",
    "한 번 트리가 되면 되돌아가지 않는다 — 트리 노드는 리스트 노드보다 크지만 다시 바꾸는 비용이 더 크기 때문이다",
    "노드가 7개 이하가 되는 즉시 매번 리스트로 되돌린다 — remove 가 불릴 때마다 개수를 세어 기준 아래면 바꾼다",
    "맵이 비워질 때만 되돌아간다 — clear 를 부르면 모든 버킷이 초기 상태의 빈 리스트로 재설정된다" ] },
] };
