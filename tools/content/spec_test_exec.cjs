module.exports = {
  track: "test", guide: "", xp: 80,
  unit: "직접 구현 — 테스트 도구가 속으로 하는 계산",
  source: "./test_exec.cjs",
  lessons: [
    { t: "판정과 비교 — toEqual 을 만들어 본다", n: 5,
      th: { sum: "`toBe` 로 충분하지 않은 순간이 곧 테스트 프레임워크가 필요한 순간이다. **무엇을 같다고 볼 것인가**를 정하는 일이 비교 함수의 전부다.",
        body: [
          { h: "구조 비교와 소수 비교", t: "`===` 는 객체를 **같은 자리인가**로 본다. 테스트가 원하는 것은 거의 언제나 내용 비교라 깊은 비교가 따로 필요하고, 그때 키를 정렬해야 `{a:1,b:2}` 와 `{b:2,a:1}` 이 같아진다 — `JSON.stringify` 로 때우면 여기서 조용히 틀린다. 소수는 또 다른 문제다. `0.1+0.2 !== 0.3` 은 2진 부동소수점의 성질이라, 절대 오차와 **값의 크기에 비례하는 상대 오차**를 함께 써야 큰 수에서도 무너지지 않는다." },
          { h: "실패했을 때 무엇을 보여 주는가", t: "실패 메시지의 값어치는 **스택 트레이스를 열지 않고 원인이 보이는가**로 정해진다. 값을 문자열로 그냥 붙이면 배열이 `1,2` 가 되고 객체가 `[object Object]` 가 되어 아무것도 알려 주지 못하므로, 형태가 보이게 적는다. 계약 확인에서는 **키의 존재**만 보는 것이 정석이다 — `in` 은 값이 `undefined` 여도 있다고 답하는데, JSON 응답에서는 '값이 null' 과 '키가 없음' 이 서로 다른 뜻이라 이 구분이 중요하다." }],
        code: { c: "const ka = Object.keys(a).sort();\nconst kb = Object.keys(b).sort();\nreturn ka.every((k, i) => k === kb[i] && deepEqual(a[k], b[k]));", cap: "키를 정렬해야 순서가 문제 되지 않는다" },
        key: ["=== 는 자리를, toEqual 은 내용을 본다", "소수는 상대 오차까지 함께", "실패 메시지는 형태가 보이게", "키의 존재와 값의 null 은 다르다"] } },

    { t: "무엇을 넣어 볼 것인가 — 경계 · 분할 · 축소", n: 5,
      th: { sum: "테스트의 어려움은 '어떻게 확인하나' 가 아니라 **'무엇을 넣어 보나'** 에 있다. 이 셋이 그 질문에 답하는 도구다.",
        body: [
          { h: "가장자리와 묶음", t: "버그는 가운데가 아니라 **가장자리**에 산다. `<` 를 `<=` 로 잘못 쓴 실수는 범위 한가운데 값으로는 절대 드러나지 않는다. 그래서 경계마다 **바로 아래·경계·바로 위** 세 값을 본다. 반대로 같은 취급을 받는 입력끼리는 묶어 대표 하나만 확인한다 — 20살·30살·40살을 각각 넣는 것은 같은 가지를 세 번 지날 뿐이다. 이때 `NaN` 은 어떤 부등호 비교도 거짓이라 **맨 앞에서 걷어 내지 않으면** 마지막 가지로 조용히 흘러간다." },
          { h: "줄여야 원인이 보인다", t: "무작위로 찾아낸 실패 입력은 크고 지저분해 그대로는 읽을 수 없다. **빼 보고 여전히 실패하면 그건 필요 없던 것**이라는 원리로 최소 형태까지 줄이는 것이 속성 기반 도구의 축소(shrink)다. 조건이 여럿 얽혔을 때는 결정 표를 만들어 **정해지지 않은 조합**을 드러낸다 — 다만 조건 20개면 100만 줄이라 전수 확인은 불가능하고, 결과가 같은 줄을 묶어 대표만 고른다." }],
        code: { c: "const next = cur.slice(0, i).concat(cur.slice(i + 1));\nif (fails(next)) cur = next;   // 필요 없던 원소\nelse i++;                      // 이건 있어야 실패한다", cap: "빼 보고 여전히 실패하면 지운다" },
        key: ["경계마다 아래·경계·위 세 값", "같은 묶음은 대표 하나만", "NaN 은 맨 앞에서 걷어 낸다", "재현을 줄이는 것이 원인 찾기보다 먼저"] } },

    { t: "테스트 더블 — 무엇에 결합되는가", n: 5,
      th: { sum: "더미·스텁·스파이·목·페이크는 이름 다툼이 아니라 **결합의 세기**로 갈린다. 강하게 못 박을수록 리팩터링에서 잘 깨진다.",
        body: [
          { h: "다섯 이름과 고르는 기준", t: "**더미**는 자리만 채우고, **스텁**은 정해진 답을 돌려주고, **스파이**는 호출을 기록하고, **목**은 '이렇게 불려야 한다' 는 기대를 걸어 검증하고, **페이크**는 실제로 동작하는 간이 구현이다. 결과로 확인할 수 있으면 스텁이나 페이크를 쓰고, 메일 발송·결제 요청처럼 **결과에 드러나지 않는 부수 효과**만 스파이나 목으로 본다. 순서가 계약이라면 부분 수열로 확인해 관계없는 호출이 끼어도 깨지지 않게 한다." },
          { h: "가짜 시계와 거짓 같은 값", t: "'하루가 지나면 잠긴다' 를 진짜 시간으로 확인하면 테스트가 하루 걸린다. 시계를 **인자로 받게** 만들면 원하는 만큼 감을 수 있다 — 코드가 `Date.now()` 를 직접 부르면 바꿔치기할 자리가 없다. 스텁 표를 찾을 때 `table[key] || fallback` 은 **`0`·`false`·`\"\"` 를 기본값으로 둔갑시키는** 대표적인 버그다. '할인 0%' 가 10% 가 되고 '알림 끔' 이 켬으로 되돌아간다. 존재 검사로 확인해야 한다." }],
        code: { c: "return Object.prototype.hasOwnProperty.call(table, key)\n  ? table[key]      // 0·false·null 도 그대로\n  : fallback;", cap: "|| 는 값이 아니라 참·거짓을 본다" },
        key: ["목이 많으면 리팩터링에 약하다", "결과로 볼 수 있으면 결과로 본다", "시계는 인자로 받는다", "|| 기본값은 0·false 를 삼킨다"] } },

    { t: "층과 커버리지 — 숫자 뒤의 뜻", n: 5,
      th: { sum: "커버리지는 **실행된 줄의 비율**일 뿐이고, 층은 **의존이 결정한다.** 이 두 문장을 알고 나면 숫자에 휘둘리지 않는다.",
        body: [
          { h: "퍼센트가 아니라 목록", t: "어서션이 하나도 없는 테스트도 커버리지는 올린다. 봐야 할 것은 85% 라는 숫자가 아니라 **어느 줄이 한 번도 실행되지 않았는가** 이고, 특히 예외 처리 구간이 비어 있다면 '실패하면 어떻게 되는지 아무도 모른다' 는 뜻이다. 전체가 0일 때 `0/0` 이 `NaN` 이 되고 그 `NaN` 이 게이트 비교에서 **조용히 거짓**이 되므로, 0으로 나누기는 언제나 먼저 막는다." },
          { h: "층은 의존이 정한다", t: "브라우저가 필요하면 이미 E2E 이고, DB 나 네트워크가 필요하면 통합이다. 그래서 '단위 테스트를 늘리자' 는 실제로는 **의존을 줄이자**는 말이다. 모양으로 보면 E2E 가 가장 많은 **아이스크림콘**은 느리고 잘 흔들려 신뢰를 잃고, 가운데가 빈 **모래시계**는 통합 지점의 버그를 E2E 에서야 만나 원인 찾기가 몇 배로 어려워진다. 다만 비율은 규칙이 아니라 출발점이고, **우리 시스템에서 어디서 사고가 났는지**가 언제나 더 강한 근거다." }],
        code: { c: "if (!total) return 100;   // NaN 게이트를 막는다\nreturn Math.round(covered / total * 100);", cap: "숫자보다 안 덮인 줄 목록" },
        key: ["커버리지는 실행 여부일 뿐", "덮이지 않은 줄이 진짜 정보", "층은 의존이 결정한다", "비율은 출발점, 장애 이력이 근거"] } },

    { t: "실행과 운영 — 꼬리 · 회귀 · 분배 · 흔들림", n: 4,
      th: { sum: "테스트를 **돌리는 쪽**의 계산이다. 얼마나 느린지, 나빠졌는지, 어떻게 나눌지, 무엇이 흔들리는지.",
        body: [
          { h: "꼬리를 보고 비율로 판정한다", t: "평균은 꼬리를 감춘다. 대부분 100ms 인데 5%가 3초면 평균은 245ms 로 멀쩡해 보이지만 스무 명 중 한 명이 3초를 기다린다. 하루 1억 건이면 p99 밖의 1%도 **100만 건**이다. CI 에서 성능을 볼 때는 기계 성능이 실행마다 달라 절대 시간 대신 **기준선 대비 비율**을 쓰고, 경계를 `>` 로 할지 `>=` 로 할지까지 정해 테스트로 못 박는다 — 그 한 글자 때문에 매일 아침 경보가 울리는 팀이 실제로 있다." },
          { h: "나누기와 흔들림", t: "병렬로 쪼갤 때 아무렇게나 나누면 가장 오래 걸리는 샤드가 전체 시간을 정한다. **긴 것부터 가장 한가한 샤드에** 넣는 단순한 규칙(LPT)이 최적의 4/3배 안을 보장한다. 플레이키는 진짜 실패보다 나쁘다 — 빨간불을 무시하는 습관을 만들기 때문이다. 같은 커밋에서 여러 번 돌려 결과가 섞이는 것을 고르되, **매번 실패하는 것은 플레이키가 아니라 그냥 버그**다. 재시도로 덮으면 원인은 사라지지 않고 숨는다." }],
        code: { c: "const idx = Math.ceil(p / 100 * sorted.length) - 1;\nreturn sorted[Math.min(Math.max(idx, 0), sorted.length - 1)];", cap: "평균이 아니라 백분위" },
        key: ["평균은 꼬리를 감춘다", "CI 성능은 기준선 대비로", "긴 것부터 한가한 샤드에", "재시도는 원인을 숨긴다"] } },
  ],
};
