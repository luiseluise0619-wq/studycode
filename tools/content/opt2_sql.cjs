/* sql 트랙 — 정답이 5자 이상 단독 최장인 23문항의 오답을 내용 있게 다시 쓴다 (71차) */
module.exports = { track: "sql", fixes: [
  { k: "CAP", o: [
    "가용성(A)과 파티션 내성(P)을 동시에 만족할 수 없다",
    "일관성(C)과 파티션 내성(P)을 동시에 만족할 수 없다",
    "일관성(C)과 가용성(A)을 동시에 만족할 수 없다",
    "세 가지 모두 파티션 중에도 동시에 보장할 수 있다" ] },
  { k: "재귀 CTE 구조", o: [
    "결과 순서를 정하기 위해 ORDER BY 가 반드시 있어야 하며 없으면 재귀가 돌지 않는다",
    "재귀 멤버 안에서는 서브쿼리 사용이 금지되므로 조인으로만 자기 자신을 참조해야 한다",
    "앵커(anchor) 멤버와 재귀(recursive) 멤버를 UNION ALL로 결합한다",
    "집계를 하지 않더라도 GROUP BY 절을 함께 포함해야 재귀 CTE 의 문법이 성립한다" ] },
  { k: "주문 스냅샷", o: [
    "products 의 price 가 여러 번 바뀌면 조인 결과 행이 늘어나 주문 테이블의 기본키가 중복된 것처럼 보인다",
    "나중에 상품 가격이 바뀌면 과거 주문 금액까지 달라 보여 회계·정산과 어긋난다",
    "가격 컬럼이 조인 대상이 되면 그 컬럼에는 인덱스를 만들 수 없게 되어 주문 조회가 점점 느려진다",
    "조인 때문에 조회 성능이 조금 나빠질 뿐 값은 항상 최신 가격이므로 정확성 자체에는 문제가 없다" ] },
  { k: "lock-granularity", o: [
    "락 관리 메모리는 절약되지만 락 범위가 넓어져 다른 트랜잭션의 동시성이 크게 떨어진다",
    "락 하나만 관리하면 되므로 동시성도 올라가고 메모리도 절약되어 사실상 단점이 없는 최적화다",
    "테이블 락은 다른 트랜잭션의 읽기를 막지 않으므로 쓰기가 잠깐 기다릴 뿐 동시성에는 영향이 없다",
    "테이블 락은 행 락보다 더 세밀한 제어가 가능해져 갱신 범위를 정확히 지정할 수 있게 된다" ] },
  { k: "원인 분석", o: [
    "목록 안의 NULL 이 0 으로 변환되어 x &lt;&gt; 0 비교가 대부분 거짓이 되기 때문",
    "x &lt;&gt; NULL 비교가 UNKNOWN을 반환해 AND 전체가 결코 TRUE가 되지 못하기 때문",
    "IN 이 목록의 NULL 을 무시하고 나머지만 비교하므로 NOT IN 도 같은 규칙을 따르기 때문",
    "NOT IN 이 목록 안의 NULL 을 만나면 목록 전체를 제거해 비교할 대상이 남지 않기 때문" ] },
  { k: "범위 후 컬럼", o: [
    "인덱스에 컬럼이 2개뿐이라 첫 컬럼으로 범위를 좁힌 뒤에는 남은 컬럼 하나를 더 쓸 여지가 없어서",
    "b 가 문자열 타입이라 정수 5 와 비교할 때 형 변환이 일어나 인덱스의 정렬 순서를 쓸 수 없어서",
    "b = 5 같은 등치 조건은 선행 컬럼이 무엇이든 인덱스를 탈 수 없도록 옵티마이저에 정해져 있어서",
    "선행 컬럼 a에 범위(&gt;) 조건이 걸리면 그 이후 컬럼 b는 인덱스 정렬을 이어서 활용하기 어려워서" ] },
  { k: "데드락 대응", q: "적절하지 않은", o: [
    "모든 트랜잭션의 격리 수준을 SERIALIZABLE 로 강제해 충돌을 막는다",
    "모든 트랜잭션이 테이블과 행을 동일한 순서로만 접근하도록 규칙을 둔다",
    "데드락이 감지되어 롤백된 트랜잭션을 짧은 지연 뒤 자동으로 재시도한다",
    "트랜잭션을 짧게 유지해 락을 잡고 있는 시간을 최소로 줄이고 빨리 커밋한다" ] },
  { k: "인덱싱", o: [
    "Postgres는 jsonb에 GIN 인덱스, 또는 표현식/생성 컬럼에 B-tree 인덱스를 건다",
    "JSON 컬럼 전체를 문자열로 보고 LIKE '%\"k\":%' 로 검색하면 텍스트 인덱스를 탈 수 있다",
    "JSON 값은 구조가 가변이라 어떤 DB 에서도 인덱싱이 불가능하므로 풀스캔으로만 조회할 수 있다",
    "인덱스 대신 애플리케이션 캐시에 결과를 담아 두면 DB 를 거치지 않으므로 인덱스가 필요 없다" ] },
  { k: "비정규화 판단", q: "리포트 화면", o: [
    "조인이 병목이므로 관련 테이블을 하나의 넓은 테이블로 합쳐 조인을 없애고, 중복은 배치로 주기적으로 정리한다",
    "읽기 성능을 위해 선택적 비정규화(집계 컬럼·요약 테이블·구체화 뷰)를 도입하되, 쓰기 정합성 비용과의 트레이드오프를 관리한다",
    "조인은 무조건 비용이 크므로 전부 제거하고, 필요한 데이터는 애플리케이션이 여러 번 조회해 메모리에서 합친다",
    "어떤 상황에서든 최대한 정규화하는 것이 옳으므로, 조인이 아무리 많아져도 구조를 그대로 유지하고 인덱스만 보강해서 해결하는 편이 낫다" ] },
  { k: "DML 계획 확인", o: [
    "UPDATE 문에는 EXPLAIN 을 쓸 수 없으므로 같은 조건의 SELECT 로 바꿔 계획을 확인해야 한다",
    "EXPLAIN 이 붙으면 자동으로 읽기 전용 모드로 실행되므로 따로 주의해야 할 것이 전혀 없다",
    "ANALYZE 는 계획만 세우고 락을 걸지 않으므로 운영 중에 자유롭게 실행해도 된다",
    "실제로 데이터가 변경되므로 BEGIN으로 감싸고 확인 후 ROLLBACK 해야 한다" ] },
  { k: "정규화의 대가", o: [
    "테이블이 늘어난 만큼 기본키와 외래키가 반복 저장되어 저장 공간이 항상 크게 늘어난다",
    "테이블이 나뉘면 트랜잭션 격리 수준을 한 단계 더 높여야만 정합성이 유지되는 부담이 생긴다",
    "조회 시 조인이 늘어나 쿼리가 복잡해지고, 조인 비용 관리를 위한 인덱스 설계가 필요해진다",
    "데이터가 여러 테이블로 흩어져 갱신 순서를 맞추기 어려워지므로 정합성이 오히려 나빠진다" ] },
  { k: "ROWS vs RANGE", q: "2 PRECEDING AND CURRENT ROW 와 RANGE", o: [
    "ROWS는 물리적으로 앞의 2행과 현재 행을 보고, RANGE는 ORDER BY 값을 기준으로 범위를 정해 값이 같은 동률 행들을 모두 포함한다",
    "둘은 완전히 같으며 표기 차이일 뿐이다 — 어느 쪽을 쓰든 옵티마이저가 같은 프레임으로 바꿔 실행하므로 결과가 항상 같다",
    "ROWS는 숫자 컬럼에만 쓸 수 있고 RANGE는 날짜 컬럼에만 쓸 수 있으며, 두 문법을 서로 바꿔 쓰면 곧바로 문법 오류가 난다",
    "ROWS 는 오름차순 정렬에서만, RANGE 는 내림차순 정렬에서만 동작하므로 ORDER BY 의 방향에 따라 둘 중 하나를 반드시 골라 써야 한다" ] },
  { k: "1NF", q: "핵심 요건", o: [
    "모든 결정자가 후보키여야 한다는 것이 핵심 요건이다",
    "키가 아닌 속성 사이에 이행 함수 종속이 없어야 한다",
    "복합키의 일부에만 종속되는 부분 함수 종속이 없어야 한다",
    "모든 속성이 원자값(더 이상 분해되지 않는 단일 값)을 가진다" ] },
  { k: "email 인덱스", o: [
    "SELECT 에 컬럼을 너무 많이 적어 느린 것이다 → 필요한 컬럼만 고르면 읽는 바이트가 줄어 스캔이 빨라지므로, 인덱스 없이도 응답 시간이 회복된다",
    "email 을 대문자로 저장하면 빨라진다 → 대소문자 비교가 사라져 문자열 비교 비용이 줄고, 정렬 순서가 단순해져 옵티마이저가 더 빠른 계획을 고른다",
    "email에 인덱스가 없어 전체 테이블을 스캔한다 → CREATE INDEX idx_users_email ON users(email); 로 만들면 조회가 로그 시간으로 빨라진다",
    "LIMIT 1을 붙이면 인덱스 없이도 빨라진다 → 원하는 행을 찾는 즉시 스캔을 멈추므로 전체를 다 읽지 않게 되고, 인덱스를 따로 만들지 않아도 응답 시간이 크게 줄어든다" ] },
  { k: "cost의 의미", o: [
    "쿼리의 예상 실행 시간을 밀리초 단위로 환산해서 나타낸 값이라 그대로 시간으로 읽으면 된다",
    "읽어야 할 디스크 페이지의 바이트 수를 나타내므로 값이 크면 I/O 가 많다는 뜻이다",
    "옵티마이저가 계획을 비교하려고 쓰는 상대적 비용 추정 단위이며 밀리초가 아니다",
    "실제로 사용된 CPU 시간을 실행 뒤에 측정해 적은 값이라 EXPLAIN 만으로는 나오지 않는다" ] },
  { k: "저선택도", o: [
    "값이 두 종류뿐인 컬럼에 인덱스를 걸면 그 컬럼에는 더 이상 쓰기가 불가능해지기 때문",
    "인덱스 파일이 지나치게 커져 버퍼 풀을 다 차지해 버리기 때문이다",
    "값 종류가 적으면 B-tree 의 정렬 순서가 깨져 탐색이 처음부터 다시 시작되기 때문",
    "선택도가 낮아 인덱스가 대부분의 행을 가리키므로 옵티마이저가 풀스캔을 택함" ] },
  { k: "mvcc-snapshot", o: [
    "쓰기는 새 버전을 만들고 읽기는 자신의 스냅샷에 보이는 이전 버전을 읽으므로 서로 락으로 막지 않는다",
    "읽기는 디스크를 보지 않고 버퍼 캐시만 보므로 쓰기가 디스크를 잠가도 영향을 받지 않기 때문이다",
    "MVCC 는 모든 쓰기를 직렬화해 한 번에 하나만 실행하므로 읽기가 쓰기와 겹칠 일이 아예 없기 때문이다",
    "읽기 트랜잭션이 배타 락을 먼저 잡아 쓰기 트랜잭션을 뒤로 밀어내기 때문에 블로킹되지 않는다" ] },
  { k: "클러스터링", o: [
    "PRIMARY KEY 는 별도의 인덱스를 만들지 않고 행만 저장하므로 공간을 아끼기 때문이다",
    "단순히 테이블에 처음 만들어진 인덱스라 옵티마이저가 가장 먼저 고려하기 때문이다",
    "항상 자동 증가 정수여야 하므로 삽입이 언제나 끝에 붙어 페이지 분할이 없기 때문이다",
    "테이블 행 데이터가 PRIMARY KEY 순서로 저장되는 클러스터형 인덱스이기 때문" ] },
  { k: "정규화적합", o: [
    "해당 값으로 자주 조인·필터·집계하고 무결성 제약이 중요할 때",
    "해당 값이 거의 조회되지 않고 방치될 때 정규화 컬럼이 더 알맞다",
    "구조가 매 행마다 달라 컬럼을 미리 정할 수 없을 때가 더 알맞다",
    "임시 로그 덤프처럼 한 번 쓰고 버리는 데이터일 때가 더 알맞다" ] },
  { k: "HashJoin", o: [
    "인덱스 없는 큰 두 테이블을 등가(equi) 조인할 때 한쪽으로 해시 테이블을 만들어 매칭",
    "정렬된 출력이 반드시 필요할 때 — 해시 순서가 곧 정렬 순서라 별도 정렬이 필요 없다",
    "부등호(범위) 조인에서 최적이며, 등가 조인보다 좋은 성능을 낸다는 점이 장점이다",
    "한 건만 조회할 때 — 해시 테이블 조회가 O(1) 이라 인덱스 탐색보다 빠르기 때문이다" ] },
  { k: "원자적 갱신", o: [
    "UPDATE 문은 락을 사용하지 않아 다른 트랜잭션을 기다리게 하지 않으므로 경합 자체가 없기 때문",
    "SQL 문은 어떤 경우에도 자동으로 SERIALIZABLE 수준으로 실행되도록 정해져 있기 때문",
    "읽기와 계산과 쓰기가 하나의 문장에서 행 락과 함께 처리되어 중간에 다른 트랜잭션이 끼어들 수 없기 때문",
    "stock &gt; 0 조건이 인덱스를 사용해 재고가 있는 행만 빠르게 골라내므로 충돌할 행이 줄어들기 때문" ] },
  { k: "기본 격리 수준", o: [
    "둘 다 READ UNCOMMITTED — 커밋 전 데이터도 읽어 성능이 좋다",
    "PostgreSQL은 REPEATABLE READ, MySQL은 READ UNCOMMITTED",
    "둘 다 SERIALIZABLE — 표준이 정한 기본값이라 엔진이 따른다",
    "PostgreSQL은 READ COMMITTED, MySQL InnoDB는 REPEATABLE READ" ] },
  { k: "결측 날짜", o: [
    "프레임 안의 행 수가 7 이 안 되는 날은 계산할 수 없다는 오류가 발생해 쿼리가 실패한다",
    "데이터베이스가 빠진 날짜를 자동으로 보간해 채워 주므로 정확히 7일 평균이 나온다",
    "빠진 날짜는 매출 0 인 행으로 취급되어 평균이 실제보다 낮게 나온다",
    "행이 없는 날은 건너뛰므로 실제로는 7일이 아니라 최근 7건의 평균이 되어 기간이 늘어난다" ] }
] };
