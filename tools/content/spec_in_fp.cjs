module.exports = {
  track: "fp", guide: "", xp: 60,
  unit: "단답으로 확인하기 — 합성·커링·불변성",
  source: "./in_fp.cjs",
  lessons: [
    { t: "값을 함수 사이로 흘려보내기", n: 8,
      th: { sum: "함수형의 도구는 몇 개 안 된다 — **모으기·거르기·접기**, 그리고 그것을 잇는 **합성**과 **커링**.",
        body: [
          { h: "순서와 방향", t: "<code>compose(f, g)</code> 는 수학 표기를 따라 <b>g 를 먼저</b> 하고, <code>pipe(f, g)</code> 는 <b>적은 순서대로</b> 흘려보낸다 — 같은 두 함수라도 순서가 바뀌면 값이 달라진다. <code>pipe</code> 는 <code>reduce</code> 로 세 줄이면 만들어진다. 커링은 인자를 <b>하나씩</b> 받는 사슬로 바꾸는 것이고, '설정을 미리 먹여 둔 함수' 를 만들 때 쓴다 — 인자 일부만 미리 먹이는 <b>부분 적용</b>과는 다르다." },
          { h: "접기가 가장 일반적이다", t: "<code>map</code>·<code>filter</code>·<code>flatMap</code> 은 전부 <code>reduce</code> 로 만들 수 있지만 그 반대는 안 된다 — 표현력의 위아래가 정해져 있다. 그렇다고 전부 접기로 적으면 읽기 어려우니, 뜻이 분명한 이름을 쓰고 한 번에 끝내야 할 때만 접는다. <code>flatMap</code> 은 원소 하나가 <b>0 개 또는 여러 개</b>로 늘어날 때의 도구라, 버릴 원소에 빈 배열을 돌려주면 거르기까지 겸한다." }],
        code: { c: "const pipe = (...fs) => x => fs.reduce((v, fn) => fn(v), x);\ncompose(f, g)(3) === f(g(3))   // 오른쪽부터\nconst addTax = withRate(0.1);  // 설정을 먹여 둔 함수", cap: "값 하나가 함수들 사이를 지나간다" },
        key: ["compose 는 오른쪽부터, pipe 는 왼쪽부터", "커링과 부분 적용은 다르다", "reduce 가 가장 일반적인 형태", "freeze 는 한 겹만 얼린다"] } },
    { t: "빈 입력과 참조가 말해 주는 것", n: 7,
      th: { sum: "함수형의 보장은 **참조 투명성**에서 나오고, 그 보장을 확인하는 가장 좋은 입력은 **빈 것**과 **같은지 물어보는 것**이다.",
        body: [
          { h: "참조 투명성과 그것을 깨는 넷", t: "같은 입력에 같은 출력을 주어 <b>식을 결과값으로 바꿔 써도</b> 뜻이 안 변하는 성질이다. 이 성질이 있으면 메모이제이션·병렬 실행·순서 바꾸기가 전부 안전해진다. 깨뜨리는 것은 대개 넷이다 — <b>전역 상태·입출력·현재 시각·난수</b>. 시각과 난수를 인자로 받게 바꾸는 것만으로 테스트가 결정적으로 변한다. 지연 평가는 안 쓰이는 값을 아예 계산하지 않아, 큰 파일도 한 줄씩 흘려보낼 수 있게 한다." },
          { h: "빈 입력이 드러내는 항등원", t: "빈 배열에 '모두 참인가' 를 물으면 <b>참</b>(공허한 참)이고, '하나라도 참인가' 를 물으면 <b>거짓</b>이다. 두 값은 각 연산의 <b>항등원</b>이고, 같은 기준으로 <code>reduce</code> 의 초기값을 고른다 — 덧셈 0, 곱셈 1, 최댓값 <code>-Infinity</code>. '모든 항목이 승인됐으면 진행' 이 <b>목록이 비었을 때 통과</b>하는 것도 이 때문이다. 펼치기로 만든 객체는 참조가 달라지는데, 화면 라이브러리가 변경을 알아채는 근거가 바로 그것이다." }],
        code: { c: "[].every(x => false)   // true  — 반례가 없다\n[].some(x => true)     // false — 예가 없다\n{ ...o, a: 2 } !== o   // 참조가 바뀐다", cap: "빈 입력이 규칙을 드러낸다" },
        key: ["참조 투명성이 최적화의 근거", "시각·난수는 인자로 받는다", "빈 입력에서는 항등원이 나온다", "펼치기는 참조를 바꾼다"] } },
  ],
};
