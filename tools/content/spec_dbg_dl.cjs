module.exports = {
  track: "dl", guide: "🐛", xp: 80,
  unit: "실행형 실전 — 손실은 줄어드는데 틀린 코드 고치기",
  source: "./dbg_dl.cjs",
  lessons: [
    { t: "수치 안정성과 마스킹", n: 6,
      th: { sum: "딥러닝 버그는 **작은 예제에서 완벽하게 동작한다** — 실전 크기의 값과 실제 패딩을 넣어야 드러난다.",
        body: [
          { h: "넘치는 쪽과 사라지는 쪽", t: "`exp(1000)` 은 `Infinity` 이고 `Infinity/Infinity` 는 `NaN` 이다. 소프트맥스는 **모든 입력에 같은 수를 더해도 결과가 같으므로** 최댓값을 빼면 절대 넘치지 않는다. 워밍업·누적 스텝처럼 나눗셈이 들어간 스케줄은 **기능을 끈 설정**(0)에서 반드시 한 번 지나간다." },
          { h: "분자와 분모를 같이 고친다", t: "패딩 손실을 0 으로 지웠으면 분모도 **실제 토큰 수**로 바꿔야 한다 — 한쪽만 고치면 손실 곡선이 배치 구성에 따라 출렁인다. 어텐션 마스크는 소프트맥스 **앞**에서 `-Infinity` 로 걸어야 확률이 0 이 된다. 0 을 곱하는 것으로는 `exp(0)=1` 이라 미래가 그대로 보인다." }],
        code: { c: "const e = xs.map(x => Math.exp(x - Math.max(...xs)));\nscores.map((v, i) => (mask[i] ? v : -Infinity));", cap: "실전 크기 값과 마스크를 테스트에" },
        key: ["소프트맥스는 최댓값을 뺀다", "기능을 끈 설정을 테스트한다", "마스킹은 분자·분모를 함께", "마스크는 소프트맥스 앞에서"] } },
    { t: "학습·추론이 갈리는 곳과 규약", n: 6,
      th: { sum: "같은 코드가 학습과 추론에서 **다르게 동작해야** 하는 지점, 그리고 함수가 **무엇을 받는지** 헷갈리는 지점.",
        body: [
          { h: "모드가 있는 층", t: "드롭아웃과 배치 정규화는 학습·추론에서 동작이 다르다. 모드를 무시하면 같은 입력에 **매번 다른 예측**이 나오고 배치 크기 1 에서는 출력이 0 이 된다. '학습에서는 좋은데 추론이 이상하다' 면 이 둘을 가장 먼저 본다." },
          { h: "무엇을 입력으로 받는가", t: "로그 확률에 또 로그를 씌우면 손실이 음수나 `NaN` 이 된다 — **음의 로그 가능도는 절대 음수가 될 수 없다**. He 초기화는 `fanIn` 기준이고, 코사인 유사도는 두 노름으로 나눠야 순위가 벡터 크기 순으로 바뀌지 않는다. 기울기 클리핑은 **방향을 지키고 보폭만** 줄인다." }],
        code: { c: "if (!training) return xs.slice();     // 추론은 그대로\nreturn dot / (normA * normB);         // 크기를 나눈다", cap: "모드와 입력 형식을 계약에 적는다" },
        key: ["드롭아웃·BN 은 모드가 있다", "손실이 음수면 로그 중복", "He 는 fanIn 기준", "노름 클리핑은 방향을 지킨다"] } },
  ],
};
