/* 첫 유닛 다음에 만나는 레슨들의 이론 — 레슨마다 따로, 쉬운 것부터.

   앞 배치에서 각 트랙 첫 유닛 23개를 다시 썼다. 여기는 그 바로 다음 —
   초보자가 첫 유닛을 마치고 이어서 여는 곳이다.

   쓰는 규칙(주입기가 검사한다)
     · 한 문장 90자 넘지 않기
     · 한 문단에 한 가지만
     · 반드시 '안 그러면 어떻게 되는지' 를 적기
     · 용어를 쓰면 그 자리에서 풀기

   더한 규칙 하나: 문단을 쉬운 것부터 놓는다. 첫 문단은 '이게 뭔지',
   두 번째가 '어떻게 쓰는지', 세 번째가 '어디서 틀리는지' 순서다. */
module.exports = {

python: [
{
  unit: "반복과 데이터", lesson: "리스트 다루기",
  th: {
    sum: "리스트는 값을 **순서대로** 담아 두는 상자다. 한 이름으로 여러 값을 다룰 수 있다.",
    body: [
      { h: "만들고 꺼내기", t: "대괄호로 만든다. `과일 = [\"사과\", \"배\"]` 처럼 쉼표로 나눠 담는다. 꺼낼 때는 번호를 쓰는데 **0부터 센다.** 첫 번째가 `과일[0]` 이다. 1부터라고 생각하면 항상 한 칸씩 어긋난다." },
      { h: "뒤에서 세기", t: "`과일[-1]` 은 마지막이다. 길이를 몰라도 마지막을 꺼낼 수 있어 편하다. `과일[len(과일)]` 이라고 쓰면 없는 자리라 오류가 난다 — 마지막 번호는 길이보다 하나 작다." },
      { h: "없는 자리를 부르면", t: "두 칸짜리 리스트에서 `과일[5]` 를 부르면 `IndexError` 로 멈춘다. 조용히 넘어가지 않는 것이 다행이다 — 잘못된 값을 계속 쓰는 것보다 그 자리에서 멈추는 편이 낫다." },
    ],
    code: { c: "과일 = [\"사과\", \"배\", \"감\"]\nprint(과일[0])    # 사과\nprint(과일[-1])   # 감\nprint(len(과일))  # 3", cap: "번호는 0부터, 마지막은 -1" },
    key: ["대괄호로 만든다", "번호는 0부터", "마지막은 `[-1]`"],
  },
},
{
  unit: "반복과 데이터", lesson: "반복문 for",
  th: {
    sum: "`for` 는 '이 안의 것들을 하나씩 꺼내서 같은 일을 해' 라는 뜻이다.",
    body: [
      { h: "하나씩 꺼내 쓴다", t: "`for 하나 in 과일:` 이라고 쓰면 리스트의 값이 차례로 `하나` 에 들어온다. 번호를 다룰 일이 없어서 경계를 넘는 실수가 아예 생기지 않는다. 번호가 꼭 필요하면 `enumerate` 를 쓴다." },
      { h: "정해진 횟수만큼", t: "`range(5)` 는 0,1,2,3,4 다. **5는 들어가지 않는다.** 1부터 5까지 돌고 싶으면 `range(1, 6)` 이라고 적는다. 끝 번호를 그대로 적으면 마지막 하나가 늘 빠진다." },
      { h: "모으는 그릇은 밖에", t: "합계를 담을 변수는 반복문 **밖에서** 만든다. 안에 두면 돌 때마다 새로 만들어져 앞의 결과가 사라진다. 결과가 항상 마지막 값 하나만 남는다면 이걸 의심한다." },
    ],
    code: { c: "합계 = 0            # 밖에서 준비\nfor 값 in [1, 2, 3]:\n    합계 += 값       # 안에서 채운다\nprint(합계)         # 6", cap: "그릇은 밖, 채우기는 안" },
    key: ["`for x in 목록` 으로 하나씩", "`range(5)` 에 5는 없다", "모으는 그릇은 반복문 밖에"],
  },
},
],

javascript: [
{
  unit: "함수와 데이터", lesson: "함수 만들기",
  th: {
    sum: "함수는 '이름 붙인 일거리' 다. 한 번 만들어 두면 이름만 불러 여러 번 쓸 수 있다.",
    body: [
      { h: "만들고 부르기", t: "`function 두배(n) { return n * 2; }` 로 만들고 `두배(5)` 로 부른다. 괄호 안의 `n` 은 '받을 자리' 이고, 부를 때 넣은 값이 그 자리에 들어간다." },
      { h: "return 이 있어야 값이 나온다", t: "`return` 이 없는 함수는 `undefined` 를 돌려준다. `console.log` 로 찍는 것과 `return` 으로 돌려주는 것은 다르다 — 찍기는 사람이 보는 것이고, 돌려주기는 코드가 이어 쓰는 것이다." },
      { h: "화살표로 짧게", t: "`const 두배 = n => n * 2;` 도 같은 뜻이다. 중괄호를 쓰면 `return` 을 적어야 하고, 중괄호를 빼면 그 식의 값이 저절로 돌아간다. 중괄호를 쓰고 `return` 을 빠뜨리는 것이 흔한 실수다." },
    ],
    code: { c: "function 두배(n) { return n * 2; }\nconst 세배 = n => n * 3;\nconst 네배 = n => { return n * 4; };\n\n두배(5);   // 10", cap: "중괄호를 쓰면 return 을 적는다" },
    key: ["`return` 이 없으면 `undefined`", "찍기와 돌려주기는 다르다", "중괄호 화살표에는 `return` 필요"],
  },
},
{
  unit: "함수와 데이터", lesson: "배열 다루기",
  th: {
    sum: "배열은 값을 순서대로 담는다. 다루는 방법이 두 갈래인데, **원본을 바꾸는 것**과 **새로 만드는 것**이다.",
    body: [
      { h: "원본을 바꾸는 것", t: "`push`(뒤에 추가), `pop`(뒤에서 빼기), `sort`(정렬), `reverse`(뒤집기)는 받은 배열을 그 자리에서 고친다. 남의 배열을 받아서 정렬하면 부른 쪽 배열까지 바뀐다 — 실무에서 자주 터지는 사고다." },
      { h: "새로 만드는 것", t: "`map`(모두 바꾸기), `filter`(골라내기), `slice`(잘라내기)는 새 배열을 돌려주고 원본은 그대로 둔다. 결과를 변수에 받지 않으면 아무 일도 일어나지 않는다." },
      { h: "숫자 정렬은 기준을 준다", t: "`sort()` 는 기본이 **글자 순서**라 `[10, 9]` 가 그대로 남는다. 숫자로 정렬하려면 `sort((a, b) => a - b)` 라고 기준을 넘긴다. 안 그러면 10이 9보다 앞에 온다." },
    ],
    code: { c: "const xs = [10, 9, 2];\nxs.sort();                    // [10, 2, 9] — 글자 순서!\n[...xs].sort((a, b) => a - b); // [2, 9, 10]\n\nxs.map(x => x * 2);           // 새 배열", cap: "sort 는 원본을 바꾸고 글자 순서다" },
    key: ["`push`·`sort` 는 원본을 바꾼다", "`map`·`filter` 는 새로 만든다", "숫자 정렬은 `(a,b) => a-b`"],
  },
},
],

sql: [
{
  unit: "집계와 그룹", lesson: "개수·합계 구하기",
  th: {
    sum: "집계 함수는 **여러 행을 하나의 값으로** 줄인다. 개수를 세거나 합을 구할 때 쓴다.",
    body: [
      { h: "무엇을 세는가", t: "`COUNT(*)` 는 행 수를 센다. `COUNT(열)` 은 그 열이 **비어 있지 않은** 행만 센다. 둘의 차이가 곧 빈 칸의 개수라, 데이터가 얼마나 채워졌는지 확인할 때 나란히 써 본다." },
      { h: "빈 칸은 건너뛴다", t: "`SUM`·`AVG` 는 빈 칸(NULL)을 아예 빼고 계산한다. 그래서 `SUM(금액) / COUNT(*)` 와 `AVG(금액)` 이 다를 수 있다. 앞엣것은 빈 칸까지 분모에 넣기 때문이다." },
      { h: "아무 행도 없으면", t: "조건에 맞는 행이 하나도 없으면 `COUNT` 는 0이지만 `SUM` 은 빈 값(NULL)이다. 0으로 보이게 하려면 `COALESCE(SUM(금액), 0)` 처럼 채워 준다." },
    ],
    code: { c: "SELECT COUNT(*)        AS 전체,\n       COUNT(나이)     AS 나이있음,\n       AVG(나이)       AS 평균나이\nFROM 회원;", cap: "COUNT(*) 와 COUNT(열) 은 다르다" },
    key: ["`COUNT(*)` 는 행 수", "`COUNT(열)` 은 빈 칸을 안 센다", "행이 없으면 `SUM` 은 NULL"],
  },
},
{
  unit: "집계와 그룹", lesson: "GROUP BY 로 묶기",
  th: {
    sum: "`GROUP BY` 는 같은 값끼리 묶는다. 묶고 나면 묶음마다 한 행이 남는다.",
    body: [
      { h: "묶으면 행이 줄어든다", t: "`GROUP BY 도시` 는 도시가 같은 행을 하나로 합친다. 그래서 결과에는 도시별로 한 줄만 남는다. 묶은 뒤에는 **묶은 열과 집계 값만** 고를 수 있다 — 개인 이름 같은 것은 사라진다." },
      { h: "언제 거르는가", t: "`WHERE` 는 **묶기 전에** 행을 거르고, `HAVING` 은 **묶은 뒤에** 묶음을 거른다. '2명 이상인 도시' 처럼 센 결과로 걸러야 하면 `HAVING` 이다. `WHERE COUNT(*) > 1` 이라고 쓰면 아직 센 것이 없어 오류가 난다." },
      { h: "순서를 기억한다", t: "`WHERE` → `GROUP BY` → `HAVING` → `ORDER BY` 순으로 적는다. 이 순서가 곧 데이터베이스가 일하는 순서라, 왜 어떤 건 되고 어떤 건 안 되는지도 여기서 설명된다." },
    ],
    code: { c: "SELECT 도시, COUNT(*) AS 수\nFROM 회원\nWHERE 나이 >= 20      -- 묶기 전\nGROUP BY 도시\nHAVING COUNT(*) >= 2  -- 묶은 뒤\nORDER BY 수 DESC;", cap: "WHERE 는 묶기 전, HAVING 은 묶은 뒤" },
    key: ["묶으면 묶음마다 한 행", "`WHERE` 는 묶기 전, `HAVING` 은 뒤", "순서는 WHERE→GROUP BY→HAVING"],
  },
},
],

web: [
{
  unit: "CSS 기초", lesson: "CSS 적용하기",
  th: {
    sum: "CSS 는 '어디에' 와 '어떻게' 로 이루어진다. 선택자가 '어디에' 이고 중괄호 안이 '어떻게' 다.",
    body: [
      { h: "세 가지 고르는 법", t: "태그 이름으로 `p { }`, 클래스로 `.card { }`, 아이디로 `#top { }` 를 고른다. 클래스는 여러 곳에 붙일 수 있고 아이디는 한 페이지에 하나만 쓴다. 대부분은 클래스로 충분하다." },
      { h: "겹치면 누가 이기나", t: "같은 요소에 여러 규칙이 걸리면 더 **구체적인** 쪽이 이긴다. 아이디가 클래스보다, 클래스가 태그보다 세다. 세기가 같으면 나중에 쓴 것이 이긴다 — 그래서 순서도 중요하다." },
      { h: "안 먹히면 확인할 것", t: "스타일이 안 먹히면 대개 셋 중 하나다. 선택자가 그 요소를 못 고르고 있거나, 더 센 규칙에 밀렸거나, 오타로 규칙 전체가 무시됐다. 개발자 도구에서 그 요소를 보면 어떤 규칙이 이겼는지 바로 보인다." },
    ],
    code: { c: "p { color: gray; }\n.card { padding: 12px; }\n#top { height: 60px; }\n\n/* 겹치면 아이디 > 클래스 > 태그 */", cap: "구체적인 쪽이 이긴다" },
    key: ["`.클래스` 를 주로 쓴다", "구체적인 쪽이 이긴다", "안 먹히면 무엇에 밀렸는지 본다"],
  },
},
{
  unit: "CSS 기초", lesson: "색·글꼴·박스",
  th: {
    sum: "화면의 모든 요소는 네모다. 그 네모는 **내용 · 안쪽 여백 · 테두리 · 바깥 여백** 네 겹으로 되어 있다.",
    body: [
      { h: "안쪽과 바깥쪽", t: "`padding` 은 테두리 **안쪽** 여백이라 배경색이 칠해진다. `margin` 은 테두리 **바깥** 여백이라 칠해지지 않는다. 배경이 어디까지 보이는지로 둘을 구별할 수 있다." },
      { h: "폭이 커지는 이유", t: "기본 설정에서 `width: 200px` 에 `padding: 20px` 을 주면 실제 폭은 240px 이 된다. 여백이 폭 바깥에 더해지기 때문이다. 칸이 밀려 나거나 줄이 바뀌면 대개 이것이 원인이다." },
      { h: "폭을 지키는 법", t: "`box-sizing: border-box` 를 주면 여백과 테두리를 **폭 안에 포함**한다. 정한 숫자가 그대로 화면의 폭이 되어 계산이 쉬워진다. 그래서 요즘은 문서 전체에 이 설정을 기본으로 깔아 둔다." },
    ],
    code: { c: ".box {\n  box-sizing: border-box;\n  width: 200px;\n  padding: 20px;   /* 폭은 여전히 200px */\n}", cap: "border-box 는 여백을 폭 안에 넣는다" },
    key: ["`padding` 은 안쪽, `margin` 은 바깥", "기본은 여백이 폭에 더해진다", "`border-box` 로 폭을 지킨다"],
  },
},
],

go: [
{
  unit: "슬라이스와 배열", lesson: "슬라이스와 append",
  th: {
    sum: "슬라이스는 값을 순서대로 담는 것인데, **길이가 늘어날 수 있다.** 배열은 길이가 고정이다.",
    body: [
      { h: "만들고 늘리기", t: "`xs := []int{1, 2}` 로 만들고 `xs = append(xs, 3)` 으로 늘린다. `append` 는 결과를 **돌려주므로 반드시 다시 담아야** 한다. 다시 담는 것을 빠뜨리면 원래 슬라이스는 그대로다." },
      { h: "길이와 용량", t: "`len` 은 지금 담긴 개수, `cap` 은 더 담을 수 있는 자리다. 용량이 모자라면 `append` 가 더 큰 배열을 새로 잡아 옮긴다. 몇 개가 들어올지 안다면 `make([]int, 0, n)` 으로 미리 잡아 두면 옮기는 일이 없어진다." },
      { h: "잘라내도 원본을 본다", t: "`ys := xs[:2]` 는 새 배열을 만들지 않는다. 같은 배열의 일부를 보는 창일 뿐이다. `ys` 를 고치면 `xs` 도 바뀐다 — 진짜 복사가 필요하면 `make` 로 새로 잡고 `copy` 로 옮긴다." },
    ],
    code: { c: "xs := []int{1, 2}\nxs = append(xs, 3)      // 다시 담아야 한다\n\nys := make([]int, len(xs))\ncopy(ys, xs)             // 진짜 복사", cap: "append 결과는 다시 담는다" },
    key: ["`append` 결과를 다시 담는다", "`len` 은 개수, `cap` 은 자리", "잘라내기는 복사가 아니다"],
  },
},
],

java: [
{
  unit: "메서드", lesson: "메서드 기초",
  th: {
    sum: "메서드는 '이름 붙인 일거리' 다. 자바에서는 반드시 클래스 안에 만든다.",
    body: [
      { h: "돌려줄 것을 먼저 적는다", t: "`int 두배(int n)` 에서 앞의 `int` 는 **돌려줄 값의 타입**이다. 아무것도 안 돌려주면 `void` 라고 적는다. `void` 인데 `return 값;` 을 쓰면 컴파일 오류가 난다." },
      { h: "이름이 같아도 된다", t: "인자의 개수나 타입이 다르면 같은 이름을 여러 번 만들 수 있다(오버로딩). `더하기(int, int)` 와 `더하기(double, double)` 를 따로 둘 수 있다. 자바가 넘긴 값의 타입을 보고 알맞은 쪽을 고른다." },
      { h: "static 이 붙는 이유", t: "`static` 이 붙으면 객체를 만들지 않고 클래스 이름으로 바로 부를 수 있다. `main` 이 `static` 인 것도 그래서다 — 프로그램이 시작될 때는 아직 아무 객체도 없다." },
    ],
    code: { c: "public class Util {\n    static int 두배(int n) {\n        return n * 2;\n    }\n}\n// 부르기: Util.두배(5)", cap: "돌려줄 타입을 앞에 적는다" },
    key: ["돌려줄 타입을 먼저 적는다", "안 돌려주면 `void`", "`static` 은 객체 없이 부른다"],
  },
},
],

c: [
{
  unit: "흐름 제어", lesson: "조건문",
  th: {
    sum: "조건이 둘이면 `if`, 값이 여럿이면 `switch` 가 읽기 쉽다.",
    body: [
      { h: "중괄호를 쓴다", t: "`if (조건) 한줄;` 처럼 중괄호를 빼면 **바로 다음 한 줄만** 조건에 걸린다. 나중에 줄을 하나 더 넣었다가 조건 밖에 놓이는 사고가 자주 난다. 항상 중괄호를 쓰는 편이 안전하다." },
      { h: "switch 는 break 가 필요하다", t: "`case` 에 걸리면 그 아래로 **계속 흘러내려 간다.** `break` 를 빠뜨리면 다음 `case` 까지 실행된다. 일부러 흘려보내는 경우도 있지만, 대부분은 실수라서 주석으로 밝혀 두는 것이 관례다." },
      { h: "default 를 둔다", t: "어느 `case` 에도 안 걸리면 `switch` 는 아무 일도 하지 않는다. 예상 못 한 값이 조용히 지나가므로, `default` 를 두어 '여기 왔으면 이상한 것' 을 남기는 편이 낫다." },
    ],
    code: { c: "switch (등급) {\n    case 'A': printf(\"우수\\n\"); break;\n    case 'B': printf(\"보통\\n\"); break;\n    default:  printf(\"모름\\n\");\n}", cap: "break 를 빠뜨리면 아래로 흘러간다" },
    key: ["중괄호를 생략하지 않는다", "`case` 마다 `break`", "`default` 로 빠짐을 잡는다"],
  },
},
],

cpp: [
{
  unit: "컨테이너와 클래스", lesson: "vector 다루기",
  th: {
    sum: "`std::vector` 는 길이가 늘어나는 배열이다. C++ 에서 값을 여러 개 담을 때 기본으로 쓴다.",
    body: [
      { h: "만들고 넣기", t: "`std::vector<int> v;` 로 만들고 `v.push_back(3)` 으로 넣는다. `v.size()` 가 개수이고 `v[0]` 으로 꺼낸다. `#include <vector>` 를 잊으면 못 쓴다." },
      { h: "없는 자리를 부르면", t: "`v[10]` 은 범위를 확인하지 않는다. 없는 자리라도 그냥 읽어서 이상한 값이 나오거나 프로그램이 죽는다. 확인이 필요하면 `v.at(10)` 을 쓴다 — 범위를 넘으면 예외를 던져 준다." },
      { h: "미리 잡아 두기", t: "몇 개가 들어올지 알면 `v.reserve(n)` 으로 자리를 미리 잡는다. 안 그러면 넣다가 자리가 모자랄 때마다 더 큰 자리로 통째로 옮긴다. 개수가 많아질수록 이 옮기는 비용이 커진다." },
    ],
    code: { c: "#include <vector>\n\nstd::vector<int> v;\nv.reserve(100);       // 미리 잡아 둔다\nv.push_back(3);\nv.at(0);              // 범위를 검사한다", cap: "v[i] 는 검사 안 함, v.at(i) 는 검사함" },
    key: ["`push_back` 으로 넣는다", "`v[i]` 는 범위를 안 본다", "개수를 알면 `reserve`"],
  },
},
],

react: [
{
  unit: "React 더 배우기", lesson: "리스트 렌더링",
  th: {
    sum: "`props` 는 부모가 자식에게 내려 주는 값이다. 방향은 **위에서 아래로만** 흐른다.",
    body: [
      { h: "내려 주고 받아 쓰기", t: "부모가 `<Item name=\"가영\" />` 이라고 적으면 자식은 `props.name` 으로 받는다. 이름을 지어 주는 쪽이 부모이므로, 자식은 어떤 부모가 쓰는지 몰라도 된다 — 그래서 재사용이 된다." },
      { h: "자식은 고칠 수 없다", t: "자식이 받은 props 를 직접 바꾸면 안 된다. 바꿔야 할 값이 있으면 **부모가 함수를 함께 내려 주고** 자식이 그 함수를 부른다. 이 방향이 지켜져야 '누가 값을 바꿨는지' 를 추적할 수 있다." },
      { h: "안 내려오면 undefined", t: "부모가 안 넘긴 props 를 읽으면 `undefined` 다. 오류가 아니라서 화면에 아무것도 안 나오고 끝난다. 기본값이 필요하면 `props.name ?? \"이름 없음\"` 처럼 적어 둔다." },
    ],
    code: { c: "function Item(props) {\n  return <span>{props.name ?? \"이름 없음\"}</span>;\n}\n\n<Item name=\"가영\" />", cap: "값은 위에서 아래로만 흐른다" },
    key: ["부모가 내려 주고 자식이 받는다", "자식은 props 를 고치지 않는다", "안 내려오면 `undefined`"],
  },
},
],

};
