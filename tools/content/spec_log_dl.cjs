module.exports = {
  track: "dl", guide: "", xp: 70,
  unit: "로그 분석 — 학습이 남긴 것",
  source: "./log_ops2.cjs", slice: [16, 24],
  lessons: [
    { t: "숫자가 무너지는 자리", n: 4,
      th: { sum: "학습 로그를 읽는 요령은 **손실 하나만 보지 않는 것**이다. 기울기 크기·메모리·단계 시간이 원인을 먼저 알려 준다.",
        body: [
          { h: "NaN 은 결과이지 원인이 아니다", t: "손실이 NaN 이 되기 전에는 거의 언제나 **기울기가 폭주한 자리**가 있다. 3에서 800으로, 다시 백만으로 커지다 표현할 수 없는 값이 되고, 한 번 그렇게 되면 곱셈으로 모든 가중치에 번져 회복되지 않는다. 손실만 보고 있으면 이미 늦으므로 기울기 크기를 함께 기록한다. 처방은 잘라내기·학습률 낮추기, 그리고 데이터에 섞인 극단값 확인이다 — 배치 하나의 이상한 값이 방아쇠가 되는 일이 흔하다." },
          { h: "메모리와 속도가 말해 주는 것", t: "학습보다 **검증이 메모리를 더 먹으면** 거꾸로 된 것이다. 갱신을 하지 않는 구간이니 원래 훨씬 가벼워야 하는데, 기울기 계산이 켜진 채면 중간 결과가 모두 남아 배치마다 쌓인다. 속도도 마찬가지다 — 가속기 사용률이 낮고 코어 하나만 100퍼센트라면 병목은 계산이 아니라 **데이터 준비**다. 단계 시간을 계산과 대기로 나눠 기록하면 한눈에 보인다." }],
        code: { c: "log(step, loss, grad_norm)      # 손실만 보지 않는다\nwith torch.no_grad(): evaluate()\nDataLoader(..., num_workers=8, prefetch_factor=2)", cap: "손실 옆에 무엇을 적어 두었는가" },
        key: ["NaN 앞에 기울기 폭주가 있다", "검증이 더 무거우면 거꾸로다", "사용률이 낮으면 준비가 병목", "단계 시간을 나눠 적는다"] } },
    { t: "평가와 재현", n: 4,
      th: { sum: "점수가 이상할 때 **모델을 의심하기 전에 평가 절차를 의심한다.** 그리고 이어서·다시·다른 곳에서 같은 결과가 나오는지가 실력을 가른다.",
        body: [
          { h: "평가가 망가지는 두 자리", t: "평가 점수가 무작위와 같은 수준이면 못 배운 것이 아니라 평가가 망가진 것이다. 모드를 바꾸지 않으면 정규화 층이 배치별 통계를 쓰고 **누적 통계까지 평가 데이터로 덮으며**, 드롭아웃도 계속 켜져 있다. 학습과 서비스의 답이 다를 때도 같은 식으로 본다 — 가중치 확인값이 같다면 달라진 것은 **모델에 들어가기 전의 처리**다. 전처리를 모델과 함께 묶어 저장하고 배포 전에 같은 표본으로 양쪽을 견준다." },
          { h: "이어서 하는 학습과 여러 장비", t: "체크포인트에 가중치만 담으면 **최적화기의 누적 상태**가 사라진다. 그것 없이 큰 학습률로 다시 시작하면 잘 자리 잡은 가중치가 흔들려 손실이 뛴다. 가속기를 늘릴 때도 저절로 나뉘지 않는다 — 분산 방식을 켜지 않으면 한 장만 일하고 나머지는 요금만 낸다. 켤 때는 **전체 배치가 커진 만큼** 학습률과 예열도 함께 손봐야 한다. 그리고 두 곡선이 갈라지는 지점에서 멈추는 규칙(조기 종료)이 없으면 열 에폭을 헛돈다." }],
        code: { c: "model.eval()                     # 평가 모드로\ntorch.save({model, optimizer, scheduler, epoch})\n# val loss 가 오르기 시작하면 멈춘다", cap: "모델보다 절차를 먼저 의심한다" },
        key: ["무작위 수준이면 평가를 의심한다", "전처리는 모델과 함께 묶는다", "체크포인트에 최적화기 상태를", "두 곡선이 갈라지면 멈춘다"] } },
  ],
};
