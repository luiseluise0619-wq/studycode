/* go 트랙 — 정답이 단독으로 가장 긴 문항의 오답을 내용 있게 다시 쓴다.
   node fix_opts.cjs --apply opt_go.cjs */
module.exports = { track: "go", fixes: [
  { k: "go2-cs-02", o: [
    "고루틴에서 발생한 패닉을 자동으로 복구해 무시해 주기 때문 — recover 를 일일이 걸지 않아도 한 고루틴의 패닉이 프로세스를 죽이지 않는다",
    "고루틴을 OS 스레드에 고정해 주기 때문 — 워커마다 스레드가 붙어 스케줄러의 선점 없이 끝까지 돌아 처리량이 안정된다",
    "채널을 자동으로 닫아 주기 때문 — 모든 고루틴이 끝나면 결과 채널을 close 해 주어 수신 쪽 range 가 저절로 끝난다",
    "첫 에러를 Wait 에서 돌려주고, WithContext 로 나머지 작업을 취소하며 SetLimit 으로 동시 실행 수도 제한할 수 있기 때문" ] },
  { k: "go2-ms-04", o: [
    "인터페이스는 런타임에 메서드를 찾으므로 컴파일 시점 정보가 전혀 없고, 주소를 취할지 말지를 컴파일러가 정할 근거가 어디에도 없기 때문",
    "Go 컴파일러가 아직 구현하지 못한 미완성 기능이기 때문 — 제안은 받아들여졌지만 이스케이프 분석과 얽혀 미뤄지고 있다",
    "인터페이스는 값의 복사본을 담으므로 몰래 원본 주소를 잡으면 원본과 사본이 어긋나고, 대입 대상이 addressable 하지 않을 수도 있어서",
    "인터페이스 값은 항상 힙에 있어 주소 계산 비용이 크고, 대입할 때마다 그 비용을 치르게 하지 않으려고 언어가 막아 둔 것이기 때문" ] },
  { k: "go-test-table", o: [
    "테스트 커버리지를 올리기 위해 — t.Run 으로 감싼 케이스는 커버리지 도구가 각각 별도 경로로 세어 수치가 높아진다",
    "각 케이스를 이름 있는 서브테스트로 분리해 실패한 케이스를 바로 식별하고 개별 실행할 수 있게 한다",
    "테스트를 병렬로 강제 실행하기 위해 — t.Run 안의 케이스는 기본적으로 고루틴에서 동시에 돌아 전체 시간이 준다",
    "반복문을 대신하기 위해 — t.Run 이 테이블을 받아 케이스마다 함수를 불러 주므로 for 문을 직접 쓰지 않아도 된다" ] },
  { k: "go-err-wrap", o: [
    "둘은 완전히 동일하다 — %w 는 %v 의 별칭으로, 에러 타입에 대해 어느 쪽을 쓰든 같은 문자열과 같은 체인이 만들어진다",
    "%w 는 원본 에러를 체인에 보존해 errors.Is/As 로 찾을 수 있게 하고, %v 는 문자열만 남긴다",
    "%w 는 한 서식 문자열에 한 번만 쓸 수 있고 %v 는 여러 번 쓸 수 있다는 개수 제한만 다르고, 체인 보존 여부는 같다",
    "%w 는 문자열만 남기고 %v 가 체인을 보존한다 — 그래서 errors.Is 로 찾으려면 %v 로 감싸야 한다" ] },
  { k: "go2-gs-01", o: [
    "ListenAndServe 는 미들웨어를 붙일 수 없기 때문 — 핸들러를 감싸는 체인은 Server 구조체의 Handler 필드로만 지정할 수 있다",
    "타임아웃이 전혀 설정되지 않아 느린 클라이언트가 커넥션을 무한정 붙들 수 있고, Shutdown 같은 제어도 할 수 없기 때문",
    "ListenAndServe 는 요청을 순차 처리해 동시성이 없기 때문 — 요청마다 고루틴을 띄우려면 Server 를 직접 만들어야 한다",
    "ListenAndServe 는 HTTP/2 를 지원하지 않기 때문 — 프로토콜 협상은 Server 구조체의 TLSConfig 를 거쳐야만 켜진다" ] },
  { k: "go3-less-strict", o: [
    "아무 문제 없다 — <= 를 쓰면 같은 값끼리의 원래 순서가 유지되어 오히려 안정 정렬이 되고, 결과도 < 와 같다",
    "컴파일러가 비교 함수의 부등호를 검사해 컴파일 에러로 막아 준다 — less 라는 이름의 함수는 < 만 허용된다",
    "같은 값에 대해 true 를 돌려주므로 엄격한 대소 관계가 깨져 정렬 결과가 잘못되거나 예기치 못한 동작이 발생한다",
    "정렬은 정상이지만 성능만 두 배 느려진다 — 같은 값을 만날 때마다 자리를 바꾸느라 교환 횟수가 늘어난다" ] },
  { k: "go2-si-01", o: [
    "Builder 만 유니코드를 올바르게 처리하기 때문 — += 는 바이트 단위로 붙여 멀티바이트 글자가 경계에서 깨질 수 있다",
    "문자열은 불변이라 += 는 매번 새 문자열을 할당·복사해 전체가 O(n^2) 이 되는데, Builder 는 내부 버퍼를 키워 가며 복사를 줄이기 때문",
    "+= 는 이어 붙일 문자열 개수가 많아지면 컴파일 에러가 나기 때문 — 한 식에서 연결할 수 있는 피연산자 수에 상한이 있다",
    "Builder 는 결과를 자동으로 정렬해 주기 때문 — WriteString 으로 넣은 조각을 사전순으로 배치해 돌려주므로 정렬을 따로 할 필요가 없다" ] },
  { k: "go-sl-fullslice", o: [
    "결과 슬라이스의 cap 을 c-a 로 제한해, append 시 반드시 새 배열을 할당하도록 만든다",
    "원소를 c 간격으로 하나씩 건너뛰며 골라 낸다 — 파이썬의 s[a:b:step] 과 같은 뜻으로, 짝수 인덱스만 뽑을 때 쓴다",
    "복사할 원소 개수를 지정한다 — a 부터 b 까지를 새 배열로 복사하되 최대 c 개까지만 담는 copy 의 축약형이다",
    "슬라이스의 증가 폭을 지정한다 — 이 슬라이스에 append 할 때 배열을 두 배가 아니라 c 개씩 늘리도록 정책을 바꾼다" ] },
  { k: "go2-md-05", o: [
    "내 모듈의 코드가 직접 임포트하지 않고, 의존성의 의존성으로 필요한 전이 의존성이라는 뜻이다",
    "그 의존성의 소스를 신뢰할 수 없다는 경고 표시 — 체크섬 데이터베이스에 없는 모듈에 go 도구가 자동으로 붙인다",
    "그 의존성이 테스트에서만 쓰인다는 뜻이다 — _test.go 파일에서만 임포트한 모듈은 배포 빌드에서 빠진다는 표시다",
    "빌드에서 제외된다는 표시다 — 주석 처리된 require 줄처럼 취급되어 go build 가 그 모듈을 내려받지 않는다" ] },
  { k: "go2-em-02", o: [
    "임베딩된 타입의 필드는 비공개가 되기 때문 — 바깥 타입에서는 승격된 이름으로만 접근할 수 있고 원래 이름은 숨겨진다",
    "리시버가 안쪽 타입으로 고정되어 있어, 안쪽 메서드가 호출하는 이름은 바깥에서 재정의해도 바뀌지 않기 때문(동적 디스패치 없음)",
    "임베딩은 런타임에만 결정되어 컴파일 시점 타입 검사가 없기 때문 — 어떤 메서드가 승격될지 실행해 봐야 알 수 있어 사실상 상속과 같다",
    "임베딩된 타입의 메서드는 바깥 타입에서 호출할 수 없기 때문 — 필드만 승격되고 메서드는 안쪽 타입 이름을 거쳐야 부를 수 있다" ] },
  { k: "go2-es-06", o: [
    "new 는 힙, &P{} 는 스택에 할당된다 — new 는 런타임 할당 함수를 부르고 리터럴은 컴파일러가 프레임 안에 자리를 잡는다",
    "new 는 제로값으로만 초기화하고 &P{} 는 필드를 지정할 수 있다는 차이뿐이고, 어디에 할당되는지는 둘 다 이스케이프 분석이 정한다",
    "두 표현은 타입이 달라 서로 대입할 수 없다 — new 는 *P 를, &P{} 는 P 의 주소 리터럴을 돌려주어 변환이 필요하다",
    "&P{} 는 힙 할당이고 new 는 최적화로 제거된다 — 어디에 할당되는지는 문법으로 정해져 있어 new 를 쓰는 편이 빠르다" ] },
  { k: "go2-si-06", o: [
    "Scanner 는 파일만 읽을 수 있어 다른 Reader 에는 쓸 수 없다 — 네트워크나 파이프 입력이면 os.File 로 바꿔야 한다",
    "Scanner 는 한 번만 Scan 할 수 있다 — 두 번째 호출부터 false 를 돌려주므로 매 줄마다 새 Scanner 를 만들어 이어 읽어야 한다",
    "기본 토큰 크기 한도(약 64KB)를 넘겼다. scanner.Buffer 로 한도를 늘리거나 bufio.Reader.ReadString 을 쓴다",
    "인코딩이 UTF-8 이 아니기 때문이다 — Scanner 는 잘못된 바이트 열을 만나면 멈추므로 변환기를 거쳐 넣어야 한다" ] },
  { k: "go3-tx-rollback", o: [
    "Commit 이후의 Rollback 은 ErrTxDone 을 돌려줄 뿐 아무 영향이 없어, 조기 반환 시 안전망 역할을 한다",
    "Commit 한 트랜잭션을 되돌려 데이터가 사라진다 — defer 는 함수 끝에서 실행되므로 커밋된 내용을 다시 지운다",
    "Rollback 이 먼저 실행되므로 Commit 이 실패하게 된다 — defer 는 등록 즉시 예약되어 Commit 보다 앞서 트랜잭션을 끝낸다",
    "Commit 과 Rollback 이 충돌해 패닉이 발생한다 — 이미 끝난 트랜잭션을 다시 조작하면 database/sql 이 패닉으로 알린다" ] },
  { k: "go2-hd-02", o: [
    "핸들러를 경로에 등록하는 라우터 메서드다 — mux.HandleFunc 의 짧은 이름으로, 패턴과 함수를 받아 테이블에 넣는다",
    "요청이 들어올 때마다 새 고루틴을 만들어 주는 스케줄러다 — 이 타입으로 감싼 함수만 요청마다 고루틴에서 실행된다",
    "핸들러의 실행 시간을 측정하는 계측 도구다 — 감싼 함수의 시작과 끝을 기록해 느린 요청을 로그로 남긴다",
    "func(w, r) 시그니처의 함수에 ServeHTTP 메서드를 붙여 http.Handler 로 만들어 주는 어댑터 타입이다" ] },
  { k: "go2-gu-06", o: [
    "함수 인자가 3개 이상일 때 — 인자가 많아지면 타입 파라미터로 묶는 편이 시그니처가 짧아지고 읽기 쉬워진다",
    "같은 코드를 서로 다른 타입으로 세 번 이상 복제하게 되었고, 알고리즘 자체는 타입과 무관할 때",
    "interface{} 를 한 번이라도 쓰게 될 때가 기준이다 — 빈 인터페이스는 곧 타입 정보 손실이므로 제네릭으로 대체해야 한다",
    "코드가 최신 문법을 쓰는 것처럼 보여야 할 때 — 타입 파라미터가 있으면 리뷰어가 설계를 더 신뢰하는 경향이 있다" ] },
  { k: "go2-gu-04", o: [
    "비슷한 메모리 형태끼리 코드를 공유(GC shape stenciling)하고 런타임 딕셔너리를 넘기므로, 인터페이스와 비슷하거나 느릴 수도 있기 때문",
    "제네릭 코드는 인라이닝이 금지되기 때문 — 타입 파라미터가 있는 함수는 컴파일러가 호출 지점에 펼치지 못해 호출 비용이 그대로 남고 이득이 사라진다",
    "제네릭은 컴파일 시점에 모든 타입을 완전히 복제해 바이너리가 커지고 캐시 미스가 늘기 때문 — 인스턴스화마다 코드가 늘어나는 것이 유일한 이유다",
    "제네릭은 리플렉션으로 구현되어 오히려 훨씬 느리기 때문 — 타입 파라미터마다 reflect 로 값을 풀어 다루므로 인터페이스보다 느리다" ] },
  { k: "go3-defer-close-err", o: [
    "Close 가 반환하는 에러를 버리게 되어 마지막 기록 실패(디스크 가득참 등)를 놓칠 수 있다",
    "defer 는 파일에는 동작하지 않아 파일이 닫히지 않는다 — os.File 의 Close 는 defer 큐에서 실행되지 않고 GC 가 대신 닫는다",
    "함수가 패닉으로 끝나면 defer 가 실행되지 않는다 — 패닉 경로에서는 파일이 열린 채 남아 디스크립터가 샌다",
    "Close 가 두 번 호출되어 패닉이 발생한다 — 쓰기용 파일은 Sync 가 내부적으로 Close 를 부르므로 defer 와 겹친다" ] },
  { k: "go-cc-unbuffered", o: [
    "값을 하나까지 버퍼링한다 — 크기를 적지 않으면 기본 버퍼가 1 이라 송신 한 번은 수신자 없이도 즉시 끝난다",
    "송신은 수신자가 준비될 때까지 블록된다 — 송·수신이 동시에 만나는 랑데부다",
    "송신은 항상 즉시 반환된다 — 값은 런타임 큐에 잠시 보관되고 수신자가 나타나면 그때 전달된다",
    "수신자가 없으면 보낸 값이 그대로 버려진다 — 언버퍼드 채널은 보관할 자리가 없으므로 받을 쪽이 없는 값은 사라진다" ] },
  { k: "go2-bm-02", o: [
    "-cpuprofile 플래그를 함께 주어야 할당 정보가 나온다 — 프로파일 파일을 pprof 로 열어야 B/op 와 allocs/op 를 볼 수 있다",
    "runtime.GC() 를 반복문 안에서 호출한다 — GC 를 강제로 돌리면 런타임이 그 사이의 할당량을 벤치마크 결과에 찍어 준다",
    "go test -bench=. -race 를 쓴다 — 경합 검출기가 메모리 접근을 추적하므로 할당 횟수도 함께 집계되어 출력된다",
    "go test -bench=. -benchmem 을 쓰거나 벤치마크 안에서 b.ReportAllocs() 를 호출한다" ] },
  { k: "go2-bm-04", o: [
    "for 루프 대신 range 를 써야 정확하게 측정되기 때문 — b.N 을 range 로 돌려야 런타임이 반복 횟수를 올바르게 센다",
    "벤치마크는 -O0 으로 컴파일되기 때문 — 최적화가 꺼진 코드라 실제 배포 바이너리보다 호출 비용이 낮게 나온다",
    "반환값을 쓰지 않아 컴파일러가 호출을 최적화로 제거하거나 인라이닝해 없애 버릴 수 있기 때문",
    "b.N 이 항상 1 이기 때문 — 한 번만 실행한 시간을 연산당 시간으로 보고하므로 캐시가 데워진 뒤의 값이 나온다" ] },
  { k: "go3-scan-pointer", o: [
    "값을 그대로 넘기면 리플렉션으로 알아서 채워 준다 — Scan 은 인자의 타입을 보고 변수 자리를 찾아 대입한다",
    "구조체 하나만 넘기면 태그를 보고 자동으로 채워 준다 — db:\"col\" 태그와 컬럼 이름을 맞춰 필드에 나눠 넣는다",
    "컬럼 이름 문자열을 함께 넘겨야 순서와 무관하게 매핑된다 — Scan(\"id\", &id, \"name\", &name) 처럼 짝을 지어 넘긴다",
    "값을 담을 변수의 포인터를 넘겨야 하며, 개수와 순서가 SELECT 컬럼과 일치해야 한다" ] },
  { k: "go-sl-range-copy", o: [
    "슬라이스는 애초에 수정할 수 없기 때문 — 원소를 바꾸려면 새 슬라이스를 만들어 다시 대입해야 한다",
    "range 가 읽기 전용 슬라이스를 새로 만들기 때문 — 순회 중 원본이 바뀌지 않도록 복사본 위에서 돈다",
    "range 의 v 는 원소의 복사본이라, 수정하려면 nums[i] 에 직접 대입해야 한다",
    "v 가 상수로 선언되기 때문 — range 가 만드는 두 번째 변수는 const 라 대입하면 컴파일 오류가 난다" ] },
  { k: "go-ctx-value", o: [
    "함수의 필수 매개변수를 대신 전달할 때 쓴다 — 인자가 많아지면 ctx 에 담아 시그니처를 짧게 유지한다",
    "요청 스코프의 부가 데이터(요청 ID, 인증 주체)를 전달할 때만 쓴다",
    "설정 값을 전역으로 공유할 때 쓴다 — context.Background() 에 넣어 두면 모든 고루틴이 같은 설정을 본다",
    "성능 최적화를 위해 캐시로 쓴다 — 한 요청 안에서 반복 조회되는 값을 ctx 에 담아 DB 왕복을 줄인다" ] },
  { k: "go-err-join", o: [
    "여러 에러를 하나의 에러로 묶으며, errors.Is 는 그중 어느 것과도 매칭된다",
    "에러를 nil 로 만든다 — 인자 가운데 nil 이 하나라도 있으면 전체를 nil 로 접어 '에러 없음' 으로 취급한다",
    "가장 마지막 에러만 남긴다 — 앞의 에러는 로그로 흘리고 최근 것 하나만 반환해 호출자가 다루기 쉽게 한다",
    "에러 메시지를 문자열로 이어 붙이기만 한다 — 원본 에러 값은 버려지므로 errors.Is 로는 더 이상 찾을 수 없다" ] },
  { k: "go2-al-06", o: [
    "[]T 는 GC 가 스캔할 포인터가 없고 원소가 연속 배치되어 캐시 지역성이 좋으며, 할당도 한 번으로 끝나기 때문",
    "[]*T 는 인덱스 접근이 O(n) 이기 때문 — 포인터를 따라가려면 앞 원소부터 차례로 거쳐야 해 배열의 상수 시간 접근이 사라진다",
    "[]*T 는 원소를 수정할 수 없기 때문 — 포인터가 가리키는 값은 읽기 전용으로 취급되어 필드를 바꾸려면 새 객체를 만들어야 한다",
    "[]T 는 원소가 자동으로 정렬 상태를 유지하기 때문 — 값 슬라이스는 append 할 때마다 정렬되어 이진 탐색이 바로 가능하다" ] },
  { k: "go3-iface-arg", o: [
    "파일뿐 아니라 HTTP 응답 본문, 문자열, 버퍼도 그대로 넘길 수 있어 테스트와 재사용이 쉬워진다",
    "파일 읽기 속도가 자동으로 빨라진다 — 인터페이스를 거치면 런타임이 버퍼링을 끼워 넣어 시스템 콜이 줄어든다",
    "동시에 여러 고루틴에서 호출해도 안전해진다 — io.Reader 는 내부에 락을 두어 동시 Read 를 직렬화하는 계약이 있다",
    "파일을 닫는 책임이 함수 쪽으로 자동 이전된다 — Reader 로 받은 쪽이 다 읽으면 런타임이 Close 를 대신 불러 준다" ] },
  /* 2차 — 27~47 */
  { k: "go2-mr-02", o: [
    "Go 는 모든 메서드를 포인터 리시버로 자동 변환해 컴파일하기 때문 — 값 리시버는 문법 편의일 뿐 실제로는 항상 주소를 넘긴다",
    "인터페이스를 거치면 리시버 종류가 무시되기 때문 — c 는 암묵적으로 인터페이스 값으로 바뀌어 메서드 집합 검사를 건너뛴다",
    "c 가 주소를 얻을 수 있는 대상(addressable)이라 컴파일러가 (&c).Add() 로 바꿔 주기 때문",
    "값 리시버와 포인터 리시버는 명세상 완전히 동일하기 때문 — 어느 쪽으로 선언하든 같은 메서드 집합에 속한다" ] },
  { k: "go-err-message", o: [
    "대문자로 시작하고 마침표로 끝낸다 — 로그에 한 줄로 찍힐 때 문장처럼 읽혀야 하므로 영어 문장 규칙을 그대로 따른다",
    "소문자로 시작하고 마침표를 붙이지 않는다 — 상위에서 감싸며 이어 붙기 때문",
    "'Error:' 접두사를 붙여서 쓴다 — 로그에서 일반 메시지와 구분되도록 모든 에러 문자열이 같은 머리말로 시작해야 한다",
    "항상 스택 트레이스를 포함한다 — 에러가 어디서 났는지 메시지만으로 알 수 있도록 생성 지점의 호출 스택을 함께 담는다" ] },
  { k: "go2-gc-04", o: [
    "불가능하다 — 메서드와 타입 요소는 함께 쓸 수 없고, 하나의 제약은 타입 집합이거나 메서드 집합 중 어느 한쪽이어야 한다",
    "가능하다 — interface{ ~string; String() string } 처럼 타입 집합과 메서드 요구를 함께 걸 수 있다",
    "가능하지만 별도 인터페이스로 나눠 임베딩해야만 한다 — 타입 유니온과 메서드를 한 인터페이스에 나란히 적는 문법은 없다",
    "가능하지만 메서드는 무시되고 타입 유니온만 검사된다 — 제약에 적은 메서드는 문서 역할만 하고 컴파일러가 확인하지 않는다" ] },
  { k: "go2-cp-06", o: [
    "고루틴은 비싸므로 1~2개로 제한한다 — 워커마다 스택과 스케줄러 항목이 붙으므로 많이 띄울수록 런타임 부담이 커진다",
    "CPU 바운드는 코어 수 근처, I/O 바운드는 대기 시간이 길수록 코어 수보다 크게 잡는다",
    "많을수록 빨라지므로 가능한 한 크게 잡는다 — 고루틴은 거의 공짜라 워커가 많을수록 큐가 비는 시간이 줄어 유리하다",
    "GOMAXPROCS 와 정확히 같게 맞춘다 — 그보다 많으면 스케줄러가 돌아가며 실행하느라 문맥 전환만 늘고 이득이 없다" ] },
  { k: "go2-pk-01", o: [
    "github.com/acme/shop 트리 안에 있는 패키지만 임포트할 수 있다 (internal 의 부모 디렉터리 하위)",
    "github.com/acme 조직의 모든 모듈이 자유롭게 임포트할 수 있다 — internal 은 조직 단위로 공개되고 외부 조직에만 막힌다",
    "누구나 임포트할 수 있고 internal 은 관례적 표시일 뿐이다 — 컴파일러는 디렉터리 이름을 보지 않으므로 린터가 경고만 한다",
    "같은 디렉터리에 있는 파일만 임포트할 수 있다 — internal 은 패키지 밖으로 아무것도 내보내지 않겠다는 선언이다" ] },
  { k: "go-ctx-cancel", o: [
    "타이머와 부모에 등록된 자식 항목이 남아 자원이 누수된다 — go vet 이 경고한다",
    "타임아웃 자체가 동작하지 않게 된다 — cancel 을 불러야 타이머가 시작되므로 부르지 않으면 데드라인이 영영 오지 않는다",
    "즉시 패닉이 발생한다 — 런타임이 cancel 이 호출되지 않은 컨텍스트를 감지해 함수가 반환되는 순간 프로그램을 멈춘다",
    "아무 문제도 없다 — 데드라인이 지나면 컨텍스트가 스스로 정리되므로 cancel 은 일찍 끝내고 싶을 때만 부르는 선택 사항이다" ] },
  { k: "go-err-customtype", o: [
    "에러 메시지 문자열에 담고 호출자가 파싱하게 한다 — 문자열 파싱이 가장 간단하고 타입을 새로 정의할 필요가 없다",
    "Error() string 메서드를 가진 커스텀 구조체 타입을 정의하고 errors.As 로 꺼내게 한다",
    "전역 변수에 마지막 에러 정보를 저장한다 — errno 처럼 호출 직후 전역을 읽으면 되므로 반환값을 바꾸지 않아도 된다",
    "패닉으로 전달한다 — 패닉 값에는 어떤 타입이든 담을 수 있으므로 구조체를 넣고 recover 에서 꺼내면 정보가 그대로 전해진다" ] },
  { k: "go-test-bench", o: [
    "측정이 안정될 때까지 프레임워크가 조정하는 반복 횟수 — 루프를 b.N 번 돌려야 한다",
    "나노초 단위 목표 실행 시간 — 벤치마크 본문이 이 시간 안에 끝나야 하고, 넘기면 결과가 실패로 표시된다",
    "벤치마크에 사용할 스레드 수 — b.N 개의 고루틴이 본문을 동시에 실행해 병렬 처리량을 잰다",
    "벤치마크를 실행할 CPU 코어 개수 — 코어 수만큼 나눠 돌리고 합산해 연산당 시간을 계산한다" ] },
  { k: "go2-ms-06", o: [
    "go vet 이 혼용을 에러로 막기 때문 — 같은 타입에 두 종류의 리시버가 있으면 정적 분석 단계에서 빌드가 실패한다",
    "값과 포인터의 메서드 집합이 달라져 어떤 형태를 인터페이스에 담을 수 있는지가 헷갈리고, 값 복사로 상태가 갈리기 쉽기 때문",
    "혼용하면 메서드 호출이 리플렉션으로 처리되어 느려지기 때문 — 리시버 종류를 실행 시점에 판별해야 해 직접 호출이 불가능해진다",
    "혼용하면 컴파일이 되지 않기 때문 — 한 타입의 메서드 집합은 하나의 리시버 종류로만 이루어져야 한다는 규칙이 명세에 있다" ] },
  { k: "go3-binarysearch-ret", o: [
    "인덱스와 '실제로 그 값이 있었는지' 를 나타내는 bool 을 함께 반환한다",
    "찾은 값과 error 를 반환한다 — 값이 없으면 ErrNotFound 가 돌아오므로 다른 표준 함수처럼 err != nil 로 검사한다",
    "값의 등장 횟수를 반환한다 — 정렬된 슬라이스에서는 같은 값이 붙어 있으므로 양 끝을 찾아 개수를 세어 돌려준다",
    "찾은 인덱스 하나만 반환하고 없으면 -1 을 돌려준다 — 다른 언어의 이진 탐색과 같은 규약이라 int 하나로 판단한다" ] },
  { k: "go3-flag-stop", o: [
    "-verbose 플래그가 정상 인식되고 flag.Args() 는 [data.csv] 가 된다 — 위치 인자와 플래그는 어느 순서로 와도 상관없다",
    "flag 패키지가 인자를 자동 정렬해 플래그를 먼저 처리한다 — GNU getopt 처럼 - 로 시작하는 인자를 앞으로 모아 파싱한다",
    "인자 순서가 잘못되었다는 에러와 함께 프로그램이 종료된다 — 플래그 뒤에 위치 인자가 와야 한다는 규칙을 어겼기 때문이다",
    "첫 비플래그 인자 data.csv 에서 파싱이 멈춰 -verbose 는 플래그로 인식되지 않고 남은 인자에 들어간다" ] },
  { k: "go-defer-loop", o: [
    "컴파일 에러가 발생한다 — defer 는 함수 본문의 최상위에만 둘 수 있고 반복문 안에서는 허용되지 않는다",
    "defer 가 즉시 실행되어 파일이 너무 일찍 닫힌다 — 루프 반복이 끝나는 순간 정리가 돌아 다음 반복에서 닫힌 파일을 읽게 된다",
    "함수가 끝날 때까지 파일이 닫히지 않아 파일 디스크립터가 고갈될 수 있다",
    "루프가 무한 반복된다 — defer 가 쌓일수록 반복 조건 평가가 미뤄져 종료 조건에 도달하지 못한다" ] },
  { k: "go-panic-when", o: [
    "네트워크 요청 타임아웃 — 응답이 오지 않으면 더 진행할 수 없으므로 패닉으로 호출 스택을 통째로 끊는 편이 낫다",
    "파일이 존재하지 않는 경우 — 필요한 파일이 없으면 프로그램이 할 일이 없으므로 흔한 실패라도 패닉으로 알린다",
    "사용자 입력 검증 실패 — 잘못된 입력은 호출자의 책임이므로 error 로 돌려주기보다 즉시 멈추는 편이 원인을 빨리 드러낸다",
    "프로그램이 계속 진행될 수 없는 프로그래머 오류(불변식 위반, 초기화 실패)" ] },
  { k: "go-test-fatal", o: [
    "둘은 동일하며 출력 형식만 다르다 — Fatalf 는 메시지 앞에 FATAL 을 붙여 눈에 띄게 할 뿐 실행 흐름은 같다",
    "Errorf 는 실패를 기록하고 계속 진행하지만, Fatalf 는 그 테스트 함수를 즉시 중단한다",
    "Fatalf 가 계속 진행하고 Errorf 가 중단한다 — 이름과 반대라서 헷갈리기 쉬운 대표적인 API 다",
    "Fatalf 는 프로세스 전체를 종료한다 — os.Exit(1) 을 부르므로 그 뒤의 다른 테스트 함수도 실행되지 않는다" ] },
  { k: "go2-gs-05", o: [
    "context 값으로 종료 이유를 클라이언트에 전달하기 위해 — 남은 연결에 503 과 함께 ctx 에 담긴 사유를 응답한다",
    "context 를 통해 새 요청을 계속 받을지 결정하기 위해 — ctx 가 살아 있는 동안은 리스너를 닫지 않고 요청을 받는다",
    "진행 중인 요청을 얼마나 기다려 줄지 상한을 정하고, 초과하면 기다림을 멈추고 반환하기 위해",
    "context 가 없으면 Shutdown 자체가 컴파일되지 않기 때문 — 시그니처가 ctx 를 요구하므로 형식상 넘길 뿐 값은 쓰이지 않는다" ] },
  { k: "go2-em-05", o: [
    "s.Name 을 실제로 참조할 때만 모호(ambiguous) 에러가 나고, 참조하지 않으면 컴파일된다",
    "구조체 선언 자체가 즉시 컴파일 에러가 된다 — 같은 깊이에 같은 이름이 두 번 승격되는 선언은 허용되지 않는다",
    "런타임에 panic 이 발생한다 — 컴파일러는 통과시키지만 s.Name 을 읽는 순간 어느 쪽인지 정할 수 없어 멈춘다",
    "선언 순서가 빠른 A 의 Name 이 자동으로 선택된다 — 임베딩 순서가 곧 우선순위라 앞에 적은 타입이 이긴다" ] },
  { k: "go2-ec-05", o: [
    "필요한 메서드가 그 타입의 메서드 집합에 모두 있는지 (선언 없이 구조적으로 판정)",
    "implements 키워드로 구현을 명시적으로 선언했는지로 정한다 — 선언이 없으면 메서드가 다 있어도 만족하지 않는다",
    "같은 패키지에 있는지 — 인터페이스와 구현 타입이 한 패키지에 있어야 컴파일러가 둘을 연결할 수 있다",
    "임베딩 관계로 연결되어 있는지 — 인터페이스를 구조체에 임베딩해야 그 타입이 인터페이스를 만족하게 된다" ] },
  { k: "go2-tm-06", o: [
    "Parse 는 나노초를 버리고 ParseInLocation 은 유지한다 — 정밀도가 필요한 로그 시각은 후자로 읽어야 한다",
    "Parse 는 UTC 만, ParseInLocation 은 RFC3339 만 처리한다 — 처리할 수 있는 레이아웃 범위가 서로 다르다",
    "레이아웃에 타임존 정보가 없을 때 Parse 는 UTC 로 해석하고, ParseInLocation 은 지정한 Location 으로 해석한다",
    "Parse 는 에러를 반환하지 않고 ParseInLocation 은 반환한다 — 잘못된 문자열이면 Parse 는 제로 시각을 돌려준다" ] },
  { k: "go-cc-wg-done", o: [
    "Done 은 defer 로만 호출할 수 있기 때문 — 문법적으로 강제되어 있어 일반 문장으로 부르면 vet 이 경고한다",
    "성능이 더 좋기 때문 — defer 로 등록하면 런타임이 여러 Done 을 모아 한 번의 원자 연산으로 처리한다",
    "Add 와 순서를 맞추기 위해 — Add 가 함수 밖에서 불리므로 Done 도 함수가 끝나는 시점에 맞춰 대칭으로 둔다",
    "패닉이나 이른 return 이 있어도 반드시 카운터가 줄어 Wait 이 영원히 멈추는 것을 막는다" ] },
  { k: "go-panic-nilmap", o: [
    "컴파일 에러가 발생한다 — nil 맵에 값을 대입하는 문장은 정적으로 잡히므로 빌드 자체가 되지 않는다",
    "읽기 단계에서 이미 패닉이 발생한다 — nil 맵은 읽기도 막혀 있어 첫 줄의 조회에서 프로그램이 멈춘다",
    "0 이 출력된 뒤 nil 맵 쓰기로 패닉이 발생한다 — 읽기는 되지만 쓰기는 안 된다",
    "둘 다 정상 동작한다 — nil 맵에 쓰면 런타임이 빈 맵을 만들어 대신 넣어 주므로 make 없이도 쓸 수 있다" ] },
  { k: "go-err-is", o: [
    "err 의 타입이 ErrNotFound 의 타입과 같은지 확인한다 — 값은 보지 않고 동적 타입만 비교하므로 같은 타입의 다른 에러도 참이다",
    "err 이 nil 인지 확인한다 — 두 번째 인자는 nil 일 때 대신 돌려줄 기본 에러라 err == nil 검사의 축약형이다",
    "Unwrap 체인을 따라가며 어느 단계에서든 ErrNotFound 와 같은지 확인한다",
    "err 의 문자열에 'not found' 가 포함되는지 확인한다 — 메시지 부분 일치로 판단하므로 감싼 에러에서도 잘 동작한다" ] },
] };
