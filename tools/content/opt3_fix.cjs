/* 72차 마무리 — 새로 쓴 오답이 만든 '…그대로 둔다' 쏠림(31개 중 정답 4)과 5자 차이 7문항, 되풀이 1개를 고친다 */
module.exports = [
{ track: "backend", fixes: [
  { k: "cache-aside", from: "TTL 이 만료될 때까지 그대로 둔다", to: "캐시는 건드리지 않고 TTL 이 만료될 때까지 기다린다" },
  { k: "캐시 스탬피드", from: "캐시 설정은 그대로 둔다", to: "커넥션 누수이며, 커넥션 풀을 무제한으로 늘리면 해결된다. 대기가 사라지면 몰린 요청도 처리된다 대기 큐가 사라지면 DB 도 정상 속도로 돌아오므로 캐시 설정은 손대지 않는다" }
] },
{ track: "devops", fixes: [
  { k: "cloud2-k8s-oomkilled", from: "메모리 설정은 그대로 둔다", to: "재시작 정책을 Never 로 바꿔 반복 재시작을 막는다. 죽은 파드는 그대로 두고 새로 배포하면 되므로 메모리 설정은 손대지 않는다" }
] },
{ track: "go", fixes: [
  { k: "go-err-customtype", from: "반환값은 그대로 둔다", to: "전역 변수에 마지막 에러 정보를 저장한다 — errno 처럼 호출 직후 전역을 읽으면 되므로 반환값은 손대지 않는다" }
] },
{ track: "linux", fixes: [
  { k: "시간대", from: "시간대는 기본값 그대로 둔다", to: "시간대는 기본값을 유지한다" }
] },
{ track: "mleval", fixes: [
  { k: "층화 목적", from: "클래스 비율은 그대로 둔다", to: "데이터를 무작위로 섞기만 하고 클래스 비율은 무시한다" }
] },
{ track: "mobile", fixes: [
  { k: "두 번 도착하면", from: "서버는 그대로 둔다", to: "알림 도착 시각을 비교해 짧은 간격의 중복을 걸러 낸다. 도착 간격이 짧으면 같은 알림으로 보고 뒤에 온 것을 버린다. 서버는 손대지 않는다" }
] },
{ track: "python", fixes: [
  { k: "response-model-leak", from: "출력 스키마는 그대로 둔다", to: "응답 미들웨어에서 문자열 치환으로 민감 필드를 지워 버린다. 출력 스키마는 따로 두지 않는다" }
] },
{ track: "arch", fixes: [
  { k: "여기서 멈춰라", from: "오히려 빨라진다. 멈춤은 없다", to: "그 아래 코드가 캐시에 미리 올라가도록 만든다. 그 지점부터는 필요한 코드가 미리 캐시에 올라와 오히려 빨라지고 멈춤도 사라진다" }
] },
{ track: "algo", fixes: [
  { k: "문자열연결", from: "전체 성능은 선형으로 유지된다", to: "O(n) — 문자열이 제자리에서 그대로 늘어나기 때문에 몇 번을 이어 붙이더라도 전체 성능은 선형으로 그대로 유지된다" }
] },
{ track: "compiler", fixes: [
  { k: "몇 개로 줄었나", from: "상수 전파는 명령을 줄이지 않아서다", to: "7개 — 상수 전파는 명령의 수를 줄이지 않아서다" }
] },
{ track: "java", fixes: [
  { k: "java-st-parallel-state", from: "스레드 안전성은 순서 문제일 뿐이다", to: "순서만 뒤바뀔 뿐 결과 자체는 정확하므로 ArrayList 를 그대로 써도 되고, 스레드 안전성 문제는 순서가 섞이는 것으로 끝난다" },
  { k: "java3-hc-missing", from: "put 줄에서 컴파일이 실패한다", to: "hashCode 없이 equals 만 재정의한 클래스는 키로 쓸 수 없어 put 줄에서 컴파일이 실패하고 만다" }
] },
{ track: "math", fixes: [
  { k: "주기와 나머지", from: "손으로 세어 보는 것이 유일하다", to: "100 은 7 로 나누어떨어지지 않으므로 요일을 하루씩 손으로 세어 보는 것이 유일한 길이다" }
] },
{ track: "os", fixes: [
  { k: "osfs-safe-write", from: "fsync 는 rename 이 대신한다", to: "임시 파일에 쓰기 → rename → fsync 는 rename 이 알아서 대신한다" }
] },
{ track: "php", fixes: [
  { k: "PHPDoc", from: "타입 선언을 생략할 수 있게 해 주어서", to: "이 주석은 array 타입 힌트를 완전히 대체해 타입 선언을 통째로 생략할 수 있게 해 주어서" }
] }
];
