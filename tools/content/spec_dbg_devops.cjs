module.exports = {
  track: "devops", guide: "", xp: 80,
  unit: "실행형 실전 — 배포 계산이 조용히 어긋난 코드 고치기",
  source: "./dbg_devops.cjs",
  lessons: [
    { t: "비교와 파싱이 어긋난 곳", n: 6,
      th: { sum: "운영 자동화의 버그는 대개 **조용히** 실패한다 — 잡이 안 돌고, 순서가 뒤집히고, 값이 사라지는데 오류는 없다.",
        body: [
          { h: "사전순과 숫자순이 갈리는 지점", t: "문자열 비교는 한 글자씩 견주므로 `'1.10.0' < '1.9.0'` 이고 `'a.log.10' < 'a.log.2'` 다. 아홉을 넘기 전까지는 정확히 맞아서, **10 이 나오는 날** 처음 드러난다. 이름에 번호를 넣을 때 자릿수를 채우거나(`010`) 숫자로 파싱해 견주는 것이 답이다." },
          { h: "아무 일도 안 일어나는 실패", t: "`Number('*/15')` 는 `NaN` 이라 어떤 값과도 같지 않다 — 잡이 **오류도 로그도 없이** 한 번도 안 돈다. `if (over[k])` 는 빈 문자열을 걸러 '비우기' 라는 정당한 설정을 뭉갠다. 콜론 하나로 이미지 고정을 판정하면 레지스트리 포트에 걸려 거의 모든 참조가 통과한다." }],
        code: { c: "if (over[k] === null) delete out[k];\nelse out[k] = over[k];        // '' 도 정당한 값", cap: "'없음' 과 '빈 값' 은 다르다" },
        key: ["버전·파일번호는 숫자로 견준다", "조용한 실패에는 '마지막 실행' 지표를", "every 는 빈 배열에서 참", "형식 판정은 위치를 특정한다"] } },
    { t: "비율과 반올림이 어긋난 곳", n: 6,
      th: { sum: "지표와 용량 계산은 **분모·올림 방향·표본 수** 셋에서 갈린다 — 셋 다 값이 그럴듯해 보여서 늦게 발견된다.",
        body: [
          { h: "분모가 무엇인가", t: "오류율의 분모는 **전체 요청**이지 성공 수가 아니다. 남은 오류 예산의 분모는 **예산**이지 전체 요청이 아니다 — 99.9% 가용률은 안심되는 숫자지만 '예산 90% 소진' 은 행동을 부른다. 같은 사실을 다르게 표현한 두 숫자가 전혀 다른 결정을 만든다." },
          { h: "쪼갤 수 없으면 올림", t: "필요한 레플리카 수는 언제나 **올림**이다. `Math.round` 는 내려갈 수 있어 목표 사용률을 계속 넘긴다. 그리고 비율로 판단하는 자동화에는 **최소 표본 수**가 있어야 한다 — 요청 두 건의 오류율 0% 는 아무것도 말해 주지 않는다." }],
        code: { c: "return Math.max(1, Math.ceil(current * curUtil / targetUtil));\nif (canary.total < minN) return false;", cap: "올림 + 최소 표본" },
        key: ["오류율 분모는 전체 요청", "예산은 예산 대비로 읽는다", "자원 수는 올림", "판단할 수 없으면 통과가 아니라 보류"] } },
  ],
};
